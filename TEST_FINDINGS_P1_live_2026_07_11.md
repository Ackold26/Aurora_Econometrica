# Живой GUI-прогон P-1 — находки (2026-07-11, мост :9223)

Ветка `feat/econ-planning-mode` @HEAD (после аудит-2 + прокси-гейт). Собранный sidecar
(freshness verified). Фикстура: демо-проект «демо-медиаплан-с-хвостом» (модель+posterior),
media_plan.json создан программно (validate+confirm), validation восстановлен (роли ТВ/ОЛВ).

## ✅ Подтверждено вживую (P-1 работает)
- Baseline авто-строится при входе на Планирование ($effect + econ_scenario, carry_in).
- Карточка итога: Выручка 141 316 056 ₽ за 6 пер., интервал [138 207 490 – 144 407 835].
- **График «Факт → Базовый план»** рисуется (canvas): оранжевая история 2024→2025 (17–27M)
  + синий прогноз вперёд (~23M) + пунктир «Дата отсечки» + endpoint-метка 23M + CI-веер.
- Кнопка «+ Создать вариант» — синяя btn-primary (заметная).
- planning.json автозаписан: {variant_ids:["Базовый план"], accepted:"Базовый план"}.
- A-2: карточка «Бюджет плана ₽» скрыта (total_spend_money=None для этих каналов) — не врёт.
- HTTP endpoint econ_save_planning в собранном sidecar → 200 ok.

## 🔴/🟠/🟡 Находки

### 🟠 F1 — даты прогноза на оси X сломаны («< -31T00:00:00»)
Экран Планирования, график. История «2024-01-31» отображается нормально, а прогнозная часть
оси — «< -31T00:00:00» (сырой ISO с временем). Причина: future_dates из media_plan.json идут
как `2026-01-31T00:00:00` (pandas isoformat с временем), а historical.dates из decompose —
`2024-01-31` (без времени). Ось получает смешанные форматы → прогнозные метки кривые.
Repro: Планирование → график, смотреть правую (прогнозную) часть оси X.
Fix (кандидат): нормализовать даты (обрезать `T..` → YYYY-MM-DD) в ContinuationChart для оси.

### 🟠 P-2 — раздел «Прогноз на будущий период» НЕ попадает в PPTX (НЕ докопано, → след. сессия)
Отчёт → «Создать отчёт (PPTX)» → 13 слайдов, разделы 01 ГЛАВНОЕ · 02 ДЕКОМПОЗИЦИЯ ·
03 МЕТОДОЛОГИЯ · 04 ДАННЫЕ · 05 ПРИЛОЖЕНИЕ. Раздела ПРОГНОЗ/ПЛАНИРОВАНИЕ НЕТ.
Зонды (что ИСКЛЮЧЕНО): load_saved_forecast(демо)=ok, 1 scenario, 6 preds (данные есть);
timing ок (planning.json 14:35 + scenario 14:33 существовали ДО PPTX 14:36);
is_live=True (bool(data.channels), каналы есть); narrative_adapter:1119 форвардит
data['forecast'] при status ok+scenarios; builder условие `if self.forecast:` (3975),
self.forecast = _fc if is_live AND status==ok AND scenarios (builder:308). Все условия
формально истинны — но раздел не рендерится. НЕ докопано: почему self.forecast пуст при
GUI-генерации, хотя все входы ok. СЛЕД. ШАГ: прогнать генерацию PPTX через backend
напрямую (HTTP /report на sidecar :7529 ИЛИ python narrative+builder) на демо-проекте,
проверить self.forecast значение — изолировать разрыв (какой report-endpoint реально
вызывается из GUI и передаёт ли forecast; возможно GUI зовёт НЕ server.py:2086-путь).

### 🟡 F2 — ось X перегружена + наложение «Дата отсечки» на метки
Вертикальный лейбл cutoff «Дата отсечки» накладывается на метки дат. Читаемость средняя. Минор.

### ✅ Fixed — historicalSeries был всегда null (график не рисовался никогда)
PlanningStep.historicalSeries читал `dData.dates`/`dData.kpi_actuals` — таких полей в decompose
НЕТ (структура: `decomposition_series.{dates,series}` + `time_series.dates`). → график история→
прогноз не рендерился НИКОГДА (предсуществующий баг, критичен для P-1). Исправлено: historical.
actuals = сумма вкладов decomposition_series (data уже signed) = модельная реконструкция истории.
