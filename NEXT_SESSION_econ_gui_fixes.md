# Промт след. сессии — Econometrica: разбор итогов живой приёмки + фикс всех находок + ретест (2026-07-05)

> Скопируй в начало новой сессии. cwd = `D:\Docs\Aurora_Ai\Dev\Aurora_Econometrica`,
> ветка `feat/econ-e1-backtest`. Мандат: кодинг — субагентами (lower-power), приёмка/дизайн/
> мутации — main-модель ([[feedback_delegate_coding_to_lower_model_subagent]]). Метод корпус-
> оракула для детекторов ([[feedback_growing_input_corpus_oracle_over_code_reading]]).
> ⚠️ ПЕРВЫМ ДЕЛОМ: `git log --oneline -6` + `git status -sb` + ahead/behind (параллельная сессия).

## 🎯 Что было (2026-07-05, Opus 4.8, живая приёмка Блок A у экрана)
Прошли ВЕСЬ pipeline Econometrica на `synth_fmcg_brand` (Импорт→Валидация→Модель→
Декомпозиция→Оптимизация→Отчёт). Антон кликал, я инспектировала через MCP-мост
(screenshot + DOM) + зонды. **22 находки F-A1-1..22 + наблюдения — ВСЕ с корнем в коде и
фиксом в durable `TEST_FINDINGS_GUI_2026_07_05.md`** (читать ПЕРВЫМ — там симптом, корень,
файл:строка, предложенный фикс по каждой). Мета-класс большинства: **UI-слои держат
собственные вычисления/лейблы/детекторы мимо SSOT.**

## 🔧 Запуск для ретеста (dev + мост)
`npm run tauri:dev` (НЕ `tauri dev` — нужен `tauri.dev.conf.json`, мост 127.0.0.1:9223).
Инспекция: `mcp__tauri__webview_screenshot` РАБОТАЕТ; `webview_interact` клик мажет (0,0) —
навигацию ведёт Антон вживую; `webview_execute_js` timeout-ит (runtime-инспекция недоступна).
Sample-файлы Антону скопированы: `C:\Users\ackol\Desktop\Тестовые_файлы_оптимизатора\`.
Зонды приёмки в `tmp/`: `r1_corpus_probe.py`, `probe_pairs.mjs`, `probe_holidays.py`.

## 📋 РЕЕСТР НАХОДОК ПО ПРИОРИТЕТУ (детали → TEST_FINDINGS_GUI_2026_07_05.md)

### 🔴🔴🔴 P0 — критичные (ядро продукта / доверие)
- **F-A1-21+22+18 — режим «Планирование» НЕ перераспределяет бюджет.** Дефолтный+главный
  режим (клиент планирует будущее) делает proportional split (−75% всем, `server.py:1434`)
  вместо SLSQP → +0.0% прирост, врущий текст «перекладка из перенасыщенных в недонасыщенные»
  (F-A1-22, все −75%, никакой перекладки), Прогноз KPI обвал 8057→5076 (F-A1-18). **Анализ
  РАБОТАЕТ (доказано: performance +99%, +1.7%, текст правдив).** Фикс: планирование →
  SLSQP-перераспределение на плановом бюджете, как Анализ. Текст сдвигов — из фактических
  per-канал дельт (знак + / −). Файлы: `server.py:1201 optimize_budget`, `engines/optimizer.py`.
- **F-A1-14 — drill «Динамика по периодам» не раскрывает группы.** Клик работает (chips
  active), данные полные (backend кладёт члены), legend-toggle жив — но echarts не
  перестраивается при expanded. Звено `ChannelTimeline.svelte:566 option`→EChartBase
  (setOption notMerge / universalTransition морф). Блокирует A2-морф + A3-визуал праздников.
  Нужен HMR+console (execute_js timeout был). Плюс F-A1-13 (fullscreen ½ высоты, echarts resize).
- **F-A1-2+3 — реко-слой не наследует SSOT-kind → вредная рекомендация.** `category_sales`
  (kind=category, control) получает бейдж «Альтернативная цель — исключите» → потеря
  экзогенного контроля спроса (OVB). Корень `ColumnMapperConfirm.svelte:502` (regex-имя,
  не col.kind) + `:199 isPhysicalMetric` без `contacts`. F-A1-3: SSOT `column_detection`/
  `channel-pairs` не ловят прилагательные `рыночн*`/`конкурентн*` (RU+EN), «рыночные продажи»→
  ложно KPI. Фикс: реко читает col.kind (альт.цель только target_*); +прилагательные в
  паттерны +канарейка. Метод корпус-оракула обязателен.

### 🔴 P1 — SSOT/детекторы (UI врёт о факте)
- **F-A1-1 — физ-пары бейдж** (`ooh_contacts` не физика в `isPhysicalMetric`) — фит ВЕРЕН
  (4 канала, resolvePairSelection знает contacts), но реко-таблица врёт «Оставить» +
  счётчик «3 пары» вместо 4. Косметика доверия. Тот же корень, что F-A1-2.
- **F-A1-19 — светофор/miROAS-вердикты не по определению** (digital 1.27×/ooh 1.08× → 🔴
  «Сократить», хотя 0.8-1.5×=🟡; performance 3.86× недонасыщен, но светофор 4🔴). Нестабильны
  между состояниями. Фикс: вердикты строго из mROAS-порогов, SSOT.

### 🟠 P2 — метрики/подача (не-SSOT + смешение)
- **F-A1-5 — три ratio** (вход 4.4 / pre-train 6.9 / post-train 3.0). Канон = эффективный
  3.0 (учитывает праздники+сезонность). Pre-train завышает (не учёл авто-инжект). SSOT ratio.
- **F-A1-6 — единица периода «нед» хардкод** (`ModeDerivedExplanation.svelte:360`): 48 мес =
  «48 нед < 52 нед» ложная тревога. Из гранулярности (ruPeriodForm существует).
- **F-A1-7 — оценка времени обучения** факт 10 сек, «~5мин»/«30-60сек» завышены → «10-30 сек»,
  единый источник.
- **F-A1-9 — конфликт вердиктов надёжности** (ratio «идеально/высокая надёжность» vs prior
  «ненадёжно» на одном экране). Убрать «надёжность» из ratio-classifier (ratio=объём); единый
  предобучающий вердикт.
- **F-A1-12 — дисклеймер ненадёжности не доезжает** до Декомпозиции/Оптимизации (в Отчёте
  частично есть). Плашка при low prior-coverage везде, где вердикты/деньги.
- **F-A1-16 — sticky настройки существующего проекта** (manual budget 800.7М, per-channel
  overrides) неотличимы от предложенных. Пометка «ваше значение / предложено» + сброс шага.
- **F-A1-17 — рассинхрон бюджета Блок B** (226.5М планирование vs 906М распределение/слайдеры).
- **N-5** R² тремя числами (0.931/0.935/93%) — SSOT.

### 🟠 P3 — клиентский текст (client-surface hygiene)
- **F-A1-8 — adstock привязан к носителям** (TV/OLV/digital) в 5 файлах — убрать носители,
  описывать природу отклика. **ФОРМУЛИРОВКА УТВЕРЖДЕНА Антоном** (в TEST_FINDINGS). Карта
  файлов: ConfigPanel:187/593, insights-rules:1035-1036, IntroTutorial:25, glossary.js.
  Смежно: «авто по BIC» ≠ текст «по имени канала».
- **F-A1-15 — англицизмы/жаргон**: «see methodology», «per-period Hill summation / 3-way
  alignment с scenario engine», «autocorr», «Блок D». Человеческий язык. Грепнуть весь pipeline.

### 🟢 P4 — улучшения/фичи (демо-качество + прозрачность)
- **F-A1-10+20 — демо-примеры как ОБРАЗЕЦ** (гейт демо-качества, ВСЕ категории fmcg/pharma/
  realestate/ecom): (1) проходят prior-check (fmcg=44% FAIL сейчас); (2) дают оптимизационный
  lift (fmcg=+0.0%). GROUND_TRUTH `synthetic_pilot_data.py` калибровать под субоптимальный
  старт + prior-диапазон. Постоянный регресс-тест `test_sample_files_prior_check.py`.
- **F-A1-11 — управляемый выбор праздников + прозрачность ratio** (двухчастная: ДО обучения
  выбор с Δratio+data-signal; ПОСЛЕ — инсайт «праздники снизили ratio 6.9→3.0, выключите
  пустые»). ⚠️ ПРЕДОХРАНИТЕЛЬ: выбор по ПАРЕ (влияние+Δratio), НЕ только ratio (иначе OVB).
  Инфра готова (`disabled_holidays`/`use_holidays`, modeler.py:305, ConfigPanel:473).
- **F-A1-4 — адаптивная подсказка «Метрики каналов»** (всё в ₽ → «переводить нечего»; физ →
  «укажите CPP»). validateMetricsInsights усилить.
- N-1 (две «Далее» на Импорте), N-3 (validateKpiInsights «физ конвертируются» при парах),
  Блок D заголовок ≠ функции.

## 🛠 ПЛАН ФИКСА (пакеты субагентам по мандату, каждый: субагент → моя приёмка+мутации+гейты)
1. **Пакет SSOT-детекторы** (F-A1-1/2/3/19): ColumnMapperConfirm читает col.kind (реко,
   isPhysicalMetric→SSOT parseChannelMetric); альт.цель только target_*; прилагательные в
   column_detection+channel-pairs; вердикты по mROAS-порогам. Корпус-оракул + мутации.
2. **Пакет Оптимизатор-планирование** (F-A1-21/22/18/17): планирование→SLSQP; текст сдвигов
   из дельт; KPI-метрика/горизонт консистентны. ⚠️ самый сложный, backend — возможно НЕ
   делегировать полностью (INV-50-критично). Ретест обоими режимами.
3. **Пакет Декомпозиция-drill** (F-A1-14/13): echarts option→EChartBase раскрытие +
   fullscreen resize. Нужен HMR+console.
4. **Пакет Метрики-SSOT** (F-A1-5/6/7/9/12/N-5): ratio канон 3.0, период-единица из
   гранулярности, время 10-30с, «надёжность» из ratio убрать, дисклеймер везде, R² один.
5. **Пакет Клиентский-текст** (F-A1-8/15): носители adstock убрать (формулировка утверждена),
   англицизмы/жаргон. vitest на insights-тексты.
6. **Пакет Демо-качество** (F-A1-10/20): prior-check+lift всех примеров, GROUND_TRUTH, регресс-тест.
7. **UX-мелочи** (F-A1-16, N-1/N-3, D-заголовок).
8. **Фичи** (F-A1-11 праздники, F-A1-4 подсказка) — по решению Антона (объём).

## ✅ РЕТЕСТ (после фиксов)
Повторная живая приёмка тем же путём (Антон у экрана): pipeline на synth_fmcg + доказать
закрытие КАЖДОЙ находки. Обязательно: (а) Оптимизация в режиме ПЛАНИРОВАНИЕ даёт реальное
перераспределение + правдивый текст (F-A1-21/22); (б) drill раскрывает группы + праздники
видны (F-A1-14 + A3-визуал); (в) category_sales не «исключите» (F-A1-2); (г) демо даёт lift
(F-A1-20). + непокрытое этой сессией: **A4** (бейдж пар на карте корреляций — Валидация heatmap),
**A5** (клиентская колонка события без префикса — спец-файл), **A6** (суммарная колонка →
авто-снятие роли — спец-файл), **экспорт-артефакт** (PPTX/HTML на F-A1-22 текст-ложь).

## 📌 Гейты входа (доказано, ветка синхронна до приёмки)
python nightly 2272 · vitest 925 · svelte-check 0. Приёмка НЕ меняла код (только durable +
tmp-зонды). Фронт-изменение ⇒ ОБЯЗАТЕЛЬНО vitest (svelte-check контракты не ловит). JS+JSDoc.

## ▶️ С чего начать
1. Recon 60с. 2. Прочитать `TEST_FINDINGS_GUI_2026_07_05.md` целиком (22 находки + корни).
3. С Антоном согласовать порядок пакетов (рекомендую P0 первым: Оптимизатор-планирование +
   drill + SSOT-детекторы — они бьют по ядру/доверию). 4. Пакет за пакетом: субагент → приёмка →
   гейты → коммит узким pathspec → fetch→push. 5. После фиксов — ретест живьём.
