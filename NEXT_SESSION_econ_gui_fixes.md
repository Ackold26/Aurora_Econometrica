# Промт след. сессии — Econometrica: ЖИВОЙ РЕТЕСТ фиксов приёмки (обновлено 2026-07-06)

> cwd = `D:\Docs\Aurora_Ai\Dev\Aurora_Econometrica`, ветка `feat/econ-e1-backtest`.
> ⚠️ ПЕРВЫМ ДЕЛОМ: `git log --oneline -10` + `git status -sb` + fetch/ahead-behind (параллельные сессии).
> Мандат: кодинг — субагентами, main = оркестратор+приёмка ([[feedback_delegate_coding_to_lower_model_subagent]]).

## ✅ СДЕЛАНО 2026-07-06 (сессия фиксов, 9 коммитов dc893de→971d167)

Все 22 находки приёмки F-A1-1..22 + 2 новых закрыты КЛАССАМИ по карте дрейфа
`DRIFT_MAP_2026_07_06.md` (метод: 5 линз-разведчиков → корни → пакеты; мета-совет
«грепни класс до фиксов» исполнен — близнецы найдены и закрыты разом):

| Коммит | Пакет | Суть |
|---|---|---|
| `20d87f4` | Оптимизатор-планирование (F-A1-21/22/18 корень) | SLSQP не двигался: numeric-якобиан на money-оси 1e8 = машинный ноль → нормализация y=x/budget только в planning. Зонд: было −0.00% всем/nit=1 → стало tv −60/perf +106/lift +2.3%, 8/8 стартов сходятся. Analyst байт-в-байт цел |
| `ee62940` | SSOT-детекторы (F-A1-1/2/3) | ColumnMapperConfirm читает parseChannelMetric + col.kind (4 локальных детектора удалены); прилагательные рыночн*/конкурентн* в оба SSOT; контакт[а-яё]* в PHYSICAL_SUFFIX_RE; validator импортирует списки column_detection. Мутационная проверка пройдена |
| `c715f3a` | Drill+fullscreen (F-A1-14/13) | Корень: side-write seriesTopGroup из $derived.by рвал граф Svelte 5 → чистый derived + $effect (legacy-ветка тоже). Fullscreen: flex-цепочка до EChartBase |
| `bd5936b` | Демо-качество (F-A1-10/20) | 44% FAIL = дефект ЧЕКЕРА (симулировал без сезонности/праздников, std 0.103 vs 1.0) → честная симуляция с приорами modeler: coverage 0.875-0.938 все 4 категории. Субоптимальный сплит → теор. lift ≥22%. Регресс-гейт 28 тестов, порог продуктовый 0.50 |
| `3d86308` | Метрики-SSOT (F-A1-5/6/7/9/12/17/18/22) | n_params_effective_pretrain из backend (конец «трёх ratio»); период из гранулярности (period-format.js); время «10-30 сек» (training-estimate.js); honesty_verdict при обучении (bayesian+OLS) → плашки DecomposeStep/OptimizeStep-idle/HTML-экспорт; текст сдвигов из знаков дельт; подпись KPI из planningLabel |
| `0f2a2e3` | NEW-1 adstock | 'auto' резолвится BIC-селектором ПРИ обучении (до JAX-гейта); конец молчаливого статического geometric 0.5 |
| `f1ed7c5` | Клиентский текст+UX (F-A1-8/15/16, N-1/N-3) + DRIFT_MAP | Носители из adstock-описаний (формулировки Антона), англицизмы/Блок B-E/Forward solver → русские; N-3 ветка пар; sticky-пометки «ваше значение/изменено вручную»; PPTX «недельной» из гранулярности; em-dash → «–» |
| `971d167` | R3-долг | svelte-check 41→0 (JSDoc-типизация юнитов без смены смысла) |

**Гейты финальные (прогон оркестратором, не со слов агентов):** svelte-check **0 ERRORS** ·
vitest **988** · pytest sidecar **398** · канарейка+демо-гейт **214**.

## 📦 ИНСТАЛЛЯТОР v2.2.0 СОБРАН (2026-07-06, `c27d64a`)
`Optimizer MMM_2.2.0_x64-setup.exe` (245 MB, sha256 `44ab3880…`) в
`D:/cargo-targets/ai-agency/release/bundle/nsis/`. Bump 2.1.0→2.2.0 minor, облачная
редакция, БЕЗ публикации клиентам. Sidecar пересобран (7 py-фиксов внутри). Для ретеста —
ставить ЭТОТ .exe (боевой артефакт) ИЛИ `npm run tauri:dev` (dev+мост). Публикация клиентам —
после успешного ретеста, через aurora-release-update (⚠️ V52: productName «Optimizer MMM» ↔
канал апдейтера `aurora-econometrica-gui`; обе строки app_versions; локальная M1 отдельно).

## 📖 Файлы для контекста (порядок чтения)
1. `DRIFT_MAP_2026_07_06.md` — карта класса «UI мимо SSOT» (5 линз + личная верификация), базис всех фиксов.
2. `TEST_FINDINGS_GUI_2026_07_05.md` — 22 находки приёмки с корнями/файлами (полная фактура F-A1-*, вкл. Пакет 8 F-A1-11/F-A1-4).
3. Память: `INDEX_econometrica.md` (состояние) + feedback'и сессии: [[feedback_agent_report_requires_raw_outputs]], [[feedback_agent_gate_from_requirement_not_achieved]], [[feedback_generated_timestamp_phantom_git_drift]].
4. Ключевой код фиксов (если ретест вскроет регресс): `engines/optimizer.py` (planning-нормализация), `ChannelTimeline.svelte` (drill), `reliability_a4.py` (prior-чекер), `insights-rules.js`/`ratio-classifier.js` (метрики).

## 🎯 ГЛАВНОЕ СЛЕД. СЕССИИ — ЖИВОЙ РЕТЕСТ (Антон у экрана)

**🔴 На СВЕЖЕМ проекте + свежее обучение (НЕ MMX 0507-26 — sticky-настройки исказят,
потеряли ~2 круга в прошлый раз).** Запуск: боевой .exe (v2.2.0, выше) ИЛИ `npm run tauri:dev`
(мост 127.0.0.1:9223, скриншоты работают, клики мажут — ведёт Антон). Sample:
`C:\Users\ackol\Desktop\Тестовые_файлы_оптимизатора\` (файлы ПЕРЕСОБРАНЫ — скопировать
свежие из static/sample-data!).

### ⚡ МЕТОД РЕТЕСТА (включить рефлекс ДО первого клика — окупилось дважды в прошлой сессии)
**При любой странности — разделяющий зонд первым ходом, до починки и до чтения кода.**
1. Найдено «сломано»? Прогони СОСЕДНИЙ рабочий путь на тех же данных. Разница «работает vs нет»
   локализует проблему — часто она вообще НЕ в коде, а в состоянии/окружении.
   - Прецедент 1: F-A1-21 раскрылся не чтением SLSQP, а прогоном **Анализа** на том же pkl
     (работает: perf +99%) → мгновенно ясно «жив движок, ломается только planning-ветка».
   - Прецедент 2: sticky per-channel overrides стоили **~2 круга** — `+0.0%` приняли за «плато
     оптимизатора» (дефект кода), а был артефакт состояния прошлого проекта. Чистый разделяющий
     прогон отделил бы артефакт от дефекта за минуту.
2. **Зонды уже написаны** — `tmp/probe_pairs.mjs`, `tmp/probe_holidays.py`,
   `tmp/probe_planning_optimizer.py`: доказывают/опровергают находку на backend-уровне БЫСТРЕЕ
   клика и без шума GUI. При странности — сначала зонд, потом глаза.
3. Перед докладом «регресс!» — спроси: **дефект кода или артефакт данных/состояния?** (stale
   sidecar, старый pkl, кэш webview, унаследованные настройки). На свежем проекте артефактов
   меньше, но не ноль — разделяющий прогон = дешёвая страховка от ложного круга.
4. **Порядок ретеста — ОТ КОРНЯ К ЗАВИСИМЫМ, не по списку находок.** F-A1-19 («4🔴 перенасыщены»)
   был КАСКАДОМ F-A1-21, а не своим багом. Проверяй планирование (корень) → потом светофор/вердикты
   (зависимое). От симптомов — будешь гоняться за призраками, что корневой фикс уже закрыл.
> Одной фразой: ты не тестируешь 22 находки по списку — подтверждаешь горсть корней, зависимые
> симптомы падают следом; разделяющий зонд отличает настоящий регресс от вранья окружения
> раньше, чем потратишь на него круг.

Чек-лист закрытия (по каждой находке доказательство глазами):
1. **Оптимизация в ПЛАНИРОВАНИИ**: реальное перераспределение (знаки дельт разные), текст
   сдвигов правдив, Прогноз KPI подписан «за плановый период», без обвала-сюрприза.
2. **Drill «Динамика по периодам»**: клик chip раскрывает группы; **морф-анимация глазами**
   (юнит доказал данные option, НЕ рендер); праздники видны в БАЗЕ; fullscreen во всю высоту.
   + смежные ExpandableCard (Декомпозиция продаж, Расходы vs Эффект) — fullscreen тоже.
3. **Роли колонок**: category_sales → «Оставить: контроль категории» (НЕ «исключите»);
   ooh_contacts → пара найдена, счётчик «исключим 4»; «рыночные продажи» → контроль.
4. **Демо fmcg**: prior-check зелёный на экране (было 44% FAIL), оптимизация даёт видимый lift.
5. **Ratio один и честный** на всех экранах (+ «с учётом авто-праздников»), «48 мес» не «48 нед»,
   время «10-30 сек», R² одним числом, плашка ненадёжности на Декомпозиции/Оптимизации при
   uncertain/unreliable.
6. **Тексты**: без Expert mode/Блок D/bisекция/see methodology/носителей в adstock.
7. **Непройденное приёмкой**: A4 (бейдж пар на карте корреляций), A5 (клиентская колонка
   события без префикса), A6 (суммарная колонка → авто-снятие роли) — спец-файлы.
8. **Экспорт-артефакт**: PPTX+HTML на свежей модели — заголовок периода по гранулярности,
   дисклеймер ненадёжности в HTML, тире «–», без англицизмов.

## 📌 Открытое (не блокирует ретест)
- ~~NEW-2~~ ✅ ЗАКРЫТ `f76835a` (решение Антона: явный `allow_wireframe`-флаг; гейт
  honest-fail 400 на ОБОИХ endpoints /export/pptx+html, 9 тестов, pytest 407).
- ~~tokens.generated.css~~ ✅ РАЗОБРАН+откачен в `f76835a`: фантомный дрейф — тело
  побайтово = HEAD, менялся только timestamp шапки от стороннего запуска
  `Standards/tokens/build.py` 05.07. Структурный недостаток build.py: пишет метку
  даже без изменений тела (нет skip-if-identical) — микрозадача в Standards при случае.
- **OLS-долг adstock**: 'auto' в OLS-пути не резолвится (ols_modeler:146/152/336/351) —
  движок деградированный, honesty уже ≤ uncertain; чинить при случае.
- **Пакет 8** (управляемые праздники F-A1-11 + подсказка F-A1-4) — ОТДЕЛЬНЫЙ ТРЕК после
  ретеста (решение Антона 2026-07-06). Полная спека — `TEST_FINDINGS_GUI_2026_07_05.md` §F-A1-11/§F-A1-4.
  Кратко: (а) F-A1-11 двухчастная — ДО обучения выбор праздников по паре сигналов
  (влияние + Δratio, НЕ только ratio → иначе OVB), ПОСЛЕ — инсайт «праздники снизили ratio
  6.9→3.0, выключите пустые» с per-праздник вкладом из декомпозиции. Инфра ГОТОВА:
  `disabled_holidays`/`use_holidays` в `modeler.py:305,322`; фронт `ConfigPanel.svelte:473`
  (панель «Авто-праздники РФ», сейчас в Эксперт-режиме — вынести в обычный). Дефолт =
  авто-отбор по данным, НЕ «все 12». (б) F-A1-4 подсказка «Метрики каналов» — 3 состояния
  (всё ₽ → «переводить нечего» · физ без ₽ → «укажите CPP/CPM» · смешанный), усилить
  `validateMetricsInsights` в insights-rules.js. Крупный трек → кандидат в отдельное тех-задание.
- Пересборка sidecar перед ретестом обязательна (py-правки: optimizer/modeler/validator/
  reliability_a4/sections/builder/server; в dist/_internal лежат stale-копии).

## ⚡ Метод-урок сессии (для будущих фикс-сессий)
1. Карта класса ДО фиксов окупилась: +близнецы isMonetaryMetric/extractChannelRoot/py-py
   validator-дрейф/HTML-без-дисклеймера, которых не было в 22 находках.
2. **Фантом-агент**: субагент отчитался о фиксе с дифами и числами, сделав НОЛЬ правок.
   Детекторы: tool_uses=1 при заявленном коде + арифметика тестов не сходится (385≠391+6).
   Правило приёмки: сырые выводы git diff --stat + pytest ОБЯЗАТЕЛЬНЫ в отчёте агента.
3. F-A1-19 оказался КАСКАДОМ от F-A1-21 (proportional split → все ratio 0.25 → «4🔴») —
   чинить корень до переоценки симптомов на зависимых экранах.
