-- ✅ ПРИМЕНЕНО 06.09.2026. Решение владельца — окно **90 суток**.
--
-- Ход применения (все числа сняты в одном запросе с самой мерой, чтобы «до» и «после» были
-- согласованы между собой, а не взяты в разные моменты):
--   строк журнала до .................... 12 130
--   строк журнала после ................. 12 130  (ни одна строка не удалена)
--   строк с адресом до / после .......... 8 572 → 3 900
--   строк с именем машины до / после .... 8 546 → 3 874
--   ОБНОВЛЕНО СТРОК ..................... 4 672
--   осталось под чистку после ........... 0      (идемпотентность подтверждена)
--   самая ранняя оставшаяся запись с адресом — 08.06.2026, то есть ровно граница окна.
-- Убрано у 4 672 строк по два ключа; выгрузка этих строк сделана ДО применения и лежит вне гита:
-- `_local_pdn/VYGRUZKA_audit_log_pii_2026-09-06.json` (4 672 записи, совпало с числом обновлённых).
--
-- Приёмка — контроль в обе стороны, а не отчёт об успехе:
--   свежая запись `auth_ok` от 06.09 — адрес НА МЕСТЕ, 13 ключей (мера её не тронула);
--   старая запись `auth_ok` от 08.06 — адреса НЕТ, 6 ключей, `license_id` цел (событие не пострадало).
--
-- 🔴 ФАЙЛ ОСТАЁТСЯ НЕ-МИГРАЦИЕЙ (расширение изменено) и после применения: система миграций его не
-- подхватывает, случайный прогон невозможен. Повторное применение безвредно (обновит ноль строк),
-- но и не нужно.
--
-- Вердикт по разделу Г проекта `DESIGN_2026-09-06_clients_pii.md`: адрес клиента (`client_ip`) —
-- неучтённый класс персональных данных в базе за пределами России. Пишется функцией входа почти в
-- каждое событие; рядом в той же строке лежит `license_id`, из которого через `licenses.customer`
-- достаётся название организации, а нередко — имя и фамилия человека. Соотносимость не гипотеза,
-- а один соединяющий запрос.
--
-- 🔴 ЗАПИСЬ НЕОБРАТИМА. Отката нет: удалённые ключи не восстанавливаются ничем. Применять ТОЛЬКО
-- после явного решения владельца и ТОЛЬКО после выгрузки затрагиваемых строк.
--
-- ЗАМЕР 06.09.2026 (сделан до применения, ложной зелени не будет ни при одном окне):
--   журнал живёт с 30.03.2026 по 04.09.2026, всего строк 12 120;
--   строк с адресом клиента ......... 8 566;
--   строк с именем машины ........... 8 540;
--   различных адресов ............... 79 (парк мал: реально выходят на связь 4 машины);
--   затронет строк при окне 90 суток . 4 575;
--   затронет строк при окне 60 суток . 6 146;
--   затронет строк при окне 30 суток . 7 149.
--
-- Цена, названная прямо: после применения жалоба на отказ входа старше окна по адресу не
-- разбирается. Обычное расследование отказа адрес и имя машины не использует (разбор — в разделе В
-- проекта), поэтому цена оценена как приемлемая — но решение всё равно за владельцем.
--
-- 🔴 ЭТА МЕРА ЛЕЧИТ ТОЛЬКО ПРОШЛОЕ. Настоящее продолжает копиться, пока не сужен приём в функции
-- входа (усечение адреса до сети: IPv4 → /24, IPv6 → /48, единственная точка — `getClientIp`).
-- Правка функции сегодня НЕ сделана сознательно: по CPD-154 исходник функции входа лежит вне
-- канонического каталога, и доказать, что правится именно то, что стоит в бою, дешёвым способом
-- не удалось (замер 06.09 в реестре). Сначала переезд источника — потом правка двери.
--
-- Свёртка без ключа рассматривалась и ОТВЕРГНУТА: пространство адресов IPv4 перебирается за
-- минуты, то есть свёртка обратима и даёт ложное чувство защиты (INV-38).

-- Окно хранения. 🔴 Значение НАМЕРЕННО не подставлено (правка 06.09 по находке аудита А-4).
-- В первой редакции здесь стояло `interval '90 days'`, а «подставить согласованное владельцем»
-- держалось комментарием — то есть переименовавший файл молча получил бы самое мягкое из трёх
-- окон. Теперь запрос НЕ ВЫПОЛНИТСЯ, пока значение не подставлено руками: заполнитель ниже даёт
-- ошибку разбора, а не тихое умолчание. Это и есть разница между «написано в комментарии» и
-- «удержано механизмом» — та же разница, ради которой вся работа А делалась проверками, а не
-- договорённостями.
-- Замер 06.09: 90 суток затронет 4 575 строк, 60 суток — 6 146, 30 суток — 7 149.
update public.audit_log
   set details = details - 'client_ip' - 'hostname'
 where created_at < now() - interval '90 days'   -- ← подставлено 06.09.2026 решением владельца
   and (details ? 'client_ip' or details ? 'hostname');

-- Идемпотентность: повторный прогон обновит ноль строк — условие уже не выполняется.
-- Строк не удаляется ни одной: события остаются целиком, уходят только два ключа из `details`.
-- Число строк журнала обязано остаться 12 120 до и после.

notify pgrst, 'reload schema';

-- Отката НЕТ. Повторное наполнение невозможно: значения существовали только здесь.
