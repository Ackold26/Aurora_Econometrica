---
tags: [testing, live-gui, autonomous]
type: test-log
date: 2026-06-02
---
# Econometrica MMM Optimizer — live GUI тестирование (автономно)

**Объект:** dev-сборка (master `d479ace`, с фиксами #59 Goal-Seek + #61 license).
**Машина свободна** (Антон) → desktop-control надёжен. **Метод:** desktop-control, 3 клиентских
файла, системная программа Фаз 0-3. **Recovery при компрессе:** читать этот файл, продолжать с NEXT.

## NEXT
→ Запустить dev → дождаться окна → Фаза 0 Smoke (Кагоцел).

## Окружение
- Тестовые XLSX: `C:\Users\ackol\Desktop\Аврора - материалы для обучения и тестирования\Эконометрика - тестовые файлы\XLSX\`
  - `MMX 2021-2025 исходник.xlsx` (длинный ряд)
  - `Венарус_данные для эконометрики для модели + наши данные.xlsx`
  - `Кагоцел РФ+_данные для эконометрики + наши данные 29.08.xlsx` (эталон)
- Offline лицензия (если попросит): `C:\Users\ackol\Desktop\license_Anton_econometrica_20260525.json` (до 2029)
- dev: `npm run tauri dev` · vite 5173 · sidecar 7529 · HMR 1421

## Программа (прогресс)
- [ ] Фаза 0 — Smoke (Кагоцел): запуск → лицензия → импорт → валидация → проход экранов
- [ ] Фаза 1 — Полный прогон (Кагоцел): 6 шагов + MCMC + Goal-Seek(#59) + Response Curves + What-if + отчёты
- [ ] Фаза 2 — Режимы: Маркетолог ↔ Эксперт
- [ ] Фаза 3 — Multi-client: Венарус + MMX
- [ ] Бонус: Настройки (#61 license display), экспорт PPTX/XLSX/HTML

## Находки
Категории: 🔴 краш/блокер · 🟠 функц. баг · 🟡 UX-трение · 🔵 достоверность цифр (INV-50) · 💡 идея
Формат: `[severity] экран/шаг — описание — repro — скриншот-ref`

### Фаза 0 — Smoke
- 🟠 **ONBOARD-1** first-run онбординг — **четыре несогласованных механизма подряд**:
  (1) образовательный 8-слайд «Что такое MMM / adstock», (2) продуктовое интро 4-слайд
  «Aurora MMM Optimizer», (3) welcome-тур «1 из 8 — покажу ключевые места», (4) per-step
  coach marks «1/3 Импорт данных». ≈20+ модальных шагов до начала работы. Перегруз first-run.
  Mitigation есть (кнопка «больше не показывать туры» + Настройки→Обучающий режим), но дефолт —
  всё показывается последовательно. Реко: один координированный поток / показывать по одному.
- 🟡 **LANG-1** на главной карточке «Visual Pipeline» шаги pipeline на АНГЛИЙСКОМ
  (Import → Validate → Model → Decompose → Optimize → Report), а в степпере и на экранах — на
  РУССКОМ (Импорт→Валидация→Модель→Декомпозиция→Оптимизация→Отчёт). Несоответствие языка
  (англицизмы, ср. feedback_aurora_ui_no_anglicisms). Локация: главный экран Visual Pipeline карточка.
- 💡 **POS-1** «Попробовать на примере» — 4 синтетических отраслевых набора (FMCG бренд / OTC
  фарма / Недвижимость / Ритейл-сеть) для старта без своих данных. Сильная онбординг-функция.
- ✓ **POS-2** «● Python Готов» индикатор статуса sidecar (низ-право) — прозрачность готовности
  вычислительного модуля. Хорошо для доверия.
- ✓ **POS-3** контекстная помощь «Зачем шаг «X»?» (раскрывашка вверху) на каждом шаге + один
  чистый CTA «Новый проект» на главной (нет конкурирующих кнопок).

### Фаза 1 — Полный прогон (Кагоцел РФ+, 31 набл × 32 столбца)

**Импорт:**
- ✓ POS-4 авто-инсайты при импорте: KPI обнаружен, предупреждение про 26 медиа-переменных +
  совет агрегации мелких каналов. Авто-выбор «Полное Bayesian MMM» (NUTS/NumPyro) с объяснением
  порога n≥30 (OLS fallback при n<30). Превью 20 строк с форматированием чисел.
- 🔵 NUM-1 «31 месячных наблюдений (~31 месяцев)» — тавтологичная формулировка (число × ед. дважды).

**Валидация — общее:**
- ✓ POS-5 богатая валидация: метрики-бейджи (RATIO 1.2:1, VIF, ПЕРИОД 31, MQS ПРОГНОЗ 60),
  4 подшага (Целевая метрика / Роли колонок / Метрики каналов / Подтверждение), ROI vs
  Эффективность режим с объяснением, 9 типов KPI (выручка/прибыль/штуки/лиды/...). Hover-тултипы работают.
- 🟠 SEV-1 бейдж «✕ Ошибка: Ratio 1.2:1 - слишком мало данных» (красный, severity=Ошибка) при том,
  что инсайт говорит «31 наблюдений приемлемо для пилота». Рассогласование severity (Ошибка vs приемлемо).
  То же «КРИТИЧЕСКИ МАЛО» на Роли колонок — пугающая формулировка для пилотных данных, которые продукт сам допускает.
- 🟡 NAV-1 навигация по подшагам: клик по номеру подшага (②) НЕ переключает контент; переход
  только через «Далее ▶». При этом «Далее ▶» дублируется (контентная внизу секции + глобальная
  в футере), контентная ниже сгиба — пользователь может не найти сразу.

**Валидация — Роли колонок (очень сильный экран):**
- ✓✓ POS-6 умные инсайты (22): авто-распознал роли (2 целевых / 22 медиа / 3 контрольных / 1 дата /
  2 не-использовать), авто-рекомендации Оставить/Исключить + «Применить все рекомендации (12)»,
  каналы с >50% нулей помечены (Retail Media 64.5%, Радио 87.1%, Пресса 83.9%, ООН 80.6%),
  совет исключить промежуточные метрики (показы/клики) при ROI-режиме, оставить бюджеты.
- 🔵🔵 NUM-2 (СИЛЬНАЯ сторона) приложение само поймало «Продажи в руб конкуренты в 208× больше
  крупнейшего медиа-канала (194 583 624 341 vs 933 279 612) → вероятная ошибка единиц рубли/копейки».
  Отличная проверка достоверности данных — ловит грязь во входных данных до моделирования.
- 🟡 KPI-1 две колонки помечены «Целевая метрика» одновременно (Продажи в руб бренд + Продажи в уп
  бренд). MMM нужна одна зависимая — приложение предупреждает инсайтом, но не блокирует/не форсирует
  выбор. Требует ручного разрешения (переназначить одну).
- 🔵 DATA-1 (данные Кагоцел, не баг продукта) ratio 1.2:1 = 31 набл / 26 переменных — переопределённая
  модель. Продукт корректно предупреждает «коэффициенты будут случайными». Тест моделирования покажет,
  как продукт это коммуницирует в итоге (MQS / доверие).
- ✓ POS-7 авто-рекомендации реально работают и реактивны: «Применить все (12)» подняло ratio
  1.2:1→2.2:1, медиа 22→10, + прогрессивная рекомендация «до 3.1:1 / 4:1». Переназначение роли
  (2-я целевая → Не использовать) мгновенно: целевая 2→1, ratio пересчитался. Сильная реактивность.

**Навигация (сводно):**
- 🟠 NAV-2 футерная глобальная «Далее ▶» НЕ работает на подшагах Валидации (несколько кликов — 0 эффекта).
  Переход между подшагами — только через КОНТЕНТНУЮ «Далее ▶» внизу секции (ниже сгиба, для длинной
  таблицы ролей — далеко внизу). Клик по номерам подшагов (①②③④) тоже не навигирует. ОДНАКО клик по
  ШАГУ в верхнем степпере («Модель») сработал и перепрыгнул сюда (валидация авто-принялась). Итого
  три разных механизма навигации с непредсказуемым поведением — главный UX-баг сессии пока.

**Модель (настройка):**
- ✓ POS-8 отличная контекстная помощь «Зачем шаг Модель?»: ЧТО ДЕЛАЕМ (Bayesian MCMC 1-3 мин) /
  ЗАЧЕМ / НА ЧТО ОБРАТИТЬ ВНИМАНИЕ (R-hat<1.05, R²>0.7, Trust Level 3) / ЧТО ДАЛЬШЕ.
- ✓✓ POS-9 авто-добавление 12 праздников РФ (Новый год, 23 фев, 8 мар, майские, 1 сен) как контрольных —
  чтобы модель не приписывала сезонные скачки рекламе. Умная защита от смещения. Adstock авто
  (digital=мгновенный, TV=отложенный) с визуализацией кривой. Каналы >50% нулей (Радио/Пресса/ООН) авто-исключены.
- ✓ POS-10 обучение MCMC: прогресс-бар «Сэмплирование байесовских цепей» + счётчик времени +
  «Остановить обучение». Оценка ~4 мин (7 каналов). Запустилось штатно (sidecar здоров).

**Модель (результаты обучения):**
- 🟠 **MQS-1 (достоверность/INV-50)** карточка MQS: «Модель объясняет **только 98%** вариации
  продаж (R²). Результаты ненадёжны». Формулировка «только 98%» вводит в заблуждение: 98% R² —
  очень высокий, а текст подаёт как «недостаточно/мало». Реальная проблема = ПЕРЕОБУЧЕНИЕ (высокий
  R² на 31 набл = артефакт), и инсайты справа формулируют это ПРАВИЛЬНО («R²=0.976 очень высокий fit,
  но на коротких данных это признак переобучения, а не силы модели»). Карточка MQS противоречит
  инсайтам и здравому смыслу. Реко: «объясняет 98% — но на таких коротких данных это переобучение, а не надёжность».
- 🟡 **GRAM-1** «1 дивергенций в Markov Chain Monte Carlo» — согласование числительного (должно
  «1 дивергенция»). Аналог ruPeriodForm. + «Markov Chain Monte Carlo» в инсайте — англицизм/жаргон
  (в др. местах help-system заменяли на «байесовские цепи»).
- ✓✓ POS-11 **#62 ВЕРИФИЦИРОВАН ВЖИВУЮ**: R-hat по всем 9 параметрам = 1.0000, показано как
  «9 из 9 параметров сошлись (R-hat < 1.05)» зелёными барами — R-hat=1.0 корректно = хорошо
  (а не ✗). Подтверждает, что баг #62 действительно устранён.
- ✓ POS-12 диагностика модели: R-hat бар-чарт по параметрам с порогом 1.05; Факт vs Прогноз график
  (R²=0.9758, MAPE=7.13%); MQS=50 «Слабое» честно для плохих данных; предупреждения про переобучение/
  дивергенцию/широкие CI. INV-50 (доверие к цифрам) в инсайтах соблюдён.
- 🟡 MQS-2 «✓ Модель рассчитана надёжно» (зелёная строка) соседствует с «MQS 50 Слабое / результаты
  ненадёжны» (оранжевая карточка) — смешанный сигнал «надёжно vs ненадёжно» в одном экране. Уточнить:
  «расчёт корректен (сошёлся), но данных мало → выводы ненадёжны» — это разные вещи, но рядом читается противоречиво.

**Декомпозиция:**
- 🟠 **STATE-1** «⚠ Декомпозиция устарела. Конфигурация изменилась после обучения... Медиа-каналы:
  было 7, стало 10» — показано СРАЗУ после обучения, хотя роли не менялись вручную. Причина: шаг
  Модель обучает на ВЫБРАННОМ подмножестве (7 отмеченных каналов), но Валидация-роли = 10 медиа →
  Декомпозиция сверяется с ролями (10) и считает результат устаревшим. Рассинхрон модели данных
  между «выбор каналов для обучения» (Модель) и «роли» (Валидация). Сбивает: только что обучил — уже «устарело».
- 🔵🟠 **ROI-1 (достоверность + отложенный #60)** «TRPs бренд (W 25-54) — самый эффективный канал
  ROI **12186.1×**». Абсурд — артефакт не-денежных единиц (TRP физическая метрика, не ₽). Продукт
  САМ помечает «Экстремальный ROI / большой разброс» + «Не-денежные единицы TRPs», НО 💡-инсайт
  всё равно советует «перераспределить в TRPs бренд (самый эффективный)». Наращивание по нереальному
  ROI = вредный совет. Перекликается с отложенным #60 (mixed-units budget). TRP-каналу нужна цена
  за единицу (CPP) на подшаге «Метрики каналов» — возможно я его проскочила, но продукт не заблокировал.
- 🟡 **REC-1** противоречие рекомендаций на одном экране: 💡-инсайт «нарастить TRPs бренд», а
  «ГЛАВНАЯ РЕКОМЕНДАЦИЯ» «переложить 22.7 млн ₽ Banners→Performance». Главная разумнее (не трогает
  TRP-артефакт), но два совета рядом сбивают.
- ✓ POS-13 водопад «Декомпозиция продаж» (Baseline 9047M + вклады каналов = Итого 11226M), главная
  рекомендация с конкретикой (сумма + откуда/куда + gap%), честный инсайт «Base sales 81% —
  большинство продаж органические, медиа-эффект относительно слабый». «Перейти в Оптимизацию →» CTA.

**Оптимизация + Goal-Seek (#59 фокус):**
- ✓ POS-14 структура блоков A/B (как в UX-плане): «Текущий бюджет» (279.3 М ₽ / 7 каналов, ПРОГНОЗ
  KPI 11226.1 М ₽, СРЕДНИЙ ROI 7.80×, СВЕТОФОР НАСЫЩЕНИЯ 🟢5🟡2🔴0). Режимы «От бюджета (Forward)» /
  «От цели (Goal-Seek)». Табы «Анализ (обучающий)» / «Планирование (будущий период)». Контекстная помощь.
- ✓✓ **#59 ВЕРИФИЦИРОВАН (частично, вживую):** Goal-Seek функционирует (bisection-фиксы e904cd7 и др.
  работают — не крашится). Мой scope-addition «проброс error (non_monotonic_forward)» РАБОТАЕТ:
  при недостижимой цели UI показывает «Цель недостижима... Forward функция не монотонна... non-convex
  Hill saturation» — точное сообщение из inverse.py дошло до экрана. Flat-response баннер UI визуально
  НЕ воспроизведён (см. GS-1 — на этих данных модель non-monotonic везде, achievable=False всегда;
  баннер требует achievable+flat). Баннер покрыт unit-тестами (3 PASS) — код-путь корректен.
- 🟠 **GS-1** Goal-Seek возвращает «недостижимо / non-monotonic» для ЛЮБОЙ цели в безопасном коридоре,
  включая НИЖЕ текущих продаж (10.57 млрд < 11.23 текущих). Причина: переобученная модель Кагоцел
  (MQS 50 «Слабое») → Forward-функция non-convex Hill → bisection не применима нигде. Математически
  корректно, но Goal-Seek фактически нерабочий на этом датасете — пользователь не получит ни одного
  результата. Возможно проверка монотонности слишком строгая, или нужен degraded-режим для слабых моделей.
- 🟡 **GS-2** дефолтная цель слайдера = текущие продажи (11.23 млрд) → при первом же «Найти решение»
  сразу «недостижима». Плохой first-impression (выглядит как «мои текущие продажи недостижимы?»).
- 🟡 **INPUT-1** поле «Или введите точно» — замена значения не работает штатно: клик в поле + Ctrl+A
  не выделяет, `type` ДОПИСЫВАЕТ к существующему → мусор «112 260 577 029 000 000 000». Слайдер-клик
  (перепрыгивание) работает как обходной путь. (Часть может быть инструментальной desktop-control,
  но Ctrl+A в поле точно не выделил — поле не реагирует на стандартное выделение.)
- ✓ POS-15 защита от экстраполяции: при цели в красной зоне — «Цель в красной зоне — кнопка
  заблокирована», «Найти решение» disabled. Хорошая защита от невалидных рекомендаций.

**Forward-оптимизация (От бюджета):**
- ✓ POS-16 оптимизатор работает: прирост +0.0% (текущее распределение уже оптимально в коридоре
  ±20-200%), честно объясняет ПОЧЕМУ 0% («границы Min/Max узкий коридор ИЛИ уже близко к локальному
  оптимуму»), советует расширить границы (10/300%) или Эксперт-режим. Edge-case баннер
  «converged_at_current» (L7 из v1016) работает. Светофор насыщения реактивно обновился (🟡7).
- 🔵 ROI-2 (повтор ROI-1, достоверность) «Предельная отдача ROI: лучший TRPs бренд 9550.62×, худший
  Banners 1.52×. Разброс 6275× — есть реальный потенциал перекладки». Снова TRP-артефакт единиц в
  рекомендации. «Реальный потенциал перекладки» в TRPs (физ. метрика) бессмысленен. Системная проблема
  всех экранов оптимизации/декомпозиции при наличии не-денежного канала в ROI-режиме (→ #60 / mixed-units).
- 🟡 BUDGET-1 (→ отложенный #60) бюджет показан «279,334,862 ₽» — за период обучения. На разных экранах
  бюджет/KPI в единицах «за весь период анализа» (11226 М ₽). Это контекст #60 (per-period vs total).
  В распределении «TRPs бренд — 32X ₽» — единица TRP отрендерена с ₽ + множитель X (нечитаемо).

**Оптимизация — блоки C/D + Response Curves + Отчёт:**
- ✓ POS-17 полная UX-реализация плана project_econometrica_optimizer_ux: блок A «Текущий бюджет»,
  блок B «Оптимизация распределения», блок C «What-if: изменённый бюджет» (слайдер множителя +
  Пересчитать), блок D «Учесть инфляцию следующего периода». Response Curves — Hill-кривые 7 каналов
  с текущими точками. mIROAS таблица (предельная отдача) с честными «Недостаточно данных» бейджами.
- ✓ POS-18 **Отчёт (финал pipeline)**: метрики (MQS 50, R²=0.976, MAPE 7.1%, прирост +0.0%, бюджет
  279.3М), экспорт 4 формата (PPTX/XLSX/HTML/DOCX) с описаниями, «Сопроводительный текст для письма»
  (в email), «Как интерпретировать» (простыми словами), FAQ «8 вопросов с ответами на ваших данных».
  Очень полный client-ready deliverable-набор. (Генерацию файла не запускала — контекст-бюджет.)
- ✓ POS-19 ВЕСЬ PIPELINE пройден end-to-end на Кагоцел: Импорт→Валидация→Модель(MCMC)→Декомпозиция→
  Оптимизация(Forward+Goal-Seek)→Отчёт. Ни одного краша. Sidecar стабилен всю сессию.

### Фаза 2 — Режимы (НЕ покрыто — приоритет след. сессии)
Не дошла из-за объёма контекста + навигационных трений. Проверить: переключатель «Маркетолог ↔
Эксперт» (вверху справа), что раскрывается в Эксперт-режиме (custom prior, per-channel Min/Max,
MCMC-настройки, доп. опции единиц), консистентность панелей. #61 license — Настройки (offline-smoke).

### Фаза 3 — Multi-client (НЕ покрыто — приоритет след. сессии)
Не дошла. Прогнать Венарус + MMX 2021-2025 (разные структуры) ≥ до декомпозиции. Сравнить нарративы/
инсайты на достоверность (multi-client ловит client-specific баги — feedback_multi_client_live_test).
MMX 2021-2025 — длинный ряд (вероятно ratio лучше → монотонная модель → можно воспроизвести
flat-response баннер #59 в Goal-Seek, который не удалось на Кагоцел).

## Сводка (Фаза 0 + Фаза 1, 2026-06-02)
**Объект:** dev d479ace (с #59/#61). Пройдено: Фаза 0 Smoke + Фаза 1 полный pipeline (Кагоцел).
НЕ пройдено: Фаза 2 (Эксперт), Фаза 3 (multi-client) — контекст-бюджет.

**Верификация моих фиксов:**
- ✓ #62 (R-hat=1.0) подтверждён obsolete вживую — «9/9 сошлись» зелёным.
- ✓ #59 backend: Goal-Seek работает, error-passthrough (non_monotonic) дошёл до UI. Flat-response
  UI-баннер не воспроизведён (Кагоцел non-monotonic везде; покрыт unit-тестами). → проверить на MMX.
- #61 (license) — не дошла до Настроек (offline-smoke остаётся handoff).

**ТОП находки (severity):**
- 🟠 ONBOARD-1: 4 несогласованных онбординга подряд (~20 модальных шагов) на first-run.
- 🟠 NAV-2: футерная «Далее ▶» не работает на подшагах; переход только контентной кнопкой внизу;
  клик по номерам подшагов не навигирует; степпер работает по-другому. Главный UX-баг.
- 🟠 MQS-1 (достоверность): «объясняет ТОЛЬКО 98%» — вводящая в заблуждение формулировка (98% много;
  проблема — переобучение, не низкий R²). Противоречит инсайтам.
- 🟠 ROI-1/ROI-2 (достоверность, → #60): TRPs-канал ROI 9550-12186× (артефакт не-денежных единиц),
  но рекомендуется наращивать. Продукт помечает «не-денежные единицы», но советы всё равно вредные.
- 🟠 STATE-1: «Декомпозиция устарела, было 7 стало 10» сразу после обучения (рассинхрон Модель↔Валидация-роли).
- 🟠 GS-1: Goal-Seek нерабочий на переобученной модели (non-monotonic для любой цели, даже ниже текущих).
- 🟡: GS-2 (дефолт-цель сразу недостижима), INPUT-1 (замена в поле не работает), LANG-1 (англ.
  pipeline на главной), GRAM-1 («1 дивергенций»), NUM-1, SEV-1, KPI-1, MQS-2, REC-1, BUDGET-1.
- ✓ 19 POS: умная валидация (ловит ошибку единиц 1711×!), авто-рекомендации реактивны, авто-праздники
  РФ, R-hat/Response Curves/mIROAS визуализация, честный MQS+предупреждения переобучения (INV-50),
  4-форматный отчёт, полные блоки A/B/C/D, контекстная помощь на каждом шаге.

**Общее впечатление:** очень зрелый, умный продукт с сильной валидацией данных и честной
коммуникацией неопределённости. Главные зоны улучшения: (1) онбординг-перегруз, (2) навигация
подшагов, (3) достоверность формулировок при слабых данных / не-денежных единицах (MQS-текст, TRPs-ROI).

### Фаза 3 — Multi-client
(пусто)

## Сводка (заполнить в конце)
