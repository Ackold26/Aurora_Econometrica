# Находки внешнего аудита блока 11.09 (s41)

severity | файл:строка | суть | сценарий
---|---|---|---
High | src-tauri/tests/guard_assistant_route_single_path.rs:143 | сторож автоопределения НЕ ищет `decide_with_history(` — а он оставлен в дереве как выведенный из употребления | вызов `execution_mode::decide_with_history(..)` из claude.rs возвращает молчаливое автоопределение, оба сторожа зелёные
High | src-tauri/tests/guard_assistant_route_single_path.rs:107 | запрет `run_claude_inner(` проверяется только в телах двух точек входа | добавить `async fn dispatch(){ run_claude_inner(..) }` и звать `dispatch()` из run_claude — сторож зелён, маршрут в чужой CLI восстановлен
High | src-tauri/tests/guard_left_position_egress.rs:160 | карв-аут «голая схема без хоста» даёт обход | `format!("{}{}", "https://", cfg.host)` или `const SCHEME:&str="https://"` — литерал даёт host=="" → обращение невидимо сторожу
High | src-tauri/tests/guard_left_position_egress.rs:34 | `GATED_BY_ROUTE` объявляет gateway_executor.rs и rag_client.rs закрытыми гейтом, но НИ ОДНА проверка этого не доказывает | egress из rag_client.rs в левом положении пройдёт мимо — сторож по построению его прощает
Medium | src-tauri/tests/guard_left_position_egress.rs:38 | is_loopback по подстроке | адрес `https://127.0.0.1.attacker.example` считается своей машиной
Medium | src-tauri/tests/guard_left_position_egress.rs:150 | `line.find(marker)` берёт только ПЕРВОЕ вхождение на строке | два адреса в одной строке — второй не виден
Medium | src-tauri/tests/guard_*.rs (strip_comments) | символьный литерал `'"'` открывает режим строки и до следующей кавычки комментарии НЕ вырезаются | адрес в комментарии после такого литерала даёт ложную красноту
Medium | src-tauri/tests/guard_left_position_egress.rs:187 | белый список по ИМЕНИ файла, не по пути | новый `src/commands/x/updater.rs` с чужим адресом проходит как «обновления»
High | src-tauri/src/commands/user_config.rs:109 | `migrate_route_axis` переносит вправо, НЕ спросив ни согласия, ни наличия шлюза в сборке | у кого `execution_mode=="local"` и согласие отозвано/устарело (terms_version bump) — признак сообщения ставится, человек читает «теперь через шлюз Авроры», а фактическое положение остаётся ЛЕВЫМ (route_is_cloud требует consent). Сообщение врёт о маршруте
High | src-tauri/src/commands/user_config.rs:114 | `let _ = save(...)` — отказ записи проглочен, возвращается true | настройки только для чтения / диск полон: сообщение показывается, `clear_route_notice` уходит в ранний Ok (в файле pending=false), при каждом запуске перенос срабатывает заново. Идемпотентность заявлена, но держится только на успешной записи
Medium | src-tauri/src/commands/user_config.rs:163 | `save` = `fs::write` без временного файла и без блокировки | две копии программы стартуют одновременно → read-modify-write затирает чужую правку (потеря `local_only` или `cabinet_paths`); обрыв питания в момент записи даёт битый JSON, `load` молча возвращает `UserConfig::default()`, все настройки исчезают без единого слова человеку
Medium | src-tauri/src/commands/user_config.rs:46 | откат на прежнюю версию после переноса | старая сборка читает `execution_mode:"cloud"` как ЯВНЫЙ облачный выбор; человек, у которого вчера работа шла через свой Claude Code, после отката идёт на шлюз молча – старая версия сообщения о смене маршрута не знает. Обратно: старая версия не знает поля `route_notice_pending` и при первом же сохранении настроек его теряет, сообщение пропадает навсегда
Low | src-tauri/tests/guard_left_position_egress.rs (проверено, не находка) | карв-аут «голая схема» оправдан: `lib.rs::add_url_to_inbox` действительно только пишет .url-файл, сетевого вызова нет | прочитан lib.rs:1691-1724
Critical | aurora_pptx/builder.py:4046 + narrative_adapter.py:1029 | NaN/бесконечность в ряду роняют сборку ВСЕЙ колоды, а не один слайд | `float('nan')` проходит проверку адаптера молча (`float(nan)` не бросает). Зонд на месте: `add_chart(LINE_MARKERS, ...)` с NaN в ряду → `TypeError: NAN/INF not supported in write_number()` из xlsxwriter внутри python-pptx. Вокруг `make_actual_vs_predicted` нет try/except → клиент не получает колоду вовсе. Пропуск в факте или разошедшаяся цепь в прогнозе дают ровно этот вход
High | aurora_pptx/charts.py:287 | ряд из ОДНОЙ точки молча теряет «Прогноз» | у «Прогноз» `marker.style = NONE` и только линия; при одной точке линии нет → на слайде виден только факт, расхождения не видно вовсе, а подпись-норма обещает сравнение форм
High | tests/test_no_em_dash_in_client_text.py:172 | `in_replace_call` гасит ВЕСЬ стек вызовов, а не только сам `.replace` | `("Канал — убыточен").replace("ё","е")` или клиентский шаблон, проходящий через `.replace()` — литерал с длинным тире пропускается молча
High | tests/test_no_em_dash_in_client_text.py:153 | `in_logging_call` — по ИМЕНИ метода в любом месте стека | `return response.error("Бюджет пуст — расчёт невозможен")` / `ctx.info(...)` / `raise X(warn("… — …"))`: клиентская строка тонет, потому что метод назвали `error`
Medium | tests/test_no_em_dash_in_client_text.py:39 | allowlist привязан к паре (путь, имя) | переименование файла или функции молча снимает исключение → сторож краснеет на служебном тексте; обратное переименование клиентской функции в `report`/`generate` даёт совпадение с чужой записью списка и глушит настоящую находку
Medium | tests/test_no_em_dash_in_client_text.py:27 | область — только движок Python | клиентские строки Rust (кроме трёх ad-hoc assert в execution_mode.rs) и весь фронтенд .svelte/.js под сторожем не стоят; тот же класс дефекта там не ловится ничем
Medium | narrative_adapter.py:1035 | остатки считаются, но НЕ проверяются на конечность | ряд с inf даёт residuals inf; веб-отчёт покажет NaN-разрыв, колода упадёт — «общий адаптер» даёт двум выходам РАЗНОЕ поведение на одном входе
Medium | engines/channel_action.py (правка `if eff_mroas < 0.8`) | нижняя граница снята, а сторож неопределённости ниже (стр. 508) требует `eff_mroas > 0` | канал с eff_mroas ровно 0 и ЛЮБОЙ ширины правдоподобного диапазона минует ветку «Недостаточно данных» и получает жёсткий «Остановить»; проверка неопределённости для неположительной отдачи выключена по построению
Medium | engines/channel_action.py (`_below_breakeven_str`, `eff_mroas <= 0`) | при eff_mroas РОВНО 0 клиенту сказано «канал уменьшает продажи, а не приносит их» | нулевая предельная отдача = канал не добавляет; утверждение о вычитании неверно. Граница должна быть `< 0`, не `<= 0`
Medium | engines/channel_action.py (`ratio <= 0.5`) | нестрогая граница ловит каналы, упёртые оптимизатором в НИЖНЮЮ границу коробки | сценарий сокращения общего бюджета: оптимизатор ставит все каналы на пол 50%, включая лучшие; каждый такой канал теперь получает «Остановить» вместо прежнего прохода мимо
Low | src/routes/+layout.svelte:246 | `acknowledgeRouteChange()` снимает durable-признак сразу после вызова toast, не после прочтения | закрытие окна в течение 30 секунд показа = человек о смене маршрута данных не узнал никогда
Проверено, находки нет | src/routes/settings/+page.svelte:610-633 | непрочитанное положение обработано честно: переключатель `disabled`, `aria-checked="mixed"`, подпись HEADLINE_UNKNOWN | искал состояние, где подпись врёт о маршруте — не нашёл
Проверено, находки нет | гонка чтения и переключения | `toggleAssistantRoute` считает `!cloud` из возможно устаревшего стора; обе стороны развилки безопасны (влево — подтверждение намерения, вправо — упирается в согласие) | перебрал оба устаревших состояния
