# Внешний аудит блока P0.6 — отрицательный базовый уровень

База: `28742b8..HEAD`, ветка `feat/econ-canon-p0`. Аудитор в чистом контексте.

## Находки

Medium | sidecar/econometrica/utils/negative_baseline.py:621-624 | ОДИН недопустимый отсчёт гасит всю проверку: фильтр непригодных значений режет ПЕРИОДЫ (`axis=1` — свёртка по отсчётам), а не отсчёты, поэтому единственный NaN/inf среди тысяч выборок делает непригодной каждую строку и функция возвращает `None`. | Зонд (D=500, один `intercept[7]=nan`): без контролей → `None`; с двумя контролями и T=30 → `None`. При корректном отбрасывании одного отсчёта проверка посчиталась бы по 499 годным. На экране это «проверка недоступна» вместо результата — тихая деградация, ложного числа не даёт.

Medium | sidecar/econometrica/utils/negative_baseline.py:610-616 | Несогласованные формы контролей выпадают МОЛЧА: если после эвристики ориентации `x_norm.shape[1] != betas.shape[0]`, `control_effect` остаётся нулевым, и база считается вообще без контролей — но результат отдаётся как полноценный, без признака и без записи в журнал (except ловит только TypeError/ValueError, а здесь исключения нет). | Зонд: betas (3, 500), x_norm (30, 2) → `baseline_mean=6548.73`, `verdict='not_applicable'`, `share_periods_negative=0.0` — ровно значение «контролей нет» (−0,5·500+6800=6550). Читатель диагностики не отличит «контроли учтены» от «контроли выброшены». В текущем движке контракт форм соблюдён (modeler.py:1597 даёт (n_controls, n_samples), X_control_norm — (T, n_controls), инжект праздников/Фурье на строках 424/507 идёт ДО сборки X_control на 578), поэтому дефект латентный: срабатывает при изменении контракта или при `n_controls == n_draws` (зона неуверенности №3 автора — подтверждена: 3×3 отдаёт число молча).

Medium | sidecar/econometrica/utils/negative_baseline.py:598-645 + src/lib/insights-rules.js:796-801 | Нет защиты от неположительного `y_mean`: для денежного KPI со средним ниже нуля (убыточная прибыль) проверка объявляет `fail` и пользователь получает красный инсайт «без рекламы продаж не было бы вовсе – это невозможно, значит вклад каналов завышен» — утверждение ложное, отрицательная база там нормальна по смыслу метрики. | Зонд: `y_mean=-2000, y_std=500, intercept~N(-0,5;0,1)` → `baseline_mean=-2251.27`, `verdict='fail'`, `detectable=False`. Вердикт `fail` по инварианту №5 засчитывается всегда, поэтому нечувствительность его не гасит, и инсайт с severity `error` показывается.

Low | sidecar/econometrica/utils/negative_baseline.py:661 | Поле `basis: 'displayed_baseline_mean'` утверждает равенство с показанной базой, но считается величина ДО выноса факторов. Полоса «Базовый уровень» на экране — `baseline_reduced` после вычитания каждого выносимого фактора (decomposer.py:517 `baseline_reduced[t] -= pp[t]`, любой знак). При отрицательном контроле (конкурент) вычитание поднимает показанную полосу, при положительном — опускает, то есть числа расходятся всегда, когда есть хоть один выносимый фактор. | Живой сторож `test_база_совпадает_с_декомпозицией` складывает базу обратно с факторами и потому расхождения не увидит по построению (зона неуверенности №5 автора — подтверждена как реальная: сверяется не то, что видит пользователь, а до-факторная величина).

Low | sidecar/econometrica/utils/negative_baseline.py:628-629 | Вероятность по одному отсчёту подаётся так же, как по тысячам: `prob_negative` из выборки размера 1 равна 0,0 либо 1,0, а `baseline_p05 == baseline_p95 == baseline_mean`. | Зонд `D=1`: `prob_negative=0.0`, `n_draws=1`, `baseline_p05=6550.0`. Смягчает то, что `n_draws` едет в ответе, и на реальном прогоне отсчётов тысячи — порога минимального размера выборки нет.

## — Проверено и НЕ находка:

- **Математика восстановления масштаба верна.** `base = (intercept + Σβ·x_norm)·y_std + y_mean` совпадает с движком: modeler.py:707-708 (`y_norm=(y−y_mean)/y_std`), 863 (`control_effect = dot(X_control_norm, control_betas)`), decomposer.py:1065+1080 (`intercept_mean·y_std + y_mean` и `(X_norm@β)·y_std`). Тождество то же самое.
- **Формы выборок совпадают с реальными.** modeler.py:1593-1601: intercept (n_samples,), control_betas (n_controls, n_samples); X_control_norm — (T, n_controls). Эвристика ориентации на этом контракте транспонирования не делает и умножает правильно.
- **Приор свободного члена действительно `Normal(0, 0.5)`** (modeler.py:811) — расчёт `sigmas_needed = |mean/std| / 0.5` и порог `MIN_CV_FOR_DETECTION = 0.5` (≤4 сигм) внутренне согласованы; сам порог остаётся продуктовым соглашением, как и заявлено.
- **Совместимость с `tools/recompute_mqs.py` проверена по инструменту, не по тесту.** Строки 94-97 сливают секции (`mqs`, `verdict`, `checks` заменяются, `metrics` дополняется), строка 120 пишет `sanitize_nonfinite(diag)`, строка 133 переносит слитую диагностику в `latest-params.json` — ключ верхнего уровня переживает пересчёт в обоих файлах.
- **Вердикт надёжности не меняется**: `test_вердикт_надёжности_не_изменился_от_нового_ключа` вызывает настоящую `optimizer_honesty.model_reliability_verdict` с ключом и без — это реальная проверка, а не копия логики.
- **Ключ доезжает до фронта**: modeler.py:1896 и 1913 кладут `diagnostics` и в `latest-params.json`, и в возврат `train_model`; InsightsPanel.svelte:325 передаёт этот объект в `modelInsights`, а тот читает `data.diagnostics.negative_baseline` (insights-rules.js:1217-1219, 793). Обрезки/сортировки списка инсайтов, способной вытеснить новый пункт, в `modelInsights` нет — блок просто добавляется в `out`.
- **Сторожа способны падать.** Мутация показа (`verdict === 'watch'` → `verdict !== 'ok'`) красит vitest-сторож: 1 упавший из 7 («проверка неприменима: молчим»). Файл восстановлен из копии, исходная строка на месте. Python-набор: 12 passed.
- **Границы вердикта** соответствуют объявленным: 0,199→ok, 0,2→watch, 0,8→watch, 0,801→fail.
- **Клиентский текст** инсайтов: длинного тире нет (сторож это ловит), обращение на «вы», англицизмов из списка (baseline/intercept/posterior/prior) нет, псевдоточности нет — вероятность округляется до целых процентов.
- **Молчание при недоступности** (`ok` / `not_applicable` / ключ отсутствует / `null` / `{}`) — реально молчит: проверено прогоном сторожа.
