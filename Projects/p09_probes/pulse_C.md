# Пульс — зонды CPD-36/37/38/39 для Aurora AI Econometrica (EC)

**Задача (пересказ):** проверить 4 клетки колонки EC в кросс-продуктном реестре дефектов — CPD-36 (формульная инъекция в CSV/XLSX-выгрузке), CPD-37 (шаг конвейера рапортует успех при отсутствующем результате), CPD-38 (invoke плоскими аргументами vs сигнатура-структура), CPD-39 (зеркальная синхронизация стирает прошлые результаты клиента). Только зонды — фактические команды + вывод + файл:строка, без правок кода. Продукт: Tauri v2 (Rust) + SvelteKit + Python sidecar, байесовское MMM-моделирование, дерево `D:\Docs\Aurora_Ai\Dev\Aurora_Econometrica_canon`.

**План:** 1) прочитать 4 записи реестра (строки 1049–1195) — сделано. 2) по каждой — grep/чтение реального кода EC, живой зонд где applicable. 3) писать результат в result_C.md по мере готовности каждой клетки. 4) отметки в этом файле после каждого шага.

**Старт:** 2026-08-03, 15:31.

---
- 15:31 — прочитала реестр строки 1049–1195 (CPD-36..39 полностью). Начинаю искать выгрузки CSV/XLSX в дереве EC (CPD-36).
- 15:40 — CPD-36 закрыла: EC пишет ТОЛЬКО XLSX (`rust_xlsxwriter`), CSV только читается (`pd.read_csv`) нигде не пишется. В `report.rs` пользовательские имена (`name`/`brand`) идут через `.write(row, col, name)` → строковый тип ячейки, а не `write_formula`; единственные `Formula::new()` (report.rs:1225,1230) строят ссылки на ячейки внутри самого XLSX, не из пользовательских данных. Вектора инъекции нет. Статус ⚪. Пишу в result_C.md.
- 15:41 — перехожу к CPD-37. Открыла lib.rs:3174-3222, session/manager.rs, campaign.rs — обнаружила, что это ТОТ ЖЕ донор-скелет, что у Legal: session/manager.rs::sync_dir_to/sync_exports/open_session, campaign.rs::persist_step_exports, lib.rs::emit_wf_status — БУКВАЛЬНО те же функции, байт-в-байт похожий код.
- 15:50 — подтвердила CPD-37: lib.rs:3174 `let _ = state.session_manager.sync_exports(cabinet_id)` и :3182 `let _files = commands::campaign::persist_step_exports(...)` в шаге workflow — оба результата отброшены, затем :3190 close_session, затем :3192 emit_wf_status(..., "done") безусловно. Тот же файл в одиночном чате (lib.rs:1455-1456) корректно пробрасывает ошибку `.map_err(...)?`. Ровно расхождение из реестра. persist_step_exports (campaign.rs:474) push'ит имя файла в `copied` БЕЗ проверки успеха fs::copy — тоже совпадает с находкой Legal.
- 15:55 — перехожу к CPD-39 (та же зона кода). Прочитала sync_dir_to (manager.rs:545-576, зеркальное удаление) и open_session (manager.rs:229-256) — восстанавливает ТОЛЬКО inbox, exports создаётся пустым. sync_exports (manager.rs:294-311) зовёт sync_dir_to до 5 раз в один и тот же dst. Тесты на sync_dir_to/sync_exports по имени не нашла (grep пуст) — как и в Legal до починки.
- 16:00 — важный нюанс достижимости: campaign/workflow UI гейтится `$isCreativeHub` (src/routes/+page.svelte:228, CommandPalette.svelte:45) — productType для Econometrica = 'econometrica' (creative-store.js:13-21), то есть `isCreativeHub=false`. Кампании/бренды НЕ видны в интерфейсе Econometrica → путь CPD-37 (шаг workflow) вероятно СПЯЩИЙ, как и в Legal. НО sync_exports вызывается ПОСЛЕ КАЖДОГО сообщения в одиночном чате советника econometrist (lib.rs:1455, lib.rs:1212) — это НЕ требует campaign UI. Проверила: облачная редакция Econometrica показывает ровно кабинет-советник econometrist (cabinet.rs test `cloud_edition_exposes_econometrist_advisor` строка 613, `filter_by_product` cabinet.rs:121 `"econometrica" => Some(&["econometrist"])`). Значит путь CPD-39 (перезапуск стирает exports) ДОСТИЖИМ через обычный чат советника, без кампаний.
- 16:05 — CPD-38: искала команды со структурным аргументом (payload/struct с обязательными полями) — нашла только Value-блобы (`config: Value`) и HTTP-request-структуры к auth-серверу (не Tauri IPC). Ни одной команды типа `export_items(payload: ExportPayload)`. Проверила реальный вызов `econ_optimize` (15 именованных параметров) — invoke() во фронте (OptimizeStep.svelte:1350) передаёт все 15 ключей camelCase, совпадают 1:1 с Rust snake_case именами. Механики CPD-38 у EC нет.
- 16:10 — дописываю result_C.md полными разборами всех 4 клеток. Завершаю.
- 16:15 — result_C.md готов полностью (CPD-36 ⚪, CPD-37 🔴 частично-достижим, CPD-38 ⚪, CPD-39 🔴 достижим через обычный чат советника). Отправляю итоговое сообщение team-lead. ЗАДАЧА ЗАВЕРШЕНА.
