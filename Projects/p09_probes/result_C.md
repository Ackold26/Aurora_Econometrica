## CPD-36 — предлагаемый статус: ⚪ не применимо
**Зонд:** `grep -rn "write_all\|writeln!\|\.csv\b" src-tauri/src` (весь Rust-код продукта) + `grep -rn "to_csv(" sidecar/econometrica --include=*.py` + чтение `src-tauri/src/commands/report.rs` (единственный писатель клиентской выгрузки).
**Вывод зонда:** во всём Rust-дереве нет ни одной записи CSV-файла с данными клиента — найденные `write_all`/`writeln!` пишут JSON project-файлы, zip-архивы campaign.rs, служебный tsv-лог аудита (`metrics/audit.rs:58`, не выгрузка клиенту) и файлы сессии. В python sidecar `to_csv(` с записью пользовательских данных нет вовсе — весь CSV-код там читает (`pd.read_csv`). Единственный клиентский экспорт продукта — XLSX (`econ_export_xlsx` → `build_xlsx`, `report.rs`), через `rust_xlsxwriter`.
**Адрес:** `src-tauri/src/commands/report.rs:1142,1159,1275,1348,1499` — пользовательские имена (канал/бренд) пишутся через `ws.write(row, col, name)` → строковый тип ячейки (shared string), не формула. Единственные два `Formula::new(...)` во всём файле — `report.rs:1225` (`=B{row}/B${total}`) и `:1230` (`=SUM(B4:B{total})`) — строят ссылку на диапазон из номеров строк программно, без байта пользовательских данных внутри тела формулы.
**Разбор:** класс CPD-36 бьёт по CSV/TSV, где Excel угадывает тип ячейки по первому символу текста при открытии (типа в файле нет) — отсюда `=1+1` исполняется как формула. У EC такого писателя нет физически: единственный экспорт — XLSX, где тип ячейки (строка/формула) кодируется явно в самом файле, и Excel доверяет этой разметке, а не эвристике. Это подтверждает оговорку самого реестра про `rust_xlsxwriter` («формулу из строки не собирает, там вектора нет») — здесь проверено чтением конкретного кода EC: путь `.write()` со строкой пользователя используется, путь `write_formula` с пользовательскими данными — нет ни разу.
**Уверенность:** высокая. Проверила весь Rust-код продукта на паттерны записи (не только имя функции) и весь python sidecar на `to_csv`. Ни одного писателя CSV с пользовательскими данными, ни одного `write_formula`/`Formula::new()` с данными пользователя внутри.

---

## CPD-37 — предлагаемый статус: 🔴 ПОДТВЕРЖДЁН (код байт-в-байт как у Legal до починки), достижимость — частичная
**Зонд:**
1. `grep -n "fn sync_exports\|fn sync_dir_to\|fn open_session\|fn close_session" src-tauri/src/session/manager.rs`
2. `grep -n "emit_wf_status\|\"done\"" src-tauri/src/lib.rs`
3. Чтение `lib.rs:3170-3222` (обработчик шага workflow), `lib.rs:1440-1461` (одиночный чат-запрос), `campaign.rs:464-482` (`persist_step_exports`).

**Вывод зонда:** это ТОТ ЖЕ донор-скелет, что у Legal Center — не аналог, а буквально те же имена функций и почти идентичный код:
- `lib.rs:3174` — `let _ = state.session_manager.sync_exports(cabinet_id);` внутри шага workflow — результат отброшен.
- `lib.rs:3182` — `let _files = commands::campaign::persist_step_exports(&chain_lock.campaign_dir, id, &exports_dir);` — результат (список скопированных имён) получен, но НЕ используется для решения об исходе шага.
- `lib.rs:3190` — `let _ = state.session_manager.close_session(cabinet_id);` — тоже отброшен.
- `lib.rs:3192` — `emit_wf_status(&app, &exec_id, id, "done", None);` — вызывается безусловно, независимо от того, что произошло на трёх строках выше.
- Для сравнения: тот же файл в ОДИНОЧНОМ чат-запросе (не в workflow) корректно пробрасывает отказ — `lib.rs:1455-1456`: `state.session_manager.sync_exports(&cabinet_id).map_err(|e| e.to_string())?;`.
- `campaign.rs:474` — `persist_step_exports`: `let _ = std::fs::copy(entry.path(), dest.join(&name)); copied.push(name);` — имя пушится в список «скопировано» БЕЗ проверки, что `fs::copy` реально отработал. Ровно находка Legal («список ИМЁН, а не подтверждение записи»).

**Адрес:** `src-tauri/src/lib.rs:3174,3182,3190,3192` (шаг workflow) против `src-tauri/src/lib.rs:1455-1456` (одиночный запрос, корректно); `src-tauri/src/commands/campaign.rs:465-482` (`persist_step_exports`).

**Разбор:** расхождение то же самое, что описано в реестре для Legal: канал/функция обязана сообщать ФАКТ (экспорт шага не удался), а вызывающая сторона в контексте кампании обязана превращать это в отказ шага — здесь этого решения нет вовсе, `emit_wf_status(..., "done")` не видит ни одного из трёх предыдущих результатов. Тестов, которые ловили бы отказ (`fs::copy` не удался / `sync_exports` вернул `Err`) в цепочке шага workflow, не нашла.
🔴 **Достижимость — ключевая оговорка, как и в самой записи реестра (урок CPD-28/37 о «спящем» коде).** Campaign/workflow UI гейтится `$isCreativeHub` (`src/routes/+page.svelte:228`, `CommandPalette.svelte:45` — пункт «Бренды» скрыт, если не CreativeHub) и `productType` для сборки Econometrica = `'econometrica'` (`src/lib/creative-store.js:13-21`), то есть `isCreativeHub=false`. У Econometrica в интерфейсе нет ни брендов, ни экрана кампаний/workflow — путь к шагу с `emit_wf_status` в обычном пользовательском потоке НЕ достижим напрямую, аналогично «спящему» состоянию, найденному в Legal.
**Уверенность:** высокая по коду (дефект есть, байт-в-байт совпадает с уже подтверждённым в Legal классом), средняя по достижимости в проде Econometrica — не проверяла живьём, доступны ли Tauri-команды кампаний в обход UI (все команды скомпилированы в общий бинарь независимо от того, какие вкладки показывает фронт — если внешний инструмент/скрипт напрямую дёрнет `invoke('campaign_...')`, шаг всё равно возможен). Для полной уверенности в «спящем» статусе нужен живой прогон: собрать edition Econometrica, попытаться дойти до экрана кампании через UI.

---

## CPD-38 — предлагаемый статус: ⚪ не применимо
**Зонд:** `grep -rn -B2 "tauri::command" src-tauri/src/commands/*.rs src-tauri/src/lib.rs | grep "pub.*fn.*(" | grep -iE "payload|req:|request:"` (0 строк) → `grep -n "struct \w+Payload|struct \w+Config|struct \w+Request" src-tauri/src` → проверка, используются ли найденные структуры (`AuthRequest`, `HeartbeatRequest`, `SearchRequest`, `UserConfig`) как аргумент tauri-команды → живой пример: `grep -rn "invoke('econ_optimize'" src` + чтение вызова.
**Вывод зонда:** ни одна Tauri-команда EC не принимает единственный аргумент-структуру с обязательными полями (как `export_items(payload: ExportPayload)` в Media Radar). Найденные структуры (`AuthRequest`/`HeartbeatRequest` в `online_auth.rs`, `SearchRequest` в `rag_client.rs`) — тела ИСХОДЯЩИХ HTTP-запросов к своим серверам (auth/RAG), не параметры Tauri IPC от фронта. Команды продукта либо принимают набор именованных примитивов/`Option<...>` (`econ_optimize` — 15 отдельных параметров), либо общий `serde_json::Value` (`econ_train`, `econ_awareness_forecast` и др.) — Value принимает любую форму JSON и не может упасть на «missing field» при разборе на границе IPC.
**Адрес:** `src-tauri/src/commands/econometrica.rs:217-271` (`econ_optimize`, образец многоаргументной команды) против вызова `src/lib/components/pipeline/OptimizeStep.svelte:1350-1368` — все 15 ключей (`projectDir, totalBudget, totalBudgetMoney, minPct, maxPct, minPerChannel, maxPerChannel, brandMinPct, brandMaxPct, perfMinPct, perfMaxPct, unitCosts, forecastPeriods, forecastPeriodLabel, unitCostInflationPct, kpiUnitCost`) совпадают 1:1 (camelCase↔snake_case, автоконвертация Tauri) с именами параметров Rust-функции.
**Разбор:** класс CPD-38 требует, чтобы Rust ожидал ОДИН аргумент-структуру с обязательными полями, а фронт слал плоские ключи вместо вложенного объекта `payload`. У EC такой архитектуры команд нет вовсе — многоаргументные команды и так принимают «плоские» именованные параметры (это и есть их штатная, рабочая форма, не баг), а Value-команды не имеют обязательных полей на границе десериализации. Живой пример (`econ_optimize`) показал полное совпадение по всем 15 ключам.
**Уверенность:** средняя-высокая. Проверила всю кодовую базу на паттерн «структура с обязательными полями как единственный параметр команды» (0 совпадений) и один живой вызов сквозь всю цепочку сигнатура↔invoke. Не прогоняла ВСЕ ~30+ команд EC по отдельности через сравнение сигнатура/invoke (это отняло бы значительно больше времени) — ограничилась образцом с максимальным числом параметров (наиболее вероятная точка расхождения) плюс структурным поиском по всей базе, давшим ноль кандидатов на саму механику дефекта.

---

## CPD-39 — предлагаемый статус: 🔴 ПОДТВЕРЖДЁН (код байт-в-байт идентичен неисправленному состоянию Legal), достижим через обычный чат советника
**Зонд:**
1. `grep -n "fn sync_dir_to" -A 20 src-tauri/src/session/manager.rs`
2. `grep -n "sync_dir_to" src-tauri/src/session/manager.rs` — сколько вызовов подряд в один приёмник, восстанавливается ли `exports` в `open_session` наравне с `inbox`.
3. Проверка достижимости: какие кабинеты доступны в облачной сборке Econometrica (`cabinet.rs`, `online_auth.rs::detect_product`).

**Вывод зонда:**
- `session/manager.rs:545-576` (`sync_dir_to`) — зеркальная синхронизация: сначала `remove_file` всех файлов приёмника, которых нет в источнике (строки 558-565), потом копирование (567-574). Комментарий в коде («Remove files in dst that are NOT in src (deleted by user)») дословно совпадает с описанием в реестре.
- `session/manager.rs:229-256` (`open_session`) — при открытии сессии восстанавливается симметрично **только `inbox`**: строка 256 `sync_dir_to(&inbox, &work_dir.join("inbox"))`; каталог `exports` создаётся пустым на строках 231-237 и НИЧЕМ не наполняется из Desktop/exports.
- `session/manager.rs:294-311` (`sync_exports`) — зовёт `sync_dir_to` в ОДИН и тот же `dst` (`user_workspace/exports`) до 5 раз подряд: сначала из `work_dir/exports` (:302), затем из `pretensions`/`nda`/`contracts`/`reports`, если такие каталоги существуют в рабочей папке (:303-307) — каждый следующий вызов стирает из приёмника то, что положил предыдущий, если условие `src.exists()` выполняется для нескольких каталогов сразу.
- Тестов с именами `sync_dir_to`/`sync_exports` в файле не нашла (`grep -n "fn test.*sync_dir_to\|fn test.*sync_exports\|sync_dir_to\|sync_exports" src-tauri/src/session/manager.rs` — только сами определения и вызовы, ни одного `#[test]`).
- Отдельная функция `secure_delete_dir`/`WipeOutcome` (manager.rs:594-729, поправка M20) — это ДРУГОЙ механизм (безопасное затирание при очистке сессии), к `sync_dir_to`/зеркальному стиранию `exports` отношения не имеет и его не чинит.

**Достижимость:** облачная редакция Econometrica показывает ровно один кабинет — советник `econometrist` (`cabinet.rs:121` `"econometrica" => Some(&["econometrist"])`, тест `cloud_edition_exposes_econometrist_advisor` строка 613 подтверждает явно). После КАЖДОГО сообщения в обычном одиночном чате с этим советником вызывается `sync_exports` (`lib.rs:1455` в основном потоке, `lib.rs:1212` в потоковом/pipeline-ветке ответа) — то есть путь достижим через рутинное использование продукта, БЕЗ кампаний/workflow (в отличие от CPD-37 этот путь не гейтится `$isCreativeHub`).

**Сценарий поражения клиента:** советник econometrist выдал документ/файл в exports в прошлой сессии → пользователь закрыл приложение → `open_session` при следующем запуске восстановил только `inbox`, `exports` в рабочей папке пуст → первое же сообщение вызывает `sync_exports` → `sync_dir_to(workspace/exports [пусто], Desktop/exports [есть прошлые файлы])` → все файлы приёмника, которых нет в источнике, удаляются → прошлые результаты советника исчезают с Desktop клиента молча, без следа в интерфейсе.

**Адрес:** `src-tauri/src/session/manager.rs:229-256` (open_session, не восстанавливает exports), `:294-311` (sync_exports, до 5 вызовов в один dst), `:545-576` (sync_dir_to, зеркальное удаление); достижимость — `src-tauri/src/commands/cabinet.rs:121,613`; вызовы после сообщения — `src-tauri/src/lib.rs:1212,1455`.

**Родство:** тот же класс, что CPD-17 (потеря отчётов, стрелявший в Creative Center/Smart Analytica) и то же самое родство, что уже отмечено в реестре для Legal — общий скелет `session/manager.rs`.

**Живой прогон НЕ проводила** (регламент задачи — зонды, не правка/сборка приложения); статус даю по чтению кода, как и в самой записи реестра для большинства продуктов-кандидатов («живьём НЕ воспроизводился» и там). Рекомендую перед выпуском Econometrica то же самое: живой тест — выдать файл советником, закрыть приложение, открыть заново, отправить новое сообщение, проверить `exports`.

**Уверенность:** высокая по коду (100% совпадение механики и байт-в-байт совпадение имён функций/комментариев с уже подтверждённым в Legal дефектом), высокая и по достижимости (путь не требует кампаний — обычный чат советника, единственный кабинет облачной Econometrica). Не хватает только живого воспроизведения (не запускала собранное приложение).
