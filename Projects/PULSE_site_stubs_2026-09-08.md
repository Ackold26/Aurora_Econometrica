# Маячок: заглушки сайтов mmm-optimizer.pro и auroraai.pro

**Задача своими словами.** Сделать две одностраничные заглушки в стиле работающего сайта synthetic-research.pro: одну про продукт MMM Optimizer, вторую про компанию Aurora AI и линейку целиком. Текст только из готовых проверенных материалов, без цены и без форм сбора данных. Собрать и проверить локально; наружу ничего не выкладывать.

**План.**
1. Разобрать устройство монорепо-образца: sites/synthetic-research, packages/guards, оформление, подвал.
2. Прочитать исходники: ванпейджер (.docx), «Чем отличается», PORTFOLIO.md.
3. Завести sites/mmm-optimizer и sites/aurora-ai по образцу, унаследовать оформление.
4. Прогнать применимые сторожа + сборку Astro.
5. Разобраться, чем отличается путь выкладки auroraai.pro (площадка Vercel, не наш узел) – описать, не публиковать.

## Ход работы
- 02:10 – старт. Прочитана разведка SITE_RECON_mmm_optimizer_2026-09-08.md.
- 02:35 – разобрано устройство образца и все 23 сторожа. Вывод: периметр сторожей – свойство дерева (`sites/*`), новый сайт попадает под них автоматически. Часть сторожей привязана к `src/data/product-facts.ts` (решётка панели, виды исследований, срок с оговоркой) и упадёт, если завести файл с таким именем без полей SR. Решение: у заглушек файл фактуры называется иначе (`src/data/facts.ts`) – эти сторожа честно вне периметра, остальные работают.
- 02:25 – собраны оба сайта: `sites/mmm-optimizer` и `sites/aurora-ai`. Оформление унаследовано от образца (токены, шрифты, шапка, подвал, кнопки, карточки), выкинуто то, чего на заглушке нет (окно программы, приборы, меню, печатная версия).
- 02:26 – `astro check`: 0 ошибок, 0 предупреждений на обоих сайтах.
- 02:31 – сторожа: 852 проверки, из них мои сайты покрыты 16 сторожами из 23, все зелёные. Красных две – обе на чужом сайте `sites/smart-analytica`, который другой агент собирает в этом же дереве (висячие слова в его dist и отсутствие i18n в его astro.config.mjs). Мои каталоги и общие пакеты не трогала.
- 02:33 – живой прогон в браузере (свой статический сервер + playwright из соседнего дерева, общий playwright-MCP был занят другим агентом): 1440 и 390 px, обе темы, переключатель темы нажат и выбор пережил перезагрузку. Ошибок в консоли нет, незагруженных ресурсов нет, горизонтального выезда нет.
- 02:35 – зонд по auroraai.pro: домен ЖИВОЙ, отдаёт Vercel, заголовок «Aurora Analytics Suite – российская analytical platform…». Пусто там не будет – заглушка заменяет действующую страницу, это решение владельца.
- Наружу ничего не выложено: ни узла, ни Vercel, ни Cloudflare не касалась.
- 02:35 – прочитала правила соседства от руководителя работ (в дереве ещё две сессии). Приняла: правлю только `sites/mmm-optimizer` и `sites/aurora-ai`; `sites/smart-analytica` и `sites/synthetic-research` не касаюсь; сборка только фильтром по своему пакету; `packages/` и корень – через руководителя.
- 02:35 – сверка по времени файлов, а не по памяти: `sites/synthetic-research/dist/index.html` – 02:12:18, `.astro/types.d.ts` – 02:12:17, `astro.config.mjs` – 01:27:07. Мои сборки прошли в 02:25:16 и 02:29:46 – образец в обеих старше моих сборок, значит его `dist` мною не пересобирался. Общую сборку не запускала ни разу: только `--filter mmm-optimizer build` и `--filter aurora-ai build`.
- 02:35 – 🔴 попутная находка: корневой `pnpm build` в этом монорепо – это `pnpm --filter synthetic-research build`, то есть он пересобирает РОВНО боевой образец, а не всё дерево. Команда выглядит безобидно и опасна именно этим.
- 02:50 – комплект выкладки для mmm-optimizer.pro положен в СВОЙ каталог `sites/mmm-optimizer/deploy/`: nginx.conf, security-headers-mmm-optimizer.conf, 404.html, deploy.sh, README.md с порядком из трёх шагов. Общий `deploy/` не трогала.
- 02:50 – 🔴 две грабли, найденные в собственной заготовке до передачи владельцу: (1) сниппет заголовков у образца лежит по ОБЩЕМУ пути `/etc/nginx/snippets/security-headers.conf` – положить туда свой значило бы молча переписать заголовки работающего чужого сайта; развела по имени `security-headers-mmm-optimizer.conf`. (2) Общий `deploy/deploy.sh` зашит на каталог образца – запуск его с нашей сборкой переключил бы симлинк synthetic-research.pro на нашу страницу; сделала свой скрипт с предохранителем по имени домена в index.html (проверено: в своей сборке 3 совпадения, в сборке образца 0).
- 02:52 – структурная проверка конфигурации: скобки сходятся, 2 блока server, 7 location, `access_log off` и `error_log` в никуда в КАЖДОМ блоке, следов чужого сайта нет. Настоящий `nginx -t` локально не прогнать: nginx не установлен, демон docker не отвечает – поднимать его не стала, шаг 1 инструкции и так начинается с `nginx -t` на узле.
- 02:52 – 🔴 поправка к моему прежнему докладу: живая страница auroraai.pro собирается НЕ из aurora-platform-web. Ни заголовка «Aurora Analytics Suite», ни фразы из её H1 нет ни в рабочем дереве репозитория, ни в одной из 12 веток, включая main. Источник живой страницы локально не установлен – искать в кабинете Vercel. Записано в `sites/aurora-ai/deploy/README.md`.
- 02:53 – сторожа после всех правок: 28 файлов, 1118 проверок, все зелёные (соседка починила smart-analytica, красных в дереве не осталось).
- 02:56 – адрес почты заменён руководителем работ на `sales@auroraai.pro` (прежний `sales@aurora-platform.pro` оказался мёртвым: у домена нет почтовых записей). Обе заглушки пересобраны фильтром. Проверка по собранному: `aurora-platform` – 0 вхождений в обоих dist, `sales@auroraai.pro` – 5 и 4, единственная ссылка mailto ведёт на новый адрес, видимый текст страниц содержит только его.
- 02:57 – убрала в своих двух `site-config.ts` ставшую неверной фразу «адрес взят из материалов продаж»: она противоречила красному блоку двумя строками ниже. Адрес и решение владельца не трогала. Обновила адрес в своих `deploy/README.md` – там он назывался в списке того, что осталось на владельце.
- 02:57 – `astro check` 0/0/0 на обоих; живой прогон 1440 и 390 px, обе темы – ошибок нет, выезда нет.
- 02:58 – 🔴 сторожа: 1117 зелёных, 1 красный – чужой `sites/aurora-ai-store` (слово «platform» в его собранной странице, которого больше нет в его исходниках). Диагноз: его dist собран ДО правки адреса. Прямой сверкой нашла, что мёртвый адрес остался в собранных страницах ТРЁХ соседних сайтов – creative-center (5), legal-center (5), aurora-ai-store (4), – а сторож покраснел только на одном: у остальных адрес живёт в атрибуте ссылки, а не в видимом тексте, и детектор склеенных слов его не видит. Чужие каталоги не трогала.

## Итог смены (03:05, работа принята)

Сделано и принято руководителем работ: две заглушки (`sites/mmm-optimizer`, `sites/aurora-ai`) + комплект выкладки для mmm-optimizer.pro + порядок действий для площадки Vercel по auroraai.pro. Наружу не выложено ничего: ни узла, ни Vercel, ни Cloudflare не касалась. Чужие каталоги (`synthetic-research`, `smart-analytica`, `creative-center`, `legal-center`, `aurora-ai-store`) и общие пакеты не трогала; единственный след вне своих папок – строки новых пакетов в `pnpm-lock.yaml`.

Состояние на момент закрытия: обе заглушки собраны с адресом `sales@auroraai.pro`, `astro check` 0/0/0, сторожа зелёные, мёртвого адреса нет ни в одной собранной странице дерева (перепроверено руководителем по всем семи каталогам).

**Урок смены, принят в правила руководителем:** зелёный прогон сторожей честно отвечает на СВОЙ вопрос и молчит о соседнем слое. Мёртвый адрес почты остался в трёх собранных страницах, а покраснела одна – та, где адрес стоял в видимом тексте; у двух других он жил в атрибуте `mailto:`, куда детектор склеенных слов не смотрит. Надёжной проверкой оказался не сторож, а прямой поиск по собранному (`grep` по dist). Второе следствие того же вечера: правка исходника без пересборки до страницы не доезжает – «закрыто» ≠ «доставлено».

Что осталось на владельце (полный список – в `sites/mmm-optimizer/deploy/README.md` и `sites/aurora-ai/deploy/README.md`): перенос выкладки на узел и сертификат для mmm-optimizer.pro; поиск проекта Vercel и выбор пути для auroraai.pro; живое письмо на sales@auroraai.pro; решения по цене, счётчику статистики и публикации состава линейки.
