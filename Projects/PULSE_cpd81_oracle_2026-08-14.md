# Пульс: CPD-81 в Oracle (2026-08-14)

## Задача (своими словами)
Класс дефекта CPD-81 – отброшенный результат `fs::copy` (`let _ = ...`), при котором имя файла всё равно попадает в список «сохранено», источник затем удаляется, потеря невидима. В Oracle нужно найти ВСЕ носители (минимум два известны машинной проверке: `campaign.rs:474`, `campaign.rs:494`), плюс собственным поиском проверить третий тип носителя (копирование файлов брифа кампании) и любые другие места без `if src.exists()`. Для носителей с необратимостью – полный эталон (`{forwarded/copied, failed, fatal}` + closed create_dir_all/read_dir/entry.file_type()); для носителей без необратимости – честный список без fatal. Работать в отдельной рабочей копии (git worktree), тесты с доказательством мутацией, отчёт в FIX_CPD81_ORACLE_2026-08-14.md.

## План
1. Маячок создан (этот файл).
2. Разведка: состояние `D:\Docs\Aurora_Ai\Dev\Aurora_Oracle` (ветка, незаписанные файлы), теги релизов, эталон CARGO_TARGET_DIR.
3. Создать git worktree `_wt_cpd81_oracle`, ветка `cpd81-oracle-fix` от актуального тега/ветки.
4. Поиск носителей: `let _ = std::fs::copy`, `fs::copy`, grep по campaign.rs / brief, любые безусловные добавления в список.
5. Классифицировать каждый носитель (полный класс / без необратимости / не тот класс).
6. Написать/поправить код по эталону.
7. Тесты для каждого носителя + доказательство мутацией.
8. Прогон полного набора тестов до/после.
9. Написать отчёт FIX_CPD81_ORACLE_2026-08-14.md.
10. Финальное сообщение team-lead.

## Отметки
- [начало] Пульс создан, начинаю разведку.
- Разведка завершена. Ветвление: `cpd77-oracle-fix` (не master!) – это выпускная ветка с CPD-77, содержит тег v0.4.5-oracle как предка и коммит «Версия 0.4.7» – самое свежее состояние Oracle, не занята другим worktree. Master стоит только на v0.4.2 (устарел). Worktree `D:\Docs\Aurora_Ai\Dev\_wt_cpd81_oracle`, ветка `cpd81-oracle-fix`, HEAD da3ee45. Начинаю поиск носителей CPD-81.
- Найдено 3 носителя в `campaign.rs`: 474 `persist_step_exports` (полный класс, необратимость – exports_dir удаляется close_session после), 494 `forward_exports_to_inbox` (пара, необратимости своей нет, но эталон закрывает и её), 527 `campaign_set_brief` (третий носитель – файлы брифа, без необратимости – источник остаётся на диске клиента). Прочитан эталон Econometrica (5c34d7a – пара функций, 79e3fad – copy_brief_files). Код перенесён 1:1 с адаптацией под структуру Oracle (mutex `unwrap_or_else` вместо `unwrap`). Обновлён вызывающий код в lib.rs (2 места: forward + persist). Добавлены тесты по эталону (без двух тестов про переименование/коллизию – у Oracle никогда не было unique_export_path, вне области CPD-81). Дальше – прогон базлайна и правленого кода, доказательство мутацией.
- Python-каталог (gitignored) отсутствовал в свежем worktree – скопирован robocopy /E из основного дерева (2267 файлов), сборка пошла.
- Базлайн (git stash): 302 passed / 0 failed / 1 ignored. После правки: 312 passed / 0 failed / 1 ignored (10 новых тестов). Доказательство мутацией по всем трём носителям – каждый упал на дефектном образце (persist: assertion failed result.copied.is_empty(); forward: assertion failed result.forwarded.is_empty(); brief: left ["занятый.docx","обычный.md"] vs right ["обычный.md"] – точное совпадение с эталоном). cargo build --lib чисто (4 предсуществующих warning, не мои файлы).
- ЗАВЕРШЕНО. Запись 4c81b9c91adbafc97a1346fd2308a0249c5b0425 в ветке cpd81-oracle-fix (git add поимённо, без -A). Отчёт FIX_CPD81_ORACLE_2026-08-14.md написан. Отправляю итог team-lead.
