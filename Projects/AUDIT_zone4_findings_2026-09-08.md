# Внешний аудит, зона 4 — 2026-09-08

Аудитор с чистым контекстом. База: `73a13265..HEAD`. Участки A–E (ранее не проверялись ничем).
Код не правился, git не трогался. Зонды лежат в
`C:\Users\ackol\AppData\Local\Temp\claude\D--Docs-Aurora-Ai\04beb1a8-eb7a-46ed-b3ec-a7cd3a9e41f3\scratchpad\zone4\`
(`probe_a.py`, `probe_c.py`, `probe_c2.py`, `probe_c3.py`) — запускать из `sidecar/econometrica`.

Формат: `severity | файл:строка | суть | воспроизводимый сценарий`

---

## Находки

### High

**High-1 | sidecar/econometrica/engines/scenario.py:86-108 (`_looks_like_binary_flag`) + :191-199 | Защита масштаба проверяет НЕ ТУ сторону равенства: убедившись, что КЛИЕНТСКАЯ колонка — признак 0/1, код подставляет каноническую дамми, построенную в режиме `fraction` (доля дней периода) и по КАНОНИЧЕСКОМУ окну события. Ни шкала, ни сроки не совпадают с тем, на чём оценена β.**

Доказано исполнением на боевом демо-наборе планирования
`static/sample-data/planning/synth_retail_ecom.xlsx` (зонд `probe_c3.py`). В наборе есть
колонка «Чёрная пятница» со значениями строго 0/1, месячная сетка (даты — концы месяцев),
60 строк истории + 18 строк будущего. Обучающие статистики этой колонки:
`control_means = 0,083333`, `control_stds = 0,278718` → `_looks_like_binary_flag` = **True**,
защита пропускает подстановку.

При β = 0,30 и `y_std` = 1 000 000 ₽ (значения синтетические — важна ФОРМА и относительный
масштаб, они от β и y_std не зависят):

| период | канон. дамми | вклад факт | вклад эталон (дамми=1, как при обучении) |
|---|---|---|---|
| 2026-11-30 | 0,133 | **53 782 ₽** | **986 661 ₽** |
| 2026-12-31 | 0,323 | **+257 536 ₽** | −89 696 ₽ |
| остальные 16 | 0,000 | −89 696 ₽ | −89 696 ₽ |

То есть ноябрьский вклад события — **5,5 % от того, что следует из истории**, а масса события
переезжает в декабрь (в истории клиент отмечал единицей именно ноябрь). Сумма вклада за 18
будущих периодов: факт −1 123 825 ₽ против эталона −538 179 ₽, **систематический сдвиг прогноза
вниз на 585 646 ₽**, тогда как до правки этот столбец давал ровно ноль и сдвига не было.

Причина в одной строке: `holiday_df = generate_holiday_dummies(date_series, mode=holiday_mode)`
(scenario.py:151), где `holiday_mode` берётся из `norm['holiday_dummies_mode']`, а обучение
проставляет туда `'fraction'` (modeler.py:1758, инжект — modeler.py:496 `mode='fraction'`).
Проверка двоичности клиентской колонки никак не связана с тем, чем её потом заменяют.

Достижимость: полностью на живом пути показа — файл лежит в комплекте примеров кнопкой
«Ритейл / e-com» в блоке планирования. На четырёх остальных примерах праздничных колонок нет,
там подстановка не срабатывает вовсе.

---

**High-2 | sidecar/econometrica/engines/scenario.py:147 (`holiday_mode = norm.get('holiday_dummies_mode', 'binary_point')`) | На модели без ключа `holiday_dummies_mode` (старый pickle) подстановка даёт дамми, тождественно равную нулю во ВСЕХ будущих периодах: событие исчезает целиком, остаётся постоянный отрицательный сдвиг в каждом периоде — ровно тот дефект, ради устранения которого правку делали.**

Тот же зонд `probe_c3.py`, ветка `mode=binary_point`. В этом режиме дамми ставится по
попаданию САМОЙ ДАТЫ СТРОКИ в окно события (`holiday_calendar_ru.py:357`), причём по
укороченному окну `date_range_v20` (28–30 ноября, 3 дня). На месячной сетке дата строки —
конец месяца (30.11, 31.12), в окно не попадает никогда:

| период | канон. дамми | вклад факт | эталон |
|---|---|---|---|
| 2026-11-30 | 0,000 | **−89 696 ₽** | **+986 661 ₽** |
| все прочие 17 | 0,000 | −89 696 ₽ | −89 696 ₽ |

Итог за 18 периодов: −1 614 536 ₽ против эталона −538 179 ₽, расхождение −1 076 357 ₽, и знак
вклада события противоположен историческому. Умолчание `'binary_point'` поставлено намеренно
(комментарий modeler.py:1756: «старые pickle без ключа»), так что срабатывает на любой модели,
обученной до v2.1, и на любом pickle, где ключ потерян.

---

**High-3 | src/lib/project-state.js:1613-1624 (`loadPipelineForProject`) и :1682-1699 (`resetForNewAnalysis`) против :1651-1679 (`resetResultsKeepImport`) | Три стора чужого проекта снимаются ТОЛЬКО на пути «создать проект». На переключении между проектами, на импорте архива `.aurora` и на кнопке «Новый анализ» они переживают смену — и продукт выносит по конфигурации проекта А вердикт о проекте Б.**

Доказательство — отсутствие присваиваний: единственные места, где эти сторы вообще
устанавливаются, — `ConfigPanel.svelte:514` (`lastTrainedConfig` при обучении),
`OptimizeStep.svelte:527/537` (`forecastContext`), `OptimizeStep.svelte:903`
(`optimizeLiveState`) и `project-state.js:1664-1672` (сам сброс в `resetResultsKeepImport`).
Ни `loadPipelineForProject`, ни `resetForNewAnalysis` их не трогают. Пути без сброса:
`ProjectSelector.svelte:89-93` (`selectProject` → `resetPipeline` → `loadPipelineForProject`),
`ProjectSelector.svelte:202-212` и `ImportStep.svelte:49-59` (импорт `.aurora`),
`routes/+page.svelte:390` («Новый анализ» → `resetForNewAnalysis`).

Сценарий 1 — ложный вердикт «модель устарела». В одной сессии: обучить проект А (KPI «Выручка»,
7 медиа, 3 контрольных) → через `ProjectSelector` переключиться на ранее обученный проект Б
(KPI «Упаковки», 5 медиа, 2 контрольных; на диске есть `results/validation.json` и
`results/model-diagnostics.json`). `restoreProjectResults` поднимает диагностику и валидацию Б,
`lastTrainedConfig` остаётся от А → `modelStaleStatus` (project-state.js:1101-1173) сравнивает
роли Б с конфигурацией А, получает непустой `diff`, и на шаге «Декомпозиция»
(DecomposeStep.svelte:502-515) выводится баннер «**Декомпозиция устарела. Конфигурация
изменилась после обучения… Переобучите модель**» со списком расхождений вида «KPI: «Выручка» →
«Упаковки», Медиа-каналы: было 7, стало 5». Модель Б не менялась, вердикт ложный.
Защита `reconstructed_from_project_json` (:1110) здесь не работает — она гасит баннер только
когда `validation.json` отсутствует.

Сценарий 2 — чужой контекст прогноза. Проект А в режиме планирования, обучен на неделях с
обнаруженной сезонностью → переключиться на проект Б без модели, не выходя из режима
планирования. `$effect` OptimizeStep.svelte:522-539 перезапрашивает контекст для Б,
`econ_forecast_context` отвечает отказом (модели нет), ошибка гасится пустым `catch` (:538) →
`forecastContext` остаётся от А, и `ForecastHorizonPicker` (:36, :41, :54, :171, :182) показывает
для Б гранулярность обучения А, предельный горизонт от длины ряда А и «сезонность обнаружена,
период N» от модели А.

Сценарий 3 — коридоры и бюджеты каналов А (`optimizeLiveState`) остаются входом `InsightsPanel`
до первого расчёта на Б; для каналов, которых у Б нет, они просто повисают в сторе.

---

### Medium

**Medium-1 | sidecar/econometrica/engines/validator.py:449-470 (четыре ранних выхода) против :546-549 (запись состояния) | Договор «состояние пишется при ЛЮБОМ исходе проверки данных» не выполняется на четырёх путях: `results/media_plan.json` прошлых данных переживает новую проверку, и `media_plan_absent()` отвечает «план есть» на данных, где плана нет.**

Доказано исполнением, зонд `probe_a.py`. В каталог проекта кладётся файл с
`n_future_periods: 3, confirmed: true`, затем вызывается `validate_data` на шести входах:

| вход | validate | `media_plan.json` после | `media_plan_absent()` |
|---|---|---|---|
| файла нет | error | 🔴 остался старым (3) | **False** («план есть») |
| `.txt` (неподдерж. формат) | error | 🔴 остался старым (3) | **False** |
| битый `.xlsx` | error | 🔴 остался старым (3) | **False** |
| пустой `.csv` | error | 🔴 остался старым (3) | **False** |
| нормальный без хвоста | warning | перезаписан (0) | True |
| нормальный с хвостом | warning | перезаписан (3) | False |

Список ранних выходов из самопроверки полон — их ровно четыре; замечу лишь, что «пустой файл»
попадает не в свою ветку (`n_cols == 0`, :470), а раньше — в «Ошибка чтения файла: No columns
to parse from file» (:462), потому что pandas падает до неё.

Живой сценарий: проект с данными, где хвост плана найден (например пример «Ритейл / e-com» из
блока планирования) → на шаге «Импорт» выбрать файл, который движок не прочитает, либо просто
переместить или переименовать ранее импортированный файл и переоткрыть проект (`imp.file` —
внешний путь, ValidateStepV13.svelte:242 зовёт `econ_validate` именно по нему) → проверка
падает ранним выходом → на диске остаётся описание ПРОШЛЫХ данных, и всякий последующий
`optimize` в этом каталоге получает `media_plan_absent = False`.

Атомарность самой записи нареканий не вызвала — см. раздел «Проверено и чисто», пункт 2.

---

**Medium-2 | src/lib/components/pipeline/DecomposeStep.svelte:463-469 (новый `$effect`) | Правка реактивности односторонняя: `idle → done` пересчитывается, обратный переход `done → idle` не предусмотрен. При смене проекта шаг «Декомпозиция» остаётся в состоянии `done` с пустыми данными и не рисует НИЧЕГО — тот самый пустой экран, ради которого ветку `idle` и заводили.**

Механизм: `stepState` — локальный `$state` (:75), а компоненты всех шагов живут в DOM постоянно
(`routes/pipeline/+page.svelte:81-120`, переключение видимостью, без `{#key}`), и
`ProjectSelector` находится внутри того же макета (`routes/pipeline/+layout.svelte:378`) —
значит смена проекта НЕ перемонтирует `DecomposeStep` и не сбрасывает `stepState`. Все
присваивания `stepState` (строки 329, 331, 355, 383, 400, 409, 423, 434, 439, 444, 467) находятся
внутри `onMount`, `runDecompose`, `handleError` и двух `$effect`; ни одно не реагирует на
обнуление `decomposeData`.

Сценарий: открыть проект А с посчитанной декомпозицией, перейти на шаг «Декомпозиция»
(`stepState = 'done'`, графики видны) → в шапке выбрать проект Б, у которого декомпозиции нет →
`resetPipeline` → `loadPipelineForProject` (:1621) делает `decomposeData.set(null)` → `data`
(`$derived`, :79) становится null → блок результатов `{#if stepState === 'done' && data}` (:549)
не рисуется, ветка `{#if stepState === 'idle'}` (:541) не рисуется тоже, потому что состояние
осталось `done`. На экране — только заголовок шага. Новый `$effect` не спасает: его условие
`if (d && stepState === 'idle')`.

---

**Medium-3 | src/lib/project-state.js:1247 (`restoreProjectResults`) + :1370 (подписка) | У асинхронного восстановления нет проверки актуальности: после `await` никто не сверяет, что проект всё ещё активен. Результаты проекта А могут доехать в контекст проекта Б — дефект «новый проект показывает результаты предыдущего» возвращается через гонку.**

`async function restoreProjectResults(pid)` делает `await invoke('project_load_results', …)` и
затем безусловно раскладывает ответ по сторам; ни в одной точке нет
`if (get(activeProjectId) !== pid) return`. Внутри есть ещё один `await`
(`hydrateRolesFromProjectIfEmpty`, :1285), расширяющий окно.

Сценарий: выбрать в шапке проект А с крупными `results/decomposition.json` и
`results/optimization.json` (запрос ушёл, ответ ещё в пути) → не дожидаясь отрисовки, создать
новый проект Б (`ProjectSelector.createProject`, :132-143): `activeProjectId.set(Б)` поднимает
второе восстановление, следом синхронно отрабатывает `resetResultsKeepImport()`, а последним
резолвится обещание для А и кладёт в сторы модель, разбор и оптимизацию проекта А. Экран
проекта Б показывает результаты А, `reconcileStepMetaFromDisk` доводит отметки шагов до
«завершено». Гонка редкая, но именно её симптом уже наблюдали в живом прогоне.

---

### Low

**Low-1 | sidecar/econometrica/engines/scenario.py:105-108 | Допуск защиты в четверть от ожидаемого разброса пропускает колонки вида 0/k при k ≈ 0,7 — это не признак 0/1, но проверку он проходит.**
Зонд `probe_c.py`, случай 4: колонка со значениями 0 и 0,7 (ноябрь), 24 месяца → `mean = 0,0583`,
`std = 0,1976`, `_looks_like_binary_flag` = **True**, подстановка выполняется и вклад считается.
Формы 0/2 и «рубли» защита отсекает корректно (случаи 2 и 3 того же зонда), константная колонка
и колонка без сохранённых статистик — тоже.

**Low-2 | src/lib/project-state.js:1266 | Третье честное состояние («определить не удалось», `n_future_periods: null`), которое писатель специально ввёл, фронтовый читатель сводит к «плана нет»: `(r.mediaPlan.n_future_periods ?? 0) > 0`. Различие, ради которого правку делали, на стороне интерфейса теряется.**
Питонов читатель (`optimizer.media_plan_absent`) третье состояние держит честно — см. «Проверено
и чисто», пункт 1. Практический эффект ограничен: при `unavailable` шаг «Планирование» молча
остаётся запертым (`reconcileStepMetaFromDisk`, :1325-1327), продукт об этом не сообщает.

**Low-3 | src/lib/project-state.js:1651-1679 | `mediaPlanDetected` не снимается в `resetResultsKeepImport` — расчёт на то, что асинхронный `restoreProjectResults` поставит его в null. Если `project_load_results` отклонится (перехват :1298 «Silent»), медиаплан прошлого проекта останется в сторе нового.**

---

## Проверено и чисто

1. **B — чтение признака в `optimizer.media_plan_absent` (optimizer.py:267-301).** Зонд
   `probe_a.py` прогнал все четыре состояния диска: файла нет → `None`; `n_future_periods: null`
   → `None`; `0` → `True`; `3` → `False`. Молчаливого умолчания нет, третье состояние ни к
   одному из двух других не сводится. Сверила всех потребителей признака — `optimizer.py:1569`,
   `narrative_adapter.py:811`, `aurora_html/sections.py:698` и `:1562`, `OptimizeStep.svelte:2323`:
   везде строгое сравнение `is True` / `=== true`, «не знаю» нигде не превращается в «плана нет».
   **Дефекта нет.** Рассинхрона имени ключа между писателем и читателем нет: обе стороны работают
   с `results/media_plan.json` и полем `n_future_periods`, отдельного ключа `media_plan_absent`
   на диске не существует.

2. **A — атомарность записи (validator.py:420-442).** `mkstemp` в тот же каталог + `os.replace`
   (на Windows атомарная подмена, не `rename`), файловый дескриптор закрыт до подмены, кодировка
   задана явно (`encoding='utf-8'`), временный файл удаляется при сбое, старый файл при неудачной
   записи отводится в `.stale`. Порчи существующего файла при исключении внутри записи зонд не
   показал: после всех шести прогонов файл всегда либо старый целый, либо новый целый.
   **Дефекта нет.**

3. **A — полнота списка ранних выходов.** Между входом в `validate_data` и вызовом
   `_write_media_plan_state` ровно четыре `return` (:449, :459, :462, :470); все четыре
   подтверждены исполнением. Пятого пути нет.

4. **C — законный случай не срезан.** Колонка с каноническим именем (`holiday_black_friday`)
   идёт мимо защиты (`is_substitution = False`) и считается как прежде — зонд `probe_c.py`,
   случай 8, значения совпадают с рабочим путём до правки. Колонки в рублях (случай 2), значений
   0/2 (случай 3), сплошь нулевая (случай 5) и колонка без сохранённых статистик отсекаются с
   записью в журнал. Пропуски NaN защите не мешают (случай 6): статистики считаются по
   заполненным, колонка остаётся распознанной как 0/1.

5. **C — двойной счёт одного события.** Разбор претендентов (scenario.py:219-233) проверен
   чтением: при двух колонках на одно событие берётся та, чьё нормализованное имя точно равно
   каноническому, остальные отбрасываются; при отсутствии точного владельца отбрасываются все.
   Регрессии против прежнего поведения нет.

6. **D — очистка ключа localStorage.** `lastTrainedConfig.set(null)` действительно стирает
   `aurora-last-trained-config` с диска: подписка project-state.js:1082-1087 вызывает
   `localStorage.removeItem`. На пути создания проекта ключ не остаётся. (На путях переключения
   и «Нового анализа» он не стирается — это High-3.)

7. **D — гонка синхронного сброса и `restoreProjectResults` на пути СОЗДАНИЯ проекта (та, что
   была Medium в прошлом заходе) закрыта.** После того как `resetResultsKeepImport` стал снимать
   и импорт тоже, оба исхода гонки сходятся: `defaultStepMeta()` (:105-115) даёт
   `[ready, locked × 6]`, и `reconcileStepMetaFromDisk` на пустом проекте даёт ровно то же.
   Расхождения, которое раньше показывало «файл загружен» при «Импорт не пройден», больше нет.

8. **D — прочие пути создания и переключения проекта.** Найдены и проверены все:
   `ProjectSelector.createProject` (:132, сброс есть), автосоздание проекта при импорте файла
   `ImportStep.svelte:337-355` (сброса нет, но путь заперт условием `if (!get(activeProjectId))` —
   чужого состояния в этот момент быть не может, кроме трёх сторов из High-3), импорт архива
   `.aurora` в двух местах (`ProjectSelector.svelte:202`, `ImportStep.svelte:49`) и
   `selectProject` (:87). Отдельной команды «дублировать проект» в интерфейсе нет —
   `project_duplicate` в коде отсутствует.

9. **E — признак «модель есть» в ветке `idle`.** Условие
   `$modelData?.channelParams || $pipelineStepMeta?.[2]?.status === 'complete'`
   (DecomposeStep.svelte:530) действительно шире, чем наличие модели в памяти, и это сделано
   осознанно (комментарий :531-538), а текст ветки честно предупреждает: «Если файла модели не
   окажется на месте, она об этом скажет». Прошлую находку считаю снятой по существу — ложного
   утверждения «модель обучена» экран больше не делает, он предлагает попробовать и обещает
   сообщить об отказе.

10. **E — ветка `idle` при позднем приезде разбора с диска.** Новый `$effect` (:463-469) переводит
    шаг в `done`, как только `decomposeData` появляется в сторе — заявленный сценарий (открыть в
    новой сессии проект с готовым `results/decomposition.json`) закрыт. Односторонность правки —
    Medium-2.

---

## До чего не дошла

- **Живого прогона собранной программы не делала** — сборка запрещена условиями, а зонды вызывают
  функции движка напрямую. Все числа в High-1 и High-2 получены на реальном файле примера, но с
  синтетическими β = 0,30 и `y_std` = 1 000 000 ₽: обучать модель ради точных рублей — часы.
  Форма дефекта (доля 5,5 %, перенос массы события в декабрь, тождественный ноль на старом
  pickle) от β и `y_std` не зависит.
- **High-3 и Medium-2, Medium-3 доказаны чтением, а не исполнением.** Это утверждения об
  ОТСУТСТВИИ кода (нет присваивания стору, нет проверки актуальности, нет ветки перехода), и я
  подкрепила их исчерпывающим перечнем всех мест присваивания. Прогнать их зондом означало бы
  поднимать окружение Svelte с заглушками Tauri и класть новые файлы в дерево проекта — запрещено
  условиями «код не править».
- **Не проверяла** `engines/planning.py::confirm_media_plan` и поведение поля `confirmed` при
  повторной проверке данных (запись всегда ставит `confirmed: false`) — это соседняя тема, за
  границей участков A–E; беглый разбор показал, что интерфейс в этом случае переспрашивает
  пользователя, то есть молчаливой лжи нет.
- **Не проверяла** режим `fraction` на дневной сетке (там он совпадает с `binary_point` по
  построению) и поведение при пустом `future_dates` (ранний возврат нулей).
