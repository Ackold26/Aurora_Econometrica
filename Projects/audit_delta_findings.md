# Находки аудита дельты 09.08

High | execution_mode.rs:234 (+ claude.rs:411) | `cmd /C` получает путь отдельным аргументом; Rust квотирует аргумент только при пробеле/табе, а `&`, `^`, `|` — нет, и cmd их разбирает как операторы | Имя учётной записи Windows допускает `&` (запрещены только `" / \ [ ] : ; | = , + * ? < >`). Пользователь `A&B` → путь `C:\Users\A&B\AppData\Roaming\npm\claude.cmd` без пробелов → без кавычек → `cmd /C C:\Users\A & B\AppData\...\claude.cmd --version` → exit != 0 → «Claude Code найден, но не запускается» → автоопределение уводит в облако при рабочей установке | Маршрутный инвариант ADR-049 §4 (ложное «локально недоступно»), плюс исполнение произвольной команды из имени пользователя

Medium-High | claude.rs:1432 | Жёсткий приоритет `claude.exe` меняет, КАКАЯ установка запускается в бою, а не только резолвится в зонде | На машине две установки: нативная `%USERPROFILE%\.local\bin\claude.exe` (старая, без выполненного входа) и рабочая npm `%APPDATA%\npm\claude.cmd`. До правки резолвился npm-путь, после — `.exe`. `--version` у него отвечает успехом (вход не проверяется, см. док-комментарий execution_mode.rs:210-212) → «локально доступно» → реальный прогон падает отказом входа | Зонд обязан доказывать запускаемость ИМЕННО того файла, который пойдёт в бой; смена боевого файла — необъявленный побочный эффект правки зонда

Medium | claude.rs:1456 + 1434 | `find_claude_binary` резолвит через `where` — это команда Windows; на macOS/Linux `Command::new("where")` даёт ENOENT, функция всегда возвращает Err. Ветка `else { &["claude"] }` и фраза «на остальных системах путь запускается как есть» декоративны | Сборка под macOS/Linux, `claude` установлен через npm и есть в PATH → `local_available()` всегда `(false, "Claude Code на этой машине не найден")` → локальный режим недоступен навсегда | Кроссплатформенный резолв; комментарий описывает поведение, которого нет

Medium | claude.rs:1468 | Ложное «недоступно» сохраняется для установок вне пяти доверенных префиксов — правка этот путь не закрывает | Claude Code установлен в `D:\tools\claude.exe` (не APPDATA/LOCALAPPDATA/USERPROFILE/PROGRAMFILES) → все три кандидата отбракованы → «Claude Code на этой машине не найден» → облако при полностью рабочем локальном Claude Code | Тот же маршрутный инвариант, что чинила правка

Low-Medium | claude.rs:1414-1415, execution_mode.rs:637-638 | Оба сторожа под `#[cfg(windows)]`; тест порядка проверяет `candidate_names()` против самого себя и не связан с `find_claude_binary` | Сборка/прогон тестов на Linux (CI) — оба теста вырезаны, регрессия порядка проходит зелёной. Отдельно: возврат в `find_claude_binary` собственного массива в обход `candidate_names()` оставляет оба теста зелёными | Сторож обязан краснеть на том дефекте, который он охраняет
