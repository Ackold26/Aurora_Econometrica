# Маячок — карта интерфейса для сценария показа (2026-08-31)

## Пересказ задачи своими словами
Через неделю владелец показывает Aurora AI Econometrica (MMM Optimizer) потенциальному покупателю. Нужна точная карта пути пользователя от старта до готового отчёта — все подписи кнопок, полей и шагов дословно из кода фронтенда (SvelteKit, `src/`), без придумывания. Особый фокус на четыре опорных места: импорт данных (кнопки-примеры), ввод валовой маржи, шаг оптимизации (профит-фронтир, таблица каналов), шаг отчёта (кнопки выгрузки). Плюс — где видны показатели качества модели и предупреждение об ориентировочности рекомендаций.

Работа только на чтение. Программа сейчас запущена (порт 7529) — не трогать.

## План
1. `src/routes/+page.svelte` — стартовый экран.
2. `src/lib/components/pipeline/` и `src/routes/pipeline/` — список шагов мастера, найти навигацию/индикатор прогресса.
3. Пройти по каждому компоненту шага, выписать дословные подписи (кнопки, поля, подсказки, результаты).
4. Углубиться в 4 опорных места: ImportStep.svelte, StepTargetConfirm (или аналог), OptimizeStep+ProfitFrontierCard, ReportStep.
5. Найти индикаторы качества модели и предупреждение об ориентировочности.
6. Собрать отчёт таблицей + разбор опорных мест.

## Отметки времени
- 23:16 — старт, маячок создан, начинаю разведку структуры каталогов.
- 23:20 — прочитаны ключевые файлы (+page.svelte, pipeline/+layout.svelte, pipeline/+page.svelte, PipelineStepper, ImportStep, StepTargetConfirm, ProfitFrontierCard, OptimizeStep, TrustBanner, ReportStep, MQSBadge, ModelTrainingStep, ConvergenceDashboard, BacktestCard, ConfigPanel, DecomposeStep, PlanningStep, channel_action.py). Пишу отчёт.

## Отчёт

См. итоговый текст, отправленный team-lead через SendMessage (дословные подписи из кода, таблица шагов + разбор 4 опорных мест).

