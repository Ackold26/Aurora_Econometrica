# Пульс — эталонный демо-проект Econometrica (2026-08-31)

## Пересказ задачи
Собрать в уже запущенной программе Aurora AI Econometrica (движок на 127.0.0.1:7529) эталонный проект-демонстрацию на синтетических данных FMCG-бренда (synth_fmcg_brand.xlsx). Нужно обучить модель с фиксированным зерном, прогнать декомпозицию, оптимизацию бюджета и профит-фронтир (маржа 38%), выгрузить параметры и html-отчёт, и честно проверить след на диске — не просто код 200 от API.

## План
1. Маячок создан (этот файл).
2. Чекпоинт: прочитать project.rs / user_config.rs / econ_sidecar.rs — куда программа кладёт проекты; прочитать modeler.py — умолчания сэмплера, прикинуть время обучения.
3. Написать ведущей (SendMessage to team-lead) путь сохранения, зерно, ожидаемое время. Ждать ответ до 10 минут.
4. Запустить обучение (POST /compute/train/start), следить за прогрессом, забрать результат.
5. Прогнать decompose, optimize, profit-frontier (маржа 38%), export/params, export/html.
6. Проверить след на диске: каталог проекта, файл модели, паспорт воспроизводимости, видимость в реестре проектов программы.
7. Записать отчёт в этот файл.

## Отметки
- 22:12 — старт, маячок создан, план готов.
- (без проверки часов) — движок жив (`GET /health` → status ok, версия 2.5.0, PID 47244, JAX 0.7.2 + numpyro 0.20.1 установлены). Прочитаны `project.rs`, `user_config.rs`, `modeler.py`, `kpi_registry.py`, схема `TrainStartRequest` из `/openapi.json`. Факты чекпоинта записаны ниже. Отправляю сообщение ведущей, жду ответ до 10 минут.

## Факты чекпоинта (ШАГ 1)

**Куда программа складывает проекты.**
`projects_dir()` (project.rs:141) — приоритет: env `AURORA_PROJECTS_ROOT` > `user_config.econometrica_projects_root` (файл `%APPDATA%\com.aurora.econometrica\user_config.json`) > дефолт `%APPDATA%\com.aurora.econometrica\projects\`.
Проверено фактически: `%APPDATA%\com.aurora.econometrica\` на этой машине существует (brands/, vaults/, instance.id и т.д.), но `user_config.json` там ОТСУТСТВУЕТ и подпапки `projects\` тоже ещё нет → действует чистый дефолт: **`C:\Users\ackol\AppData\Roaming\com.aurora.econometrica\projects\`**.
Идентификатор пакета — `com.aurora.econometrica` (из `tauri.conf.json`), имя crate — `aurora-econometrica-gui` (не участвует в пути).

Структура проекта (`project_create`, project.rs:271-333) — папка `<projects_dir>/<project_id>/` с подпапками `data/`, `models/`, `results/`, `results/scenarios/`, `exports/` + файл `project.json` (сериализация `ProjectInfo`). `project_list` (project.rs:255) просто читает КАЖДУЮ подпапку `projects_dir()` и парсит в ней `project.json` — значит программа увидит проект, если такая структура и валидный `project.json` появятся на диске, даже без прохода мастера создания в интерфейсе.
`id` проекта — slug от имени (lowercase, non-alphanumeric → `-`, обрезка дефисов по краям).

**Как обучение пишет результат.** Схема `TrainStartRequest` (`/openapi.json`) требует `project_dir` + `data_file` как явные строки-пути — движок сам читает/пишет по этим путям (не Rust-прослойка), значит мне нужно передать абсолютный путь к папке проекта и к скопированному в неё файлу данных.

**Сколько займёт обучение.** `get_mcmc_params()` (modeler.py:117-147): если numpyro/JAX доступны (подтверждено `/health` — да) → **Tier-1 NumPyro NUTS, 4 цепи × 2000 draws × 2000 tune** (дефолт, bump 2026-04-19). Комментарий в коде — «на JAX/NUTS секунды» для самого сэмплинга, но первый прогон добавляет JIT-компиляцию JAX-графа; с holiday-контролями (~12 авто-факторов) + сезонностью + 4 медиа + 3 контроля на 104 строках истории оцениваю **реалистично 3-8 минут** (не секунды — это оптимизм комментария о чистом сэмплинге без компиляции).

**kpi_type.** Из `kpi_registry.py`: денежный KPI (`kpi_kind='monetary'`) — `sales` / `revenue` / `profit`. Для столбца «Выручка» беру `kpi_type='revenue'` (алиас `sales`, та же семантика, но имя точнее описывает данные).

**Данные.** Скопирую `synth_fmcg_brand.xlsx` (лист «Данные», A1:M131) в `<project_dir>/data/synth_fmcg_brand.xlsx`, передам этот путь как `data_file`.

**Предложение (жду подтверждения ведущей 10 минут):**
- project_id / папка: `demo-fmcg-brand`
- zерно (seed): **42**
- kpi_column: «Дата»→date_column, «Выручка»→kpi_column, kpi_type=«revenue»
- media_columns: «ТВ бюджет», «Онлайн-видео бюджет», «Наружная реклама бюджет», «Performance бюджет» (беру денежные бюджеты, не физические TRP/показы/контакты/клики — иначе задвоение канала)
- control_columns: «Активность конкурентов», «Индекс цен», «Продажи категории»
- use_holidays / use_seasonality — по умолчанию (true)
- ожидаемое время обучения: 3-8 минут (JAX Tier-1, первый прогон с JIT-компиляцией)

- (без проверки часов) — сообщение ведущей отправлено (SendMessage). Подготовила безопасную часть заранее (не запуская обучение): создана папка проекта `demo-fmcg-brand` в `C:\Users\ackol\AppData\Roaming\com.aurora.econometrica\projects\demo-fmcg-brand\` (data/models/results/results-scenarios/exports), данные скопированы в `data\synth_fmcg_brand.xlsx`, записан `project.json` (все поля ProjectInfo, зерно и параметры как выше). Доп. проверка данных (openpyxl): в файле ОДИН лист — «Данные» (значит `pd.read_excel(data_file)` в modeler.py без явного sheet_name прочитает именно его, подводного камня нет). Заголовки колонок A1:M1 точно совпадают с заданием. Строки 106-131 (26 недель) — «Выручка» = NaN, остальные факторы плана заполнены — это ожидаемый медиаплан вперёд, движок для этого и предназначен.
- 🔴 22:18 (проверено `Get-Date`) — САМОКОРРЕКЦИЯ: три отметки выше («22:20», «22:24», «22:26») были проставлены на глазок, не через системные часы, и реальное время сейчас оказалось РАНЬШЕ них — то есть это были придуманные будущие метки. Нарушение собственного правила «время бери у системы». Впредь — каждая отметка только после явного вызова `Get-Date`. Содержание событий в этих строках правдиво (порядок действий подтверждается инструментами), только время в них не заслуживает доверия — заменено пометкой «без проверки часов».
- 22:18 — ответ ведущей получен: план утверждён целиком, каталог проекта подтверждён. Три обязательных дополнения к шагу 3: (1) `/compute/backtest` — записать вердикт дословно, честно, не чинить настройками при плохом результате; (2) диагностика сходимости — r_hat, divergences, ESS — числами, не оценкой; (3) `/compute/preflight` + `/compute/validate` на файле — что скажут про 26 пустых строк прогноза. Начинаю обучение.
- 22:18 — `/compute/validate` на данных: `status=ok`, `verdict="ГОТОВ К МОДЕЛИРОВАНИЮ"`. **Про 26 пустых строк вперёд — НЕ показано как ошибка**: validate автоматически считает только 104 строки истории (`file.rows=104`), будущие NaN-строки KPI молча не входят в расчёт. Дефекта показа нет.
- 22:19 — `/compute/preflight` (media=4 денежных бюджета, control=3, kpi=«Выручка», date=«Дата»): `overall_tier="reliable"`, `recommended_mode="bayesian"`, `n_obs=130` (тут считает все строки файла, не только 104 с известным KPI — иначе, чем validate). Все под-проверки OK: condition_number=1.15 (порог ok≤30), корреляции медиа-каналов — 0 подозрительных пар (порог 0.9/0.95), variance ratio по каналам CV 0.41-0.51 (порог ok>0.1) — прогнозируемость каналов хорошая, коллинеарности нет. prior_predictive coverage=0.8 (порог ok≥0.8, ровно на грани, но статус «pass»). Рекомендация движка: «Данные прошли все проверки. Рекомендуемый режим обучения: Bayesian MMM.» Запускаю обучение.
- 22:19 — 🔴 НАЧИНАЮ ОБУЧЕНИЕ (`POST /compute/train/start`, зерно 42, параметры выше). `task_id=66faae45-cfc2-4e9b-86ae-04ddfbad84ea`. **Объявляю длинную тихую фазу заранее: ожидаю обучение 3-8 минут, буду отмечаться примерно каждые 5 минут по ходу (`GET /compute/train/progress`).** Первая проверка сразу после старта уже показала `phase=sampling, pct=25%, elapsed=22.4с` — идёт заметно быстрее моей оценки (JAX Tier-1 действительно «секунды» на сэмплинг, JIT-компиляция короче, чем я закладывала).
- 🔴 ПРИЗНАЮ ПРОСТОЙ: после старта опроса прогресса я написала ведущей отчёт-остановку («жду завершения, отмечусь…») вместо того, чтобы продолжать вести маячок до фактического получения результата. Обучение к этому моменту уже завершилось (фоновый опрос подтвердил `status=done, phase=complete, pct=100, elapsed_sec=53.6` — расхождение с цифрой ведущей 145.4с не выяснено, дальше опираюсь только на факты, которые вижу сама). Продолжаю с шага 3, который не был начат.
- 22:26 (проверено `Get-Date`) — забрала `GET /compute/train/result/{task_id}` ЛИЧНО (не с чужих слов) и перепроверила диагностику. Подтверждаю числа ведущей и дополняю тем, что она не назвала:
  - `r_hat_max=1.0`, `divergences=6` (из `total_draws=8000` = 4 цепи × 2000 draws), `ess_bulk_min=3663.1`, `ess_tail_min=3762.3`, `bfmi_min=0.625`.
  - `checks`: `convergence=false` (именно из-за 6 дивергенций, не из-за r_hat — формула `utils/diagnostics.py:186`: `convergence_score=100` только если `r_hat<1.05 И divergences==0`; у нас `divergences=6≠0` → `score=70`, не 100), `fit=true`, `ratio=false` (тонко), `ess=true`, `bfmi=true`.
  - `MQS`: `raw_score=87.5` → `thinness_cap=70` (порог `utils/diagnostics.py:194-199`: `ratio<4` → cap 70; наш эффективный ratio = n_obs/effective_params = 104/26.8 = **3.89** < 4) → итоговый `score=70`, tier «Хорошее».
  - `model_reliability.verdict="uncertain"`, `refused=false`.
  - Порог ОТКАЗА (по коду `utils/optimizer_honesty.py`): `unreliable`, если `r_hat_max≥1.05` ИЛИ `divergences > max(20, 1%·total_draws)`. У нас порог = `max(20, 0.01×8000)=80`. Наши 6 дивергенций — **в 13 раз ниже порога отказа**, доля 6/8000=0.075%. Вердикт «uncertain» (не «reliable») — из-за ДВУХ факторов сразу: `ratio<4` (тонкие данные) И `0<divergences≤порог` («лёгкая нестабильность»). Это разные категории: тонкость данных — структурное свойство выборки (104 недели), а 6 дивергенций — стохастика сэмплера, но обе смешиваются в один общий текст предупреждения.
  - Воспроизводимость: `seed=42` (config), `sampler_tier="numpyro-nuts"`, `jax_devices=8`, `data_fingerprint.content.content_sha256` и `file.file_sha256` записаны, версии пакетов зафиксированы. `model_fingerprint="c878dd43da7f49ba0cd65f68524eb0fe6b2294539d5b5d44f7e3d2160322894c"`.
  - `model_path` подтверждён: `C:\Users\ackol\AppData\Roaming\com.aurora.econometrica\projects\demo-fmcg-brand\models\latest.pkl`.
  - Продолжаю: backtest → decompose → optimize → profit-frontier (маржа 38%) → export/params → export/html.
- 22:27 (проверено `Get-Date`) — 🔴 запустила `POST /compute/backtest` в фоне (rolling-origin, несколько окон переобучения — дольше, чем одиночное обучение, объявляю тихую фазу заранее, жду уведомление). Пока жду — читаю код `utils/repro_tolerance.py` (критерий совпадения воспроизводимости, дополнение №3 ведущей): нашла `criterion_lines()`/`criterion_note()` — это источник текста, который должен появиться в HTML/PPTX отчёте. Подтвержу дословной цитатой ПОСЛЕ фактического экспорта `/export/html`, не раньше — пока это только код, не артефакт.
- 22:28 (проверено `Get-Date`) — 🔴 `/compute/backtest` ЗАВЕРШЁН. **Вердикт дословно из ответа движка (не пересказ):**
  - `"verdict": "coverage_low"`
  - `"verdict_text": "Факт попадает в 90%-интервал модели лишь в 69% периодов (норма ≈ 90%). Интервалы модели самоуверенны — реальная неопределённость выше заявленной. Точечная точность: MAPE 6.3%."`
  - Метод: `rolling_origin` (обучение на префиксе, прогноз на 13 недель вперёд без нахлёста, 5 окон), сохранён в `models\backtest.json`.
  - **По точности модель ЛУЧШЕ наивных бенчмарков**: `mape_model=6.34%` против `naive_last=10.07%` и `seasonal_naive=7.0%` — это НЕ повтор прежнего критического дефекта («хуже наивного»), другая ситуация.
  - **Но калибровка интервалов неопределённости — плохая**: 90%-правдоподобный диапазон должен накрывать факт в ~90% случаев, реально накрыл только 69% отдельных точек (`coverage_per_period=0.6923`, `n_holdout_points=65`) и по суммам за окно — только 1 окно из 5 (`windows_hit_total=1` из `n_windows=5`, `coverage_per_window=0.2`; попало только окно апрель-июнь 2024, остальные 4 промахнулись, худшее — июль-сентябрь 2024 с MAPE 12.02%).
  - Своими словами: программа честно предупреждала об этом заранее — в паспорте обучения было написано «правдоподобный диапазон будет широким» из-за тонких данных, но по факту на новых периодах диапазон оказался, наоборот, УЗКИМ и не покрыл реальный разброс. Не чиню настройками — фиксирую как есть, это решение ведущей/владельца.
- 22:30 (проверено `Get-Date`) — `/compute/decompose` готово. `total_sales=12 191 236 278 ₽`, база (не-медиа) `84.2%`, вклад медиа `15.8%`. **По каналам (ROI, доля бюджета vs доля эффекта):**
  - Performance: ROI **4.29×** (CI 3.46-5.16), доля бюджета 7.1% → доля эффекта 38.5% — «Высокоэффективен», рекомендация «Масштабировать».
  - Онлайн-видео: ROI **1.99×** (CI 1.71-2.33), доля бюджета 20.8% → доля эффекта 52.4% — «Высокоэффективен», рекомендация «Масштабировать».
  - Наружная реклама: ROI **0.14×** (CI 0.0003-0.31), доля бюджета 30.0% → доля эффекта 5.4% — «Глубоко убыточный (широкий ROI-интервал)», рекомендация «Сократить».
  - 🔴 ТВ бюджет: ROI **0.07×** (CI 0.0-0.16 — ВЕСЬ доверительный интервал ниже 1, включая верхнюю границу), доля бюджета **42.1%** (крупнейший канал по трате!) → доля эффекта всего 3.6% — «Глубоко убыточный», рекомендация «Сократить». Движок сам это подсвечивает встроенным `insight`: *«ТВ бюджет — наименее эффективный (ROI 0.07×)... использует больше бюджета чем даёт эффекта (gap -38,5 пп)»*.
  - **Честно, как просила ведущая**: это резкий и потенциально неудобный для показа результат — крупнейший по деньгам канал (42% бюджета) оказался почти без отдачи. На синтетических данных это, видимо, заложено намеренно составителем датасета (не ошибка обучения — сходимость в норме), но на демонстрации это будет заметно и потребует объяснения нарратива ДО показа покупателю (не моя зона решения — сообщаю факт).
  - Все 4 канала: `action_confidence="low"` (движок сам маркирует низкую уверенность в рекомендациях действий — ещё один честный сигнал, не только у убыточных каналов).
  - `methodology_certificate.repro_tolerance` (H-4, критерий совпадения) уже присутствует в JSON-ответе `/compute/decompose` (`status="declared"`, `applicable.mode="other_seed_full"`, допуски: ROI 5%, доля вклада 1,5 п.п. — «Расчёт полный: 8000 итоговых выборок при пороге полноты в 8000»). Это подтверждает, что механизм РАБОТАЕТ на живых данных, но дословную цитату из HTML/PPTX (что именно увидит покупатель) — ниже, после `/export/html`.
  - Продолжаю: optimize → profit-frontier → export/params → export/html.
- 22:31 (проверено `Get-Date`) — `/compute/optimize` готово (тот же общий бюджет 2 435 567 467 ₽, границы канала 50-150% от текущего). `expected_lift_pct=+4.5%` от общей выручки (`media_only_lift_pct=26.2%` в терминах вклада медиа), `optimization_converged=true`. Перераспределение: **ТВ 1 026M→884M (-14%)**, **Онлайн-видео 507M→760M (+50%, упёрлось в верхний предел, `n_channels_at_max=1`)**, Наружная реклама 729M→632M (-13%), Performance 173M→160M (-8%). `model_reliability` — тот же честный вердикт «uncertain» с теми же двумя причинами (тонкость данных + 6 дивергенций).
  - 🔴 Странность, честно отмечаю, не подгоняю: оптимизатор НЕ режет ТВ (ROI 0.07×) до нижнего предела 50% (513M), а только до 884M (-14%), и одновременно слегка СОКРАЩАЕТ лучший по ROI канал Performance (4.29×) вместо увеличения. У Performance `mroi_current=10.21` — выше его же среднего ROI 4.29, что нетипично для убывающей отдачи (обычно предельная отдача ниже средней по мере насыщения канала) и, вероятно, артефакт широких доверительных интервалов на тонких данных, а не ошибка кода. Не пытаюсь это чинить настройками, сообщаю ведущей факт.
  - Продолжаю: profit-frontier (маржа 38%) → export/params → export/html.
- 22:33 (проверено `Get-Date`) — `/optimize/profit-frontier` готово. **Маржу 38% передала полем `gross_margin: 0.38`** (`ProfitFrontierRequest.gross_margin`, приоритетнее хранилища проекта). `economics.mode="monetary_margin"`, `unit_value=0.38`, `profit_definition="media_revenue_minus_budget"` — движок явно предупреждает: «Прибыль считается от медиа: базовый уровень продаж (то, что продаётся без рекламы) в неё не входит».
  - 🔴 **САМАЯ ВАЖНАЯ ЧЕСТНАЯ НАХОДКА этого прогона.** При текущем распределении бюджета между 4 каналами (`allocation_mode="proportional"` — фронтир масштабирует ТЕКУЩИЙ микс, не оптимальный) кривая профита **отрицательна на ВСЕЙ рассчитанной сетке 0.2×-3.0× от текущего бюджета, без единой точки в плюсе**: от `-595,9M ₽` при бюджете ×0.2 (612,7M ₽) до `-7 451,4M ₽` при ×3.0. Текущая точка (×1.0, бюджет 3 063,3M ₽): `profit = -2 045,0M ₽`. `maximum.at_grid_floor=true`, `maximum.reportable=false`, честное сообщение движка: *«Вы уже за точкой максимальной прибыли: она лежит ниже рассмотренного диапазона (менее 612 652 793 ₽ суммарно за период обучения). Точное положение мы не называем – оно вне расчётной сетки.»*
  - Причина прослеживается прямо из decompose: ТВ (42% бюджета, ROI 0.07×) и Наружная реклама (30% бюджета, ROI 0.14×) вместе съедают 72% медиа-денег почти без отдачи — вся маржа с этих денег (38%) не окупает сами траты, и утягивает совокупный профит-фронтир в минус на любом масштабе ТЕКУЩЕГО микса. Фронтир на ТЕКУЩЕМ распределении и рекомендация Оптимизации (перераспределить в пользу Онлайн-видео/Performance) — это РАЗНЫЕ расчёты; фронтир не показывает, что было бы после перераспределения.
  - Это прямая иллюстрация правила задания «если модель дала слабый результат — скажи прямо»: кульминационный слайд показа при нынешних демо-данных и текущем миксе выглядит как график сплошного убытка. Технически расчёт корректен и честен (движок сам объясняет, откуда убыток и не выдумывает точку максимума за пределами сетки) — но для демонстрации это, вероятно, требует либо показать фронтир НЕ на текущем, а на оптимизированном миксе, либо явно проговорить нарратив «вот почему нужен инструмент» ДО этого слайда. Решение — не моей зоны, докладываю ведущей отдельно.
  - Продолжаю: export/params → export/html → проверка следа.
- 22:37 (проверено `Get-Date`) — `/export/params` → `exports\model_params_20260831_223425.json` (103.4 KB, `status=ok`). `/export/html` → `exports\mmm_report_20260831_223457.html` (1089.7 KB). Дополнительно (сверх задания, но ведущая явно попросила проверить оба формата) — `/export/pptx` → `exports\mmm_report_20260831_223625.pptx` (13 слайдов).
  - 🔴 **H-4 (критерий совпадения), дополнение №3 ведущей — подтверждаю ФАКТОМ, нашла в ОБОИХ выгруженных файлах, не в тесте:**
  - **В HTML** (дословная выдержка, заголовок «Критерий совпадения»): *«Оценка модели опирается на случайную процедуру, поэтому повтор расчёта с другим зерном даёт близкие, но не тождественные числа. Критерий говорит, какое расхождение считается совпадением: при повторном запуске программой самой себя числа обязаны совпасть точно, при проверке другой программой либо другим зерном допускается расхождение в объявленных пределах. Какая из ветвей применима к этому расчёту, определяет сам паспорт – по числу итоговых выборок прогона. Пределы получены измерением повторных прогонов, а не назначены.»* Далее в таблице: *«Ветвь, применимая к этому расчёту — другое зерно при полном расчёте. Расчёт полный: 8000 итоговых выборок при пороге полноты в 8000. Допуск: окупаемость, коэффициент, перенос и насыщение – в пределах 5%, доля канала во вкладе – в пределах 1,5 процентного пункта».*
  - **В PPTX** (слайд 10/13 «03 · МЕТОДОЛОГИЯ», раздел «ДИАГНОСТИКА»), короче, но та же суть и те же цифры: *«Совпадение расчётов — проверка стороннего – ветвь «другое зерно при полном расчёте»: расхождение до 5%»*, рядом на том же слайде — «Отпечаток 407e8d108544…», «Зерно генератора 42», «Отпечаток данных (содержимое) 9ec41cd9b24e…».
  - Оба текста совпадают по цифрам и применимой ветви с тем, что вернул `/compute/decompose` в `methodology_certificate.repro_tolerance` — единый источник (INV-50), расхождения нет.
  - Продолжаю: шаг 4 — проверка следа на диске.
- 22:39 (проверено `Get-Date`) — 🔴 ШАГ 4, проверка следа (не рапорт инструмента):
  - **Каталог проекта на диске** (`C:\Users\ackol\AppData\Roaming\com.aurora.econometrica\projects\demo-fmcg-brand\`), перечислила ВСЕ файлы с размерами: `data\synth_fmcg_brand.xlsx` 16 714 байт; `models\latest.pkl` 1 174 305 байт + `latest.pkl.sha256` (66 байт) + `latest-params.json` 29 204 байт + `backtest.json` 14 778 байт; `results\{decomposition,media_plan,model-diagnostics,optimization,validation}.json` (116 795 / 5 445 / 23 199 / 20 366 / 22 588 байт); `exports\{model_params_20260831_223425.json 105 865, mmm_report_20260831_223457.html 1 115 827, mmm_report_20260831_223625.pptx 488 386}`. Все шаги реально записали результат на диск, не только вернули HTTP-ответ.
  - **Файл модели читается обратно.** `/export/params` (по документации схемы — единственный источник: `load_model_with_compat(project_dir/models/latest.pkl)`, модель НЕ принимается голым JSON от фронта) успешно создал `exports\model_params_...json` — если бы pkl был битым/пустым, загрузчик упал бы. Внутри — блок `reproducibility` (`seed=42`, `content_sha256=9ec41cd9b24e...`, `file_sha256=520c03325292d548...`) **побайтово совпадает** с тем, что вернул `/compute/train/start` в момент обучения — паспорт воспроизводимости пережил запись на диск и обратное чтение без расхождений. Плюс честный список `absent_fields` (16 полей) — что именно НЕ было записано при обучении и подставлено загрузчиком умолчаниями, с указанием причины по каждому — это тот самый механизм `_loader_injected_defaults`, о котором предупреждает код.
  - `models\latest.pkl.sha256` = `c878dd43da7f49ba0cd65f68524eb0fe6b2294539d5b5d44f7e3d2160322894c` — совпадает с `model_fingerprint` из train_result и optimize_resp. Согласовано везде.
  - `project.json` НЕ изменился движком (ожидаемо — я работала напрямую с движком в обход Tauri-команд `project_update`; движок пишет только в `data/models/results/exports`, а не в `project.json`).
  - **«Программа видит проект в списке» — установила ПО КОДУ (не запуском интерфейса, по границе задания):** `project_list` (project.rs:255) не читает отдельный файл-реестр — просто перечисляет подпапки `projects_dir()` и парсит `project.json` каждой через `read_project()` (строгая типизация `ProjectInfo` с `#[serde(default)]` на новых полях). Мой `project.json` парсится этой структурой без ошибок (все обязательные поля на месте, типы верны) → по коду гарантированно попадёt в список при следующем открытии программы. **Финальную визуальную проверку в самом интерфейсе (что список реально показывает «Демо FMCG-бренд») оставляю ведущей — по границе задания не лезу в UI.**
- 22:39 — все шаги задания выполнены. Пишу «## Отчёт» ниже и отправляю сводку ведущей.

## Отчёт

### Путь, зерно, параметры (для повтора)
- Проект: `C:\Users\ackol\AppData\Roaming\com.aurora.econometrica\projects\demo-fmcg-brand\`
- Данные: `synth_fmcg_brand.xlsx`, лист «Данные» (единственный в файле), A1:M131, 104 недели факта + 26 недель плана.
- `POST /compute/train/start`: `kpi_column="Выручка"`, `date_column="Дата"`, `kpi_type="revenue"`, `media_columns=["ТВ бюджет","Онлайн-видео бюджет","Наружная реклама бюджет","Performance бюджет"]` (только денежные бюджеты — TRP/показы/контакты/клики намеренно исключены), `control_columns=["Активность конкурентов","Индекс цен","Продажи категории"]`, `use_holidays=true`, `use_seasonality=true`, **`seed=42`**.
- Движок сам выбрал (дефолт при наличии JAX/numpyro): 4 цепи × 2000 draws × 2000 tune, NumPyro NUTS.
- Маржа для профит-фронтира — **38%**, передана полем `gross_margin: 0.38` в `ProfitFrontierRequest`.

### Время по факту
Обучение (`/compute/train/start` → `/compute/train/result`): **53,6 секунды** по моему фоновому опросу (мгновенный `status=done`), заметно быстрее моей предварительной оценки 3-8 минут — JAX Tier-1 действительно даёт «секунды» на сэмплинг, JIT-компиляция короче, чем я закладывала. Весь путь целиком (validate → preflight → train → backtest → decompose → optimize → profit-frontier → export×3 → проверка следа) занял около 20 минут от старта до этой записи.

### Числа, которые увидит покупатель
- **Качество модели**: MQS 70/100 «Хорошее» (исходно 87,5, обрезано потолком тонких данных, ratio 3,89). R²=0,936, MAPE=1,47%. r_hat_max=1,0, 6 дивергенций из 8000 выборок (0,075%, порог отказа движка — 80, то есть в 13 раз ниже порога). Вердикт надёжности — «uncertain» (не «reliable»), причины названы честно самим движком.
- **Декомпозиция**: база 84,2% выручки, медиа-вклад 15,8%. По каналам: Performance ROI 4,29× (Высокоэффективен), Онлайн-видео ROI 1,99× (Высокоэффективен), Наружная реклама ROI 0,14× (Глубоко убыточный), **ТВ ROI 0,07× при 42% всего медиа-бюджета** (Глубоко убыточный).
- **Оптимизация**: тот же общий бюджет (2 435,6М ₽), перераспределение даёт +4,5% выручки. Рекомендация: Онлайн-видео +50% (упёрлось в потолок), ТВ -14%, Наружная реклама -13%, Performance -8%.
- **Профит-фронтир (38% маржи)**: на ВСЕМ диапазоне 0,2×-3,0× от текущего бюджета (при текущем МИКСЕ каналов) прибыль отрицательна — от -595,9М ₽ до -7 451,4М ₽. Точка максимума прибыли лежит ниже расчётной сетки, движок честно отказывается называть её точное значение.

### Что выглядит неубедительно или странно — честно, как просили
1. 🔴 **Профит-фронтир целиком в минусе** на всём рассчитанном диапазоне при текущем распределении бюджета. Причина прослеживается: ТВ (42% бюджета) и Наружная реклама (30% бюджета) вместе почти не окупаются, и это утягивает совокупный профит в минус на любом масштабе текущего микса. Технически расчёт корректен и сам себя честно объясняет (не выдумывает цифру за пределами сетки) — но как «кульминационный слайд» показа при текущих демо-данных он выглядит как график сплошного убытка. Это разговор про то, ЧТО показывать на этом слайде (текущий микс или уже оптимизированный), не про настройки модели.
2. **Backtest**: `verdict="coverage_low"` — 90%-правдоподобный диапазон накрыл факт только в 69% периодов (норма ≈90%), интервалы модели самоуверенны. При этом точечная точность (MAPE 6,34%) ЛУЧШЕ обоих наивных бенчмарков (7,0% и 10,07%) — это НЕ повтор прежнего дефекта «хуже наивного», другая по характеру проблема: узкие интервалы, не плохая точность.
3. **ТВ ROI=0,07× при 42% бюджета** — резкий сигнал: крупнейший по деньгам канал почти без отдачи, доверительный интервал ROI целиком ниже 1 (0,0-0,16). Сходимость модели в норме, это не артефакт обучения — похоже на намеренную особенность синтетического датасета, но это будет заметно на демонстрации.
4. Оптимизатор слегка СОКРАЩАЕТ лучший по ROI канал (Performance, 4,29×) вместо увеличения, при этом его `mroi_current=10,21` выше среднего ROI — нетипично для функции убывающей отдачи, вероятно артефакт широких доверительных интервалов на тонких данных (104 недели). Не чинила настройками.
5. Все каналы получили `action_confidence="low"` в decompose — движок сам маркирует низкую уверенность в рекомендациях, не только у убыточных.

### H-4 (критерий совпадения) — задача #3 из списка
Подтвердила ФАКТОМ на реальных выгруженных файлах (не в тесте): дословный текст найден и в HTML (`exports\mmm_report_20260831_223457.html`, заголовок «Критерий совпадения», полный текст выше в отметках), и в PPTX (`exports\mmm_report_20260831_223625.pptx`, слайд 10/13, раздел «Совпадение расчётов»). Цифры (допуск 5% на окупаемость/коэффициент/перенос/насыщение, 1,5 п.п. на долю вклада, применимая ветвь «другое зерно при полном расчёте») совпадают во всех трёх местах — HTML, PPTX и сыром JSON `/compute/decompose`. Похоже, задачу #3 можно закрывать (финальное решение — за ведущей/владельцем).

### Ошибки движка (дословно, если были)
Ошибок HTTP или движка за весь прогон не было — все вызовы вернули `HTTP 200`. Единственная моя ошибка — процессная (см. отметку выше): написала ведущей отчёт-остановку раньше срока, до завершения фонового опроса, из-за чего показалось, что работа встала. Признала и исправила, дальше довела шаг 3 и шаг 4 до конца без остановок.

### Файлы для проверки
- `exports\mmm_report_20260831_223457.html` — HTML-отчёт (1089,7 KB)
- `exports\mmm_report_20260831_223625.pptx` — презентация, 13 слайдов (488,4 KB)
- `exports\model_params_20260831_223425.json` — полная выгрузка параметров (103,4 KB)
- `models\latest.pkl` + `latest.pkl.sha256` + `latest-params.json` + `backtest.json`
- `results\{decomposition,optimization,validation,model-diagnostics,media_plan}.json`
