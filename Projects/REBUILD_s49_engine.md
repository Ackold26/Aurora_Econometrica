# REBUILD s49 — пересборка движка Econometrica и доказательство попадания правки

## 1. Исходное состояние (до сборки)

| Что | Значение |
|---|---|
| Старая сборка `dist/econometrica-sidecar/` | 14.09.2026 10:10:45, 5817 файлов, 969 616 532 байт (≈970 МБ) |
| Правка `aurora_html/sections.py` | 18.09.2026 11:39:24 |
| Правка `aurora_html/strings_ru.json` | 18.09.2026 11:39:24 |
| Процессы `econometrica-sidecar.exe` перед сборкой | не найдено (проверено дважды: 11:54 и повторно перед подъёмом службы) |

Вывод: старая сборка младше правок на ~3,5 часа — ровно то расхождение, что требовало пересборки.

## 2. Сборка

Команда: `python sidecar/econometrica/build_sidecar.py`
Журнал: `Projects/_build_sidecar_s49.log`
**КОД ВОЗВРАТА: 0** (записан явной строкой сразу после команды, не через конвейер)

Ключевые строки журнала: `Build SUCCESS`, `Bundle size: 970 MB`, `[OK] validate CSV: status=ok`, `[OK] validate XLSX: status=ok`, `[OK] Bundle smoke test passed`, `[OK] Freshness verified (exe newer than all .py sources)`.

Попутно сборщик сам перегенерировал `src/tokens.generated.css` (5450 байт, байт-в-байт как в прошлой сборке s48c) и синхронизировал бандл в `sidecar/econometrica/_internal` (ресурс-путь Tauri) — это штатное поведение самого `build_sidecar.py`, не моя правка `src/`.

| Что | Значение |
|---|---|
| Новая сборка `dist/econometrica-sidecar/` | 18.09.2026 12:01:56, 5817 файлов, 969 617 465 байт |
| exe | 18.09.2026 12:01:56 — новее обеих правок |

## 3. Сплошная побайтная сверка

Список файлов-источников движка (исключены `dist/`, `build_tmp/`, `build/`, `_internal/`, `.hypothesis/`, `__pycache__/`, `tests/`, но НЕ `tools/` и НЕ `optimize/` — они в списке кандидатов, расхождение объясняется отдельно): 102 `.py` + 4 `.json` (`aurora_html/strings_ru.json`, `aurora_pptx/strings_en.json`, `aurora_pptx/strings_ru.json`, `data/kpi_display_registry.json`) = **106 файлов**. Каждый сверен `cmp` с копией в `dist/econometrica-sidecar/_internal/<тот же путь>`.

| Всего | Совпало | Расхождений | Объяснённых (не нашлись в поставке) |
|---|---|---|---|
| 106 | **94** | **0** | **12** |

Двенадцать необъяснённых по путям — ровно совпадают с ожиданием прошлого блока:
- `server.py`, `build_sidecar.py` — точка входа и скрипт сборки компилируются внутрь (не лежат отдельными файлами в `_internal`);
- `optimize/__init__.py`, `optimize/auto_price.py`, `optimize/bounds.py`, `optimize/frontier.py`, `optimize/inverse.py`, `optimize/split_ci.py` — пакет `optimize` уходит в архив движка (PYZ), не как открытые `.py`;
- `tools/fetch_macro_series.py`, `tools/recompute_mqs.py`, `tools/sync_kpi_display.py`, `tools/test_fetch_macro_series.py` — четыре вспомогательных скрипта в поставку не входят.

Расхождений (файл найден, но байты разные) — **0**. Числа совпадают с ожиданием прошлого блока (94/0/12) без единого исключения.

**Целевые файлы правки** — `aurora_html/sections.py` и `aurora_html/strings_ru.json` — оба в списке MATCH (побайтно идентичны исходнику).

## 4. Доказательство правки текстом

В собранной копии `_internal/aurora_html/sections.py`:
- «в квартал» — 1 вхождение, но это **комментарий разработчика** (строка 646: `# s49: «в квартал» было wireframe-предположением о периодичности —`), объясняющий саму правку, не клиентский текст.
- `data_window_label` — 2 вхождения (используется).
- `_internal/aurora_html/strings_ru.json` содержит `period_phrase` — 1 вхождение (плейсхолдер в шаблоне).

## 5. Доказательство правки вызовом

Подняла собранную копию `dist/econometrica-sidecar/econometrica-sidecar.exe` как службу на порту 28911 (`econometrica-sidecar.exe 28911`), дождалась `/health` → 200.

Данные — готовые (без переобучения, дёшево): скопировала READ-ONLY `results/decomposition.json`, `results/optimization.json`, `results/model-diagnostics.json` из реального проекта владельца `demo-fmcg-brand` (`%APPDATA%\aurora-econometrica-gui\projects\demo-fmcg-brand\`) в изолированный каталог сессии (scratchpad) — в проект владельца ничего не писала, только читала.

`POST http://127.0.0.1:28911/export/html` с этими данными и `project_dir` = изолированный каталог → **HTTP 200**, HTML построен (1108,9 КБ).

Проверка сгенерированного файла:
- «в квартал» в клиентском тексте — **0 вхождений**.
- Реальная фраза в отчёте: **«размещает 2436 млн ₽ за период янв 2023 – дек 2024 через 4 активных каналов»**.

Это та же сумма (2436 млн ₽), что в СТАРОМ образце из `SAMPLES_s49.md` с багом «в квартал» — то есть подтверждение на тех же данных: было «2436 млн ₽ в квартал» (ложь), стало «2436 млн ₽ за период янв 2023 – дек 2024» (правда, из `meta["data_window_label"]`).

Служба остановлена штатно (`POST /shutdown` → 200), процессов `econometrica-sidecar.exe` после — не осталось (проверено).

## Вердикт

**Движок пересобран и правка в нём доказана: ДА.**
- Код возврата сборки 0, свежая сборка новее правок.
- Сплошная сверка 106/94/0/12 — точь-в-точь ожидание, расхождений нет.
- Правка подтверждена и текстом (сравнение в `_internal`), и делом (реальный HTTP-вызов собранной копии вернул честную подпись периода вместо «в квартал»).
