# Handoff — блок aurora-upgrade (усиление промптов econometrist каноном)

## 1. Цель блока
Применить к промптам кабинета econometrist (Aurora Econometrica, Optimizer MMM) 9 методологических
находок скилла aurora-upgrade — подкрепить/поправить методологию первоисточниками RAG-библиотеки (канон
уже проиндексирован, докупка не нужна). Правки текстовые (методология промптов), не код-логика. Диапазон
diff = base `a34791d`..HEAD (2 коммита: `4d0d913` приоритет 1, `a52b935` приоритет 2-3).

## 2. Ключевые инварианты
- **INV-50 (честность метрик):** ни одного числа под чужим ярлыком; производные помечаются `[ДОПУЩЕНИЕ]`.
- **Клиентский текст:** короткое тире «–» (U+2013), НИКОГДА U+2014; русский без англицизмов (термины как есть).
- **Цитаты канона** — только из реально прочитанных фрагментов книг (не из cos-сниппета).
- **Контракт команд не сломан:** `cabinet_eval --dry` строит все 6 сообщений; линтер `lint_prompt_commands.py`
  0 FAIL; четверное совпадение (help-consistency) OK.
- **Бюджет always-on:** правки в CLAUDE.md кабинета (множитель) — минимальные (только G4 1 строка + G8 замена).

## 3. Осознанные компромиссы
- **G7 (SBC fake-data recovery) и G9 (geo-иерархия Sun 2017) НЕ вписаны в текст промптов** — они относятся
  к ДВИЖКУ (Python sidecar считает MCMC / иерархическую модель), а кабинет econometrist — консультационный
  слой поверх готовых результатов. В тексте консультанта они смысла не имеют → продуктовый бэклог.
- **Durable-документы CANON_A/B/C.md + D.md в diff — рабочие заметки аудита, НЕ код продукта.** Аудировать
  как артефакт не нужно; предмет аудита — 16 строк промпт-правок в 6 файлах (.md команд + CLAUDE.md).
- **G1 pilot-design расширен тремя блоками** (TBR + CausalImpact + петля пилот→модель) — команда стала
  длиннее; сознательно, т.к. pilot-design — главный запрос клиента, а каждый блок закрывает свой сценарий
  (гео есть / гео нет / после пилота).

## 4. Зоны неуверенности
1. **C1 ESOV (awareness-forecast):** снята атрибуция коэффициента Binet&Field к awareness (он про долю
   рынка SOM). Не уверена, не осталось ли где-то ещё в awareness-цепочке неявного использования SOM-числа
   как awareness-прокси; проверялась только строка 18.
2. **G3 стационарность (mmm-model):** добавлен ADF/KPSS на остатке ПОСЛЕ Prophet-детрендирования. Не на 100%
   уверена, что тест на остатке Prophet методологически эквивалентен тесту на исходном ряде — Prophet
   вычитает гладкий тренд, но остаток может иметь иную интегрированность; формулировка опирается на
   рассуждение среза C, не на прямой эксперимент.
3. **G4 pretreatment (CLAUDE.md §5, always-on):** добавлена строка в множитель. Не уверена, не дублирует ли
   она по смыслу существующие M-bias/collider пункты настолько, что становится избыточной (риск: раздувает
   Red Teaming без новой информации для модели).
4. **G2 CausalImpact + G6 петля (pilot-design):** команда обещает контрфактуальную оценку и цикл калибровки,
   но реально их выполняет движок/ручной анализ — не уверена, не создаёт ли текст обещание метода, который
   консультант сам исполнить не может (граница «совет vs исполнение»).

## 5. Затронутые файлы (предмет аудита — первые 6; остальное — durable-заметки)
- `New_AI_Agency/econometrist/.claude/commands/awareness-forecast.md` — C1 ESOV атрибуция (INV-50).
- `New_AI_Agency/econometrist/.claude/commands/awareness-to-sales.md` — C2 дисклеймер идентификации + backdoor-контроли.
- `New_AI_Agency/econometrist/.claude/commands/pilot-design.md` — G1 TBR + G2 CausalImpact + G6 петля.
- `New_AI_Agency/econometrist/.claude/commands/mmm-model.md` — G3 стационарность (ADF/KPSS).
- `New_AI_Agency/econometrist/.claude/commands/mmm-scenarios.md` — G5 робастность по осям (Chan-Perry).
- `New_AI_Agency/econometrist/CLAUDE.md` — G4 pretreatment (§5) + G8 rank plots (Diagnostics); always-on множитель.
- `Projects/audit_prompts_2026-07-13/CANON_A/B/C.md`, `D.md` — durable-заметки срезов aurora-upgrade (не код).
