# Маячок: достроить починку затирания файлов клиента (Эконометрика 2.5.0)

## Задача своими словами
Предшественник написал три проверки в `src-tauri/src/session/manager.rs` (модуль `export_sync_tests`), но саму починку не применил: `copy_dir_into` делал голый `std::fs::copy` поверх файла клиента. Нужно применить починку в продуктовом коде так, чтобы проверки B (совпадающий файл не трогать) и C (расходящийся сохранить, новый положить рядом под `имя (2).ext`) стали зелёными по существу. Запись атомарная, отказ на одном файле не блокирует остальные, клиент видит развилку через существующий канал.

## План
1. Прочитать проверки и `copy_dir_into`, найти готового помощника в `commands/mod.rs`.
2. Понять, как продукт сообщает о развилке клиенту.
3. Применить починку, пробросить предупреждение.
4. Прогон трёх проверок + соседних, `cargo check`.

## Журнал
- **09:52:44 17.08.2026** – старт, маячок создан.
- **~09:56** – разобрана работа предшественника: в `commands/mod.rs` уже лежит ГОТОВОЕ решение CPD-70 – `place_generated_export` (три исхода Created / Unchanged / SavedAside), `place_generated_exports`, `saved_aside_notice`, потоковое `same_bytes`, `move_file`. Структурный сторож в `manager.rs` прямо требует, чтобы `copy_dir_into` звал `place_generated_export(` – замысел предшественника прочитан правильно.
- **~10:00** – правки применены (см. отчёт ниже).
- **10:0x** – прогон: три проверки зелёные, `cargo check` = 0.
- **10:1x** – добавлен один НОВЫЙ сторож на неразмножение копий, проверен мутацией (краснеет, ловит 5 лишних копий).
- **10:2x** – полный прогон: 486 пройдено, 0 упавших, код возврата 0.
- **10:18:50** – указание тимлида: в дереве работают ещё двое, общая папка цели `D:/cargo-targets/ai-agency` даёт ложные красные по блокировке файлов (CPD-86). Перехожу на свою папку `D:/cargo-targets/ec-overwrite`. **Начинается длинная тихая фаза: папка пустая, крейт со всеми зависимостями Tauri собирается с нуля – ожидаю от 10 до 30 минут без отметок.** Прогон запущен в фоне, пишет в `scratchpad/isolated.txt`. Все выводы предыдущего раздела отчёта получены в общей папке и БЕЗ ошибок связывания; изолированный прогон – подтверждение, а не замена.
- **~10:35** – тимлид снял границу по `lib.rs`: довожу уведомление клиента о переименовании. Все три места переведены на `sync_exports_reported`, написан сторож последней мили, проверен тремя мутациями. Идёт приёмочный прогон с признаком `thin` (CPD-112).
- **10:25:56** – изолированный прогон завершён целиком, ~7 минут вместе со сборкой с нуля. Все три этапа с кодом возврата 0, ошибок связывания и блокировки файлов ноль (проверено поиском по `LNK1104` и «занят другим процессом» – 0 совпадений). Итог совпал с прогоном в общей папке вплоть до сумм содержимого.

---

# ОТЧЁТ

## Что изменено

**`D:\Docs\Aurora_Ai\Dev\Aurora_Econometrica_thinwt\src-tauri\src\commands\mod.rs`**
- `mod.rs:59` – новая `pub(crate) fn numbered_variant(path, n)`: сборка имени `отчёт (2).docx`, вынесена ИЗ `unique_export_path` (`mod.rs:39`, поведение прежнее, тесты зелёные). Нужна доставке, чтобы узнавать свои прежние копии рядом с файлом клиента, и не заводить вторую копию правила именования (CPD-71).
- `mod.rs:180` – `same_bytes` подняла с приватной до `pub(crate)`: доставка сверяет содержимое тем же кодом, что и размещение выдачи.

**`D:\Docs\Aurora_Ai\Dev\Aurora_Econometrica_thinwt\src-tauri\src\session\manager.rs`**
- `manager.rs:684` – новый тип `ExportDelivery { blocked, saved_aside }`: два РАЗНЫХ исхода доставки разведены (не записан вовсе / лёг рядом под другим именем), плюс `absorb` и `tidy` (сортировка + дедуп, прежнее поведение по `blocked`).
- `manager.rs:720` – `copy_dir_into` переписан: приёмник-не-файл → `blocked` и `continue`; копия в промежуточный файл рядом с целью (`stage_copy`) → сверка «этот же результат уже лежит рядом» → решение принимает `crate::commands::place_generated_export`. Ветка `SavedAside` пишет имя в `saved_aside`, любой отказ → `blocked` + `continue` (доставка остальных продолжается). Голого `std::fs::copy` поверх файла клиента в функции больше нет.
- `manager.rs:792` – `staging_path`: `.aurora-<pid>-<счётчик>.tmp` в папке выдачи.
- `manager.rs:802` – `stage_copy`: копирование + `sync_all` до переименования, иначе отказ питания оставил бы клиенту файл правильного имени с мусором.
- `manager.rs:813` – `already_delivered_beside`: перебор `имя (2)`, `имя (3)`… и сверка байтов – без него правленый клиентом файл получал бы нового соседа на КАЖДОЕ сообщение.
- `manager.rs:321` – `sync_exports` теперь тонкая обёртка (сигнатура и смысл прежние: имена незаписанных), `manager.rs:331` – новый `sync_exports_reported` → `ExportDelivery`, с `warn!` о сохранённых рядом.
- `manager.rs:1614` – ОДИН новый сторож `a_hand_edited_file_gets_one_neighbour_not_one_per_message` (5 доставок поверх правленого файла → ровно один сосед). Существующие проверки не тронуты.

## Что из работы предшественника использовано
Всё ядро решения – его: `place_generated_export` / `PlacedExport` / `place_generated_exports` / `saved_aside_notice` / `same_bytes` / `move_file` в `commands/mod.rs`. Своего второго правила «сравни и положи рядом» я не писала – доставка выгрузок теперь идёт через ту же единственную функцию, что и генераторы выдачи. Все три его проверки и структурный сторож оставлены дословно.

## Прогон ДО правки (снято тимлидом)
```
run_a_missing_file_is_created ......................... ПРОЙДЕН
run_b_identical_file_is_not_touched_at_all ............ УПАЛ: время изменения сдвинулось (файл переписан заново)
run_c_differing_file_survives_and_the_new_one_lands_beside ... УПАЛ: правка клиента затёрта молча
```

## Прогон ПОСЛЕ правки
`CARGO_TARGET_DIR="D:/cargo-targets/ai-agency" cargo test -- run_a_ run_b_ run_c_ --test-threads=1 --nocapture`, **код возврата самой команды = 0**:
```
[прогон а] файла не было → создан: ...\exports/разбор-модели.md сумма=8fe372f3...ae24 время=1786950839975016000
[прогон б] содержимое совпало: было время=1700000000000000000 сумма=ad13970e...c528; стало время=1700000000000000000 сумма=ad13970e...c528
[прогон в] содержимое разошлось: файл клиента было время=1786950840009476100 сумма=7d4f667f...3e00; стало время=1786950840009476100 сумма=7d4f667f...3e00; рядом=...\exports/разбор-модели (2).md есть=true
test result: ok. 3 passed; 0 failed
```
Соседние и полный набор: `cargo test` → **486 пройдено, 0 упавших, 3 пропущено, код возврата 0** (включая `wipe_tests`, весь `export_sync_tests`, `place_generated_export_tests`, `unique_export_path_tests`, структурные сторожи CPD-70 и CPD-39). `cargo check` – код возврата 0 (взят у самой команды).

Промежуточно в первом полном прогоне падал `commands::updater::tests::writable_install_dir_launches_without_elevation` – чужая работа в `updater.rs` (файл менялся в 10:05 параллельным агентом, `needs_elevation` стояла заглушкой `true`); к моим правкам отношения не имеет, в финальном прогоне уже зелёный.

### Подтверждающий прогон в СВОЕЙ папке цели (CPD-86, указание тимлида)
`CARGO_TARGET_DIR="D:/cargo-targets/ec-overwrite"`, папка пустая, сборка с нуля, всего около 7 минут:
```
КОД cargo check = 0
[прогон а] файла не было → создан: ...\exports/разбор-модели.md сумма=8fe372f3...ae24
[прогон б] было время=1700000000000000000 сумма=ad13970e...c528; стало время=1700000000000000000 сумма=ad13970e...c528
[прогон в] файл клиента было=стало (время 1786951452756401600, сумма 7d4f667f...3e00); рядом=...\exports/разбор-модели (2).md есть=true
test result: ok. 3 passed; 0 failed
КОД cargo test три = 0
test result: ok. 486 passed; 0 failed; 3 ignored
КОД cargo test полный = 0
```
Ошибок связывания и блокировки файлов не было ни одной (поиск по `LNK1104` и «занят другим процессом» – 0 совпадений). Суммы содержимого совпали с прогоном в общей папке байт в байт, то есть исход не зависит от папки цели.

Оба исхода по указанию тимлида: **в общей папке** `D:/cargo-targets/ai-agency` – три проверки зелёные, полный набор 486/0, столкновений со чужими прогонами я не поймала (единственная красная в первом полном прогоне – `commands::updater::tests::writable_install_dir_launches_without_elevation`, это чужая незавершённая правка в `updater.rs`, не блокировка файла). **В своей папке** `D:/cargo-targets/ec-overwrite` – тот же результат, код возврата 0 у каждой команды.

Проверка нового сторожа мутацией: `if false && already_delivered_beside(...)` → сторож краснеет со списком `["разбор-модели (2).md" … "(6).md"]`. Мутация откачена.

## Как клиент узнаёт о переименовании – НЕЗАКРЫТО, нужно решение тимлида
Канал в продукте есть и он один: событие `claude-stream-{cabinet_id}` с типом `notice` + общий текст `commands::saved_aside_notice(...)` – ровно так уже сообщают о том же событии генераторы выдачи. Но **все три точки вызова `sync_exports` живут в `lib.rs`** (`lib.rs:1250`, `lib.rs:1528`, `lib.rs:3335`), а `lib.rs` мне запрещён границами задачи. Своего канала не изобретала.

Со своей стороны довела до границы: имена доступны через `sync_exports_reported`, старый `sync_exports` не сломан (lib.rs компилируется без правок), сами имена уже пишутся в журнал `warn!`. Владельцу `lib.rs` останется в `lib.rs:1528` заменить вызов:
```rust
match state.session_manager.sync_exports_reported(&cabinet_id) {
    Ok(delivery) => {
        if !delivery.blocked.is_empty() { /* прежний блок про занятые файлы, blocked вместо blocked */ }
        if !delivery.saved_aside.is_empty() {
            let _ = app_handle.emit(
                &format!("claude-stream-{cabinet_id}"),
                serde_json::json!({
                    "type": "notice",
                    "message": commands::saved_aside_notice(&delivery.saved_aside),
                }).to_string(),
            );
        }
    }
    Err(e) => return Err(e.to_string()),
}
```
и то же самое (без `emit`, только журнал, либо с `emit`) в двух остальных точках.

---

# ДОБАВЛЕНИЕ (11:14, граница по `lib.rs` снята тимлидом): уведомление клиента доведено

## Что изменено в `lib.rs`
Перед каждой правкой файл перечитывался заново, правки точечные.
- `lib.rs:1250` (конвейер презентаций внутри ответа кабинета) – переведён на `sync_exports_reported`; список незаписанных по-прежнему в журнал, список переименованных уходит событием `claude-stream-{cabinet_id}` с типом `notice` и текстом `commands::saved_aside_notice` (`lib.rs:1268`).
- `lib.rs:1546` (основной путь ответа кабинета) – то же; прежний блок про занятые файлы сохранён дословно, добавлен отдельный `notice` о переименованных (`lib.rs:1583`). Тип события именно `notice`, не `error` – ответ получен полностью, это развилка, а не отказ (прежняя находка аудита про `getEmpathyError`).
- `lib.rs:3376-3385` (шаг рабочего процесса) – своего потока событий у шага нет, канал там другой: предупреждение шага. Имена собираются в `renamed_beside`, а на `lib.rs:3446` вливаются в `step_warning`, который уезжает в `emit_wf_status_full` → интерфейс показывает «Шаг завершён, но …».

Текст в первых двух местах – существующий общий `saved_aside_notice`: «В папке результатов уже лежали файлы с такими же именами, и содержимое у них другое. Прежние файлы оставлены без изменений, новые сохранены рядом: … Сверьте оба варианта и удалите лишний сами – программа ваши файлы не удаляет.» Для шага формат другой (фраза-продолжение к «Шаг завершён, но»), поэтому там своя строка: «файлы с такими именами у вас уже есть и отличаются, поэтому новые результаты сохранены рядом: … – ваши прежние файлы не изменены». Оба текста на «вы», короткое тире, без англицизмов и эмодзи. **Переименований нет – сообщения нет вовсе** во всех трёх местах.

## Доказательство: значение доходит до канала
Новый сторож `manager.rs:1711` `every_delivery_site_tells_the_person_about_renamed_files` разбирает `lib.rs` (строки-комментарии отсекаются до разбора, окна берутся посимвольно – срез по байтам рвёт кириллицу) и проверяет ПУТЬ ЗНАЧЕНИЯ, а не факт вызова: краткого `sync_exports(` не осталось ни одного, мест доставки ровно три, в каждом `delivery.saved_aside` читается и уходит либо в событие `notice` через `saved_aside_notice`, либо в `renamed_beside` → `step_warning = Some(...)` → `step_warning.as_deref()` в событии шага.

Три мутации, каждая откачена:
1. Убрать блок `emit` в основном пути (`lib.rs:1583`) → КРАСНЫЙ: «список переименованных прочитан, но никуда не уходит – человек о развилке не узнает».
2. У шага заменить вливание в `step_warning` на `warn!` (только журнал) → КРАСНЫЙ: «разворачивается в текст, но не попадает в предупреждение шага».
3. Вернуть место шага к краткому `sync_exports(` (состояние ДО правки) → не компилируется вовсе: у `Vec<String>` нет полей `blocked`/`saved_aside`. То есть откат этой части кода ловится ещё компилятором.

## Прогон (своя папка цели `D:/cargo-targets/ec-overwrite`, код у самой команды)
```
КОД cargo check = 0
cargo test --lib      → test result: ok. 487 passed; 0 failed; 3 ignored   КОД = 0
cargo test -- --test-threads=1 → 487 + 5 + 7 пройдено, 0 упавших          КОД = 0
```
Промежуточно дважды выпал `LINK : fatal error LNK1104` на `aurora_econometrica_lib-*.exe`. По твоему правилу код я не трогала: нашла держателя – осиротевший тестовый двоичный файл PID 80448 от моего же прерванного прогона (CPU 0.5 с за 8 минут, то есть висел), снят по PID, прогон повторён и прошёл начисто.

**Приёмка с признаком `thin` (CPD-112) в этом дереве невозможна:** `cargo test --features thin` отвечает «the package 'aurora-econometrica-gui' does not contain this feature: thin» – в `src-tauri/Cargo.toml` объявлены только `default` и `cloud_advisors`, хотя `commands/mod.rs:16` содержит `#[cfg(feature = "thin")] pub mod gateway_executor;`. Это расхождение вне моей задачи и вне моих границ, чинить не стала – передаю фактом.

## Что осталось незакрытым
1. ~~Уведомление клиента о переименовании~~ – **закрыто в добавлении от 11:14** после снятия границы по `lib.rs`.
2. Приёмка с признаком `thin` (CPD-112) в этом дереве не запускается: признак не объявлен в `Cargo.toml`. Не моя зона, передано фактом.
3. Гонка двух окон продукта в одну папку выдачи: промежуточные файлы разведены по номеру процесса, но окно между сверкой соседей и переименованием остаётся (как и у `unique_export_path` – TOCTOU там признан осознанно).
3. Живого прогона в собранном продукте не было – сборку не запускала по границам задачи.
