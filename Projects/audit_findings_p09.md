# Аудит блока P0.9 — живые находки

**Старт:** 2026-08-03, внешний аудитор в чистом контексте.

**Задача своими словами:** проверить правку блока P0.9 в ветке `feat/econ-canon-p0` — четыре
дефекта (CPD-39 зеркальное удаление выгрузок, CPD-41 проглоченный отказ сохранения переписки,
CPD-32 текст обрыва канала сохраняется как отчёт, CPD-10 сырая английская строка отказа клиенту).
Ищу: не породила ли сама починка новый класс потери/разрастания данных, пропуски и ложные
срабатывания распознавания отказа, перехват чужих строк образцами квоты, честность клиентских
текстов (INV-50), живость новых сторожей, согласованность поля `unsaved` между слоями.

## Находки

| severity | file:line | суть | сценарий |
|---|---|---|---|
| High | `src-tauri/src/commands/claude.rs:35` | образец `limit reached` перехватывает обычное ограничение частоты | stderr `Rate limit reached. Please retry after 60s` → `UsageLimit` → `is_retryable=false` → автоповтор в `run_claude` (:565) не срабатывает, клиенту показан CL-010 «Исчерпан лимит подписки… автоматического повтора не будет» — ложное утверждение о причине (INV-50) |
| High | `src-tauri/src/commands/claude.rs:570-582` | второй путь записи файла обходит гейт CPD-32 | stderr содержит `overloaded`, а stdout успел отдать `API Error: Connection closed` → ветка `has_retryable && exit_code != 0` пишет `slug-TS-partial.md` напрямую через `std::fs::write`, минуя `auto_save_response`/`failure_notice_reason` → текст отказа снова лежит у клиента файлом-отчётом. Сторож `auto_save_checks_whether_the_answer_happened_before_writing` этот путь не видит — окно берётся от `auto_save_response` |
| High | `src-tauri/src/session/manager.rs:390` + `:317-386` | снятие зеркала сделало Рабочий стол `exports` неограниченным, а `auto_route_artifacts` на каждое сообщение перекопирует всю историю в `inbox` чужих кабинетов | 200 накопленных выгрузок с именами вида `media-plan-*.md` → каждое сообщение любого кабинета копирует их в `econometrist/inbox` и `media-analyst/inbox`; файл, удалённый пользователем из `inbox`, возвращается на следующем сообщении (через `sync_inbox`→ workspace он снова уедет в Claude) |
| Medium | `src-tauri/src/commands/claude.rs:622` | пустой ответ — молчаливый пропуск без события, вопреки заявленному инварианту «отказ громкий» | канал оборвался до первого токена (`timed_out=true`, `final_text` пуст) → ранний `return` ДО гейта: ни файла, ни `claude-stream` type=error → ровно CPD-17 «файла нет, и никто не знает почему». Сторож `failure_notice_reason("   ").is_some()` проверяет в бою недостижимую ветку |
| Medium | `src-tauri/src/commands/claude.rs:127-142` | ложное срабатывание гейта выбрасывает настоящий короткий ответ и говорит клиенту неправду | вопрос «переведи: Connection closed» → ответ `Connection closed — соединение закрыто…` (<400 симв., начинается маркером) → файл не сохранён + сообщение «Ответ не дошёл целиком, повторите команду», хотя ответ дошёл полностью (INV-50) |
| Medium | `src-tauri/src/commands/claude.rs:138-141` | `starts_with` ломается о markdown-обёртку — пропуск CPD-32 | `final_text = "**API Error**: Connection closed mid-response."` → `head` начинается со `**`, ни один маркер не совпал → сообщение об обрыве снова сохраняется как отчёт |
| Medium | `src-tauri/src/session/manager.rs:399` + `lib.rs:1455` | `std::fs::copy` под `?` валит весь ответ при заблокированном файле-приёмнике | клиент открыл выданный `.docx` в Word → следующее сообщение той же сессии повторно копирует тот же файл поверх → os error 32 → `sync_exports` Err → `send_message` возвращает отказ, хотя ответ получен. (Ответ на зону неуверенности №1: объём копирования НЕ вырос — `sync_dir_to` тоже копировал все файлы src безусловно) |
