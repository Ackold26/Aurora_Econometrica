# План выката Econometrica 2.3.1 (облачная) — решения Антона 2026-07-13

> cwd = `D:\Docs\Aurora_Ai\Dev\Aurora_Econometrica_v230` (ветка `feat/econ-v2.3.0`).
> 🔴 Сборка/публикация/живой прогон — ТОЛЬКО ВМЕСТЕ с Антоном (GUI/live/необратимое). НЕ в одиночку.

## Контекст версий
- **Опубликовано сейчас (клиентам):** облачная **2.1.0** (`v2.1.0-cloud-consent`, master `7fbfd96`).
- **Цель релиза:** **2.3.1** — скачок через 2.2.x/2.3.0 (2.3.0 наружу НЕ публиковалась). Объём =
  консолидация (kpi-units +14, avrora +27, security path-traversal) + аудит промптов (9 коммитов) +
  полировка (7 коммитов). Смоук строить от 2.1.0 — клиент увидит ВСЁ это как новое.

## Зафиксированные решения (Антон, 2026-07-13)
| # | Развилка | РЕШЕНИЕ |
|---|---|---|
| A1 | Версия | bump → **2.3.1** синхронно ×4 (Cargo.toml, tauri.conf.json, tauri.local.conf.json, package.json) — на шаге сборки по регламенту |
| A2 | Объём/смоук | от опубликованной **2.1.0** (широкий смоук) |
| B1 | Vault | **пересобрать обязательно** + bump vault-версии + контрольный греп свежей строки |
| B2 | 2c серверная vault_versions | **ОТЛОЖИТЬ** (нет existing-клиентов; installer покрывает; фикс B уже в exe, готов к 2c) |
| B3 | Content-pack | **не трогать** — sync OK (v7, подпись валидна, проверено) |
| C1 | installer_hooks/tokens | ✅ **СДЕЛАНО** (`3a7b9a2`): installer_hooks влит (фикс кодировки + окно успеха); tokens не влит (только timestamp) |
| C2 | CI на ветке | прогнать гейты в чистом окружении до тега |
| D1 | Живой прогон | **обязателен** до сборки (+проверить ReportStep completeStep, доставку Батч 0, страж INV-50, next-quarter секцию) |
| E1 | Редакции | **ТОЛЬКО облачная** (локальную не развиваем; облачная = локальная при отключении интернета) |
| E2 | Code signing | **сертификата НЕТ** → публикуем неподписанным, пометка в релиз-нотах (SmartScreen предупредит) |
| E3 | Мультипродукт | правки НЕ портировать сейчас; синхронизировать при сборке др. продуктов (общий код) |

## Пошаговый план 2b (по регламенту aurora-release-update + aurora-fix)
1. **[ВМЕСТЕ, GUI] Живой прогон** `npm run tauri:dev` (мост, env БЕЗ `ANTHROPIC_API_KEY` — egress через
   подписку). Пройти пайплайн до Отчёт → «Завершить анализ». Проверить: доставка Батч 0 (version-канал
   вызывается), страж INV-50 на сценарии, next-quarter «Что собрать» выдаётся, психо-фазы, **ReportStep
   доходит до «завершён»** (прошлый аудит: completeStep(5)→нужен (6) по 7-шкале).
2. **[механически] Bump 2.3.1** ×4 манифеста.
3. **[механически] Пересборка VAULT** `vault-packer` с актуальными промптами econometrist → bump
   vault-версии → **контрольная распаковка + греп** свежей строки (напр. next-quarter «Что собрать к
   следующей сборке» в обязательном костяке, или C1 ESOV «доля рынка (SOM)»). Нет строки → брак.
4. **[ВМЕСТЕ] Сборка облачного installer** `CARGO_TARGET_DIR="D:/cargo-targets/ai-agency" npm run tauri build`
   (пересборка sidecar). Результат: `<target>/release/bundle/nsis/*-setup.exe`. **Только облачная** (без
   `tauri:build:local`).
5. **[ВМЕСТЕ] Живой смоук .exe** — установить собранный installer, проверить: окно успеха установки (C1),
   отсутствие кракозябр в логе (C1), запуск, один сквозной сценарий.
6. **[ВМЕСТЕ, необратимо] Публикация** — ТОЛЬКО облачный канал `aurora-econometrica-gui`:
   rosst-updates `latest.json` + Supabase `app_versions` (checksum sha256 сверить). Локальный
   `-local` манифест НЕ трогаем.
7. **[после смоука] Тег** `v2.3.1` + мерж `feat/econ-v2.3.0` → master (master +247 позади).

## Открытые пред-сборочные проверки
- **C2:** прогнать полный CI в чистом окружении (PR в master или ручной прогон) — линтеры/тесты не
  гонялись на GitHub (CI check = только master/PR).
- **ReportStep completeStep(5→6):** проверить статус фикса до живого прогона (прошлый аудит-триаж).
- Незакоммиченные в kpi-units дереве: installer_hooks влит; tokens.generated.css оставлен (идентичен).

## Риски/пометки
- Без подписи: SmartScreen «неизвестный издатель» при установке — терпимо для пилота, блокер для широкой
  продажи. Signing — отдельная задача (общая с Legal/Oracle).
- 2c отложен → промпты доедут только через новый installer (existing-клиентов у Econometrica нет).
- Точка стопа: шаги 1, 4, 5, 6 — с Антоном. Шаги 2, 3 — механические (могу подготовить), но vault-греп
  проверить перед сборкой.
