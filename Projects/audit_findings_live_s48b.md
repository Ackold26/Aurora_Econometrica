# Внешний аудит блока s48b — живой журнал находок

Формат: `важность | файл:строка | суть | сценарий воспроизведения`

## Находки

High | sidecar/econometrica/aurora_pptx/builder.py:1000-1003,1027,1038,1042 | В колоде живёт ВТОРОЙ форматтер суммы переброски `_fmt_mln`, не переведённый на `format_realloc_mln`; разделитель — точка, и целое печатается как «1.0» | Расчёт с `amount_mln = 1.0`, вид `rebalance`: слайд «Ключевые находки» даёт «перераспределить 1.0 млн», слайд SCQAR той же колоды (builder.py:2914) — «Перераспределить 1 млн ₽». При 0.6 — «0.6 млн» против «0,6 млн ₽». Инвариант «единственная функция форматирования» нарушен.

High (гипотеза, нужен визуальный зонд) | sidecar/econometrica/aurora_html/interactive.py (bootstrap_js, ветка `data-copy-chart`) | Офф-скрин экземпляр для выгрузки PNG создаётся с унаследованной `animation: true`, а `getDataURL()` зовётся синхронно в том же такте — растр снимается с ПЕРВОГО кадра анимации появления | Собрать отчёт, нажать кнопку выгрузки картинкой у любого столбчатого графика: в PNG столбцы нулевой высоты / пустые оси вместо того, что на экране. Зонд: добавить `opt.animation = false;` перед `exportChart.setOption(opt, true)` и сравнить два PNG.

Medium | src-tauri/src/commands/user_config.rs (докстринг TRIAL_TERMS_REVISION vs `trial_terms_revision_matches_frontend_checkbox_text`) | Двузначность номера редакции объявлена обязательной, но НИ ОДИН сторож её не проверяет: `number_part.parse::<u32>()` принимает и однозначный | Поставить `TRIAL_TERMS_REVISION = "2026-09-15.3"` → `cargo test` весь зелёный (текст отметки «редакция 3 от 15.09.2026» проходит). Следующий бамп «2026-09-15.10»: `"2026-09-15.10" < "2026-09-15.3"` = истина наоборот → `trial_consent_outdated` = false → принявший 3-ю редакцию не увидит 10-ю никогда.

Medium | src/lib/components/TrialConsentOverlay.svelte (`$effect` + `{#if visible}`) и src/routes/+page.svelte | В окне гонки (`resolved=false`) закрыта только клавиатура: разметка оверлея не смонтирована (`visible` = сырой `trialConsentPromptOpen` = false), значит физического перекрытия нет, а мышь не проверяет гейт нигде | Подменить `invoke` на никогда не резолвящийся промис (как в trial-consent-gate-race.test.js), смонтировать главную страницу и кликнуть мышью по карточке кабинета или по шестерёнке «Настройки» — переход происходит, хотя `trialConsentBlocking` истинно.

Medium | src-tauri/src/commands/user_config.rs:121 | `c.revision.as_str() < TRIAL_TERMS_REVISION` — строго меньше: согласие на БОЛЕЕ НОВУЮ редакцию считается действительным для старой сборки | Принять условия в будущей 2.5.5 (редакция «2026-10-01.01»), поставить поверх 2.5.4 (`%APPDATA%` общий, конфиг сохраняется) → окно не поднимется, человек работает под документом, которого в этой сборке нет.

Low | src-tauri/src/lib.rs::clear_workspace_dirs | Проверки `is_file()` нет (в `clear_inbox` она есть): подкаталог, созданный человеком в папке приёма, теперь уезжает в корзину целиком при каждом запуске, раньше `remove_file` его не трогал | Создать папку внутри inbox, перезапустить программу — папки нет, она в корзине.

Low (гипотеза) | src-tauri/src/errors.rs (regex по Cargo.lock) | Регулярка склеена литеральными `\n`; при CRLF-окончаниях в Cargo.lock на машине сборки совпадения нет → тест падает с «формат Cargo.lock изменился» | Перевести Cargo.lock в CRLF (`git config core.autocrlf true` + пересборка рабочей копии) и прогнать `help_error_codes_page_matches_error_code_enum_both_ways`.

Подтверждение собственной зоны неуверенности №1 автора | src-tauri/src/soft_delete.rs (guard_soft_delete_boundary) | Половина «чего не должно быть» смотрит только тела семи перечисленных функций — откат «не вышло в корзину, сотру насовсем», вынесенный во вспомогательную функцию ТОГО ЖЕ файла, сторож не видит | Внести в brand.rs новую `fn hard_purge(p:&Path){ let _ = std::fs::remove_file(p); }` и позвать её из `brand_delete` в ветке ошибки `soft_delete` — оба теста зелёные, дефект вернулся.
