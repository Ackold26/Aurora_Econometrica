# PREBUILD s49 — предсборочный чеклист aurora-fix, Econometrica 2.5.5 (поставка со шлюзом)

Дерево: `D:\Docs\Aurora_Ai\Dev\Aurora_Econometrica_thinwt`, ветка master, git worktree.
Команда поставки: `npm run tauri:build:thin` (= `node tools/build-cloud.mjs`).
Прогон: 2026-09-18, 12:01–12:20 (время системных часов).

## Таблица вердиктов

| # | Проверка | Вердикт | Чем доказано |
|---|---|---|---|
| V1 | Версии (package.json/Cargo.toml/tauri.conf.json) | PASS | все три = 2.5.5 |
| V2 | Identifier | PASS | `com.aurora.econometrica`, совпадает с каноном products.md |
| V3 | APP_VERSION не константа | PASS | `settings/+page.svelte` берёт версию из `invoke('display_version')`; Rust-команда возвращает `env!("CARGO_PKG_VERSION")`, константы в сравнении нет |
| V52 | productName continuity | PASS | `productName="Optimizer MMM"` без изменений во всей истории `tauri.conf.json` (`git log --follow -p`), есть собственный ревёрт-коммит «вернуть Optimizer MMM для upgrade-continuity» |
| V7 | Content-pack — обязательные файлы | PASS | все 8 файлов на месте (help/ у этого продукта не входит в content-pack — справка доставляется бандлом `help-econometrica/*`, отдельным каналом, это N/A для V7 у данного продукта — см. V12) |
| V8 | Manifest checksums | PASS | пересчитан SHA-256 всех 6 файлов из `manifest.json.files` — все совпали; BOM в manifest.json отсутствует |
| V70 | Порядок: пакет подписан ДО правок содержимого | PASS | `manifest.sig` (2 авг) новее `cabinets.json`/`classifier-data.json` (20 июля) — подпись не протухла; сборка установщика в этой сессии ещё не запускалась, порядок для PHASE 3 не нарушен |
| V9/V10 | Bundle resources существуют, content-packs включены | PASS | все 6 путей `bundle.resources` физически на месте: `help-econometrica/`, sidecar exe+`_internal/`, `content-packs/*`, `sidecar/pptx_pipeline.py`, `NOTICE.md` |
| V11 | NSIS hooks | PASS | `installerHooks: "./installer_hooks.nsh"`, файл существует |
| V13 | Sidecar | PASS (с оговоркой формы) | путь ресурса — PyInstaller-заморозка (exe + `_internal/`), не сырые `.py`+requirements: продукт добился свойства «sidecar рабочий» другим устройством поставки — по критерию 1 чеклиста это законно |
| V12 | Help files | PASS | `help-econometrica/` содержит все обязательные (index/about/user-guide/error-codes/pipeline/econ-nav.js) + продуктовые страницы; код `open_help`/`open_user_guide`/`save_help_pdf` резолвит именно этот каталог (grep `src-tauri/src/lib.rs`, `user_config.rs`, `errors.rs`) |
| V60 (смежно) | Cross-variant contamination справки | WARNING (гигиена, не блокер) | `src-tauri/help/` (14 чужих кабинетов — lawyer-*, creative-director, media-analyst) физически лежит в дереве, но НЕ упомянут в `bundle.resources` (используется только `help-econometrica/*`) — в поставку не попадает. Предложение: убрать мёртвый каталог как мусор чужой синхронизации, решение не моё |
| V69 | Клиентские данные / балласт в resources | PASS | resources — точечные пути (`_internal/`, конкретный exe), НЕ `sidecar/econometrica/**/*`; внутри `_internal/` csv/results/exports/клиентских папок не найдено; известный CPD-16 (клиентские каталоги в `sidecar/econometrica/`) не бандлится этой конфигурацией |
| V16 | Secrets | PASS | оба файла на месте, `SUPABASE_SERVICE_ROLE_KEY` в env-файле есть |
| V17 | npm + clean cache | PASS | `node_modules/.package-lock.json` новее `package-lock.json` — `npm ci` не требуется |
| V18 | Exe lock | 🔴 BLOCKER (временный, объяснён) | `econometrica-sidecar.exe` (PID 25872) запущен ПРЯМО СЕЙЧАС — держит движок, который другая линия сессии (engine-rebuild) пересобирает параллельно. Перед стартом сборки установщика процесс обязан быть остановлен, иначе новые файлы не перезапишутся |
| V74 св.1 (шлюз в бинаре) | PASS (по процессу, не по свежему артефакту) | Несжатый `<target>/release/aurora-econometrica-gui.exe` в `D:\cargo-targets\econometrica\release\` даёт 5 совпадений `aurora_gateway` — НО этот файл от 14.09 (session s48), устарел относительно текущего дерева. Проба сделана правильным путём (несжатый exe, не установщик — учтена ловушка V74), но не на свежей сборке. Статически: `tools/build-cloud.mjs` собирает манифест с `--features cloud`, зовёт `verifyGatewaySource`+`checkDeliveryGuard` на КАЖДОМ прогоне (grep кода, строка вызова в `main()`) — механизм встраивания шлюза цел |
| V74 св.1а (метка/линия не протухла) | PASS (с подтверждённой оговоркой) | `Projects/_gate_core_s49b.log`: метка `aurora_gateway-v0.6.7`, отставание 1 запись (`7a14c99`) ПОДТВЕРЖДЕНО оператором (`AURORA_CRATE_TAIL_ACK`), линия дерева сошлась с origin/master. checkDeliveryGuard гоняется даже в `--test`/`--check`, а не только в релизной сборке |
| V74 св.2 (currentUser, без прав) | PASS | `installMode: "currentUser"` в tauri.conf.json; `grep -c netsh installer_hooks.nsh` = 0 (никаких правил, требующих прав) |
| V75 | Приёмка в конфигурации поставки | 🔴 BLOCKER (свежесть) | `node tools/build-cloud.mjs --test` дал 630/0 (`Projects/_gate_core_s49b.log`), НО скомпилирован `aurora-econometrica-gui v2.5.4` — журнал/лог написан в 11:56, а `Cargo.toml`/`package.json`/`tauri.conf.json` подняты до 2.5.5 в 11:58 (позже). Хуже: `src-tauri/src/commands/user_config.rs` правлен в 12:02 — ПОСЛЕ теста — это находка 5 внешнего аудита (`trial_consent_outdated`: `<` → `!=`, юридически значимая логика согласия на условия), с новыми тестами, которые НИ РАЗУ не запускались. Ни одного прогона тестов после 12:02 в дереве нет (см. список журналов) |
| V76 | Апдейтер: переезд/честный отказ для legacy-когорты | PASS | `updater.rs::needs_elevation` — фактическая проба записи в каталог (`dir_is_writable`), не разбор строки пути; `installer_launch_command` сохраняет `/S`,`/R` в обеих ветках; при отказе `apply_update` возвращает `Err`, `UpdateBlockingOverlay.svelte` ловит через `catch`, показывает `updateState='error'` с текстом — не путает с успехом |
| V77 | Снятие процесса устойчиво к чужой учётной записи | 🔴 BLOCKER (структурный, не только «не проверено делом») | Сегодняшняя правка `installer_hooks.nsh` чинит РЕАЛЬНО существовавший дефект («БЕДА ПЕРВАЯ»: `%USERNAME%` в `nsExec::Exec` не разворачивался вовсе, фильтр был мёртв с 23.05 — образец не мой, доказан зондом автора правки) и добавляет опрос по факту вместо сна. НО сам опрос `tasklist /FI "IMAGENAME eq..." /FI "USERNAME eq %USERNAME%"` СНОВА фильтрует по текущему пользователю — процесс другого пользователя тасклист не увидит, цикл сочтёт его отсутствующим за 1 попытку, установщик продолжит как ни в чём не бывало и молча пропустит залоченные файлы. Ни честного отказа (а), ни проверки по пути образа без `/FI` (б) — требуемых чеклистом — в правке нет. Плюс собственное признание автора в комментарии: «правка НЕ проверена делом на собранном установщике» |
| V79 | Внешний аудит перед клиентской поставкой | В РАБОТЕ (по брифу, не моя зона) | Параллельная линия (external-audit). Находка 5 (согласие на редакцию условий) уже дошла и внесена в код — см. V75 |

## Блокирующие находки

1. **V18 — движок сейчас запущен.** `econometrica-sidecar.exe` (PID 25872) активен — держит файлы. Сборку установщика нельзя стартовать, пока процесс не остановлен (координация с engine-rebuild).
2. **V75 — приёмка устарела относительно дерева.** «Зелёные 630/0» получены ДО подъёма версии на 2.5.5 и ДО правки `user_config.rs` (находка 5 внешнего аудита — юридически значимая логика согласия, `trial_consent_outdated`). Ни один прогон тестов не покрывает текущее состояние файла. Нужен свежий `node tools/build-cloud.mjs --test` ПОСЛЕ всех правок, до сборки.
3. **V77 — фикс не закрывает сценарий чужой учётной записи.** Опрос «процесс исчез?» в новом макросе `AURORA_KILL_AND_WAIT` использует тот же `/FI "USERNAME eq %USERNAME%"`, что и сам taskkill — процесс другого пользователя не будет обнаружен ни при снятии, ни при проверке, и установщик продолжит, решив, что путь свободен. Требование V77 (честный отказ ИЛИ проверка по пути образа без username-фильтра) не выполнено. Плюс — правка не проверена живой установкой поверх запущенной программы (признаёт сам автор правки в комментарии).

## Предупреждения (не блокируют, назвать владельцу)

- **V60-смежно:** мёртвый каталог `src-tauri/help/` с чужими 14 кабинетами (lawyer-*, creative-director, media-analyst и др.) лежит в дереве, в поставку не попадает (`help-econometrica/*` — единственный бандлящийся путь), но это мусор чужой синхронизации — предложение убрать, решение не моё.
- **build-cloud.mjs, шапка файла:** комментарий описывает скрипт как «Сборка облачной поставки Aurora AI Oracle» — похоже, унаследовано копированием из Oracle без правки заголовка. Не влияет на работу (сам код продукт-специфичен и проверен логом), но вводит в заблуждение при чтении.
- **V79:** внешний аудит идёт параллельно (не моя зона по брифу) — итог не входит в этот прогон.

## Итог

**СБОРКА ЗАПРЕЩЕНА** до устранения трёх блокеров (V18, V75, V77). После остановки движка и правки V77 — обязателен свежий прогон `node tools/build-cloud.mjs --test` на дереве в его текущем состоянии (с учётом версии 2.5.5 и правки `user_config.rs`), и только затем сборка установщика.
