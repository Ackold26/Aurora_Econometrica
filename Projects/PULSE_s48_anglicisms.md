# Пульс s48 — инвентаризация англицизмов на экранах

## Задача (своими словами)
Пройти пользовательские тексты интерфейса (`src/**` — .svelte, .ts, локализация) продукта Aurora AI Econometrica.
Собрать все латинские слова/аббревиатуры, которые видит пользователь на экране (не имена файлов, не тех.идентификаторы,
не названия продуктов/технологий/единиц/каналов). По каждой находке — файл:строка, контекст, частота.
Разделить на группы A (перевести) / B (оставить как отраслевой термин) / C (перевести с пояснением), с обоснованием.
Спорные термины (Adstock, Draws, Chains) — сверять с русскоязычной литературой по MMM/байесовскому моделированию,
не переводить буквально. Отчёт — `Projects/ANGLICISMS_s48.md`. НИКАКИХ правок кода, коммитов, пушей.

## План
1. Собрать список .svelte/.ts файлов в src/**, исключая sidecar/econometrica (чужая зона).
2. grep на латинские слова в текстовых узлах/атрибутах (label, title, placeholder, тексты кнопок, сообщения).
3. Прочитать найденные места в контексте, отсеять ложные срабатывания (имена компонентов, CSS-классы, импорты).
4. Классифицировать по группам A/B/C с обоснованием, включая специфику MMM-терминологии.
5. Записать отчёт таблицей по экранам + сводку.

## Ход работы
- 02:54 — старт, создан маячок, начинаю сбор файлов.
- 03:10 — собран src/lib/i18n (мал, 70 строк, не основной источник текстов), src/lib/data/tooltip-texts.js (169 строк, TOOLTIPS) и src/lib/glossary.js (452 строки, GLOSSARY) прочитаны целиком — канон уже переводит термины с параллельным англ. в скобках (эталон группы C).
- 03:25 — широкий grep по .svelte (Workflow/Expert/Tune/Draws/Chains/dashboard/wizard/preset/template/backtest/onboarding/toast/calibration/convergence/tornado/corridor/frontier/goal-seek/what-if/drag/drop/hover/dropdown/checkbox/toggle/slider/badge/chip/canvas/hint/tooltip), отфильтровано по наличию кириллицы на той же строке (445 строк) и убраны комментарии разработчика — вручную просмотрены все находки постранично.
- 03:40 — обнаружен ключевой факт: экраны /workflow, /brands, /brand/[id], /data-chat, /campaign гейтятся флагом $isCreativeHub (CommandPalette.svelte:41-48) — в редакции Econometrica (MMM Optimizer) недостижимы через палитру команд, хотя маршруты не удалены. Это резко снижает приоритет находок в этих экранах.
- 03:45 — прочитаны причинный экран (causal/+page.svelte, CausalMethodForm/ResultCard/ArtifactList) — обнаружен почти полностью непереведённый экран (лейблы полей формы, дисклеймер v1.0.14). Прочитаны также IntroTutorial, FirstRunTour, NavRail, GlossaryPanel — почти без находок (эталон локализации).
- 03:55 — прочитаны ConfigPanel (Adstock tooltip, Tune/Draws/Chains presets), ConvergenceDashboard, settings/+page.svelte (Inline tooltips, Expert/Novice toggle) — собраны точные строки.
- Пишу итоговый отчёт ANGLICISMS_s48.md.
- 04:15 — отчёт записан в Projects/ANGLICISMS_s48.md (26 кластеров находок: A=14, B=7, C=8 с пересечениями по спорным; 3 пункта "не уверена" вынесены отдельно). Код не менялся, коммитов/пушей не было. ЗАДАЧА ЗАВЕРШЕНА.

## Второй заход (правки групп A и C) — начат 2026-09-14 ~05:00

Задача: внести одобренные владельцем правки (группа A целиком, группа C по схеме "русский (англ.) при первом появлении на экране, дальше только русский"), НЕ трогать Creative Hub, НЕ трогать экран первого запуска/согласия, target_accept/Causal Forest/Hill function оставить как есть.

- Внесено 24 точечных правки текста в 10 файлах (см. список ниже, отправлен team-lead).
- `npm run check` (svelte-check) — 0 ERRORS, 182 WARNINGS (все существовали до правок, не по моим строкам).
- `npm run test` (vitest) — 125 test files passed, 1609 tests passed, 0 failed.
- Ничего не коммичено.
- ЗАДАЧА ЗАВЕРШЕНА.

## Третий заход (posterior'а + класс ошибки "англ.слово+рус.окончание") — 2026-09-14 ~05:20

Владелец поймал: «posterior'а» осталось в ConvergenceDashboard.svelte:302,314 — том же файле, что я правила. Задача: заменить на «апостериорное распределение», проверить весь продукт на тот же класс ошибки (латиница+кириллическое окончание, и просто латиница), пройти свои же правки на предмет соседних непереведённых слов, оставить VIF/NUTS/MCMC/target_accept как есть, приложить прогоны, посчитать строки.

- Нашла и исправила: ConvergenceDashboard.svelte (2), ExpertModelPanel.svelte (6 - posterior mean/distribution/means/параметры/draws), ImportStep.svelte (1, заодно divergences→дивергенции в той же фразе), StepSummary.svelte (2, Posterior predictive + observed→факт), insights-rules.js (1), DecomposeStep.svelte (1, posterior uncertainty).
- Широкий grep `[A-Za-z]+['ʼ’][а-яё]+` по всему src — нашёл ~50 совпадений, но 48 из них — жаргон в dev-комментариях (regex'ов, mount'ится, guard'ом и т.п.), не видны пользователю. Реально видимых было 2: posterior'а (уже нашли) и sampler'а (ExpertModelPanel.svelte:203) — оба исправлены.
- Замечена и НЕ исправлена россыпь других англицизмов в тех же тултипах не по прямому запросу (Adstock decay/long brand/Trust Level 3 в DecomposeStep, industry standard/Stan-PyMC docs/priors/tune в ExpertModelPanel bullet-списке) — вынесены отдельным пунктом находок.
- `npm run check` — 0 ошибок, 182 варнинга (прежние). `npm run test` — 125 файлов, 1612 тестов, всё зелёное (рост с 1609 до 1612 — тесты параллельного исполнителя по TrialConsentOverlay, не мои).
- 🔴 ВАЖНО: worktree общий с другими агентами сессии — `git diff --stat` изначально показал посторонние изменения в `src/lib/store.js`, `src/routes/+layout.svelte`, `src/tokens.generated.css` (работа другого исполнителя над экраном согласия TrialConsentOverlay, ровно то, что просили не трогать) — НЕ мои правки, исключены из подсчёта и отчёта.
- Итог по строкам МОИХ правок за всю задачу (два+три захода): 15 файлов, 50 изменённых строк (`git diff --stat` только по списку файлов, которые я редактировала).
- Ничего не коммичено. ЗАДАЧА ЗАВЕРШЕНА (ждём финального решения по 3+1 новым находкам).

## Четвёртый заход (унификация Adstock, три находки, target_accept) — 2026-09-14 ~05:45

Задача: 1) свести Adstock к одному имени «Остаточный эффект (adstock)» по всему классу мест (не по одному); 2) добить все три найденные ранее находки (6 лейблов CausalMethodForm, project→проект в causal, draws/tune строчными в ConvergenceDashboard); 3) убрать невыполнимый совет про target_accept, оставить только факт-дисплей; 4) прогоны + итоговая цифра строк.

- 🔴 Обнаружила: `ConfigPanel.svelte` и `AdstockPreview.svelte` уже были изменены кем-то ДРУГИМ в общем worktree на момент моей попытки правки — Adstock уже унифицирован до «Остаточный эффект (adstock)» почти везде (включая находку, которую я сама не заметила: `Adstock: {adstockAutoLabel} (авто по BIC)` на строке 868). Не переписывала эти чужие правки — они верны и совпадают с задачей; добавила только «(adstock)» в `AdstockPreview.svelte:66`, где парентетика не хватало.
- Сама добила: 6 лейблов `CausalMethodForm.svelte` (KPI column, Unit column, Time column, Treatment column, Feature columns, Confidence), `causal/+page.svelte` (project→проект ×2, causal методов→причинных методов), `ConvergenceDashboard.svelte` (draws/tune строчными → число сэмплов/прогрев ×2 места; советы про target_accept убраны на строках 300 и 308, факт-дисплей на 293/302 оставлен).
- При прогоне тестов обнаружился 1 упавший тест (`insights-uncovered-functions.test.js:505`, ожидал `'Adstock'` с большой буквы) — регрессия от ЧУЖОЙ правки `insights-rules.js:1144` (та же унификация Adstock, но со строчной «adstock»). Обновила ассерт теста под новую (верную) формулировку — не мой источник правки, но тест был мой к прогону.
- Финальные прогоны: `npm run check` — 0 ошибок, 182 варнинга (прежние). `npm run test` — 125/125 файлов, 1612/1612 тестов зелёные.
- Итог по строкам за ВСЮ задачу (все 4 захода, `git diff --stat` по списку файлов, которые я или редактировала, или проверяла/дополняла вслед за чужими правками): **17 файлов, 71 изменённая строка**. Из них ConfigPanel.svelte (18 строк) — не мои правки, чужие (проверены и не тронуты повторно).
- Ничего не коммичено. ЗАДАЧА ЗАВЕРШЕНА.
