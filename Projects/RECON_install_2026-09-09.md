# RECON — какая сборка Optimizer MMM установлена (2026-09-09)

## Вывод одной строкой

**Установлена СЕДЬМАЯ сборка (15:27), НЕ восьмая.** Восьмая (комплект показа, `Optimizer MMM_2.5.1_x64-setup.exe`, 08.09 16:11) на машине не поставлена. Перед показом покупателю нужна переустановка.

## Доказательство

### 1. Где физически лежит установленная программа

Реестр `HKCU:\Software\Microsoft\Windows\CurrentVersion\Uninstall\*`:
- DisplayName: `Optimizer MMM`, DisplayVersion: `2.5.1`
- InstallLocation: `C:\Users\ackol\AppData\Local\Optimizer MMM`
- UninstallString: `C:\Users\ackol\AppData\Local\Optimizer MMM\uninstall.exe`

Ярлык меню «Пуск» (`%APPDATA%\Microsoft\Windows\Start Menu\Programs\Optimizer MMM.lnk`) указывает туда же: `C:\Users\ackol\AppData\Local\Optimizer MMM\aurora-econometrica-gui.exe`. Реестр и ярлык согласованы — это и есть активная установленная программа (installMode: currentUser, как требует политика выпуска).

**Побочная находка:** есть второй ярлык `Optimizer MMM Local.lnk` → `C:\Program Files\Optimizer MMM Local\aurora-econometrica-gui.exe`. Это отдельная, более старая служебная/dev-сборка (главный .exe от 29.07.2026 01:27, 15 128 064 байт) — к комплекту показа отношения не имеет, в реестре Uninstall не числится, для вывода не учитывается. Отдельно проверено: `C:\Program Files\Aurora AI Econometrica` содержит только пустой `_up_` (след предыдущей деинсталляции/переноса) — программы там нет.

### 2. Время сборки, размер, хэш главного .exe установленной программы

`C:\Users\ackol\AppData\Local\Optimizer MMM\aurora-econometrica-gui.exe`
- Размер: 16 019 456 байт
- LastWriteTime / CreationTime: **08.09.2026 15:21:58**
- SHA256: `2F52B556C611536EDDCCFCED7858975B59B3D16353A6F8E3C7F154B2D1EAE6E3`

Прямое сравнение с хэшами установщиков невозможно (установщик — контейнер NSIS, не совпадает побайтово с распакованным .exe). Но время файла — 15:21:58 — на 5–6 минут раньше времени сборки седьмого установщика (15:27) и почти на 47 минут раньше восьмого (16:08). Это соответствует обычному порядку: сначала собирается `.exe`, затем через несколько минут пакуется установщик. Само по себе это косвенный, не решающий признак — решающий см. п.4.

### 3. Какой установщик её поставил

По времени файлов (п.2) и решающему текстовому признаку (п.4) — **установщик `_1527.exe` (седьмая сборка, 15:27)** из `_прежние_сборки\`, а не `Optimizer MMM_2.5.1_x64-setup.exe` (восьмая, 16:08) из комплекта показа.

### 4. РЕШАЮЩИЙ ПРИЗНАК — формулировка «gap» vs «разрыв»

Из репозитория (`git log --oneline -3 -- '*channel_action.py'`):

```
81cd7c32 fix: третья формулировка разрыва в движке — «разрыв», а не «gap»
9f12f600 fix: величина разрыва в текстах движка — «разрыв», а не «gap», и один знак после запятой
```

Диф коммита `81cd7c32` (`sidecar/econometrica/engines/channel_action.py`, строка 446):

```diff
-                f'{_metric_str}, gap +{eff_gap:.0f}пп - канал даёт больше эффекта '
+                f'{_metric_str}, разрыв +{eff_gap:.1f} пп - канал даёт больше эффекта '
```

Седьмая сборка = старая строка (`gap +{:.0f}пп`), восьмая = новая (`разрыв +{:.1f} пп`).

Найдено внутри установленной программы **три копии файла**, во всех трёх — **старая** формулировка (строка 446):

| Путь внутри `C:\Users\ackol\AppData\Local\Optimizer MMM\_up_\sidecar\econometrica\...` | Строка 446 | Время файла |
|---|---|---|
| `dist\econometrica-sidecar\_internal\engines\channel_action.py` (реально исполняемый sidecar) | `f'{_metric_str}, gap +{eff_gap:.0f}пп - канал даёт больше эффекта '` | 08.09 15:16 |
| `_internal\engines\channel_action.py` | то же (старое) | 08.09 15:16 |
| `engines\channel_action.py` | то же (старое) | 08.09 15:09 |

Все три файла отдают устаревший текст «gap +Nпп», а не «разрыв +N,N пп». **Это однозначно доказывает: на машине стоит седьмая сборка** — фикс из коммита `81cd7c32` (восьмая сборка) в установленный код не попал.

Согласуется и по данным: `results\decomposition.json` в `demo-fmcg-brand` пересчитан 08.09 15:40 — раньше коммита фикса (15:43:32) и раньше сборки восьмого установщика (16:08). То есть эталонный демо-проект тоже посчитан ещё старым (седьмым) движком.

## Каталог данных (`%APPDATA%\aurora-econometrica-gui\projects\`)

| Проект | Время каталога | Размер | models | results |
|---|---|---|---|---|
| `demo-fmcg-brand` | 08.09.2026 15:40 | 3,4 МБ | есть, непусто | есть, непусто (`decomposition.json` пересчитан 08.09 15:40 — **старым, седьмым движком**) |
| `synth-fmcg-brand-ммх-1007-26` | 08.09.2026 15:12 | 2,8 МБ | есть, непусто | есть, непусто |
| `кагоцел-рф--данные-для-эконометрики---на-ммх-3108-26` | 08.09.2026 05:36 | 12 КБ | пуст | есть, непусто |
| `демо-медиаплан-с-хвостом-ммх-1007-26` | 20.07.2026 04:29 | 3,6 МБ | есть, непусто | есть, непусто |
| `кагоцел-рф--данные-для-эконометрики---на-ммх-2306-26` | 04.07.2026 04:19 | 4,7 МБ | есть, непусто | есть, непусто |
| `synth-fmcg-a5-a6-ммх-1007-26` | 10.07.2026 14:30 | 12 КБ | пуст | есть, непусто |
| `synth-fmcg-brand-ммх-0507-26` | 10.07.2026 13:17 | 2,9 МБ | есть, непусто | есть, непусто |
| `кагоцел-рф--данные-для-эконометрики---на-ммх-0208-26` | 02.08.2026 22:32 | 12 КБ | пуст | есть, непусто |
| `mmx-2021-2025-исходник-ммх-2704-26` | 27.04.2026 02:33 | 8 КБ | пуст | есть, непусто |

`demo-fmcg-brand` — последний пересчёт: `results\decomposition.json` 08.09.2026 15:40:18 (седьмым движком, см. выше); остальные файлы results (`media_plan.json`, `model-diagnostics.json`, `optimization.json`, `validation.json`) и `models\latest.pkl` — 31.08.2026 22:20–23:11 (более раннего расчёта, до самой седьмой сборки).

### Три клиентских проекта «Кагоцел»

Точные имена каталогов:
1. `кагоцел-рф--данные-для-эконометрики---на-ммх-0208-26` — 12 КБ
2. `кагоцел-рф--данные-для-эконометрики---на-ммх-2306-26` — 4,7 МБ
3. `кагоцел-рф--данные-для-эконометрики---на-ммх-3108-26` — 12 КБ

Суммарный размер: **≈4,72 МБ** (4,7 МБ + 12 КБ + 12 КБ).

## Где искал и не нашёл

Всё найдено, пробелов нет. Для полноты: помимо трёх копий `channel_action.py`, найден и `.pyc`-кэш (`__pycache__\channel_action.cpython-312.pyc`) в тех же трёх местах — байткод не проверялся текстовым поиском (бинарный), но при неизменном `.py`-исходнике и одинаковом времени модификации `.py`/`.pyc` доверия к нему как к отдельному источнику не требуется — вывод сделан по исходному коду.
