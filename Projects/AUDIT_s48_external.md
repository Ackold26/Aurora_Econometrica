# Внешний аудит s48 (независимый взгляд)

Задача своими словами: я внешний аудитор. Проверяю диапазон `0d195968..HEAD` (2 записи, 73 файла)
в Aurora AI Econometrica перед передачей покупателю на недельный тест. Ищу вред клиенту:
ложь в числах и справке, утечку личных данных, сторожей-пустышек (мутирую их), регрессы,
незакрытые классы дефектов. Стилистика — не находка. Ничего не коммичу.

Старт: 06:12 (14.09.2026)

## Находки (СЕРЬЁЗНОСТЬ | файл:строка | суть | вред клиенту)
High | sidecar/econometrica/engines/narrative_adapter.py:629 (+ aurora_pptx/builder.py:2914,2916,2918,3009,3014,3023; aurora_html/sections.py:739) | Порог значимости сведён к 0,5 млн, а ВСЕ места печати суммы остались с округлением до целых `:.0f`. Зонд (реальный вызов derive_action_headline): 0.5 -> «Перераспределить 0 млн руб в Онлайн-видео»; 0.6 и 0.7 -> «1 млн руб». До s48 колода гейтилась порогом 1,0 и эта полоса до неё не доходила — регресс класса «число на экране ≠ расчёт», внесённый самой починкой порога. | Клиент видит в заголовке слайда совет перебросить «0 млн руб» (абсурд) либо «1 млн руб» там, где расчёт дал 0,6 — завышение на 67 %.
Medium | sidecar/econometrica/tests/test_reallocation_threshold_single_source.py:~200 | Сторож стережёт СОГЛАСОВАННОСТЬ мест, но не саму величину порога. Мутация `SIGNIFICANT_REALLOCATION_MLN = 100.0` — оба теста файла зелёные (проверено). | Случайная правка константы молча выключает совет по перебросу во ВСЕХ документах (отчёт, колода, заголовки), клиент недополучает рекомендацию, а проверка это пропускает.
High | sidecar/econometrica/tests/test_reallocation_threshold_single_source.py:test_reallocation_significance_agrees_everywhere_in_the_gap_zone | Поведенческий сторож НЕ ловит возврат дефекта. Мутация: в aurora_pptx/builder.py:2913 ветку ОТВЕТА SCQAR снова загейтил порогом (`and realloc >= 1` и `and realloc > 1`) — ровно тот дефект, ради которого тест написан. Тест зелёный в обоих случаях: `_names_cut_source` читает ТЕКСТ ВСЕГО слайда, а блок РЕКОМЕНДАЦИИ на том же слайде продолжает называть cut_source, маскируя молчание блока ОТВЕТ. | Слайд SCQAR снова сможет говорить «сохранить аллокацию» при 0,5–1 млн, пока проверка рапортует «все места согласны».
Medium | sidecar/econometrica/tests/test_reallocation_threshold_single_source.py:_INLINE_THRESHOLD_RE | Сторож исходников ловит только `>=`. Мутация `realloc > 1` — зелёный; `realloc >= 1` — красный (обе проверены). | Эквивалентная запись порога литералом (`>`, `<`, `1 <= realloc`) вернёт расхождение мимо сторожа.
High | sidecar/econometrica/tests/test_html_datalabel_contrast.py:80 (+ aurora_html/interactive.py:254) | Сторож контраста проверяет ПОРТ логики на питоне (`_label_color_for_fill`), а не отгружаемый JS. Мутация: тело `labelColorForFill()` в interactive.py заменено на `return '#FFFFFF';` (имя функции, `relLuminance`, литерал 0.179 в комментарии остались) — все 17 тестов contrast зелёные. | Подпись значений снова станет нечитаемой белой на светлом столбце (2,3:1), и ни одна проверка этого не заметит; клиент получает отчёт с нечитаемыми числами.
High | sidecar/econometrica/aurora_html/interactive.py:1062 (кнопки «Сохранить PNG», sections.py:1068,1261,1287…) | Кнопка обещает PNG, а отдаёт SVG под расширением .png. Хосты графиков — `renderer:'svg'` (interactive.py:627,814); в echarts 5.5.1 `getDataURL` при svg-painter возвращает `this.getSvgDataURL()` (проверено по тексту бандла echarts.common.5.5.1.min.js), то есть `data:image/svg+xml;…`, игнорируя `type:'png'` и `pixelRatio`. Файл сохраняется как `<chart>-aurora.png`. Дефект не внесён этой работой, но правка s48 трогала ровно этот путь и её собственный комментарий фиксирует, что опции getDataURL игнорируются. | Клиент жмёт «Сохранить PNG», получает файл .png, который не открывается штатным просмотрщиком Windows и не вставляется в PowerPoint/Word как картинка.
High | src/routes/+page.svelte:150-164 (регистрация — :167) | НЕЗАКРЫТЫЙ КЛАСС: обход блокирующего экрана условий закрыт только в `$lib/global-shortcuts.js`, а на ГЛАВНОЙ (маршрут, на котором экран и появляется) живёт второй window-обработчик без проверки согласия: цифры 1–9 открывают кабинет, Ctrl+, уводит в Настройки. Экран условий не гасит всплытие (onkeydown обрабатывает только Tab), а маршрут рендерится под оверлеем (+layout.svelte:311 app-shell без условия). Фокус на старте не в поле ввода (autofocus в оверлее нет), поэтому фильтр INPUT/TEXTAREA не спасает. | Клиент начинает работу в программе (открывает кабинет, уходит в Настройки), не приняв условия — ровно то, что гейт обязан не допустить; юридическая конструкция «начало использования = согласие» рушится.
Medium | src/routes/cabinet/+page.svelte:233-247 (регистрация — :275) | Тот же класс: обработчик кабинета (Ctrl+Shift+Z, Escape→выход) тоже не смотрит на `trialConsentPromptOpen`. Достижим сразу после обхода с главной. | Продолжение работы под непринятыми условиями.
High | src-tauri/help-econometrica/econometrica-help.pdf (140 ссылок-аннотаций, напр. смещение 279333) | В поставляемом PDF справки зашиты ссылки вида `file:///C:/Users/ackol/AppData/Local/Temp/aurora-help-pdf-m48rjgt3/about.html` — имя пользователя сборочной машины и её временный каталог. Дефект НЕ внесён этой работой (в 0d195968 те же 140 вхождений), но PDF пересобран в этой работе и едет покупателю. | Утечка личных данных владельца плюс мёртвая навигация: все ссылки оглавления справки у клиента ведут в несуществующий путь.
Info | src-tauri/help-econometrica/ (рабочее дерево, не запись) | Второй правовой PDF в дереве переименован в «Порядок обработки данных - Aurora AI Econometrica.pdf» и имеет ДРУГУЮ сумму (26b666d5…) против манифеста (524467505c…, имя «… — Econometrica.pdf»). Сейчас `cargo test data_processing_pdf_matches_manifest_hash` обязан падать. Похоже на незавершённую правку владельца дерева — не записываю в находки диапазона. | Если уедет как есть — открытие второго документа из настроек не найдёт файл.
Medium | src-tauri/src/errors.rs:help_error_codes_page_matches_error_code_enum_both_ways + help-econometrica/error-codes.html | Модель «достижимо = встречается как `ErrorCode::` в src-tauri/src» неверна: коды строит и общий крейт. `aurora_core::feedback::submit` (pinned rev 35d8345, feedback.rs:73) возвращает `[FB-002] Подождите минуту перед повторной отправкой`, а зовётся из `src-tauri/src/commands/feedback.rs:349` (команда `submit_feedback`, lib.rs:4170). Справка FB-002 не описывает (есть только FB-001), и новый сторож ДОБАВИТЬ его запрещает — попадёт в список «выдумка». | Клиент, нажавший «Отправить» второй раз за минуту, видит код FB-002 и не находит его на странице кодов ошибок; правка справки будет валить прогон.
Low | src/routes/causal/+page.svelte:87 | В тексте экрана названо имя стороннего клиента — «после получения региональных данных Materia Medica». Экран сейчас НЕДОСТИЖИМ (кнопка «Причинность →» закомментирована в src/routes/+page.svelte:393-407, других ссылок нет), поэтому покупателю не покажется. | Если маршрут вернут в навигацию, имя чужого клиента уедет в интерфейс к другому покупателю.
Low | sidecar/econometrica/charts/generators.py:51 (waterfall_chart) | Тот же класс, что починили в отчёте: подписи значений белые (`color='white'`) ВНУТРИ столбцов, залитых CHANNEL_COLORS. Посчитанный контраст белого к каждой из десяти заливок: 2.15–4.23:1, все ниже нормы 4.5. НО достижимость сегодня отсутствует: единственный путь — `econ_chart` с chart_type='waterfall', а фронт запрашивает только 'response_curves' (ScenarioCompare.svelte:221). | Если тип графика вернут в интерфейс, повторится ровно та жалоба владельца, из-за которой делалась правка s48.
Low | src/lib/components/pipeline/ConvergenceDashboard.svelte:242 | Баннер считает параметры по `v >= 1.05` (строка 203), а текст говорит «параметров с R-hat > 1.05». Ровно на 1.05 счётчик и текст спорят. Дефект не внесён этой работой. | Мелкое расхождение числа и подписи в техническом блоке.

## Что проверено и НЕ дало находок (тоже результат)
- Порог сходимости в справке (`R-hat ≥ 1.05`, отключение авто-переброски) сверен с кодом: `RHAT_REFUSE_THRESHOLD = 1.05`, сравнение `>=` (optimizer_honesty.py:40,99,215). Совпадает.
- Состав лицензионного трафика в справке сверен с `AuthRequest` (online_auth.rs:505-516): fingerprint_hash, instance_id, session_id, app_version, content_version, hostname, product, отметка облачного согласия. Перечень в справке полный и верный; данных клиента в запросе нет. Прежняя формулировка («только идентификатор машины и статус») была ложью — исправлена верно.
- Обращение в поддержку (feedback.rs:196-230, 296-311): уходит только хеш отпечатка, версия, время, сводка ОС (`os_version_summary` — без имени машины). Имени пользователя, имени машины, путей нет.
- Суммы обоих правовых PDF пересчитаны вручную: «Условия…» совпадает с манифестом байт в байт.
- Манифест справки `tools/help_pdf_manifest.json`: все 23 HTML совпали по сумме, лишних/недостающих нет.
- Поставляемый `econometrica-help.pdf` (116 страниц) реально содержит новые формулировки: «отпечаток устройства» ×3, «R-hat ≥ 1.05» ×2, старого «только идентификатор машины» нет.
- Сторож гигиены клиентских текстов не ослаблен — из списка исключений убраны четыре файла (тексты реально поправлены), а не добавлены новые.
- Сторож обхода горячих клавиш ЖИВОЙ: мутация `if (trialConsentOpen && false) return;` красит 3 теста.
- Сторож сумм документов ЖИВОЙ: красное доказано юнит-тестами на синтетике, красит и на реальном расхождении (сейчас падает на втором документе — см. Info выше).
- z-index блокирующего экрана (10001) выше всех прочих слоёв (палитра 9998, обновление 9999, облачное согласие 10000) — перекрытия сверху нет.
- `npx vitest run`: 1617/1618. Единственный отказ — `error-feedback-buttons-optimize-model.test.js` (таймаут 5 с под нагрузкой параллельной сборки); в одиночном прогоне зелёный, регрессом НЕ является.
- `pytest sidecar/econometrica/tests`: 1685 пройдено, 2 пропущено, отказов нет.
- Коды ошибок: python-движок кодов вида `XX-NNN` не порождает; в закреплённой редакции `aurora_core` (35d8345) AU-*/SY-* нигде не конструируются — решение исключить их из справки верное.

## Непроверенное (на что не хватило бюджета)
- `cargo test` не запускался вовсе (компиляция дорогая, в дереве идёт сборка): Rust-сторожи прочитаны и разобраны по коду, но не прогнаны. `data_processing_pdf_matches_manifest_hash` по состоянию дерева обязан падать.
- `npm run check` (svelte-check) не запускался.
- Живого прогона окна условий в собранном приложении не было — обход клавишами доказан чтением кода (обработчик на window + оверлей не гасит всплытие + маршрут рендерится под оверлеем), не кликом.
- Колода PPTX целиком не собиралась; проверены функции текста заголовков и разбор веток, не визуальный слайд.
- Содержание самих правовых PDF (текст условий и порядка обработки) не читалось — сверялись только имена, суммы и факт поставки.
