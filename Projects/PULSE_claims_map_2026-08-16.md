# PULSE — сверка обещаний сайта о воспроизводимости с кодом (2026-08-16)

## Пересказ задачи
Сайт обещает сильную воспроизводимость («сторонний аналитик воспроизводит модель
самостоятельно»), код сегодня — слабую («программа повторяет свои числа на той же
машине»). Нужно построчно сверить каждую цитату из готовой карты обещаний
(`aurora-platform-web/Projects/PASSPORT_CLAIM_MAP.md`, 12 файлов) с фактическим кодом
в `sidecar/econometrica/`, определить силу формулировки и вердикт по каждой строке
отдельно. Только чтение, ничего не править.

## План
1. Найти PASSPORT_CLAIM_MAP.md — сделано (`aurora-platform-web/Projects/`).
2. Прочитать карту целиком — сделано.
3. Прочитать код: seeding.py, modeler.py (паспорт reproducibility), json_export.py,
   methodology_cert.py, test_mcmc_determinism_live.py, sections.py (render_methodology),
   channel_categorization.py — подтвердить факты, данные лидом, своими глазами.
4. Свести таблицу утверждение-за-утверждением (не одной строкой на весь сайт).
5. Выделить: утверждения, не закрываемые блоком; утверждения сильнее факта; что не
   удалось проверить.
6. Записать отчёт в Projects/REPORT_claims_map_2026-08-16.md.

## Ход работы
- 14:48 — старт. Карта обещаний найдена и прочитана целиком (12 позиций/файлов,
  разметка [слабое]/[сильное] уже есть в карте).
- 14:49-14:51 — прочитан код: `utils/seeding.py`, `engines/modeler.py` (786),
  `engines/json_export.py`, `engines/methodology_cert.py`,
  `tests/test_mcmc_determinism_live.py`, `utils/channel_categorization.py`.
  Ключевая находка: карта составлена по ветке `Aurora_Econometrica_v230`
  (07-26), я сверяю по `Aurora_Econometrica_thinwt` (08-16) — код УСПЕЛ
  измениться: `methodology_cert.py` больше НЕ мёртвый, подключён к
  `decomposer.py:591 _build_methodology_certificate` и реально рендерится в
  HTML/PPTX (`sections.py:1802 _render_certificate_block`,
  `aurora_pptx/builder.py:145 строки_сертификата`). `json_export.py`
  (коэффициенты по каналам) по-прежнему мёртв — 0 вызывающих, подтверждено
  grep.
- 14:52 — прочитан `sections.py:1914 render_methodology` +
  `_render_brand_perf_split_block` + `strings_ru.json` (formulas/prior_note) —
  подтверждено: формула публикуется в общем виде без чисел, приоры — либо
  шаблонная фраза, либо (только при иерархических приорах) производная
  величина «период полураспада» по ГРУППЕ каналов, не по каждому каналу и не
  сырые mu/sigma. Ключевая цитата кода: статус заверения
  «...повторный запуск НА ТЕХ ЖЕ ДАННЫХ В ТОЙ ЖЕ СРЕДЕ даёт те же числа» —
  прямое подтверждение слабой формы внутри самого продукта.
- 14:52 — таблица сведена, отчёт готов.
- 14:53 — завершено. Отчёт отправлен team-lead текстом сообщения (харнесс блокирует запись
  отдельного report/findings-файла субагентом) — полный текст таблицы и разделов (1)-(3)
  передан через SendMessage, не в `Projects/REPORT_claims_map_2026-08-16.md`.
