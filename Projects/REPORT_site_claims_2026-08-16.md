# Свип класса «обещание сильнее факта» по сайту — 2026-08-16

Исполнитель `site-claims-fix` (Sonnet). Правка одобрена владельцем 16.08. Отчёт записан мной:
исполнителю харнесс запрещает создавать файлы отчётов. **Публикации не было**, правки лежат в
рабочем дереве `aurora-platform-web`, в общий репозиторий не отправлялись, меток нет.

## Итог

**13 адресов в 8 файлах**, русские и английские близнецы правлены симметрично.
🔬 **Моя верификация:** греп по живому коду (`app/`, `components/`, `content/`, `.design-sync/`)
выражением
`воспроизвести вручную|воспроизвести самостоятельно|за пределами программы|перепроверить со стороны|reproduce by hand|independently reproduce|независимое доказательство`
даёт **ноль совпадений**. Единственный оставшийся хит во всём дереве — внутри комментария
`app/[locale]/mmm/page.tsx:167`, который документирует прежний фикс M-7 и не рендерится.
`npx tsc --noEmit` — 0 ошибок, длинного тире «—» в правленых строках — 0.

## Главная правка (проверена мной дословно)

`components/sections/WhyAurora.tsx:126` (главная страница):
- **было:** «…паспорт воспроизводимости – полная спецификация модели, **которую сторонний аналитик
  может воспроизвести самостоятельно**. Мы не просим верить на слово.»
- **стало:** «…паспорт воспроизводимости: **специалист с теми же данными и в той же среде получит
  те же числа**. Мы не просим верить на слово.»

Продающая часть сохранена и **теперь подкреплена фактом**: паспорт действительно есть, доезжает
до клиента и содержит зерно, ярус сэмплера, раскладку цепей, версии библиотек и платформу.

## Состав правок

| № | Адрес | Класс правки |
|---|---|---|
| 1 | `components/sections/WhyAurora.tsx:126,128` | «сторонний аналитик воспроизводит самостоятельно» → честная форма |
| 2 | `components/sections/ProofBar.tsx:50-51` | «воспроизвести вручную» → честная форма |
| 3 | `components/sections/ProgramFeature.tsx:277,281` | «перепроверить со стороны» → честная форма |
| 4 | `content/products/optimizer.ru.mdx:80` + `.en.mdx:80` | «воспроизвести вручную за пределами программы» |
| 5 | `optimizer.ru.mdx:131` + `.en.mdx:131` | «независимое доказательство корректности» → «проверяемое подтверждение **целостности** расчёта и раскрытая методология» |
| 6 | `optimizer.ru.mdx:163-167` + `.en.mdx` | FAQ про паспорт; убрано «источники данных» – паспорт их не фиксирует |
| 7 | `app/[locale]/methodology/page.tsx:111-113` | «источники и период данных» → «зерно расчёта, версии библиотек и платформа» (то, что реально записано) |
| 8 | `methodology/page.tsx:114-116` | приоры – **при иерархических приорах**, а не безусловно |
| 9 | `methodology/page.tsx:176-178` | «формула, приоры и диагностика в каждом отчёте» → приоры условно |
| 10 | `app/[locale]/about/page.tsx:82-84` (ru) и `:210-212` (en) | то же, что №9 |
| 11–13 | `.design-sync/previews/ProductPage.tsx:139-141, 214-216, 250-252` | тот же класс в фикстуре превью |

**Сверх моего списка исполнитель нашёл три адреса:** страницу `methodology` (сразу три нарушения –
и это самая чувствительная страница для аудиторов и финансовых директоров), зеркало в `about`,
и подтвердил, что `.design-sync/previews/` ведётся руками, а не генерируется.

**Своя ошибка, пойманная исполнителем до сдачи:** двоеточие внутри незакавыченного скаляра YAML
ломало разбор заголовка `.mdx`; поймано парсером `gray-matter`, исправлено, перепроверено.

## 🟢 Основание правок — факт кода (проверено)

- `utils/seeding.py:113-164` — паспорт несёт зерно, источник зерна, ярус сэмплера, раскладку цепей,
  число устройств JAX, наличие компилятора, параметры MCMC, версии, платформу и (с 16.08)
  отпечаток данных. **Периода данных там нет** – потому формулировка про период убрана.
- `utils/data_fingerprint.py` — отпечаток отвечает на вопрос «те ли это данные», но **не является
  записью источника** (Mediascope, DSM и т. п.) и не является периодом.
- `aurora_html/sections.py:1691-1751` — числовой блок приоров рендерится **только** при
  иерархических приорах. Значит «приоры раскрыты» безусловно – ложь, условно – правда.

## 🔴 Открытые пункты (мои решения и то, что за владельцем)

1. **`content/products/launch.ru/en.mdx:99`** – «построен по задокументированной, воспроизводимой
   методологии». **Моё решение: не трогать сейчас.** Launch Planner – другой продукт, его кода мы
   не смотрели (в сверке обещаний позиции 17–19 помечены «не проверено»). Править текст чужого
   продукта, не проверив его код, – то же гадание, только с другой стороны. Сначала аудит Launch
   Planner, потом слова.
2. **`.design-sync/previews/ProductPage.tsx`** устарела шире этого класса: старое имя «сертификат
   методологии», сравнение с BASES Marketing Mix, которого в живом контенте больше нет.
   **Моё решение: отдельная задача на полный ресинк**, в этом блоке не делаем.
3. **`Projects/onepagers/mmm-optimizer.md`** – ✅ **поправлен** (владелец подтвердил 16.08, что
   ванпейджер раздаётся клиентам). Две строки: 21 («воспроизвести вручную за пределами программы»
   → честная форма) и 68 («независимое доказательство корректности» → «проверяемое подтверждение
   целостности расчёта и раскрытая методология»). Греп после – ноль совпадений, длинного тире 0.
   Терминология файла («сертификат методологии», а не «паспорт воспроизводимости») сохранена как
   была – унификация имён это отдельное решение, самовольно не делали.

   🔴 **Находка сверх задания, оставлена владельцу по его слову.** Собственно ванпейджер – первые
   **86 строк** из 151. Строки **87–151** – внутренняя рабочая переписка, попавшая в клиентский
   документ: обращение к владельцу по имени, цена продукта «700 тысяч в год», внутренние
   обозначения задач и волн, планы развития, ссылки на внутренние файлы дорожной карты и
   завершающее «мы с тобой отличная команда». Проверено мной лично чтением файла.
   Остальные четыре ванпейджера (`creative-center`, `legal-center`, `smart-analytica`,
   `synthetic-research`) **чисты** – грепом по признакам внутреннего текста совпадений нет.
   **Решение владельца 16.08: оставить, разберётся сам.** Файл не тронут.
4. **`.next/standalone/`** – 6 файлов со старыми формулировками. Сборочная копия, не трогали;
   исчезнут при следующей сборке. Проверить после неё.
