# Аудит починки находок P0.2–P0.5 (записи a129506, 975404e) — 2026-08-03

Внешний аудитор, чистый контекст. Формат: severity | file:line | суть | сценарий отказа с числами

High | src/lib/components/pipeline/ValidateStepV13.svelte:861 + 1361 (RatioInfoCard.svelte:108) | Разведение «гейт vs показ» вернуло два разных числа ОДНОЙ величины на один экран. На под-шаге «Роли колонок» карточка «Запас данных» печатает эффективное `ratioCardData.ratio`, а красная плашка «Нельзя продолжить» прямо под ней (ColumnMapperConfirm.svelte:858, видимый текст, не подсказка) печатает `data.gate` — и обе подписаны одинаково по смыслу («Запас данных N:1» / «Наблюдений на параметр N:1») | Зонд на живом модуле ratio-classifier.js: 20 наблюдений, 10 назначенных столбцов, 12 авто-праздников, байес. Карточка: «0,9:1 – Модель не определяется». Плашка под ней: «Наблюдений на параметр 1,8:1 – ниже минимальных 2:1». Другие точки: 18/9 → 0,8 против 1,8; 30/16 → 1,0 против 1,8; 21/10 → 0,9 против 1,9. Ровно тот класс дефекта, ради которого писался P0.3 (движок печатал 2,7 против карточки 1,3) — число просто переехало из сообщения движка в плашку фронта

Medium | sidecar/econometrica/engines/validator.py:658-682 | В долю «не читаются как числа» попадают ПУСТЫЕ ячейки: `pd.to_numeric(..., errors='coerce').isna()` не отличает текст от пропуска. Совет в том же сообщении – «оставьте ячейки пустыми» – прямо противоречит собственному счёту | Зонд (реальный прогон validate_data, 40 строк, колонка digital_spend: 1 текстовая «н/д» + 3 пустые): выдано «digital_spend - 10.0% значений не читаются как числа (4 из 40). Например: н/д. … Замените текст числами или оставьте ячейки пустыми». Реально не читается ОДНА ячейка (2,5%), три оставшиеся при обучении становятся нулём (`fillna(0)`) и в числовой колонке те же три дали бы мягкое предупреждение «пропусков 7,5% … считаются нулём». Одни и те же данные получают то critical, то warning – в зависимости от соседней ячейки

Medium | src/lib/components/pipeline/TrafficLight.svelte:236-258 | Новый `stats` без `min/max/mean/std/cv/zeros_pct` пробивает условие `{#if col.stats}`, и битая колонка впервые попадает в таблицу «Статистика по столбцам» – с прочерками и ЗЕЛЁНОЙ подсветкой. `cvClass(undefined)`: `undefined < 5` → false, `undefined < 20` → false → 'val-ok'; `zerosClass(undefined)` → 'val-ok'; `fmtNum(undefined)` → '-' (src/lib/fmt.js:25) | Файл из зонда выше: строка «digital_spend | - | - | - | -% (зелёный CV) | -% (зелёные нули) | 4 (жёлтые пропуски)». До починки колонка в таблицу не попадала вовсе. Чинили «0% пропусков на самой битой колонке» – получили «зелёные CV и нули на самой битой колонке», тот же класс: признак здоровья там, где здоровья нет

Medium | sidecar/econometrica/engines/validator.py:658 ↔ :187-209 | Внутри ОДНОЙ функции два разных парсера числа: гейт роли `_is_numeric_parseable` чистит валюту/пробелы и пробует обе стратегии запятой (money-строки и «11,35» проходят, роль сохраняется), а новая ветка зовёт голый `pd.to_numeric` и объявляет те же значения нечитаемыми | Зонд: колонка radio_spend со значениями «11,35», «11,4» (десятичная запятая – штатный вывод русского Excel в CSV; `_read_csv_smart` подхватывает разделитель «;», но `decimal=','` не задаёт) получает роль media (гейт доволен) И critical «radio_spend - 100.0% значений не читаются как числа (40 из 40). Например: 11,35, 11,4, 11,94 … Замените текст числами». Пользователь видит в ячейках числа, и совет «замените текст числами» ему нечего исполнить; на файле из русского Excel такой critical выпадет на КАЖДУЮ денежную колонку

Medium | src/lib/ratio-classifier.js:183-197 ↔ sidecar/econometrica/engines/modeler.py:398-416 | Знаменатель научили мастер-переключателю праздников, но не поштучному отключению (`disabled_holidays`): та же панель, та же логика, дефект остался на слой ниже | ConfigPanel.svelte:766 показывает HolidayControlsPanel рядом с запасом данных. 40 наблюдений, 10 столбцов: пользователь снимает 10 из 12 праздников поштучно – модель заведёт 10+2+1=13 параметров (запас 3,1), экран продолжит показывать 40/23 = 1,7 и прежний вердикт. Мастер-выключатель теперь работает, поштучный – по-прежнему инертен

Medium | sidecar/econometrica/tests/test_validator_gate_and_non_numeric.py:74-85 | Сторож «в тексте порогов нет числа» покрывает ОДНУ ветку из трёх. Набор строится один – 12 наблюдений на 8 медиа, гейт 1,3 – и попадает только в `insufficient_data`; ветки `low_data` (гейт 2–3) и `borderline_data` (3–4) не проверяются ни разу | Проверено мутацией. Вернула число в `low_data` (`f'Ratio {ratio_gate:.1f}:1 - ниже минимума…'`) – пять из шести тестов зелёные, сторож молчит. Вернула его же в `insufficient_data` – тест краснеет. Обе непокрытые ветки живые: зонд 25 набл./8 медиа → гейт 2,8 → `low_data`; 40/10 → гейт 3,6 → `borderline_data`

Medium | docs/USER_GUIDE_v2_1_0.md:235 | Починка сняла мёртвую ссылку на NRMSE, но в том же файле осталась вторая – того же класса и с запрещённым термином: `[вероятность](GLOSSARY_v2_1_0.md#доверительный-интервал-posterior-ci)`. В глоссарии этот раздел называется «Правдоподобный диапазон» (docs/GLOSSARY_v2_1_0.md:175), якоря `#доверительный-интервал-posterior-ci` нет | Скрипт-сверка всех 10 якорей руководства с заголовками глоссария: девять есть, этот один – нет. Плюс CLAUDE.md §18 (INV-50) прямо запрещает «доверительный интервал» в клиентском тексте. Клиент в таблице «Как читать отчёт» кликает и попадает в начало глоссария

Low | src/lib/components/pipeline/TrafficLight.svelte:173-196 ↔ src/lib/ratio-classifier.js:classifyRatio | Число из текстов движка убрано, но качественное расхождение осталось: движок судит по гейтовому знаменателю, карточка и шапка – по эффективному, а формулировки взяты из одного словаря | Зонд, 25 наблюдений / 8 медиа, байес: карточка и липкая шапка – «1,2:1 – Критически мало», предупреждение движка ниже – «Наблюдений на параметр ниже минимума. Модель сойдётся, но правдоподобные диапазоны будут очень широкими» (это дословно описание классификатора для 2,8). «Модель сойдётся» против «критически мало» на одном экране. Low, а не High, потому что единственный экран, где сообщения движка без ключа `column` вообще рендерятся, – legacy `ValidateStep.svelte` за флагом `useDerivedModeUX`, а флаг нигде не сбрасывается в false (project-state.js:1008) – ветка спящая. В живом V13-пути эти сообщения не показываются никому (ColumnMapperConfirm.svelte:147 фильтрует по `column`), то есть починенная High №5 в проде и не проявлялась

High | src/lib/insights-rules.js:380-391 (validateConfirmInsights) | Починка раздала режим и флаг праздников трём инсайт-функциям из четырёх. `validateConfirmInsights` – под-шаг «Подтверждение», последний экран перед обучением – осталась со своей формулой `rows / detected.n_params_effective_pretrain` (всегда байесовская, всегда 12 праздников, число предикторов из ответа движка) и ПЕЧАТАЕТ это число. До починки расхождения не было: под-шаг «Роли колонок» тоже считал по байесовскому знаменателю. Рассинхрон СОЗДАН этой правкой | Зонд на живом модуле insights-rules.js, 40 наблюдений, 8 медиа + 2 контроля, 12 авто-праздников. Режим OLS: под-шаг «Роли колонок» – «Мало данных: ratio 3.6:1 (рекомендуется ≥4:1)»; следующий клик, под-шаг «Подтверждение» – «Ratio данных 1.7:1 … ниже рекомендованного 4:1». Байес с выключённым мастер-переключателем праздников – ровно то же: 3,6 против 1,7. Два числа одной величины через один клик, оба напечатаны

Medium | src/lib/components/pipeline/InsightsPanel.svelte:196-215 (syncStepLockAfterValidate) | Третья формула гейта, починкой не тронутая: блокировка ШАГА (бейдж «Ошибка» и запрет «Далее») считается по СЫРОМУ `rows / (media + control)` – без свободного члена, без праздников, без режима – и печатает своё число в бейдж | 9 наблюдений, 10 назначенных столбцов: липкая шапка показывает «Запас данных 0,4:1» (эффективный), бейдж шага рядом – «Ошибка: Ratio 0.9:1 - модель не определяется» (сырой), плашка под таблицей – «Наблюдений на параметр 0,8:1» (гейтовый). Три числа одной величины, два из них в одной строке заголовка. Дефект существовал и до починки, но именно этот аудит требовал проверить, что гейт остался один

Medium | src/lib/ratio-classifier.js:196-205 ↔ sidecar/econometrica/engines/modeler.py:482-500 | Заявленный принцип правки – «показ остаётся честным числом параметров фактического режима». Знаменатель научили праздникам, но Фурье-гармоники сезонности (второй мастер-флаг `use_seasonality`, ВКЛ по умолчанию, та же панель) в него по-прежнему не входят: `n_params_effective_bayesian = предикторы + праздники + свободный член` | `decide_n_harmonics` даёт K до 4 пар → до 8 дополнительных параметров в байесовской модели (`utils/fourier_seasonality.py:55-73`). 40 наблюдений, 10 столбцов, 12 праздников, K=4: модель заведёт 31 параметр (запас 1,3), экран покажет 40/23 = 1,7 – завышение на 30% там, где INV-50 требует обратного. Гейта это не касается (OLS сбрасывает Фурье, `ols_modeler.py:104-110` – там знаменатель верен), касается ровно показа. Правкой не внесено, но её же принципу противоречит

Low | src/lib/components/pipeline/OptimizeGoalSeek.svelte:92-101 ↔ :121-125 | Признак «коридор настоящий» научили отсеивать вырожденные границы, а сами границы, которыми красится зелёная зона и задаётся слайдер, остались на `??`: он подменяет только null/undefined, а `{lo: 0, hi: 0}`, бесконечность и перевёрнутые границы пропускает | При `{lo: 0, hi: 0}` текст честно скажет «зелёная зона – ориентир от текущего уровня продаж» (то есть ×0,7…×1,5), а нарисована будет зона 0…0: `corridorLo = salesCorridor?.lo ?? …` вернёт 0, `sliderMax` схлопнется, `step` станет 0. Low, потому что родитель сейчас передаёт `salesCorridor={null}` жёстко (OptimizeStep.svelte:1782), а движок `aggregate_sales` не считает вовсе (bounds.py:103-106) – ветка спящая до P1. Но правка готовилась именно к P1

Low | src-tauri/help-econometrica/pipeline.html:64 | Совет справки, ради которого чинили знаменатель, обращается к пользователю на «ты»: «Отключай, если данных мало (Ratio < 4) или категория не зависит от праздников» | Клиентское правило – обращение на «вы». Правкой строка не тронута; называю, потому что теперь этот совет наконец работает и его будут читать

Medium | src/lib/components/pipeline/ColumnMapperConfirm.svelte:818-822 | Доля непригодных значений положена в поле `missing_pct`, которое интерфейс подписывает как пропуски и оценивает по СВОЕЙ шкале: до 20% – нейтральный значок с подсказкой «не критично». На той же строке движок объявляет колонку critical | Файл из зонда, колонка digital_spend (10,0%): бейдж «10% пусто», тон neutral, подсказка «Доля пропусков (NaN): 10.0%. Небольшое число пропусков, не критично» – и рядом рекомендация «Исключить» с текстом «Обучение на такой колонке не запустится». Один и тот же факт в одной строке таблицы назван и некритичным, и блокирующим

— Проверено и НЕ находка:

* **Гейт зеркалит движок точно.** `validator.py:794` `ratio_gate = n_rows / max(n_params_effective_ols, 1)`, `:780` `n_params_effective_ols = n_predictors + N_INTERCEPT`; фронтовый `gateRatio` жёстко передаёт режим `'ols'`, и `effectiveParamCount` в этой ветке даёт `живые предикторы + 0 праздников + свободный член`. Совпадение подтверждено и тестом, и чтением обеих сторон. Флаг праздников гейта справедливо не касается – в OLS их не было и так.
* **Все потребители знаменателя обновлены.** Шесть вызовов `effectiveParamCount`/`effectiveRatio` в рабочем коде (ValidateStepV13 ×4, insights-rules ×4, project-state ×2) передают и режим, и флаг; вызовов с молчаливым умолчанием в рабочем коде не осталось. Единственное исключение – собственные формулы `validateConfirmInsights` и `syncStepLockAfterValidate`, они вынесены в находки выше.
* **Липкая шапка и карточка сошлись.** `validationHeaderMetrics` теперь считает по живому числу ролей той же формулой, что `ratioCardData`; `ratioRaw` рядом остался справочным. Третьего числа в шапке нет.
* **Ветка нечисловых колонок висит на `if pd.api.types.is_numeric_dtype(...)` (validator.py:570 / :640)** – роли `date` и `unused` в неё не попадают. Зонд: колонка «Дата» получила `date_stats` и не получила `stats`; полностью пустая колонка становится float64 и уходит в числовую ветку, деление на ноль в новой ветке закрыто (`if _n_rows_total else 0.0`).
* **Перенос стора `useHolidays` безопасен.** `activeProject` объявлен на project-state.js:29, задолго до новой позиции (:479) – мёртвой зоны нет. Подписка ровно одна (старая удалена), двойного присвоения нет. Порядок подписчиков `activeProject` изменился, но ни один из них не читает `useHolidays` при инициализации (`get(useHolidays)` встречается только в обработчиках ConfigPanel и HolidayControlsPanel) – смена порядка инертна. `svelte-check`: 0 ошибок.
* **Сторож фронта способен падать.** Мутации: `data.gate >= 2` → `data.ratio >= 2`; `noHolidays = engine === 'ols' || useHolidays === false` → `engine === 'ols'`; `validateRolesInsights` перестаёт пробрасывать аргументы. Итог – 7 из 16 тестов красные, каждый по адресу своей мутации. Мутации откачены из копий, `git diff` по `src/lib` пуст.
* **Сторож движка падает на своей ветке.** Убрала `issues.append` для нечисловой колонки – тест краснеет; вернула число в `insufficient_data` – краснеет. (Непокрытые ветки `low_data`/`borderline_data` вынесены в находку выше.)
* **Сигнал о нехватке данных не потерян.** Зонд: 12/8 → `insufficient_data`, 25/8 → `low_data`, 40/10 → `borderline_data` – типы и severity на месте, ушло только число.
* **Других сообщений движка с числом запаса нет.** Прогон по `engines/`, `utils/`, `server.py`: ни `verdict`, ни прочие сообщения валидатора числа запаса не печатают.
* **Фронтовые тесты старых формулировок движка не читают.** `Ratio N:1` в тестах относится к собственным текстам `insights-rules` (`insights-rules-ratio-honesty`, `tier2-context`), не к снятым строкам валидатора.
* **Сообщение «обучение не запустится» – правда, а не перестраховка.** Ни в `modeler.py`, ни в `ols_modeler.py`, ни в загрузке файла нет очистки денежных строк; `df[col].fillna(0).values.astype(float)` действительно бросит ValueError. (Отдельная находка выше – про то, что гейт роли считает такие колонки пригодными.)
* **NRMSE вычищен полностью.** По дереву остались только упоминание в комментарии `src/lib/program-help.js:139` и словарь терминов `tools/cabinet_eval/graders.mjs:120`; `check_glossary_sync.py` – OK, три выхода синхронны, U+2014 нет.
* **Развилка `corridorIsObserved` решена верно по существу**: конечность, положительность и порядок границ – ровно те три условия, которые отсекают вырожденный коридор (остаток – в Low выше).
* **Клиентский текст новых строк:** U+2014 нет ни в одной, обращения на «ты» нет, псевдоточности нет; дефис вместо короткого тире и десятичная точка («10.0%», «1.8:1») – ровно та же запись, что во всех соседних строках продукта, отдельной регрессии тут не появилось.
* **Прогоны:** фронт `npx vitest run` – 89 файлов, 1403 теста, все зелёные; движок `pytest -k "valid or column or missing or stats"` – 96 зелёных; `npm run check` – 0 ошибок, 177 предупреждений (все про неиспользуемый CSS и a11y, к правке отношения не имеют).
