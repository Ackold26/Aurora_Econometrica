# Живой журнал находок — аудит A (движок + Rust)

База `9b0fc382` → вершина `dd9c9dde`. Формат строки: `severity | файл:строка | суть | сценарий отказа`.

High | sidecar/econometrica/engines/validator.py:962 | запасная ветка `except` ставит `_total_holidays = 12` молча и БЕЗ записи в журнал, причём тот же `except` накрывает и вызов `user_covered_auto_holidays` | календарь несёт 13 событий; при сбое импорта (или любой ошибке внутри `user_covered_auto_holidays`) знаменатель = 12 вместо 13 → «запас данных» завышен, ровно нарушение INV-50, от которого правка и уходила
High | sidecar/econometrica/_internal/engines/validator.py:935 и sidecar/econometrica/dist/econometrica-sidecar/_internal/engines/validator.py:935 | второе (и третье) место с `N_HOLIDAYS_DEFAULT = 12` — собранный sidecar не правлен | программа запускает собранный sidecar, а не исходник; тесты зелёные на исходнике, у клиента знаменатель по-прежнему 12
High | sidecar/econometrica/engines/ols_modeler.py:504-511 | `latest-params.json` в OLS-тренере пишется БЕЗ `sanitize_nonfinite`, тогда как соседний `model-diagnostics.json` через него проходит, и в снимок кладётся тот же dict `diagnostics` | любой NaN/Infinity в diagnostics или channel_params → в файле голое `NaN` → новый читатель `project.rs` получает ошибку разбора → `modelParams: null` → «Кривые отдачи» у OLS-проекта так и остаются спрятанными, то есть правка не работает ровно там, где NaN и водится
Medium | src-tauri/src/commands/feedback.rs:398 vs :91 | сторож состава «и не называет ничего лишнего» (INV-38) стоит на `attribution_tail` – МЁРТВОМ пути (`submit_feedback` из интерфейса больше не зовётся), а живой `feedback_form_link` проверяется только на наличие четырёх полей | дописать в `feedback_form_link` `&host=` + `hostname()` – все проверки остаются зелёными, имя машины уезжает во внешнюю форму
Medium | src-tauri/src/commands/feedback.rs:39-43 | имена полей `produkt/versiya/licenziya/vremya` противоречат документации в этом же файле («имена выглядят как answer_short_text_12345, угадать нельзя»), и ни один прогон не доказывает, что предзаполнение доезжает | если имена не те, форма откроется и человек отправит обращение, но скрытые поля придут пустыми – возвращается ровно тот дефект (обращение без привязки к программе и машине), ради которого правка делалась, и заметить его нечем
Low | sidecar/econometrica/_internal/... и dist/econometrica-sidecar/_internal/... (git не отслеживает) | собранный sidecar в дереве всё ещё несёт `N_HOLIDAYS_DEFAULT = 12` | без пересборки sidecar правка INV-50 до клиента не доезжает
