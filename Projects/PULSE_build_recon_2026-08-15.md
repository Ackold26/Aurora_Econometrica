# Пульс: разведка сборочной инфраструктуры (2026-08-15)

## Задача (своими словами)
Владелец решил вынести дублируемый Rust-код (18 файлов, 9429 строк, 3+ продукта каждый) в общий крейт.
Продукты живут в отдельных git-репозиториях, не в монорепо. Нужно выяснить ФАКТЫ о текущей сборочной
инфраструктуре (Cargo-конфиги, vendor, offline-сборка, CI, нестандартные зависимости, подмодули, версии Rust,
устройство aurora-llm), чтобы решение о способе доставки крейта (git-тег / подмодуль / vendored / монорепо)
опиралось на реальность, а не на догадки. Только чтение, ничего не менять.

## План
1. Обзор деревьев в D:\Docs\Aurora_Ai\Dev\
2. По каждому продукту: .cargo/config.toml (проект, src-tauri, глобальный), vendor/, offline-признаки,
   .github/workflows/, Cargo.toml зависимости не с crates.io, .gitmodules, rust-toolchain/rust-version
3. Разбор aurora-llm как крейта-продукта
4. Свести таблицу + отчёт BUILD_RECON_2026-08-15.md

## Ход работы
- 12:25 — старт, маячок создан
- 12:26 — обзор деревьев в Dev/: подтверждены все 9 продуктов + aurora-llm, найдены git remotes
- 12:30 — обзорная проверка .cargo/config.toml, vendor/, .gitmodules, workflows, rust-toolchain по всем 9 — ничего из этого нет нигде
- 12:33 — rust-version в src-tauri/Cargo.toml — не задан ни в одном из 9 продуктов
- 12:35 — поиск git=/path=/registry= зависимостей в src-tauri/Cargo.toml всех 9 — ноль совпадений
- 12:37 — разбор структуры aurora-llm: workspace 3 крейта, rust-version=1.94, git-теги v0.9.0-rc.1..7
- 12:40 — обнаружен ключевой факт: CLAUDE.md 5 продуктов почти идентичны — единая кодовая база копируется между репо («Синхронизация N репо»), а не общий крейт
- 12:45 — прочитаны ci.yml (продукт) и build.yml/release.yml/audit.yml/network-policy.yml (aurora-llm) — релиз продуктов отключён в CI (if: false), aurora-llm доставляется как отдельный бинарь-сайдкар через draft GitHub Release, упаковка в продукты помечена TODO
- 12:48 — найдена единственная точка интеграции aurora-llm↔продукт: ROSST_AI_DocMaster/src-tauri/src/commands/bridge.rs — спавн процесса по пути из env AURORA_LLM_BIN, НЕ Cargo-зависимость
- 12:50 — сверка Cargo.lock/git remote по всем 9 — все независимые репо, все с закоммиченным Cargo.lock
- 12:52 — пишу отчёт BUILD_RECON_2026-08-15.md
- 12:55 — отчёт готов, разведка завершена
