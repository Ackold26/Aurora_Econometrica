# Econometrica — роутер следующей сессии (релиз v2.3.0)

> Скопируй в начало новой сессии. cwd = `D:\Docs\Aurora_Ai\Dev\Aurora_Econometrica_v230`
> (worktree релизной ветки `feat/econ-v2.3.0`). Создан 2026-07-13.

## Контекст — что сделано (НЕ переделывать)

**Консолидация Фаза A ЗАВЕРШЕНА, тег `v2.3.0`, запушено на origin.**
Релизная ветка `feat/econ-v2.3.0` = kpi-units (+14: KPI-паспорт) + avrora (+27:
Аврора/RAG/кабинет/петля) + твой security-фикс `57d5e21` (path-traversal updater.rs).
- `8ecfae5` — версия 2.3.0 (package+tauri+Cargo, был рассинхрон) + чистка 11 ошибок
  типов kpi-units (0 новых от мержа; поправлен реальный баг дубля `cpuPerLabel`).
- `e67f42e` — merge avrora → v2.3.0; конфликты lefthook (оба хука) и InsightsPanel
  (обе логики: kpi count-aware + панель «Спросить Аврору») разрешены.
- Гейты объединённой линии: **svelte-check 0 · vitest 1263 · cargo 188 · kpi-display drift OK**.
- Ветка+тег на origin (`origin/feat/econ-v2.3.0`, `v2.3.0`).

**До консолидации (в ветке avrora, тоже в v2.3.0):** внешний аудит блока Авроры —
5 находок починены (Critical доставка данных `$ARGUMENTS` + High телеметрия + Medium×2
шкала/атрибуция + Low focusChannelType), детали `Projects/audit_verification_2026-07-12.md`.
Петля харнеса — средний путь INV-50 (грейдеры калиброваны, промпты помечают производные).

## Первым пунктом: проверить результат отложенного внешнего аудита
Wrap-up 2026-07-13 запустил opus-аудитор (фон) по diff блока «аудит-фиксы + петля»
(`Projects/audit.diff`, 711 строк) + `handoff.md`. Durable находок —
`Projects/audit_findings_live.md`. **Действие:** прочитать находки, триаж (подтвердить
исполняемым доказательством для Critical), Critical/High — чинить, fix-коммит.
Если аудитор не завершился — перезапустить свежим сабагентом (вход + накопленный
audit_findings_live как «продолжи»).

## Задачи продолжения (приоритет)

### 1. 🔑 Фаза B — релиз-публикация v2.3.0 (главное, наружу — гейт Антона)
Регламент `aurora-release-update` + `aurora-fix` pre-build чеклист:
- Пересборка sidecar → installer (`CARGO_TARGET_DIR="D:/cargo-targets/ai-agency" npm run tauri build`).
- Supabase manifest + latest.json (облачная редакция; локальная — ОТДЕЛЬНЫЙ манифест
  `aurora-econometrica-gui-local`, см. CLAUDE.md D2).
- Живой смоук установленного .exe.
- ⚠️ В **основном дереве kpi-units** висят незакоммиченные `installer_hooks.nsh` и
  `tokens.generated.css` (не мои) — если нужны релизу, закоммитить/влить в v2.3.0 ДО сборки.

### 2. Мерж v2.3.0 → master (по решению Антона)
master (`7fbfd96`) древний (15 июня, +247 позади). Релизная линия v2.3.0 — кандидат
в новый master. Origin/planning-mode отстал на 5 (локальная работа планирования не
запушена) — синхронизировать, скоординировав с Машей небесной (shared-репо).

### 3. Стратегия (roadmap `../Aurora_Econometrica/COMMERCIAL_READINESS_ROADMAP.md`)
🔴 Главный разрыв к продаже — НЕ код (~90% готов), а **доказанная ценность на клиенте
(~30-40%)**. Горизонт 3: пилот «прогноз→факт» на своих данных Aurora — запускать
параллельно с релизом. Горизонт 2: нарратив честности (широкий коридор = честны, не сыро).

### 4. Хвосты кабинета/Авроры (не блокеры)
- Живой RAG в окне — заблокирован инфра (нет `engine.env`/`AURORA_CLIENT_SECRET` на машине,
  ssh-agent стоит). Нужен туннель до узла Б системным ssh.exe + прогон в `npm run tauri:dev`.
- Юнит-тесты грейдеров харнеса + dry-эвал в CI (детерминированный гейт).
- next-quarter-plan кейс харнеса стабильно упирается в timeout (даже 300с) — разобрать.

## Инварианты/правила
- INV-50 средний путь (см. handoff §2). Прод-страж `insights-grounding.js` НЕ трогать.
- Кабинет-эконометрист развивается ТОЛЬКО в MMM-оптимайзере (Econometrica); Agency архивный.
- Онлайн-редакция первична (roadmap); офлайн-инфру (`cloud_advisors`, `tauri.local.conf.json`) НЕ удалять.
- JS+JSDoc не TS. Клиентский текст: короткое тире «–», без англицизмов, без slash-команд.
- Релиз доезжает по СВОЕМУ каналу; content-pack из УСТАНОВЛЕННОГО (`%LOCALAPPDATA%`).

## С чего начать
1. Прочитать `Projects/audit_findings_live.md` (результат отложенного аудита) → триаж.
2. Прочитать `INDEX_econometrica` (память, шапка) + этот роутер.
3. Уточнить у Антона: Фаза B (релиз v2.3.0) сейчас, или мерж в master, или пилот.

## 🔴 Руководство по стилю действий (прочитать ПЕРВЫМ)
1. **Shared-репо — зонд ветки/коммитов ПЕРВЫМ ходом.** В этой сессии Антон закоммитил
   `57d5e21` в мою релизную ветку параллельно — я обнаружила это только при `git log` перед
   тегом. Всегда сверяй HEAD/автора/дату верхних коммитов ДО тега/мержа (`git show -s --format`).
2. **Мерж 3 веток — merge-tree ДО реального мержа** показал 2 файла пересечения → мержить
   в изолированный worktree от КОММИТА (не рабочего дерева, чтобы чужой WIP не попал).
   Авто-слитый файл (InsightsPanel) верифицировать лично (grep обеих логик + нет маркеров).
3. **svelte-check регресс — сверить с baseline РОДИТЕЛЬСКОЙ ветки** прежде чем чинить.
   11 ошибок оказались предсуществующим долгом kpi-units, не регрессом мержа — доказано
   прогоном svelte-check на чистом kpi-units. Не чини чужой долг под видом мержа вслепую.
4. **Полный эвал-харнес недетерминирован** — не гоняй его в цикле за «зелёным» (каждый
   прогон LLM отвечает иначе). Калибруй ГРЕЙДЕРЫ на фиксированном наборе сохранённых
   ответов (`results/*.json`, egress не нужен) + негативная проба, что грейдер ловит выдумку.
5. **Аудитор фоновым сабагентом БЕЗ имени** (Agent-name дважды зависал в idle в начале
   сессии). Вывод claude/node буферизуется через `| tail` — редиректь в файл, следи по
   `results/` появлению, не по буферу.
6. **egress для эвала/CLI** — `env -u ANTHROPIC_API_KEY node run_eval.mjs` (баланс ключа
   исчерпан → claude.ai-подписка). Проверять дешёвой пробой ДО тяжёлого прогона.
7. **🔴 Перед релизом v2.3.0 — ОБЯЗАТЕЛЬНЫЙ живой прогон в окне; зелёные гейты его НЕ
   заменяют.** Вся консолидация зелёная (svelte-check/vitest/cargo), но HIGH-баг
   `ReportStep.completeStep(5)` (Отчёт не помечался завершённым) не поймал НИ ОДИН
   юнит-тест — только чтение аудитора. v2.3.0 объединил 3 ветки и не прогонялся вживую
   ни разу. Перед сборкой installer: `npm run tauri:dev` (с мостом), окружение БЕЗ
   `ANTHROPIC_API_KEY`, пройти полный пайплайн ДО шага Отчёт → «Завершить анализ»
   (ровно где был баг) и проверить, что шаг реально помечается complete. Юнит доказывает
   функцию, живой прогон ловит проводку/триггер [[feedback_unit_test_proves_function_not_wiring_live_catches_trigger]].
   AVT: спросить состояние машины (:5173/:9223) ДО поднятия окна — мост tauri полумёртв
   после rebuild [[feedback_tauri_mcp_bridge_half_dead_after_rebuild]].
8. **Рассинхрон шкалы/enum — grep НЕ только символы, но и голые числа-индексы.** Медиум-1
   фикс согласовал 3 таблицы через поиск `STEP.`/`STEP_TERMS`, но пропустил
   `completeStep(5)` в ReportStep — голое число прячется от символьного grep. При любой
   правке шкалы шагов дополнительно грепать `completeStep([0-9])`, `\.set([0-9])`,
   `=== [0-9]`, `\[[0-9]\]` по `src/lib/components/pipeline/*` и `project-state.js`.
9. **Проверяющий (аудитор/гейт/сторож) дороже верить, чем поспешно отвергать.** Дважды в
   прошлой сессии ошиблась в оценке проверяющего: сторож зависания дал ложную тревогу по
   неверному сигналу (транскрипт size=0 вместо durable findings), и я поспешно назвала
   валидный аудит «галлюцинацией» по промежуточным durable-записям — а финал нашёл HIGH.
   Отвержение результата проверки требует того же личного доказательства, что и принятие;
   дождись ПОЛНОГО вывода, не суди по промежуточному.
