# Econometrica — роутер следующей сессии (после сессии 2026-07-18/19)

> Скопируй в начало новой сессии. cwd = `D:\Docs\Aurora_Ai\Dev\Aurora_Econometrica_v230`
> (worktree ветки `feat/econ-v2.3.0` = прод-линия 2.4.0). Полный бэклог → память
> `project_econometrica_backlog` (грузится по слову «Econometrica»).

## ✅ Сделано в сессии 2026-07-18/19 (НЕ переделывать)
- **`ec3dced`** (запушено) — Low-хвосты аудита 2.4.0: мёртвая `else[]` в смоук-гейте
  `build_sidecar.py` упрощена (недостижимость доказана); тире «—»→«–» в клиентском тексте
  `PromisesCard.svelte`.
- **`7eeb0e6`** (запушено) — график Планирования `ContinuationChart.svelte`:
  - **F2** — метка «Дата отсечки» теперь горизонтальная (`rotate:0` у label markLine; ECharts
    дефолтно поворачивал её вдоль вертикальной линии). Подтверждено живым рендером.
  - **Разрыв линии факт↔прогноз устранён** — узел стыковки в `effectiveCutoff` = последнее
    фактическое значение, `connectNulls` дотягивает непрерывно.
- **Скролл** — оказался НЕ багом актуального кода (был в старой установленной версии у Антона /
  dev-артефакт). Две гипотезы-правки (overflow таблицы, display:contents обёртки) ОТКАЧЕНЫ.
  Урок → память `feedback_establish_user_build_before_diagnosing_ui_bug`.
- **Разведка справки+PDF ЗАВЕРШЕНА** → `Projects/HELP_PDF_RECON.md` (durable, костяк верифицирован).
- wrap-up: diff-аудит блока графика (opus), handoff `Projects/handoff.md`.

## 🔜 ГЛАВНАЯ ЗАДАЧА СЛЕДУЮЩЕЙ СЕССИИ — Справка + загружаемый PDF (крупная)
Антон выбрал её как следующую крупную; **все 5 гейт-вопросов уже решены** (см. HELP_PDF_RECON.md §7):
1. Канонический email поддержки = **`support@auroraai.pro`** (привести к нему все слои справки).
2. `uninstall-cleanup.ps1` (чужой ROSST-файл в бандле) — **заменить** корректным для
   `com.aurora.econometrica` (чистит per-app пути econometrica[.local]) либо удалить.
3. HTML-справку **обновить под 2.4.0** (7 шагов, Планирование как отдельный шаг, актуальные фичи).
4. Официальный сайт = **`auroraai.pro`** (вписать вместо плейсхолдера).
5. Глоссарий в PDF — **полный** (все 34 термина).

Объём работы (из разведки):
- **INV-50 системно** — 25 «доверительный интервал»/«CI» в 8 HTML-файлах → «правдоподобный диапазон»
  (эталон — `program-help.js`, он уже корректен). Синхронно править 3 слоя: `glossary.html`,
  `glossary.js`, будущий PDF (иначе разъедутся).
- Актуализация 14 HTML в `src-tauri/help-econometrica/` под 2.4.0 (шаг Планирование, версии).
- НОВЫЙ раздел «Установка/удаление» (материал в реестровом «Инструкция по установке и запуску.pdf»).
- Раздел «Коды ошибок + поддержка»: точный путь логов `%LOCALAPPDATA%\com.aurora.econometrica\logs\`,
  email, чеклист «что приложить к обращению».
- **Загружаемый PDF** — сгенерировать ОДИН раз на сборке (pandoc у разработчика / скилл `pdf`) и
  зашить как bundled resource; кнопка «Скачать справку (PDF)» рядом с `open_help` в
  `CommandPalette.svelte`/шапке пайплайна (Rust-команда по паттерну `open_help`).
- Директива Антона: этот паттерн справка+PDF → СВИП на все 12 продуктов (Econometrica — эталон).

## 📋 Прочие открытые (перенос, приоритет с Антоном)
- **[Medium, из аудита 2026-07-19] Утечка стыковочной точки графика в axis-тултип** (`ContinuationChart.svelte`):
  на месяце отсечки каждый сценарий показывает в подсказке значение = последний факт (инъекция
  `aligned[effectiveCutoff]`), хотя прогноза там не было. Не блокер (число=факт, 1 точка), но
  INV-50-чувствительно — **закрыть до следующего билда**. Фикс: пометить точку `{value, __stitch:true}`
  → скрывать в tooltip formatter для серий-сценариев (образец `isForecast` в `forecast-chart-format.js`).
- **[Low, латентный] Ложная диагональ при mid-history `cutoffIndex`** — не достижим текущим кодом
  (PlanningStep передаёт `cutoffIndex=histLen-1`); defensive: инъектировать только при `effectiveCutoff==histLen-1`.
- Реестровые PDF-материалы: `D:\Документы\ООО Платформа Аврора - документы\Реестр ПО — техдокументация\01 Econometrica MMM Optimizer\Материалы для подачи заявления\PDF для сайта` (4 ключевых для справки: Руководство пользователя · Общее описание · Инструкция по установке · Жизненный цикл/поддержка).
- Тестовые компы PC443/PC583 — баннер 2.4.0, реакция (мягко/форс min_version) — ждёт Антона.
- Крупное отдельными сессиями: пилот прогноз→факт (Горизонт 3) · порт Авроры Tier2+RAG
  (`feat/econ-avrora-assistant`, 212 коммитов, ПОРТ не merge) · петля E1→E4 · Фаза 5 cleanup OptimizeStep.

## Файлы для контекста (порядок чтения)
1. `Projects/HELP_PDF_RECON.md` — ПОЛНАЯ разведка справки+PDF (структура, gap, план, решённые вопросы).
2. Память `INDEX_econometrica` (📍 Состояние сверху) + `feedback_establish_user_build_before_diagnosing_ui_bug`.
3. Код справки: `src-tauri/help-econometrica/*.html` (14) · `src/lib/program-help.js` · `src/lib/glossary.js` ·
   `src-tauri/src/lib.rs` (`open_help` ~стр.1670-1725) · `tools/sync_help_lists.py` (анти-дрейф).
4. Реестровые PDF (см. путь выше).
5. `CLAUDE.md` проекта §18 (регламент правки промптов/справки, линтеры).

## Инварианты/правила
- Справка HTML едет БАНДЛОМ (пересборка .exe), НЕ content-pack (частые правки текста без релиза —
  требуют ADR о переносе в content-pack). При правке `content-packs/*.json` — re-sign обязателен.
- INV-50: клиентский текст — «правдоподобный диапазон», не «доверительный интервал»/«CI»;
  короткое тире «–», не «—»; без вендор/внутр-имён.
- Shared-репо: зонд `git rev-parse --abbrev-ref HEAD` + fetch/status ahead-behind ДО работы;
  коммит своим pathspec; в дереве живут чужие незакоммиченные (`audit.diff`, `Cargo.toml` и др.) — не трогать.
- Линтеры справки/промптов (CI + lefthook): новый линтер проверять «внести-поймать-откатить».

## С чего начать
1. Прочитать `HELP_PDF_RECON.md` целиком (там весь план + решённые вопросы).
2. Согласовать с Антоном приоритет внутри задачи: сначала INV-50 sweep (быстрая победа, 3 слоя) или
   сначала актуализация структуры под 2.4.0. Рекомендация: начать с INV-50 (изолированный, доказуемый
   линтером), затем структура, затем PDF-генерация.
3. Прогнать реализацию через субагентов (механика: правка HTML по gap-списку, INV-50 замены,
   сборка PDF) с личной верификацией; финальные коммиты/публикация — самой.

## 🔴 Руководство по стилю действий (прочитать ПЕРВЫМ — уроки сессии 2026-07-18/19)
1. **UI-жалоба пользователя → ПЕРВЫМ делом выясни, какую СБОРКУ он видит** (установленная? какая
   версия? dev?), сверь с `activations.app_version`. НЕ бросайся чинить свой `npm run tauri:dev` —
   в этой сессии полдня ушло на диагностику dev-инстанса, а баг был в старой установленной версии
   пользователя («обновил программу — заработало»). Companion INV-96.
2. **Два слепых захода = неверный диагноз, а НЕ повод для третьего.** Если правка-гипотеза не
   сработала (пользователь подтвердил «нет»), не применяй следующую гипотезу того же класса и не
   размножай на N мест — остановись, измерь причину на реальной сборке. Скролл-фикс чуть не поехал
   на 11 мест по неверной гипотезе.
3. **Живой рендер для визуальных правок работает** (метод №12): `npm run tauri:dev` → мост :9223 →
   `driver_session` → `webview_execute_js`/`webview_screenshot`. НО: native скриншот НЕ отражает
   прокрутку вложенного `overflow`-контейнера (скроллить через `element.scrollTop` + мерить позицию
   `getBoundingClientRect`, для общего вида — временный `document.documentElement.style.zoom='0.6'`);
   мост-жест `webview_interact scroll` бывает no-op (не диспатчит trusted wheel); реальное колесо —
   только пользователь или desktop-control над активным окном.
4. **Справка = 3 слоя, не путать:** `program-help.js`/`glossary.js` (Tier2-контекст ассистента,
   УЖЕ на INV-50 — трогать осторожно, тест стережёт) vs 14 HTML в `src-tauri/help-econometrica/`
   (основная справка в браузере). INV-50-правку делать по ВСЕМ слоям одним проходом + PDF.
5. **Делегирование субагентам оправдалось** (Low-хвосты, разведка справки) — но находки/diff
   верифицировать ЛИЧНО (в сессии субагент дал чистый результат, git diff совпал; разведка —
   костяк перепроверила грепом). Механику (правка HTML, INV-50 замены, PDF-сборка) — субагентам
   Sonnet; INV-50-корректность и финальные коммиты — самой.
6. **PDF-генерацию** делать на этапе сборки (готовый файл в resources), НЕ рантайм-конвертацией:
   pandoc/wkhtmltopdf не бандлятся у клиента → рантайм-путь тихо скипнет.
