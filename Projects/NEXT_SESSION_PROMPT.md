# Промт следующей сессии — Aurora AI Econometrica (составлен 2026-08-10)

*Рабочий каталог: `D:\Docs\Aurora_Ai\Dev\Aurora_Econometrica_thinwt` (ветка `master`).*

---

## 0. 🔴 Первое действие — проверить, что этот промт не устарел

Прошлый блок начался с того, что владелец дал промт из дерева `_canon`, а тот стоял на ветке,
отставшей от `master` на 25 записей: две его задачи были давно закрыты, и полчаса ушло на
разбирательство вместо работы. Поэтому **первым же ходом**, до любой задачи:

```
git -C "D:/Docs/Aurora_Ai/Dev/Aurora_Econometrica_thinwt" log -1 --format='%h %ci %s'
git worktree list          # где какая ветка живёт
git log --oneline origin/master..master | wc -l   # сколько не отправлено
git status --short
```
Если вершина новее этого промта — верить дереву, а не тексту. Читать
`Projects/AUTONOMOUS_STATE_2026-08-10.md` — там доказанное состояние.

⚠️ Работа идёт в `_thinwt`. Дерево `_canon` (ветка `feat/econ-canon-p0`) сведено в `master`
записью `4d0c918` и оставлено на месте намеренно; промт той линии сохранён рядом как
`Projects/NEXT_SESSION_PROMPT_canon_2026-08-10.md`.

## 1. Состояние на конец блока 10.08

**Версии.** У клиентов 2.4.4. В `master` — 2.4.5, выпуска не было.

**Гейты (личная приёмка, не со слов агентов):** движок **1091/0** · фронт **1465/0** ·
Rust **406/0** (3 пропущено). Тонкая облачная поставка — снять невозможно, см. блокеры.

**Сделано в блоке:**
- сведение `canon` в `master` без потерь (`4d0c918`), метка `archive/feat-econ-canon-p0-2026-08-10`;
- спасена и записана чужая незаписанная правка: каталог файла состояния движка берётся из
  идентификатора сборки (`4d763bb`) — редакции облачная и локальная делили один `sidecar.json`,
  одна могла снять расчёт другой. Занесено как **CPD-63** (`174933e` в `aurora-meta`), попутно
  класс CPD-51 приведён к списку ADR-047;
- **честность вердикта закрыта с обеих сторон шва** — отсутствие вердикта больше не выглядит как
  надёжная модель ни в одном из четырёх выходов;
- обрезка термина глоссария по пределу ячейки (термин длиннее предела ронял всю книгу);
- **движок пересобран** (`build_sidecar.py`, 10.08 06:04) — до этого в поставку ехал июльский.

## 2. 🔴 Блокеры, которые держат выпуск

1. **`github.com` не разрешается в этой среде** (`api.anthropic.com` при этом отвечает). Из-за
   этого: нельзя отправить записи (Econometrica ~13, `aurora-meta` 1 — всё локально сохранно),
   нельзя снять гейт тонкой облачной поставки и **нельзя собрать тонкую редакцию**:
   `tools/build-cloud.mjs` обязательно проверяет метку `aurora_gateway-v0.6.5` через
   `git ls-remote`. Метка в кеше cargo есть (`bcd41e7`), но сторож намеренный — поставлен после
   боевого случая на Oracle, обходить правкой скрипта нельзя. **Нужен мост.**
2. **Установщик 2.4.5 на рабочем столе негоден — не выдавать.** Оболочка из `master` (доказано
   совпадением sha256 с результатом сборки), но движок внутри был июльский: тот же файл, что лежал
   в рабочей папке с 18.07. Обычная сборка движок не пересобирает, он едет ресурсом. Движок теперь
   пересобран, но **установщик с ним ещё не собран** — это первая задача, как появится сеть.

## 3. Порядок работ следующей сессии

1. Поднять мост → отправить записи обоих репозиториев → снять гейт тонкой облачной поставки.
2. Собрать установщик тонкой облачной редакции из текущего `master` со свежим движком.
   🔴 После сборки — доказать, что движок внутри новый: сверить sha256 файла
   `sidecar/econometrica/econometrica-sidecar.exe` с тем, что лежит внутри установщика (7-Zip
   распаковывает NSIS). Поиск строк внутри движка как метод НЕ работает: формат PyInstaller нечитаем,
   контрольная старая строка тоже не находится — доказывать только хешем и датой.
3. Публикация клиентам — по регламенту `aurora-release-update`, отдельным манифестом для облачной
   редакции. Без слова владельца наружу не выходить.
4. Незакрытое: живой прогон облака на PC204 · High-1: вердикт по-прежнему из двух источников —
   решено предупреждать о расхождении, а не сводить · 🔴 **интерфейс и канал доставки не сверяются
   ничем** (см. ниже: перечень кнопок задан таблицей в коде, состав на узле — отдельно; сегодня это
   дало шесть нерабочих кнопок у облачных клиентов, разово вылечено, но сверка так и не заведена).

### 🟢 Выкачено на узел 10.08 — методология кабинета выровнена

Было: интерфейс облачной редакции показывал 8 кнопок кабинета `econometrist`, а на узле лежали 9
файлов старой методологии — **шесть кнопок из восьми отдавали клиенту отказ** (`/interpret-model`,
`/why-channel`, `/explain-ratio`, `/pilot-design`, `/next-quarter-plan`, `/data-gaps`). Это был не
«отставший текст», а функциональный разрыв.

Сделано по решению владельца: полная резервная копия каталога (`/opt/aurora-cloud-cabinets.bak-2026-08-10`)
и настроек службы → сборка штатным `build_cabinets_bundle.py` в промежуточный каталог → перенос
ТОЛЬКО подкаталога `econometrist` (пробный прогон показал: 9 файлов добавляется, 11 обновляется,
удалений ноль) → пересчёт описи `--describe` → версия службе в `/etc/aurora-cloud/service.env` →
перезапуск. Итог: версия набора `cab-887763a03a41`, 19 кабинетов, 271 файл, журнал службы чист,
все шесть команд на месте.

🔴 **Чужие кабинеты трогать нельзя:** на узле лежат кабинеты минимум трёх продуктов, и у части из
них узел ВПЕРЕДИ своего дерева (их источник — репозитории Legal Center и Smart Analytica). Сборщик
`build()` стирает ВЕСЬ целевой каталог, поэтому сборка прямо в живой каталог снесла бы чужую
свежую работу. Только промежуточный каталог и перенос одного подкаталога.

⚠️ **Что вскрылось попутно:** при редизайне методологии 02.08 из `CLAUDE.md` кабинета пропала строка
принадлежности продукту, и защита сборщика (после случая 1 августа с чужим набором) для этого
кабинета молча перестала работать — пропажу инструмент считает «не указано», а не подменой. Сторож
поднял тревогу на первом же выкате и вернул код разбора; причина починена (`6466cb2`), а не заглушена.
⚠️ Узел исполняет `build_cabinets_bundle.py` из НЕСЛИТОЙ ветки шлюза и на одну запись позади её
вершины — отдельный вопрос владельцу шлюза, к этому выкату отношения не имеет.

⚠️ **Известная нестабильность:** `session::cleanup::tests::orphan_directory_is_zero_wiped_not_just_unlinked`
один раз упал в полном прогоне и прошёл изолированно (10.08). Файл никто не менял. Если увидишь его
красным — сначала повтори изолированно, прежде чем считать регрессом.

### 🟢 Проверено 10.08 и ПЕРЕОТКРЫВАТЬ НЕ НУЖНО

Два пункта, которые прежние промты требовали «чинить до выпуска», оказались уже закрытыми — оба
проверены зондом, а не чтением:
- **Вторая волна CPD-53 (гонка обновления хранилища) — ПОЧИНЕНА.** Общий замок по целевому файлу
  (`vault_lock_for` / `store_vault_guarded`) накрывает весь неделимый участок «скачать → сверить →
  зашифровать → записать → отметить версию», и все четыре точки вызова идут через единую точку
  входа `download_updates`. Тесты на гонку настоящие: детерминированные, через тестовые паузы,
  краснеют под мутацией (снятие общего замка).
- **Маршрутное квотирование `cmd /C` — ЗАКРЫТО записью `d897362`.** Подтверждено эмпирически, а не
  рассуждением: отдельная программа вне репозитория прогнала прежнюю и нынешнюю конструкции на
  настоящем `cmd.exe` по каталогам `A&B`, `A^B`, `A(B)C` — прежняя валится на всех трёх, нынешняя
  проходит; обычные пути и пути с пробелом не сломаны. Обе точки (боевой запуск и зонд) идут через
  общий построитель командной строки с `raw_arg`. Зонд теперь различает «не найден», «вне доверенных
  расположений» и «найден, но не запускается» — молчаливого увода материалов в облако нет.

## 4. Уроки блока — применять с первого хода

1. 🔴 **Промт — гипотеза, а не факт.** Первый ход тратить не на задачу, а на проверку, что
   постановка ещё верна. Здесь это сэкономило бы полчаса и уберегло от починки уже починенного.
2. 🔴 **Доказывать хешем, а не совпадением времени.** Тревога «установщик собран не из того дерева»
   держалась на совпадении минут и была снята прямым сравнением sha256. И наоборот: настоящий
   дефект (июльский движок внутри) вскрылся тем же приёмом.
3. 🔴 **Структурный сторож защищает форму кода, а поведение защищает только поведенческий тест.**
   Сторож «чтение вердикта ровно в одном месте» оказался слеп к эквивалентной цепочке другого
   синтаксиса: 406 тестов зелёные, дефект живой.
4. 🔴 **Задание исполнителю наследует слепоту автора.** Правка вышла неполной ровно там, где я не
   назвала третью функцию, хотя старый промт её называл. Проверять задание против исходной
   постановки, а не только против своего понимания.
5. **Мутационная проверка обязательна, и заказывать её лучше постороннему.** Один исполнитель
   честно доложил, что его собственный тест мутацию НЕ ловил, и усилил фикстуру — без этой привычки
   в наборе остался бы декоративный сторож.
6. **Код возврата фоновой команды с `| tail` — не код возврата команды.** Дважды за сессию
   уведомление говорило «успех» там, где надо было читать сам вывод.
7. **Пульс исполнителя — про живость, не про качество.** Двое пропускали отметки, работая; факты
   (время файлов, живые процессы, растущие журналы) надёжнее статуса.

## 5. Инварианты, которые нельзя нарушать

- **Отсутствие вердикта = «Надёжность модели не подтверждена»** во всех четырёх выходах (решение
  владельца 10.08). Но «вердикта нет» ≠ «рекомендации отключены»: `verdict_refuses` остаётся только
  про `unreliable`, иначе рекомендации выключатся у всех старых проектов.
- **Текст клиенту — единым источником** и побайтово зеркалится Python ↔ Rust, сторож на обоих концах.
- **Клиентский текст:** короткое тире «–», без англицизмов и внутренних имён.
- **Правки Python доезжают до клиента только через пересборку движка** (`build_sidecar.py`).
- **Прогоны строго последовательно** — clippy вместе с `cargo test` дважды давал ложные падения.
- **Удаление — только в корзину**, чужие незаписанные правки не уносить под своей записью.

## 6. 🧠 Распределение моделей и нажима по фазам

| Фаза | Главный поток | Субагенты | Обоснование из эмпирики блока 10.08 |
|---|---|---|---|
| 0. Проверка актуальности промта, состояние деревьев, гейты | **Sonnet 5 · low** | **Sonnet 5 · low** (прогон гейтов отдельным исполнителем) | четыре команды и чтение вывода; нажим здесь не покупает ничего — проверено трижды за блок |
| 1. Выпуск 2.4.5 после моста: сборка, доказательство хешем, публикация | **Opus 5 · high** | **Sonnet 5 · medium** (сверка состава установщика, распаковка, хеши) | необратимое наружу и деньги клиента; в этом блоке именно на выпуске вскрылось, что движок внутри июльский — цена ошибки максимальная |
| 2. Сверка «интерфейс против канала» (корень CPD-67) | **Opus 5 · medium** | **Sonnet 5 · medium** (сбор фактического состава каналов), **Opus 5 · high** один на замысел сторожа | тонкость не в коде, а в том, ЧТО сверять: сверка не того конца уже один раз была зелёной при живом дефекте |
| 3. Живой прогон облака на PC204 | **Opus 5 · medium** | **Sonnet 5 · low** (сбор журналов, повторные попытки) | диагностика по чужим журналам; механика дешёвая, суждение на приёмке |
| 4. High-1: два источника вердикта — решать или закрывать как компромисс | **Opus 5 · high** | **Opus 5 · high**, один в чистом контексте на второе мнение | архитектурная развилка, видимая клиенту; в этом блоке именно второе мнение перевернуло постановку |
| 5. Механика: журналы, реестр, канон, документы | **Sonnet 5 · low** | **Sonnet 5 · low** | работа по образцу, суждение не требуется |
| 6. Внешний аудит блока | **Opus 5 · medium** (приёмка) | **Opus 5 · high**, узкий, в чистом контексте | канон: аудитор не ниже модели сессии. В этом блоке аудитор нашёл пропущенное место правки и слепоту сторожа — оба при полностью зелёных гейтах |
| 7. Wrap-up | **Opus 5 · medium** | **Sonnet 5 · medium** (репро при триаже) | завершение по регламенту |

## 7. 🔴 Протокол остановок для смены модели

**Граница ответственности.** Остановки касаются **только главного потока** — его модель и нажим
переключает исключительно владелец командой `/model`. **Модель и нажим субагентов оркестратор
назначает сам, на каждый запуск, явными параметрами, без остановок и без спроса** — иначе субагент
унаследует дефолт и шаг тихо подорожает либо просядет. Спрашивать разрешение на выбор модели
субагента — потеря хода.

**Стартовая модель сессии: `Sonnet 5 · low`.** Если сессия начата на другой — попросить переключиться
**первым сообщением**, до начала работы. Причина: первая фаза — проверка актуальности этого промта и
снятие гейтов, высокий нажим там не окупается.

### Контрольные точки

| № | Что закончено (наблюдаемый признак) | Переключить на | Причина |
|---|---|---|---|
| 1 | Промт сверен с деревом, гейты сняты, состояние доложено | **`/model opus high`** | дальше выпуск — необратимое действие наружу |
| 2 | Установщик собран, движок внутри доказан хешем, публикация выполнена либо отложена владельцем | **`/model opus medium`** | дальше сверка каналов по известному образцу |
| 3 | Сверка «интерфейс против канала» внедрена, сторож краснеет на мутации | **`/model opus medium`** (остаётся) | облачный прогон — диагностика, не проектирование |
| 4 | Облачный прогон дошёл до конца либо упёрся в назван­ную причину | **`/model opus high`** | High-1 — архитектурная развилка, видимая клиенту |
| 5 | По High-1 принято решение (свести или закрыть компромиссом) | **`/model opus medium`** | дальше аудит блока и завершение |

### Шаблон предупреждения

> Закончила: <что именно, наблюдаемый признак>. Дальше: <следующий этап>. Прошу переключить:
> **`/model <модель> <нажим>`** — потому что <причина одной фразой>. Скажи, когда переключишь.

### Пока владелец не ответил

Не переключаюсь сама и в следующий этап не захожу, но и не простаиваю: записываю сделанное узким
перечнем путей, обновляю память и файл состояния, готовлю входы и задания субагентам следующего
этапа.

### В автономном режиме

Остановок **нет вообще**, владельца не дёргать. Переключить главный поток технически невозможно —
работать на стартовом нажиме, а разрыв компенсировать субагентами: этап требует нажима выше
текущего — тонкую часть вынести субагенту с нужным уровнем, себе оставить приёмку; ниже текущего —
массовую механику отдать дешёвому. В финальном отчёте перечислить точки, где переключение главного
потока было бы уместно, и что компенсировано субагентами.

### Внеплановая эскалация

Необратимая развилка · Critical от аудита · класс дефекта вместо единичного случая · обнаружение
данных клиента там, где их быть не должно · расхождение вердикта надёжности между форматами на живом
отчёте · любое действие на боевом узле → остановиться немедленно, не дожидаясь границы фазы.
