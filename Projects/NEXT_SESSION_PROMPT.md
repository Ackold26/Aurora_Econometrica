# Промт следующей сессии — после блока 11.09.2026

Скопируй в начало новой сессии (cwd = `D:\Docs\Aurora_Ai\Dev\Aurora_Econometrica_thinwt`).

**Стартовая модель: `Opus · high`.** Первое дело — починить находки аудита и собрать установщик.
Ничего из прошлого блока не проверено живьём.

---

## Контекст

Блок 11.09 закрыт, **но сборки не было**. Владелец: «соберём установщик после wrap-up» — это и есть
первая задача.

**Записи блока сделаны и НЕ отправлены** (отправку владелец решает отдельно):
`a6881a2f` два режима и метка шлюза · `d35851a7` находки аудита и графики · `c885fbad` документы
блока. Память: `e13561d` в репозитории `aurora-claude-memory`.

Сделано и принято:

- **Два режима вместо трёх.** Один переключатель, автоопределение и путь в Claude Code клиента
  выведены из рабочих путей. Хранимая ось одна, согласие — часть выбора режима. Тесты Rust **520/0**.
- **Починки по внешнему аудиту прошлых блоков** в движке: вердикт убыточного канала, сторож слайда
  прогноза, тире в клиентском тексте. Тесты движка **1470/2/0**.
- **Графики**: пара «разброс и остатки» разделена на два независимых разворота, декомпозиция —
  друг под другом, полноэкранный режим больше не вечно тёмный, в колоду добавлен слайд «Факт
  против прогноза» (родная диаграмма с данными), в веб-отчёт — три графика качества. Фронтенд
  **1489/0**.
- **Метка общего слоя поднята `v0.6.6 → v0.6.7`**, проверка сборки зелёная, контракт сошёлся.
  Долг доставки правки кириллицы закрыт: она вошла в главную линию записью `e427db7`.
- **Долг перед серверной линией**: их три вопроса закрыты, ответ отправлен. Наш открыт.

---

## 🔴 ЗАДАЧА №1 — ПОЧИНИТЬ НАХОДКИ АУДИТА, ПОТОМ СОБРАТЬ

Внешний аудит блока дал **13 находок: 6 высоких, 6 средних, 1 отклонена самим аудитором**.
Перечень: `Projects/audit_findings_s41.md`.

### ✅ Critical — НАЙДЕН И ПОЧИНЕН В ЭТОЙ ЖЕ СЕССИИ

**Нечисловое значение в ряду «факт/прогноз» роняло сборку ВСЕЙ колоды.** Родная диаграмма
встраивает книгу данных, а та падает на `NaN`/бесконечности: `TypeError: NAN/INF not supported in
write_number()`. Исключение поднималось на `add_chart`, то есть до возврата из слайда качества, и
уходило наружу из построителя — клиент не получал колоду **вовсе**, а не колоду без одного графика.
Вход достижим: пропуск наблюдения в факте, разошедшаяся цепь в прогнозе, `NaN` в файле результата.
🔴 Режим отказа **внесён этой же правкой** — раньше ряд через адаптер не прокидывался.

Ирония класса: ровно то свойство, ради которого требовалась родная диаграмма с данными вместо
картинки, и делает нечисловое значение фатальным.

Починено в `engines/narrative_adapter.py` — отбраковка `math.isfinite` по каждому элементу; ряд с
негодным значением не кладётся вовсе, и срабатывает уже существующее правило «график только при
живых данных». Доказано прогоном: чистый ряд → ключ есть; с `NaN` → нет; с бесконечностью → нет.
18 тестов зелёные.

**Две высокие видны клиенту — чинить обязательно до сборки:**

1. **Перенос настроек показывает ложное сообщение** (`user_config.rs:109`). `migrate_route_axis`
   ставит `execution_mode="cloud"` и признак сообщения, **не проверяя согласие**. А фактическое
   правое положение согласия требует (`execution_mode.rs:241`, `route_is_cloud`). У кого согласие
   отозвано или устарело — человек прочтёт «теперь запросы идут через шлюз Авроры», а маршрут
   останется левым. Это ровно тот класс лжи продукта о себе, ради которого блок и затевался.
   Проверено лично, не со слов аудитора.
2. **Отказ записи настроек проглочен** (`user_config.rs:114`, `let _ = save(...)`). При настройках
   только для чтения или полном диске перенос возвращает «успех», сообщение показывается, признак
   не снимается — и перенос срабатывает заново при каждом запуске. Заявленная идемпотентность
   держится только на успешной записи.

**Четыре высокие — про сторожей; клиенту не видны, но ослабляют защиту:**

3. Сторож маршрута не ищет `decide_with_history(` — автоопределение можно вернуть через него.
4. Запрет локального пути проверяется только в телах двух точек входа — обходится промежуточной
   функцией (`fn dispatch(){ run_claude_inner(..) }`).
5. 🔴 **Мой собственный карв-аут «голая схема без хоста» даёт обход** (`guard_left_position_egress.rs:160`):
   `format!("{}{}", "https://", host)` невидим сторожу. Я вводила его, чтобы снять ложную красноту
   на разборе ссылки в «Входящих» — сузить надо точнее, по контексту разбора, а не по виду литерала.
6. **`GATED_BY_ROUTE` прощает три файла по списку, не доказывая, что гейт там есть.** Обращение из
   `rag_client.rs` в левом положении пройдёт мимо сторожа по построению.

**Средние — в бэклог, кроме одной:** запись настроек не атомарна (`fs::write` без временного файла
и блокировки) — две копии программы затрут правки друг друга, обрыв питания даст битый файл, и
`load` молча вернёт значения по умолчанию, потеряв все настройки человека без единого слова.

### Потом — сборка

🔴 **Первым действием `Skill(aurora-fix)`**, вердикт по PHASE 2 таблицей, и только потом:
```
CARGO_TARGET_DIR="D:/cargo-targets/ai-agency" npm run tauri:build:thin
```
Приёмка — та же команда с `--test` (с признаком `thin`, иначе 28 проверок шлюза не выполняются).
Сборку не гнать через конвейер: код возврата съест последняя команда.

---

## 🔴 ЗАДАЧА №2 — ЖИВОЙ ПРОГОН (без него блок не принят)

Ничего из сделанного не видел человек. Смотреть по списку:

1. **Три темы × развёрнутый график**: фон, подписи осей, сетка, читаемость точек разброса. Плюс
   смена темы при уже открытом развороте.
2. **Шаг после обучения**: два отдельных разворота вместо одного на оба графика.
3. **Переключатель режимов**: подпись «Сейчас: …» в обоих положениях, сообщение при обращении к
   ассистенту слева, однократное сообщение о смене маршрута.
4. **Колода**: новый слайд «Факт против прогноза» — открыть график в PowerPoint и убедиться, что
   таблица данных доступна (требование владельца: график несёт данные, а не картинку).
5. **Веб-отчёт**: три графика качества, подписи о норме, три темы.

---

## 🔴 ЗАДАЧА №3 — НАШ ВОПРОС К СЕРВЕРНОЙ ЛИНИИ

Их сторона закрыта полностью: `meta` маршрутом не управляет, посредник `POST /jobs` не видел,
записей об обрывах ноль, окно `apt-daily-upgrade` в ~09:14 МСК найдено и признано.

**Что установлено у нас** (`Projects/firstrun_trace.md`):
- «минута» = лестница повторов 2+5+10+20+30 = 67 с при шести попытках;
- 🔴 **ни один отказ подготовки не даёт шести попыток** — повторяется только `CloudError::Connection`
  (`client.rs:1526-1529`). Значит наблюдаемое однозначно сетевого класса, а не «запрос не собрался»;
- единственное отличие `POST /jobs` от `GET /meta` в заголовках — ключ отправки `Idempotency-Key`,
  строго ASCII; всё остальное строит одна функция;
- случаев **три** (12:52, 19:06, 22:25), все одинаковы; гипотеза «первый вход» опровергнута их
  таблицей закреплений.

🔴 **МЕХАНИЗМ НАЙДЕН — `client.rs:2017-2025`, `describe_transport`:**

```rust
if err.is_timeout()      { "сервис не ответил вовремя" }
else if err.is_connect() { "не удалось установить соединение" }
else                     { format!("обрыв связи: {err}") }   // ← ловит ВСЁ остальное
```

Третья ветка заворачивается в `CloudError::Connection` (`:1606-1610`) — **единственный вид, который
повторяется** (`:1526-1529`). А в используемой библиотеке ошибка ПОСТРОЕНИЯ запроса (негодное
значение заголовка, негодный адрес) не возникает в момент `.header(...)` — она хранится в
построителе и возвращается из `.send()`.

Тело собирается один раз (`:701-702`), а **сам запрос пересобирается на каждой из шести попыток**
(`:1523` → `:1607` → `signed_request` `:1579-1593`). Значит отказ сборки повторяется ровно шесть
раз, мгновенно, и в сеть не уходит ни байта — ровно то, что видел узел.

Структурно это возможно только у `POST /jobs`: у него есть заголовок, которого у `GET /meta` нет
вовсе — `Idempotency-Key` (`client.rs:704`, значение собирает продукт `gateway_executor.rs:897`).
Пять общих заголовков одинаковы у обоих запросов — потому `meta` и уходит.

🔴 **Честная оговорка, не выдавать за доказанное:** сделать это значение не-ASCII в Эконометрике
НЕ удалось, искали тремя способами.

1. Генератор метки `gateway_executor.rs:217-232` — `format!("tc-{cabinet_id}-{:08x}")`, чистая
   латиница.
2. Метка берётся не только оттуда: `let label = resume_session_id.unwrap_or_else(|| generate…)`
   (`:776`). Серверная линия указала на это как на непроверенный источник — проверено: значение
   приходит из `state.session_manager.get_claude_session_id(cabinet_id)` (`lib.rs:1306-1310`), то
   есть **из памяти процесса от предыдущего прогона**, а не от человека и не из интерфейса.
   Кладёт его `set_claude_session_id` (`session/manager.rs:460`); в облачном пути это наша же
   сгенерированная метка (`gateway_executor.rs:1127`). Цепочка замкнута на собственные латинские
   значения.
3. Серверная сторона проверила метки дошедших заданий: не-ASCII ноль. Оговорка их же: задания,
   которые не собрались, на сервере невидимы по определению — опровергнуть версию их данными
   нельзя, подтвердить тоже.

4. 🔴 **Последнее звено закрыто серверной линией: узел НЕ возвращает метку клиенту вовсе.**
   Вид ответа собирается одной функцией (`cloud/store.py:610-624`), в нём одиннадцать полей, и
   `session_key` среди них нет — ни при создании задания, ни при опросе, ни в ленте. Метка целиком
   живёт внутри нашего процесса.

**Значит версия «в метке не-ASCII» исчерпана: виновник не метка.** Механизм (отказ сборки запроса
выдаётся за обрыв связи) остаётся подтверждённым, но искать надо среди ОСТАЛЬНЫХ заголовков.
Единственное значение, приходящее извне нашего кода, — **билет**: его выдаёт функция входа.
По построению это base64, то есть чистая латиница, — но что в него попало при передаче или
хранении, проверить можем только мы у себя.

**Два дешёвых хода, согласованных с серверной линией:**
- поставить проверку значений заголовков на не-ASCII ПЕРЕД сборкой запроса и падать своим внятным
  отказом. Это одновременно диагностика и та самая правка «отдельный вид ошибки без повторов» —
  одна работа, а не две;
- журнал машины показа: строка `Облачное обращение не удалось […]` несёт дословный текст ошибки
  библиотеки, а он назовёт и поле, и причину одним словом.

🔴 **Тот же код в четырёх продуктах** — `format!("{label}-{}", short_stamp())` дословно в
Эконометрике (`:897`), Smart Analytica (`:1231`), Creative Hub (`:1284`), Docs Lab (`:890`).
Подтвердится — чинить один раз в общем слое, задето четверо.

**И моя гипотеза «общее у обоих случаев — файлы» по коду вывернулась наизнанку.** У соседнего
продукта, наоборот, СЕТЕВОЙ отказ одет в текст про файлы: `client.rs:1279` → `:1317-1335`
(`NotSent{phase: Uploading}`) → «Файлы для работы отправлены не полностью – связь прервалась».
Общее у двух случаев не файлы, а `describe_transport`.

**Что делать дальше:**
1. Починить само превращение: отказ сборки запроса — НЕ «обрыв связи» и повторять его шесть раз
   бессмысленно. Отдельный вид ошибки, без повторов, с честным текстом. Это правка общего слоя —
   согласовать с серверной линией, задевает всю линейку.
2. Добавить в журнал клиента причину каждой неудачной попытки (сейчас шесть попыток не пишут ни
   строки — `client.rs:1526-1535`, только сон; наружу уходит лишь последняя). Это Б-40.
3. **Решающий факт для конкретного случая** — журнал программы с машины показа
   (`%LOCALAPPDATA%\<продукт>\logs`, окна 12:52–12:56 и 19:06 UTC), строка
   `Облачное обращение не удалось […]`: она несёт текст `{err}` из третьей ветки и назовёт
   настоящую причину. Запросить у владельца.

🔴 И отдельно к чтению любых жалоб: код `[TC-GW-NET]` в начале строки **не доказывает сетевую
причину** — он навешивается на ЛЮБОЙ `CloudError` (`gateway_executor.rs:1008`), включая отказ
сервера по существу. Различает только текст из `user_text()`.

---

## Задачи после этого

- **Сравнительный график вариантов вставляется в колоду картинкой** (`aurora_pptx/builder.py:3991`,
  растр из `charts/generators.py`) — нарушает требование «график несёт данные». Чинить или в бэклог,
  решает владелец.
- **Бэклог Б-36…Б-44** — `Projects/BACKLOG_after_audit_2026-09-08.md`. Главные: Б-40 (код причины
  отказа облака не пишет никто), Б-42 (отказ поставщика приезжает как успешный результат), Б-37
  («сошлось» не значит оптимум при уменьшённом бюджете).
- **Два документа показа с версией 2.5.0** — ждут владельца.
- **Ежедневное окно ~09:14 МСК на узле**: автоматическое обновление системы перезапускает службы
  без предупреждения, задание клиента в этот момент оборвётся. Серверная линия взяла в работу.

---

## Файлы для контекста (порядок чтения)

1. `Projects/handoff.md` — цель блока, девять инвариантов, **шесть зон неуверенности**.
2. `Projects/audit_findings_s41.md` — находки внешнего аудита (перечень выше — выжимка).
3. `Projects/firstrun_trace.md` — разбор пути от нажатия до отправки задания.
4. `Projects/ANSWER_from_server_team_2026-09-11.md` — ответ серверной линии, их журнал целиком.
5. `Projects/TASKS_after_show_2026-09-10.md` — задания владельца, включая решения Р-1..Р-3.
6. Память: `INDEX_econometrica` · `feedback_claim_of_absence_needs_proof_of_coverage` ·
   `feedback_silent_substitution_of_the_subject`.

---

## Инварианты блока (не менять без явного решения)

- Одна хранимая ось режима; `execution_mode` — её зеркало.
- Подпись «Сейчас: …» считается от фактического положения и называет **все три** допустимых
  обращения наружу: лицензия, обновления, докачка содержимого.
- Отказ чтения состояния не выдаётся ни за одно из положений.
- Редакция для 152-ФЗ (`--no-default-features`) не трогается: пути наружу там нет в коде вовсе.
- График в колоде несёт данные внутри себя; легенда только родная.
- Клиентский текст — короткое тире «–».
- Сторож обязан уметь покраснеть; мутация называется явно.
- Сборка поставки — только `npm run tauri:build:thin`, приёмка — она же с `--test`.
- Удаление — только мягкое, в корзину.

---

## 🎯 Руководство по стилю действий

1. **Утверждение об ОТСУТСТВИИ доказывается охватом поиска, а не результатом.** Прежде чем сказать
   «нет такого» — ответить себе: где искал · чем искал · все ли носители перебрал. За прошлые сутки
   шесть взаимных поправок между тремя сессиями, и все одного рода. Чужое «нет» — гипотеза:
   проверять СВОИМ способом до того, как понесёшь дальше.
2. **Проверка может работать НЕ НАД ТЕМ предметом, и признака этого нет.** Поиск цели по подстроке
   берёт первое совпадение (тест семь месяцев мерил слайд оглавления вместо слайда плана);
   запасное значение несуществующей переменной работает как жёстко вписанное (полный экран был
   вечно тёмным). Требовать два независимых структурных признака и печатать опознавательный
   признак найденного.
3. **Статус исполнителя надёжнее его маячка.** За сессию шесть обрывов службы; трижды я собиралась
   забирать работу по правилу «три пустые проверки», а `ListAgents` показывал `running` — агент был
   жив и писал. Спрашивать статус раньше, чем идти по правилу тишины.
4. **Durable-агента поднимают ЕГО ЖЕ именем, а не заводят нового.** Поднятый продолжает со своим
   контекстом; новый, даже с журналом предшественника, повторяет половину работы. Проверено пять
   раз за сессию, потерь не было ни разу.
5. **Двум исполнителям — разные файлы отчётов.** Два трассировщика писали в один файл и затёрли
   друг друга дважды; спасла только копия, снятая заранее. Разводить имена ДО запуска, а не после
   первого затирания.
6. **Перед правкой сторожа спросить, что он пропустит, а не что поймает.** Мой собственный карв-аут
   в стороже обращений наружу аудитор обошёл за одну строку. Сторож пишется под опровержение так же,
   как задание аудитору.
7. 🔴 **Финальный неочевидный приём этой сессии: заявленная идемпотентность держится на успешной
   записи — и рушится молча.** `migrate_route_axis` объявлена идемпотентной в докстроке, тест это
   подтверждает, а отказ записи проглочен (`let _ = save(...)`). При настройках только для чтения
   перенос срабатывает при каждом запуске, и человек видит сообщение о смене маршрута снова и снова.
   **Класс шире этого случая:** любое свойство, доказанное в памяти процесса (идемпотентность,
   «признак снят», «состояние сохранено»), обязано проверяться на отказе носителя — иначе доказано
   не свойство, а удачный день. Спрашивать не «выполняется ли свойство», а **«что произойдёт, если
   запись не удастся, и узнает ли об этом хоть кто-нибудь»**.

---

## 🧠 Распределение моделей и нажима по фазам

| Фаза | Главный поток | Субагенты |
|---|---|---|
| 1. Починка находок аудита | `Opus · high` — маршрут данных и ложное сообщение клиенту | правки сторожей `Sonnet · high` · мутации `Opus · medium` |
| 2. Предсборочный чеклист и сборка | `Opus · medium` — вердикт таблицей, приёмка | прогон тестов `Haiku · low` · наблюдение за сборкой `Sonnet · low` |
| 3. Живой прогон | `Opus · high` — форма проверяется глазом, находки разбирает человек | фиксация находок `Sonnet · medium` |
| 4. Наш вопрос к серверной линии | `Opus · high` — противоречивые данные с двух сторон | чтение журналов `Sonnet · high` · трассировка `Opus · medium` |
| 5. Бэклог Б-36…Б-44 | `Opus · medium` — приёмка | Б-40, Б-41 `Sonnet · high` · Б-37 (математика) `Opus · high` |

**Правило раскладки:** проверяемое фактом уходит вниз по цене; решения на противоречивых данных
остаются наверху. Триаж не делегируется никогда.

---

## Протокол остановок

**Стартовать на `Opus · high`.** Модель главного потока переключает только владелец вручную —
останавливаться и просить нужно явно. **Модель и нажим субагентов оркестратор назначает сам, на
каждый запуск, без спроса** — это его ответственность, а не повод для остановки.

| Контрольная точка | Что сказать владельцу |
|---|---|
| Находки аудита починены, тесты зелёные | «Починки закрыты. Дальше чеклист и сборка — можно снизить до `Opus · medium`: там приёмка по таблице» |
| Чеклист прогнан, вердикт получен | «Чеклист дал <вердикт>. Собираю? Это около получаса» |
| Сборка готова | «Установщик собран, сумма <…>. Дальше живой прогон — верни `Opus · high`, форму проверяет глаз» |
| Перед отправкой наружу | «Отправляю записи / письмо. Подтверди» |

**Шаблон предупреждения:** что закончено → что дальше → команда `/model <модель> <нажим>` → причина
одной фразой → «скажи, когда переключишь».

**Пока владелец не ответил** — не переключаться самой и не заходить в следующий этап, но и не
простаивать: записи изменений, память, подготовка заданий исполнителям.

**В автономном режиме** остановок нет вовсе. Главный поток переключить технически невозможно —
работать на стартовом нажиме, а разрыв компенсировать субагентами: тонкое (разбор противоречий,
проверка сторожа мутацией) вынести в `Opus · high`, механику отдать `Sonnet · low`. В финальном
отчёте назвать точки, где переключение было бы уместно.

**Внеплановая эскалация:** нашлось противоречие в данных, где обе стороны выглядят верными, либо
находка задевает маршрут данных клиента — остановиться немедленно, не дожидаясь границы фазы.
