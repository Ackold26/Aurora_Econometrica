# Промт следующей сессии — после блока подготовки к показу (01.09.2026)

Скопируй в начало следующей сессии (cwd = `D:\Docs\Aurora_Ai\Dev\Aurora_Econometrica_thinwt`).
**Стартовая модель: `Opus · high`** — первый шаг упирается в пересборку и живой прогон перед показом.

## Где мы

🎯 **Через несколько дней — демонстрация продукта потенциальному покупателю и выдача ему
тестового доступа.** Всё остальное подчинено этой дате.

**Комплект собран:** `C:\Users\ackol\Desktop\Демо-показ Эконометрики\` — установщик 2.5.0,
сценарий показа, «Лёгкий старт», ванпейджер, обзор рынка, справка по конкурентам, контекст сессии.

**Запись `6446df7`** — четыре правки честности продукта, гейты зелёные (движок 1378/1, типы 0
на 4159 файлах). 🔴 **Не отправлена в хранилище и не собрана в установщик.**

🔴 **Главное расхождение, которое надо снять первым:** установщик в комплекте демонстрации
собран **17.08**, а правки блока сделаны **01.09**. То есть покупатель получит программу
БЕЗ починок L-08, CPD-104, журнала аудита и H-4 в интерфейсе.

## Файлы для контекста (порядок чтения)

1. `Projects/handoff.md` — **канонический handoff блока** (цель, семь инвариантов, шесть
   компромиссов, шесть зон неуверенности, затронутые файлы). Читать первым.
2. `Projects/audit_findings_live.md` + отчёт внешнего аудитора — находки по записи `6446df7`.
3. `C:\Users\ackol\Desktop\Демо-показ Эконометрики\Контекст сессии – подготовка демо-показа.md`
   — полный протокол подготовки: что решено, что проверено, что осталось.
4. `Projects/TEST_FINDINGS_live_2026-08-31.md` — находки живого прогона установщика, включая
   L-08 с пробой на живом движке.
5. `Projects/DEMO_SCRIPT_2026-09.md` — сценарий показа (в комплекте лежит его версия в Word).
6. Память: `INDEX_econometrica` (канон состояния обновлён 01.09),
   `feedback_probe_the_verdict_not_only_the_number`, `feedback_acceptance_by_use_not_by_checks`.

⚠️ Промежуточные редакции прошлых блоков (`handoff_2026-08-14.md`, `handoff_p07.md`,
`handoff_s5.md`, `handoff_cpd77_*.md`, `handoff_kpi_units.md`) — **читать не обязательно**.

## 🔴 Задачи по приоритету

### 1. Пересобрать установщик с правками 01.09 — иначе весь блок не доехал
- **Только через скилл `aurora-fix`** (распоряжение владельца, вся линейка).
- Собирать **из этого дерева** (`_thinwt`). Из основного `Aurora_Econometrica` НЕЛЬЗЯ: там
  12 каталогов клиентских данных в путях ресурсов (CPD-16).
- 🔴 **Сначала пересобрать движок** `python sidecar/econometrica/build_sidecar.py` — `npm run
  tauri build` его НЕ пересобирает, страж свежести при сборке установщика не исполняется.
- Поставка клиенту — **только со шлюзом**: `npm run tauri:build:thin`. Штатная `npm run tauri
  build` даёт поставку БЕЗ шлюза (CPD-115).
- После сборки — **заменить установщик в папке демонстрации** и сверить контрольную сумму.

### 2. Разобрать ОСТАВШИЕСЯ находки внешнего аудита
Аудит записи `6446df7` выполнен старшим аудитором (конкурентность — замок журнала).
**Обе находки High уже разобраны и исправлены записью `f04c307`** — правка была неполной:
обрезка хвоста медиаплана не срабатывала, когда истории нет вовсе или целевая величина
рвётся посреди ряда. Теперь наблюдением считается строка с известной целевой величиной.
Приёмка на четырёх файлах, сторож проверен мутацией, движок 1379/1.

🔴 **Пять Medium остались неразобранными** (`Projects/audit_findings_live.md`, полный отчёт
аудитора там же). Каждую подтвердить или опровергнуть техническим доказательством:
1. `server.py:1085` — обрезается не только число наблюдений, а сам набор данных: меняется вход
   быстрой оценки и априорного прогноза. Аудитор считает это расхождением с инвариантом
   handoff «меняем основание вердикта, а не вердикт». **Проверить пробой**: сдвигается ли
   вердикт на демонстрационном файле из-за роста показателя коллинеарности.
2. `claude.rs:960` — строка `NaN` в исходной таблице даёт нечисловой литерал в кэше формулы;
   до правки там был ноль, и файл открывался.
3. `report.rs:1621` — при нулевой сумме в кэш пишется ноль, а формула при пересчёте даст
   ошибку деления: кэш и формула разойдутся.
4. `audit.rs:48` — замок внутрипроцессный; две копии программы у одного пользователя всё ещё
   могут смешать строки. Дешёвая страховка — собрать строку целиком и записать одним вызовом.
5. `audit.rs:97` — проверка берёт срез файла по длине, снятой до прогона; ротация внутри самой
   проверки уронит её по выходу за границу, а не по существу.

**Один вопрос аудитор не закрыл из-за бюджета** (пометка `OPEN`): может ли ветвь допуска
вернуть режим точного совпадения — тогда карточка критерия скажет «расхождение до 0 %» под
заголовком «если расчёт повторит независимый специалист». Закрывается одним поиском по
`applicable_mode` в `utils/repro_tolerance.py`.

### 3. Живой прогон интерфейса после H-4
Блок «Критерий совпадения» проверен диффом, типами и содержимым JSON, но **глазами в собранной
программе не виден ни разу**. Открыть эталонный проект `demo-fmcg-brand`, дойти до шага
«Декомпозиция», убедиться, что блок отрисован и числа верные (ветвь «другое зерно при полном
расчёте», допуск 5 %, доля вклада 1,5 процентного пункта).

### 4. Прогнать сценарий показа целиком, по документу
`Projects/DEMO_SCRIPT_2026-09.md`, часть 6. Три места, помеченные «проверить накануне»:
появляется ли баннер «модель будет ориентировочной»; переспрашивает ли мастер незаполненные
поля проекта; как выглядит оценка времени при живом переобучении (интерфейс показывает до
десяти минут при реальных 2,5).

### 5. Долги, ждущие решения владельца
- **CPD-151** — публикуемый ключ имеет право очистки боевых таблиц лицензий. Правка — одна
  команда `revoke`, безопасность доказана 30.08. 🔴 Это боевая база: применится немедленно ко
  всем клиентам, «копить до сборки» нельзя. Делать самой, с готовым откатом, не субагентом.
- **Телефон-заглушка** `8-800-123-12-34` в ванпейджере — владелец уберёт сам.
- **«ГРП» → `GRP`** в `src/lib/components/pipeline/AnalysisModeSelector.svelte:126`. Правка
  владельца не сохранилась; слово живёт в пяти деревьях продукта. Профессиональные термины и
  аббревиатуры под правило отказа от англицизмов НЕ подпадают (уточнение владельца 01.09).
- **Материалы продаж устарели** — `aurora-meta/SALES/econometrica-one-pager.md` и питч-дек:
  цена 2 млн вместо 700 тыс., версия 1.0.16 вместо 2.5.0, обещаны импортёры данных, которых
  нет, сертификат назван PDF-документом.

### 5а. 🔴 Четыре вопроса по материалам покупателя — ждут решения владельца
Вскрыты независимым проверочным проходом уже после того, как я приняла обзор рынка. Сам обзор
исправлен и в комплекте лежит чистым (чужих цен нет, проверено по готовому файлу), но эти
четыре пункта остались открытыми:

1. **Под тремя агрегированными числами обзора нет фактуры.** Утверждения «локальная обработка
   заявлена у 1 из 24», «метод опубликован у 9 из 24», «требования к данным у 4–5 из 24»
   опираются на разведочные файлы разбора от 02.08, которые **удалены вместе с каталогом той
   сессии**. Спросят «как считали» — показать нечего. Либо пересобрать просмотр (день работы),
   либо снять три строки: документ без них не рушится.
2. **Два утверждения об открытых библиотеках не подтверждены:** у Meridian «тренд узловой
   интерполяцией» и «опубликованный чек-лист здоровья модели», у PyMC-Marketing «причинный
   слой». Подтверждено другое (гео-иерархия и калибровка экспериментами у Meridian, лицензия
   Apache 2.0 и полные апостериорные распределения у PyMC). Либо подтвердить, либо убрать.
3. **Заголовок заявки 2026118554 содержит «доверительный интервал»** — термин, запрещённый
   нашим же правилом для клиентского текста (положено «правдоподобный диапазон», INV-50). Это
   дословное название поданной заявки, менять нельзя. На показе прозвучит вразрез с остальным
   текстом — решить, оговаривать ли это вслух.
4. **Внутренняя справка по конкурентам, раздел 6, устарела:** там цена описана как открытый
   вопрос с рекомендацией 1,5–2 млн, тогда как решение владельца — 700 000. Два документа на
   одном столе будут противоречить друг другу. Правка на пару минут, но готовый документ по
   своей инициативе не трогали.

### 6. Отложенное, но не забытое
- Обновить внутреннюю справку по конкурентам под новую рамку: она писалась до сужения круга
  сравнения и до подтверждения консультанта, поэтому занижает нашу позицию.
- Остальные ~30 мест повторного чтения файла в движке (`backtest.py`, `scenario.py`,
  `optimize/*`) — влияют ли на них строки медиаплана, не выяснялось.
- Балласт в поставке: установка весит 1 883 МБ, около половины — второй экземпляр движка в
  `dist/`. 🔴 Сужать состав поставки **не в этом выпуске**: установщик резко похудеет, и
  пост-сборочная проверка «меньше предыдущего» даст ложную тревогу.

## Инварианты и правила блока

- **Ничего наружу без слова владельца:** публикация, отправка в общий репозиторий, боевая база.
- **Удаление — только в корзину.** Исключение — собственный сборочный сор.
- **Тесты движка запускать из каталога `sidecar/econometrica`.**
- **Пороги достаточности данных (20 / 30 наблюдений) — не трогать.** Правки меняют основание
  вердикта, а не сам вердикт.
- **Число наблюдений считается только по истории**, через единственную функцию
  `engines.planning.detect_media_plan_tail`. Своих копий этой логики не заводить.
- Клиентский текст: короткое тире «–», русский без англицизмов **кроме профессиональных
  терминов и аббревиатур** (`GRP`, `TRP`, `ROI`, `MMM` — латиницей), на «вы», без эмодзи.
- Сборка любой программы — **только через скилл `aurora-fix`**.

## Руководство по стилю действий на следующую сессию

1. 🔴 **Проверяй ВЕРДИКТ, а не только число.** Найдя расхождение двух чисел об одном объекте,
   спроси следующий вопрос: какое решение принимается на каждом. Разночтение в подписи —
   косметика, разночтение в основании вердикта — ложь продукта о себе. В этой сессии
   косметическая задача «подписать 104 и 130» вскрыла L-08, где надутое число меняло вердикт
   о пригодности данных.
2. 🔴 **Приёмку правки, меняющей вердикт, требуй на ДВУХ входах:** где вердикт обязан остаться
   прежним (защита рабочего сценария) и где обязан измениться (доказательство починки). Одного
   мало: первый без второго доказывает лишь, что ничего не сломано.
3. **Дефект маскируется на удобном входе.** Демонстрационный набор (104 против 130) не
   проявлял L-08 вовсе — оба числа выше порога. Понадобился специально собранный файл из
   15 строк истории. Собирай граничный вход сама, не жди, что дефект вылезет на штатных данных.
4. **Принимай работу исполнителя СВОЕЙ мутацией продуктового кода.** Четыре правки этой сессии
   приняты так: отключаешь починку — проверка обязана покраснеть ровно тем дефектом, ради
   которого написана. Зелёный тест без этого ничего не доказывает.
5. **Упавшего агента поднимай сообщением, а не заводи нового.** Агенты durable: обращение по
   имени возобновляет его с СОБСТВЕННЫМ контекстом. В этой сессии я завела нового и потеряла
   всю сверку по коду, которую предшественник держал в голове и не успел записать.
6. **Клиентский документ проверяй глазами через PDF, а не грепом.** Три раза за сессию
   визуальная проверка ловила то, чего не видно в разметке: растянутые в «решето» строки,
   разорванные между страницами таблицы, вложенное форматирование, напечатанное служебными
   символами, и два списка со сквозной нумерацией.
7. 🔴 **Утверждения клиентских материалов сверяй с кодом поимённо.** В ванпейджере нашлись два
   обещания, которых продукт не выполняет: «подписанный сертификат» (в коде хеш, подписи нет) и
   перечень источников данных, читавшийся как готовые интеграции. Оба прожили месяцы.

8. 🔴 **ГЛАВНЫЙ ПРИЁМ СЕССИИ (пост-промтная рефлексия): дешёвая задача — лучший разведчик
   дорогого класса.** Три самые ценные находки этого блока пришли не из целевого поиска, а из
   попутного взгляда при выполнении мелочи. L-08 (ложное «надёжно») вскрылся, когда исполнитель
   правил подписи на экране. Гонка журнала аудита — когда я читала журнал ради проверки
   лицензии. Два ложных обещания ванпейджера — когда его чистили от вклеенного текста.
   **Свойство, которое это объясняет:** целевой поиск идёт по гипотезе и видит только то, что
   в неё укладывается; попутный взгляд не отягощён гипотезой и потому замечает несоответствие.
   **Как применять:** в задание на любую мелкую правку добавляй пункт «если по дороге увидишь
   что-то, что противоречит заявленному поведению — не чини сразу, доложи». Это стоит одной
   строки в задании и окупается непропорционально: именно так исполнитель, правивший подписи,
   нашёл дефект, который бил по главному обещанию продукта.

## 🧠 Распределение моделей и нажима по фазам

Обоснование — из эмпирики 31.08–01.09, не из шаблона.

| Фаза | Главный поток | Субагенты | Почему |
|---|---|---|---|
| Пересборка движка и установщика по `aurora-fix` | **Opus · high** | **Sonnet · low** (прогоны, копирование, сверка сумм) | необратимо для покупателя; тихие дефекты вида «зелёная сборка со старым движком» ловятся вниманием к признакам, не к статусам |
| Разбор находок внешнего аудита | **Opus · high** | **Opus · high** на логику и конкурентность, **Sonnet · medium** на тексты | опровержение находки рассуждением дважды за две сессии оказалось бы ошибкой; зонд обязателен |
| Живой прогон интерфейса и сценария показа | **Opus · medium** | **Sonnet · medium** (подготовка данных, чеклисты) | работа по готовому документу, тонкого решения нет |
| Правка клиентских материалов и текстов | **Opus · medium** | **Sonnet · medium** (сборка docx), **Opus · high** на формулировки, задевающие обещания продукта | 01.09: два обещания ванпейджера расходились с кодом; цена ошибки — доверие покупателя |
| CPD-151 (боевая база) | **Opus · high** | **не делегировать вовсе** | необратимо, применяется немедленно ко всем клиентам; субагентам боевую инфраструктуру не отдаём |
| Правки движка и математики | **Opus · high** | **Opus · high** | новая логика в расчёте |
| Массовая механика (сборка документов, копирование, регенерация) | **Opus · medium** | **Sonnet · low** | форма известна, решений нет |
| Память, промт, wrap-up | **Opus · medium** | **Sonnet · low** | записи не требуют высокого нажима |

**Стартовая модель сессии: `Opus · high`.** Если сессия начата на другой — попросить
переключиться ПЕРВЫМ сообщением, до начала работы.

🔴 **Граница ответственности:** остановки касаются ТОЛЬКО главного потока — его модель и нажим
меняет исключительно владелец командой `/model`. **Модель и нажим СУБАГЕНТОВ оркестратор
назначает сам, на каждый запуск, без остановок и без спроса** — задавать явно параметрами,
иначе субагент унаследует умолчание и шаг тихо подорожает либо просядет.

### Протокол остановок (главный поток)

| Что закончено (наблюдаемый признак) | На что переключить | Почему |
|---|---|---|
| Установщик пересобран, сумма снята, заменён в комплекте | остаться на `Opus · high` | дальше разбор находок аудита — опровержение требует зондов |
| Находки аудита разобраны, fix-запись сделана | `/model opus medium` | дальше живой прогон и тексты по готовым документам |
| Дошли до CPD-151 (боевая база) | `/model opus high` | необратимо, применяется ко всем клиентам немедленно |
| Дошли до правки движка или математики | `/model opus high` | новая логика в расчёте |
| Перед wrap-up | `/model opus medium` | записи не требуют высокого нажима |

### Шаблон предупреждения на остановке

> Закончила **<что именно, наблюдаемым признаком>**. Дальше **<следующий этап>** — это
> **<причина: необратимое / новая логика / аудит>**.
> Переключи, пожалуйста: `/model <модель> <нажим>`
> Скажи, когда переключишь, — до этого продолжу работу текущего уровня.

### Пока владелец не ответил

Не переключаться самой и в следующий этап не заходить, но **не простаивать**: делать работу
текущего уровня — записи, обновление памяти, подготовка входов и заданий субагентам, сбор
фактуры, прогон гейтов. Субагентов при этом назначать свободно.

### В автономном режиме

Остановок нет вообще, владельца не дёргать. Главный поток переключить технически невозможно →
**разрыв компенсировать субагентами**: этап требует нажима выше текущего — тонкую часть
вынести субагенту нужного уровня, себе оставить приёмку; ниже текущего — массовую механику
отдать дешёвому. В финальном отчёте перечислить точки, где переключение было бы уместно, и что
компенсировано.

### Внеплановая эскалация

Остановиться **немедленно**, не дожидаясь границы фазы, если: правка задевает уже выпущенные
модели, сертификаты или боевую базу · аудит дал Critical · вместо единичного дефекта
обнаружился класс · собственная цифра, на которой строится решение, разошлась с цифрой
исполнителя.
