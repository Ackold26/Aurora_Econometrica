# Промт следующей сессии — после блока 16.08.2026 (воспроизводимость + подготовка выпуска 2.5.0)

Скопируй в начало следующей сессии (cwd = `D:\Docs\Aurora_Ai\Dev\Aurora_Econometrica_thinwt`).
**Стартовая модель: `Opus · high`** — первый шаг упирается в сборку и выпуск клиентам.

## Где мы

**Ветка `feat/econ-p1-winning` слита в `master`, гейт полноты пуст. Версия поднята до 2.5.0.**
У клиентов по-прежнему **2.4.9**; ни профит-фронтир, ни воспроизводимость к ним не уехали.

Гейты на конец сессии (прогнаны лично): движок **1333 / 1 пропущен** · фронт **1471 из 1471**
(100 файлов) · типы **0 ошибок** на 4158 файлах · `cargo check` **код возврата 0** · крючок
справки зелёный, PDF пересобран (117 страниц).

**Что сделано за день:** профит-фронтир доведён до экрана (утро) и блок воспроизводимости (день) —
отпечаток исходных данных при обучении, протокол выбора затухания, живой генератор параметров
каналов (был мёртв, отдавал пустышку), 19 приоров с семействами распределений, календарь
праздничных окон, самоописание алгоритма отпечатка, измеренный критерий совпадения.

🎯 **Главный результат: посторонний аналитик в чистом контексте, имея ТОЛЬКО паспорт и таблицу
клиента, собрал модель заново и получил числа в объявленном допуске — 16 величин из 16, худшее
отклонение 5,80 % при допуске 10 %.** Это третий заход; первый дал «нельзя» (20 пунктов), второй —
совпадение с одной вынужденной сверкой, третий — вслепую, с письменной фиксацией всех прочтений
ДО расчёта. Отчёты: `REPORT_outside_analyst_2026-08-16.md`, `PULSE_outside_analyst2/3_*.md`.

## 🔴 ПЕРВЫЙ ШАГ: сборка (в этом порядке, иначе бессмысленно)

1. **Пересобрать движок:** `python sidecar/econometrica/build_sidecar.py`, дождаться
   `[OK] Freshness verified`.
   🔴 **Почему это блокер:** собранный `econometrica-sidecar.exe` был от **13.08**, а исходники
   правились по 16.08 — новее двоичного файла **27 файлов**, и это ровно вся работа 2.5.0.
   `npm run tauri build` движок НЕ пересобирает, страж свежести живёт внутри сборки движка и при
   сборке установщика не исполняется. Без пересборки установщик встанет зелёным, а профит-фронтир
   и воспроизводимость будут отвечать кодом от 13-го. Дефект абсолютно тихий.
2. **Собирать из ЭТОГО дерева** (`_thinwt`). Проверено: движок полный (67 МБ + `_internal/` 912 МБ),
   пустоты не будет; доказательство от противного — установщик 2.4.10 собран отсюда 15.08 и весит
   258 648 371 байт, ровно в ряду 2.4.8 и 2.4.9.
   🔴 **Из основного дерева `Aurora_Econometrica` НЕ СОБИРАТЬ:** там в `sidecar/econometrica/`
   лежат **12 каталогов клиентских проектов** «кагоцел-рф-…», а шаблон ресурсов рекурсивный —
   данные клиента уехали бы в установщик всем получателям (CPD-16, не закрыт, только обойдён).
3. **Сборка — только по скиллу `aurora-fix`** (распоряжение владельца). Полный pre-build чеклист
   уже прогнан, отчёт: `Projects/PULSE_prebuild_gate_2026-08-16.md`, раздел «## Отчёт». Все правки
   версии из его списка **уже внесены** (`15c4ae0`, `f7c83b9`): 2.5.0 в конфигурации Tauri,
   манифесте сборки, пакете фронта, замке зависимостей, свойствах файла движка, манифесте справки.
4. **Живой прогон — на PC204, владелец.** Готовить список того, что проверять руками: карточка
   профит-фронтира на шаге оптимизации, поле валовой маржи, отказ при отсутствии экономики,
   паспорт в выгруженном отчёте (HTML и PPTX).

**Желательное перед сборкой** (из чеклиста, не блокирует): `updater.rs` — добавить `/R` к
аргументам установщика, иначе после тихого обновления программа у клиента не стартует сама;
`beforeBuildCommand` → `npm run check:manifest && npm run build` как заслон на будущее.

## 🔬 Внешний аудит блока: 12 находок (`PULSE_audit_repro_2026-08-16.md`)

**Чинится в конце сессии 16.08** (исполнитель `fix-critical`, проверить результат первым делом):
- **C-1 Critical.** Загрузчик подставляет умолчания (`kpi_type='sales'`, `likelihood='normal'`,
  `kind='monetary'`), а выгрузка метит их «прочитано из файла модели как есть». **Модель,
  обученная на знании марки, заверяется как модель продаж.** Подтверждено моим зондом лично.
  Сторож слеп по построению: кормит модуль голым словарём МИМО загрузчика, а продукт всегда идёт
  через него.
- M-2 (сверка выносит «совпало», не сверив ничего), M-3 (факт «отпечатки не сверялись» теряется
  по дороге к вердикту), L-1 (текст «nan» в ячейке неотличим от пропуска).

🔴 **Класс C-1 шире, чем KPI — проверить первым делом.** След загрузчика
(`_loader_injected_defaults`, заведён починкой `cd7a029`) на ПОЛНОЙ живой модели показывает
**19 подставленных полей**, а не три: `mcmc_diagnostics`, `backtest_results`, `ppc_results`,
`analysis_mode`, `derived_mode`, `weibull_params_per_channel`, `safe_corridor_cache`,
`value_per_count_unit*` и другие. Починен и проверен зондом только блок KPI. **Любое место, где
выгрузка или сертификат метят эти поля «записано в модели», сегодня может врать так же.**
Дешёвая проверка: прогнать выгрузку и сертификат на модели, перечитанной загрузчиком, и сверить
каждое поле со следом. Это ровно тот случай, когда правка по списку адресов не закрывает класс.

**Остаётся на следующую сессию — обе про доставку до клиента:**
- 🔴 **H-4. Критерий совпадения до клиента не доходит.** Измерен семью прогонами, ветвь считается
  автоматически — а до глаз клиента не добирается.
- 🔴 **H-5. Обещана выгрузка параметров, которой у пользователя нет.** Решение владельца: полное
  раскрытие **по явному действию**. Паспорт про действие говорит, самого действия в интерфейсе нет.
  Строить: команда Rust + кнопка на фронте.
- H-1 / H-2 (диапазон дат включает строки, на которых модель не обучалась; тот же отсев даёт ложное
  «не сошлось» по праздничным признакам), H-3 (вторая опора критерия объявлена для всех величин,
  работает для одной), M-1, M-4, L-2 — разобрать по отчёту.

## Дальше по плану линии «выигрывать»

Разведка под **досье решения** готова: `PULSE_dossier_recon_2026-08-16.md`. Три факта меняют
постановку: (1) «в подпись входит только разбивка» устарело — подписаны пять полей; (2) половина
досье уже собрана, но вне подписи — дешёвый ход не строить новое, а перенести готовое В хешируемый
набор; (3) **сведение частей не даст досье на реальных данных** — у живого клиента три части из
четырёх физически отсутствуют, постановку надо переписать с «собрать в один объект» на «сделать
так, чтобы части возникали». И предупреждение: первое же настоящее досье будет с плохим
вердиктом (единственная живая проверка на истории — «хуже наивного»), это стоит показать
владельцу до того, как документ уедет клиенту.

Остальное линии: реестр допущений «что сломает вывод» (правая колонка готова, левой нет) ·
знание бренда и ESOV (движок готов, Rust-команды не вызываются; пометка «вне области v1.3»
снимается только по слову владельца) · советник, видящий всю диагностику · локальный советник
для 152-ФЗ · слоистый отчёт по адресату · панель здоровья продукта.

## 📌 За владельцем

Живой прогон 2.5.0 на PC204 · выпуск (публикация в Supabase, обе строки `app_versions`:
`aurora-econometrica-gui` — её спрашивает клиент — и `econometrica`, отставшая на 2.4.0; плюс
судьба отсутствующей `aurora-econometrica-gui-local`) · коммит правок сайта (13 адресов +
ванпейджер, лежат в рабочем дереве `aurora-platform-web`, там же работает Маша небесная) ·
отправка **CPD-100** в общий репозиторий · клиентские данные в основном дереве (CPD-16) ·
зомби без порта · второй потребитель ядра (Docs Lab) · CPD-81/88 у Media Radar и PR Studio.

## Инварианты и правила блока

- **Ничего наружу без слова владельца:** публикация, метки, отправка в общий репозиторий.
- **Удаление — только в корзину.**
- **Тесты движка запускать из каталога `sidecar/econometrica`.**
- ⚠️ **Полный прогон — только когда исполнители закончили** (CPD-86: под нагрузкой ложные красные).
- Клиентский текст: короткое тире «–», русский без англицизмов, на «вы», без псевдоточности,
  **без эмодзи**.
- 🔴 **Правка чужого дерева.** Генератор оформления теперь пишет в своё дерево (`--product-root`,
  запись `37f1013`). Если увидишь `M` на `src/tokens.generated.css` в чужом дереве — это чей-то
  прогон без флага, восстановить `git checkout` и сказать владельцу.

## 🎯 Что этот день доказал (методы, оплаченные ошибками)

1. 🔴 **Достаточность документа проверяется только попыткой им ВОСПОЛЬЗОВАТЬСЯ.** 1272 наших теста
   не поймали ни одного из 20 пунктов постороннего: они сверяют «записано то, что записано».
   → `feedback_acceptance_by_use_not_by_checks`
2. 🔴 **Правя противоречие, можно породить худшее.** Я назвала точкой нормировки насыщения
   `media_means` — неверно; цена измерена аналитиком: 19,3 % при допуске 10 %, ответ не проходит
   собственный критерий документа. Объяснение роли величины проверяется кодом её вычисления,
   а не соседним комментарием.
3. **Пустой вывод при коде возврата ноль — не «зелёный».** Запустила несуществующий скрипт тестов,
   npm вернул 0 и молчание. Читать вывод, а не код возврата.
4. **`git checkout` не откатывает неотслеживаемый файл** — команда промолчит, мутация останется.
   Проверять результат отката прогоном, а не фактом выполнения команды.
5. **Метка времени в маячке исполнителя должна браться у системы.** По меткам «на глазок» я
   объявила живую работу умершей.
6. **Канал сообщений теряет текст отчётов** — просить исполнителей дублировать отчёт в свой маячок.
7. **Проверка, выглядящая правильной, может молча не применяться:** `--out-*` у генератора токенов
   перекрывается флагом `--product`. Доказывать прогоном, а не видом команды.

8. 🔴 **ГЛАВНЫЙ ПРИЁМ ДНЯ (пост-промтная рефлексия): проверяй СЛЕД, а не рапорт инструмента.**
   Трижды за день «зелёное» оказалось ложным, и каждый раз по-своему: npm вернул код 0 на
   несуществующем скрипте (тесты не запускались вовсе); генератор токенов честно напечатал
   `[OK] wrote`, записав файл в ЧУЖОЕ дерево; `git checkout` промолчал на неотслеживаемом файле,
   оставив мутацию в коде. **Общее свойство: инструмент рапортует о факте выполнения, но не об
   адресате и не об объёме.** Спасало одно и то же — перед действием спросить себя, какой
   НАБЛЮДАЕМЫЙ СЛЕД останется, если оно сработало, и проверять именно след: время изменения
   чужого файла до и после, число строк в выводе, содержимое перечитанного с диска артефакта.
   Приём переносим на весь класс: сборка, публикация, генерация, откат, запись в общий ресурс.
   Особенно важен там, где адресат действия задан не тобой, а умолчанием инструмента.

## 🧠 Распределение моделей и нажима по фазам

Обоснование — из эмпирики 16.08, не из шаблона.

| Фаза | Главный поток | Субагенты | Почему |
|---|---|---|---|
| Пересборка движка и сборка установщика по `aurora-fix` | **Opus · high** | **Sonnet · low** (прогоны, копирование, сверка сумм) | необратимо для клиентов; тихие дефекты вида «зелёная сборка со старым движком» ловятся только вниманием к признакам, а не к статусам |
| Разбор находок аудита H-1…H-5, M-1, M-4, L-2 | **Opus · high** | **Opus · high** на логику критерия и дат, **Sonnet · medium** на тексты | 16.08 опровержение находки рассуждением дважды оказалось бы ошибкой; проверка зондом обязательна |
| Доставка до клиента (H-4 критерий, H-5 выгрузка параметров) | **Opus · medium** | **Sonnet · medium** (фронт, Rust-команда), **Opus · high** на паритет форматов | работа по готовому описанию; тонкое — только паритет HTML и PPTX |
| Досье решения: решение о составе подписи | **Opus · high** | **Opus · high** (разведка по коду) | смена набора хешируемых полей меняет смысл ВСЕХ выданных сертификатов, откат у клиента невозможен |
| Досье решения: сборка и вывод | **Opus · medium** | **Sonnet · medium** | по принятому описанию |
| Реестр допущений, слоистый отчёт, панель здоровья | **Opus · medium** | **Sonnet · medium** | форма известна, математики нет |
| Тренд компонентой, обучаемый Weibull | **Opus · high** | **Opus · high** | новая математика в модели |
| Приёмка опытом (посторонний по паспорту) | **Opus · high** | **Opus · high**, чистый контекст | 16.08 этот шаг нашёл то, чего не поймали 1272 теста; дешевле не делать нельзя |
| Память, промт, wrap-up | **Opus · medium** | **Sonnet · low** | записи не требуют высокого нажима |

**Стартовая модель сессии: `Opus · high`.** Если сессия начата на другой — попросить переключиться
ПЕРВЫМ сообщением, до начала работы.

🔴 **Граница ответственности:** остановки касаются ТОЛЬКО главного потока — его модель и нажим
меняет исключительно владелец командой `/model`. **Модель и нажим СУБАГЕНТОВ оркестратор назначает
сам, на каждый запуск, без остановок и без спроса** — задавать явно параметрами, иначе субагент
унаследует умолчание и шаг тихо подорожает либо просядет.

### Протокол остановок (главный поток)

| Что закончено (наблюдаемый признак) | На что переключить | Почему |
|---|---|---|
| Установщик собран, сумма снята, отчёт о сборке записан | остаться на `Opus · high` | дальше разбор находок аудита — опровержение требует зондов |
| Находки аудита разобраны, fix-запись сделана | `/model opus medium` | доставка до клиента идёт по готовым описаниям |
| Дошли до решения о составе подписи досье | `/model opus high` | меняется смысл всех выданных сертификатов |
| Дошли до тренда компонентой или обучаемого Weibull | `/model opus high` | новая математика в модели |
| Перед wrap-up | `/model opus medium` | записи не требуют высокого нажима |

### Шаблон предупреждения на остановке

> Закончила **<что именно, наблюдаемым признаком>**. Дальше **<следующий этап>** — это
> **<причина: необратимое / новая математика / аудит логики>**.
> Переключи, пожалуйста: `/model <модель> <нажим>`
> Скажи, когда переключишь, — до этого продолжу работу текущего уровня.

### Пока владелец не ответил

Не переключаться самой и в следующий этап не заходить, но **не простаивать**: делать работу
текущего уровня — записи, обновление памяти, подготовка входов и заданий субагентам, сбор фактуры,
прогон гейтов. Субагентов при этом назначать свободно.

### В автономном режиме

Остановок нет вообще, владельца не дёргать. Главный поток переключить технически невозможно →
**разрыв компенсировать субагентами**: этап требует нажима выше текущего — тонкую часть вынести
субагенту нужного уровня, себе оставить приёмку; ниже текущего — массовую механику отдать дешёвому.
В финальном отчёте перечислить точки, где переключение было бы уместно, и что компенсировано.

### Внеплановая эскалация

Остановиться **немедленно**, не дожидаясь границы фазы, если: правка задевает уже выпущенные
модели или сертификаты · аудит дал Critical · вместо единичного дефекта обнаружился класс ·
собственная цифра, на которой строится решение, разошлась с цифрой исполнителя.
