# Промт следующей сессии — после блока s45 (13.09.2026)

Скопируй в начало новой сессии (cwd = `D:\Docs\Aurora_Ai\Dev\Aurora_Econometrica_thinwt`).

**Стартовая модель: `Opus · high`.** Если сессия начата на другой — попроси переключиться ПЕРВЫМ
сообщением, до начала работы.

---

## 🔴 ПОРЯДОК РАБОТ — указание владельца 12.09, действует

**чиним → проверяем → аудируем → исправляем и дорабатываем → проверяем → и ТОЛЬКО ПОСЛЕ ЭТОГО
собираем установщик.** Сборка — последнее действие, а не способ проверить правку.

Проверять вёрстку дёшево и без сборки: `npm run tauri:dev` → мост 9223 → измерение через
`webview_execute_js`.

---

## Что сделано в s45

🟢 **ОТПРАВЛЕНО В `origin` 13.09:** `61e0afe7..fc94298f`, пятнадцать записей. Расхождения с
удалённой линией нет — начинать можно с чистого места, ничего догонять не надо.
Гейты сняты лично после последней правки: фронтенд **1578 прошло / 115 файлов**, типы **0 ошибок /
183 предупреждения**, движок **1571 прошло / 2 пропущено / 0 упало**.
🔴 Движок гонять ТОЛЬКО как `pytest tests` из `sidecar/econometrica`.

| Запись | Что |
|---|---|
| `59a6abc9` | первая ступень графиков отчётов: подписи значений, `scale:true` на двух осях |
| `f2e7e7d0` | Пасха 13-й записью в панели клиента + сторож читает живой календарь движка |
| `1da6bc50` | **кривые отдачи — корень**: параметры каналов не читались с диска вовсе |
| `a15aeb20` | подсказки шага «Планирование», каждая условна от настоящего порога |
| `36a02e11` | справка перестроена на семь страниц по шагам мастера + паспорта показателей |
| `95489b21` | список отвязанных страниц справки сам падает, если запись пережила файл |
| `5eee7544` | **число праздников в знаменателе «запаса данных» — из календаря, не константы** |
| `dd9c9dde` | обратная связь подключена к форме Яндекса + воскрешён мёртвый сторож ПДн |
| `8228bb94` | документы передачи |
| `b251706e` | находки внешнего аудита A: тихая запасная ветка, NaN в снимке параметров, сторож ПДн на мёртвом пути |
| `31c0d3cb` | загрузчик макрорядов ЦБ и Росстата + пробная выгрузка ИПЦ |
| `3d2c6a1d` | находки внешнего аудита B: утечка чужого проекта на «Планировании», противоречивые подсказки, утверждение без проверки, тупиковая ссылка справки |

**Три находки, которых в постановке не было:**
1. `validator.py` считал число параметров по константе «12 праздников», а их 13 — знаменатель
   занижен, «запас данных» на экране выглядел **лучше настоящего** у каждого нового проекта.
2. Сторож паритета праздников сверял два фронтовых артефакта между собой, а не фронт с движком —
   горел зелёным всё время расхождения.
3. В `feedback.rs` сторож на состав обращения (ПДн, INV-38) был **вложен в соседний тест** из-за
   незакрытой скобки и не выполнялся ни разу. Поймано предупреждением компилятора.

**Форма обратной связи собрана и проверена двумя живыми отправками.**
`https://forms.yandex.ru/cloud/6aa6912e6d2d73e851bbccda/`, имена полей читаемые:
`produkt`, `versiya`, `licenziya`, `vremya`, `ekran`, `sistema`, `oshibka`.

---

## 🔴 ЗАДАЧА №1 — три живые пробы пути отказа обновления (условие перед сборкой)

Перешла из промта s44 не сделанной. Внешний аудит s43 назвал этот путь главным риском выпуска.
Тесты есть (сторож 22 проверки), **живых проб не было ни одной**.

Прогнать три сценария и записать, что видит человек:
1. **нет сети** — отключить интернет либо сделать недоступным адрес обновления;
2. **отказ в правах администратора** — нажать «Нет» в системном запросе;
3. **манифест без контрольной суммы** — подложить манифест с пустым полем суммы.

Ожидаемое: в первом случае человек читает про проверку подключения (а не «напишите в
поддержку»); во втором — «подтвердите запрос прав» вместо совета освободить место на диске;
в третьем — текст без обещания, что повтор поможет.

🔴 Пробы 1 и 3 гоняются на рабочей сборке без установщика. Проба 2 честно проверяется **только на
настоящем установщике** — значит она идёт ПОСЛЕ сборки, и это единственное исключение из порядка
работ. Не имитировать её на рабочей сборке: имитация даст ложное «проверено».

Опора: `src-tauri/src/commands/updater.rs` (метка `[клиенту]`, помощники `user_err`/`user_text`),
`src/lib/updateErrorText.js`, журнал `Projects/AUDIT_s43_external.log` (находки H-1, H-2).

---

## 🔴 ЗАДАЧА №2 — обратная связь: доделать до конца

Механизм в `src-tauri/src/commands/feedback.rs` работает, адрес и четыре поля подставлены.
Осталось четыре вещи, **первая блокирующая**:

**2.1. 🔴 Доказать, что исправленный текст письма доезжает.** В блоке была исправлена опечатка в
имени продукта («Aurora Ai» → «Aurora AI») в первом автоответе. Правка сохранена в форме, но
письмо, пришедшее в прогоне, было ещё старым. **Одна отправка с любой темой, кроме «Предложение
или отзыв», это закроет.** Пока не доказано — считать неподтверждённым.

**2.2. Три поля из семи код не шлёт.**
- `sistema` — версия Windows, уже собирается в `diagnostics.rs`, раздел «System»;
- `ekran` — шаг мастера знает ФРОНТЕНД, значит команде нужен параметр;
- `oshibka` — текст последней ошибки, туда же параметром.

То есть `open_feedback_form()` становится `open_feedback_form(ekran, oshibka)` с необязательными
значениями. 🔴 Сторож `settings_page_opens_the_form_and_does_not_call_the_old_path` проверяет, что
страница настроек зовёт именно `open_feedback_form` — не сломать его сменой подписи.
🔴 Текст ошибки обрезать (ориентир 300 знаков): адрес с семью полями может перерасти то, что
принимает системный вызов открытия браузера, и тогда браузер молча не откроется.

**2.3. 🔴 Кнопка живёт только в настройках — это закоулок.** Человек, у которого сломалось, идёт
не туда. Место кнопки — в окне ошибки и в шапке рядом со справкой. И это не только про
доходимость: именно оттуда `ekran` и `oshibka` берутся САМИ, без единого вопроса клиенту. Из
настроек их взять неоткуда в принципе.

**2.4. Пять остальных продуктов.** Те же пять постоянных в их `feedback.rs`. 🔴 Новая метка
`aurora_core` НЕ нужна — адрес и поля живут в самой программе, доезжает обычной сборкой.

---

## 🔴 ЗАДАЧА №3 — макроряды: доложить курсы и ставку с машины с доступом к ЦБ

**Загрузчик НАПИСАН и работает** — `sidecar/econometrica/tools/fetch_macro_series.py`, сторож
`tools/test_fetch_macro_series.py` (19 проверок, зелёные, способность падать доказана). Только
стандартная библиотека плюс уже имеющийся `openpyxl`, новых зависимостей нет.
Запуск: `cd sidecar/econometrica && python tools/fetch_macro_series.py [--years 10]`.
Пробная выгрузка — `Projects/macro_series_draft/` (`macro_weekly.csv`, `macro_monthly.csv`,
`README_macro_series.md` со статусом каждого источника).

**Что уже загружено по-настоящему:** индекс потребительских цен из Росстата, 2016-09 – 2026-08,
уровень 100,0 → 184,63, помесячное изменение из самого файла Росстата (не пересчитано нами).

🔴 **Что НЕ загружено и почему — причина известна и к этой сессии уже устранена.** Доллар, евро,
юань и ключевая ставка не загрузились: сайт ЦБ не отвечал, `curl` к
`https://www.cbr.ru/scripts/XML_daily.asp` уходил в таймаут на 25 секундах при 200 от Росстата
(проверено лично). **Причина — новый VPN на машине владельца, уводивший ВЕСЬ трафик наружу**: для
российских государственных сайтов это выглядит как зарубежный адрес, и они не отвечают. Владелец
сказал 13.09, что VPN перенастраивается. Дефекта в загрузчике нет.

**Что делать дальше — первым делом:**
1. 🔴 **Проверить доступ одной командой, прежде чем что-либо делать:**
   `curl -s -o /dev/null -w "%{http_code}
" --max-time 20 https://www.cbr.ru/scripts/XML_daily.asp`
   Ответ `200` — можно грузить. Таймаут — сказать владельцу, что VPN всё ещё уводит трафик наружу,
   и не тратить ход на разбор загрузчика: он тут ни при чём.
2. Прогнать загрузчик. Это закроет четыре показателя из пяти за один запуск.
2. 🔴 При этом прогоне **проверить схему ответа метода `KeyRate`**: исполнитель заложил её по
   независимой документации, а не по факту — вживую метод не отвечал. Разбор падает явно при
   несовпадении схемы, то есть молча неверных чисел не будет, но поправить, возможно, придётся.
3. Если `cbr.ru` не отвечает даже после перенастройки VPN — запасной путь: ручная выгрузка курсов
   и ставки владельцем. 🔴 Зеркала вроде `cbr-xml-daily.ru` **не официальный источник** — для
   поставки, за числа которой мы отвечаем, их использовать нельзя (INV-50).
4. 🔴 То же ограничение бьёт по ЛЮБОЙ работе с российскими государственными сайтами из этой
   машины — Росстат сегодня отвечал, но у него может быть иначе завтра. Правило на будущее: прежде
   чем объявлять источник недоступным, проверить, не уводит ли VPN трафик наружу. Признак: один
   зарубежный сайт отвечает, государственный российский — нет.

🔴 **Находка, меняющая постановку: понедельного ИПЦ у Росстата НЕТ.** Соседний файл
`Nedel_ipc.xlsx` — это еженедельный мониторинг примерно 116 отдельных товаров, а не составной
индекс. Подставлять его вместо ИПЦ нельзя, это другой показатель. Понедельный ряд ИПЦ в выгрузке —
честное растягивание месячного значения, и так и написано в `README_macro_series.md`.
Решить с владельцем, годится ли это, или понедельная частота для ИПЦ просто не предлагается.

**Следующая ступень (не делалась):** укладка справочника в `content-packs` и пере-подпись
`python tools/sign_content_pack.py --bump`. 🔴 Без пере-подписи контрольная сумма в манифесте
устареет и доставка молча не доедет до клиента.

## 🟡 ЗАДАЧА №4 — шаг планирования в отчётности

Часть 5.3 из промта s44, осталась не сделанной. Клиент видит план на экране, а в уносимом
документе его нет — ни в колоде, ни в веб-отчёте. Состав согласовать с владельцем, опираясь на
`Projects/CHART_SPEC_static_reports.md` (утверждён 14.09). Оттуда же следующие ступени графиков:
дыра с декомпозицией в колоде (функции написаны, не вызываются) и печатный режим веб-отчёта.

---

## 🔴 ЗАДАЧА №5 — передаточный пакет для тестирования клиентом (задача владельца 13.09)

🔴 **Порядок обязателен: пробы (задача №1) → сборка установщика → пакет.** Собирать пакет раньше
нельзя: отдать первому клиенту сборку, у которой путь обновления не проверен вживую, значит отдать
ему самый неприятный сценарий — программа поставилась, поработала, а через неделю сломалась при
обновлении. Первое впечатление не переделывают.

**Принцип отбора, по которому пакет и собирается:** класть не «что у нас есть», а то, что закрывает
конкретный режим отказа теста. Иначе в пакет заводятся красивые, но бесполезные вещи.

| Что сорвёт тест | Что кладём |
|---|---|
| не смог установить (антивирус, SmartScreen, права) | установщик + записка для ИТ-специалиста |
| установил, но **нечего загрузить** | 🔴 демо-проект с данными + шаблон входного файла |
| запустил, но не понял, что считать хорошим результатом | пример готового отчёта на демо-данных |
| не знает, что именно от него хотят | сценарий теста на одну страницу |
| уткнулся и промолчал | контакт живого человека + обещанный срок ответа |
| безопасность спросила «куда уходят наши данные» | одна страница про данные |

**Состав пакета:**
1. **Установщик** — собранный командой со шлюзом (`npm run tauri:build:thin`), не штатной.
2. **Краткая инструкция: установка и первые тридцать минут.** Не руководство пользователя —
   справка теперь встроена и перестроена по шагам мастера, бумажный дубль устареет с первой же
   версией и начнёт противоречить программе. Вместо него страница со ссылками ВНУТРЬ встроенной
   справки. 🔴 Отдельным разделом — **процедура выдачи лицензии по шагам**: где в программе взять
   отпечаток машины, куда его прислать, что делать с полученной лицензией. Это шаг, на котором
   тест спотыкается чаще всего.
3. 🔴 **Демо-проект с готовыми данными** — самое важное из всего пакета. Это MMM: без таблицы
   медиарасходов и продаж программу нельзя даже открыть. Клиент, которому надо сначала выпросить
   данные у своего медиаотдела, вернётся через три недели или никогда. С демо-проектом он проходит
   весь путь за двадцать минут в первый же вечер. 🔴 Данные — синтетические или обезличенные,
   ни в коем случае не чужого клиента (см. правило про утечку в сборках, CPD-16).
4. **Шаблон входного файла и список обязательных столбцов** — вынести из справки отдельным файлом.
   Второй шаг после демо: «а теперь на своих».
5. **Пример готового отчёта** на демо-данных — часто убеждает сильнее описания продукта: человек
   видит конечный результат до того, как вложил свои усилия.
6. **Сценарий теста на страницу** — три-пять шагов «что сделать» и два-три вопроса «что скажете».
   Иначе тестирование превращается в «установил, покликал, забыл». Это же инструмент сравнения
   ответов разных клиентов между собой.
7. **Одна страница про данные:** где хранится, что уходит наружу, что не уходит. 🔴 Здесь сильная
   карта — **локальная редакция без обращений наружу** (`npm run tauri:build:local`). Для первого
   клиента всерьёз рассмотреть именно её: вопрос «вы не сливаете наши медиапланы» снимается
   устройством, а не обещанием.
8. **OnePager** — канон в `aurora-meta/SALES/`, брать оттуда, не писать заново.

**Записка для ИТ-специалиста — самая тонкая часть, формулировки важнее содержания:**
- 🔴 Про подпись: **не** «мы не купили сертификат», а факт — выдача сертификатов подписи кода
  российским юрлицам сейчас закрыта. Это внешнее ограничение, оно касается всех российских
  разработчиков, и оно не про нашу экономию.
- 🔴 Вместо подписи — **контрольная сумма установщика, переданная ДРУГИМ каналом** (сумма письмом,
  установщик ссылкой). Подписать не можем, но обнаружить подмену дать можем; безопасность это
  оценит, потому что сама предложила бы ровно эту меру.
- Дословно, что увидит человек: «Windows защитила ваш компьютер» → «Подробнее» → «Выполнить в
  любом случае». Два клика, названные заранее, снимают почти все вопросы.
- 🔴 **Установка и обновление не требуют прав администратора** (`installMode: currentUser`) — для
  корпоративной среды это редкость и сильный довод, назвать прямо.
- Точные пути для доверенной зоны антивируса: каталог программы в профиле пользователя и каталог
  её данных (`%LOCALAPPDATA%` и `%APPDATA%\com.aurora.econometrica`).
- 🔴 Отдельно предупредить про **ложные срабатывания на расчётный модуль**: он собран упаковщиком
  Python, и эвристики некоторых антивирусов реагируют на сам способ упаковки, а не на поведение.
  Предложить услугу: «пришлите название антивируса и вердикт — отправим на перепроверку вендору».
  Это переводит разговор из оправдания в сервис.

**Чего в пакет НЕ класть:** полное руководство пользователя (дублирует встроенную справку и
устареет), презентацию (OnePager закрывает вопрос «что это», колода — инструмент другого
разговора).

---

## ⬜ Не проверено живьём в s45 — перенести в проверку

- **подсказки шага «Планирование»** — исполнителю был запрещён подъём моста, текст на экране и
  момент появления подсказок глазами никто не видел;
- **перестроенная справка** — 23 файла, автопроверки зелёные, но ни в приложении, ни в PDF не
  открывалась;
- **кривые отдачи** — замер сделан на одном демо-проекте с четырьмя каналами; поведение при
  отсутствии `models/latest-params.json` и на проекте другой редакции движка не смотрели;
- **сдвиг знаменателя «запаса данных»** — на реальных клиентских данных не прогонялся. Часть
  проектов сменит цвет надёжности: клиент увидит, что «стало хуже», хотя стало честнее. Стоит
  заранее решить, говорим ли мы об этом в примечаниях к версии;
- **живая установка на машине БЕЗ прав администратора (V74)** — по-прежнему не проводилась.

**🟡 Долг по справке, найден дизайн-сторожем уже на завершении блока.** В
`src-tauri/help-econometrica/features.html` — два места с функциональным текстом мельче 11 пикселей
(нечитаемо на плотных экранах) и пропуск уровня заголовка (ломает оглавление для чтения с экрана).
🔴 Это НЕ наша правка: в блоке файл менялся на пять строк, только перенацеливание ссылок; дефекты
были там до нас и всплыли потому, что файл тронули. Чинить вместе со следующим заходом в справку,
а не отдельно.

**Якоря проверены, они на месте.** Перенацеливание поставило ссылки вида `step-3-model.html#read`
и `step-4-decompose.html#read`; якорь `read` в обеих страницах есть (проверено `grep` на
завершении блока). Отмечаю потому, что битая ссылка внутри справки не даёт ни ошибки сборки, ни
пустоты — страница просто откроется не в том месте, и заметить это можно только глазами.

---

## Файлы для контекста (порядок чтения)

1. **этот промт** — целиком, включая руководство по стилю и таблицу моделей внизу;
2. `Projects/handoff.md` — передача аудитору, канонический экземпляр на конец блока;
3. `Projects/audit_findings_live_A.md` и `Projects/audit_findings_live_B.md` — находки внешнего
   аудита блока (движок и Rust · фронтенд и инструменты);
4. `Projects/AUDIT_s43_external.log` — аудит позапрошлого блока, для задачи №1;
5. `Projects/CHART_SPEC_static_reports.md` — для задачи №4;
6. `Projects/HELP_RECON_s45.md` — опись справочной системы, если придётся её трогать;
7. память: `INDEX_econometrica.md` (горячая зона = текущее состояние), затем
   `feedback_guard_that_does_not_guard_two_mechanisms.md`,
   `reference_yandex_forms_no_programmatic_submit.md`,
   `feedback_acceptance_command_checks_less_than_it_looks.md`;
8. код по задачам: `src-tauri/src/commands/updater.rs`, `src-tauri/src/commands/feedback.rs`,
   `src/lib/updateErrorText.js`.

---

## Инварианты (соблюдать без напоминаний)

1. **Клиентский текст** — русский без англицизмов где есть русское слово; короткое тире «–»,
   длинное «—» запрещено; «доверительный интервал» и «CI» запрещены (это «правдоподобный диапазон»).
2. **Честность метрик (INV-50)** — число на экране соответствует источнику; псевдоточность
   запрещена. Утверждение о данных — только если порог реально выполнен по числам.
3. **Старые модели воспроизводятся своим списком праздников** — добавление события не меняет
   поведение ранее обученных моделей.
4. **INV-38** — ни имени пользователя, ни имени машины, ни путей к файлам в обращении.
5. **Поставка — только командой со шлюзом**; установка и обновление без прав администратора.
6. **Удаление — только в корзину**, никогда `rm -rf` и `Remove-Item`.
7. **Сборка — через `Skill(aurora-fix)` первым действием**, вердикт по таблице PHASE 2.

---

## С чего начать

Прочитать этот промт → `handoff.md` → находки внешнего аудита блока. Затем спросить владельца
ровно одно: **какую из четырёх задач берём первой** — отправка уже сделана, догонять нечего.
Моя рекомендация — задача №2.1, она закрывается одной отправкой и снимает неподтверждённость,
а дальше задача №1: она блокирует сборку, а сборка блокирует передаточный пакет (задача №5),
который владелец назвал целью 13.09.

---

## 🔴 Руководство по стилю действий на следующую сессию

Выведено из хода этого блока, читать ПЕРВЫМ и работать по нему.

1. **Прежде чем «подключить» то, что стояло заглушкой, — прочитай сторожей этого места.**
   В `feedback.rs` тест ветвился по тому самому признаку, который я собиралась изменить: пока
   стояла заглушка, он проверял честный отказ; после подстановки настоящего адреса ушёл в другую
   ветку и стал **вызывать команду, открывающую браузер** — то есть открывал бы окно на каждом
   прогоне тестов, а на сборочной машине краснел бы. Признак класса виден заранее: в тесте есть
   ветвление по тому самому условию, которое как раз и собираются менять.

2. **В зелёном прогоне читай предупреждения.** Мёртвый сторож на персональные данные нашёлся не
   проверкой и не ревью, а строчками `warning: unnameable_test_items` и `function ... is never
   used` в выводе успешного прогона. Самый дешёвый детектор, переносимый на любой язык: сверять
   ЧИСЛО выполненных тестов с числом объявленных. После правки стало 6 вместо 5 — вся разница и
   есть весь сигнал.

3. **Наблюдатель за исполнителями обязан исключать завершившихся.** Я поставила наблюдателя на
   маячки и получила ложную тревогу по двум агентам, которые честно сдали работу: маячок
   перестаёт расти, когда агент закончил. Правило записано в памяти, и я его повторила. Либо
   наблюдать только по живым, либо не наблюдать вовсе — завершения приходят сами.

4. **Буфер обмена общий с владельцем.** Набор кириллицы через управление экраном идёт через
   буфер: заполняй его сама непосредственно перед каждой вставкой и сверяй снимком ДО нажатия
   кнопки подтверждения. И следи за фокусом окна: `Ctrl+L` уходит в терминал, если браузер не в
   фокусе, — мой адрес дважды улетел в чужую строку ввода.

5. **Условия в форме ссылаются на опознаватель варианта, а не на его надпись.** Поменять тексты
   вариантов местами — значит молча перевесить ветку на другую тему. Переставлять только
   перетаскиванием. Это общий класс: всё, что «выглядит как текст», но используется как ключ.

6. **Делегируй по непересекающимся файлам и говори исполнителю, что занято.** Шесть параллельных
   потоков отработали без единого столкновения ровно потому, что в каждом задании был явный
   список «эти файлы заняты другим». Один исполнитель сам положил свой стор в отдельный файл,
   чтобы не влезть в чужую правку, — и написал об этом в отчёте. Это работает.

7. **🔴 Главный приём блока, найденный уже после того, как промт был написан: следи за
   ЗНАМЕНАТЕЛЕМ, а не за цветом.** В этом блоке два дефекта одного класса, и они в разных мирах.
   В продукте: `validator.py` считал число параметров по константе 12 вместо 13 — знаменатель
   занижен, «запас данных» на экране **выглядел лучше настоящего**, и никто бы не заметил, потому
   что число само по себе правдоподобно. В собственной приёмке: тест был вложен в соседний и не
   выполнялся — знаменатель «сколько проверок реально прошло» тихо уменьшился, а прогон остался
   зелёным. **Занижение знаменателя всегда делает картину лучше, чем она есть, и никогда не
   выглядит как ошибка.** Отсюда переносимое правило: у любой сводной величины — «всё зелено»,
   «N прошло», «запас данных 2,0» — спрашивать не «какое число», а «из чего оно поделено и не
   изменилось ли это из-за моей правки». Проверять знаменатель первым, а не последним.

---

## 🧠 Распределение моделей и нажима (effort) по фазам

| Фаза | Главный поток | Субагенты | Почему так |
|---|---|---|---|
| Разбор состояния, чтение передачи, план | `Opus · high` | — | Развилка «чинить корень или симптом» решается здесь; в этом блоке именно на разборе цепочки чтения с диска найден настоящий корень кривых отдачи |
| Задача №2.1 — доказать доставку письма | `Opus · medium` | — | Одна отправка и чтение почты; суждение простое, но цена пропуска высока: неподтверждённое уедет к клиентам |
| Задача №1 — пробы пути отказа обновления | `Opus · high` | `Sonnet · medium` — подготовка окружения проб | Блокирующее окно, клиентские формулировки; сами пробы механичны, но вывод «что увидел человек» — суждение |
| Задача №2.2–2.3 — поля и место кнопки | `Opus · medium` | `Sonnet · medium` | Правка по готовому диагнозу; тонкое место — смена подписи команды под живым сторожем |
| Задача №2.4 — пять продуктов | `Sonnet · low` | `Sonnet · low` | Тупой перенос пяти строк в пять деревьев, развилок нет |
| Задача №3 — макроряды | `Opus · medium` | `Sonnet · medium` | Механика загрузки; опасное место (выдумать числа) закрывается запретом в задании, а не нажимом |
| Задача №4 — планирование в отчётности | `Opus · high` | `Sonnet · medium` | Состав раздела — содержательное решение, согласуется с владельцем; сборка раздела механична |
| Задача №5 — передаточный пакет | `Opus · high` | `Sonnet · medium` — сборка демо-данных и шаблонов | Клиентские тексты, которые читает покупатель и его безопасность; формулировка про сертификат и санкции — место, где неточное слово стоит сделки |
| Живые проверки непроверенного | `Opus · high` | `Opus · high` | Измерение живого состояния и суждение о том, что видно на экране; исполнитель на меньшем нажиме в прошлом блоке оборвался, не дойдя до вывода |
| Клиентские тексты | `Opus · medium` | `Sonnet · medium` | Текст пишется быстро, принимается лично: канон тире и англицизмов агенты нарушают |
| Внешний аудит блока | — | `Opus · high` | Канон: аудитор не ниже модели сессии; дифф трогает персональные данные и честность метрик |
| Триаж находок аудита | `Opus · high` | `Sonnet · medium` — сборка репро | Опровержение находки требует зонда, а не рассуждения; тёплая сессия мотивирована опровергать |
| Завершение сессии (память, передача, записи) | `Opus · medium` | — | Механика с высокой ценой пропуска: незакоммиченная память теряется молча |

---

## 🔴 ПРОТОКОЛ ОСТАНОВОК ДЛЯ СМЕНЫ МОДЕЛИ

**Граница ответственности.** Остановки касаются ТОЛЬКО главного потока: его модель и нажим меняет
исключительно владелец вручную (`/model`). **Модель и нажим СУБАГЕНТОВ оркестратор назначает САМ,
на каждый запуск, без остановок и без спроса** — задавать явно параметрами, иначе субагент
унаследует умолчание и шаг тихо подорожает либо просядет. Спрашивать разрешение на выбор модели
субагента — потеря хода; ответственность за этот выбор целиком на оркестраторе.

**Стартовая модель: `Opus · high`.**

| № | Что закончено (наблюдаемый признак) | Переключить на | Причина |
|---|---|---|---|
| 1 | Решение владельца об отправке получено, исправленное письмо обратной связи доехало | `Opus · high` | дальше пробы обновления: клиентский текст в блокирующем окне |
| 2 | Три пробы прогнаны, наблюдения записаны | `Opus · medium` | правки полей обратной связи по готовому диагнозу |
| 3 | Поля и место кнопки сделаны, гейты зелёные | `Sonnet · low` | перенос пяти строк в пять продуктов, развилок нет |
| 4 | Пять продуктов подключены | `Opus · medium` | макроряды: механика с запретом выдумывать числа |
| 5 | Макроряды выгружены либо признаны недоступными | `Opus · high` | состав раздела планирования в отчётности — содержательное решение |
| 6 | Все правки блока внесены, гейты зелёные | `Opus · high` | триаж находок аудита требует зондов, не рассуждений |
| 7 | Установщик собран и проверен живой установкой | `Opus · high` | передаточный пакет: клиентские тексты для покупателя и его безопасности |
| 8 | Перед завершением сессии | `Opus · medium` | память и передача: механика с высокой ценой пропуска |

**Шаблон предупреждения** (короткий и одинаковый):

> Закончила <что>. Дальше <что делаем> — для этого нужен другой нажим.
> Переключи, пожалуйста: `/model <модель> <effort>`. Причина: <одна фраза>.
> Скажи, когда переключишь, — продолжу.

**Пока владелец не ответил:** ждать, не переключаясь самой и не заходя в следующий этап. Но не
простаивать: делать работу текущего уровня — записи в git, обновление памяти, подготовка входов и
заданий субагентам, чтение материалов следующего этапа.

**В автономном режиме остановок НЕТ ВООБЩЕ**, владельца не дёргать ни на одной точке. Главный
поток переключить технически невозможно (`/model` доступна только владельцу) → работать на
стартовом нажиме, а разрыв компенсировать субагентами: этап требует нажима выше текущего — тонкую
часть (вывод о причине, аудит логики, проектирование критерия) вынести субагенту с нужным уровнем,
себе оставить приёмку; ниже текущего — массовую механику отдать дешёвому субагенту, чтобы не жечь
дорогой нажим на переносе файлов. В финальном отчёте перечислить точки, где переключение главного
потока было бы уместно, и что из этого компенсировано субагентами.

**Внеплановая эскалация:** всплыла необратимая развилка, критическая находка аудита или класс
дефекта вместо единичного → остановиться немедленно, не дожидаясь границы фазы, и просить поднять
нажим.
