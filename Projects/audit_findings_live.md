## Аудит блока доставки 2026-07-26 (c8f1d91..a851001) — внешний аудитор, чистый контекст

Формат: severity | file:line | суть | сценарий

High | src-tauri/src/lib.rs:557-570 + commands/content_updater.rs:504-552 | Инвариант «в сообщение об ошибке всегда входит число полученных байт» на главном пути не выполняется: download_updates глотает ошибку файла (error!) и возвращает Ok(vec![]); open_cabinet попадает в ветку `Ok(_)` и подставляет «сервер не отдал файл кабинета» — ни кода HTTP, ни байт, ни причины | /content отвечает 403 «нет лицензии» ИЛИ поток рвётся на 12 КБ из 34 КБ → пользователь и поддержка видят один и тот же текст без диагностики; настоящая причина только в журнале
High | src-tauri/src/commands/content_updater.rs:323-352, 504-547 (+ lib.rs:93,114,556 — checksums пустые всегда) | Усечённое тело, пришедшее БЕЗ Content-Length и завершённое штатно, принимается за полный файл, шифруется, пишется на диск И фиксируется версия кабинета → состояние необратимо: open_cabinet качает только при !exists, get_cabinets — только missing/undecryptable (AES расшифровывается), check_update_per_cabinet видит local==server | Посредник/HTTP-2 END_STREAM обрывает chunked-ответ /content (по handoff §4.2 длина не приходит) → обрезанный tar.gz на диске → кабинет не открывается НИКОГДА, повторные запуски не перекачивают; лечится только ручным импортом или удалением файла
High | src-tauri/src/commands/updater.rs:298-338 | Полный `.part`, не переведённый в финальный файл, навсегда ломает канал обновлений: Range с first-byte-pos == длине даёт 416, 416 не is_success → Err без удаления `.part`, и так все 5 попыток и на каждом следующем запуске | rename и copy-фолбэк оба провалились (файл занят антивирусом — код это прямо допускает) ИЛИ процесс убит между flush и rename → обновления мертвы до ручной чистки %TEMP%\aurora-update
Medium | src-tauri/src/commands/content_updater.rs:325-327 | `Vec::with_capacity(n)` резервирует память по НЕПРОВЕРЕННОМУ Content-Length | Инспектирующий TLS посредник (ровно среда клиента) или страница ошибки объявляет Content-Length ~10^12 → аварийное завершение процесса при отказе аллокации, без сообщения; повторяется 5 раз
Medium | src-tauri/src/commands/content_updater.rs:302-306, 404-428 | Верхней границы времени нет вообще: read_timeout = тишина сокета, общий дедлайн удалён и ничем не заменён | Посредник отдаёт по байту раз в 25 с → попытка живёт бесконечно, open_cabinet/get_cabinets ждут без предела (UI без ответа); даже без злого умысла 5×30 с + 37 с пауз ≈ 3 мин на файл последовательно по всем кабинетам
Medium | src-tauri/src/commands/online_auth.rs:305-329 | Повтор авторизации не покрывает 5xx: любой ответ С ТЕЛОМ выходит из цикла, затем падает разбор JSON — инвариант «5xx повторяем» на авторизации не выполнен | Шлюз Supabase отдаёт 503 HTML → check_auth → Err → кэша нет (первый запуск / TTL 7 суток истёк) → «программа не запускается», хотя повтор через 2 с прошёл бы
Medium | src-tauri/src/commands/content_updater.rs:371-378 | 401/403 отнесены к фатальным, хотя тем же блоком доказано (updater.rs:21-30), что шлюз Supabase отвечает 401 UNAUTHORIZED_NO_AUTH_HEADER ДО входа в функцию; /content и /frontend-bundle по-прежнему вызываются без apikey/Authorization | Включение verify_jwt на функции /content (или WAF/лимит, отвечающий 403) → мгновенный отказ без повторов + текст «связь обрывается»; лечится только правкой на сервере
Medium | src/routes/settings/+page.svelte:777 + 225-233 | Кнопка запасного пути скрыта именно у пострадавших: секция под `{#if vaultStatus.length > 0}`, а vaultStatus — пересечение с get_cabinets, который требует успешной авторизации/кэша/Ed25519-лицензии | Канал режет и /auth (потому и добавлены повторы), кэшу больше 7 суток (у клиента две недели), офлайн-лицензии нет → get_cabinets Err → vaultStatus=[] → секции нет → импортировать материалы из файла невозможно
Medium | src-tauri/src/lib.rs:841-915 (import_cabinet_vault) | Нет проверки подлинности: принимается любой gzip-tar и молча ПЕРЕЗАПИСЫВАЕТ материалы существующего кабинета; Ed25519-подпись контента в проекте есть (crypto::content_sig::verify_manifest), здесь не применена | Клиенту, которому официально велено «запросить файл в поддержке», приходит письмо от «поддержки» с econometrist.vault → импорт заменяет кабинет, содержимое распаковывается в workspace и становится инструкциями кабинета для Claude
Medium | src-tauri/src/lib.rs:953-961 | Текст отказа безусловно называет причиной обрыв связи, хотя ветка else срабатывает и при недоступном сервере лицензий, и при 403 «нет лицензии» | online.status == "error"/"expired"/"offline" → загрузка даже не запускалась, а клиент читает «связь с сервером обрывается при передаче файла» и уводит поддержку в диагностику сети
Medium | src-tauri/src/commands/content_updater.rs:874-1010 (тесты) | Все четыре теста устойчивости объявляют Content-Length, то есть доказывают ТОЛЬКО защищённый случай; продовый /content длину не отдаёт (handoff §4.2) — главный риск не покрыт. `truncated_body_is_not_accepted_as_success` вдобавок реально спит 2+5+10+20 = 37 с | Регрессия «принят обрезанный файл без Content-Length» пройдёт зелёным набором тестов

---

# Живые находки аудита — блок thin (крейт aurora_gateway + feature `thin`)

Формат: severity | file:line | суть | сценарий

---

Medium | gateway_executor.rs:283-292 (generate_session_label) | Монотонный счётчик COUNTER полностью обрезается маской и НЕ влияет на метку. `(counter as u64) << 32` кладёт counter в биты [32,63], а `mixed & 0xFFFF_FFFF` берёт только биты [0,31] → counter вклад = 0. Уникальность метки держится ИСКЛЮЧИТЕЛЬНО на изменении младших 32 бит наносекунд (pid константа в процессе). Заявленный инвариант «наносекунды + монотонный счётчик + PID» не выполняется — счётчик мёртв. | Сценарий A (флаки-тест): `session_label_is_unique_across_calls` — два вызова подряд; если системный таймер не тикнул между ними (совпали младшие 32 бита nanos), метки идентичны → assert_ne падает в CI. Сценарий B (коллизия сессий): два «новых диалога» одного кабинета за интервал < разрешения таймера → одинаковая метка → сервер по sticky-маршрутизации продолжит СТАРУЮ серверную сессию вместо новой (переиспользование контекста). Фикс: включить counter в младшие биты, напр. `mixed ^= counter as u64` до маски, или брать hex от полного u64.

Medium | gateway_executor.rs:336-373 (execute) + claude.rs:466-467 (CLI-эталон) | Нет клиентского таймаута на `spawn_blocking(send_to_gateway)`. CLI-путь оборачивает стрим в `tokio::time::timeout(30 мин)` + taskkill; gateway полагается ТОЛЬКО на SSH ConnectTimeout=10 / ServerAlive 15×4=60с. SSH keepalive держит канал, пока жив ssh-демон сервера, НЕ forced-command. При этом init эмитится безусловно ДО вызова и снимает safety-таймер ChatPanel (465). | Сервер жив (ssh-демон отвечает на keepalive), но forced-command/движок завис в poll не отдавая stdout → ServerAlive не рвёт соединение, send_to_gateway висит без верхней границы, а safety-таймер уже снят init'ом → вечный spinner (isLoading=true), UI восстанавливается только перезапуском. CLI-путь от этого защищён 30-мин потолком. Фикс: обернуть spawn_blocking в tokio::time::timeout с потолком (≈ серверный poll-лимит), при истечении — [TC-GW-TIMEOUT].

Medium | lib.rs:1166-1184 (send_message retry-loop, ветка resume-fallback) | Ветка `Err(e) if use_resume.is_some() && attempt == 0` безусловна по типу ошибки (не проверяет класс). Унаследована от CLI, где означала «--resume отклонён». Для gateway ЛЮБАЯ ошибка при продолжении диалога (TIMEOUT/SRV/SSH) на первой попытке трактуется как resume-failure. | Пользователь ведёт диалог (session label в session_manager), сервер временно недоступен → (1) ложное системное сообщение «Контекст диалога сброшен. Начинаю новую сессию.» (ничего не сбрасывалось); (2) вторая попытка с resume=None → generate НОВЫЙ label → при транзиентном сбое (сервер ожил на 2-й попытке) стартует НОВАЯ серверная сессия, старый контекст осиротает, хотя он был жив; (3) лишний повторный вызов сервера. Фикс: в thin-ветке не заворачивать gateway-ошибки в resume-fallback (проверять, что ошибка — именно отказ resume, а не транспорт/таймаут).

Medium (латентный) | online_auth.rs:308-310 (R-04) ↔ local_mode.rs:84-113 (decide_from_online) | R-04 возвращает Err(HttpStatus) на ЛЮБОЙ не-2xx ДО чтения тела. authorize() маппит Err→"cached"(при свежем кэше)/"offline". decide_from_online: "cached"→allowed=true, "offline"→None→Ed25519-fallback. Значит серверный «blocked» доходит как Denied ТОЛЬКО если сервер отдаёт его с HTTP 2xx+телом. Если blocked приходит с 4xx/5xx — R-04 превращает его в cached/offline → блокировка обходится. До R-04 (4xx+{status:blocked}) давало Denied — это регресс для blocked-через-не-2xx. | Сервер отзывает лицензию клиента, отвечает HTTP 403+{"status":"blocked"} → local_mode: свежий кэш (<24ч) → allowed, ИЛИ нет кэша → Ed25519 с валидной подписанной лицензией → allowed. Обход отзыва. СМЯГЧЕНИЕ: (а) verify_local_mode НИГДЕ не вызывается (grep по обоим worktree) — код не на пути исполнения, латентно до подключения локального режима; (б) безопасно, если серверный контракт /auth ВСЕГДА отдаёт вердикт через HTTP 200 (blocked в теле). Проверить серверный контракт до подключения local mode; если 200 — не проблема, если 4xx — дыра. Эскалация до High при выполнении обоих условий (local mode подключён И blocked через не-2xx).

— Проверено и НЕ находка: default-сборка (весь thin за cfg, рефакторинг auto_save_response эквивалентен старому inline по всем 4 веткам suppress/empty); UI-зависание при терминальной gateway-ошибке (основной invoke('send_message') обёрнут в try/catch, ChatPanel.svelte:778-785 → снимает safety-таймер + сообщение + isLoading=false); инвариант недостижимости CLI при thin (run_claude_inner/find_claude_binary/isolated_claude_config_dir приватны, вызовы только за cfg(not thin)); безопасность транспорта (промпт через stdin не argv; node с префиксом user@ гасит option-injection; StrictHostKeyChecking=yes+CA; stderr дренаж в потоке против deadlock; MAX_RESPONSE_BYTES против OOM); invoke импортирован в settings.svelte:2 (не missing).

## ТРИАЖ (рабочая сессия, 2026-07-20)
- #1 counter мёртв — ПОДТВЕРЖДЕНО (XOR со сдвигом <<32 не задевает биты [0,31] под маской) → ИСПРАВЛЕНО: counter замешан и в младшие биты (gateway_executor.rs::generate_session_label).
- #2 нет клиентского таймаута — ПОДТВЕРЖДЕНО (ServerAlive держит канал при живом sshd независимо от forced-command; init уже снял safety-таймер UI) → ИСПРАВЛЕНО: tokio::time::timeout 1830с вокруг spawn_blocking → [TC-GW-TIMEOUT]; жёсткий kill осиротевшего потока — фаза 2.
- #3 ложный resume-fallback — ПОДТВЕРЖДЕНО (guard lib.rs:1166 безусловен по типу ошибки) → ИСПРАВЛЕНО: guard исключает префикс "[TC-GW-" (gateway-ошибки падают в честную ошибку, label/контекст сохраняются; CLI-путь не задет — его ошибки без такого префикса).
- #4 R-04↔local_mode обход blocked через не-2xx — ПОДТВЕРЖДЕНО КАК ЛАТЕНТНЫЙ (verify_local_mode не вызывается ни в одном продукте прода; пилот Econometrica local mode не использует). НЕ исправлено в блоке: правка семантики общего крейта aurora_fleet шире блока + требует факта серверного контракта /auth. → БЭКЛОГ с жёстким триггером «ДО подключения local mode»: (а) проверить, отдаёт ли auth Edge Function blocked всегда с HTTP 2xx; (б) если нет — научить R-04 читать тело при не-2xx и различать вердикт-в-теле от транспортного сбоя. Оценка: 1–2 ч + прогон fleet 53. Вписано в NEXT_SESSION_thin_module_port.md.
