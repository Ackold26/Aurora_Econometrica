# Внешний аудит блока 31.08–01.09.2026 — живые находки

Формат: `severity | файл:строка | суть | сценарий отказа`

High | sidecar/econometrica/server.py:1080-1092 | ветвь `no_history` не обработана: при полностью пустом KPI detect_media_plan_tail отдаёт `{found:False,error:'no_history'}`, обрезки нет, n_obs = все строки файла | файл 104 строки медиаплана без продаж (или пользователь указал не тот столбец KPI) → n_obs=104 → overall_tier='reliable' при НУЛЕ наблюдений; prior_predictive гоняется по y=нулям (df[kpi].fillna(0), server.py:1120)
High | sidecar/econometrica/server.py:1080-1092 | ветвь `internal_gaps` не обработана: пропуск KPI в середине истории + хвост плана → found=False → хвост снова считается наблюдениями | 15 недель истории, одна неделя без продаж + 20 строк плана → n_obs=35 → overall_tier='reliable', реальных наблюдений 14; ровно тот дефект, который блок объявляет закрытым
Medium | sidecar/econometrica/server.py:1085 | обрезается не только n_obs, а сам df — меняется вход quick_proxy (media_matrix, :1112) и prior_predictive (y_obs, :1120); расхождение с инвариантом №2 handoff «меняем основание вердикта, а не вердикт» | 104 истории + 26 строк ровного плана: до правки коллинеарность считалась по 130 строкам, после — по 104 → VIF выше → tier reliable→directional при неизменных порогах; тесты блока этот путь не покрывают
Medium | src-tauri/src/commands/claude.rs:960-964 | Rust парсит "NaN"/"inf"/"Infinity" как f64 → col_sum нефинитен → set_result("NaN") кладёт нечисловой литерал в `<v>` кэша формулы; до правки там был 0 и файл открывался | markdown-таблица из ответа кабинета со столбцом, где пропуск напечатан как `NaN` (типовой вывод pandas), ≥3 строк данных → xlsx с `<f>SUM(B2:B4)</f><v>NaN</v>` → Excel предлагает восстановить файл, сторонний читатель получает NaN
Medium | src-tauri/src/commands/report.rs:1621-1623 | при total_value==0 в кэш пишется 0, а формула =B{r}/B${t} при пересчёте даёт #DIV/0! — кэш и формула расходятся (нарушен инвариант №5 handoff) | waterfall с взаимно гасящимися вкладами (TV +500, прочее −500) или все нули → не-Excel читатель видит 0, Excel показывает ошибку деления
Medium | src-tauri/src/metrics/audit.rs:48 | замок только внутриПРОЦЕССНЫЙ, плагина единственного экземпляра в src-tauri нет (grep single_instance = 0), а `writeln!` по File без буфера выдаёт несколько системных вызовов на одну строку → чередование строк между процессами реально | две копии одного продукта у одного пользователя, обе проверяют лицензию → в audit.log строка с 5-7 табами вместо 4; дешёвая страховка — собрать строку в String и один write_all
Medium | src-tauri/src/metrics/audit.rs:97,117 | тест берёт срез `&content[before_len..]` по байтовой длине, снятой ДО прогона; ротация внутри самого теста (rotate_if_large, 5 МБ) укорачивает файл → паника «byte index out of bounds», а не отказ проверки; `File::open().unwrap()` паникует и когда app_state_dir не смог создать каталог (append_to_file молча вернул Ok) | audit.log в fallback-пути дорос до 5 МБ → `cargo test` детерминированно падает на этом тесте не по существу
NO-FIND | src-tauri/src/metrics/audit.rs:44-69 | вложенных вызовов и взаимных блокировок нет: durable_store не зовёт log_event (проверено grep), область замка = ровно файловая часть, AUDIT_LOG на диск не удерживается, отравление снято into_inner, best-effort сохранён | —
NO-FIND | src/lib/components/pipeline/DecomposeStep.svelte:409-417 | охранное условие совпадает с формой backend: без паспорта `applicable` = None (repro_tolerance.py:710-719) → блок не рисуется; при паспорте есть title + tolerances.roi/contribution_pct | —
OPEN | src/lib/components/pipeline/DecomposeStep.svelte:440-441 + repro_tolerance.py:217-225 | у MODE_EXACT все допуски 0.0; если applicable_mode (repro_tolerance.py:359) способен вернуть MODE_EXACT, карточка скажет «расхождение до 0 %» под заголовком «если расчёт повторит независимый специалист» — точное совпадение бывает только при перепрогоне с тем же зерном (SELF_RERUN_NOTE) | не закрыто из-за бюджета обращений: один grep по applicable_mode закрывает вопрос
