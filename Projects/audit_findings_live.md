# Живой журнал находок внешнего аудита — блок 2.4.10

Формат строки: `severity | файл:строка | суть | сценарий отказа`

## Находки

High | src-tauri/src/econ_sidecar.rs:264-272 (expected_engine_image_path) + sidecar_runtime.rs:~317-327 (ветка `_ =>` в should_kill_port_holder) | В РЕЛИЗНОЙ сборке `resolve_bundled_exe()` может вернуть `None` (образ движка изъят антивирусом, повреждённая установка, ресурс не распакован) — тогда `expected_image_path = None`, и решение молча откатывается на сверку ПО ИМЕНИ образа. Имя у обеих редакций одинаково; собственный тест `skip_engine_of_other_product_edition` прямо утверждает `image_matches(Some(OTHER_EDITION_IMAGE), &KILL_CFG) == true`. То есть в этом откате возвращается ровно High-1 прошлого аудита. | Обе редакции стоят на машине (штатный сценарий по CLAUDE.md), `legacy_port` у них общий (7430). Локальная редакция считает MCMC. Kaspersky кладёт `econometrica-sidecar.exe` облачной редакции в карантин (10.08 он уже снимал оболочку этого продукта). Пользователь запускает облачную → `start_sidecar` → рукопожатие с чужим движком не проходит → `kill_port_holder(7430)` → `resolve_bundled_exe()` = None → сверка по имени → `econometrica-sidecar` совпадает → расчёт локальной редакции снят молча. Лечение: различать «отладочная сборка» (откат допустим) и «релиз, путь установки не разрешён» (обязан быть `Skip(ImageUnknown)`), например передавать `expected_image_path: Result<Option<&str>, ()>` либо отдельный признак `image_check_may_fall_back`.

Medium | src-tauri/src/econ_sidecar.rs:508+ (start_sidecar → kill_port_holder) | Зависший движок, который БОЛЬШЕ НЕ СЛУШАЕТ порт, теперь не снимается никогда: `listening_port_owners` вернёт пусто → `NoListener` → выход. Прежний путь по файлу состояния снимал его по записанному номеру. Handoff §2 утверждает «незакрытый зомби максимум удержит порт» — на деле он не держит ничего и просто копится. | Движок падает в потоке HTTP-сервера (слушающий сокет закрыт, процесс жив с загруженной моделью в памяти). Перезапуск приложения: `kill_port_holder` → «порт никто не слушает» → процесс остаётся. Каждый следующий запуск добавляет ещё один осиротевший процесс с сотнями МБ. Лечение: оставить снятие по записи файла состояния ВТОРЫМ, строго подчинённым путём — только когда `listening_port_owners(port)` пуст И записанный процесс проходит полную сверку образа с путём установки.

Medium | src-tauri/src/commands/campaign.rs:90-122 (copy_brief_files) | Список сохранённых по-прежнему лжёт при совпадении имён: два разных исходных пути с одинаковым именем файла дают ОДИН файл на диске и ДВЕ записи в `saved`; первый файл затирается вторым без следа в `failed`. Заявленный инвариант функции — «закрывается ровно ложь списка — и она закрывается полностью». | Пользователь выбирает в окне брифа `C:\Проект\бриф.docx` и `C:\Архив\бриф.docx`. `campaign.brief_files` = ["бриф.docx","бриф.docx"], в `brief-files/` один файл — содержимое первого недоступно. Лечение: при совпадении имени либо `unique_export_path`-переименование, либо запись в `failed` с причиной «имя уже занято».

Medium | src-tauri/tests/guard_cpd88_feedback_category_map.rs:29-49 (extract_settings_category_values) | Разбор принимает ЧАСТИЧНЫЙ результат за полный. Проверка `!ui_values.is_empty()` ловит только нулевой разбор. Разбиение строго по литералу `<option value="` пропускает любую иную форму записи (`<option class="x" value="…">`, `<option selected value="…">`, одинарные кавычки), и пропущенная тема просто не проверяется. | Разработчик добавляет `<option class="fb-option" value="billing">Оплата</option>` и забывает пару в `CATEGORY_MAP`. Первый тест: значение не извлечено — зелёный. Второй тест (сироты): пары в карте нет — зелёный. Форма отвечает 400 на тему «Оплата», человек видит FB-001 — регресс CPD-88 при зелёных сторожах. Лечение: посчитать вхождения `<option` в блоке и потребовать равенства с числом извлечённых значений.

Medium | src-tauri/src/errors.rs:1-4 | Полная замена на `pub use aurora_core::errors::*;` — стык с крейтом НИЧЕМ не проверяется на стороне продукта: юнит-тесты (`error_code_format`, `all_codes_unique`) уехали в крейт, а единственный новый сторож линкуется с `feedback::form_category`, не с `errors`. Handoff §3.6 заявляет, что стык закрыт сторожем — для `errors` это не так. | В `aurora_core` меняют `coded()` на формат `"LI-005: detail"` либо переименовывают вариант с сохранением сигнатуры; продукт собирается без единой правки, а строки ошибок у клиента расходятся с `help/error-codes.html` и `5_Документация/ERROR_CODES.md`. Лечение: оставить в продукте один приёмочный тест на формат (`coded(ErrorCode::LI005, "x") == "[LI-005] x"`) и на присутствие CL010/AU005.

Low | src-tauri/src/sidecar_runtime.rs:~1512-1548 (collect_v4/collect_v6) | `buf.as_ptr() as *const Table` при `Vec<u8>` (выравнивание 1) — обращение по невыровненному указателю формально UB; практически системный распределитель Windows отдаёт 16-байтовое выравнивание, воспроизводимого отказа нет. Разбор гибкого массива и границы буфера при этом посчитаны верно (`header_len` = 4 для обеих таблиц, `safe_entry_count` ограничивает по фактической длине), переполнения при разборе порта нет — `u16::from_be((raw & 0xFFFF) as u16)` корректно. | Сценария отказа нет; правка дешёвая — `std::ptr::read_unaligned` для заголовка и строк.

Low | src-tauri/src/sidecar_runtime.rs:1449-1490 (tcp_table_raw) + 1373 (listening_port_owners) | Отказ системного вызова неотличим от «порт свободен»: обе ситуации дают пустой список и причину `NoListener` («порт никто не слушает»), а сам отказ пишется только в `debug!`. Механизм снятия зомби вживую ни разу не проверялся (handoff §4.1), поэтому среда, где IpHelper недоступен, выключит его целиком незаметно. | На машине с ограничением доступа к IpHelper (политика, песочница) снятие зомби перестаёт работать; в журнале релизной сборки — только «порт никто не слушает». Лечение: различать `None` (спросить не удалось, уровень `warn!`) и `Some(vec![])` (порт свободен).

Low | src/lib/audio.js:31-41 + src/routes/settings/+page.svelte:20 | Заявлено «вызывается один раз при запуске», фактически `forgetAudioPreference()` стоит в теле компонента настроек — выполняется при каждом открытии настроек и НИ РАЗУ у пользователя, который настройки не открывает. | Клиент, включавший звук до 15.08 и не заходящий в настройки, сохраняет `ai-agency-audio-enabled=true` навсегда — ровно то, что правка обещала устранить. Лечение: перенести вызов в `+layout.svelte`.

Low | src/routes/workflow/[id]/+page.svelte:147-155 и 332-347 | Обработчик `node-status` продублирован в двух местах файла, и предупреждение добавлено в оба. Если оба слушателя одного события `workflow-execution-{exec_id}` окажутся зарегистрированы одновременно, человек получит два одинаковых всплывающих сообщения на один шаг. | Проверить не удалось: в diff не видно, взаимоисключающи ли регистрации (запуск против переподключения). Лечение: вынести разбор события в одну функцию.

Low | src-tauri/src/session/manager.rs:129-140 | При `sessions_root.parent() == None` починка прав молча не выполняется вовсе — раньше ветка входила по одному лишь `LOCALAPPDATA`. Ни строки в журнал. | Крайний случай (корень тома), практически недостижим; замечание о диагностируемости, не о поведении.

## Проверено и находкой НЕ является

- `APP_HANDLE.set()` стоит ПЕРВОЙ строкой `start_sidecar` (econ_sidecar.rs:509) — путь «глобальный дескриптор ещё не выставлен → откат на имя» закрыт; остаётся только отказ `resolve_bundled_exe` (см. High).
- `scan_file` (guard_no_regressed_cpd77_cpd79.rs:227-229) падает `panic!` на нечитаемом файле — молчаливого пропуска файла нет.
- Разбор порта, границы буфера, `header_len`, дедупликация держателей, переспрос после удержания дескриптора, освобождение дескриптора (`Drop` у `HeldProcess`, ветка `Kill` перепроверяет `held`) — дефектов не найдено.
