## Аудит блока доставки 2026-07-26 (c8f1d91..a851001) — внешний аудитор, чистый контекст

Формат: severity | file:line | суть | сценарий

High | src-tauri/src/lib.rs:557-570 + commands/content_updater.rs:504-552 | Инвариант «в сообщение об ошибке всегда входит число полученных байт» на главном пути не выполняется: download_updates глотает ошибку файла (error!) и возвращает Ok(vec![]); open_cabinet попадает в ветку `Ok(_)` и подставляет «сервер не отдал файл кабинета» — ни кода HTTP, ни байт, ни причины | /content отвечает 403 «нет лицензии» ИЛИ поток рвётся на 12 КБ из 34 КБ → пользователь и поддержка видят один и тот же текст без диагностики; настоящая причина только в журнале
High | src-tauri/src/commands/content_updater.rs:323-352, 504-547 (+ lib.rs:93,114,556 — checksums пустые всегда) | Усечённое тело, пришедшее БЕЗ Content-Length и завершённое штатно, принимается за полный файл, шифруется, пишется на диск И фиксируется версия кабинета → состояние необратимо: open_cabinet качает только при !exists, get_cabinets — только missing/undecryptable (AES расшифровывается), check_update_per_cabinet видит local==server | Посредник/HTTP-2 END_STREAM обрывает chunked-ответ /content (по handoff §4.2 длина не приходит) → обрезанный tar.gz на диске → кабинет не открывается НИКОГДА, повторные запуски не перекачивают; лечится только ручным импортом или удалением файла
High | src-tauri/src/commands/updater.rs:298-338 | Полный `.part`, не переведённый в финальный файл, навсегда ломает канал обновлений: Range с first-byte-pos == длине даёт 416, 416 не is_success → Err без удаления `.part`, и так все 5 попыток и на каждом следующем запуске | rename и copy-фолбэк оба провалились (файл занят антивирусом — код это прямо допускает) ИЛИ процесс убит между flush и rename → обновления мертвы до ручной чистки %TEMP%\aurora-update
Medium | src-tauri/src/commands/content_updater.rs:325-327 | `Vec::with_capacity(n)` резервирует память по НЕПРОВЕРЕННОМУ Content-Length | Инспектирующий TLS посредник (ровно среда клиента) или страница ошибки объявляет Content-Length ~10^12 → аварийное завершение процесса при отказе аллокации, без сообщения; повторяется 5 раз
Medium | src-tauri/src/commands/content_updater.rs:302-306, 404-428 | Верхней границы времени нет вообще: read_timeout = тишина сокета, общий дедлайн удалён и ничем не заменён | Посредник отдаёт по байту раз в 25 с → попытка живёт бесконечно, open_cabinet/get_cabinets ждут без предела (UI без ответа); даже без злого умысла 5×30 с + 37 с пауз ≈ 3 мин на файл последовательно по всем кабинетам
Medium | src-tauri/src/commands/online_auth.rs:305-329 | Повтор авторизации не покрывает 5xx: любой ответ С ТЕЛОМ выходит из цикла, затем падает разбор JSON — инвариант «5xx повторяем» на авторизации не выполнен | Шлюз Supabase отдаёт 503 HTML → check_auth → Err → кэша нет (первый запуск / TTL 7 суток истёк) → «программа не запускается», хотя повтор через 2 с прошёл бы
Medium | src-tauri/src/commands/content_updater.rs:371-378 | 401/403 отнесены к фатальным, хотя тем же блоком доказано (updater.rs:21-30), что шлюз Supabase отвечает 401 UNAUTHORIZED_NO_AUTH_HEADER ДО входа в функцию; /content и /frontend-bundle по-прежнему вызываются без apikey/Authorization | Включение verify_jwt на функции /content (или WAF/лимит, отвечающий 403) → мгновенный отказ без повторов + текст «связь обрывается»; лечится только правкой на сервере
Medium | src/routes/settings/+page.svelte:777 + 225-233 | Кнопка запасного пути скрыта именно у пострадавших: секция под `{#if vaultStatus.length > 0}`, а vaultStatus — пересечение с get_cabinets, который требует успешной авторизации/кэша/Ed25519-лицензии | Канал режет и /auth (потому и добавлены повторы), кэшу больше 7 суток (у клиента две недели), офлайн-лицензии нет → get_cabinets Err → vaultStatus=[] → секции нет → импортировать материалы из файла невозможно
Medium | src-tauri/src/lib.rs:841-915 (import_cabinet_vault) | Нет проверки подлинности: принимается любой gzip-tar и молча ПЕРЕЗАПИСЫВАЕТ материалы существующего кабинета; Ed25519-подпись контента в проекте есть (crypto::content_sig::verify_manifest), здесь не применена | Клиенту, которому официально велено «запросить файл в поддержке», приходит письмо от «поддержки» с econometrist.vault → импорт заменяет кабинет, содержимое распаковывается в workspace и становится инструкциями кабинета для Claude
Medium | src-tauri/src/lib.rs:953-961 | Текст отказа безусловно называет причиной обрыв связи, хотя ветка else срабатывает и при недоступном сервере лицензий, и при 403 «нет лицензии» | online.status == "error"/"expired"/"offline" → загрузка даже не запускалась, а клиент читает «связь с сервером обрывается при передаче файла» и уводит поддержку в диагностику сети
Medium | src-tauri/src/commands/content_updater.rs:874-1010 (тесты) | Все четыре теста устойчивости объявляют Content-Length, то есть доказывают ТОЛЬКО защищённый случай; продовый /content длину не отдаёт (handoff §4.2) — главный риск не покрыт. `truncated_body_is_not_accepted_as_success` вдобавок реально спит 2+5+10+20 = 37 с | Регрессия «принят обрезанный файл без Content-Length» пройдёт зелёным набором тестов

---

# Живые находки аудита — блок thin (крейт aurora_gateway + feature `thin`)

Формат: severity | file:line | суть | сценарий

---

Medium | gateway_executor.rs:283-292 (generate_session_label) | Монотонный счётчик COUNTER полностью обрезается маской и НЕ влияет на метку. `(counter as u64) << 32` кладёт counter в биты [32,63], а `mixed & 0xFFFF_FFFF` берёт только биты [0,31] → counter вклад = 0. Уникальность метки держится ИСКЛЮЧИТЕЛЬНО на изменении младших 32 бит наносекунд (pid константа в процессе). Заявленный инвариант «наносекунды + монотонный счётчик + PID» не выполняется — счётчик мёртв. | Сценарий A (флаки-тест): `session_label_is_unique_across_calls` — два вызова подряд; если системный таймер не тикнул между ними (совпали младшие 32 бита nanos), метки идентичны → assert_ne падает в CI. Сценарий B (коллизия сессий): два «новых диалога» одного кабинета за интервал < разрешения таймера → одинаковая метка → сервер по sticky-маршрутизации продолжит СТАРУЮ серверную сессию вместо новой (переиспользование контекста). Фикс: включить counter в младшие биты, напр. `mixed ^= counter as u64` до маски, или брать hex от полного u64.

Medium | gateway_executor.rs:336-373 (execute) + claude.rs:466-467 (CLI-эталон) | Нет клиентского таймаута на `spawn_blocking(send_to_gateway)`. CLI-путь оборачивает стрим в `tokio::time::timeout(30 мин)` + taskkill; gateway полагается ТОЛЬКО на SSH ConnectTimeout=10 / ServerAlive 15×4=60с. SSH keepalive держит канал, пока жив ssh-демон сервера, НЕ forced-command. При этом init эмитится безусловно ДО вызова и снимает safety-таймер ChatPanel (465). | Сервер жив (ssh-демон отвечает на keepalive), но forced-command/движок завис в poll не отдавая stdout → ServerAlive не рвёт соединение, send_to_gateway висит без верхней границы, а safety-таймер уже снят init'ом → вечный spinner (isLoading=true), UI восстанавливается только перезапуском. CLI-путь от этого защищён 30-мин потолком. Фикс: обернуть spawn_blocking в tokio::time::timeout с потолком (≈ серверный poll-лимит), при истечении — [TC-GW-TIMEOUT].

Medium | lib.rs:1166-1184 (send_message retry-loop, ветка resume-fallback) | Ветка `Err(e) if use_resume.is_some() && attempt == 0` безусловна по типу ошибки (не проверяет класс). Унаследована от CLI, где означала «--resume отклонён». Для gateway ЛЮБАЯ ошибка при продолжении диалога (TIMEOUT/SRV/SSH) на первой попытке трактуется как resume-failure. | Пользователь ведёт диалог (session label в session_manager), сервер временно недоступен → (1) ложное системное сообщение «Контекст диалога сброшен. Начинаю новую сессию.» (ничего не сбрасывалось); (2) вторая попытка с resume=None → generate НОВЫЙ label → при транзиентном сбое (сервер ожил на 2-й попытке) стартует НОВАЯ серверная сессия, старый контекст осиротает, хотя он был жив; (3) лишний повторный вызов сервера. Фикс: в thin-ветке не заворачивать gateway-ошибки в resume-fallback (проверять, что ошибка — именно отказ resume, а не транспорт/таймаут).

Medium (латентный) | online_auth.rs:308-310 (R-04) ↔ local_mode.rs:84-113 (decide_from_online) | R-04 возвращает Err(HttpStatus) на ЛЮБОЙ не-2xx ДО чтения тела. authorize() маппит Err→"cached"(при свежем кэше)/"offline". decide_from_online: "cached"→allowed=true, "offline"→None→Ed25519-fallback. Значит серверный «blocked» доходит как Denied ТОЛЬКО если сервер отдаёт его с HTTP 2xx+телом. Если blocked приходит с 4xx/5xx — R-04 превращает его в cached/offline → блокировка обходится. До R-04 (4xx+{status:blocked}) давало Denied — это регресс для blocked-через-не-2xx. | Сервер отзывает лицензию клиента, отвечает HTTP 403+{"status":"blocked"} → local_mode: свежий кэш (<24ч) → allowed, ИЛИ нет кэша → Ed25519 с валидной подписанной лицензией → allowed. Обход отзыва. СМЯГЧЕНИЕ: (а) verify_local_mode НИГДЕ не вызывается (grep по обоим worktree) — код не на пути исполнения, латентно до подключения локального режима; (б) безопасно, если серверный контракт /auth ВСЕГДА отдаёт вердикт через HTTP 200 (blocked в теле). Проверить серверный контракт до подключения local mode; если 200 — не проблема, если 4xx — дыра. Эскалация до High при выполнении обоих условий (local mode подключён И blocked через не-2xx).

— Проверено и НЕ находка: default-сборка (весь thin за cfg, рефакторинг auto_save_response эквивалентен старому inline по всем 4 веткам suppress/empty); UI-зависание при терминальной gateway-ошибке (основной invoke('send_message') обёрнут в try/catch, ChatPanel.svelte:778-785 → снимает safety-таймер + сообщение + isLoading=false); инвариант недостижимости CLI при thin (run_claude_inner/find_claude_binary/isolated_claude_config_dir приватны, вызовы только за cfg(not thin)); безопасность транспорта (промпт через stdin не argv; node с префиксом user@ гасит option-injection; StrictHostKeyChecking=yes+CA; stderr дренаж в потоке против deadlock; MAX_RESPONSE_BYTES против OOM); invoke импортирован в settings.svelte:2 (не missing).

## ТРИАЖ (рабочая сессия, 2026-07-20)
- #1 counter мёртв — ПОДТВЕРЖДЕНО (XOR со сдвигом <<32 не задевает биты [0,31] под маской) → ИСПРАВЛЕНО: counter замешан и в младшие биты (gateway_executor.rs::generate_session_label).
- #2 нет клиентского таймаута — ПОДТВЕРЖДЕНО (ServerAlive держит канал при живом sshd независимо от forced-command; init уже снял safety-таймер UI) → ИСПРАВЛЕНО: tokio::time::timeout 1830с вокруг spawn_blocking → [TC-GW-TIMEOUT]; жёсткий kill осиротевшего потока — фаза 2.
- #3 ложный resume-fallback — ПОДТВЕРЖДЕНО (guard lib.rs:1166 безусловен по типу ошибки) → ИСПРАВЛЕНО: guard исключает префикс "[TC-GW-" (gateway-ошибки падают в честную ошибку, label/контекст сохраняются; CLI-путь не задет — его ошибки без такого префикса).
- #4 R-04↔local_mode обход blocked через не-2xx — ПОДТВЕРЖДЕНО КАК ЛАТЕНТНЫЙ (verify_local_mode не вызывается ни в одном продукте прода; пилот Econometrica local mode не использует). НЕ исправлено в блоке: правка семантики общего крейта aurora_fleet шире блока + требует факта серверного контракта /auth. → БЭКЛОГ с жёстким триггером «ДО подключения local mode»: (а) проверить, отдаёт ли auth Edge Function blocked всегда с HTTP 2xx; (б) если нет — научить R-04 читать тело при не-2xx и различать вердикт-в-теле от транспортного сбоя. Оценка: 1–2 ч + прогон fleet 53. Вписано в NEXT_SESSION_thin_module_port.md.

---

# Аудит блоков 2 и 3 — докачка по частям + порог на соединение (старт 27.07.2026)

Внешний аудитор, чистого контекста сессии написания не видел.

## Как я понял задачу (своими словами)
Диф (~1524 строки) по тонкой редакции Aurora Econometrica: Tauri (Rust + SvelteKit), все рабочие
материалы приезжают по сети с облачной функции Supabase Edge (Deno, `supabase/functions/content/index.ts`).
Блок 2 добавляет докачку по частям: сервер объявляет `Content-Length` и понимает `Range`, отвечает 206
с `Content-Range`; клиент просит куски по 2 КБ и склеивает. Блок 3 запрещает переиспользование соединений
(`pool_max_idle_per_host(0)`), потому что посредник у клиента считает порог на соединение, и правит текст
об отсутствии файлов доступа. Ищу: ошибки границ диапазонов, потерю/дублирование байт при склейке,
зацикливание, гонки; разбор внешнего ввода (`Range` на сервере, `Content-Range` на клиенте) — переполнения,
отрицательные, огромные числа, паника в Rust; безопасность (выделение памяти по чужим числам, DoS, обход
лицензии, утечки в журнал); расхождения с handoff; слом старых сборок клиента, которые `Range` не шлют.

## План просмотра
1. handoff.md, секции «блок 2» и «блок 3» — сделано.
2. CLAUDE.md проекта — сделано.
3. audit.diff: сначала `supabase/functions/content/index.ts` (боевой прод, разбор `Range`),
   затем `content_updater.rs` (разбор `Content-Range`, склейка, цикл, замок, память о версии),
   затем `updater.rs`/`online_auth.rs`/`lib.rs`/`gateway_executor.rs`, затем гейт `tools/check_content_range.mjs`
   и зонд `tools/probe_content_delivery.sh`, затем фронт/конфиги.
4. Перекрёстная сверка diff ↔ инварианты handoff + отдельный проход на обратную совместимость.

Старт: 27.07.2026.

## Находки (формат: severity | file:line | суть | сценарий отказа)

просмотрен handoff.md (блоки 2 и 3) · просмотрен CLAUDE.md · просмотрен audit.diff целиком (1524 стр.)

High | supabase/functions/content/index.ts:1266 (diff) | Путь в хранилище строится из непроверенных `version` и `file`; лицензия сверяется только по `product`, сегменты `..` нормализуются HTTP-слоем supabase-js → выход за префикс продукта | ДОКАЗАНО БОЕМ: `version=..%2Feconometrica%2Fc3` → 200, 34165 байт (тот же файл эквивалентным путём); контроль `version=zz-none` → 404. Значит `version=../<чужой-продукт>/<версия>` отдаёт vault другого продукта линейки владельцу самой дешёвой лицензии; в хранилище файлы лежат в открытом виде (шифруются только на клиенте, download_updates:780)
High | src-tauri/src/commands/content_updater.rs (diff 248-250, 299) | `total` из чужого `Content-Range` ничем не ограничен: MAX_BODY_BYTES применяется только к одному ответу, к сборке — нет; общего бюджета времени больше нет (attempt_with_budget переехал на кусок) | Посредник отвечает `206 Content-Range: bytes 0-2047/999999999999` и отдаёт по 2 КБ → цикл на ~488 млн запросов, память растёт, open_cabinet не возвращается никогда, замок lock_for(url) держится всё это время. Подслучай: `bytes 0-0/0` + пустое тело → Ok(пустой файл) → пустой vault шифруется, пишется, версия фиксируется → кабинет сломан навсегда
High | src-tauri/src/commands/content_updater.rs (diff 471-473 + 104) | Дробление по 2 КБ и `pool_max_idle_per_host(0)` применены и к бандлу фронтенда (call site :1041, URL из /auth = Supabase Storage) | ЗАМЕР: tar.gz build/ = 1 502 606 байт → 734 последовательных запроса, каждый с новым TLS-рукопожатием; ПРОБА: Storage на Range отвечает 206 (bytes 0-9/3972522) — дробление включится гарантированно. Общего потолка времени нет, единичный сбой откатывает в ноль → repair_frontend и обновление интерфейса перестают доезжать. Handoff блок 3 §3 заявляет дробление «непригодным для больших материалов», в коде порога нет
Medium | src-tauri/src/commands/content_updater.rs (diff 430-432) | Повтор зовёт fetch_vault_once заново — сборка откатывается к байту 0, накопленный buf теряется | При 20% сбоя одного запроса P(17 подряд)=2,2%, за 5 попыток ≈10,5% → 9 запусков из 10 кабинет не открывается, хотя каждый кусок ниже порога. Тесты не ловят: RangeAware никогда не рвёт кусок ниже порога
Medium | src-tauri/src/lib.rs:1026-1033 (diff) | `d.contains("403")`/`("404")` срабатывают на числах байт внутри технической строки | Обрыв на 4034 байт (наблюдённое окно 3251…5649) → «поток оборван на 4034 байт» содержит «403» → пользователю «Сервер не подтвердил право на кабинет» + совет проверять лицензию; поддержка уходит в ложный след — ровно то, ради чего функция написана. Тест берёт 4278 и не ловит
Medium | tools/check_content_range.mjs | Гейт не подключён ни к CI (.github/workflows/ci.yml шаг «Prompt & delivery linters» — 5 python-линтеров, этого нет), ни к lefthook.yml | Прогнал руками: 17 проверок, exit 0 — гейт рабочий, но не запускается никем. Следующая правка parseRange уедет на необратимый деплой зелёной; нарушает CLAUDE.md §18
Medium | src-tauri/src/commands/content_updater.rs (diff 213-223) | 416 классифицируется как Fatal (повторов нет), хотя сервер отдаёт его при start >= size | Клиент собрал 30 КБ из 34 КБ, выкладывается новая версия кабинета в 2 КБ → `bytes=30720-32767` → 416 → отказ без единого повтора. Соседний случай «размер изменился» автор сделал повторяемым (diff 262-269) — классификация непоследовательна
Medium | src-tauri/src/commands/content_updater.rs (diff 151-153, 280) | parse_content_range_total берёт только число после «/», фактические границы куска отбрасываются; RFC 9110 §15.3.7 требует сверки с запрошенным диапазоном | Посредник/кэш отдаёт на `bytes=2048-4095` кусок того же размера с другим смещением → итоговая длина совпадает, сверка проходит, файл сдвинут. open_cabinet передаёт checksums={} (lib.rs:623) → битый архив шифруется, версия фиксируется → кабинет не открывается и НИКОГДА не перекачивается

— Проверено и НЕ находка: parseRange (границы, огромные числа, bytes=-0, пустой файл, не-байтовая единица, несколько диапазонов) — 17/17 гейта + чтение, isSafeInteger закрывает переполнение; Vec::with_capacity ограничен MAX_PREALLOC_BYTES; lock_for/take_recent — гонки и тупика нет (std-мьютекс не удерживается через await, замок берётся до чтения памяти); обратная совместимость в ОБЕ стороны проверена боем (старый клиент без Range → 200 + явный Content-Length 34165; новый клиент против сервера без Range → RangeReply::Whole); online_auth last_err.unwrap() безопасен (ветка Ok делает break до warn); текст шлюза называет верный путь (gateway_executor.rs:119-121 читает app_data_dir()/client); forget_vault_version корректен; боевая функция снаружи: 200 с явным Content-Length, 206 с bytes 0-9/34165, Accept-Ranges: bytes, порционной передачи нет.

ИТОГ: High 3, Medium 5. Вердикт — требует исправлений.

---

# Аудит блока P0.1 (старт 02.08.2026) — внешний аудитор, чистый контекст

## Как я понял задачу (своими словами)
Мне дали diff на 667 строк по блоку P0.1 плюс handoff автора (его ЗАЯВЛЕНИЕ, не истина)
и CLAUDE.md продукта (17 критических правил). Ищу дефекты корректности с воспроизводимым
сценарием: конкретный вход/состояние → неверный вывод или падение. Расхождение diff ↔ handoff
само по себе находка. Стилевые замечания не пишу.

## План проверки
1. auditor.md (канонический промпт) → работать по нему.
2. audit.diff целиком — первоисточник.
3. handoff.md + CLAUDE.md — сверка заявленного с фактическим.
4. Прицельно: roiBase() на легаси/эффективности/счётном режиме/битом vpcu;
   money_roi_unavailable (смысл в ответе vs употребление в движке); порядок вставки
   инсайта в начало decomposeInsights; corridorIsObserved при lo/hi = 0, отрицательных,
   lo > hi; roi_base в фактах советника на ВСЕХ ветках switch по step; 4 новых файла
   тестов — нет ли зеленеющего на вырожденном входе.
5. Находки дописываю СРАЗУ строкой: severity | file:line | суть | сценарий.

## Находки (формат: severity | file:line | суть | сценарий воспроизведения)

просмотрен auditor.md · audit.diff целиком (667 стр.) · handoff.md · CLAUDE.md
доп. зонды: kpi-aware-formatting.js:1-130 (typedef KpiView) · tier2-context.js:175-249 (switch)
· decomposer.py (grep _insight_money_roi_na / derived_mode) · OptimizeGoalSeek.svelte (grep salesCorridor)

High | src/lib/tier2-context.js:189-193, 236 (+ InsightsPanel.svelte:402-439) | `mode` НЕ доезжает до `roiBase` в фактах советника: деструктуризация берёт только `kpiType/kpiKind/valuePerCountUnit`, экран передаёт ровно их же, а ветка «режим эффективности → база не определена» (kpi-aware-formatting.js:572) читает `kpi.mode`. Инвариант handoff §2 «база выводится ОДНОЙ функцией» формально соблюдён, но ВХОД собирается тремя разными способами (DecomposeStep передаёт `mode`, tier2-context — нет, insights-rules отдаёт целый KpiView) → результат расходится для одного и того же состояния проекта. Это ровно дефект ярусов MQS в новой форме | Проект в режиме эффективности (`derivedMode='effectiveness'`, `kpiKind='monetary'` — комбинация штатная, есть в тестах продукта kpi-aware-formatting.test.js:124). Экран (DecomposeStep, `mode` передан) в подсказке «ROI»/«Вердикт» пишет «абсолютные пороги окупаемости отключены»; советник в тот же момент получает `facts.roi_base='оборот'` и по правилу 7 вправе писать «канал окупается по обороту». Движок при этом отдаёт `money_roi_unavailable=True` (decomposer.py:1391) и вердикты считает веткой `_eff_mode` без денежных порогов. Хуже в счётном режиме с маржой: `count + effectiveness + vpcu=200` → `roi_base='прибыль'` → советнику прямо разрешены слова о прибыльности там, где порогов нет. Сторож не ловит: describe «база доезжает в факты» (tier2-roi-base.guard.test.js:332-355) вызывает buildTier2Context вообще без `mode` | Фикс: принимать `derivedMode` в input buildTier2Context и передавать `mode` в roiBase; в сторож добавить кейс effectiveness ЧЕРЕЗ buildTier2Context

High | src/lib/kpi-aware-formatting.js:573-575 (+ tier2-context.js:236, DecomposeStep.svelte:402-407) | `roiBase` требует `typeof vpcu === 'number'`, но оба вызывающих передают СЫРОЕ значение стора `$valuePerCountUnit` мимо нормализации `kpiView` — а строковый vpcu в продукте реален: собственный тест kpi-aware-formatting.test.js:86 заведён именно на `valuePerCountUnit: '120.5'` | Счётный KPI, «Маржа на упаковку» пришла строкой '200' (из сохранённого проекта/поля ввода). Движок: `kpi_unit_cost` задан → `_insight_money_roi_na=False` (decomposer.py:1276) → абсолютные пороги ПРИМЕНЯЮТСЯ, `money_roi_unavailable=False`. Фронт: `roiBase` → 'не определена' → подсказки «ROI» и «Вердикт» утверждают «Экономика не задана, поэтому абсолютные пороги окупаемости отключены» (ложь), а советнику запрещено слово «окупается» на исправном проекте. Инсайт-раскрытие при этом молчит (поле False) → на одном экране два взаимоисключающих утверждения | Фикс: приводить vpcu через ту же нормализацию, что в kpiView (Number + isFinite), либо принимать KpiView и не собирать вход вручную

Medium | src/lib/kpi-aware-formatting.js:574 ↔ sidecar/econometrica/engines/decomposer.py:1276 | Граница «есть экономика» разъехалась между фронтом и движком на нуле и отрицательных: фронт `vpcu > 0`, движок `kpi_unit_cost is None`. Сторож эту расхождённость ЗАКРЕПЛЯЕТ (tier2-roi-base.guard.test.js:295: `vpcu: 0` → 'не определена') | Пользователь ввёл 0 (или минус) в «Маржа на упаковку»: движок `_insight_money_roi_na=False` → пороги применяются, `money_roi_unavailable=False` → инсайт об отключённых порогах НЕ показан; фронт `roiBase='не определена'` → подсказка «пороги отключены» + советнику запрещены слова про окупаемость. Тот же разлад, что выше, но достигается штатным вводом с клавиатуры | Фикс: свести условие в один контракт (движок отдаёт признак, фронт его читает — как и заявлено в §2 handoff), либо синхронизировать порог `> 0` на стороне движка

Medium | src/lib/components/pipeline/OptimizeGoalSeek.svelte:117 (corridorIsObserved) ↔ :92,101 | Признак «настоящего коридора» проверяет только «ключ не null», без конечности, знака и `lo < hi`; при этом `corridorLo/corridorHi` используют `??`, который ноль пропускает | (а) `salesCorridor={lo:0,hi:0}` → corridorIsObserved=true → текст утверждает «модель считает за пределами того, что наблюдала в данных» для вырожденной зоны нулевой ширины; попутно `sliderMax=0`, `min=corridorLo*0.5=0`, `step=(0-0)/100=0` → ползунок цели неработоспособен. (б) `hi=NaN` → признак true, `sliderMax=NaN` → тот же слом при «честном» тексте. (в) `{lo:100}` без `hi` → признак false, но `corridorLo` уже настоящий, а `corridorHi` запасной ×1,5 → пользователю говорят, что ВСЯ зона — ориентир от текущего уровня, хотя нижняя граница наблюдённая. Латентно ровно до P1: сегодня `aggregate_sales` движок не отдаёт, признак всегда false | Фикс: требовать `Number.isFinite(lo) && Number.isFinite(hi) && lo < hi`

Medium | src/lib/__tests__/goalseek-corridor-honesty.guard.test.js:210-238 | Сторож «зелёной зоны» зеленеет на вырожденном входе: все 5 проверок — поиск подстрок в исходнике, ни одна не проверяет, что `corridorNote` ПОДСТАВЛЕН в `attentionTo`. Объявления const достаточно | Регрессия: вернуть в `attentionTo` безусловный литерал «Цель должна быть в зелёной зоне коридора – иначе модель считает за пределами того, что наблюдала в данных.», оставив неиспользуемые `corridorIsObserved`/`corridorNote` объявленными. Проверка 1 (`toContain('corridorIsObserved')`) — зелёная, 2 (regex на `$derived(...salesCorridor`) — зелёная, 3 (`'ориентир от текущего уровня'`) — зелёная (строка жива в объявлении), 4 ловит только СТАРУЮ формулировку с «observed range» — зелёная, 5 — зелёная. Ложное обещание снова доезжает до клиента при полностью зелёном наборе | Фикс: проверять факт подстановки — напр. regex `attentionTo=\{\[[\s\S]{0,80}corridorNote`

Medium | src/lib/kpi-aware-formatting.js:597-598 (ветка default `roiBaseNote`) ↔ DecomposeStep.svelte:402-407 | Для монетарного проекта в режиме эффективности пользователю показывается «Экономика не задана» — утверждение ложное: метрика денежная и экономика задана, отсутствуют именно абсолютные пороги. Handoff §4.5 отмечает вопрос как стилистический, фактически это неверное утверждение о состоянии проекта | Проект «Выручка», `derivedMode='effectiveness'` → подсказки «ROI» и «Вердикт» на шаге декомпозиции: «Экономика не задана, поэтому абсолютные пороги окупаемости отключены». Пользователь идёт задавать экономику, которая уже задана | Фикс: отдельная ветка текста для режима эффективности (доли, а не деньги)

— Проверено и НЕ находка: `roiBase`/`roiBaseNote` через KpiView в insights-rules.js:539 корректны — typedef KpiView (kpi-aware-formatting.js:35-47) содержит ровно `kpiKind`/`mode`/`vpcu`/`kpiType`, kpiView нормализует их; legacy-монетарный (kpiType=null) даёт 'оборот', как заявлено; `facts.roi_base` действительно попадает на ВСЕ ветки switch (tier2-context.js:202-236 — присваивание после switch, все case делают `break`, `default` тоже присваивает `facts`); мутации входных объектов нет (`facts` во всех ветках — свежий объект из summarize*/compact, не алиас `dec`/`mod`/`opt`); строгое `=== true` в insights-rules.js:519 отсекает строковое 'true'; питоновский сторож читает существующий путь и ловит прибитую константу; порядок инсайтов — потребителя, ломающегося от вставки в начало, найти не удалось (в src на `decomposeInsights(` отвечают только тесты), сценария нет → не находка.

ИТОГ блока P0.1: High 2, Medium 4. Вердикт — требует исправлений (блокирующие: обе High).

---

# Аудит блока P0.2–P0.5 (2026-08-03)

Внешний аудитор, чистый контекст. Просмотрено: audit.diff целиком (2293 стр.), handoff.md, CLAUDE.md.
Доп. зонды: server.py (сборка конфига обучения), ValidateStepV13.svelte:812-848, insights-rules.js:250-270,
sensitivity.py (форма возврата), потребители result.issues, остатки NRMSE по дереву.
Формат: severity | file:line | суть | сценарий

Critical | src/lib/components/pipeline/ValidateStepV13.svelte:810 + 835-848 | Жёсткая блокировка кнопки «Подтвердить роли» переехала на эффективный знаменатель: `ratioCardData.ratio` теперь `effectiveRatio(...)`, а `ratioBlockedReason` проверяет это же поле. Handoff §4.1 утверждает обратное — «порог остался на сыром, рассинхрон осознанный» | 40 наблюдений, 10 назначенных столбцов, байес: было 40/10=4.0 → кнопка активна; стало 40/23=1.7 → disabled. Действующий пилот перестаёт проходить валидацию. Лекарство тоже не работает: `afterExcludeRatio` считается тем же знаменателем, 13 авто-параметров не убираются ничем

High | src/lib/ratio-classifier.js:2070-2095 (diff) ↔ TrainRequest.use_holidays | `effectiveParamCount` знает про семантический дедуп, но не про мастер-переключатель «Учитывать праздники РФ» на том же экране; встроенная справка (pipeline.html) советует его выключать именно при нехватке данных | 40 obs / 10 столбцов, праздники ВЫКЛ: модель заведёт 11 параметров (3.6), экран показывает 1.7 и держит блокировку. Пользователь делает ровно то, что советует справка, и не видит реакции

High | src/lib/project-state.js:1992-1999 (diff) | `validationHeaderMetrics` зовёт `effectiveParamCount(result.detected, $engine)` БЕЗ живого числа ролей, тогда как ValidateStepV13 и validateInsights его передают; в том же объекте `ratioRaw` живой. Нарушен инвариант handoff §2.8 | 52 obs, 8 медиа + 2 контроля; снять роль с 3 каналов без повторной валидации: карточка → 2.6, липкая шапка → 2.3 (устаревшее), вторая строка подписи → 7.4. Три числа одной величины на одном экране

High | src/lib/insights-rules.js:267-269 (validateRolesInsights) | Делегирует в `validateInsights(result, objective)` без третьего аргумента → всегда 'bayesian'. Обновлены только ValidateStepV13:917 и InsightsPanel:307 | Проект в OLS (штатный путь для коротких рядов): шапка 4.7 «рекомендуемый уровень», подсказки под-шага «Роли колонок» в той же панели 2.3 «критически мало». Сторож ratio-single-source.guard.test.js зовёт validateInsights напрямую с явным режимом и путь не покрывает

High | sidecar/econometrica/engines/validator.py:399-421 (diff) | На `ratio_gate` (OLS) переведены не только пороги, но и ПОДСТАВЛЯЕМОЕ В СООБЩЕНИЕ число; сообщения доезжают до пользователя (ValidateStep.svelte:361 `issues={result.issues ?? []}`, ColumnMapperConfirm.svelte:144) | 30 obs / 10 столбцов, байес: движок «Ratio 2.7:1 – ниже минимума», карточка рядом «1.3 – критически мало» + блокировка кнопки. Ровно тот дефект, ради устранения которого написан P0.3. Handoff §3.1 признаёт расхождение полей, но не печать мягкого числа пользователю

Medium | sidecar/econometrica/engines/validator.py:288-306 (diff) | `missing_pct` и оба новых предупреждения лежат внутри ветки `is_numeric_dtype` | Медиа-колонка с текстовой ячейкой «н/д» → dtype object → `stats` не создаётся → пять экранов через `?? 0` показывают «0% пропусков» именно на самой битой колонке. Корень закрыт для здоровых колонок и открыт для больных

Medium | docs/USER_GUIDE_v2_1_0.md:250,257,358 + tools/build_glossary.py:66 | Якорь `#nrmse-нормализованная-ошибка` удалён из глоссария, но руководство пользователя ссылается на него из таблицы порогов и держит раздел «NRMSE > 15% (красный)»; таблица генератора всё ещё сопоставляет удалённый термин | Клиент открывает руководство → строка таблицы качества с метрикой, которой в продукте нет, и ссылка в никуда. Handoff §5 перечисляет затронутую документацию и USER_GUIDE не называет

— Проверено и НЕ находка: `config = req.model_dump()` (server.py:882, :1223) — зерно из запроса реально доезжает до train_model, поле не теряется; `compute_sensitivity_tornado` возвращает ключ `parameters` (sensitivity.py:100, :173) — условие показа в ReportStep совпадает с формой данных, немого скрытия карточки нет; `seed_from_model` для моделей до P0.2 честно отдаёт значение по умолчанию (совпадения с исходным обучением ни код, ни комментарий не обещают); AST-сторожа проверяют инварианты, защита от тихого нуля выдержана на каждом; раскладка цепей через `nuts_sampler_kwargs` — исправление настоящее, прямой аргумент действительно проглатывался PyMC; перенос `modelEngine` выше по файлу (зона §4.5) корректен — writable объявлен до подписанного на него derived; снятие PPTX — мёртвых веток не осталось, тест перевёрнут на отсутствие обещания.

ИТОГ блока P0.2–P0.5: Critical 1, High 4, Medium 2. Вердикт — требует исправлений (блокирующие: блокировка «Подтвердить роли» на эффективном ratio; инертный переключатель праздников; устаревший знаменатель в шапке; режим не прокинут в validateRolesInsights; два разных числа запаса от движка и фронта на одном экране).

---

# Аудит хвоста блока P0.7 (2026-08-03) — внешний аудитор, чистый контекст

Просмотрено: audit.diff (446 стр.) целиком, handoff.md, CLAUDE.md. Зонды: methodology_cert.py:140-250 и
495-548, sections.py:1700-1800, utils/negative_baseline.py (пороги и смысл вероятности), modeler.py:150-225
(_resolve_auto_adstock), utils/adstock.py:87-107 (что делает движок с неизвестным типом), grep adstock_config.
Формат: severity | file:line | суть | сценарий

High | sidecar/econometrica/engines/methodology_cert.py:212-225 | В хешируемый payload уезжает НАСТРОЙКА вместо применённого типа адстока. Восстановление берёт значение из `config['adstock_config']` и кладёт `str(тип)` без сверки со списком известных типов, хотя комментарий над кодом (:195) заявляет «карта заполняется ФАКТИЧЕСКИ применёнными типами», а ключ у проверяющей стороны называется `adstock_types`. Приоритет отдан конфигурации; `channel_params`, где у OLS лежит хотя бы записанное значение, читается только вторым. Движок неизвестный тип НЕ отвергает: `utils/adstock.py:95-107` молча считает по геометрическому с одним предупреждением в журнал — то есть расчёт идёт по geometric, а сертификат утверждает `auto`. | OLS-модель (у неё `channel_adstock_types` не бывает никогда, поэтому ветка достижима всегда) с `adstock_config={'ТВ':'auto','Диджитал':'auto'}` — по зонду самого автора (handoff §3) на реальных проектах там стоит именно `auto` → карта собирается целиком → в отпечаток и в сертификат уезжает `adstock_types={'ТВ':'auto','Диджитал':'auto'}`, при том что применён геометрический. Словарная форма `{'type':'auto'}`, документированная в modeler.py:159, даёт в payload строку вида `"{'type': 'auto'}"`. Это тот же класс, ради которого чинили F-01 и Ф-01. Фикс: белый список `geometric|weibull` (иначе ключ не класть) — ровно та дисциплина, что уже применена к `kpi_type`.

High | sidecar/econometrica/aurora_html/sections.py:93-102 (новый подвал) | Подвал безусловно обещает «При повторном обучении на тех же данных числа сохранятся», а печатается по ОДНОМУ условию — наличию отпечатка. Отпечаток выдаётся и при `status='not_attested'` с `reproducibility.status='absent'` (собственный сторож test_certificate_in_reports.py:205-250 фиксирует эту комбинацию как штатную), а `absent` означает ровно одно: зерно сэмплера не записано, повторить прогон побитово невозможно — methodology_cert.py:550-553 говорит это прямым текстом. | Модель, обученная до P0.2 → сертификат `not_attested`, отпечаток есть → клиент читает в отчёте «числа сохранятся, а отпечаток станет другим», переобучает на тех же данных и получает ДРУГУЮ разбивку (MCMC без записанного зерна). Тот же эффект у режима малых данных с `auto`: повтор заново гоняет BIC-селектор. Нарушен инвариант handoff §2.4 «не обещается больше, чем проверено». Фикс: клаузу о сохранении чисел печатать только при `reproducibility.status in ('recorded','deterministic')`.

Medium | sidecar/econometrica/aurora_html/sections.py:84-102 | Зона неуверенности автора №4 подтверждается как дефект: подвал привязан к отпечатку, список — к строкам, и комбинация «список есть, подвала нет» достижима, потому что `checks` и `reproducibility` считаются ДО try и уезжают в ответ даже при отказе (methodology_cert.py:530-548). Вместе с подвалом исчезает оговорка «Базовый уровень взят до выноса отдельных факторов, поэтому может отличаться от полосы на графике разбивки» — а поясняет она именно ту строку списка, которая осталась. | Свежая байесовская модель (паспорт записан, в model-diagnostics.json есть negative_baseline) + любой отказ payload: `total_sales=0`, столбец назван «Base», нефинитный вклад канала → блок печатает «Заверение недоступно: …», под ним «Зерно генератора», «Цепи и шаги» и вердикт по базовому уровню — без единой оговорки, о каком базовом уровне речь. Клиент сверяет вердикт с полосой на графике и видит расхождение. Сторож Ф-06 мимо: он взят на `checks='absent'`, где список пуст по построению. Фикс: подвал печатать при наличии списка, а клаузу про отпечаток — внутри условия по отпечатку.

Low | sidecar/econometrica/tests/test_certificate_in_reports.py:289-300 | Сторож длинного тире проверяет ровно ОДНУ причину отказа из восьми: `build_cert_payload` с пустым манифестом всегда падает на «Манифест файла модели недоступен», до остальных веток дело не доходит. Плюс `причины[0] if причины else 'причина'` делает тест нечувствительным и к исчезновению исключения. | Вернуть длинное тире в любую из остальных причин (нет типа KPI, неполная разбивка, не число, канал назван «Base», JCS недоступен) → набор зелёный, текст доезжает до клиента. Фикс: проверять исходник модуля на U+2014 целиком, как это делает tools/lint_prompt_commands.py для промптов.

Low | sidecar/econometrica/tests/test_certificate_in_reports.py:303-312 | Сторож связи слайда и сертификата ищет подстроку `строки_сертификата` в исходнике `s10_methodology` через inspect.getsource — упоминание в комментарии или закомментированный вызов его удовлетворяют. | Закомментировать вызов, оставив имя в комментарии (ровно так выглядит откат правки) → тест зелёный, блок молча исчезает из презентации. Фикс: собрать слайд и проверить подпись в тексте фигуры — приём уже есть в тестах выше по файлу.

Low | sidecar/econometrica/aurora_html/sections.py:45-61 | Пороги 20 % и 80 % продублированы словами в клиентском тексте; источник — PROB_OK=0.2 / PROB_FAIL=0.8 (utils/negative_baseline.py:52-53). Сверено: сегодня формулировки ВЕРНЫ, включая границы и то, что вероятность считается по среднему по периодам уровню (negative_baseline.py:189-192) — дефекта в текстах нет. Дефект в связи: сторожа паритета на эти два числа нет, инвариант handoff §2.5 требует единого источника. | Продуктовое решение сдвинуть PROB_FAIL на 0.9 → движок ставит «провал» с 90 %, отчёт продолжает утверждать «выше 80 %»; расхождение доезжает до клиента молча.

Low | docs/GLOSSARY_v2_1_0.md:13 + docs/glossary.json + src/lib/glossary.js | Иллюстративный балл MQS изменён с 78 на 75 при том же входе (60 недель, четыре канала без сильной взаимосвязи) и без изменения формулы в этой правке — одно из двух чисел неверно, клиент читает его как ориентир. | Пользователь с 60 неделями и четырьмя каналами сверяет свой балл с примером справки. Фикс: взять число из прогона расчёта MQS либо убрать цифру, оставив уровень словами.

— Проверено и НЕ находка: тексты вердиктов проверки базы (зона №5) фактически верны; длинного тире в клиентском тексте diff нет (в комментариях — не клиентский текст); «доверительный интервал»/«CI» отсутствуют; порядок источников описания модели (зона №3) корректен для обеих веток — байесовская пишет верхний уровень только вместе с config, у OLS верхний уровень подставляет загрузчик и он справедливо отброшен; условие «карта только целиком» (зона №2) само по себе безопаснее частичной карты; переменный набор ключей model_spec (зона №1) внешний контракт не ломает — поле свободной формы, сравнение двух сертификатов между собой продуктом нигде не обещано; приоритет операторов в тернарнике :217-219 верен; test_причина_отказа_не_содержит_имени_исключения реально ловит (RuntimeError содержит «Error»); пересборка PDF-манифеста справки соответствует маршруту §18.

ИТОГ хвоста P0.7: High 2, Medium 1, Low 4. Вердикт — требует исправлений (блокирующие: `auto` в хешируемом описании модели; безусловное обещание «числа сохранятся» на моделях без записанного зерна).

