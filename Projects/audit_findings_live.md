# Живые находки адверсариального аудита (блок econ + петля Авроры)

Формат: severity | файл:строка | суть | сценарий

Critical | src-tauri/src/lib.rs:711 (resolve_slash_command) + ChatPanel.svelte:702 | Контракт данных СЛОМАН в реальном рантайме. ChatPanel прикладывает блок «=== Данные проекта ===» к message; но send_message→resolve_slash_command берёт всё после первой строки как arguments и делает template.replace(\"\$ARGUMENTS\", arguments). Ни один командный .md кабинета (interpret-model/why-channel/explain-ratio/pilot-design/next-quarter-plan/data-gaps) НЕ содержит \$ARGUMENTS → замена ничего не подставляет → resolved === template байт-в-байт → блок данных ОТБРАСЫВАЕТСЯ, до Claude уходит только текст шаблона команды. | Пользователь в проде запускает /interpret-model с обученной моделью. Эмпирически доказано на реальных фикстурах: message 14688 симв (с данными) → resolved 2174 симв (=длина шаблона), 0 байт данных доезжает; маркеры 77.49/80.6/[model-diagnostics] отсутствуют в промпте. Ассистент НЕ видит ни одного числа модели → либо галлюцинирует (нарушение INV-50, ради которого весь блок и строился), либо всегда отвечает «шаг не пройден». Мимо тестов, т.к. эвал-харнес (run_eval.mjs) шлёт message в claude -p через stdin НАПРЯМУЮ, минуя send_message/resolve_slash_command — тестирует не тот путь, что прод (ложное «зелёно»). Фикс: либо командные .md должны содержать \$ARGUMENTS (куда подставится блок), либо send_message должен инжектить блок данных в final_message ПОСЛЕ resolve_slash_command (как это сделано для media-analyst slides.json на lib.rs:1057-1074), а не полагаться на \$ARGUMENTS.
High | src/lib/econ-project-context.js:1579 (stripDecompTelemetry) | Вырезает только time_series и waterfall, но НЕ signed_factor_contributions — а именно там ~18 факторов × ~31 значение per_period = ~500+ поточечных служебных чисел (динамика контролей/праздников по неделям). Заявленная цель (§2/§3 handoff: не раздувать промпт, не засорять страж чисел поточечной графикой) НЕ достигнута. | Кабинет econometrist, реальная фикстура kagocel decomposition.json (строки 620-1245): блок signed_factor_contributions целиком уходит в === Данные проекта ===, промпт раздут на сотни служебных чисел; в grounded-множестве INV-50 появляются сотни значений вида 15307095.1, что резко расширяет поверхность ложных совпадений и маскирует галлюцинации ассистента. Тест econ-project-context.test.js проверяет только отсутствие time_series/waterfall — signed_factor_contributions не покрыт, дефект прошёл мимо тестов. Замер на реальной эвал-фикстуре decomposition.json: полный блок 1083 числовых токена; после текущей вырезки остаётся 729 токенов / 14812 симв; из них signed_factor_contributions = 496 поточечных чисел (16 факторов × 31 неделя). Если вырезать и его — осталось бы 182 токена / 7953 симв. То есть вырезка снимает лишь ~треть поточечного шума. hierarchical dict тоже остаётся (мелкий, не критично).

Medium | src/lib/rag-query.js:1691 (STEP_TERMS) + InsightsPanel.svelte:383 | STEP_TERMS проиндексирован по СТАРОЙ 5-шаговой нумерации (report=5), но реальный $pipelineCurrentStep использует НОВУЮ 7-шаговую (project-state PIPELINE_STEPS: planning=5, report=6; DecomposeStep.set(4), PlanningStep.set(6)). На шаге Report (реальный индекс 6) STEP_TERMS[6]=undefined → тематизация деградирует до DEFAULT_TERMS; на шаге Planning (5) выдаются REPORT-термины. | Пользователь на шаге «Отчёт» задаёт вопрос ассистенту Авроры → RAG-запрос уходит без домен-терминизации шага (ровно то, ради чего петля делалась), англ. MMM-статьи тонут. Тесты buildRagQuery проверяют только step 1-4 через STEP.* → рассинхрон не пойман. Прим.: связанный pre-existing перекос buildTier2Context switch(step) на шагах 5/6 — вне зоны дифа (STEP не менялся), но тем же корнем.

Low | src/lib/rag-query.js:1722 (CHANNEL_TYPE_TERMS/focusChannelType) | Ветка focusChannelType — мёртвый код в проде: единственный вызов buildRagQuery (InsightsPanel:383) не передаёт focusChannelType. CHANNEL_TYPE_TERMS достижим только из тестов. | Не дефект поведения, а незавершённая фича; риск — ложное впечатление, что why-channel тематизирует по типу канала (не тематизирует).

Medium | src/lib/rag-query.js:1775 (humanizeSource) | Искажение атрибуции источника (не падение) на форматах, отличных от «Автор_ГОД_Название» и «Название_-_Автор». Прогон живьём: «2017_Jin_Bayesian»→«Jin Bayesian» (автор Jin потерян, стал частью названия); «ГОСТ_Реклама_2006_Требования»→«Реклама, „Требования"» (ГОСТ отфильтрован surname-regex как all-caps, «Реклама» ложно стала автором); «Report_2019456_data»→«Report, „456 data"» (псевдогод 2019 из середины числа 2019456 расколол имя). | Ассистент Аврора цитирует источник методологии в клиентском тексте: «как отмечает <ложный автор>…» или теряет реального автора. Для нормативного/юр-корпуса (ГОСТ, ФЗ) атрибуция особенно искажается. Серьёзность зависит от фактического состава корпуса узла Б: если там только книги формата Автор_ГОД (Jin/McElreath/Gelman/Hernan) — не проявится; появление нормативных имён или формата ГОД_Автор — проявится. Санитайз секций (===→≈≈≈) применён, prompt-инъекция через имя закрыта.
