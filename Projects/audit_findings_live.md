# Находки аудита 09.08 (durable, дописывается по ходу)

High | src-tauri/src/commands/report.rs:750 | Директива ПРОСАЧИВАЕТСЯ мимо гейта: `- [ВЫСОКАЯ] Приоритизировать канал **{top_ch}** – наивысший ROI в миксе` печатается безусловно, вне цепочки `if mr_refused`. | `optimize.model_reliability.refused=true`, `decompose.channels=[{name:"TV", roi:2.0}]` (ровно данные нового теста) → в разделе РЕКОМЕНДАЦИИ подряд: «Модель не завершила расчёт корректно – рекомендации по переброске отключены» И «[ВЫСОКАЯ] Приоритизировать канал TV – наивысший ROI в миксе». Новый тест это не ловит (проверяет только литерал «Перераспределить бюджет согласно оптимальному плану»).

High | sidecar/econometrica/engines/narrative_adapter.py:711 | `bool((optimize_data.get("model_reliability") or {}).get("refused", False))` — ровно тот паттерн, который первый коммит блока (3ab7b6f) чинил на 300 строк ниже через `isinstance`. | `optimization.json` с `"model_reliability": "unreliable"` (строка вместо словаря — класс входа, объявленный самим автором реальным) → AttributeError в `_derive_narrative_facts` → падает сборка HTML и PPTX целиком, тогда как сверка вердиктов рядом это переживает по замыслу коммита.

Medium | sidecar/econometrica/aurora_pptx/builder.py:2692–2712, 2736–2739 | Гейт закрыл только `answer_body`; `complication_body` и текст вопроса SCQAR флаг не читают. | refused=true + `cut_source_channel`+`scale_destination_channel`+`reallocation_mln>=1`+`expected_lift_pct>=0.5` → слайд SCQAR: «Портфель требует перебалансировки» / вопрос «Как перераспределить бюджет…» / ответ «рекомендации по переброске отключены». Тот же класс дефекта, что чинил B1-fix R-13 (вопрос обещает то, чего ответ не даёт).

Medium | src-tauri/src/commands/report.rs:713 | `unwrap_or(false)` = «нет поля → модель в порядке» (зона неуверенности §4.3 подтверждается). | Проект, обученный до появления `model_reliability`, R-hat 1.2 → Markdown печатает «[ВЫСОКАЯ] Перераспределить бюджет согласно оптимальному плану». Нарушает инвариант 3 handoff (отсутствие проверки ≠ пройденная).

Medium | src-tauri/src/commands/report.rs (build_xlsx) | Расхождение diff и handoff §5: заявлено «Сборка XLSX/Markdown; гейт директивы переброски», фактически `build_xlsx` получил только `#[allow(clippy::too_many_arguments)]`. Теста на XLSX нет. Директивы «Перераспределить» вне `build_markdown` грепом не найдено, но формат остался вне контура гейта и вне регресс-покрытия.

Medium | src-tauri/src/commands/report.rs:739–748 | Гейт не согласован с MQS-лестницей: строки MQS печатаются независимо от `mr_refused`. | Документ с refused=true и MQS в зоне «Хорошее» и выше несёт одновременно «Модель не завершила расчёт корректно» и «результаты модели надёжны для принятия решений». (Достижимость высокого MQS при R-hat≥1.05 через формулу MQS не проверяла — в json-тесте автора MQS=70.)

Проверено, дефекта нет: `refused=True` выставляется только по bad_rhat/bad_div (optimizer_honesty.py:155–173) — ложного срабатывания на тонких данных НЕТ, текст «не завершила расчёт корректно» совпадает с SSOT caveat; гейт в derive_action_headline стоит после guard `not channels or not facts`, ключи facts в builder.py совпадают с `_derive_narrative_facts` (гейт не мёртвый); Python-тесты способны краснеть; clippy-правки (`is_some_and`, `copied`, отступ doc-комментария в auth_sig.rs) поведение не меняют.
