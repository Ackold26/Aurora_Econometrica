# Живые находки внешнего аудита блока 09.09 (клиентские тексты)

High | tools/verify_aurora_html_brand.py:212 ← sidecar/econometrica/aurora_html/strings_ru.json:10 | Сторож бренда HTML-отчёта проверяет `"CONFIDENTIAL" in html`, а строка `brand.confidentiality` переведена в «КОНФИДЕНЦИАЛЬНО» и рендерится именно из неё (aurora_html/builder.py:648) | Сгенерировать HTML-отчёт и прогнать tools/verify_aurora_html_brand.py → пункт «Confidentiality watermark» FAIL на корректном отчёте
High | src/lib/components/pipeline/OptimizeStep.svelte:1953 | `{#if false}` убрал единственное место, где сказано, что абсолютные суммы оптимума относятся к периоду обучения и их надо делить на (training_horizon ÷ forecast_horizon). Побочных эффектов в блоке нет (только статическая разметка), но исчезла количественная инструкция | Режим «аналитик» (он же по умолчанию, planningMode.set('analyst') на строке 137) → шаг «Оптимизация» → видны абсолютные рубли по каналам без единого указания на масштаб периода; клиент переносит их в план следующего квартала → бюджет завышен во столько раз, сколько лет в обучении
СНЯТО (не подтверждено) | sidecar/econometrica/aurora_html/strings_ru.json:8 | В том же блоке `brand`, где переведена `confidentiality`, ключ `tagline` остался «Marketing Mix Modeling · Quarterly Report»; кроме английского, слово Quarterly утверждает квартальный период независимо от фактического окна данных | Ключ brand.tagline в aurora_html/builder.py не встречается (grep пуст) — не доказала, что строка вообще попадает на экран. Как дефект не заявляю, оставляю наблюдением: если tagline рендерится где-то ещё, слово Quarterly утверждает квартальный период независимо от окна данных
Medium | sidecar/econometrica/aurora_pptx/strings_ru.json:12,36,37 + aurora_pptx/master.py:132 | Параллельная таблица строк PPTX осталась английской (CONFIDENTIAL - For {client} only, SITUATION, COMPLICATION), тогда как HTML-таблица переведена. Живой путь выгрузки идёт через builder.py (engines/pptx_export.py → aurora_pptx.build_pptx → AuroraPPTXBuilder), layouts/master/i18n никем не импортируются → сегодня это мёртвый код | Дефекта у клиента нет; риск в том, что при переходе на layouts.py английские подписи вернутся в колоду молча

## Проверено, дефектов не нашла
- CommandPalette.svelte: набор пунктов для Creative Hub не изменился (те же три), порядок сместился (Workflows/Data Chat теперь после «Настройки») — поведение не ломает; иных ссылок на id `nav-workflow`/`nav-data-chat` в коде нет; список недавних команд по id не восстанавливается (обращений к recentCommands в палитре нет); /brands сам редиректит не-CreativeHub на «/».
- OptimizeStep `{#if false}`: внутри блока только статическая разметка — ни подписок, ни вызовов, ни `{@const}`; побочных эффектов не потеряно. CSS `.planning-warn*` остался неиспользуемым, это не дефект.
- SensitivityTornado: словарь PARAM_TYPE_LABELS точно совпадает с шестью значениями `param_type` движка (engines/sensitivity.py:232-281); запасной путь возвращает `p.name`; в подсказке `pd` — исходный объект параметра из sortedParams[dataIndex], а не объект ECharts, поэтому `paramLabel(pd)` там работает штатно.
- DecomposeStep: ячейка разрыва рендерится через formatEfficiencyGap → «+22.1 пп», единица совпадает с подсказкой и с новым заголовком «Разрыв».
- builder.py: все три копии дерева сайдкара (рабочая, _internal, dist) содержат правку — md5 совпадают, доставка не разъехалась.
