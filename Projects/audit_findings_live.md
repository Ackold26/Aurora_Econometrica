# Внешний аудит блока — живой журнал находок

База: 9af3b12a..HEAD. Формат: `severity | файл:строка | суть | воспроизводимый сценарий`.
Аудитор с чистым контекстом, вход: `Projects/audit.diff` + `Projects/handoff.md` + `CLAUDE.md`.

## Находки

**High-1 (ВНЕ диффа, движок)** | `sidecar/econometrica/engines/verdicts.py:79,81,104,106` ← вызов `sidecar/econometrica/engines/decomposer.py:1296` | вердикт канала в режиме count-KPI сравнивает разрыв, заданный в процентных пунктах (`decomposer.py:1239` → `round(share_of_effect - share_of_spend, 1)`, напр. −38.5), с порогами в долях (0.05 / 0.10) — трёхступенчатая градация схлопывается в верхнюю метку | сценарий: проект с KPI-счётчиком (упаковки, лиды), канал с долей эффекта 20.2 % и долей бюджета 20.0 % → `efficiency_gap = +0.2` пп (фактически сбалансирован), CPU ниже ценности единицы → таблица «Детализация по каналам» уверенно печатает «Высокоэффективен (CPU значительно ниже ценности)». Он же с −0.2 пп при незаданной ценности единицы (`kpi_unit_cost is None`, штатный случай, см. `_money_roi_na` в `decomposer.py:1294`) → «Слабый вклад». Метка «Сбалансирован» достижима только в полосе |gap| ≤ 0.1 пп. Денежная ветка (`compute_roi_verdict`, пороги `GAP_*` в пп) не задета.

**Medium-1** | `sidecar/econometrica/engines/decomposer.py:186,199` + `channel_action.py:32,41,50` | новая форма разрыва применяется только при ПЕРЕСЧЁТЕ: тексты движка лежат в `results/decomposition.json`, поэтому в проекте, посчитанном прежней сборкой, на одном экране две формы и две единицы | сценарий: открыть в новой сборке проект с `results/decomposition.json` от версии до 08.09 → таблица «Детализация по каналам» (живой формат фронта) показывает «−38.5 пп», а карточка «Главная рекомендация» и подсказка канала (текст из файла) — «(gap -38пп)» / «gap +32пп» с округлением до целых. Подтверждает зону неуверенности 5 handoff.

**Medium-2** | `src/lib/components/pipeline/ValidateStepV13.svelte:220,249,1291,1307` | текст ошибки проверки данных — локальное состояние компонента, компоненты шагов не перемонтируются при смене проекта → ошибка проекта А остаётся на экране проекта Б и вдобавок глушит новое пояснение о третьем состоянии медиаплана (условие `&& !validateError`) | сценарий: в проекте А на шаге «Проверка данных» выбрать нечитаемый файл → красный блок «Ошибка чтения файла: …» → переключиться на проект Б → на шаге «Проверка данных» Б висит ошибка А; если у Б состояние поиска плана `unavailable`, пояснение «Проверить, есть ли в файле план… не удалось» не показывается вовсе. Тот же класс, что закрытый в блоке Medium-2 для `DecomposeStep`.

**Medium-3** | `src/lib/components/pipeline/DecomposeStep.svelte:466-478` | добавленный обратный переход покрывает только `done → idle`; из состояния `error` шаг не выходит ни при появлении данных, ни при их пропаже | сценарий: в проекте А декомпозиция падает («Модель не найдена» после 4 попыток) → `stepState='error'` → переключиться на проект Б с готовой декомпозицией на диске и без модели в памяти (`channelParams` пуст → эффект пересчёта не срабатывает) → на шаге «Декомпозиция» проекта Б показано сообщение об ошибке проекта А, восстановленные результаты Б не рисуются (`stepState === 'done' && data`). При наличии модели у Б путь самолечится автопересчётом — поэтому Medium.

**Low-1** | `src/lib/project-state.js:1197-1209` | подписка на `lastTrainedConfig` срабатывает немедленно при загрузке модуля со значением `null`, когда `activeProjectId` ещё `null` → `localStorage.removeItem('aurora-last-trained-config')` стирает СТАРЫЙ единый ключ | сценарий: действующий пользователь ставит новую версию и открывает окно → снимок конфигурации обучения стёрт до того, как его мог бы прочитать кто-либо. Зона неуверенности 2 handoff подтверждена и усилена: миграция старого ключа невозможна и задним числом.

## Проверено и чисто

- `scenario.py`: `resolve_holiday_column` = проекция `user_covered_auto_holidays` с приоритетом точного совпадения → каноническое имя разрешается в себя, `normalize_holiday_name` совпадает, авто-инжект идёт дальше без изменений (инвариант 2 держится). Ветка «не разрешилось однозначно» осталась выше новой проверки, порядок ветвей корректен, журнал согласован с поведением.
- `validator.py`: у `validate_data` ровно 4 выхода `status: error` до записи (494/504/509/518), все закрыты; успешная запись (588) стоит СРАЗУ после чтения файла и детекции хвоста, до разбора колонок → путей «прочитали, но не записали» не осталось. Запись обёрнута в `try/except`, текст отказа не меняется, гейт абсолютного пути тот же. Читатели третьего состояния (`optimizer.py:1569`, `aurora_html/sections.py:698,1562`) используют `is True` → `None` не выдаётся за «плана нет».
- `project-state.js`: порядок «`activeProjectId.set` → сброс» соблюдён на ВСЕХ путях (ProjectSelector 79/92-93, 136→143, 211-212; ImportStep 57-58; pipeline/+layout 284→289). `resetForNewAnalysis` первым делом ставит `activeProjectId=null`, поэтому чужой per-project ключ не стирается. Защёлка `_lastRestoredPid` обнуляется при возврате в проект → ранний выход по сверке актуальности не оставляет проект без восстановления.
- `project.rs` + `build.rs:38-48`: `AURORA_APP_IDENTIFIER` берётся из `tauri.conf.json` с наложением `TAURI_CONFIG` (оверлей локальной редакции) → каталог чтения настройки совпадает с `app_config_dir()` в ОБЕИХ редакциях; расхождения между редакциями нет.
- `SensitivityTornado.svelte`: объявленный тип совпадает с фактическим выпуском `engines/sensitivity.py:662-672` целиком (`name`, `baseline_value`, `low_variation{value,delta_roi_pct}`, `high_variation{…}`, `sensitivity_pct`, `channel`, `param_type`); других мест, читающих объекты как числа, в компоненте нет (единственное прочее употребление — `sensitivity_pct` в подсказке, строка 155).
- Единая форма разрыва во фронте: все места показа (`DecomposeStep` таблица+рекомендация+подсказка, `ChannelComparisonChart`, `ExpertDecomposePanel`) идут через `formatEfficiencyGap`; `insights-rules.js:1896` и `ReportStep.svelte:336,340` используют разрыв только для классификации, своего «%» не печатают.
