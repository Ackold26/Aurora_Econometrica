# Живой журнал находок внешнего аудита

Формат: `severity | файл:строка | суть | сценарий`

- ПРОВЕРИТЬ | modeler.py:~1833 vs ~1886 | пикл модели save_model_safe(model_data) СОХРАНЯЕТСЯ ДО штампа; если model_data['diagnostics'] — тот же словарь, в пикле нет honesty_verdict/model_reliability | экраны Декомпозиция/Оптимизация читают modelData.diagnostics.honesty_verdict → молчат = регресс ровно того, что чинили
- ПРОВЕРИТЬ | narrative_adapter.py:+1006.. | блок сверки происхождения ВНЕ try/except (в отличие от блока вердикта); optimize_data.get(...) при None → AttributeError | проект без оптимизации / отсутствующий optimize_data → падение сборки отчёта

## Проверено, снято
- СНЯТО | narrative_adapter.py:891 | `optimize_data = optimize_data or {}` — падения на None нет
- СНЯТО | persistence.py:788 | write_pkl_sha256_sidecar возвращает hex-строку — JSON-сериализация не ломается
- СНЯТО | modeler.py:1597 | пикл модели НЕ содержит ключа diagnostics вовсе (он только в latest-params.json:1903 и в возврате :1920) — устаревшей копии в пикле нет
- СНЯТО | report.rs:1283-1334 | лист качества: строки 9,10,11,12,13 — жёсткие константы, наложения на строку 13 нет

## Находки
- High | report.rs:568,632,1319,1504 | Markdown и XLSX ПО-ПРЕЖНЕМУ печатают вердикт из optimize["model_reliability"], HTML/PPTX — из диагностики; единый источник (цель §1 handoff) не достигнут, расхождение только объявляется | переобучение (uncertain) поверх старой оптимизации (reliable): в одном пакете XLSX «модель надёжна», HTML «требует осторожности» + предупреждение
- High | utils/optimizer_honesty.py stamp fallback + sections.py:463 | аварийная ветка штампа кладёт verdict='unknown' с честным caveat «Не удалось проверить надёжность», но НИ ОДИН выход его не показывает (HTML рисует плашку только на unreliable/uncertain; report.rs:43 схлопывает пустой/иной вердикт в «надёжна») | model_reliability_verdict бросает исключение (например metrics не dict) → клиент получает отчёт без единого признака, что надёжность не проверялась
- Medium | optimizer.py:+1644 / narrative_adapter.py:+1010 / report.rs:63 | у проекта, обученного до блока, обе половины сверки мертвы навсегда: подпись не пишется (`if _model_fingerprint`), вердикта в диагностике нет; после переобучения на 2.4.5 работает только половина вердиктов | старая optimization.json без подписи + переобучение с тем же состоянием вердикта ('reliable'→'reliable') → отчёт молча склеен из двух моделей, предупреждения нет
- Medium | report.rs:2191 | truncate_to_excel_cell_limit применён только к «Определение»; «Термин» (та же непроверяемая величина с фронта, соседняя запись row,0) не защищён | термин >32767 симв. из glossary.js → build_xlsx возвращает Err → книга не сохраняется вовсе
- Medium | narrative_adapter.py:+1013 / report.rs:71-78 | вердикты сравниваются только по состоянию: пересчёт MQS, меняющий причины/оговорку без смены состояния, расхождения не поднимает | recompute_mqs на uncertain-модели → HTML/PPTX новая оговорка, Markdown/XLSX старая, предупреждения нет
- Low | modeler.py:+1886, ols_modeler.py:+499 | штамп и запись диагностики вне project_lock (внутри только save_model_safe) | два одновременных обучения одного проекта: подпись в диагностике перестаёт отвечать пиклу на диске
