# Живые находки внешнего аудита блока 2.4.9 (14.08.2026)

Формат: `severity | файл:строка | суть | сценарий отказа`

High | src-tauri/src/commands/campaign.rs:~470 (`persist_step_exports`) | Отказ `read_dir`/`create_dir_all`/`file_type` по-прежнему проглатывается (`if let Ok(entries)`, `let _ = create_dir_all`) — потеря остаётся молчаливой | Каталог `exports` временно недоступен (блокировка антивирусом, sharing violation, гонка с записью) → `read_dir` возвращает Err → `failed` пуст, `copied` пуст, в журнале «Persisted 0 exports (0 failed)», `lib.rs` не печатает тревогу, `close_session` стирает рабочий каталог. Заявленный инвариант «отказ копирования не теряется» держится только для отказа самого `fs::copy`.

High | src-tauri/src/sidecar_runtime.rs:650 `observe_process` + econ_sidecar.rs `kill_sidecar_from_state` | Дескриптор процесса закрывается ДО снятия, снятие идёт по номеру через `taskkill /T /F` — окно TOCTOU между проверкой и снятием | Между `observe_process` и порождением `taskkill` (десятки мс) наш зомби завершается, Windows отдаёт номер новому процессу → `taskkill /PID n /T /F` снимает чужой процесс вместе со всем деревом. Удержание уже открытого дескриптора номер пинит; здесь он закрыт намеренно.

High | src-tauri/src/sidecar_runtime.rs:583 `should_kill` | Проверяется продукт из НАШЕГО файла состояния, но не принадлежность НАБЛЮДАЕМОГО процесса нашей установке: совпадение имени образа `econometrica-sidecar.exe` считается достаточным | На машине сосуществуют облачная и локальная редакции (CLAUDE.md: `com.aurora.econometrica` и `com.aurora.econometrica.local`, тот же exe движка). Облачная падает (файл состояния остаётся), номер её движка переиспользуется движком ЛОКАЛЬНОЙ редакции; облачную запускают в пределах 300 с → все проверки проходят → `taskkill /T /F` снимает чужой расчёт. Ровно тот класс, который правка закрывала.

Medium | src-tauri/src/commands/campaign.rs:57 | При коллизии имён файл ложится как `report (2).md`, а в `result.copied` кладётся исходное `report.md` | Любой потребитель `copied` (перечисление сохранённого шага, ссылка в отчёте) получает имя, которого на диске нет.

Medium | src-tauri/src/commands/campaign.rs:47-56 | Введённое переименование меняет поведение при ПОВТОРНОМ прогоне шага: раньше перезапись, теперь прежний файл остаётся каноническим `report.md` | Шаг сценария перезапущен после ошибки → в `steps/<id>/` лежат и устаревший `report.md`, и новый `report (2).md`; передача в следующий шаг/сводка подхватывает устаревший.

Medium | src-tauri/tests/guard_no_regressed_cpd77_cpd79.rs:1730 `strip_comments` | Символьный литерал `'"'` рассинхронизирует разбор: кавычка открывает «строку», и всё до следующей кавычки в файле считается содержимым строки, то есть комментарии в этом промежутке НЕ отбрасываются | В любом файле `src/` появляется `'"'` (разбор CSV, экранирование) → следующие за ним объяснительные комментарии со словами `netstat`/`icacls`/`tasklist` (их в `econ_sidecar.rs` и `win_acl.rs` много) попадают в скан → сторож краснеет на собственном объяснении → его снимут вместе с защитой (риск назван в handoff как недопустимый).

Medium | src-tauri/src/sidecar_runtime.rs:540 `image_matches` | Сверка `stem.starts_with(hint)` вместо равенства | Процесс `econometrica-sidecar-backup.exe` (или у соседнего продукта `rag-server-old.exe`) проходит проверку образа как «наш».

High | src-tauri/src/sidecar_runtime.rs:452 `KILL_TIME_TOLERANCE_SECS` | Окно допуска симметрично (±300 с), хотя риск резко асимметричен: по документированному порядку операций `created_at` ВСЕГДА ≤ `started_at`, значит верхняя граница нужна лишь под дрожание часов | Переиспользование номера процесса на занятой машине происходит за секунды, а не за часы; окно +300 с оставляет открытыми пять минут, в которые чужой процесс с нашим именем образа снимается. Верхнюю границу можно сжать до +2..5 с без единого ложного пропуска.

Medium (подтверждено фактом) | src-tauri/tests/guard_no_regressed_cpd77_cpd79.rs | Рассинхронизация `strip_comments` на `'"'` уже происходит: `durable_store.rs:640,641`, `claude.rs:1555,1627,1628,1630,1638,1640,1645`, `crypto/fingerprint.rs:179` | В каждой из этих точек разбор входит в «строку» и держится до следующей кавычки в файле; комментарии внутри промежутка не отбрасываются. Сегодня запрещённых слов там нет — сторож зелёный случайно, а не по устройству.

Low | src-tauri/src/session/manager.rs:132 | Починка прав зависит от того, что `durable_store::init()` отработал ДО `SessionManager::new()` | Если порядок обратный, `resolve_path("")` даёт фолбэк `%LOCALAPPDATA%\<CARGO_PKG_NAME>`, а не каталог идентификатора; сторож цели отвергнет путь и починка не выполнится (только запись в журнал). Проверить порядок вызовов в `lib.rs::run()`.

Medium | src-tauri/src/econ_sidecar.rs (`start_sidecar`, ветка протухшего состояния) | `delete_state_file` вызывается и тогда, когда `should_kill` вернул Skip | Не удалось прочитать время создания (нет прав) → зомби не снят, но файл состояния стёрт → сведений о номере больше нет, зомби неснимаем до перезагрузки, порт держится.
