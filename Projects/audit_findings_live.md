# Внешний аудит блока fbc80994..HEAD — живой журнал находок

Формат: `severity | файл:строка | суть | сценарий отказа`

## Находки

High | src/routes/settings/+page.svelte:373-386 (submitFeedback + startFeedbackCooldown) | при недоступном localStorage подтверждение отправки не показывается вовсе: fbStatus='sent', затем startFeedbackCooldown() читает пустую отметку, получает 0 и тут же сбрасывает статус в 'idle' | приватный режим / переполненное хранилище / заблокированные данные сайта → человек нажал «Отправить», обращение ушло, поле очистилось, кнопка снова «Отправить», ни строки подтверждения → отправляет повторно 2-3 раза

High | src/routes/settings/+page.svelte:308-309 + src-tauri/src/commands/feedback.rs | заявленного якоря нет: комментарий ссылается на «RATE_LIMIT_SECS в commands/feedback.rs», в файле такой константы нет (есть только MESSAGE_MAX_CHARS и FB001); grep по src+src-tauri не находит ни RATE_LIMIT_SECS, ни FB-002/FB002 | если запрета нет и в aurora_core — кнопка блокируется на 60 с без причины со стороны программы, а ветка текста FB-002 мертва; если окно в крейте длиннее 60 с — человек ждёт минуту, жмёт и получает отказ, текст которого обещает обратное

Medium | src/routes/settings/+page.svelte:322-329 (feedbackErrorText) | все отказы, кроме FB-002, схлопываются в «связи с нашим сервером сейчас нет»; аккуратно написанный текст FB-001 (предел 4000 знаков) и «Сообщение не может быть пустым» до человека не доходят никогда | текст длиннее 4000 знаков доезжает до команды (вставка мимо maxlength, будущая правка поля) → Rust отвечает точным «сократите текст», человек читает «нет связи», копирует текст и идёт чинить интернет

Medium | src/lib/components/ExpandableCard.svelte:316-325 | `.overlay-content :global(*:has([data-echart]))` навязывает `flex-direction: column` КАЖДОМУ предку графика на любой глубине, без разбора его собственной раскладки | карточка, внутри которой два графика стоят рядом (строка/сетка), в развороте перестроится в столбец и сожмёт оба. Наличие такой карточки в продукте по коду не подтверждала — проверять живым разворотом каждой карточки с графиками

Low | src-tauri/src/commands/updater.rs:194,229 | UP-001 («сервер недоступен, проверьте подключение») вешается и на отказ `reqwest::Client::builder().build()` — это отказ построения клиента, а не сети | сломан TLS-бэкенд/системное хранилище сертификатов → человек читает «проверьте подключение к интернету», хотя интернет исправен

Low | src-tauri/src/commands/updater.rs:165 | комментарий «единственное место, где рождается UP-005» неверен: UP005 рождается и в `check_supabase` (разбор ответа edge-функции) | последствий для клиента нет; ложный след при следующем разборе. Сторож требует UP005 в обеих функциях, договор держится

Low | src/lib/updateErrorText.js:103-112 (clientDetail) | метка ищется по ВСЕЙ строке, код — первым совпадением `[UP-\d{3}]`; склейка вложенного отказа в общий текст покажет деталь вложенного как объяснение всего провала | сегодня не воспроизводится: в `last_err` цикла загрузки попадают только непомеченные строки. Хрупкость договора, не дефект

Low | src/lib/components/Tooltip.svelte:81-116 (portalToBody) | пузырь ушёл из `.tooltip-wrapper` в `<body>` — навести на него мышь больше нельзя: движение к пузырю даёт mouseleave обёртки и скрытие по таймеру | длинная подсказка, человек ведёт мышь к тексту, чтобы дочитать или выделить → подсказка исчезает

Low | src/lib/components/Tooltip.svelte:213-228 ($effect) | `place()` вызывается на каждое событие прокрутки (capture: true) без rAF-сглаживания, два getBoundingClientRect на событие | прокрутка длинного отчёта с открытой подсказкой — лишняя работа раскладки на каждый кадр прокрутки

## Гипотезы без сценария

- `holiday_calendar_ru.py:184-190`: `window_kind: 'calendar_period'` объявлен для окна, начинающегося за 7 дней ДО события. Использование `window_kind` вне диффа не смотрела — если он управляет формой признака (pre-event профиль), классификация может исказить вклад.
- `sidecar/econometrica/build_sidecar.py:431-436`: `purge_test_residue` работает по `DIST/OUTPUT_NAME`; совпадает ли это с каталогом, который потом копируется в `sidecar/econometrica/_internal/`, по коду блока не видно.

## Проверено и снято

- **Пасхалия** — пересчитала формулу Гаусса независимо (2019-2030): 2024-05-05, 2025-04-20, 2026-04-12, 2021-05-02, 2019-04-28, 2027-05-02, 2030-04-28 — сходится. Сдвиг `year//100 - year//400 - 2` = 13 (1900-2099), 14 (2100-2199) — верно. Граница марта/апреля верна, окно −7…+3 = ровно 11 дней, крайние Пасхи (4 апреля - 9 мая) перехода через год не дают. Заявленные доли пересечения с майскими (11/11, 8/11, 4/11, 0) пересчитаны и совпали.
- **Остатки старой сигнатуры `submit_feedback`** — один вызывающий (`settings/+page.svelte:378`) и одна регистрация (`lib.rs:4075`), оба без `contact`; слова `contact` в настройках нет. Путь отправки цел.
- **Утечка сверх хеша в хвосте обращения** — уходит `hash_fingerprint(get_machine_fingerprint())`, ключ продукта из `detect_product()` (константа от CARGO_PKG_NAME), версия, локальное время со смещением. Имени пользователя, имени машины, путей нет; тест дополнительно проверяет отсутствие COMPUTERNAME.
- **Усечённая строка / пустая деталь / чужая метка** в `describeUpdateError` — `[UP-004] [клиенту]` без текста даёт '' и падение на табличный текст; строка без метки — таблица; без кода и метки — запасной текст. Чужая метка (`[LI-005] [клиенту] …`) даст деталь без кода — это by design.
- **`resize: both` у `.overlay-card`** — `overflow: hidden` задан, требование спецификации выполнено, ручное изменение размера работать будет.
- **Сужение ресурсов Tauri** — пути назначения сохраняются (`_up_/sidecar/econometrica/...`), каталог копируется рекурсивно; `--add-data data:data` кладёт реестр в `_internal/data`, куда и смотрит `Path(__file__).parent.parent`.
- **`kpi_display._load()`** — поведение вызывающих не изменилось, исключение по-прежнему летит, добавлена запись в журнал. Оговорка: ловится только FileNotFoundError, битый JSON по-прежнему пройдёт молча.
