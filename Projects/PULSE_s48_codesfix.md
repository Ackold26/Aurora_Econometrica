# PULSE s48 codesfix

Задача: проверить, действительно ли коды AU-* (онлайн-авторизация) и SY-* (система) недостижимы в продукте Econometrica, и если да - убрать их разделы из справки error-codes.html, починить сторож-тест help_error_codes_page_matches_error_code_enum_both_ways так, чтобы он сверял справку со списком ДОСТИЖИМЫХ кодов (не полным платформенным перечнем), и доказать живость сторожа двумя красными прогонами.

План:
1. Проверить факт: grep по всему дереву (Rust, интерфейс, python sidecar) на ErrorCode::Au*/Sy*, а также проверить пути ответа сервера/общего обработчика.
2. Если подтвердилось - убрать разделы AU-*/SY-* из error-codes.html.
3. Найти и починить тест-сторож в errors.rs.
4. Два красных прогона с восстановлением состояния, зафиксировать текст ошибок.

03:55 - старт, начинаю шаг 1 (проверка достижимости).

04:05 - Шаг 1 завершён, проверено фактом:
- ErrorCode::AU001..005 и SY001..003 в src-tauri/src/ встречаются РОВНО один раз каждый — только внутри ручного списка real_codes в самом тесте errors.rs (строки 56,59). В боевом коде продукта (commands/, business logic) НИ РАЗУ не сконструированы.
- Прямой grep по сырым строкам "AU-00[1-5]"/"SY-00[1-3]" по всему дереву (искл. target/node_modules/.git) находит их только в error-codes.html и в чужих Project-заметках аудита — нигде в исходниках как литерал.
- src-tauri/src/commands/online_auth.rs (1218 строк) - модуль онлайн-авторизации Эконометрики - вообще не использует ErrorCode: у него свой стековый статус-протокол ("ok"/"blocked"/"cached"/"offline"/"denied"/"unknown_machine"), сервер отвечает AuthResponse{status, message, ...} БЕЗ поля кода ошибки.
- Сверился с определением ErrorCode в aurora_core (пиненная ревизия 35d8345, тег aurora_core-v0.1.0): комментарий крейта - "Structured error codes for Aurora AI Agency" - AU-*/SY-* спроектированы для другого продукта (Agency), не для Эконометрики.
Вывод: недостижимость подтверждена независимо. Перехожу к шагу 2 - вычистка error-codes.html.

04:02 - Шаг 2 сделан (AU-*/SY-* разделы вычищены из error-codes.html), шаг 3 - переписала сторож на автовывод достижимых кодов обходом src-tauri/src (walkdir) через regex по реальным вызовам ErrorCode::. При первом прогоне сторож покраснел НЕ на AU/SY (их я уже убрала), а на FB-002: `ErrorCode::FB002` не встречается НИГДЕ в продукте (grep подтвердил - только FB001 используется в feedback.rs, 3 раза), а в старом ручном списке FB002 присутствовал и в error-codes.html он документирован. Это тот же класс дефекта, что и AU-*/SY-*, но за пределами явного задания (велено трогать только AU-*/SY-*, "остальное не трогай").
Остановилась, написала team-lead с находкой и вопросом - убрать ли строку FB-002 (не всю категорию FB, FB-001 реальный) тем же приёмом, или оставить как есть и подстроить сторож. Жду ответа перед шагом 4 (два красных прогона нужен зелёный багслайн).

04:07 - team-lead подтвердила: FB-002 убрать (механизма лимита отправки в продукте нет вообще, форма открывается в браузере). Убрала строку FB-002 из error-codes.html (категория и FB-001 остались). Сторож перезапущен - ЗЕЛЁНЫЙ. Заодно провела полный аудит ВСЕХ категорий приёмом сторожа (grep ErrorCode:: по всему src, искл. errors.rs): единственный код с нулём вызовов кроме уже убранных AU-*/SY-*/FB-002 - отсутствует, полный список 33 оставшихся кодов имеет ≥1 реальный вызов каждый. Отчитываюсь team-lead и перехожу к шагу 4 - два красных прогона сторожа.

04:10 - Шаг 4 сделан:
Прогон 1 (выдумка): вписала <tr><td><code>ZZ-999</code></td>...</tr> в категорию FB. Сторож покраснел:
  "справка называет коды, которых нет среди достижимых в продукте (выдумка или код недостижимого класса вроде AU-*/SY-*): [\"ZZ-999\"]"
  Убрала строку - вернула файл в прежний вид.
Прогон 2 (пропажа реального): убрала строку LI-005 (реально достижим, 2 вызова). Сторож покраснел:
  "в продукте реально достижимы коды, не описанные в help-econometrica/error-codes.html: [\"LI-005\"]"
  Вернула строку на место.
Финальный прогон - зелёный (help_error_codes_page_matches_error_code_enum_both_ways ... ok, + coded_keeps_the_bracketed_format_documented_to_clients ... ok). Задача закрыта, отчитываюсь team-lead.
