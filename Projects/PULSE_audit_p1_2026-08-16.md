# Маячок аудита P1 (frontier), 2026-08-16

## Задача (пересказ своими словами)
Я — внешний аудитор блока `feat/econ-p1-winning` (4 записи поверх `6fa2498`) в дереве
`D:\Docs\Aurora_Ai\Dev\Aurora_Econometrica_thinwt`. Ничего не правлю, не коммичу, веток не завожу —
только читаю код/документы, запускаю зонды на чтение и записываю находки в
`Projects\AUDIT_P1_FINDINGS_2026-08-16.md` немедленно по мере формулировки.

Каждая находка = severity + файл:строка + суть + КОНКРЕТНЫЙ сценарий отказа у клиента.
Без сценария — в раздел «замечания вкуса». Достижимость кода проверять до записи находки.

## План
1. Создать durable-файл находок + маячок (сделано первым действием).
2. Прочитать документы замысла: FRONTIER_DESIGN, AUTONOMOUS_WORK_STATE_P1_WINNING, FIX_FRONTIER_ENGINE.
3. Прочитать diff `Projects/audit_p1_frontier.diff` целиком.
4. Глубокая проверка по зонам: frontier.py (сетка/исходы/интервал/None-sampler),
   единицы и период, bounds.aggregate_sales, inverse.py (аддитивность), server.py (точка входа),
   builder.py (слайд обещаний, нумерация/оглавление), клиентские тексты, сторожа (тесты).
5. Зонды на чтение: запуск тестов из `sidecar/econometrica`, разбор реальной модели (только чтение).
6. Итог: раздел «Проверено и находкой НЕ является» + строка «АУДИТ ЗАВЕРШЁН» со временем.

## Журнал
- 13:09 — старт, durable-файл и маячок созданы первым действием.
- 13:1x — прочитаны FRONTIER_DESIGN, AUTONOMOUS_WORK_STATE, FIX_FRONTIER_ENGINE, весь diff (2520 строк),
  файлы целиком: frontier.py, inverse.py, bounds.py, server.py (точка входа + save_kpi_settings),
  builder.py (s10d + нумерация), три тест-файла, изменённые src/*.js/svelte и help.
- 13:2x — зонды на реальной модели (только чтение): фронтир при kpi=profit и марже 50%/30%,
  коридор с/без project_dir, состав unit_costs, активность каналов, поведение extrapolation_severity.
- 13:2x — прогоны: pytest фронтир+коридор 22 passed; vitest два фронт-файла 67 passed.
- 13:3x — записаны находки F-01..F-17, раздел «проверено», аудит закрыт.
