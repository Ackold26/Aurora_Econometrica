# Живой headless-прогон ассистента «Аврора» — 2026-07-12

> Доказательство всей облачной цепочки БЕЗ окна: реальные данные движка →
> tier2-промпт с реальным каноном узла Б → реальный Claude Sonnet (подписка) →
> страж INV-50. Скрипт: `tmp/avrora_live_run.mjs`.

## Что реально задействовано (не мок)
- **Данные:** kagocel decomposition + demo_probe2 optimization/model-diagnostics (реальные артефакты движка).
- **Канон:** 4 живых хита узла Б (McElreath, Gelman ×2, Hernán-Robins) из сохранённого RAG-ответа.
- **LLM:** Claude Sonnet через claude.ai-подписку (баланс API-ключа исчерпан → снят для дочернего процесса).
- **Страж:** прод `insights-grounding.js`.
- Промпт: 13 506 символов, секции «Канон методологии» + «Надёжность модели» присутствуют.

## Вердикт
- ✅ **Страж INV-50: НОЛЬ негрунд-чисел** — ассистент не выдумал ни одного числа.
- ✅ Все правки промпта подтверждены живым ответом:
  - **Правило 1/INV-50:** «совокупного ROI канала в фактах модели нет» — честно сказал об отсутствии, не выдумал.
  - **Правило 11 (переводчик):** «маржинальный вклад: сколько принесёт именно следующий рубль» — mROI на человеческом.
  - **Правило 4/12 (диапазоны):** «опирайтесь на доверительные интервалы», «не воспринимать как точный расчёт».
  - **Методология (Sharp/Binet-Field, прав. 9):** «ТВ — охватный канал… отложенный и защитный эффект».
  - **Надёжность verbatim:** дословно процитировал honesty-вердикт OLS-режима.
  - **Структура (прав. 10):** вывод → что показывает → предостережения → что делать.

## Находки для петли улучшения
1. **Атрибуция источника не выражена явно.** Промпт требует «цитируй с атрибуцией (по Jin 2017…)», но ответ использовал методологию без сноски на книгу. (Проверка в скрипте искала имя файла-источника — ложно-отрицательная; но формальной атрибуции «по Макэлриту/Гельману» в ответе действительно нет.) → усилить правило атрибуции ИЛИ признать, что для устного ответа сноска необязательна.
2. **RAG вернул общие байес-книги, не MMM-специфику.** Для вопроса «почему такой ROI канала» релевантнее был бы Jin 2017 (carryover/shape), а вернулись McElreath/Gelman (общий Bayesian workflow). → тематизировать RAG-запрос (добавлять к вопросу домен-термины канала: saturation/adstock/response curve), а не слать голый вопрос пользователя.

## Что осталось для ПОЛНОГО живого прогона
- **Живой RAG в рантайме** (econ_rag_search через туннель :8801) — нужен passphrase ключа узла Б (`! ssh-add ~/.ssh/aurora_cloudru`). Логика RAG уже доказана (контракт corpora 200 + этот прогон на реальных хитах).
- **Прогон в окне** (клики InsightsPanel, Tauri dev + MCP-мост) — UI-интеграция; лучше синхронно.
