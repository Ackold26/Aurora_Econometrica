# PULSE — зонд порядка приёма файла vs записи media_plan.json (2026-09-08)

## Задача своими словами
Нужно доказать факт: копия файла пользователя в каталог проекта (results/) происходит ДО вызова validate_data (тогда старый media_plan.json может врать про новые данные — опасно) или ПОСЛЕ/только при успехе проверки (тогда всё безопасно). Плюс прямой зонд: 4 ранних выхода validate_data (нет файла, плохой формат, битое чтение, пустой csv) — что происходит с media_plan.json в каждом случае.

## План
1. Найти команду валидации в src-tauri (Rust) — порядок copy/validate.
2. Найти фронтенд-вызов (ImportStep.svelte / project-state.js) — порядок accept/validate.
3. Прочитать validator.py — где ранние выходы, где _write_media_plan_state.
4. Прямой зонд: временный проект, 4 сценария, читать media_plan.json после каждого.
5. Записать PROBE_mediaplan_order_2026-09-08.md с вердиктом.

## Ход
- [12:06] Старт, маячок создан.
- [~12:20] Код прочитан: validator.py:445-550 (validate_data, 4 ранних выхода без записи), validator.py:368-442 (_write_media_plan_state), econometrica.rs:183-196 (econ_validate — просто пробрасывает file_path/project_dir в сайдкар, копирования файла НЕТ), project.rs:496-524 (project_upload_data — команда КОПИРУЕТ файл в <project_dir>/data/, но НЕ вызывается нигде во фронтенде — мёртвый код), ImportStep.svelte:265-300 (loadFile → econ_data_preview — только предпросмотр, filePath остаётся ВНЕШНИМ путём пользователя, копии в каталог проекта НЕТ), ValidateStepV13.svelte:230-244 (autoRunValidate → econ_validate с filePath=imp.file — тем же внешним путём).
- Вывод по коду: "принятие" и "проверка" — ОДИН и тот же вызов (econ_validate читает файл напрямую с внешнего пути пользователя в момент проверки); отдельного шага "скопировать в каталог проекта ДО проверки" в текущем UI-потоке нет. Реальный риск сдвинут: не "подмена в каталоге до проверки", а "повторная проверка того же project_dir новым/битым файлом при живом СТАРОМ media_plan.json от прошлого успешного прогона".
- [~12:25] Начинаю прямой зонд validate_data на 4 ранних выходах.
- [~12:32] Зонд прогнан: все 4 ранних выхода подтверждены — media_plan.json.unchanged=true во всех случаях (байт-в-байт). Пишу финальный отчёт PROBE_mediaplan_order_2026-09-08.md.
- [~12:35] Готово, отчёт записан.
