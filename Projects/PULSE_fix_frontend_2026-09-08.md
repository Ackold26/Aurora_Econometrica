# МАЯЧОК — правки интерфейса Aurora Econometrica (2026-09-08)

## Задача своими словами
Чиню шесть дефектов фронтенда (SvelteKit 5 + Tauri v2) перед показом покупателю: состояние
чужого проекта переживает смену проекта, гонка асинхронного восстановления, односторонняя
реактивность шага «Декомпозиция», потеря третьего состояния медиаплана, разнобой единиц у
efficiency_gap и панель подсказок, которая врёт о состоянии. Плюс диагностика ГРАФ-1 без правки.
Git не трогаю, sidecar/ не трогаю, сборку не запускаю.

## План по шести пунктам
1. High-3 — сброс `lastTrainedConfig` / `forecastContext` / `optimizeLiveState` на ВСЕХ путях смены проекта + решение по ключу localStorage.
2. Medium-3 — проверка актуальности проекта после каждого `await` в `restoreProjectResults` / `hydrateRolesFromProjectIfEmpty`.
3. Medium-2 — обратный переход `done → idle` в DecomposeStep.
4. Low-2 + Low-3 — три состояния медиаплана у фронтового читателя + явное снятие `mediaPlanDetected`.
5. ЧИСЛА-1 — одна единица (процентные пункты) и одно округление для `efficiency_gap`.
6. ТЕКСТ-1 — панель подсказок на шаге «Отчёт» не говорит «загрузите данные».
+ ГРАФ-1 — только диагностика, вывод с числами.

## Отметки
- [x] СТАРТ — 2026-09-08, маячок создан, читаю фактуру (AUDIT_zone4, LIVE_RUN).
- [x] Пункт 1 (High-3) — `project-state.js`: общая `resetProjectScopedState` на всех путях смены проекта; ключ `aurora-last-trained-config` разделён по проектам. Сторож + мутация: красный.
- [x] Пункт 2 (Medium-3) — сверка `activeProjectId` после каждого ожидания в `restoreProjectResults` и `hydrateRolesFromProjectIfEmpty`. Сторож + мутация: красный.
- [x] Пункт 3 (Medium-2) — обратный переход `done → idle` в `DecomposeStep`. Сторож (рендер) + мутация: красный.
- [x] Пункт 4 (Low-2, Low-3) — стор `mediaPlanProbeStatus` (три состояния) + пояснение на Валидации; `mediaPlanDetected` снимается явно. Сторож + мутация: красный.
- [x] Пункт 5 (ЧИСЛА-1) — `formatEfficiencyGap` в `format-numbers.js`, три места отрисовки + два тултипа. Сторож + мутация: красный.
- [x] Пункт 6 (ТЕКСТ-1) — корень: индекс шага «Отчёт» уехал с 5 на 6 после вставки «Планирования». Номера шагов взяты из общего `STEP`. Сторож + мутация: красный.
- [x] ГРАФ-1 — диагноз: `low_variation`/`high_variation` — объекты `{value, delta_roi_pct}`, компонент читал их как числа → NaN → столбцов нет. Правку делает другой исполнитель (файл изменён в 14:00).
- [x] Приёмка: `npm test` 1489/1489 (было 1475 + 14 новых). `npm run check` — 2 ошибки, обе в `SensitivityTornado.svelte` чужой правки, мои файлы чисты.

## Статус на 14:20 (по запросу ведущей)
Закрыты ВСЕ шесть пунктов (1 High-3, 2 Medium-3, 3 Medium-2, 4 Low-2+Low-3, 5 ЧИСЛА-1, 6 ТЕКСТ-1);
в работе — ничего; ГРАФ-1 разобран, вывод есть (см. ниже). Приёмка: `npm test` 1489/1489,
`npm run check` — 0 ошибок на 4159 файлах.

🔴 Виновата за маячок: отметки дописала одним куском в конце, а не по ходу. Правило приняла —
строка после каждого пункта, а не после всей работы.

**ГРАФ-1 (диагноз, правку не делала).** Данные на месте, дефект в чтении: `sensitivity.py:662-669`
пишет `low_variation`/`high_variation` ОБЪЕКТАМИ `{value, delta_roi_pct}`, а `SensitivityTornado.svelte`
читал их числами → `Math.abs(объект)` = NaN → столбцов нет, подписи и ось остаются. Из
`model-diagnostics.json` проекта `demo-fmcg-brand`: baseline_roi 0.8201, 7 параметров, ни одного
пустого; должны рисоваться −25.52/+29.48, +21.36/−20.03, +14.98/−12.92, −11.78/+14.3, ∓10.59,
∓7.64, ∓1.06. Файл параллельно правит другой исполнитель.
