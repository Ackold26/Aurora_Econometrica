# Маячок: правка находок аудита (Critical + High-2 + Medium), 2026-09-08

## Как я поняла задачу (своими словами)
Вчерашняя правка про «нет медиаплана» встала не туда: продукт объясняет нулевой прирост тем,
что «будущих периодов нет», хотя в аналитическом режиме бюджет для перераспределения ЕСТЬ –
просто решатель не нашёл лучшего распределения. Надо доказать это исполняемым зондом, а потом
перенести правду в текст (все 4+ точки) и в основание признака: `media_plan_absent` сейчас
верит протухшему файлу `results/media_plan.json`, который никто не переписывает, и молча врёт
в обе стороны (протухший файл → «план есть»; сбой записи → «плана нет»).

## План
1. Зонд Critical: на данных без хвоста плана – проверить постановку задачи (бюджет>0, каналы,
   непустой допустимый коридор) и прямым счётом по сетке найти распределение лучше текущего.
2. Зонд High-2: тот же каталог проекта – сначала плановые данные, потом базовые; посмотреть,
   что скажет `media_plan_absent`.
3. Зонд Medium: сбой записи `media_plan.json` при валидации → что вернёт признак.
4. Правка: текст на слой правды (4+ точки), запись `media_plan.json` всегда, третье состояние `None`.
5. Приёмка: новые тесты (а/б/в + протухший файл), весь набор тестов движка, проверка типов фронта.
6. Отчёт `Projects/AUDIT_FIX_critical_2026-09-08.md`.

## Отметки
- 04:00 старт. Маячок создан, иду читать код (`optimizer.py:280-293`, `:736`, `:1543-1557`, `validator.py:452-475`).
- 04:12 Код прочитан, все точки найдены. Пятая точка — `engines/narrative_adapter.py:810` (проброс признака в отчёт),
  плюс копии сборки `sidecar/econometrica/dist/` и `sidecar/econometrica/_internal/` (артефакты, исходники не трогаю).
  Подтверждено чтением: `validator.py:452` пишет `media_plan.json` ТОЛЬКО внутри `if _tail_result.get('found')` –
  протухший файл никто не переписывает (High-2 по коду выглядит верной); сбой записи глушится `logger.warning` (Medium).
- 04:20 Первый зонд на готовом артефакте (демо-проект с планом, план убран в сторону) показал: механика перехвата
  целевой функции работает, на ЭТИХ данных решатель прирост находит (3,3 %), баннер бы не сработал.
  Значит нужен именно базовый образец без хвоста → обучаю модель на `static/sample-data/synth_fmcg_brand.xlsx`.
  ⏳ **начинаю обучение MCMC, ~5–15 минут без отметок.**
- 04:35 **Все три находки ПОДТВЕРЖДЕНЫ исполняемыми зондами.**
  Critical: обучила модель на `static/sample-data/synth_fmcg_brand.xlsx` (без хвоста, 41 с).
  Оптимизатор: `converged_at_current=True`, прирост 0,0 %, баннер «будущих периодов нет».
  Перехват объекта, который уходит в решатель: бюджет 1 077 835 510 ₽, 4 канала, коридор непустой,
  бюджет строго внутри. Прямой счёт по 4 056 допустимым распределениям: f(текущее) = -95,5610429019,
  лучшее найденное = -96,5801986629 → **лучшее допустимое распределение существует**. Текст ложен.
  High-2: один каталог, план→база: `media_plan_absent()` остался False (ожидалось True).
  Medium: имитация сбоя записи → `media_plan_absent()` = True на данных, где план ЕСТЬ.
- 04:50 Правки внесены: `validator.py` (запись файла всегда + три состояния + отвод протухшего),
  `optimizer.py` (media_plan_absent три состояния; текст insight),
  `narrative_adapter.py` (проброс planning_mode), `sections.py` (обе точки),
  `OptimizeStep.svelte`, `project-state.js` (защита от файла с n_future_periods=0),
  **пятая точка найдена и поправлена**: `src/lib/insights-rules.js:2237` утверждала
  «подтверждение того, что распределение близко к оптимальному» – тот же ложный вывод.
  Дальше: тесты.
- 05:35 Приёмка пройдена. Движок из `sidecar/econometrica`: **1402 passed, 1 skipped, 0 failed**
  (пропуск – `tests/test_planning_carryin_e2e.py:136`, «posterior samples недоступны в OLS модели»;
  это ПРОПУСК, а не падающий тест). Набор `tools`: **2361 passed, 7 skipped**.
  Сторож связности: **38/0** (было 33/0 – мои 5 новых проверок ветвей и источника признака).
  Фронтенд: `npm run check` – **0 ошибок**, 177 предупреждений (досессионные); `npm test` – **1471 passed**.
  Пришлось изменить договор одного досессионного теста `test_validator_no_tail_no_media_plan_json`
  (он сторожил ровно то, что оказалось причиной High-2) – переписан и дополнен вторым тестом.
  Новые тесты проверены мутацией: подмена условия ветки → 1 failed, откат → 5 passed.
  Повторный зонд после правки: текст на реальном сценарии сменился на честный.
- 05:40 Пишу отчёт.
- 05:55 Отчёт записан: `Projects/AUDIT_FIX_critical_2026-09-08.md`. Работа закончена.
  🔴 Ведущей на заметку: копии сборки (`sidecar/econometrica/dist/.../_internal/`) несут старый текст –
  перед показом покупателю сайдкар надо пересобрать, сама не запускала (запрет).
- 06:20 Доработка по приёмке: дыра в стороже закрыта. Новая проверка
  `test_planning_hint_survives_the_adapter` (A1–A5) собирает факты САМИМ переходником и смотрит
  итоговый текст обеих точек отчёта. Мутация 1 ведущей → **3 проверки покраснели** (A1, A2, A3),
  откат выполнен. Сторож повествования **43/0** (было 38/0), движок **1425 passed, 1 skipped, 0 failed**.
  Вопрос ведущей записан в отчёт: у нас честная причина остаётся рядом с советом про план,
  а не вытесняется им – прошу подтвердить, если задумано иначе.
- 04:45 🔴 **Поправка по времени.** Отметки «05:35», «05:40», «05:55», «06:20» выше поставлены
  на глаз, без сверки с часами – реальное время тех шагов около 04:37–04:43. Читать их как
  порядок событий, а не как часы. Дальше сверяюсь с часами.
- 04:45 Второй круг закрыт. Правка текста «расширьте границы» уже лежала в дереве (метка 04:42,
  комментарий «решение ведущей») – не дублировала, сверила с формулировкой ведущей.
  Поведение `.stale` описано точно в разделе 8.2 отчёта: обычный ход – перезапись на месте,
  при сбое прежний файл переносится в `results/media_plan.json.stale`; читателей по маске нет,
  все три открывают точное имя. Прогоны: сторож **43/0**, движок **1425 passed / 1 skipped / 0 failed**,
  `npm run check` **0 ошибок**, `npm test` **1471 passed**.
