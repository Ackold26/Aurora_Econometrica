# Живой журнал находок внешнего аудита — 2026-09-08

Формат: `severity | файл:строка | суть | сценарий`

Critical | sidecar/econometrica/engines/optimizer.py:1543-1557 (+ aurora_html/sections.py:682 и :1555, src/lib/components/pipeline/OptimizeStep.svelte:2305) | Продукт называет ложную причину нулевого прироста: «будущих периодов нет — перераспределять пока нечего». В аналитическом режиме money_target = весь исторический медиабюджет (optimizer.py:736, horizon_scale=1.0), задача перераспределения корректно поставлена, и сам автор в комментарии пишет, что улучшающее направление существует, а решатель выходит на первой итерации. Правка встала на слой текста вместо решателя | Открыть базовый образец static/sample-data/synth_fmcg_brand.xlsx (без хвоста) → обучить → «Оптимизация» → баннер и отчёт утверждают, что перераспределять нечего; на деле есть исторический бюджет и лучшее распределение, которое решатель не нашёл

High | sidecar/econometrica/server.py:1129-1170 + :934-948 | Мастер-флаги use_seasonality/use_holidays переданы в prior_predictive_check ТОЛЬКО из обучения (modeler.py:858-861); предполётная проверка их не передаёт, а PreflightRequest их вовсе не содержит. Симуляция всегда идёт с праздниками РФ (~12 столбцов) и Фурье → вердикт надёжности завышен для клиента, который эти флаги выключил | Выключить праздники (HolidayControlsPanel.svelte:53) или сезонность (SeasonalityControl.svelte:65) → предполётная проверка даёт покрытие модели, содержащей контроли, которых в обучаемой модели не будет; тот же проект внутри обучения даст другой вердикт

High | sidecar/econometrica/engines/optimizer.py:280-293 | Признак media_plan_absent читает results/media_plan.json, который НИКТО не удаляет и не обнуляет: validator.py:452-475 только пишет его при найденном хвосте. Устаревший файл в проекте делает признак ложным | В существующем проекте открыть плановый образец (media_plan.json записан) → импортировать в ТОТ ЖЕ проект базовый образец без хвоста → media_plan_absent = False → новая ветка молчит, клиенту снова показывают опровергнутый совет расширить коридор. Сторож test_media_plan_absent_source этого не ловит: он на каждый случай берёт свежий временный каталог

Medium | sidecar/econometrica/engines/optimizer.py:287-292 + engines/validator.py:452-475 | Сбой записи media_plan.json гасится (logger.warning), а validation.json пишется дальше → media_plan_absent вернёт True на данных, где план ЕСТЬ, и продукт заявит клиенту «будущих периодов нет» | Заблокировать results/media_plan.json на запись (антивирус, полный диск) при проверке планового файла → в оптимизации баннер «в этих данных есть только история»

Medium | src-tauri/src/commands/report.rs:589-603 | waterfall_rows отсеивает элемент «Итого» только по types[i] == "total"; при отсутствующем или коротком массиве types (`unwrap_or(&empty)`) итог попадает в ряды: сумма удваивается, все доли листа «Декомпозиция» вдвое ниже истинных, в диаграмме лишний столбец. Защиты по длине нет, хотя длины labels/values/types нигде не сверяются | decompose = {"waterfall":{"labels":["Baseline","TV"],"values":[500,500]}} без types → ИТОГО 1000 при истинных 500 у слагаемых и доли 50 %/50 % вместо 100 %. ГИПОТЕЗА по живому пути: движок (decomposer.py:1485) types пишет всегда, риск — старые сохранённые decompose.json

Medium | sidecar/econometrica/server.py:1129-1146 (ветка деградации detect_media_plan_tail) | y_obs = df[kpi].fillna(0): если разбор хвоста медиаплана упал и df не обрезан, строки плана входят в наблюдения нулями, а даты теперь совпадают по длине, поэтому сверка длин молчит и симуляция с сезонностью строится на выдуманных нулях | ГИПОТЕЗА. Зонд: файл, на котором detect_media_plan_tail бросает исключение (internal_gaps/нечитаемая дата) → сравнить len(y_observed) с числом строк истории в журнале предполётной проверки

Medium | tools/generate_demo_samples.py:1276-1287 (набор «Ритейл / e-com») | «Чёрная пятница» — календарный признак, известный наперёд, но в хвосте медиаплана он NaN, тогда как в истории даёт вклад bf_coef=0,16. Прогноз на 18 будущих месяцев теряет ноябрьский всплеск | ГИПОТЕЗА. Зонд: открыть «Ритейл / e-com · план» → прогноз → сравнить ноябрьские будущие периоды с ноябрями истории

Проверено, дефектов НЕ найдено: порядок ветвей (converged_at_current исключает binding_constraints по построению, optimizer.py:1285-1290 — затенения новой ветки нет ни в отчёте, ни в интерфейсе); имя поля n_future_periods совпадает с validator.py:439; формулы XLSX после исключения total (B4/B$7, SUM(B4:B6)) арифметически верны; ключи use_holidays/use_seasonality совпадают между интерфейсом (ConfigPanel.svelte:509-510) и modeler.py:484/569; бланки справки template_*.xlsx остались пустыми (только заголовки).
