# Пульс внешнего аудита починки P0.2–P0.5

## Задание своими словами
Проверяю, не сломала ли починка (записи a129506 и 975404e, ветка feat/econ-canon-p0) больше, чем починила. Главный риск – новый рассинхрон чисел: гейтовый знаменатель gateRatio против эффективного, флаг праздников useHolidays и режим, не доехавшие до части потребителей, и новый stats без min/max/mean/std/cv у нечисловых колонок, который могут читать фронтовые потребители. Правки продукта не делаю, только читаю, гоняю зонды и тесты; временные мутации откатываю из копии.

## План проверки
1. Прочитать diff, исходные находки, CLAUDE.md (§1, §18).
2. ratio-classifier.js: gateRatio / effectiveParamCount / effectiveRatio – сигнатуры и умолчания; найти ВСЕХ вызывающих grep-ом, проверить передачу mode и useHolidays.
3. Гейт vs показ: ValidateStepV13.svelte целиком, ValidateStep.svelte, ColumnMapperConfirm.svelte, StepWrapper.svelte – нет ли третьей формулы.
4. project-state.js: перенос стора useHolidays – мёртвая зона, порядок подписок, двойная подписка на activeProject.
5. validator.py: ratio_gate, ветка нечисловых колонок для kpi/media/control, полностью пустая колонка, роли date/unused; снятие чисел из issues/warnings.
6. Все читатели stats. на фронте для media/control – не упадут ли без min/max/mean/std/cv.
7. Сторожа (ratio-gate-and-holidays.guard.test.js, test_validator_gate_and_non_numeric.py) – проверка мутацией.
8. corridorIsObserved в OptimizeGoalSeek.svelte.
9. Клиентский текст: тире, англицизмы, «вы», псевдоточность.
10. Прогоны: vitest по затронутым файлам, pytest движка, npm run check.

## Отметки
- [2026-08-03 03:25] Старт. Пульс создан, иду читать diff и находки.
- [03:26] Прочитан diff целиком (1202 стр.), краткие находки, CLAUDE.md. Гипотезы: (а) при блокировке карточка показывает эффективный ratio, а текст блокировки – гейтовый: два числа одной величины; (б) обещание «станет N:1» считается гейтом, а карточка после исключения покажет эффективный; (в) новый stats без zeros_pct/cv/min/max у нечисловых колонок. Иду грепать потребителей.
- [03:28] Проверено: все потребители effectiveParamCount/effectiveRatio (6 мест) получили useHolidays, все вызовы validateInsights/validateRolesInsights/modelPreTrainingInsights в компонентах – с режимом и флагом. Находка №1 (High) подтверждена зондом: карточка 0,9 против плашки блокировки 1,8 на одном экране. Записана.
- [03:32] Прогнан реальный validate_data на битом файле (текст+пустые+десятичная запятая). Записаны находки 2-5 (все Medium): пустые ячейки в доле нечитаемых; зелёная подсветка битой колонки в TrafficLight; два парсера числа в одной функции; disabled_holidays мимо знаменателя. Проверено и НЕ находка: ветка elif висит на is_numeric_dtype – роли date/unused не задеты; перенос стора useHolidays мёртвой зоны не создал (activeProject объявлен на строке 29), двойной подписки нет, get(useHolidays) на старте никто не зовёт; шапка и карточка теперь считают одной формулой. Дальше – прогон тестов и мутации сторожей.
- [03:37] Мутации: JS-сторож ratio-gate-and-holidays падает на всех трёх подменах (7 из 16 тестов), питоновский – только на ветке insufficient_data (две другие не покрыты). Мутации откачены из копий, git diff пуст. Записаны находки 6-8. Дальше: npm run check + широкий прогон фронтовых тестов.
- [03:42] Прогон всех тестов: фронт 1403/1403 зелёные, движок 96/96 по срезу валидации, npm run check – 0 ошибок. Найдено ещё два места с собственной формулой: validateConfirmInsights (High, рассинхрон создан этой правкой – зонд 3,6 против 1,7) и syncStepLockAfterValidate (Medium, сырой знаменатель в бейдже шага). Записаны. Остались: corridorIsObserved, клиентский текст, финальный раздел «не находка».
- [03:44] Проверены: сезонность в знаменателе (Medium – до 8 параметров мимо показа), развилка коридора (Low, ветка спящая), справка про праздники (Low, «ты»). Глоссарий сверен check_glossary_sync.py – 3 выхода синхронны, NRMSE вычищен полностью. Иду писать раздел «проверено и НЕ находка».
- [03:46] Записана находка 14 (Medium, «не критично» на critical-колонке) и раздел «проверено и НЕ находка». Аудит закончен: 2 High, 8 Medium, 3 Low.
