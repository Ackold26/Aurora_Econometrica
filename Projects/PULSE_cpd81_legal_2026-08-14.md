# PULSE — CPD-81 Legal Center (2026-08-14)

## Задача (своими словами)
В Legal Center есть класс дефекта CPD-81: отказ копирования файла (`std::fs::copy`) отбрасывается через `let _ = ...`, а имя файла всё равно попадает в список "передано/сохранено" — вызывающий не узнаёт о потере, рабочий каталог потом удаляется. Машинная проверка нашла одно место — `forward_exports_to_inbox` в `campaign.rs:497`. Нужно: (1) перепроверить, что `persist_step_exports` уже вылечена (и какой записью), (2) прогнать свой поиск по всему `src-tauri/src/` на весь класс дефекта — не доверять узкому инструменту, (3) исправить по эталону Эконометрики (структура итога: передано / поимённые отказы / признак фатального отказа), (4) написать тесты по образцу Legal, включая доказательство мутацией, (5) прогнать тесты до/после, (6) отчёт.

## План
1. Маячок (этот файл) — старт.
2. Проверить теги/ветки в основном дереве ROSST_AI_Legal (какая ветка держит v0.13.2-legal).
3. Создать git worktree `_wt_cpd81_legal` с веткой `cpd81-legal-fix` от `master`.
4. Найти CARGO_TARGET_DIR для Legal.
5. Прочитать campaign.rs — обе функции (`persist_step_exports`, `forward_exports_to_inbox`), их вызывающих.
6. Прочитать эталон в Aurora_Econometrica_thinwt (master): commits 1414259, 5457a89, 68b30be + отчёты FIX_AUDIT249, FIX_CPD81_FORWARD.
7. Прогнать свой grep-поиск по всему src-tauri/src на класс дефекта.
8. Прогнать базовые тесты ДО правки (зафиксировать).
9. Внести правку по эталону (хирургически).
10. Прочитать существующий mod tests в campaign.rs, написать тесты (4 сценария + мутация).
11. Прогнать тесты ПОСЛЕ.
12. Доказательство мутацией — вернуть дефект, зафиксировать падение, откатить.
13. Написать отчёт FIX_CPD81_LEGAL_2026-08-14.md.
14. Финальное сообщение team-lead.

## Отметки
- 23:24 — старт, маячок создан, план готов.
- 23:26 — теги/ветки проверены: v0.13.2-legal ⊂ master, подтверждено фактом.
- 23:27 — рабочая копия `_wt_cpd81_legal` создана, ветка `cpd81-legal-fix` от `master` (bb8365a).
- 23:29 — CARGO_TARGET_DIR продукта (ai-agency) под запретом задачи → использую `D:/cargo-targets/legal`.
- 23:33 — persist_step_exports подтверждена закрытой записью 826fef3 (2026-07-30, CPD-37); diff прочитан.
- 23:40 — прочитан эталон Econometrica (5c34d7a, ForwardExportsResult, тесты forward_exports_to_inbox_*).
- 23:44 — свой поиск по всему src-tauri/src на класс дефекта: найден ТРЕТИЙ носитель — campaign_set_brief:530 (копирование брифовых файлов), не упомянутый в задаче и не закрытый даже в эталоне Econometrica.
- 23:48 — правка forward_exports_to_inbox (ForwardExportsResult) + вызывающий код в lib.rs.
- 23:52 — правка campaign_set_brief → выделен copy_brief_files() (BriefFilesResult) + вызывающий код.
- 23:55 — база ДО правки (git stash): 391 passed / 0 failed / 1 ignored.
- 00:05 — 7 новых тестов написаны и зелёные (4 forward_exports_to_inbox + 3 copy_brief_files).
- 00:08 — полный прогон ПОСЛЕ: 398 passed / 0 failed / 1 ignored. Clippy чист.
- 00:12 — доказательство мутацией: тест forward_exports_to_inbox_copy_failure_recorded_not_lost покраснел на возвращённом дефекте, вывод зафиксирован, правка восстановлена, скобки перепроверены компиляцией.
- 00:15 — 4 упавших сетевых теста content_updater (флак среды под нагрузкой) — перепрогнаны точечно, все зелёные.
- 00:18 — запись изменений создана: 30474fd. Пишу отчёт.
