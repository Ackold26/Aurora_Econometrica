# PULSE — разведка CPD-69/70 остатка (2026-08-14)

## Задача своими словами
Три точки в коде Econometrica (Rust) до сих пор копируют/синхронизируют файлы клиента без защиты от молчаливой перезаписи — та же болезнь, что чинили в `unique_export_path()`, но здесь не закрыта. Нужно установить фактом (не догадкой): в приоритетной точке `persist_step_exports` имена файлов уникальны по построению (тогда затирание — реальная потеря данных) или стабильны по шагу (тогда счётчик наплодит мусора и правка будет вредной). Плюс разобрать вторую и третью точку, и отдельно — дыру в структурном тесте CPD-69, который бьёт мимо боевой команды. Только чтение, отчёт — в SCOUT_OVERWRITE_2026-08-14.md.

## План
1. Прочитать audit_findings_live.md (раздел про перезапись при копировании).
2. Найти `persist_step_exports` в campaign.rs — сигнатура, тело, вызывающие.
3. Проследить источник имён файлов, попадающих в неё (кто создаёт файлы шага, по какому шаблону).
4. Проверить поведение при повторном прогоне одного шага и при коллизии двух разных шагов.
5. Проверить, куда копируются файлы — врем. папка или выдача клиента.
6. Разобрать `copy_dir_into` (manager.rs) и `sync_dir_to` — код + комментарий-предупреждение.
7. Найти `unique_export_path()` — сигнатура, применимость к найденным точкам.
8. Найти структурный тест CPD-69 (`WORKSPACE_DIRS_CLEARED_ON_START`, `clear_workspace_files`), разобрать дыру.
9. Проверить прецеденты тестирования команд с `AppHandle` в дереве.
10. Написать отчёт SCOUT_OVERWRITE_2026-08-14.md.

## Журнал
- 03:25:57 — старт, файл пульса создан, план зафиксирован.
- 03:27 — прочитан audit_findings_live.md полностью. Ключевое: предыдущая сессия УЖЕ понизила `copy_dir_into` до Medium (имена с секундной меткой времени в exports — коллизия маловероятна), но НЕ проверила `persist_step_exports` (campaign.rs:474) — прямо помечено как приоритет следующей сессии, бюджет не хватил. Также зафиксирован комментарий CLAUDE.md правило #11: `persist_step_exports` вызывается ДО `close_session()`, иначе результаты шага теряются — то есть функция критична для сохранности данных, что повышает цену молчаливой перезаписи. Далее иду в campaign.rs:474.
- 03:28 — прочитан `persist_step_exports` (campaign.rs:465-482) и точка вызова lib.rs:3250-3273. Факт: `dest = campaign_dir.join("steps").join(step_id)` — уже НАМЕРЕННО разведён по step_id (разные шаги пишут в разные подпапки, столкновение между ШАГАМИ структурно исключено). `campaign_dir` строится из `workflow_id` (lib.rs:3021), НЕ из `exec_id` — то есть при повторном запуске ТОГО ЖЕ workflow campaign_dir/steps/step_id остаётся тем же каталогом между прогонами.
- 03:29 — установлен источник имён: единственный живой писатель в `work_dir/exports` на пути пайплайна — `auto_save_response` (claude.rs:636-680). Он УЖЕ использует `unique_export_path()` (строка 676, коммент прямо ссылается на CPD-70) — имя = `{slug}-{timestamp:%Y%m%d-%H%M%S}{suffix}.md`, секундная точность + счётчик при коллизии внутри самого exports_dir. econometrica.rs пишет в СВОЙ `project_dir/exports` — другой каталог, к пайплайну кампаний отношения не имеет (проверено grep — 0 совпадений на "exports" в самом файле не подтвердилось, но путь другой). pptx_processor.rs НЕ пишет в exports вовсе.
- 03:30 — найден `pptx_postprocess` (lib.rs:1911-1970) — второй потенциальный писатель в `work_dir/exports`, имя ДЕТЕРМИНИРОВАННОЕ (`{stem}_commented.pptx`/`{stem}_commentary.docx`, без метки времени, без unique_export_path). Проверено позитивным контролем (grep нашёл команду в lib.rs) — но в `src/` (фронтенд) НИ ОДНОГО вызова `pptx_postprocess`/`postprocess` не найдено ни разу. Команда зарегистрирована, но мертва — не участвует в живом потоке persist_step_exports. Зафиксировано как латентный риск, не текущий.
- 03:31 — перечитан `copy_dir_into` (manager.rs:639-657) и `sync_dir_to` (manager.rs:664+) целиком. `copy_dir_into`: `fs::copy` в `dst.join(entry.file_name())`, коллизия НЕ ошибка — молча перезаписывает, только реальные I/O-ошибки (файл занят) идут в `blocked`. `sync_dir_to`: подтверждено grep (3 вызова) — применяется ТОЛЬКО к inbox (2 сайта в manager.rs: 253, 481, плюс sync_inbox:285), к exports сейчас не подключён нигде.
- 03:32 — разобран структурный тест CPD-69 (lib.rs:4226-4267) и боевая команда `clear_workspace_files` (lib.rs:1870-1879, вызывается из `+layout.svelte:196` через `invoke`). Оба теста бьют по `clear_workspace_dirs()` напрямую или по константе `WORKSPACE_DIRS_CLEARED_ON_START` — НИ ОДИН не вызывает саму команду `clear_workspace_files`. Grep на `AppHandle`+test/mock — 0 совпадений во всём дереве: прецедента тестирования Tauri-команд с реальным/моковым AppHandle в этом дереве НЕТ.
- 03:33:21 — все факты собраны, отчёт SCOUT_OVERWRITE_2026-08-14.md написан и сохранён. Разведка завершена.
