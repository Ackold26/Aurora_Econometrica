# Durable-состояние автономной работы — 2026-09-08 (ночь → утро)

Режим: максимальная автономия (решение владельца 08.09, ~04:40). Остановок нет;
владельцу выносится только то, что физически требует его рук.

## МИКРОСТАТУС (что прямо сейчас)
Разбор находок внешнего аудита закончен по существу; идут две доработки по моей приёмке
и одна правка движка. Сборка установщика НЕ начата — начинается после того, как все правки
движка лягут в дерево (иначе пересобранный движок протухнет сразу).

## СДЕЛАНО
- Критическая находка: подтверждена зондом (прямой счёт по 4 056 допустимым распределениям —
  лучшее распределение существует, выигрыш 1,07 % целевой функции), текст исправлен в ПЯТИ точках.
- High-1 (флаги контролей в предполётной проверке): подтверждена, разрыв был в четырёх слоях,
  правка сделана, моя приёмка через настоящий HTTP прошла: reliable против insufficient.
  Найдена дыра — молчаливая потеря ключа при рассинхроне написания; закрывается алиасами.
- High-2 (протухший `media_plan.json`): подтверждена, лечение на стороне записи.
- Medium: три разобраны зондами (одна недостижима на живом пути, две в бэклог), четвёртая
  починена вместе с High-2.
- Найден дефект шире, чем видел аудитор: вклад любой клиентски названной календарной колонки
  в прогнозе всегда ноль (`scenario.py`, сопоставление имён). Правится отдельным исполнителем.
- Метка шлюза поднята до `aurora_gateway-v0.6.6`, существование метки проверено запросом.
- Версия в `package.json` сведена к 2.5.1 (была 2.5.0 против манифестов).
- Моя приёмка мутациями: три мутации по Critical (одна дыра найдена, две проверки живые),
  одна по предполётной проверке (дыра найдена). Все мутации откачены, дерево чистое.

## ОСТАЛОСЬ (по порядку)
1. Принять доработки: положительная проверка ветки планирования (Critical), алиасы ключей
   (предполётная проверка), правка совета «расширьте границы».
2. Принять правку календарных колонок своей мутацией в соседнем звене.
3. Пересобрать движок: `python sidecar/econometrica/build_sidecar.py` (штатная сборка его НЕ трогает).
4. Сборка установщика: скилл `aurora-fix` → `npm run tauri:build:thin` (только эта команда — иначе
   поставка без шлюза). Грепать шлюз по НЕСЖАТОМУ `target/release/*.exe`, не по установщику.
5. Заменить установщик в «Демо-показ Эконометрики», сверить сумму, переписать
   `ПРОЧТИ ПЕРЕД ПОКАЗОМ.md` и версию в `DEMO_SCRIPT_2026-09.md` (2.5.0 → 2.5.1).
6. Живой прогон: вход через шлюз, эталонный проект, лист «Декомпозиция» в выгрузке, новый текст
   на шаге оптимизации.
7. Запись изменений своим перечнем путей, память, промт следующей сессии.

## ВЛАДЕЛЬЦУ (без его рук нельзя)
- Пробное письмо на `sales@auroraai.pro` — доставка ни разу не проверялась.
- Замена живой страницы `auroraai.pro` заглушкой — срок не назначен, выкладку ведёт другая сессия.
- Написание почты (камельно против строчного) — владелец сказал «не трогай до моего решения»,
  не трогаю.

## ОБНОВЛЕНИЕ 04:45
- Доработка по предполётной проверке принята **моим повторным зондом**: тело с написанием ключей
  интерфейса (`useSeasonality`) теперь даёт `insufficient`, а не `reliable` — молчаливая потеря
  флага между слоями закрыта алиасами.
- Доработка по Critical: положительная проверка ветки планирования добавлена, **проверена моей
  же мутацией** (`narrative_adapter.py` → `planning_mode: False`): сторож покраснел тремя
  проверками, до доработки был зелёным. Мутация откачена, база сторожа повествования **43/0**.
- Совет «расширьте границы» в панели подсказок переписала сама (исполнитель уже закончил):
  обещание «оптимизатор получит пространство» убрано, добавлена трактовка исхода.
- Ждут: правка календарных колонок в прогнозе (идёт полный прогон тестов движка) и сборка бэклога.

## ОБНОВЛЕНИЕ 05:00 — запись сделана, идёт подготовка сборки
- Запись `7375882a` (31 файл) — все правки аудита, обе доработки, метка шлюза, версия.
  Мои личные прогоны: движок 1425/1 пропуск/0 упавших · фронтенд 1471/1471 · типы 0 на 4159 ·
  связность 43/0. Приёмка правки календарных колонок — моя мутация в генераторе имён столбцов
  уронила 9 проверок из 16, сторож стоит на настоящем пути.
- Предсборочные проверки `aurora-fix`: версии 2.5.1 в трёх местах · идентификатор верный ·
  `installMode: currentUser` · пакет содержимого синхронен (версия 7, подпись валидна) ·
  справка согласована · линтер промтов чист · хуки установщика без чёрных окон (0 `ExecWait`,
  6 `nsExec::Exec`) · иконка 47 КБ · версия в интерфейсе берётся у поставки, не константой ·
  клиентских данных в каталоге движка НЕТ.
- 🔴 Два замечания в бэклог (не блокируют показ):
  (1) `sidecar/econometrica/dist` — 939 МБ дубля сборки под маской ресурсов, балласт установщика;
  (2) `tools/build-cloud.mjs` не зовёт сторожа доставки — правки общего слоя доедут только по памяти.
- 🔴 Запись НЕ отправлена в удалённый репозиторий (наружу — только по слову владельца).
  Сторож это видит и остановит именно ВЫПУСК; сборка демонстрационного установщика не блокируется.
- Идёт пересборка движка (PyInstaller). Следом: приёмка с признаком `thin`
  (`node tools/build-cloud.mjs --test`), затем сборка установщика `npm run tauri:build:thin`.

## ОБНОВЛЕНИЕ 05:55 — сборка сделана, живой прогон дал две находки
- **Движок пересобран** (05:00, 67,3 МБ), сторож свежести подтвердил, проверка на реальных файлах прошла.
- **Приёмка в конфигурации поставки** (признак `thin`): 520 + 5 + 7 = 532 проверки, 0 упавших.
  Дерево после прогона восстановлено, подменённого манифеста нет.
- **Первая сборка установщика**: `Optimizer MMM_2.5.1_x64-setup.exe`, 259 492 911 байт,
  SHA-256 `56b2b576eff55aebabeb5787e8861cc9f32f87d04d7a784d3f304d3c6aa839c3`.
  Шлюз в НЕСЖАТОМ двоичном файле есть (`aurora_gateway` 3, `gateway_executor` 2);
  контрольная проба по установщику дала 0 — подтверждает известную ловушку, а не дефект.
- **Установка без прав администратора прошла** тихо (`/S /R`), версия в реестре 2.5.1,
  каталог `%LOCALAPPDATA%\Optimizer MMM`. Программа поднялась и прошла вход — замыкания
  на входе со шлюзом v0.6.6 нет.
- 🔴 **Живой прогон нашёл ДВЕ находки, обе исправлены (запись `8073bf00`):**
  1. Шестая точка ложного вывода об оптимальности — в панели подсказок рядом с уже
     исправленным баннером (`insights-rules.js`, два места).
  2. **Шаг «Декомпозиция» показывал пустой экран**: состояние «нечего показывать»
     выставляется дважды, ветки разметки под него не было ни одной; значок шага при этом
     «Готово». У соседних шагов такая ветка есть. Добавлена — с объяснением и кнопкой.
- Идёт ВТОРАЯ сборка установщика с этими правками. Прежняя (05:22) уже лежит в комплекте
  показа и УСТАРЕЛА — заменить после второй сборки, пересчитать сумму в `ПРОЧТИ ПЕРЕД ПОКАЗОМ.md`.
- Наблюдателей за сборкой дважды снимала система по нехватке памяти. Это НЕ провал сборки:
  `rustc` жив, журнал растёт. Программу закрыла, освободив гигабайт.

## ОСТАЛОСЬ ПОСЛЕ ВТОРОЙ СБОРКИ
1. Грепнуть шлюз по несжатому `target/release/aurora-econometrica-gui.exe` (не по установщику).
2. Скопировать в комплект показа, пересчитать SHA-256 и обновить его в `ПРОЧТИ ПЕРЕД ПОКАЗОМ.md`.
3. Переустановить и убедиться: шаг «Декомпозиция» на сохранённом проекте показывает объяснение
   и кнопку «Рассчитать» вместо пустоты; на проекте с живым исходным файлом — считает.
4. Память, промт следующей сессии, wrap-up.

## ОБНОВЛЕНИЕ 10:05 — четыре находки живого прогона, четвёртая сборка
Живой прогон установленной сборки дал ЧЕТЫРЕ находки; записи `8073bf00` и `c0b134f6`:
1. Шестая точка ложного вывода об оптимальности (панель подсказок, соседнее правило того же файла).
2. Шаг «Декомпозиция» показывал пустой экран при значке «Готово» — ветки разметки не было вовсе.
3. Моя же первая правка сказала о пустоте неправду («модель не обучена» на обученной): признак брала
   из памяти вкладки, а продукт берёт его с диска. Перестроено.
4. 🔴 Новый проект показывал результаты ПРЕДЫДУЩЕГО как свои («Модель обучена, R² = 0,825» при
   пустых `models/` и `results/` на диске — проверено снимком каталога). В коде это стояло как
   намеренное решение защитить незавершённый импорт. Лечение сохраняет намерение: импорт живёт,
   чужие результаты снимаются. Сторож на оба свойства, «внести-поймать-откатить» пройдено.

Память приведена в порядок: канон методов и канон Эконометрики переписаны, две новые записи правил,
горячие зоны 15/15 зелёных, поисковый индекс догнан, сквозная проверка памяти 11/11.

## ОСТАЛОСЬ
1. Дождаться четвёртой сборки → заменить установщик в комплекте показа, пересчитать сумму в памятке.
2. Живая проверка правки №4: создать проект и убедиться, что шаг «Модель» ПУСТ, а не показывает чужую модель.
3. Убрать за собой мусорный проект с испорченным именем (создан моей же ошибкой ввода).
4. Промт следующей сессии, wrap-up.

## ОБНОВЛЕНИЕ 10:15 — 🔴 столкновение сессий на рабочем столе
Снимок экрана показал: на машине ОДНОВРЕМЕННО работают четыре сессии, и одна из них (Oracle) ведёт
СВОЙ живой прогон через управление рабочим столом прямо сейчас.

**Это объясняет мою же аномалию:** имя нового проекта заполнилось текстом чужой сессии
(«Ставь durable-состояние на случай обрыва и продолжай.829 ?3= 0809») — буфер обмена общий, ввод
одной сессии уходит в окно другой. Я списала это на свою ошибку ввода; на деле это столкновение.

Живой прогон интерфейса прекращён в 10:15, рабочий стол свободен.
🔴 Поправка по адресату: сперва я предупредила `aurora-ai-f5` — она рабочего стола НЕ трогала
(работает безоконным браузером), о чём сама и сообщила. Рабочий стол ведёт `aurora-ai-1e`
(живой прогон Oracle), ей отправлены факты: время моих смен фокуса и совпадение по буферу.

**Что осталось непроверенным вживую:** правка №4 (новый проект не наследует чужие результаты) —
покрыта сторожем из трёх проверок с пройденной мутацией, но глазами в собранной программе НЕ
подтверждена. Проверить, когда рабочий стол свободен: создать проект → шаг «Модель» обязан быть
ПУСТ, а не показывать «Модель обучена».
