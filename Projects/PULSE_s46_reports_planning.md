# Маячок s46 – «Планирование» в уносимых документах

## Задача своими словами
Человек видит на экране программы шаг «Планирование» (план бюджета на следующий период),
но в уносимом документе – ни в веб-отчёте, ни в колоде – этого шага нет. Уходит со встречи
без ровно того, ради чего считали. Надо закрыть дыру: сначала раздел «Планирование» в
веб-отчёте, потом подключить написанные-но-невызванные функции декомпозиции в колоде,
потом (если останется ход) раздел «Планирование» в колоде.

## План
1. Разведка: CHART_SPEC_static_reports.md, генератор веб-отчёта, генератор колоды,
   экранный шаг «Планирование» и источники его показателей.
2. Записать «предложенный состав раздела» в этот маячок.
3. (а) раздел в веб-отчёте → (б) декомпозиция в колоде → (в) раздел в колоде.
4. Сторожа на каждый новый кусок + проверка их падаемости, прогон проверок движка.
5. Отчёт `Projects/REPORTS_PLANNING_s46.md`, записи изменений по-русски.

## Лента
- 13.09, старт. Маячок создан, иду в разведку.

- 13.09, разведка закончена. Главное: раздел «Прогноз на будущий период» в ОБОИХ
  документах УЖЕ есть (`aurora_html/sections.py:2493 render_forecast_plan`,
  `aurora_pptx/builder.py:3855 s_forecast_plan`), но он тонкий – одна таблица
  сценариев. При этом в веб-отчёте он до сегодняшнего дня выходил ПУСТЫМ в каждом
  боевом отчёте (ключ `forecast` не клался в ctx; починено записью, уже в дереве –
  `aurora_html/builder.py:620-628`). Дыру закрываю составом, а не самим фактом
  наличия раздела.
- Проверено лично: `make_decomposition_stacked` (`aurora_pptx/charts.py:90`) и
  `make_share_comparison` (`charts.py:66`) не вызываются нигде – 0 вызовов на весь
  репозиторий (grep без dist/_internal). Пункт (б) подтверждён.

## ПРЕДЛОЖЕННЫЙ СОСТАВ РАЗДЕЛА «ПЛАНИРОВАНИЕ»
Всё – из `load_saved_forecast` (`engines/planning.py:339`), то есть из тех же
`results/scenarios/<имя>.json`, что показывает экран. Ничего нового не считаю.
1. **Горизонт плана** – «план на N периодов (первая метка – последняя)» из
   `period_labels`. Сейчас документ вообще не говорит, за какой срок прогноз.
2. **Принятый план крупно** – имя ★-варианта, центр прогноза KPI, правдоподобный
   диапазон 90% (низ–верх), ширина диапазона в % от центра, бюджет ₽ (только когда
   движок перевёл все каналы в деньги – иначе строки бюджета нет, INV-50).
3. **Чем план отличается от базового** – Δ бюджета и Δ KPI (абсолют + %) принятого
   варианта против «Базовый план» (медиаплан из файла). Формула ровно как на экране
   (`MultiScenarioPage.svelte:176-177`). Нет базового среди сценариев – блока нет.
4. **Различимы ли варианты** – вердикт по перекрытию 90%-диапазонов пары
   «лидер – ближайший преследователь», дословно та же выборка и те же строгие
   неравенства, что P8/P9 на экране (`insights-rules.js:2535-2568`).
5. **Таблица вариантов** – остаётся как есть, добавляю колонку «Δ к базовому».
6. **Оговорки** – остаются как есть.
Чего НЕ кладу: разбивки принятого плана по каналам×периодам (на экране она есть, в
отчёт просится, но это отдельная таблица на полстраницы – скажи, если нужна).

## Лента (продолжение)
- Сделан общий счётчик сводки `summarize_forecast` (`engines/planning.py:470`) – один
  на оба документа, читает сценарии с диска.
- Веб-отчёт: раздел расширен (срок, принятый план, диапазон + ширина, отличие от
  базового, вердикт различимости, колонка Δ в таблице). Побочная находка: классы
  `trust-*`, на которых построен раздел, были прибиты селекторами к `.section-trust` –
  таблица и плашки раздела прогноза выходили у клиента ВОВСЕ без оформления.
  Селекторы раздвоены (`templates/layout.css:1344`).
- Колода: дыра (б) подтверждена и закрыта – новый слайд «Декомпозиция продаж»
  (`s06b_decomposition_chart`) вызывает написанную-и-не-вызывавшуюся
  `make_decomposition_stacked`; числа прокинуты через адаптер из того же
  `decompose['waterfall']`, что рисует веб-отчёт.
- Колода: раздел плана (в) тоже расширен – срок, принятый план, отличие, вердикт.
- Сторожа: 19 новых, все четыре ключевых проверены на падаемость (внёс-поймал-откатил).
- 🔴 Прогон: было 1570 прошло / 1 упало, стало 1589 прошло / 1 упало. Упавшая –
  ЧУЖАЯ и была до меня: `test_no_em_dash_in_engine_client_text` ловит длинное тире в
  `tools/fetch_macro_series.py` (запись 31c0d3cb, загрузчик макрорядов ЦБ). Не трогаю –
  файл не мой.
- Примеры для глаз: `Projects/samples_s46/пример_веб-отчёта.html`, `пример_колоды.pptx`.
- Запись изменений: `05e745ff`. Отдельного файла `REPORTS_PLANNING_s46.md` нет –
  среда запрещает исполнителю писать файлы-отчёты; полный отчёт (числа с адресами
  файл:строка, дословный вывод мутаций и прогонов, находки) отдан текстом в ответе
  оркестратору.

## Лента (добавка владельца: разбивка по каналам)
- Каналы – да, периоды – нет, принято. Источник: `per_channel_spend.money` сценария
  (`engines/scenario.py:1203`) – движок кладёт туда суммы ТОЛЬКО когда перевёл в рубли
  все активные каналы, и тогда же их сумма равна «Бюджету плана», то есть доли
  складываются ровно в 100%. Нет денег – блока нет вовсе, доли из натуральных единиц
  не считаются.
- Веб-отчёт: таблица «Куда идёт бюджет» (канал · сумма за срок плана · доля).
  Колода: одна строка, шесть крупнейших поимённо, хвост свёрнут в «прочие N каналов»
  с их суммой – ничего не теряется молча.
- Формулировка: «правдоподобный диапазон 90 %» в обоих документах; колонка колоды
  «90%-интервал» переименована в «Правдоподобный диапазон» (то же, что на экране).
- 🔴 Поймала себя на мёртвом стороже: первая редакция проверки термина искала слово по
  ВСЕЙ колоде и прошла на мутации – «Правдоподобный диапазон» стоит и на соседнем
  слайде. Переписан на текст ИМЕННО слайда плана, после чего мутация его роняет.
- Прогон: 1598 прошло, 0 упало (падавшая проверка длинного тире в
  `tools/fetch_macro_series.py` починена не мной, пока не записана).
