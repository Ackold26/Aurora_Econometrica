
## fix-engine исполнитель — старт 06:32
Задача: движок sidecar/econometrica — (1) формат суммы переброски единой функцией <10 млн с десятичным знаком, запятая; (2) три сторожа теста порога 0,5 млн стерегут не то — доказать мутацией красным; (3) сторож контраста проверяет питон-порт, а не JS в бандле — переделать на реальный код; (4) кнопка PNG отдаёт SVG — разобрать и починить.
План: сначала прочитать AUDIT_s48_external.md релевантные строки, найти все места :.0f с суммой переброски, сделать format_realloc_mln(), затем чинить тесты threshold, затем datalabel contrast, затем PNG/SVG экспорт.
06:51 fix-engine: все 4 задачи закрыты. Формат суммы единой функцией; три сторожа теста порога почищены и доказаны мутацией; сторож контраста переделан на реальный JS (Node); PNG-экспорт чинён offscreen-canvas, доказано первыми байтами файла. Полный прогон tests/: 1686 passed, 2 skipped.
