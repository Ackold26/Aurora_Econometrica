# PULSE – сертификат методологии: отпечаток данных + протокол выбора затухания

🔴 **Маячок пролежал 25 минут не в том каталоге.** Я создала его в
`D:\Docs\Aurora_Ai\Projects\`, тогда как маячки исполнителей живут в
`D:\Docs\Aurora_Ai\Dev\Aurora_Econometrica_thinwt\Projects\`. Перенесён сюда в 16:15.
Ведущая смотрела в правильное место и видела пустоту – расхождение моё.

🔴 **Отметки времени ниже пересобраны по системным меткам** (`date`, mtime файлов,
время записи в git). В первой версии маячка промежуточные отметки я поставила
на глазок, и они врали на час вперёд – ровно та ошибка, о которой предупредила
ведущая. Больше – только `date` и метки файловой системы.

## Задача своими словами
Программа уже записывает в модель два новых факта – содержательный и файловый
отпечаток исходной таблицы (`reproducibility['data_fingerprint']`) и протокол выбора
затухания по каналам (`adstock_selection`: что просили / что применилось / кто выбрал).
Клиент их пока не видит. Надо довести факты до сертификата методологии – одинаково в
отчёте и в презентации, с честным статусом для моделей до 06.07.2026, где полей нет:
печатать «не записано» / «не зафиксировано» и звать переобучить, ничего не выводя
задним числом.

## План
1. Прочитать `engines/methodology_cert.py`, вызов в `decomposer.py`, рендеры в
   `aurora_html/sections.py` и `aurora_pptx/builder.py`.
2. Зонд: что лежит в свежей и в старой модели (старую – скопировать во временный каталог).
3. Добавить два блока в сертификат + честные статусы; довести в оба формата.
4. Целевые тесты + мутации продуктового кода.
5. Отчёт ведущей.

## Отметки (время – системное)
- **15:49** старт (`date`). Дальше – фаза чтения без правок: сертификат 657 строк,
  два рендера, плюс зонд по двум моделям. Эту тишину надо было объявить заранее –
  не объявила, отсюда и вопрос ведущей.
- **~15:52–15:58** прочитаны `engines/methodology_cert.py`, `engines/decomposer.py:584/1517`,
  `aurora_html/sections.py:1802`, `aurora_pptx/builder.py:145/3083`,
  `utils/data_fingerprint.py`, `engines/modeler.py:182-292` (резолвер), `engines/json_export.py`
  (формулировки для случая `auto`), существующие тесты сертификата.
- **~15:58** зонд по двум моделям. Свежая: `adstock_selection` по 4 каналам
  (requested=auto, resolved=geometric, by=bic), `data_fingerprint` с обеими половинами.
  Старая (24.06, копия в scratchpad): `reproducibility` пуст, `adstock_selection` нет,
  `channel_adstock_types` = 'auto' у всех 5 каналов → читать настройку нельзя.
- **16:10** правка `aurora_html/sections.py` (mtime): подраздел + два блока.
- **16:11** правка `engines/methodology_cert.py` и новый `tests/test_cert_data_and_adstock.py` (mtime).
- **16:12** правка `aurora_pptx/builder.py` (mtime): краткие строки на слайд + сжатие шага строк.
- между ними – живые прогоны: сертификат на двух моделях, сборка настоящих HTML и PPTX,
  разбор слайда 9 по координатам; мутации продуктового кода (3 штуки, все откачены).
- **16:13:35** запись изменений `ecda6a7` своим pathspec (время из git).
- **16:15** отчёт отдан ведущей; задача закрыта. Целевые тесты 122/122.
