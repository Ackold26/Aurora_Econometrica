## Пересказ задачи своими словами

Нужно показать в самом интерфейсе программы (не в HTML/PPTX — там уже правится параллельно) критерий совпадения — насколько числа обязаны сойтись при независимой перепроверке расчёта. Данные уже едут во фронтенд бесплатно: `decomposeData.methodology_certificate.repro_tolerance` (подтверждено разведкой PULSE_h4_recon_2026-08-31.md). Rust-команду писать не нужно. Место — шаг «Декомпозиция» (DecomposeStep.svelte), отдельным блоком НИЖЕ существующей плашки ненадёжности (reliability-warn-banner) — они про разное. Текст утверждён владельцем дословно, менять нельзя. Если поля `repro_tolerance` нет — блок не рисовать вовсе, без заглушки.

Обязательно: перед кодированием прочитать реальный `results/decomposition.json` из демо-проекта и показать team-lead фактическое содержимое `methodology_certificate.repro_tolerance` — какие поля и значения там на самом деле, компонент должен читать именно эти имена.

## План

1. Прочитать `results/decomposition.json` демо-проекта (`C:\Users\ackol\AppData\Roaming\com.aurora.econometrica\projects\demo-fmcg-brand`), выписать точную структуру `methodology_certificate.repro_tolerance`.
2. Свериться с `sidecar/econometrica/engines/methodology_cert.py:653` (`_extract_repro_tolerance`) — откуда эти поля формируются, чтобы имена совпадали 1:1.
3. Прочитать `DecomposeStep.svelte` вокруг reliability-warn-banner (~строки 500-570) — стиль, классы, паттерн карточек по соседству.
4. Прочитать `project-state.js` — как устроен `decomposeData` store, подтвердить что `methodology_certificate` реально прокидывается без фильтрации.
5. Написать Svelte-блок «Критерий совпадения»: читает `data.methodology_certificate?.repro_tolerance`, если нет — не рендерит. Текст — дословно утверждённый, значения — из данных.
6. `npm run check` — 0 ошибок.
7. Показать team-lead фактическое содержимое JSON + диф кода в отчёте.

## Отметки времени

- 23:26 — старт, маячок создан, прочитана прошлая разведка PULSE_h4_recon_2026-08-31.md (готовая фактура: цепочка данных, номера строк, три ветви допуска).
- 23:28 — прочитан фактический `results/decomposition.json` демо-проекта (`demo-fmcg-brand`) — снята точная структура `methodology_certificate.repro_tolerance` (поле `applicable.mode/title/tolerances/reason/total_draws`, ветвь в демо-проекте = `other_seed_full`, roi=beta=decay=alpha=gamma=5.0, contribution_pct=1.5).
- 23:29 — прочитан `methodology_cert.py:_extract_repro_tolerance` (1170-1210) + `repro_tolerance.py:criterion_for_certificate` (697-745) — подтверждено: поле `applicable` = `None`, если паспорт прогона пуст (статусы `deterministic`/`absent`), заполнено только при `declared`. Это значит проверка «есть title + числовые tolerances» сама по себе корректно закрывает все три статуса без явной проверки `status`.
- 23:30 — правка `DecomposeStep.svelte`: derived `reproApplicable`/`reproTolerance` + хелпер `fmtTolerance` (после `modelIsUnreliable`, до `roiNote`); markup-блок «Критерий совпадения» между `reliability-warn-banner` и `insight-banner`; стили `.repro-tolerance-card`/`.repro-lead`/`.repro-branch`/`.repro-tolerances`/`.repro-basis`/`.repro-caveat` рядом с `.reliability-warn-*`.
- 23:31 — `npm run check`: 4159 файлов, 0 ошибок, 177 предупреждений (все в чужих файлах, ни одного в `DecomposeStep.svelte`).
- 23:32 — `git diff --stat` на файл: 1 файл изменён, +77 строк, 0 удалений — хирургическая правка подтверждена диффом, не только рапортом.

## Отчёт

См. итоговый текст, отправленный team-lead через SendMessage (дословное содержимое JSON демо-проекта + диф кода + путь и номера строк).
