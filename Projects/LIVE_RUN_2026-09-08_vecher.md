# Живой прогон установленной ПЯТОЙ сборки — 08.09.2026, вечер

Сборка: установщик 11:59 (259 548 911 б, `00f93ebe…`), поставлен поверх четвёртой; в системе
`aurora-econometrica-gui.exe` от 11:53. 🔴 До этого прогона на машине стояла ЧЕТВЁРТАЯ сборка
(10:04) — прогон четвёртой не считался бы проверкой того, что поедет клиенту.

## 🟢 Закрыто живьём

1. **Новый проект НЕ наследует результаты предыдущего** (главный непроверенный пункт).
   Открыт `demo-fmcg-brand` (все шаги зелёные, «Модель обучена, R² = 0.936») → создан новый
   проект → все шаги серые, шаг «Модель» недоступен, зона импорта чистая. Диск: `models/` пуст,
   `results/` — только пустой `scenarios/`. Эталонный проект цел.
2. **Экран «Декомпозиция» полон** (находка 08.09 о пустом экране при значке «Готово» — исправлена).
3. **Блок «Критерий совпадения»** увиден впервые: тексты честные, пределы «получены измерением
   повторных прогонов, не назначены».
4. **Лист «Декомпозиция» в выгрузке .xlsx** корректен: 10 листов, ИТОГО 12 191 236 278 ₽ сходится
   с экраном; «ROI каналов» сходится до рубля с `results/decomposition.json`.
5. **Баннер ориентировочности** (пункт «а» чек-листа) есть: «Данных мало (Ratio 3.9:1 < 4:1)».
6. **Мастер НЕ переспрашивает** пустые поля проекта (пункт «б» чек-листа): проект открылся сразу
   на шаге «Оптимизация», диалогов нет.
7. **Установка без прав администратора** подтверждена экраном: каталог `%LOCALAPPDATA%\Optimizer MMM`,
   требуется 1,8 ГБ.

## 🔴 Находки прогона

**БЛОК-1 (был блокером показа, на машине устранён).** Эталонный проект лежал в
`%APPDATA%\com.aurora.econometrica\projects\`, а программа читает
`%APPDATA%\aurora-econometrica-gui\projects\`. На стартовом экране «Продолжить проект →» открывал
мусорный проект, `demo-fmcg-brand` в списке отсутствовал. Устранено копированием проекта в
рабочий каталог; мусорный проект убран в `_removed_projects_2026-09-08`.

**ПОКАЗ-1.** В списке проектов видны ТРИ клиентских проекта «Кагоцел РФ…». На показе покупателю
(рекламное агентство) это чужие клиенты на экране. Решение владельца: чистить список, показывать
с чистого профиля или переименовать. Команда переноса — в памятке.

**КОД-1 (High).** `src-tauri/src/commands/project.rs:141` — `let identifier = env!("CARGO_PKG_NAME")`:
в переменную с именем «идентификатор» кладётся имя пакета Cargo. Следствия:
(а) проекты живут в каталоге имени пакета, прочие данные — в каталоге идентификатора
`com.aurora.econometrica` (расщепление профиля);
(б) 🔴 **настройка «папка проектов» мертва**: `set_econometrica_projects_root` (lib.rs:2586) пишет
в `app_config_dir()` = `%APPDATA%\com.aurora.econometrica\user_config.json` (файл есть, ключ
`econometrica_projects_root: null`), а `projects_dir()` читает
`%APPDATA%\aurora-econometrica-gui\user_config.json` (файла нет и не будет). Пользователь выбирает
свою папку, получает «сохранено», проекты создаются в старом месте.
🔴 Чинить ТОЛЬКО чтение настройки; менять место хранения по умолчанию нельзя без переноса —
у пользователей проекты уже лежат в каталоге имени пакета.

**GUI-1 (Medium).** Удалён активный проект → стартовый экран теряет кнопку «Продолжить проект →»
и показывает только «Новый проект», хотя на диске девять проектов. Путь восстановления есть
(«+ Создать проект» → список), но экран о нём не сообщает.

**GUI-2 (Medium).** Удаление проекта — **без единого подтверждения**, один клик по корзине в узкой
строке рядом с иконкой копирования. Смягчение: удаление мягкое, проект целиком уезжает в корзину
Windows (проверено: `$RH9XBL4` с `data/`, `models/`, `results/`, `project.json`). Но продукт об
этом не говорит.

**ЧИСЛА-1 (Medium).** Одно значение `efficiency_gap = -38.5` показано на ОДНОМ экране
«Декомпозиция» тремя формами и двумя единицами: подсказка — «gap -38 пп», главная рекомендация —
«gap -39%», таблица — «-38.5%». Единица должна быть одна (это процентные пункты), округление одно.

**ТЕКСТ-1 (Medium).** Шаг «Отчёт» на полностью посчитанном проекте: панель подсказок пишет
«Загрузите данные для получения рекомендаций», счётчик 0.

**ГРАФ-1 (проверить).** Шаг «Отчёт», «Анализ чувствительности»: подписи семи параметров есть,
столбцов не видно ни одного.

**ОФОРМЛЕНИЕ-1 (Low).** Диаграмма «Декомпозиция продаж»: подписи каналов обрезаны
(«Performance бю…», «Онлайн-ви…», «Наружная …»).

**УСТАНОВЩИК-1 (Low, но это лицо продукта).** Мастер установки полностью англоязычный:
«Welcome to Optimizer MMM Setup», «Choose Install Location», «Create desktop shortcut»,
«© 2026 Aurora Platform LLC». Флаг `/S` тихой установки не даёт — открывается окно мастера.

**СЦЕНАРИЙ-1.** Пункт «в» чек-листа устарел: интерфейс показывает «Оценка: 10-30 секунд
(8 каналов)», а заготовленная реплика сценария говорит «верхняя оценка по умолчанию — до десяти
минут». Реплику надо переписать по экрану, иначе оператор соврёт покупателю.

## Условия прогона
Рабочий стол делят четыре сессии; их терминалы перехватывают передний план. Окна соседей были
свёрнуты на время прогона с согласия `aurora-ai-1e` и `aurora-ai-f5` и восстановлены после.
Инструмент управления рабочим столом теряет часть символов при наборе в поля WebView и не
передаёт сочетания клавиш — это свойство связки, НЕ дефект продукта.
