# Криминалистика установщика Optimizer MMM 2.5.4 (s49)

Дата: 18.09.2026, 11:01–11:20. Проверяю делом, что реально лежит в передаточном пакете покупателю — установщик со сборки 14.09 10:31:26, или устаревшая копия до правок второй волны аудита (запись `5ee6c875`, исходники правлены 14.09 09:49–10:04).

## 1. Совпадение копий (sha256)

Команда: `Get-FileHash -Algorithm SHA256`

| Файл | Размер | Время | SHA256 |
|---|---|---|---|
| `D:\cargo-targets\econometrica\release\bundle\nsis\Optimizer MMM_2.5.4_x64-setup.exe` | 257 712 997 байт | 14.09.2026 10:31:26 | `F61E06E0E0DF39A1D0759AB5FE6AB7E1F7B18BC16D466BAE29689A7E1BD4CC3` |
| `C:\Users\ackol\Desktop\Передаточный пакет\...\Инсталлятор\Optimizer MMM_2.5.4_x64-setup.exe` | 257 712 997 байт | 14.09.2026 10:31:58 | `F61E06E0E0DF39A1D0759AB5FE6AB7E1F7B18BC16D466BAE29689A7E1BD4CC3` |

**Вердикт: хеши совпадают побайтово. ДОКАЗАНО** — файл в передаточном пакете есть точная копия файла сборки (не отдельный, случайно похожий файл).

## 2. Шлюз в несжатом двоичном файле

Команда: `grep -a -o -c 'aurora_gateway' <файл>`, адрес шлюза найден в исходнике: `src-tauri/src/commands/gateway_executor.rs:41` — `fn gateway_base_url() { std::env::var("AURORA_CLOUD_URL").unwrap_or_else(|_| "https://rag.auroraai.pro/cloud".to_string()) }`.

**🔴 Важная оговорка.** Внешняя копия `D:\cargo-targets\econometrica\release\aurora-econometrica-gui.exe` (та, что просили проверить в задаче) НЕ идентична по хешу exe, реально запакованному в установщик — см. раздел «Побочная находка» ниже. Поэтому числа ниже приведены для файла, распакованного ИЗ САМОГО установщика (`$PLUGINSDIR`-независимая корневая копия `aurora-econometrica-gui.exe` внутри 7z-распаковки NSIS) — единственного файла, за содержимое которого можно ручаться.

| Признак | Внешняя копия (release/) | Внутри установщика |
|---|---|---|
| `aurora_gateway` (вхождений) | 3 | 3 |
| `rag.auroraai.pro/cloud` (вхождений) | 1 | 1 |

**Вердикт: ДОКАЗАНО** (≥3 и ≥1 — оба ожидания выполнены, проверено на файле из самого установщика).

## 3. Правки второй волны внутри отгружаемого движка/фронтенда

Установщик распакован: `7z x "Optimizer MMM_2.5.4_x64-setup.exe" -o<scratchpad>\installer_extract` — 5875 файлов, 989 360 853 байт, без ошибок ("Everything is Ok"). PyInstaller-движок найден в `_up_\sidecar\econometrica\` (`econometrica-sidecar.exe` + `_internal\`), где `.py`-файлы лежат **открытым текстом** (не в `.pyc`/PYZ), время правки — 14.09 10:10 (после исходной правки 09:49–10:04, до сборки установщика 10:31).

### 3а. builder.py — `_fmt_mln` → `format_realloc_mln`

Файл: `_up_\sidecar\econometrica\_internal\aurora_pptx\builder.py` (plaintext).

- `grep -n "_fmt_mln"` → **0 вхождений** (локальная функция снята).
- `grep -n "format_realloc_mln"` → **12 вхождений**, сгруппированы в 3 блока: строки 1006–1042 (импорт + 3 использования), 2875–2918 (импорт + 3 использования), 3002–3023 (импорт + 3 использования) — ровно **три места печати**, как описано в коммите `5ee6c875`.

**Вердикт: ДОКАЗАНО напрямую** (прямое чтение исходного .py внутри установщика).

### 3б. interactive.py — анимация в ветке data-copy-chart отключена

Файл: `_up_\sidecar\econometrica\_internal\aurora_html\interactive.py` (plaintext).

Ветка `[data-copy-chart]` (строки 1045–1094) содержит:
```
opt.animation = false;
exportChart.setOption(opt, true);
url = exportChart.getDataURL({{ type: 'png', pixelRatio: 2, ... }});
```
с комментарием, дословно описывающим найденную аудитом проблему («s48… анимация ему не нужна никогда»).

**Вердикт: ДОКАЗАНО напрямую** (прямое чтение исходного .py внутри установщика).

### 3в. user_config.rs — сторож двузначности номера редакции

Diff коммита `5ee6c875` показывает добавление `assert_eq!(number_part.len(), 2, ...)` внутри `mod trial_terms_frontend_consistency_guard`. Эта функция и весь модуль помечены `#[cfg(test)]` (строки 717–718 файла на 18.09) — **код компилируется ТОЛЬКО при `cargo test`, в релизный бинарник не попадает по конструкции.**

**Вердикт: НЕ ПРИМЕНИМО / нельзя ни доказать, ни опровергнуть присутствие в установщике** — это dev-time проверка качества сборки (гарантия, что TRIAL_TERMS_REVISION в исходниках двузначный на момент `cargo test`), а не рантайм-функция продукта. Искать её в поставке физически бессмысленно: по конструкции её там нет и не может быть, и это не дефект поставки.

### 3г. TrialConsentOverlay.svelte — перехват клика мышью

Прямой текстовый поиск в exe внутри установщика (`aria-labelledby="trial-consent-title"`, `"Открыть условия"`, `"редакция 2 от 14.09.2026"`, `isKeydownOutsideBlockingOverlay`) дал **0 совпадений по всем маркерам** — фронтенд (SvelteKit, `frontendDist: "../build"`) встроен в бинарник Tauri v2 в сжатом виде (проверка на gzip-сигнатуры дала 0 настоящих потоков — вероятно brotli), plaintext-grep не может прочитать содержимое.

Вместо этого проверила **локальный артефакт `npm run build`** — папку `build/` в рабочем дереве репозитория (это тот же каталог, что `beforeBuildCommand: "npm run build"` в `tauri.conf.json` генерирует ПЕРЕД сборкой Rust/NSIS):
- `build/_app/immutable/nodes/0.CYng2EPA.js`, mtime **14.09.2026 10:18** — между правкой исходника (10:04) и компиляцией `aurora-econometrica-gui.exe` (10:23); `git status build/` — «working tree clean», то есть содержимое не менялось с тех пор.
- Файл содержит: `"редакция 2 от 14.09.2026"` (текст новой Условий) и структурно — два парных `window.addEventListener(..., E, !0)` с идентичной функцией-обработчиком (`preventDefault()+stopPropagation()`): один на `"keydown"` (существовавший раньше), второй, следующий сразу за ним в коде, на `"click"` (новый) — минифицированные имена не позволяют сослаться на `isKeydownOutsideBlockingOverlay` по имени, но структура (второй capture-listener с той же логикой, появившийся ПОСЛЕ keydown-блока) точно соответствует правке из diff.

**Вердикт: ДОКАЗАНО КОСВЕННО.** Прямая побайтовая сверка с содержимым установленного exe невозможна (фронтенд-ассеты сжаты внутри бинарника, инструментов для распаковки brotli-потоков из PE не было под рукой). Косвенность держится на двух опорах: (1) временная цепочка правка исходника (10:04) → сборка фронтенда (10:18, не менялась с тех пор) → компиляция exe (10:23) → упаковка установщика (10:31) не оставляет окна для устаревшей версии; (2) структурное совпадение кода в этой сборке с ожидаемой правкой.

## 4. Версия и режим установки

- **Версия**: `FileVersion`/`ProductVersion` exe внутри установщика = `2.5.4` (оба поля), имя файла установщика — `Optimizer MMM_2.5.4_x64-setup.exe`, `tauri.conf.json:4` — `"version": "2.5.4"`. **ДОКАЗАНО напрямую.**
- **Режим установки**: `tauri.conf.json:47` — `"installMode": "currentUser"` (источник конфигурации сборки). В `$PLUGINSDIR` установщика (7z-листинг) присутствуют только `nsDialogs.dll`, `nsExec.dll`, `nsis_tauri_utils.dll`, `NSISdl.dll`, `StartMenu.dll`, `System.dll` — **`UAC.dll` (обязательный NSIS-плагин elevation для perMachine-режима) отсутствует**, как и строки `Program Files`/`LOCALAPPDATA` в самом сжатом установщике (закодированы как встроенные NSIS-переменные, не текст, поэтому не ищутся grep'ом напрямую). **Вердикт: ДОКАЗАНО КОСВЕННО** — отсутствие плагина elevation исключает возможность запроса прав администратора этим установщиком; прямого текстового `RequestExecutionLevel user` найти не удалось (в сжатом виде не читается, а в распакованных данных 7z скрипт-логика NSIS не сохраняется отдельно).

## 🔴 Побочная находка: exe в release/ ≠ exe внутри установщика (не влияет на выводы, но важно знать)

`D:\cargo-targets\econometrica\release\aurora-econometrica-gui.exe` (внешняя копия, время 10:31, SHA256 `3D52D1EB...`) НЕ совпадает по хешу с `aurora-econometrica-gui.exe`, реально лежащим внутри установщика (время 10:23, SHA256 `02989F0B...`). Расследовано командой `cmp -l`: различаются ровно **3 байта из 16 015 872** (99.99998% идентичность), локализованы в служебной таблице MIME-типов Tauri (`__TAURI_BUNDLE_TYPE_VAR_`: `"NSS."` против `"UNK."`) — не относится ни к одной из 4 проверяемых правок. Размер (16 015 872 байт), версия, PDB-имя и все контрольные строки (`aurora_gateway`×3, адрес шлюза×1, click-паттерн×1) идентичны в обеих копиях. Вывод: это две сборки одного кода (недетерминированная линковка Rust/MSVC меняет служебные метаданные), а не разные версии продукта — но **для любых будущих проверок нужно распаковывать сам установщик, а не полагаться на отдельно лежащую копию exe в `release/`.**

## 🔴 Побочная находка: диск D: заполнен на 99.75%

В процессе работы обнаружено: `D:` — 1 023 635 452 KB всего, доступно **2 557 196 KB (~2,5 ГБ)**, использовано 99,75%. Одна операция записи (`sed -i` в PULSE-файл репозитория) уже упала с `No space left on device`. Риск для всех сборок и git-операций команды на этом диске — рекомендую team-lead освободить место в приоритетном порядке (сама я работаю в режиме «только чтение репозитория» и ничего не удаляла).

## Итог

**В передаточном пакете лежит установщик, содержащий правки второй волны — ДА, доказано частично.**

Разбивка по строгости:
- Совпадение копий (пакет = сборка) — доказано прямо.
- Шлюз зашит — доказано прямо (на файле из установщика, не из отдельной копии release/).
- builder.py (_fmt_mln → format_realloc_mln) — доказано прямо.
- interactive.py (анимация выгрузки отключена) — доказано прямо.
- user_config.rs (сторож редакции) — не применимо к поставке (test-only код, по конструкции не едет в релиз; это не пробел проверки, а свойство самой правки).
- TrialConsentOverlay.svelte (перехват клика) — доказано косвенно (временная цепочка + структура кода в build/, не прямая сверка байт установленного exe — фронтенд сжат внутри бинарника, инструмента для его распаковки не нашлось).
- Версия 2.5.4 — доказано прямо. Режим установки currentUser — доказано косвенно (отсутствие плагина elevation).

Единственное, что осталось не доказано напрямую — правка мыши внутри самого установленного exe (косвенно доказано) и режим установки текстом NSIS-скрипта (косвенно доказано через отсутствие UAC-плагина). Оба этих пункта подкреплены разумными, явно помеченными косвенными доказательствами, не выдуманными.
