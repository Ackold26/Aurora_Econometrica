# PULSE — разведка CPD-77 в Legal Center, Creative Center, Oracle

## Задача своими словами
Нужно найти в трёх продуктах (Legal, Creative Center, Oracle) все места вызова `icacls` (замена прав доступа при старте, теряет SYSTEM/Администраторы) и `netstat`/`taskkill` (поиск своего процесса вместо чтения PID из файла состояния) — тот же класс дефекта, что уже починили в Эконометрике и Docs Lab. Для каждого продукта определить правильное рабочее дерево, найти все вызовы (включая спрятанные за обёртками), проверить наличие файла состояния с PID и функции проверки "процесс наш", и попробовать (только `--check`, без применения) наложить эталонный патч. Это чистая разведка — ни одной правки кода, отчёт пойдёт другому исполнителю после внешнего аудита.

## План по шагам
1. Определить правильное рабочее дерево для каждого продукта (ветки, теги).
2. Legal Center: широкий поиск `icacls`/`netstat`/`taskkill`/`obfstr`, контекст вызовов, файл состояния, функция is_our_process.
3. Creative Center: то же.
4. Oracle: то же.
5. Проверка `--check` эталонного патча (сначала посмотреть эталонные коммиты Эконометрики/Docs Lab).
6. Положительный контроль поиска (показать что тот же grep находит вызов в непочиненной версии).
7. Записать отчёт SCOUT_CPD77_REMAINING_2026-08-14.md.

## Лог
- 03:19:01 — старт, создан этот файл, получен листинг D:\Docs\Aurora_Ai\Dev. Кандидаты деревьев: Legal — `_wt_legal_master`, `ROSST_AI_Legal`, `ROSST_AI_Legal_commercial`; Creative — `Aurora_Creative_Hub`, `ROSST_AI_Creative` (symlink); Oracle — `Aurora_Oracle`, `_wt_oracle_master`, `Oracle-*` (много вариантов). Дальше — проверка веток/тегов в каждом.
- 03:21 — деревья определены:
  - **Legal → `_wt_legal_master`** (стоит на `master`, ветка из ТЗ; версия tauri.conf 0.13.2). `ROSST_AI_Legal` стоит на `fix/updater-path-traversal`, `ROSST_AI_Legal_commercial` на `feat/legal-commercial-readiness` — рабочие worktree того же репо, не выпускная ветка.
  - **Creative → `Aurora_Creative_Hub`** (стоит на `master`, версия 0.10.0, теги v0.9.9-creative-center рядом). `ROSST_AI_Creative` — symlink в `_archive`, не рабочее дерево.
  - **Oracle → `_wt_oracle_gwsign`** (стоит на `feat/oracle-install-per-user` — ровно ветка из ТЗ, версия 0.4.7, самая свежая история). `Aurora_Oracle` стоит на `feat/oracle-report-hygiene` (0.4.2), `_wt_oracle_master` на `master` (0.4.3) — не та ветка.
  - Эталон Docs Lab — дерево `_wt_cpd77_docslab`, ветка `cpd77-docslab-fix`, правка `5ea8f00`.
  - Дальше — широкий grep `icacls`/`netstat`/`taskkill`/`obfstr` по всем трём деревьям.
- 03:24 — grep завершён. Все три продукта — по ОДНОМУ вызову `icacls` в `session/manager.rs::SessionManager::new()`, никакой обёртки/обфускации (в отличие от Docs Lab с `proc::hidden`). `netstat` найден ТОЛЬКО в Legal (`commands/bridge.rs::pid_listening_on`, вызывается из `stop_gateway` ← `ensure_gateway_running` — живой путь при резидентном режиме, НЕ миграционный). У Creative Hub и Oracle нет ни одного файла верхнего уровня типа `*_sidecar.rs` и ни одного вхождения `netstat` — паттерн (Б) к ним неприменим структурно (нет процесса-сайдкара с собственным PID, который надо было бы искать). `obfstr` в всех трёх — 9-12 упоминаний, но все в `online_auth.rs`/`updater.rs`/`content_updater.rs`/`feedback.rs`/`Cargo.toml`/`Cargo.lock` — обфускация секретов/URL, НЕ обёртка над `icacls`/`netstat`. Положительный контроль поиска пройден (тот же grep находит 6 icacls в Эконометрике до правки — `ea560be~1`). `git apply --check` обоих эталонных патчей (Docs Lab, Эконометрика) провалился на все три дерева — не хватает файлов `*_sidecar.rs`, и `session/manager.rs` не совпадает по контексту (другой способ построения `sessions_root`: Legal/Oracle — прямой `LOCALAPPDATA::join`, Creative Hub — `durable_store::app_state_dir`). Правка (А) везде текстуально механическая по образцу (структура блока идентична), но патч нужно писать руками под каждое дерево, не копировать. Дальше — записать финальный отчёт.
- 03:25 — отчёт SCOUT_CPD77_REMAINING_2026-08-14.md записан. Разведка завершена, правок не вносила.
- 03:28-03:31 — team-lead прислал уточнение путей (ROSST_AI_Legal вместо `_wt_legal_master`, Aurora_Oracle вместо `_wt_oracle_gwsign`), сославшись на реестр. Проверила по меткам версий (не по активности, как просили пунктом 6): у Legal последний тег `v0.13.2-legal` (2026-08-10) лежит в `master`/`_wt_legal_master`, а `ROSST_AI_Legal` стоит на `fix/updater-path-traversal` от 2026-08-03 — тега не содержит, код icacls там СТАРОГО стиля (`match Command::new(...).args()` вместо `let mut icacls; icacls.args()`), и `bridge.rs`/netstat там нет вообще (фичи ещё не было). У Oracle тег `v0.4.5-oracle` (2026-08-10) содержится ТОЛЬКО в `feat/oracle-install-per-user` (= `_wt_oracle_gwsign`, ушла дальше до 0.4.7); `Aurora_Oracle` стоит на `feat/oracle-report-hygiene` от 2026-07-27 — тега не содержит. Вывод: мои исходные деревья (`_wt_legal_master`, `_wt_oracle_gwsign`) подтверждены версионными метками, пути из уточнения team-lead — устаревшие worktree того же репо. Досчитала `obfstr!` точным паттерном (макрос, не подстрока) — ровно 6 в каждом из трёх продуктов, реестр прав, моя первая цифра (9-12) считала заодно упоминания в Cargo.lock/Cargo.toml. netstat в обоих устаревших деревьях (ROSST_AI_Legal, Aurora_Oracle) тоже отсутствует — доп. подтверждение, что это более ранние снимки без резидентной фичи. Отправляю уточнённый ответ team-lead.
