# Аудит блока P0.2–P0.5 (2026-08-03)

начат. Формат: severity | file:line | суть | сценарий

Critical | src/lib/components/pipeline/ValidateStepV13.svelte:810 + 835-848 | Жёсткая блокировка кнопки «Подтвердить роли» переехала на эффективный знаменатель — handoff §4.1 утверждает обратное («порог остался на сыром») | 40 наблюдений, 10 назначенных столбцов, байес: было 40/10=4.0 → кнопка активна; стало 40/23=1.7 → disabled. Пилот, работавший вчера, сегодня не проходит валидацию

High | src/lib/ratio-classifier.js:2070-2095 (не знает use_holidays) ↔ TrainRequest.use_holidays | Мастер-переключатель «Учитывать праздники РФ» (ВЫКЛ) не уменьшает знаменатель — документированное лекарство «отключи праздники, если данных мало» стало инертным | 40 obs / 10 столбцов, праздники ВЫКЛ: модель заведёт 11 параметров (3.6), экран показывает 1.7 и держит блокировку; выхода нет

High | src/lib/project-state.js:1992-1999 (validationHeaderMetrics) | Эффективный знаменатель в шапке берётся из ответа движка БЕЗ живого числа ролей — нарушен инвариант §2.8 handoff; в том же объекте ratioRaw живой | 52 obs, 8 медиа + 2 контроля; снять 3 канала с роли: карточка и подсказки → 2.6, шапка → 2.3, ratioRaw → 7.4. Три числа на одном экране

High | src/lib/insights-rules.js:267-269 (validateRolesInsights) | Режим не прокинут — под-шаг «Роли колонок» всегда считает по байесовскому знаменателю | Проект в OLS: шапка 4.7 («рекомендуемый»), подсказки под ней 2.3 «критически мало»

High | sidecar/econometrica/engines/validator.py:399-421 ↔ src/lib/project-state.js | Тексты issues/warnings движка несут OLS-число, а экран — байесовское; issues рендерятся (ValidateStep.svelte:361, ColumnMapperConfirm.svelte:144) | 30 obs / 10 столбцов, байес: движок «Ratio 2.7:1 – ниже минимума», карточка рядом «1.3 – критически мало» + блокировка

Medium | sidecar/econometrica/engines/validator.py:288-306 | missing_pct считается только для числовых колонок | Канал с текстовой ячейкой «н/д» → dtype object → stats нет → пять экранов снова показывают 0% пропусков именно на битой колонке

Medium | docs/USER_GUIDE_v2_1_0.md:250,257,358 + tools/build_glossary.py:66 | NRMSE снят из глоссария, но руководство пользователя и таблица генератора о нём не знают | Клик по ссылке `GLOSSARY_v2_1_0.md#nrmse-нормализованная-ошибка` из руководства ведёт в никуда; в таблице порогов остаётся метрика, которой в продукте нет

— Проверено и НЕ находка: server.py `config = req.model_dump()` (:882, :1223) — seed доезжает до train_model; compute_sensitivity_tornado действительно возвращает ключ `parameters` (sensitivity.py:100,173) — условие показа в ReportStep корректно; seed_from_model для моделей до P0.2 честно даёт default; AST-сторожа ловят реальные инварианты.
