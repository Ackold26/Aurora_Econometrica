# Пульс — трассирующий срез общего Rust-ядра (15.08.2026)

## Пересказ задачи своими словами

Выношу первый, трассирующий срез общего крейта `aurora_core` в репозиторий `aurora-platform-core`
(рабочая копия `_wt_core_tracer`, ветка `feat/aurora-core-tracer`). Переношу ровно два предмета из
продукта Aurora AI Econometrica (`Aurora_Econometrica_thinwt`, ветка `master` — только читаю): файл
`errors.rs` целиком и тело обратной связи из `commands/feedback.rs` (`CATEGORY_MAP`, `form_category`,
логика отправки) — без атрибута `#[tauri::command]`, он остаётся в продукте как граница. В продукте
оба файла становятся тонким реэкспортом/обёрткой, остальной код продукта (десятки `use crate::errors::…`)
не трогаю. Подключаю крейт временно путём (`path = "D:/…/_wt_core_tracer/aurora_core"`) только для
проверки сборки; перед записью в продукт заменяю на закомментированную заглушку с будущей git-меткой.
Решение принято владельцем в `CRATE_DESIGN_2026-08-15.md` (особенно развилки 1 и 3) — не пересматриваю.
Мерило верного переноса — сторож `guard_cpd88_feedback_category_map.rs`: он линкуется с
`aurora_econometrica_lib::commands::feedback::form_category`, и должен остаться зелёным.

## План

1. Маячок (этот файл) — сделано.
2. Прочитать `CRATE_DESIGN_2026-08-15.md` целиком — сделано (до маячка, по ходу разбора входа).
3. Изучить образец `aurora_gateway/Cargo.toml`, проверить ветки/статус обеих рабочих копий — сделано.
4. Прочитать `errors.rs`, `commands/feedback.rs`, сторож `guard_cpd88_feedback_category_map.rs`,
   `Cargo.toml` продукта — сделано.
5. Создать крейт `aurora_core` (Cargo.toml + src/lib.rs + src/errors.rs + src/feedback.rs) в
   `_wt_core_tracer`.
6. Собрать и прогнать `cargo test` крейта (`CARGO_TARGET_DIR=D:/cargo-targets/aurora-core`).
7. Завести ветку `core-tracer-econ` в продукте от `master`; править `errors.rs` →
   реэкспорт, `commands/feedback.rs` → обёртка+реэкспорт, `Cargo.toml` → временный путь.
8. Прогнать тесты продукта (`--lib -- --skip content_updater`, затем `content_updater` отдельно с
   паузой), отдельно сторожи CPD-77/79 и CPD-88.
9. Перед записью — заменить временный путь на закомментированную заглушку с будущей меткой.
10. `git add` поимённо в обеих копиях, коммит (без пуша).
11. Написать отчёт `FIX_CORE_TRACER_2026-08-15.md`.
12. Отчитаться команде.

## Ход (отметки по факту)

- **13:40** — старт. Прочитан `CRATE_DESIGN_2026-08-15.md` целиком.
- **13:41** — проверены рабочие копии: `_wt_core_tracer` на `feat/aurora-core-tracer` (чисто),
  продукт на `master` (много чужих незакоммиченных файлов от параллельных сессий — не трогаю,
  это shared-репо, коммичу только своим pathspec). Прочитан `aurora_gateway/Cargo.toml` как образец.
- **13:42** — прочитаны `errors.rs`, `commands/feedback.rs`, сторож CPD-88, `Cargo.toml` продукта.
  Маячок создан.
- **13:45** — крейт `aurora_core` создан в `_wt_core_tracer` (Cargo.toml, lib.rs, errors.rs,
  feedback.rs — тело без `#[tauri::command]`, функция переименована `submit_feedback` → `submit`).
  `cargo test` (CARGO_TARGET_DIR=D:/cargo-targets/aurora-core): **4 passed / 0 failed**. Закоммичено
  в `feat/aurora-core-tracer` (35d8345), не запушено.
- **13:47** — в продукте заведена ветка `core-tracer-econ` от `master`. `errors.rs` → реэкспорт,
  `commands/feedback.rs` → реэкспорт + тонкая обёртка команды (юнит-тесты, дублировавшие
  проверку `form_category`, удалены вместе с телом — они теперь 1:1 совпадают с тестами крейта,
  сторож CPD-88 их не заменяет, а дополняет проверкой стыка с разметкой). `Cargo.toml` — временная
  path-зависимость на `aurora_core`, раскомментирована для проверки сборки. Дальше — сборка/прогон.
- **13:52** — прогон продукта с раскомментированным путём: `--lib -- --skip content_updater` →
  406/0/2, `content_updater` отдельно → 53/0/1 (сразу зелёный, без повторов), итого 459/0/3.
  Сторож CPD-77/79 → 7/0, сторож CPD-88 → 5/0 (главный признак: линкуется с
  `aurora_econometrica_lib::commands::feedback::form_category` через реэкспорт — собрался и зелёный).
  Разница 463→459 объяснена: 4 теста (2 из errors.rs, 2 из feedback.rs) переехали в крейт вместе
  с кодом и там же зелёные (см. отметку 13:45).
- **13:53** — путь в `Cargo.toml` возвращён к закомментированной заглушке. Коммит продукта в
  `core-tracer-econ` (4c32445), pre-commit (lefthook, v40-xss) прошёл. Не запушено.
- **13:57** — отчёт `FIX_CORE_TRACER_2026-08-15.md` написан. Задача завершена: крейт создан и
  зелёный (4/0), продукт реэкспортирует оба файла, сторож CPD-88 зелёный (5/0) с временно
  раскомментированным путём, путь возвращён к заглушке, оба коммита на месте, ничего не запушено.
  Сообщение команде — далее.
