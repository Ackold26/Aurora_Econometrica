# CPD-151 — проверено на боевой базе: НЕ ВОСПРОИЗВОДИТСЯ

**Дата проверки:** 2026-09-08, ночь. Проверяла лично, оркестратор.
**Доступ:** личный токен доступа владельца, выдан на один заход и отозван сразу после проверки.
**Изменяющих команд не выполнялось ни одной** — только чтение.

## Что утверждал реестр

Публикуемый ключ (`anon`), вшитый в каждую программу линейки, имеет права `DELETE`/`TRUNCATE`
на боевые таблицы `licenses`, `activations`, `audit_log`; единственный рубеж — построчная защита
без политик. Отсюда следовало лечение: `revoke all … from anon, authenticated`.

## Что показала боевая база

Проект `quzhkfvglqmppxcrindh` — тот самый, чей адрес зашит в клиентский код
(`src-tauri/src/commands/updater.rs`, `online_auth.rs`), сверено перед выводом.

| таблица | владельцы прав | публикуемая роль | защита строк |
|---|---|---|---|
| `licenses` | `postgres`, `service_role` | **нет прав** | включена |
| `activations` | `postgres`, `service_role` | **нет прав** | включена |
| `audit_log` | `postgres`, `service_role` | **нет прав** | включена |
| `device_bindings` | `postgres`, `service_role` | **нет прав** | включена |
| `app_versions` | те же | `SELECT` (осмысленно: проверка обновлений) | включена |
| `content_versions` | те же | `SELECT` (осмысленно: версия содержимого) | включена |

Обходной путь проверен отдельно: роли `anon` и `authenticated` **не входят ни в какие другие
роли**, унаследовать права неоткуда. Политик на трёх названных таблицах нет — и не нужно,
поскольку прав тоже нет: запрет двойной.

## Вердикт

**Отзывать нечего.** Права, описанные в реестре, в боевой базе отсутствуют. Дефект либо был
устранён ранее без записи, либо описание было неверным с самого начала.

## Урок

Выполнение «плана лечения» без предварительного снимка состояния прошло бы незаметно: три команды
отзыва отработали бы вхолостую, вернув успех, и в память с реестром уехала бы запись о закрытии
дефекта, которого не было. Правило подтверждено ещё раз: утверждение о поведении системы —
гипотеза, пока не сверено с самой системой, сколько бы документов его ни повторяли.

## Что осталось

- закрыть строку в реестре формулировкой «не воспроизводится, проверено 08.09.2026»;
- отдельно (не срочно) выяснить, откуда взялось исходное описание.

Заготовленный пакет команд `Projects/CPD151_revoke_plan_2026-09-07.sql` оставлен на диске как
образец порядка работы с боевой базой (снимок → откат → правка → проверка), выполнять его не нужно.
