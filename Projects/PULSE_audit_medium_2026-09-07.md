# Маячок: проверка пяти находок Medium (2026-09-07)

## Задача своими словами
Внешний аудитор по записи `6446df7` выдал пять находок уровня Medium и один открытый вопрос.
Моя работа — подтвердить или опровергнуть каждую **техническим доказательством** (прогон кода или точная цепочка строк),
а не рассуждением. Код не править: только доказательства, последствие для клиента, предлагаемое лечение и его цена.

## План
1. Прочитать `Projects/audit_findings_live.md` и `Projects/handoff.md`.
2. Находка 1 (server.py:1085, обрезка хвоста медиаплана) — зонд на демонстрационном наборе: вердикт до/после.
3. Находка 2 (claude.rs:960, литерал NaN в кэше формулы .xlsx) — собрать файл, открыть openpyxl.
4. Находка 3 (report.rs:1621, ноль в кэше при нулевой сумме) — чтение кода + модель входа.
5. Находка 4 (audit.rs:48, внутрипроцессный замок) — тип замка + число вызовов записи.
6. Находка 5 (audit.rs:97, срез по длине, снятой до прогона) — точная последовательность падения.
7. OPEN: `applicable_mode` в `utils/repro_tolerance.py` — может ли вернуться режим точного совпадения.
8. Отчёт `Projects/AUDIT_medium_verdicts_2026-09-07.md`.

## Лента
- **старт** — маячок создан, иду читать отчёт аудитора и handoff.
- **22:29** — Находка 1, первый прогон готов: на демо-наборе (130 строк = 104+26) вердикт НЕ сдвинулся
  (`reliable` → `reliable`); число обусловленности 1.15 в обоих случаях при пороге 30, коэффициент вариации
  0.408–0.578 при пороге 0.10. Дальше: прогон с прогнозом по априорным (тихая фаза ~30 с) и синтетический
  случай, где обрезка вердикт всё-таки двигает.
- **22:44** — **Находка 1 — ПОДТВЕРЖДЕНА** (механизм иной, чем считал аудитор). На боевом демо-проекте
  «Демо FMCG-бренд» обрезка сдвигает вердикт `reliable` → `directional`, но не через коллинеарность
  (та неизменна, 1.15 при пороге 30), а через прогноз по априорным: покрытие 0,8 → 0,548. Воспроизведено
  дважды, зерно фиксировано. Синтетикой показано, что и через коллинеарность вердикт сдвинуть можно.
- **22:52** — **Находка 2 — ПОДТВЕРЖДЕНА** прогоном. Собран настоящий .xlsx тем же кодом на
  rust_xlsxwriter 0.79.4: в кэше `<v>NaN</v>` без `t="str"`; openpyxl в режиме чтения кэша роняет
  ВЕСЬ файл (`ValueError: invalid literal for int(): 'NaN'`), контрольная сборка без `set_result`
  даёт `<v>0</v>` и открывается. Дальше — находка 3.
- **23:05** — **Находка 3 — ПОДТВЕРЖДЕНА прогоном** (кэш 0, движок пересчёта даёт `#ДЕЛ/0!`), но
  попутно выяснилось, что весь лист «Декомпозиция» на живых данных не строится: движок отдаёт
  `waterfall` словарём, Rust ждёт массив. **Находка 4 — ПОДТВЕРЖДЕНА прогоном**: два процесса дают
  874–1005 покорёженных строк из 3200; предложенная страховка (одна строка = один `write_all`)
  даёт 12000/12000 целых. **Находка 5 — ПОДТВЕРЖДЕНА прогоном**: паника
  «start byte index 5242980 is out of bounds for string of length 2120», код выхода 101.
  Осталось: открытый вопрос про режим точного совпадения и отчёт.
- **23:20** — **Открытый вопрос закрыт: НЕ МОЖЕТ.** У `applicable_mode` три возврата
  (`MODE_STRICT`, `MODE_WIDE`, `MODE_WIDE`), `MODE_EXACT` среди них нет; живой сертификат
  демо-проекта даёт `other_seed_full` (5 % / 1,5 п.п.). Отчёт записан:
  `Projects/AUDIT_medium_verdicts_2026-09-07.md`. Работа завершена, код не менялся.

## Доп. зонд (задача от ведущей): форма waterfall
- **23:30** — старт. Первым делом проверяю, создаётся ли лист «Декомпозиция» ДО проверки формы
  (тогда он был бы пустым, а не отсутствовал). Дальше — сборка книги на РЕАЛЬНОМ
  `decomposition.json`, список листов, `build_markdown`, цитата теста-приёмщика и поиск класса
  таких разрывов по `as_array()`. Отчёт — отдельным файлом `Projects/AUDIT_waterfall_shape_2026-09-07.md`.
- **23:52** — Доп. зонд закрыт. Артефакт собран: на живом `decomposition.json` книга получается
  с листами `["Обзор","Executive Summary","Спецификация","ROI каналов"]` — «Декомпозиции» НЕТ
  (лист не пустой, а отсутствует: `add_worksheet()` внутри охранного условия); раздела «Вклады
  в продажи» в разметке нет; формул с сохранённым результатом в книге ноль. На синтетике сторожа
  лист есть и три формулы с кэшем — на месте. По `as_array()` сверены все 13 путей к результатам
  движка: разрыв формы ровно один, `waterfall`. Лечить надо Rust — форму `{labels,values,types}`
  читают ещё три работающих потребителя. Отчёт: `Projects/AUDIT_waterfall_shape_2026-09-07.md`.

## Хвост 2 + вопрос про сборку 17.08
- **23:58** — старт. Хвост 1 уже закрыт ранее (`AUDIT_waterfall_shape_2026-09-07.md`), сообщения
  разминулись. Сейчас: (а) отдать воспроизводимую команду для вердикта, (б) прочитать состояние
  кода на 17.08 и ответить, будет ли баннер на выпущенной сборке.
- **00:12** — Ответ на вопрос о сборке 17.08 получен ТРИЖДЫ независимо и сходится:
  (1) чтением `git show 14b53fc:sidecar/econometrica/server.py` — `n_obs = len(df)`, обрезки нет;
  (2) прогоном настоящего кода 17.08 (`probe_1708.py`) — tier=reliable, баннера нет;
  (3) прогоном САМОГО двоичного файла из комплекта показа (`econometrica-sidecar.exe`, 17.08 10:25)
      через HTTP — n_obs=130, coverage=0.8, status=pass, tier=reliable, баннера нет.
  🔴 Осечка по дороге, записываю как след: первый прогон двоичного файла дал `directional` —
  оказалось, он НЕ поднялся (порт 7451 занят чужим процессом, `[Errno 10048]`), и я опросила
  посторонний сидкар. Поймано тем, что «поднялся с первой попытки» при заведомо долгой загрузке.
  Урок: у поднятого сервера проверять строку привязки в ЕГО логе, а не факт ответа на порту.
- **00:14** — Воспроизводящий скрипт для ведущей готов и проверен прогоном:
  `scratchpad/repro_verdict_shift.py`. Работа по обоим хвостам закончена.
