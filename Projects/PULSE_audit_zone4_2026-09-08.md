# Маячок — внешний аудит зоны 4 (2026-09-08)

## Задача своими словами
Я — внешний аудитор с чистым контекстом. Проверяю пять участков Aurora Econometrica,
которых не касался ни один предыдущий заход (оба обрывались на ошибке сети):
A) запись состояния медиаплана в validator.py (`_write_media_plan_state` + все пути `validate_data`),
B) чтение признака `media_plan_absent` в optimizer.py,
C) защиту масштаба календарных дамми в scenario.py/holiday_calendar_ru.py,
D) полный сброс состояния при создании проекта (project-state.js + ProjectSelector.svelte),
E) реактивность шага «Декомпозиция» (DecomposeStep.svelte).
Код НЕ правлю, git не трогаю, ничего не удаляю, сборку не запускаю. Каждая находка — с
воспроизводимым сценарием и, где возможно, доказана исполнением зонда, а не рассуждением.

## План проверки по участкам
1. A — читаю validator.py целиком вокруг validate_data/_write_media_plan_state; строю зонд:
   вызываю validate_data напрямую на 5 входах (нет файла / битый формат / пустой / нормальный
   с планом / нормальный без плана) во временном каталоге проекта, смотрю results/media_plan.json.
2. B — читаю optimizer.py, ищу все чтения признака; зонд: подсовываю три состояния файла
   (present/absent/unknown) + отсутствие файла, смотрю поведение оптимизатора.
3. C — читаю _compute_scenario_holidays + resolve_holiday_column; зонд на граничных формах
   колонки (0/1 другое окно, дробная доля, 0/2, пустая, NaN).
4. D — читаю project-state.js (сброс, сторы, localStorage-ключ) + ProjectSelector.svelte;
   ищу ВСЕ пути создания/переключения проекта (импорт .aurora, дубликат, восстановление сессии).
5. E — читаю DecomposeStep.svelte; ищу onMount-однократность и источник признака «модель есть».

Отчёт: Projects/AUDIT_zone4_findings_2026-09-08.md.

## Журнал
- 12:05 — старт. Прочитан `audit_findings_wrapup_2026-09-08.md` (разобранное не дублирую).
- 12:20 — участок A закрыт зондом `scratchpad/zone4/probe_a.py` (запуск из sidecar/econometrica).
  Доказано исполнением: на ЧЕТЫРЁХ ранних выходах (файла нет / .txt / битый xlsx / пустой csv)
  `results/media_plan.json` остаётся ПРЕЖНИМ (n_future_periods=3, confirmed=true), а
  `media_plan_absent()` возвращает False — «план есть» на данных, которых нет. Список из
  самопроверки полон (пустой файл попадает в ветку «ошибка чтения» ещё до проверки n_cols==0).
  На нормальных входах перезапись работает (0 → absent=True; 3 → absent=False).
- 12:22 — участок B (чтение в optimizer.py) предварительно чист: третье состояние (null)
  честно даёт None и нигде не сводится к bool (везде сравнение `is True`). Осталось проверить
  фронтовый читатель диска (project-state.js:1266) — там `?? 0` сводит «не знаю» к «плана нет».
- 12:23 — перехожу к участку C (масштаб календарных дамми), ~15 минут.
- 12:45 — участок C закрыт тремя зондами (probe_c.py, probe_c2.py, probe_c3.py), вызов
  `_compute_scenario_holidays` напрямую. 🔴 Защита проверяет НЕ ТУ сторону равенства:
  убеждается, что КЛИЕНТСКАЯ колонка — 0/1, и после этого подставляет каноническую дамми,
  построенную в режиме `fraction` (доля дней периода) и по КАНОНИЧЕСКОМУ окну. Доказано на
  боевом демо-наборе `static/sample-data/planning/synth_retail_ecom.xlsx` (колонка
  «Чёрная пятница», 0/1, месячная сетка, 18 будущих периодов): ноябрьский вклад 53 782 ₽
  против эталонных 986 661 ₽ (5,5 %), а масса события переезжает в декабрь (+257 536 ₽).
  На старом pickle (`holiday_dummies_mode` отсутствует → binary_point) дамми тождественно 0
  во ВСЕХ будущих месяцах: событие исчезает, остаётся постоянный сдвиг −89 696 ₽/период —
  ровно тот дефект, ради которого правку делали. Дыра защиты на форме 0/0,7 тоже подтверждена.
- 12:47 — перехожу к участкам D (сброс состояния проекта) и E (реактивность Декомпозиции).
- 13:10 — участки D и E закрыты чтением с исчерпывающим перечнем присваиваний (зондом не
  доказуемы без поднятия окружения Svelte и новых файлов в дереве — запрещено).
  D: три стора чужого проекта снимаются ТОЛЬКО при создании проекта; на переключении, импорте
  архива и «Новом анализе» они переживают смену → ложный баннер «Декомпозиция устарела» на
  проекте Б и чужой контекст прогноза. Плюс у `restoreProjectResults` нет проверки актуальности
  после await (гонка возвращает «результаты предыдущего проекта»).
  E: правка односторонняя (idle→done), обратного перехода нет → при смене проекта шаг остаётся
  `done` с пустыми данными и не рисует ничего.
- 13:15 — ОТЧЁТ ЗАПИСАН: Projects/AUDIT_zone4_findings_2026-09-08.md
  Итог: 3 High (два — масштаб календарных дамми, один — сторы чужого проекта),
  3 Medium, 3 Low, 10 пунктов «проверено и чисто». Работа закончена.
