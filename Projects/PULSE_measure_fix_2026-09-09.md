# МАЯЧОК: замер цены правки решателя (2026-09-09)

## Задача своими словами
Найден дефект: оптимизатор SLSQP в аналитическом режиме работает без нормировки переменных (1e9 при ftol=1e-7) и останавливается далеко от оптимума. Правка — одна строка в optimizer.py:970. Моя работа — НЕ внедрять, а ИЗМЕРИТЬ цену: сколько тестов падает и сколько из них охраняли дефект, сколько чисел показа меняется, решает ли правка задачу (сходятся ли mroi_optimal внутри границ). После замера — вернуть дерево как было.

## План
1. Прочитать PROBE_mroas и CRITICAL_optimizer_suboptimal.
2. Тесты «до»: cd sidecar/econometrica && python -m pytest tests/ -q.
3. Правка строки 970 → тесты «после», разбор каждого падения.
4. Пересчёт эталона demo-fmcg-brand НА КОПИИ, таблица было/стало + проверка схождения mroi.
5. Список мест в документах показа (файл/строка/было/стало).
6. Проверка побочной находки про вердикт наружной рекламы.
7. Откат правки + git status чистый.

## Лента
- 01:37 — старт, маячок создан.
- 01:44 — прочитаны оба разбора. Проверено: `constraints` содержит ТОЛЬКО равенство по бюджету (строка 783), per-group ограничения свёрнуты в границы → ветка нормировки эквивалентна по ограничениям, ничего молча не теряет.
- 01:47 — тесты «ДО»: **1442 прошло, 0 упало, 2 пропущено**, 107 с.
- 01:47 — вношу правку строки 970, начинаю прогон «после», ~2 минуты без отметок.
- 01:53 — тесты «ПОСЛЕ»: **1442 прошло, 0 упало, 2 пропущено** (77 с против 107 с до).
- 02:00 — 🔴 ноль падений объяснён: инструментованный прогон показал **0 вызовов `engines.optimizer.optimize`** и **0 вызовов SLSQP** за весь набор из 1442 тестов. Правка не покрыта тестами вовсе. Отдельная разовая осечка `test_methodology_cert_live` при инструментовке не воспроизвелась (тест проходит и один, и в повторном полном прогоне).
- 02:00 — перехожу к пересчёту эталона на копии.
- 02:12 — пересчёт эталона на копии сделан. «До» воспроизведено рубль-в-рубль с записанным файлом → стенд честный. **Приёмка пройдена:** два канала внутри границ (ТВ и наружная) дают mroi_optimal 0,0717991005 и 0,0717991041 — расхождение 5e-8 (было 227×). Два канала на потолке, у них отдача выше — это и есть условие оптимума.
- 02:12 — побочная находка ПОДТВЕРЖДЕНА: наружная реклама +7,5 % бюджета, вердикт «Сократить», обоснование «каждый рубль приносит убыток».
- 02:13 — иду по документам показа.
- 02:40 — правка ОТКАЧЕНА, git status по optimizer.py пуст. Отчёт Projects/MEASURE_fix_solver_2026-09-09.md записан. Работа закончена.
