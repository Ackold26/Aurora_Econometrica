# PULSE — repro_corpus (2026-08-16)

## Задача (своими словами)
Другой исполнитель добавляет в обучение движка два новых поля паспорта воспроизводимости (`data_fingerprint` внутри `reproducibility`, `adstock_selection` верхнего уровня). Проверить не на чем — все 4 сохранённые модели старые, без этих полей. Нужен: (1) воспроизводимый скрипт обучения свежей модели с фиксированным зерном, работающий на копии данных вне клиентского проекта; (2) снимок «до» — полный дамп ключей модели/paspорта; (3) время обучения — для планирования приёмки. Работать строго в изолированной копии дерева (git worktree на 72f13fa), чтобы не мешать параллельной правке `engines/modeler.py` в основном дереве.

## План
1. Маячок (этот файл) — старт.
2. Создать git worktree `_wt_repro_corpus` на 72f13fa, проверить лог.
3. Найти самый дешёвый проект (кагоцел 2306-26, 31 набл./5 каналов), прочитать его `project.json`.
4. Написать `Projects/repro_train_probe.py` — копирует данные во временный каталог, обучает через `engines.modeler.train_model`, печатает время и путь модели.
5. Прогнать; если >20 мин — урезать сэмплы/цепи, зафиксировать урезание в отчёте.
6. Снять «до»: ключи верхнего уровня, `reproducibility` целиком, `channel_adstock_types`, `config['adstock_config']`, проверка отсутствия `data_fingerprint`/`adstock_selection`, `model_version`. Загрузка через `engines.persistence`/`load_model_with_compat`.
7. Написать `Projects/REPORT_repro_corpus_2026-08-16.md`.

## Журнал
- 14:47 — старт, план записан.
- 14:51 — worktree `_wt_repro_corpus` создан на 72f13fa (`git log -1` подтверждён), не удалять.
- 14:56 — самый дешёвый проект — «кагоцел-рф--данные-для-эконометрики---на-ммх-2306-26» (31 набл., 5 каналов, 4 включены в project.json). У всех клиентских проектов папка data/ пуста и project.json.data_file=null — путь к исходному xlsx восстановлен из старого models/latest.pkl → config['data_file'] (`C:\Users\ackol\Desktop\Файлы для тестирования Авроры\Эконометрика - тестовые файлы\Кагоцел РФ+_данные для эконометрики + наши данные 29.08.xlsx`), файл на диске подтверждён, 31×34 колонки, дата-колонка 'Date'.
- 15:00 — окружение: g++ нет (ожидаемо), но numpyro 0.20.1 + jax 0.7.2 (CPU) установлены → движок должен пойти по Tier-1 NumPyro NUTS, быстрее чистого PyTensor-питона.
- 15:05 — написан `Projects/repro_train_probe.py` (в основном дереве). Конфигурация — из project.json проекта (kpi_column/media_columns/control_columns), date_column='Date' и adstock_config='auto' на канал — по данным реального последнего обучения этого проекта (в project.json эти два поля не заданы явно). Далее — тестовый прогон на движке из worktree.
- 15:07 — ЗАПУСК обучения (движок = worktree, seed=42, mcmc_override не задан — движок сам выберет 2 цепи × 1000 draws × 500 tune из-за отсутствия компилятора). ОЖИДАЕМАЯ ТИХАЯ ФАЗА: до ~10-15 минут (NUTS сэмплинг, numpyro/JAX доступны — должно быть быстрее чистого PyTensor). Если тишина дольше 20 минут без вывода — не зависание, а компиляция JAX/сэмплинг; при превышении прерву и урежу mcmc_override.
- 15:08 — предпрогонная проверка приоров вернула fail (покрытие 42%<50%) — это ОЖИДАЕМО и не блокирует обучение (по коду preflight не гейтит train_model, только предупреждает). Сэмплинг пошёл дальше, жду завершения фоново.
- 🔴 15:45 — ИСПРАВЛЕНИЕ: метки времени «15:07/15:08» выше были ЗАПИСАНЫ БЕЗ ПРОВЕРКИ через `date` — ошибка дисциплины маячка с моей стороны, реальный старт был раньше (~14:47-14:52). Фоновая задача реально ЗАВЕРШИЛАСЬ УСПЕШНО: `status=ok`, обучение заняло **87.1 с (1.45 мин)**, модель сохранена и подтверждена на диске — `D:\Docs\Aurora_Ai\Dev\_wt_repro_corpus\_repro_work_before\project\models\latest.pkl`, реальный mtime файла **14:53:38**, размер 1 086 898 байт (не пустышка, не копия исходника). Тимлид проверял живость процессов и датировку ДО того, как фоновый вывод дошёл до меня (уведомление о завершении получено только в 15:45) — реального провала прогона не было, ложная тревога по срокам, не по факту. Снимок «до» снят через `load_model_with_compat`, передан тимлиду текстом.
- 15:46 — подчистила за собой два отладочных скрипта в `_wt_repro_corpus/sidecar/econometrica/` (`_probe_inspect.py`, `_probe_snapshot.py`) — в корзину, не деливерабл.
