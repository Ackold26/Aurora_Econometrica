# Внешний аудит блока — 2026-09-08

Формат: `severity | файл:строка | суть | воспроизводимый сценарий`

## Находки

High | sidecar/econometrica/engines/scenario.py:~128-175 (`_compute_scenario_holidays`) | Подстановка КАНОНИЧЕСКОЙ дамми под β, оценённую на КЛИЕНТСКОЙ колонке с другой формой и шкалой. Раньше вклад был 0, теперь — число, которое может быть грубо неверным. | Клиент подаёт контроль «Чёрная пятница» со значениями = бюджет промо в рублях (не 0/1). Обучение оценивает β на стандартизованных рублях: control_means≈5e6, control_stds≈1.2e7. В прогнозе код берёт `holiday_df['holiday_black_friday']` (значения 0/1 или доля периода) и стандартизует ИХ теми же means/stds клиента → z≈-0.41 почти во всех периодах, включая непраздничные → постоянный сдвиг вклада β*(-0.41)*y_std во ВСЕХ будущих периодах. Проверки формы/шкалы клиентской колонки нет ни в resolve_holiday_column, ни в scenario.py.

Medium | sidecar/econometrica/engines/scenario.py:~140 | Тот же механизм даёт подстановку НЕ ТОГО ОКНА события даже для 0/1-колонки: `resolve_holiday_column` разрешает имя по substring-алиасу, а данные берутся из канонического календаря. | Колонка «Чёрная пятница (наша распродажа 1–20 ноября)» → normalize → substring-алиас `blackfriday` → resolve = holiday_black_friday. β оценена на окне 1–20 ноября, будущий вклад считается по каноническому окну (последняя пятница ноября + окно). Числа не совпадают с историей, продукт об этом не сообщает.

## В работе
- проверка resetResultsKeepImport vs resetPipeline (что чужое остаётся)
- проверка ветки idle в DecomposeStep
- контракт мастер-флагов, слой train-config.js

## Находки — заход 2 (зоны 1–4)

Medium | src/lib/project-state.js:1640-1654 `resetResultsKeepImport` + :1370 подписка + src/lib/components/ProjectSelector.svelte:136-143 | Гонка: асинхронный `restoreProjectResults` доезжает ПОСЛЕ синхронного сброса и затирает ровно ту отметку, которую функция обещает сохранить. | `createProject` делает `activeProjectId.set(newId)` (строка 136) → подписка :1370 запускает `restoreProjectResults` (не awaited) → строкой ниже синхронно отрабатывает `resetResultsKeepImport`, сохраняя `stepMeta[0]='complete'`. Через микрозадачу промис резолвится: `project_load_results` для свежего проекта не бросает (project.rs:617, каталог создан, все ключи = null) → все флаги false → `reconcileStepMetaFromDisk` перезаписывает ВЕСЬ массив: Импорт → 'ready', остальные 'locked'. Итог: заявленное «отметка шага Импорт остаётся» не выполняется, а `importData` (файл/строки прошлого проекта) остаётся в сторе → в интерфейсе виден загруженный файл при шаге «Импорт не пройден».

Medium | src/lib/project-state.js:1640-1654 | Стор `importData` НЕ сбрасывается по замыслу, но принадлежит ПРОШЛОМУ проекту: на диске у нового `data_file: null`. Если шаг Валидации окажется доступен (любой путь, ставящий stepMeta вручную, — напр. ручная навигация степпера или повторный `activeProject.set` того же id), проверка пойдёт по файлу проекта А, а `results/*.json` запишутся в каталог проекта Б. | Пользователь импортировал файл в проект А, создал проект Б, вернулся на шаг Валидации → расчёты по данным А сохраняются как результаты Б.

Low | src/lib/project-state.js:1640-1654 vs :1390 `optimizeLiveState`, :311 `forecastContext`, :1080 `lastTrainedConfig` | Три стора прошлого проекта не снимаются ни `resetResultsKeepImport`, ни `loadPipelineForProject`, ни `resetForNewAnalysis`. `lastTrainedConfig` вдобавок хранится в ЕДИНОМ localStorage-ключе `aurora-last-trained-config` (не per-project) → баннер «модель устарела» судит новый проект по конфигурации старого. `forecastContext` (гранулярность обучения, «сезонность обнаружена», квантили X) переживает смену проекта и питает ForecastHorizonPicker. | Проект А обучен на неделях с сезонностью; создать проект Б → открыть блок прогноза → пикер горизонта покажет ограничения и «обнаружена сезонность период N» от модели А.

Medium | src/lib/components/pipeline/DecomposeStep.svelte:528-540 (ветка idle) + :395-402 (onMount) | Новая ветка утверждает «в этой сессии ещё не считался» в том числе тогда, когда разбор УЖЕ загружен с диска: `stepState` вычисляется один раз в onMount и не пересчитывается, когда `decomposeData` приезжает из `restoreProjectResults` позже. | Открыть в новой сессии проект, у которого на диске есть `results/decomposition.json`. onMount DecomposeStep отрабатывает раньше, чем резолвится асинхронный `project_load_results` (подписка project-state.js:1370) → `get(decomposeData)` пуст, `modelData.channelParams` пуст → `stepState='idle'`. Затем restoreProjectResults кладёт разбор в стор, но ничего не меняет `stepState`. Блок результатов `{#if stepState === 'done' && data}` (:549) не рисуется, а видна ветка idle с ложным текстом и кнопкой «Рассчитать» — предложением пересчитать то, что уже посчитано и лежит в памяти.

Medium | src/lib/components/pipeline/DecomposeStep.svelte:530 | Признак «модель есть» взят из `pipelineStepMeta[2].status === 'complete'`, а этот статус ставится по наличию `results/model-diagnostics.json` (project-state.js:1254 `hasModel`), тогда как расчёту нужен файл модели `models/*.pkl` (project-state.js:1230-1233 прямо фиксирует: channelParams лежат в pickle, требуется переобучение). Для проекта, где JSON есть, а файла модели нет, ветка утверждает «модель этого проекта обучена, нажмите Рассчитать», расчёт падает. | Проект перенесён архивом `.aurora`/на другую машину либо каталог `models/` вычищен: `results/model-diagnostics.json` на месте → шаг «Модель» = complete → idle показывает кнопку «Рассчитать» → `runDecompose` не находит модель → `stepState='error'`. Авто-ретрай `$effect` (:466-469) здесь не спасает: он выходит по `if (!md?.channelParams) return`, а channelParams появятся только после переобучения.

Medium | sidecar/econometrica/server.py:934-972 (`PreflightRequest`) против :417,:455 (запросы обучения) и src/lib/train-config.js:103 | Мастер-флаги через четыре слоя сведены верно (ConfigPanel.svelte:492-493 camelCase → econometrica.rs:698-713 snake_case + `unwrap_or(true)` → PreflightRequest с `AliasChoices` → server.py:1199-1204 в `prior_predictive_check`), умолчания согласованы (true везде). НО `disabled_holidays` в `PreflightRequest` НЕТ, хотя обучение его получает и `modeler.py` по нему пропускает праздники поштучно. | Клиент оставляет мастер-флаг праздников включённым и снимает 10 из 12 праздников в ControlsPanel. Обучение соберёт модель с 2 праздниками, предполётная симуляция — с 12. Вердикт надёжности снова относится не к той модели, которую клиент собрался обучать: ровно тот же класс дефекта, что закрывали мастер-флагами, на одну ступень мельче.

## Зона 4 — внешним аудитом НЕ проверена (оба захода оборвались на ошибке сети)

Ниже — САМОПРОВЕРКА, а не аудит. Помечено явно: тёплая сессия проверяет собственную правку,
это дополнительная информация, а не замена чистого контекста.

Наблюдение | `sidecar/econometrica/engines/validator.py:455-480` | В `validate_data` есть ЧЕТЫРЕ ранних выхода ДО вызова `_write_media_plan_state`: файла нет, неподдерживаемый формат, ошибка чтения, пустой файл. На этих путях прежний `results/media_plan.json` остаётся описывать ПРОШЛЫЕ данные. | Опасно это или нет, зависит от порядка: если файл проекта подменяется ДО проверки, то у нового файла останется признак старого; если проверка идёт до принятия данных — состояние по-прежнему верно описывает последние принятые данные. Порядок не установлен: команда `econ_validate` принимает путь к файлу и каталог проекта, но кто и когда кладёт файл в проект — не прослежено. **Зонд для следующей сессии:** взять проект с найденным хвостом плана, импортировать заведомо битый файл, посмотреть `results/media_plan.json` и признак `media_plan_absent`.
