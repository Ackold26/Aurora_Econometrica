# PULSE — починка находок аудита колоды (F-06, F-11)

Старт: 16 авг 2026 г. 13:38

## Задача
Чиню две находки внешнего аудита в сборщике презентации (builder.py) и его тестах.
Область: ТОЛЬКО `sidecar/econometrica/aurora_pptx/builder.py` и
`sidecar/econometrica/tests/test_promises_slide.py`. Остальное не трогать (параллельные исполнители).

## План
1. F-06 (High) — `test_mutation_gate_promises_condition_is_load_bearing` мутирует КОПИЮ build()
   внутри теста, а не продуктовый код → тавтология, не доказательство. Переписать мутацию так,
   чтобы она правила настоящий объект/условие показа, а не переписывала тело build().
2. F-11 (Low) — заголовок/подпись слайда «Проверка рекомендаций» не согласуют число со словом
   («все 1 … сбылись»). Посмотреть `optimize/frontier.py::_ru_periods` как образец согласования
   и применить тот же приём в builder.py.
3. Мутация обязательна на каждую починку (внести-поймать-откатить, мутировать продуктовый код).
4. Гейт: `python -m pytest tests -q` из `sidecar/econometrica`. Отчёт по ходу в
   `Projects/FIX_AUDIT_DECK_2026-08-16.md`.

## Статус
- [x] F-06 — тест переписан на реальный build() + точечную мутацию self.promises_summary; мутация внесена/поймана/откачена
- [x] F-11 — согласование числа в title/caption s10d_promises, новый тест singular_agreement; мутация внесена/поймана/откачена
- [x] Гейт пройден: tests/test_promises_slide.py 11/11 зелёных; полный прогон 1148 passed, 4 failed (не моя область — frontier/corridor)

РАБОТА ЗАВЕРШЕНА — 16 авг 2026 г. 13:51
