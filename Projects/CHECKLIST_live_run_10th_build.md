# Чек-лист живого прогона десятой сборки (09.09, после правок INV-146/147)

Проверять на УСТАНОВЛЕННОЙ программе, не в режиме разработки. Каждый пункт — признак,
который можно увидеть или измерить, а не «выглядит нормально».

## A. Настройки — INV-146 (домены)

1. **Две строки внизу карточки «о программе»**, а не одна:
   - `© 2026 ООО «Платформа Аврора» · MMM-Optimizer.pro`
   - `Aurora AI Econometrica – часть семьи решений Aurora AI · AuroraAi.pro`
2. **Регистр читаемый** — `MMM-Optimizer.pro`, `AuroraAi.pro`, НЕ строчными.
3. **Обе ссылки открываются в браузере системы** по нажатию. 🔴 Признак провала — ничего не
   происходит: значит разрешение `opener:allow-open-url` не доехало до сборки.
4. Подчёркивание появляется только при наведении.
5. Правый клик по ссылке предлагает «копировать адрес» — адрес в разметке настоящий.

## B. Кабинет — INV-146, часть 3

6. Кнопка «Скачать Python» (видна, когда Python не установлен) открывает браузер системы,
   а НЕ пустое окно программы. Если ветка недостижима — отметить «не проверено», не выдумывать.

## C. Отчёт HTML — правки движка

7. **Обложка: «Классификация — Конфиденциально»** по-русски. Было `Confidential`.
8. **Фирменный значок на обложке цветной** (тёмно-синий с золотом), НЕ чёрный.
   Измеримо: в консоли браузера `getComputedStyle(document.querySelector('.st1')).fill`
   обязан дать `rgb(197, 164, 109)`, а не `rgb(0, 0, 0)`.
9. В консоли браузера **нет нарушения политики безопасности** про `Applying inline style`.
10. Заголовки разделов русские, как в девятой сборке (регресса нет).

## D. Колода PPTX — INV-147

11. Свойства файла — проверять **весь** список инварианта, а не только исправленные поля.
    🔴 Урок соседней линии 09.09: замер, читающий лишь то, что чинил, выглядит зелёным и врёт.
    - `docProps/core.xml`: автор и «кем изменён» — **ООО «Платформа Аврора»**, НЕ `Steve Canny`;
      дата создания — **сегодняшняя**, НЕ 27.01.2013; заголовок русский.
    - `docProps/app.xml`: приложение — **Aurora AI Econometrica**, НЕ `Microsoft Macintosh
      PowerPoint`; компания — наше юрлицо. До этого поля обычный путь НЕ достаёт, чинится
      вторым проходом по готовому файлу — проверять обязательно отдельно.
    - Поиск по всем служебным файлам меток `python-`, `Canny`, `openpyxl`, `Macintosh`, `2013`
      → **ноль совпадений**.
12. Слайдов 13, старых английских меток нет.

## E. Опорные числа — регресса быть не должно

13. Бюджет **2 435,6 М ₽** · прогноз **12 191,2 М ₽** · прирост **+4,5 %** · медийный **+26,2 %**
    · mROAS **0,04 / 5,66 / 0,08 / 10,21** · MQS **70** · R² **0,936** · MAPE **1,5 %**.
    🔴 Любое расхождение = правка задела расчёт, чего быть не должно: ядро не трогали.

## F. Условия выпуска — V74

14. Шлюз в НЕСЖАТОМ файле сборки (`<target>/release/*.exe`), не в установщике:
    поиск по установщику даёт 0 ВСЕГДА и вердиктом не является.
15. `installMode` — ровно `currentUser`.
16. Приёмка прогнана в конфигурации ПОСТАВКИ (с признаком сборки), а не обычной командой.

## G. Сторож выходного документа

17. `cd sidecar && python ../tools/verify_aurora_html_brand.py` → **34/34, код возврата 0**.
    Сегодня впервые стал полностью зелёным; красный после сборки = регресс правок.
