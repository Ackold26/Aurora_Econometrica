# Пульс внешнего аудита блока CPD-77 — 14.08.2026

## Задача своими словами

Проверяю чужую работу: в трёх продуктах убрали действия, которые антивирус читает как вредоносные
(выдача прав каталогу через `icacls` и разведка портов через `netstat` перед снятием своего дочернего
процесса), заменив источник идентификатора процесса на собственный файл состояния. Моя работа — найти,
где эта замена ломает безопасность снятия процесса или стартовый путь, и проверить записи в каноне.
Только чтение, ничего не правлю.

## План

1. Прочитать передачу и правила продукта. ✅
2. Дифф Эконометрики (`ea560be`) + контекст `econ_sidecar.rs`, `session/manager.rs`.
3. Дифф Docs Lab (`5ea8f00`) + `parser_sidecar.rs`, `rag_sidecar.rs` целиком.
4. Дифф Smart Analytica (`76f0cbb`) — построчная сверка с Docs Lab.
5. Жизненный цикл файла состояния: где пишется, где удаляется, переживает ли падение.
6. Сила проверки `is_our_process_and_user` (полный путь или имя образа).
7. `git log -S "wait_or_kill"` — не нужен ли он ещё где-то.
8. Стартовый путь после снятия `icacls` — кто создаёт каталог.
9. Канон: CPD-77, INV-125, классы десяти записей.
10. Отчёт `AUDIT_CPD77_2026-08-14.md`.

## Отметки

- **03:18:25** — старт, прочитаны передача и `CLAUDE.md` продукта. Далее: диффы трёх продуктов.
- **03:31** — разобраны диффы всех трёх продуктов, `sidecar_runtime.rs`/`port_discovery.rs`,
  жизненный цикл файла состояния, `app_state_dir`. Главное найдено: `process_image_contains`
  безусловно принимает любой процесс с «python» в имени образа, а файл состояния переживает
  падение и перезагрузку → снятие по устаревшему идентификатору. Сверка Docs Lab ↔ Smart Analytica
  показала побайтовое совпадение сайдкаров кроме двух строк конфига. Далее: канон (CPD-77,
  INV-125, классы десяти записей), затем отчёт.
- **03:32:33** — канон проверен (`git blame` по строкам класса), отчёт записан в
  `Projects/AUDIT_CPD77_2026-08-14.md`. Итог: 1 Critical, 1 High, 3 Medium, 1 Low. Работа закончена.
