# PULSE — проверка состояния методологии MMM (2026-08-16)

## Задача своими словами
Разбор рынка MMM от 2026-08-02 нашёл 10 методологических разрывов. Часть с тех пор чинили.
Моя работа — только чтение кода: по каждому из 10 пунктов сказать ЗАКРЫТО / ЧАСТИЧНО / НЕ ЗАКРЫТО
с доказательством (путь:строка + цитата), проверяя РАБОЧИЙ путь кода, а не имена файлов.
Ничего не менять, не коммитить.

## Дерево
`D:\Docs\Aurora_Ai\Dev\Aurora_Econometrica_thinwt`, ветка `master`, HEAD `6fa2498` (2026-08-16).

## План
1. Зерно случайности в pm.sample() + запись в сертификат
2. Интерполяция пропусков vs fillna(0) vs обещание в подсказках
3. Три проверки здоровья по Meridian (ppp, negative baseline, prior-posterior shift)
4. Тест на подставной канал (placebo/shuffle)
5. Тренд отдельной компонентой (GP / узлы / Prophet-подобное)
6. ROI-приор из эксперимента (Meridian) vs лифт-как-наблюдение (Robyn)
7. Обучаемый Weibull-adstock
8. Журнал приоров экспериментов
9. Следы прогона mmm-eval
10. Гео-иерархия (роль «регион» + иерархия по географии)

## Отметки
- 02:10 — старт, маячок создан.
- 02:11 — п.1 зерно: ЗАКРЫТО (3/3 вызова pm.sample с random_seed, паспорт в сертификате).
- 02:12 — п.2 интерполяция: ЧАСТИЧНО (интерполяции нет, обещание в интерфейсе живо, валидатор предупреждает честно).
- 02:13 — п.3: ЧАСТИЧНО 1/3 (negative baseline ЗАКРЫТО; ppp НЕТ; prior→posterior shift НЕТ).
- 02:14 — п.4 подставной канал: НЕ ЗАКРЫТО (плацебо только в causal/scm.py).
- 02:15 — п.5 тренд: НЕ ЗАКРЫТО (intercept — скаляр, GRW не вызывается).
- 02:16 — п.6 ROI-приор: НЕ ЗАКРЫТО (класс Robyn на месте, Meridian-приора нет).
- 02:16 — п.7 Weibull: НЕ ЗАКРЫТО (`# Weibull stays hardcoded`).
- 02:17 — п.8 журнал приоров: ЧАСТИЧНО (сверка есть, «до/после приора» и истории нет).
- 02:18 — п.9 mmm-eval: НЕ ЗАКРЫТО (ноль следов в дереве и в aurora-meta).
- 02:19 — п.10 гео: НЕ ЗАКРЫТО (роли «регион» нет, иерархия только brand/performance).
- 02:20 — durable-отчёт `VERIFY_METHOD_2026-08-16.md` записан.
- 02:22 — контрольная проверка мёртвого кода: `.interpolate(` нет нигде; `_placebo_inference` вызывается `causal/scm.py:387`; `prepare_calibrations` — `engines/modeler.py:719`. ПРОВЕРКА ЗАВЕРШЕНА.
