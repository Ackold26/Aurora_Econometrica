# Маячок — починка коридора (F-07, F-08, F-13, F-14)

**Старт:** 16 авг 2026 г. 13:37 (время системы).
**Ветка:** `feat/econ-p1-winning`. Дерево: `D:\Docs\Aurora_Ai\Dev\Aurora_Econometrica_thinwt`.
**Область:** только `sidecar/econometrica/optimize/bounds.py`, `sidecar/econometrica/server.py` и их тесты.

## Как я поняла задачу
Внешний аудит нашёл четыре дефекта в блоке коридора безопасных значений:
- **F-13 (High)** — сторож единиц молчит там, где опаснее всего: стоимость единицы канала не задана вовсе,
  прямой проход подставит 1,0 и сложит натуральные единицы (TRP) с рублями. Нужен отказ при неизвестной единице.
- **F-07 (Medium)** — `lo/hi × n_periods` завышает суммарный бюджет для флайтовых каналов; проверки разумности нет,
  статус всё равно `ok`. Нужно корректное приведение либо отказ от чисел + честный статус.
- **F-14 (Medium)** — три признака основания на четыре группы чисел в одном словаре. Нужен однозначный признак у каждой группы.
- **F-08 (Low)** — коридор подорожал ×4,8 из-за безусловного прямого прохода. Нужно не платить за то, чего не просили.

## План
1. Прочитать `bounds.py`, `server.py`, тесты коридора; понять устройство `_resolve_current_unit_costs` и приведения. ✅
2. F-13 — сторож на «единица неизвестна» + мутация. ✅
3. F-07 — корректное приведение либо отказ + проверка разумности + честный статус + мутация. ✅
4. F-14 — признак основания у каждой группы чисел. ✅
5. F-08 — прямой проход по явному запросу, без слома текущих потребителей. ✅
6. Гейт `python -m pytest tests -q` из `sidecar/econometrica`; живая проверка на кагоцеле (чтение). ✅

## Отметки по ходу
- 13:37 — маячок создан, читаю находки и код.
- 13:41 — зонд на кагоцеле подтвердил F-13 вживую: `unit_costs={}`, среди каналов модели
  `TRPs бренд (W 25-54)`, `aggregate_sales.status='ok'` с числами. Активных периодов у каналов
  23/27/31/31/22 из 31 — F-07 тоже реальна.
- 13:48 — **F-13 закрыта**: `classify_channel_units` (три группы: заданная не-рублёвая /
  неизвестная / рубль), отказ `unit_mismatch` на неизвестную единицу, допущение о рублях названо явно.
- 13:48 — **F-07 закрыта**: `n_active_periods` на канал, поканальное приведение, проверка
  «коридор накрывает текущую трату» + статусы `implausible_bounds` / `active_periods_unknown` /
  `periods_disagree`.
- 13:48 — **F-14 закрыта**: `corridor_budget` / `budget_used` / `sales`, у каждой группы свой
  `basis`; неоднозначные `basis`, `corridor_budget_basis`, `corridor_basis_mismatch` убраны;
  добавлен `aggregate_budget_basis`.
- 13:50 — **F-08 закрыта**: `include_sales` в запросе (по умолчанию выключен), статус
  `not_requested` вместо молчания. Цена прохода 0,707 с в свежем процессе больше не платится.
- 13:53 — шесть мутаций внесены в места реального вычисления, все пойманы тестами, все откачены.
- 13:55 — гейт `python -m pytest tests -q`: **1173 passed, 1 skipped, 0 упавших**.
- 13:56 — отчёт `FIX_AUDIT_CORRIDOR_2026-08-16.md` дописан. **Работа завершена.**
