# Маячок – клиентский документ docx «Чем отличается MMM Optimizer»

## Задача (своими словами)
Собрать Word-документ из готового текста `Чем отличается MMM Optimizer.md` (18,5 КБ) в фирменном
оформлении MMM Optimizer, взяв стиль с живого образца «Ванпейджер MMM Optimizer.docx». Документ пойдёт
клиенту – значит: дословный текст, ни одной внутренней пометки, ни одного имени конкурента, без
эмодзи-значков, без цен. Таблицы – настоящие таблицы Word. Колонтитул с номером страницы.
Исходный .md не трогать.

## План
1. [01:33] Старт, маячок создан.
2. Прочитать исходный .md (сделано – 9 разделов, 2 полноразмерные таблицы, списки, курсив, полужирный,
   один длинный абзац-цитата "Пределы получены измерением...").
3. Распаковать образец «Ванпейджер MMM Optimizer.docx» как zip, снять точные величины (шрифты, цвета,
   отступы, заливки, поля) – свериться с ТЗ.
4. Проверить текст на служебные пометки / имена конкурентов – по первому чтению чисто, документ
   держит рамку «классы решений», словн «код», «строк» не встречается.
5. Написать сборочный скрипт (docx, Node) во временный каталог.
6. Прогнать, получить .docx.
7. Проверить: длинные тире = 0, служебные слова = 0, конвертация в PDF + постраничный осмотр.
8. Отчёт.

## Отчёт [01:42]

**Результат:** `C:\Users\ackol\Desktop\Демо-показ Эконометрики\Чем отличается MMM Optimizer.docx` собран и на месте.
Исходный `.md` не изменён (время правки прежнее – 01:20, до старта моей работы).

**Оформление.** Распаковала образец «Ванпейджер MMM Optimizer.docx» как zip и сняла точные величины
с живого XML, а не только с пересказа в задании. Два уточнения относительно таблицы из задания,
которые я разрешила в пользу образца (задание прямо просило «соблюдать точно», а образец точнее
пересказа):
- **Заливка шапки таблицы** – в образце акцентом `#5A55E6` с белым текстом залита только колонка
  «нашего» продукта; остальные колонки шапки – серые `#F1F3F5` с текстом `#6E7682`. Сделала так же
  в обеих таблицах: последняя колонка (MMM Optimizer) – акцент, остальные – серые.
- **Чередование `#F8F9FA`/`#F5F6FF`** – в образце это не чередование по строкам, а фиксированная
  окраска по столбцам, одинаковая на каждой строке (первая колонка-метка – без заливки, «конкурентные»
  колонки – `#F8F9FA`, колонка MMM Optimizer – `#F5F6FF`). Повторила этот рисунок в обеих таблицах.
Прочие величины (шрифт Arial, поля 709/1100, US Letter, 9/11/13/20 pt, интервалы, подчёркивание
под заголовком раздела) взяты буквально из задания – они у образца совпали один в один, кроме
формата страницы (образец – A4, задание явно просило US Letter, использовала US Letter).

**Содержание.** Текст перенесён дословно, весь markdown (жирный, курсив, кавычки-ёлочки, короткие
тире) сохранён смыслово через настоящее форматирование Word, без разметки в тексте. Обе таблицы –
настоящие таблицы Word (9 строк / 3 колонки и 7 строк / 5 колонок). Три буллет-списка – через
`numbering`, без ручных «•». Колонтитул – «Aurora AI Econometrica – MMM Optimizer · стр. N», мелким
шрифтом, цвет `#6E7682`, больше ничего в нём нет.

**Проверка на протечки (пройдена):**
1. Длинных тире «—» в готовом `document.xml` – **0**.
2. Слова-сторожа «внутренн», «не для передачи», «TODO», «.py», «.svelte», «строк» –
   **0**, кроме 2 вхождений «внутренн»: «внутренний адрес самой машины» и «внутреннем пороге 4» –
   обе фразы легитимная часть клиентского текста (архитектура/статистика), не рабочая пометка.
   Проверила это же по исходному `.md` заранее – совпадает, протечки нет.
3. Имён конкурентов в тексте не встретила – весь текст держит рамку классов решений, как и было
   задумано. Цен в документе нет – это уже было заложено в исходнике.
4. Конвертировала в PDF (LibreOffice) и просмотрела все 3 страницы изображениями (100 dpi+):
   заголовки на месте, обе таблицы не разъехались и не обрезаны, «решета» из растянутых слов нет,
   текст нигде не обрезан, цвета акцента/вторичный/заливки визуально совпадают с образцом,
   колонтитул с номером страницы виден на каждой странице.

**Итог:** 3 страницы, 2 таблицы, обе проверки текстом чистые, все страницы просмотрены и выглядят
опрятно. Готово к передаче.
