
## fix-ui (s48) — старт 06:32
Задача: 4 дефекта из внешнего аудита — (1) абсолютные пути сборочной машины в help PDF, (2) обход экрана условий через 2 доп. обработчика клавиш (+page.svelte, cabinet/+page.svelte), (3) рассинхрон имени файла DATA_PROCESSING_PDF_FILENAME, (4) FB-002 ошибочно удалён из справки (строит aurora_core крейт).
План: T1 build_help_pdf.py → относительные ссылки/снятие аннотаций + сторож; T2 единый источник блокировки клавиш во всех обработчиках + мутационный тест; T3 переименование константы+манифеста+синхронизация; T4 вернуть FB-002 + починить сторож достижимости под общий крейт.

## fix-ui (s48) — финиш $(date '+%H:%M')
Все 4 задачи закрыты:
1. build_help_pdf.py: namespace_internal_links()+strip_download_links() - PDF без путей сборочной машины; новый сторож check_pdf_no_machine_paths() в check_help_pdf_consistency.py (красный на испорченном файле подтверждён, зелёный после починки).
2. global-shortcuts.js расширен: handleHomeShortcut (+page.svelte) + handleCabinetShortcut (cabinet/+page.svelte), единый источник trialConsentPromptOpen. 15 тестов, 3 мутации (по одной на обработчик) - все красные, восстановлено.
3. DATA_PROCESSING_PDF_FILENAME синхронизирован (user_config.rs + sync_trial_terms.py), sync прогнан, манифест обновлён, cargo test trial - 18/18.
4. FB-002 возвращён в error-codes.html (текст восстановлен из git-истории), сторож errors.rs расширен на коды aurora_core (feedback::submit, ревизия 35d8345 закреплена явной сверкой с Cargo.lock) - обе мутации (html-регресс, дрейф ревизии) красные, восстановлено.
Прогоны: npx vitest run - 126/126 файлов, 1627/1627 тестов; npm run check - 0 ошибок; cargo test trial - 18/18; cargo test --lib errors:: - 2/2. PDF пересобран, гейт зелёный.

## fix-ui (s48) — второй слой защиты (уточнение аудитора) $(date '+%H:%M')
По уточнению аудитора добавлен второй, независимый слой защиты экрана условий:
- `isKeydownOutsideBlockingOverlay(overlayEl, eventTarget)` в global-shortcuts.js - чистый предикат.
- `TrialConsentOverlay.svelte`: $effect с window keydown в capture:true, гасит (preventDefault+stopPropagation) любое нажатие вне дерева оверлея, пока trialConsentPromptOpen true. Первый узел на пути события - убивает весь путь до того, как дойдёт до любого обработчика продукта, старого или ненаписанного.
- Тесты: 4 юнит-теста предиката (global-shortcuts.test.js) + 3 интеграционных на реальном DOM/jsdom (trial-consent-overlay.test.js: фон не доходит / внутри оверлея работает как обычно / после закрытия перехват снят).
- Мутация (снять проверку в blockKeydownOutsideOverlay) - тест красный, восстановлено.
Итог: vitest 126/126 файлов, 1634/1634 тестов (было 1627, +7); npm run check 0 ошибок (182 warning те же, что были); cargo test trial 18/18; cargo test --lib errors:: 2/2 - не тронуты, всё зелёное.
07:12

## front-gate-s48 (подхват после обрыва) — старт 07:47
Пересказ: предыдущий исполнитель начал разводить булеву `trialConsentPromptOpen` (отвечала и «показывать окно», и «закрыт ли проход») на две — `trialConsentResolved` + производную `trialConsentBlocking`; ведущая сама доперевела последних двух потребителей (+page.svelte:158, cabinet/+page.svelte:241) без прогона. Моя задача — не переделывать, а доказать: (1) оба прогона (vitest ≥1634/0 отказов, npm run check 0 ошибок) зелёные после её правки; (2) тест гонки trial-consent-gate-race.test.js реально красится на возврате к старой величине (мутация-докажи-и-верни); (3) перечень всех security-значимых величин в store.js с умолчаниями и вердиктом безопасности.
Не трогаю src-tauri/ и sidecar/ (там соседняя линия), не коммичу, не рефакторю постороннее.
План: шаг 1 - прогон vitest+check с RC сразу после команды; шаг 2 - прочитать race-тест, определить точку красного, наложить мутацию в +page.svelte, прогнать, зафиксировать текст отказа, вернуть, сверить git diff = пусто; шаг 3 - пройти store.js построчно на класс "умолчание = разрешение до прихода ответа".

## front-gate-s48 (подхват) — факты по шагам 07:50
Шаг 1 (прогоны): `npx vitest run` (RC снят сразу после команды, не после tail) - RC=1, 126/127 файлов зелёные, 1634/1635 тестов зелёных, ОДИН отказ: `src/tests/trial-consent-gate-race.test.js` > "состояние по умолчанию (Rust ещё не ответил) - цифра 1 НЕ должна открывать кабинет" - `openCabinetByIndex` вызван 1 раз с индексом 0, ожидалось 0 вызовов. `npm run check` - RC=0, 4172 файла, 0 ошибок, 182 предупреждения (те же, что были).
Шаг 2 (мутация теста гонки): прочитан файл целиком - у него только ОДИН блок (докстринг обещает второй "блок с новыми сущностями", его в файле НЕТ - недописан). Тест НЕ импортирует и не вызывает +page.svelte вовсе - он вручную воспроизводит внутри себя выражение `trialConsentOpen: get(trialConsentPromptOpen)` и передаёт готовый объект в чистую функцию `handleHomeShortcut`. Наложена мутация +page.svelte (импорт+строка 158: `trialConsentBlocking` → `trialConsentPromptOpen`), прогнан именно этот файл - результат (текст отказа, число вызовов, номер строки) БИТ-В-БИТ идентичен прогону без мутации. Возврат сделан, `git diff -- src/routes/+page.svelte` показывает текущее состояние = `trialConsentBlocking` и в импорте, и в вызове (совпадает с состоянием до мутации).
Вывод шага 2: тест НЕ чувствителен к правке в +page.svelte - он всегда красный, потому что не читает исправленный код, а хардкодит старое выражение сам. Докстринг store.js:234-235 ("зелёный на get(trialConsentBlocking) после") неверен по факту - такого прогона тест никогда не проходит, вторая половина файла с проверкой fix'а не написана.
Шаг 3 (вторая дыра, найдена делом): `src/lib/components/TrialConsentOverlay.svelte:38` - `const visible = $derived($trialConsentPromptOpen)` (НЕ `trialConsentBlocking`); `{#if visible}` (:122) оборачивает всю разметку, включая `overlayEl`; `$effect` (:44-52) вешает второй слой (перехват keydown в capture) только при `if (!visible) return` пройденном. То есть ВТОРОЙ слой защиты (задуманный как страховка для обработчиков, которые ЗАБУДУТ проверить гейт - см. его же докстринг в global-shortcuts.js:118-130) в первые миллисекунды после запуска (пока `trialConsentPromptOpen` ещё false) НЕ СМОНТИРОВАН И НЕ СЛУШАЕТ вовсе - `isKeydownOutsideBlockingOverlay(null, target)` безопасен сам по себе (возвращает true при overlayEl=null), но до него дело не доходит, т.к. `$effect` рано выходит. Три обработчика (+layout.svelte:129, +page.svelte:158, cabinet/+page.svelte:241) уже читают `trialConsentBlocking` правильно и потому в текущем виде защищены сами по себе - но страховочная сетка на неизвестный/будущий обработчик в это окно отсутствует.

## front-gate-s48 (обе находки починены) — 08:02
Задача 1 (тест гонки): выброшен подход "объект вручную внутри теста", файл переписан с нуля - два блока разной природы:
(а) поведенческий - прямой вызов `resolveTrialConsentGate` с поддельным invoke (подвешенный/resolved {required:false}/{required:true}/reject), проверяет саму производную `trialConsentBlocking`. Мутация-докажи: `([resolved, open]) => open` вместо `!resolved || open` в store.js - краснеет РОВНО первый тест ("промис подвешен"), остальные 5 - зелёные. Снято, `resolved, open` формула сверена - совпадает с исходной.
(б) текстовая - readFileSync по 4 файлам (+layout.svelte, +page.svelte, cabinet/+page.svelte, TrialConsentOverlay.svelte), требует get(trialConsentBlocking) и запрещает get(trialConsentPromptOpen) для решения о блокировке + требует $derived($trialConsentBlocking) и запрещает `if (!visible) return` в эффекте оверлея. Мутация-докажи: вернула в +page.svelte:158 `get(trialConsentPromptOpen)` - красный текст отказа именно по этому файлу, остальные 5 тестов зелёные. Снято, git diff по +page.svelte сверен - импорт и строка 158 снова `trialConsentBlocking`.
Докстринг store.js:234-241 переписан - называет обе проверки и на каком входе каждая краснеет, старое неверное утверждение убрано.

Задача 2 (вторая дыра в оверлее): TrialConsentOverlay.svelte - `visible` (от trialConsentPromptOpen) оставлен ТОЛЬКО для {#if} разметки; заведена отдельная `blockingActive = $derived($trialConsentBlocking)`, $effect второго слоя теперь монтирует слушатель по ней. Добавлен новый тест "второй слой активен ДО монтирования разметки окна" в trial-consent-overlay.test.js: trialConsentPromptOpen=false + trialConsentResolved=false (окно ещё не решено, разметки нет) - клавиша на фоновом элементе гасится, продуктовый обработчик НЕ вызван; отдельно проверено screen.queryByRole('checkbox') отсутствует (разметки правда нет). beforeEach файла дополнен trialConsentResolved.set(true) - в бою к моменту, когда окно видимо, Rust уже ответил, иначе тест "перехват снимается после принятия" проверял бы недостижимое в бою состояние. Комментарий в коде объясняет, почему overlayEl=null → блокировка, а не недосмотр.

Итоговые прогоны: `npx vitest run` - RC=0, 127/127 файлов, 1641/1641 тестов (было 1634, +7: 4 поведенческих + 2 текстовых + 1 оверлей). `npm run check` - RC=0, 4172 файла, 0 ошибок, 182 предупреждения (планка удержана, новых не заведено). src-tauri/ и sidecar/ не трогала, записей не делала.
