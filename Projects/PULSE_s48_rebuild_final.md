# PULSE s48 — пересборка движка и полная приёмка

## Задача (пересказ)
Вторая волна аудита внесла правки в `aurora_html/interactive.py` (отключение анимации у выгрузки картинкой), `aurora_pptx/builder.py` (единая функция форматирования суммы) и новый тест-сторож `tests/test_reallocation_threshold_single_source.py` — ПОСЛЕ последней сборки движка (08:28). Нужно: пересобрать движок, доказать побайтной сверкой и поведением из собранной копии, что правки уехали, затем прогнать полную приёмку (rust-шлюз ≥624/0, pytest 1687/0/2, vitest 1645/0, npm check 0 ошибок/182 предупреждения). Код не менять, записей не делать, установщик не собирать.

## План
1. Пересборка движка (`build_sidecar.py`), код возврата в файл, не в конвейер.
2. Прочитать журнал сборки — строка о свежести.
3. Побайтная сверка исходников с собранной копией.
4. Доказать правки поведением: `format_realloc_mln` из `_internal/`, отключение анимации в `_internal/aurora_html/interactive.py`.
5. Полная приёмка: rust-шлюз (node tools/build-cloud.mjs --test), pytest, vitest, npm check.
6. Доклад числами.

## Старт: 14.09.2026, 10:06

## Факты (14.09.2026)

1. **Пересборка движка** — код возврата 0. Строка свежести: `[OK] Freshness verified (exe newer than all .py sources)`. Версия движка: 2.5.4. Размер: 970 MB. Smoke-тест: validate CSV/XLSX — ok.
2. **Побайтная сверка**: 102 файла-источника (.py) проверено, 90 совпало побайтно, **0 расхождений**, 12 отсутствуют объяснимо (tools/, optimize/, server.py, build_sidecar.py — не выносятся отдельными .py в _internal, уходят в скомпилированный PYZ-архив либо не входят в рантайм-пакет).
3. **Доказательство поведением:**
   - `format_realloc_mln` вызвана прямо из `_internal/utils/optimizer_honesty.py` собранной копии: 0.5 → «0,5», 1.0 → «1».
   - `_internal/aurora_html/interactive.py`: `opt.animation = false;` стоит перед `exportChart.setOption(opt, true)` в ветке экспорта PNG у офф-скрин инстанса — правка на месте, побайтно совпадает с исходником.
4. **Полная приёмка (конфигурация поставки):**
   - Rust-шлюз (`node tools/build-cloud.mjs --test`): первый прогон — отказ по сети (TLS handshake к GitHub, транзиент), второй прогон — **КОД 0**, 624 пройдено / 0 отказов / 4 пропущено (578+12+5+22+7 passed; doc-tests 0 passed 1 ignored). Метка `aurora_gateway-v0.6.7` подтверждена на записи e427db7ee8c3.
   - `python -m pytest sidecar/econometrica/tests -q`: **1687 passed, 0 failed, 2 skipped**.
   - `npx vitest run`: **1645 passed (1645)**, 127 файлов тестов пройдены.
   - `npm run check`: **0 ERRORS, 182 WARNINGS**, 4172 файла — совпадает с ожиданием, новых предупреждений не заведено.

## Итог
Движок пересобран и доказано, что три правки второй волны аудита (interactive.py, builder.py, тест-сторож) вошли в сборку — побайтно и поведением. Полная приёмка пройдена по всем четырём измерениям в конфигурации поставки. Код не менялся, записей не делалось, установщик не собирался (по границам задания).
