# Аудит блока «устойчивость сетевых загрузок» — 2026-07-26

Формат: severity | file:line | суть | сценарий срабатывания

---

**High** | `src-tauri/src/lib.rs:556-568` (`open_cabinet`) + `src-tauri/src/commands/content_updater.rs:549-552` | Новое подробное сообщение об отказе НИКОГДА не содержит реальной причины: `download_updates` глотает ошибку каждого файла (`Err(e) => error!(...)`, цикл продолжается) и возвращает `Ok(vec![])`. Ветка `Err(e) => download_err = Some(format!("{e:#}"))` в `open_cabinet` для сетевых отказов недостижима — всегда срабатывает `Ok(_)` с текстом-заглушкой. | Клиент с обрывом канала (ровно тот разбор, ради которого писался блок) или ответ 403/404 от `/content`: 5 попыток проваливаются → `download_vault_file` → Err → `download_updates` логирует и отдаёт `Ok([])` → пользователь и поддержка видят «Подробность для поддержки: кабинет econometrist, сервер не отдал файл кабинета». Ни кода HTTP, ни числа полученных байт, ради которых переписан `read_body_counted`, в сообщении нет. То же при несовпадении контрольной суммы (`continue`, строка 519).

**High** | `src-tauri/src/commands/content_updater.rs:323-350` (`read_body_counted`) + `473-568` (`download_updates`) | Усечённое тело БЕЗ `Content-Length` принимается за полный файл, записывается на диск и помечается актуальной версией — состояние залипает навсегда. Сверка длины работает только при объявленном `Content-Length`, а функция `/content`, по замеру самого автора (handoff §4.2), отдаёт порционный ответ без длины. В `reqwest` фичи `gzip`/`brotli` не включены (`Cargo.toml:83`), то есть `content_length()` = `None` буквально для такого ответа. | Посредник (корпоративный шлюз, проверка TLS антивирусом) закрывает соединение «чисто» на середине 34-КБ vault: `res.chunk()` отдаёт `Ok(None)` (content_updater.rs:333) → `declared == None` → сверки нет → `Ok(buf)` с половиной архива. Дальше: `open_cabinet` передаёт `checksums = json!({})` (lib.rs:556) → проверки суммы нет; `crypto::aes::encrypt` шифрует огрызок; `std::fs::write` кладёт его в `vaults/`; `set_vault_version` записывает СЕРВЕРНУЮ версию как установленную. С этого момента: `open_cabinet` докачивает только когда файла НЕТ (`if !vault_path.exists()`), а `check_update_per_cabinet` (content_updater.rs:241-258) сравнивает только номера версий (local == server) → повторная загрузка не сработает никогда. Кабинет открывается с битым tar («Unknown skill»), лечится только ручным удалением файла.

**High** | `src-tauri/src/commands/updater.rs:298-310` (`download_attempt`) | При докачке любой не-2xx ответ возвращает ошибку, НЕ удаляя `.part`. Для `Range: bytes=N-`, где N ≥ полного размера файла, сервер по RFC 7233 отвечает 416 — и это состояние самовоспроизводится: канал обновлений умирает навсегда, все 5 попыток и все последующие запуски приложения дают 416. Ветка сброса `.part` есть только для противоречивого 206 (updater.rs:326). | `.part` доходит до полного размера, но не финализируется. Два реальных пути: (а) `download_attempt` вернул Ok, `std::fs::rename` упал (файл на миг занят антивирусом — сценарий, описанный в комментарии на updater.rs:255) И `std::fs::copy` тоже упал → `?` выбрасывает ошибку из `download_update`, полноразмерный `.part` остаётся; (б) приложение закрыто/убито в промежутке между `file.flush()` и `rename` при загрузке 245 МБ. Следующий запуск: `existing == total` → `Range: bytes=<total>-` → 416 → «Download returned 416 Range Not Satisfiable» ×5 → и так каждый запуск. Пользователь не может обновиться, пока вручную не вычистит `%TEMP%\aurora-update`. Тот же исход, если по тому же URL перевыложен файл МЕНЬШЕГО размера.

**High** | `src-tauri/src/commands/content_updater.rs:302-307` (`resilient_client`) + `387-436`, вызов из `src-tauri/src/lib.rs:94/115/557` | Снят общий дедлайн запроса, но взамен не введено НИ одного ограничения общего времени: `read_timeout` срабатывает только на ПОЛНОЙ тишине сокета 30 с. Канал, который капает данные, но не передаёт — теперь висит бесконечно, тогда как раньше падал за 120 с. Загрузка вызывается внутри `get_cabinets` и `open_cabinet` — обе команды ожидаются интерфейсом. | Шлюз с проверкой TLS отдаёт по одному байту раз в 20–25 с (типичное поведение перегруженного посредника — ровно та среда, ради которой писался блок): `read_body_counted` крутится в цикле `res.chunk()` неограниченно долго, таймаут тишины не наступает никогда. `get_cabinets` не возвращается → список кабинетов не отрисовывается, ошибки нет, отмены нет — приложение выглядит зависшим навсегда. Даже без патологии: одна неудачная докачка теперь стоит до 5×(таймауты) + 37 с пауз, и на N отсутствующих vault-файлов эти интервалы складываются последовательно внутри одного вызова `get_cabinets`.

**Medium** | `src-tauri/src/commands/online_auth.rs:301-324` + `248-252` (`REQUEST_TIMEOUT_SECS = 15`) | Повторы авторизации наложены поверх сохранённого общего таймаута клиента, поэтому путь «сеть недоступна → берём кэш» подорожал с 15 с до 51 с (3×15 + паузы 2 и 4). Кэш и обработка блокировки не ломаются, но выигрыш от кэша съеден ожиданием. | Клиент в сети, где адрес Supabase просто отбрасывается (нет RST, чёрная дыра): каждый `open_cabinet` (lib.rs:518) и каждый `get_cabinets` (lib.rs:58) ждут 51 с ДО обращения к кэшу — было 15 с. У пользователя с валидным кэшем открытие кабинета замирает почти на минуту вместо пятнадцати секунд.

**Medium** | `src-tauri/src/lib.rs:203` (`import_cabinet_vault`, объявление) и `:226-233` | Ограничение размера (`MAX_VAULT_BYTES = 200 МБ`) проверяется ПОСЛЕ `std::fs::read(&path)`, то есть не защищает ни от чего: файл целиком грузится в память до проверки. Команда объявлена синхронной (`fn`, не `async fn`), поэтому чтение, шифрование (ещё одна копия буфера) и запись идут в потоке команд, блокируя интерфейс. | Пользователь по кнопке «Загрузить материалы из файла» выбирает файл с расширением `.vault`, но не тот — например присланный поддержкой общий архив `backup-2026-07.vault` на 2–3 ГБ: приложение читает его целиком в RAM, затем ещё раз в шифровании, и только после этого сообщает «Файл слишком велик». На машине с 8 ГБ это отказ выделения памяти/падение процесса, а не аккуратная ошибка; интерфейс на всё время заморожен.

**Medium** | `src-tauri/src/lib.rs:256-260` (`import_cabinet_vault`) | Импорт перезаписывает существующий файл кабинета молча и НЕ трогает `vault-versions.json`. В сочетании с тем, что `open_cabinet` докачивает только отсутствующий файл (lib.rs:544), а `check_update_per_cabinet` сравнивает лишь номера версий, ошибочный импорт залипает навсегда. | У клиента кабинет `econometrist` работает, в `vault-versions.json` записана версия 12, на сервере тоже 12. Поддержка присылает файл не от той сборки (или прошлой версии) — пользователь импортирует, файл ложится поверх рабочего. Локальная версия по-прежнему 12 = серверной → автообновление не сработает, `vault_path.exists()` = true → докачка не сработает. Кабинет навсегда на чужих/старых материалах, штатного способа откатиться нет (кнопка «сбросить» не предусмотрена).

**Medium** | `src-tauri/src/commands/content_updater.rs:971-985` (тест `truncated_body_is_not_accepted_as_success`) | Тест проходит по совпадению: он доказывает не новую проверку длины, а поведение hyper. Тестовый сервер объявляет `Content-Length` и рвёт сокет — hyper сам возвращает «connection closed before message completed», срабатывает ветка `Err(e)` на строке 334, а новая проверка `buf.len() != n` (строки 345-349) не выполняется. Утверждение `text.contains("байт")` истинно для ОБЕИХ веток и различить их не может. | Внести регрессию: удалить блок `if let Some(n) = declared { ... }` целиком — все четыре новых теста останутся зелёными. При этом единственный сценарий, который проверка обязана была закрыть (усечение без ошибки сокета), не покрыт ни одним тестом, и в реальном ответе `/content` без `Content-Length` он не закрывается вовсе (см. находку High выше).

**Medium** | `src-tauri/src/lib.rs:570-580` (`open_cabinet`) | Текст отказа утверждает конкретную причину («Связь с сервером обрывается при передаче файла») безусловно — в том числе когда никакой передачи не было или сервер ответил отказом по существу. Пользователя и поддержку это уводит в диагностику сети вместо реальной причины. | (а) Офлайн-клиент с действующей лицензией Ed25519 и отсутствующим vault: срабатывает ветка `else` на строке 566 (`сервер лицензий недоступен`), запрос за файлом вообще не отправлялся — но пользователю сообщают, что связь рвётся при передаче файла. (б) `/content` отвечает 403 «лицензия не найдена» на конкретный vault (лицензия проверяется на `/auth`, доступ к файлу — отдельно) → тот же текст про обрыв связи; совет «попросить администратора открыть доступ» бесполезен.

**Medium** | `src-tauri/src/commands/content_updater.rs:495-553` (`download_updates`) + `src-tauri/src/lib.rs:91-120` | Защиты от параллельных загрузок одного vault нет (она есть только у установщика, `in_flight_downloads`), а повторы удлинили окно перекрытия с секунд до минут. Записи идут `std::fs::write` в один и тот же путь, а параллельный читатель видит файл в промежуточном состоянии. | По журналу самого автора (handoff §4.6) при старте идут по 4 параллельные загрузки одного файла. С новым кодом каждая из них живёт до ~3 минут (5 попыток + паузы 2/5/10/20 с). Пока загрузка A дописывает `econometrist.vault`, загрузка B делает `std::fs::write` на тот же путь (усечение до нуля и запись заново), а `open_cabinet`/фильтр «undecryptable» на lib.rs:80-85 в этот момент читает файл → `crypto::aes::decrypt` падает → кабинет объявляется битым и ставится в очередь на повторную загрузку, что запускает следующий виток.

---

Вердикт: требует исправлений.
