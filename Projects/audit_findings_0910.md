# Находки внешнего аудита блока 09–10.09 (база 6a650f92..a769ad94)

Формат: `severity | файл:строка | суть | воспроизводимый сценарий`

High | src/routes/settings/+page.svelte:688 | Подпись «Сейчас: …» смотрит только на localOnly и игнорирует granted — при отозванном согласии печатает «материалы уходят на наш сервер», хотя они не уходят | Облачная редакция, нажать «Облачная обработка → Выключить» (granted=false), localOnly остаётся false. На экране одновременно «✓ Облачная обработка отключена» и жирное «Сейчас: при обращении к ИИ-ассистенту материалы уходят на наш сервер». То же на свежей установке до выдачи согласия.
Medium | src/routes/settings/+page.svelte:688 | Тот же текст не ждёт loaded — до ответа бэкенда (и навсегда, если запрос упал) печатает состояние по умолчанию localOnly=false | Открыть настройки при медленном/сбойном чтении состояния согласия: у пользователя с включённым «Только локально» жирная строка сначала утверждает обратное.
Medium | sidecar/econometrica/engines/channel_action.py:442 | Правка «вердикт vs направление бюджета» закрыла только полосу eff_mroas ∈ (0; 0,8); в полосе [0,8; 1,0) ветка 6b по-прежнему даёт «Сократить умеренно» без оглядки на ratio | Канал с eff_mroas 0,9, которому оптимизатор ДОБАВЛЯЕТ бюджет (ratio>1): на экране «+X %» рядом с «снизить риск / сократить». Ровно то противоречие, которое чинили одной полосой ниже.
High | sidecar/econometrica/engines/optimizer.py:612 | Тот же класс, что найденный AttributeError: несогласованные проценты границ роняют optimize() наружу необработанным ValueError | Зонд: optimize({'total_budget': None,'min_pct':150,'max_pct':50}, проект) -> ValueError: global_min_pct (1.5) > global_max_pct (0.5) - incoherent; optimize({... 'min_pct':-50 ...}) -> ValueError: global_min_pct=-0.5 must be non-negative. Ни одна из двух не превращается в {'status':'error'}.
Medium | sidecar/econometrica/engines/optimizer.py:760-780 | Проверки осуществимости бюджета не смотрят на конечность числа: NaN проходит оба сравнения (любое сравнение с NaN ложно) и расчёт продолжается на NaN-цели | Зонд: optimize({'total_budget': float('nan'),'min_pct':50,'max_pct':150}) -> status ok, optimization_converged False, expected_lift_pct 11.0. Клиенту показывается прирост, посчитанный от несуществующего бюджета, вместо INFEASIBLE/ошибки.
Medium | tools/verify_aurora_html_brand.py:270 | Сторож тире объявлен про «user-visible content», но вырезает <script> — а весь клиентский текст диаграмм живёт именно там (CHART_DATA), и вдобавок в ensure_ascii-виде \u2014 | Вход, на котором сторож ОБЯЗАН покраснеть и не краснеет: длинное тире в подписи водопада/названии канала/строке contrib_label (builder.py:_chart_data_json -> interactive.py:52 var CHART_DATA). Тире видно на экране в диаграмме и подсказке, сторож печатает «No em dashes in user-visible content: OK».
Medium | sidecar/econometrica (20 файлов) | Замена тире охватила 13 файлов, но в клиентских строках движка осталось 50 длинных тире в 20 файлах вне тестов — инвариант «клиентский текст = короткое тире» не достигнут, а принятый признак полноты (вставок = удалений) этого не измеряет | Зонд-скан строковых литералов (без докстрок/тестов): optimize/split_ci.py:89 «Суммарный бюджет равен нулю — сплит не определён.», utils/best_practice_rules.py:242 «Рекомендуем выбрать единый режим — либо все каналы в ₽...», server.py:1190, utils/holiday_calendar_ru.py:138/158/168.
High | sidecar/econometrica/tests/test_forecast_report.py:161 | Правка обесточила сторож: было `assert "—" in joined` (длинное тире было уникально прочерком), стало `assert "–" in joined` — а короткое тире на том же слайде есть всегда, им разделяется интервал | На том же слайде builder.py:3906 печатает `f"{ci_low} – {ci_high}"` (тест выше в этом же файле ждёт «414 – 506»). Вход, при котором проверка обязана краснеть и не краснеет: заменить прочерк отсутствующих денег на пустую строку — тест остаётся зелёным.
Medium | src/routes/settings/+page.svelte:1014,1016 и src/routes/cabinet/+page.svelte:466 | onauxclick вешается без разбора кнопки: auxclick шлёт не только средняя кнопка, но и правая и боковые (назад/вперёд) — правый клик по ссылке теперь открывает сайт в системном браузере | Правый клик (или боковая кнопка мыши) по «auroraai.pro» в «О программе» либо по «Скачать Python» в кабинете: срабатывает openUrl, чего до правки не было. Нужен фильтр event.button === 1.
Medium | sidecar/econometrica/aurora_html/sections.py:538 | Запасное значение метки конфиденциальности переведено с литерала на `strings["brand"]["confidentiality"]` доступом по ключу — теперь сам запасной путь может выбросить KeyError там, где раньше был гарантированный литерал | Таблица строк без ключа brand.confidentiality (или без раздела brand — например, будущая нерусская таблица): render_cover падает с KeyError вместо того, чтобы отрисовать обложку. Прежний код в этом же случае отрабатывал.
Low | tools/build-cloud.mjs:955 | Отдельно разобраны только коды «питона нет» (9009/127); любой сбой самого сторожа (трассировка интерпретатора = код 1) неотличим от нарушения доставки и печатается как «общий слой не доставлен: правки крейта не помечены» | Подсунуть в PATH python без нужного модуля/старой версии: сторож падает с трассировкой и кодом 1, сборка отказывает с ложным диагнозом. Свои коды сторожа (0/1/2) не отделены от аварийного выхода интерпретатора.
High | sidecar/econometrica/engines/channel_action.py:442,517 | Правка закрыла полосу eff_mroas ∈ (0;0,8), но канал с ОТРИЦАТЕЛЬНОЙ предельной отдачей не попадает ни в одну ветку «Сократить» (все требуют eff_mroas > 0) и падает в запасной «Наблюдать» — самый убыточный канал получает самый мягкий вердикт | Зонд-2: проект, где radio вычитает вклад; optimize 50/150 -> radio mroi_optimal = -75.0, оптимизатор режет его на -50 %, а вердикт «-28,84×, разрыв +0.0 пп - нужен мониторинг» (Watch). Ветка 3 (Cut при ratio<0.5) промахивается на границе ровно 0,5.
Low | sidecar/econometrica/engines/channel_action.py:161 | Замена коснулась только U+2014; в том же файле клиентские пояснения используют дефис « - » (строки 334/352/364/435/447/460/512/517), и после правки один и тот же экран смешивает «–» и « - » | Любой отчёт с вердиктами каналов: «CPU – 12,3 ₽» рядом с «насыщение, 27.31× падает с ростом вложения - …». Инвариант «клиентский текст = короткое тире» в затронутом файле не достигнут.
Low | sidecar/econometrica/engines/optimizer.py | Медиа-канал с нулевыми историческими тратами молча исчезает из результата распределения — в channels его нет, сообщения клиенту нет | Зонд-2: тот же проект с колонкой radio из одних нулей, объявленной медиа-каналом; optimize возвращает 4 канала из 5. Не проверяла, внесено ли это данной правкой — вероятно, было раньше.
