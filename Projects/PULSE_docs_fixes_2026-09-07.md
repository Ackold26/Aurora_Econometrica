# Пульс: правки документов 2026-09-07

## Задачи
1. Убрать телефон-заглушку `8-800-123-12-34 (перезвоним)` из блока контактов ванпейджера MMM Optimizer (docx). Оставить почту и упоминание формы обратной связи. Обязательна проверка глазами через PDF/PNG на предмет "решета" из пробелов после правки XML.
2. Заменить кириллическое `ГРП` на латинское `GRP` в AnalysisModeSelector.svelte (строка 126), дерево _thinwt. Проверить grep + npm run check.

## План
1. Задача 1: бэкап .docx → распаковать zip → найти фрагмент XML контактов → показать в маячке → аккуратно вырезать номер+хвост → запаковать → собрать PDF → отрендерить последнюю страницу PNG → посмотреть.
2. Задача 2: найти строку 126 → заменить ГРП→GRP → grep проверка → npm run check.
3. Отчёт REPORT_docs_fixes_2026-09-07.md.

## Ход работы
- 22:31 — старт. Папка Projects подтверждена, файл маячка создан.
- 22:33 — Задача 2: заменил "ГРП" на "GRP" в AnalysisModeSelector.svelte:126. grep -rn "ГРП" src/ → пусто (0 совпадений). Запускаю `npm run check` (svelte-check, может занять время).
- 22:36 — `npm run check` завершён: COMPLETED 4159 FILES 0 ERRORS 177 WARNINGS 31 FILES_WITH_PROBLEMS. Все предупреждения — старые (unused CSS selectors, a11y label), не связаны с правкой. Задача 2 ЗАВЕРШЕНА.
- 22:36 — Перехожу к задаче 1: бэкап ванпейджера.
- 22:38 — Задача 1: docx распакован в Temp. Найден фрагмент XML контактов:
  ```
  <w:r><w:rPr>...bold...</w:rPr><w:t xml:space="preserve">Запросить демо-период: </w:t></w:r>
  <w:r><w:rPr><w:color w:val="0070C0"/>.../><w:rPr>
    <w:t xml:space="preserve">8-800-123-12-34 (перезвоним) </w:t><w:br/>
    <w:t>sales@aurora-platform.pro · форма обратной связи на aurora-platform.pro</w:t>
  </w:r>
  ```
  Телефон ЦЕЛИКОМ в одном <w:t> (не разорван между фрагментами). Разделитель "·" стоит между почтой и "форма обратной связи" (это НЕ связано с телефоном, трогать нельзя). Между телефоном и почтой стоит <w:br/> (перенос строки), не текстовый разделитель.
  Решение: убрать <w:t xml:space="preserve">8-800-123-12-34 (перезвоним) </w:t><w:br/> целиком (текст + перенос строки). Итог: "Запросить демо-период: sales@aurora-platform.pro · форма обратной связи на aurora-platform.pro" одной строкой, без висящих разделителей.
- 22:37 — document.xml исправлен, проверен программно (8-800/перезвоним отсутствуют, sales@ на месте), архив пересобран через python zipfile (сохранены compress_type/атрибуты/порядок всех 12 файлов), testzip() = None (архив цел).
- 22:37 — Оригинальный docx на Рабочем столе заменён исправленной версией (18425 байт, было 18641). Резервная копия .bak-2026-09-07 (18641 байт) сохранена рядом.
- 22:38 — PDF собран через LibreOffice headless (soffice.exe --convert-to pdf), 2 страницы. Последняя страница отрендерена в PNG через pymupdf (dpi=200): C:\Users\ackol\AppData\Local\Temp\claude\vanpager_pdf_20260907\last_page.png
- 22:39 — Посмотрел PNG глазами: строка контактов одна ровная строка по центру "Запросить демо-период: sales@aurora-platform.pro · форма обратной связи на aurora-platform.pro". Никакого "решета" пробелов, висящих разделителей, телефона нет. ЗАДАЧА 1 ЗАВЕРШЕНА.
- 22:40 — Пишу финальный отчёт REPORT_docs_fixes_2026-09-07.md.
- 22:41 — Харнесс запретил Write-инструменту создавать REPORT_docs_fixes_2026-09-07.md (политика "не писать report-файлы, отдавать текстом"). Отчёт отправлен team-lead напрямую через SendMessage вместо файла на диске. Обе задачи завершены.

## Раунд 2 (та же дата) — три новые правки в том же ванпейджере
Задачи от team-lead: (1) заменить обещание "независимое доказательство корректности" на честное "паспорт расчёта" (аудиторы/СБ), (2) "сертификатами методологии"→"паспортами расчёта" в блоке Пилот 30 дней, (3) убрать "gross ЗП"/"~" в фразе про зарплату эконометриста + заменить два дефиса на короткое тире "–" (таблица "Оценка результата" и финальная строка). Резервную копию делаю заново с пометкой -2, прошлую не трогаю.
- $(date '+%H:%M') — старт раунда 2.
- 23:49 — Все 3 правки применены в document.xml, проверены программно (count==1 на каждую, старый текст исчез, "ТОП-уровня" (составной дефис) не тронут). Архив пересобран (testzip=None, 12 записей), оригинал заменён.
- 23:50 — PDF собран через LibreOffice. 🔴 НАХОДКА: страниц стало 3 (было 2 после раунда 1). Страница 1 без изменений (текст правок туда не попадал). Страница 2 — все 6 точек правки видны, текст ровный, без "решета" пробелов, en-dash "–" на месте вместо "-" в обеих точках, "ТОП-уровня" не тронут. Страница 3 — ТОЛЬКО строка контактов ("Запросить демо-период: sales@...") одна на пустой странице. Причина: правка 1 (абзац "аудиторов и СБ") удлинила текст, последний абзац (контакты) не поместился на стр.2 и уехал на отдельную стр.3 — визуально плохо для "ванпейджера" (one-pager).
- 23:51 — Решение по вёрстке НЕ принимаю самостоятельно (не входит в 3 согласованные правки, требует правки интервалов/сокращений в других местах документа). Докладываю team-lead с путями PNG, жду решения.
- 23:55 — team-lead сократила формулировку правки 1 (была слишком длинной, вызвала переползание на стр.3). Новый текст: "Для аудиторов и СБ – паспорт расчёта под контрольной суммой: независимый специалист повторит расчёт и сверит результат." Применяю замену, вёрстку не трогаю (правило прямо запрещено).
- 23:53 — Замена применена (count==1), архив пересобран (testzip=None, 12 записей), оригинал заменён (18452 байт). Обе резервные копии (-2026-09-07 18641 байт и -2026-09-07-2 18425 байт) на месте, не тронуты.
- 23:54 — PDF пересобран: снова 2 страницы (было 3). Страница 1 без изменений. Страница 2: строка контактов вернулась в низ страницы, "Для аудиторов и СБ – паспорт расчёта под контрольной суммой: независимый специалист повторит расчёт и сверит результат." читается ровно, без "решета" пробелов. Все прочие правки (2, 3a, 3b) на месте и не пострадали. РАУНД 2 ЗАВЕРШЁН, доложено team-lead.
- 23:57 — team-lead приняла попутное наблюдение и меняет решение: имя артефакта остаётся "сертификат методологии" (совпадает с полем продукта methodology_certificate и честной формулировкой на стр.1), лечим только обещание "независимое доказательство корректности". Новый текст правки 1: "Для аудиторов и СБ – сертификат методологии с контрольной суммой: независимый специалист повторит расчёт и сверит результат." Правку 2 откатываю на "сертификатами методологии" (как было изначально). Вёрстку не трогаю.
- 23:58 — Правки применены (count==1 на каждую), архив пересобран (testzip=None, 12 записей), оригинал заменён (18447 байт), обе резервные копии не тронуты. PDF пересобран: снова 2 страницы. Страница 2 проверена глазами: "Для аудиторов и СБ – сертификат методологии с контрольной суммой: независимый специалист повторит расчёт и сверит результат." + "Пилот 30 дней: ...сертификатами методологии" (откачено), контакты внизу страницы. grep по document.xml: "паспорт расчёта" = 0 вхождений, "сертификат методологии" (все формы) = 5 вхождений (3 прежних на стр.1 + 2 наших правки). РАУНД 3 ЗАВЕРШЁН, доложено team-lead.

## Раунд 4 (2026-09-08) — срочно: мёртвый адрес почты
team-lead проверила зондом: aurora-platform.pro без MX-записей, домен отдаёт 404 от регистратора. Единственный контакт в документе (после удаления телефона) мёртв. Решение владельца: заменить sales@aurora-platform.pro -> sales@auroraai.pro (MX на mx.yandex.net подтверждён), убрать упоминание формы обратной связи целиком (её нет ни на aurora-platform.pro, ни на auroraai.pro — там заглушка без формы сознательно). Итоговая строка: "Запросить демо-период: sales@auroraai.pro". Резервная копия -3. Проверка: aurora-platform не встречается нигде в документе. PDF - 2 страницы, обе глазами.
- $(date '+%H:%M') — старт раунда 4.
- 02:55 — Резервная копия -3 создана (18447 байт). Найдено единственное место с "aurora-platform" в документе: <w:t>sales@aurora-platform.pro · форма обратной связи на aurora-platform.pro</w:t> (одна текстовая нода, встречается 2 раза внутри неё). Заменено целиком на <w:t>sales@auroraai.pro</w:t>. Проверено программно: aurora-platform больше не встречается ни разу во всём document.xml.
- 02:56 — Архив пересобран (testzip=None, 12 записей), оригинал заменён (18420 байт), все 3 резервные копии на месте. PDF пересобран: 2 страницы (без переползания). Страница 1 без изменений. Страница 2: строка контактов "Запросить демо-период: sales@auroraai.pro" — ровно, без висящего разделителя "·", без упоминания формы. РАУНД 4 ЗАВЕРШЁН, доложено team-lead.
