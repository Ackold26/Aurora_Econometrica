# PULSE — обещания в колоде: из сноски в раздел

**Задача (своими словами):** сбывшиеся/несбывшиеся рекомендации в PPTX-колоде были
сноской (кегль 9,5-10) в правой колонке слайда SOURCES. В HTML под них уже есть
полноценный раздел «Петля доверия». Нужно поднять их в колоде до отдельного
условного слайда по образцу HTML и приёма условных слайдов (backtest/gen_compare),
без пустых заглушек, с тестами, мутацией, живой сборкой и гейтом движка.

**Решение о форме (12:55):** новый условный слайд `s10d_promises` в зоне вставных
слайдов честности «Главное» после SCQAR: backtest → gen_compare → promises →
forecast (как в HTML render_trust_loop — один раздел «Петля доверия» на все три).
`_page_shift` расширен; s_forecast_plan и TOC-подстрока про прогноз пересчитаны.
Старая сноска в s11_sources удалена, заменена комментарием-указателем.

**Прогресс:**
- 12:28 старт, разведка — сделано.
- 12:55 решение о форме принято, правки builder.py внесены.
- 13:10 написан test_promises_slide.py (9 тестов: present/absent/overflow/
  page_shift/forecast-shift/мутация), все зелёные.
- 13:15 реальная физическая мутация в builder.py (`if True:` вместо
  `if self.promises_summary:`) — 3 теста покраснели по адресу, откат подтверждён,
  снова 9/9 зелёных.
- 13:25 живая сборка на реальном проекте кагоцел (только чтение, вывод во
  временный каталог `.../scratchpad/promises_live_build/`):
  - без синтетики: backtest=yes, gen_compare=no, promises=[] (законный исход,
    файла results/promises.json в проекте нет), forecast=no → 13 слайдов,
    блока «Проверка рекомендаций» нет (проверено текстовым поиском), overflow 0.
  - с синтетическими promises (только в память, в проект не писала): слайд
    встал 7-м из 14 сразу после «Проверка на истории», подпись и цифры верны,
    overflow 0.
- 13:30 запущен полный гейт `python -m pytest sidecar/econometrica/tests -q`
  (ожидаем ≥1109 passed, 1 skipped, без падений) — в фоне, ждём результат.
