# PULSE — сторож CPD-77/79 (2026-08-14)

## Задача своими словами
Из кода Aurora Econometrica убрали два опасных действия: переустановку прав `icacls /grant:r` (CPD-77) и разведку процессов по порту через `netstat`/`findstr`/`tasklist` (CPD-77+CPD-79). Опасность не в том, что кто-то напишет их заново, а в том, что они молча вернутся при слиянии старой ветки (доказано на 14.08 — живы в `feat/econ-canon-p0` и `fix/econ-gw-sign-prefix`). Нужен тест-сторож: сканирует исходники `.rs`, отбрасывает комментарии, падает на живых образцах `icacls`/`/grant:r`/`netstat`/`findstr`/`tasklist`, не падает на объясняющих комментариях. Доказать мутацией в обе стороны.

## План
1. Прочитать win_acl.rs, session/manager.rs, econ_sidecar.rs, sidecar_runtime.rs — понять, где сейчас упоминаются эти слова (в коде vs в комментариях).
2. Решить, где разместить сторож (отдельный модуль или существующий тестовый файл) + как надёжно исключить сам файл сторожа из сканирования.
3. Написать функцию отсечения комментариев (// /* */) и тест, сканирующий все .rs в src-tauri/src кроме файла сторожа.
4. Прогнать тест на текущем коде — должен пройти.
5. Мутация: вставить настоящий вызов icacls в файл продукта, прогнать — должен упасть с указанием файла/строки/образца. Откатить.
6. Финальный `cargo test`, проверка counts >= 440+1, честная сверка RC и PASSED.
7. git status/add поимённо, запись изменений.
8. Написать отчёт GUARD_CPD77_2026-08-14.md.

## Лог
- 16:48:52 — старт, ветка master подтверждена, файл PULSE создан, список .rs файлов получен.
- 16:52:05 — прочитаны win_acl.rs, econ_sidecar.rs (kill_sidecar_from_state), grep по всем .rs подтвердил: icacls/netstat/findstr/tasklist сейчас живут только в комментариях (win_acl.rs, econ_sidecar.rs, sidecar_runtime.rs, session/manager.rs). Написан src-tauri/tests/guard_no_regressed_cpd77_cpd79.rs (интеграционный тест, вне src/ — самоисключение по расположению).
- 16:53:20 — первый прогон `cargo test --test guard_no_regressed_cpd77_cpd79` на чистом коде: 3/3 ok (компиляция 1m19s из-за clean build).
- 16:55:03 — мутация: в src-tauri/src/errors.rs вставлен настоящий `Command::new("icacls")`.
- прогон целевого теста после мутации: FAILED, назвал errors.rs:11 и образец «icacls» дословно. Мутация немедленно откачена Edit'ом, `git diff --stat errors.rs` пуст.
- 16:56:16 — старт полного `cargo test` (лог → Projects/guard_cargo_test_2026-08-14.log).
- Итог: RC=0, 440+3=443 passed, 0 failed. git add поимённо только нового файла теста, Cargo.lock и чужие PULSE/логи не тронуты.
- 16:58:54 — запись изменений ef0f5455404d3ada8db1842b671f01e96e5a0943, pre-commit (lefthook) зелёный.
- Отчёт записан в Projects/GUARD_CPD77_2026-08-14.md. Задача завершена.
