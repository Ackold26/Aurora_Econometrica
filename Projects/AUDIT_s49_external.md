# Внешний аудит несведённых правок s49 (2.5.5) — журнал и итог

Аудитор: внешний, кода не писал. Дерево: `D:\Docs\Aurora_Ai\Dev\Aurora_Econometrica_thinwt`, ветка master, поверх вершины `1e3b9976`.
Начало 18.09.2026, 12:00. Код не менялся, записей в git не делалось. Установщик продукта не собирался (учебные `.nsi` во временной папке — зонды, не сборка).

## 🔴 Расхождение задания и дерева (сказать сразу)

В постановке блок 1 описан так: «имя читается инструкцией NSIS `ExpandEnvStrings`, пустое значение снимает фильтр целиком, регистры `$R0`/`$R1` сохраняются через `Push`/`Pop`». **В дереве лежит другое решение:** команда идёт через оболочку (`cmd /c`), переменную разворачивает она, сохраняются `$0`/`$1`, а `ExpandEnvStrings` в шапке файла числится ОТВЕРГНУТЫМ путём. Аудировала то, что в дереве.

## Что проверено делом и оказалось ЧИСТО

1. **Класс дефекта Docs Lab (установщик внутри снимаемого дерева) здесь НЕ воспроизводится.** Зонд: цепочка A → B (powershell, завершился) → внук; `taskkill /PID A /T /F` — внук ЖИВ (его PPID указывает на уже мёртвого B, а снятие дерева идёт по живым связям). В продукте ровно эта форма: `updater.rs::apply_update` ждёт завершения PowerShell блокирующим `.status()` и только потом снимает движок и выходит. Других путей запуска установщика нет — поиск по нескольким признакам (`apply_update`, `installer_path`, `Start-Process`) даёт единственное место `lib.rs:2984 → updater::apply_update`.
2. **Макрос работает.** Учебный установщик с ДОСЛОВНОЙ копией `AURORA_KILL_AND_WAIT` компилируется и снимает живой процесс: жертва (копия `PING.EXE`) — «жива до: 1», «жива после: 0». Фильтр `USERNAME eq %USERNAME%` на русской Windows совпадает (иначе жертва осталась бы).
3. **Настоящий `installer_hooks.nsh` компилируется в той же раскладке, что у шаблона Tauri** (PREINSTALL+POSTINSTALL в одной секции, PREUNINSTALL+POSTUNINSTALL в другой — так вставляет `target/release/nsis/x64/installer.nsi`, строки 612 / 15748 / 15793 / 31145). Ни одного столкновения меток, ни одной ошибки. Живая проба «установщик поверх работающей программы» по-прежнему не проводилась — это остаётся признанным долгом автора.
4. **Тесты затронутого — зелёные:** сторожа границы мягкого удаления 4/4; `user_config` 20/20; `errors` 3/3 (в том числе новый про CRLF); `workspace_clear` 3/3; `pytest` подписи периода 3/3 и соседние SCQAR 54/54.
5. **Поставка образцов и копий движка сходится:** `sections.py` и `strings_ru.json` в `_internal` и в `dist` совпадают с исходником (12:02, `period_phrase` во всех трёх). Образец покупателю пересобран: «в квартал» — 0 вхождений, длинных тире — 0, фраза читается «размещает 3535 млн ₽ за период янв 2023 – дек 2024». Слов «доверительный интервал» и «CI» в клиентском тексте нет.
6. **Согласие на условия зациклиться не может:** `accept_trial_consent` записывает редакцию из Rust-константы, без участия фронта, поэтому сравнение на несовпадение всегда сходится после нажатия.
7. **Очистка при запуске выровнена с соседом:** `clear_inbox` (`session/manager.rs:602`) уже фильтровал `is_file()`; правка привела `clear_workspace_dirs` к тому же. Незакрытого близнеца нет. Список очистки — только `inbox`; персональных данных правка не касается, затирание расшифрованного осталось безвозвратным (сторож зелёный).

## Находки

**1. Medium | `sidecar/econometrica/version_info.txt:48,53` | Собранный движок отдаёт версию 2.5.4.0 при продукте 2.5.5 — ровно тот класс, ради которого заведён `sync_version_info()`.**
Проверено делом: `(Get-Item sidecar\econometrica\econometrica-sidecar.exe).VersionInfo.FileVersion` = **2.5.4.0**, `ProductVersion` = 2.5.4.0. Времена: `package.json` поднят в 11:58:47, движок собран в 12:01:56 (то есть ПОСЛЕ бампа), а `version_info.txt` остался от 08:24:17 — значит `build_sidecar.py::sync_version_info()` в этой сборке не отрабатывал. Сценарий: ИТ-специалист покупателя открывает свойства `econometrica-sidecar.exe` в установленной 2.5.5 и видит 2.5.4 — то самое расхождение, про которое в докстринге написано «движок 2.5.0 уехал внутри продукта 2.5.2». Лечение: пересобрать движок штатным `build_sidecar.py` (он сам сведёт версию и упадёт на расхождении) ДО сборки установщика.

**2. Medium | `src-tauri/installer_hooks.nsh:104-119` | Потолок ожидания стоит дорого: одна попытка ≈ 2,7 с, значит до ~55 с на образ и до ~110 с на два, если процесс не снимается.**
Замерено делом на учебном установщике: два вставленных макроса без живого процесса — 6,59 с, с живым — 5,41 с (одна попытка на образ; основная цена — `tasklist`). Потолок 20 попыток — расчёт от измеренной цены. Когда снять не удаётся (программа запущена с повышением, а установщик — нет; файл держит защитное средство), тихая установка `/S` ничего не показывает: человек до двух минут видит «ничего не происходит», а затем всё равно получает «Error opening file for writing». Прежний код стоил фиксированные 2,5 с. Не дефект правки по существу — цена, о которой стоит знать заранее; при желании лечится уменьшением числа попыток.

**3. Medium | `src-tauri/src/soft_delete.rs:390-395` | Поимённое исключение `active_path` стало ГЛОБАЛЬНЫМ: в новом стороже оно снимает запрет с любой строки любой достижимой функции.**
В стороже по телам исключение привязано к месту (`brand_delete`). В стороже достижимости все исключения собраны в плоский перечень `filter_map(|(_,_,_,isk)| *isk)` и проверяются как «строка содержит маркер» — без привязки к функции и без учёта уровня. Сценарий: будущий помощник `fn hard_purge(active_path: &Path) { let _ = std::fs::remove_file(active_path); }`, достижимый из любого из семи входов, пройдёт мимо сторожа — индульгенцию выдаёт имя параметра. Проверено репликой алгоритма на настоящем дереве: под маркер сейчас попадает ровно одна законная строка — `commands/brand.rs | let _ = std::fs::remove_file(&active_path);`. То есть сегодня дыра пустая, но она шире, чем была задумана.

**4. Low | `src-tauri/src/soft_delete.rs:430-436` | Неназванная слепота: общий `videno` плюс обход стопкой обрезают спуск у помощника, впервые встреченного на предельной глубине.**
Имя, впервые попавшее в обход на уровне 3, помечается виденным и не раскрывается; если та же функция вызывается и с уровня 1 из другого входа, её собственные помощники не проверяются никогда. Сегодня не срабатывает — проверено репликой: обход стопкой (как в стороже) и обход очередью дают одинаковые 37 имён и одинаковые глубины, расхождений нет. Слепота латентная: проявится от перестановки вызовов, без единой правки сторожа.

**5. Low | `src-tauri/src/soft_delete.rs:286-306` | Границы тела считаются по скобкам без учёта литералов, а объявление без выделенного тела молча выпадает из индекса — и спуск в него не идёт.**
`telo_ot` считает `{` и `}` в том числе внутри строковых и символьных литералов; функции, для которых он вернул `None`, в индекс не попадают вовсе, а вызов такой функции отсекается строкой `!index.contains_key(kandidat)` — тихо, без следа в сообщении. Проверено репликой: 31 имя объявлений без тела; охват сторожа — 41 тело из 1424 функций крейта. Ни одна из этих двух слепот в авторском перечне не стоит (он назвал типажи, замыкания, указатели, макросы, чужие крейты, грубое снятие комментариев и глубину).

**6. Low | `sidecar/econometrica/aurora_html/sections.py:663` | Одна и та же фраза в двух поставляемых документах расходится склонением.**
HTML: «через 4 активных каналов». PPTX (`aurora_pptx/builder.py:2834-2841`) считает склонение по числу: «через 4 активных канала». Покупатель получает оба файла по одному прогону и читает две разные фразы про один портфель — проверено на образцах `Projects/_demo_samples_s49/`. Дефект прежний, правка его не вносила, но сидит ровно в изменённой строке.

**7. Low | `src/lib/components/SessionTimer.svelte:38-47` | Запись верна для стартового экрана, но названа шире, чем доказана.**
Перехват гейта стоит только на `click` (`TrialConsentOverlay.svelte:112`) и на `keydown`. Не перехвачены также `mousedown`, `pointerdown`, `contextmenu`, `auxclick` (`CommandCard`, `FileList`, `WorkflowCanvas`, `cabinet`, `settings`) и перетаскивание файла в окно Tauri (`onDragDropEvent` — вообще не DOM-событие: `FileList.svelte:267`, `pipeline/ImportStep.svelte:225`, `brand/[id]/+page.svelte:59`). Обхода сегодня нет: все перечисленные — на других маршрутах, а авто-перехода при запуске не происходит (`autoRedirectInProgress` нигде не ставится в `true`). То есть граница держится маршрутом, а не перехватом, и запись это не называет.

**8. Low | `src/tokens.generated.css:3`, `.impeccable/hook.cache.json`, файл `nul` в корне | В правки затесалось постороннее.**
`src/tokens.generated.css` отличается от вершины ровно одной строкой — `Last build: 2026-09-18T11:54:57`; палитра не менялась. `.impeccable/hook.cache.json` — кэш средства разработки, отслеживается в дереве. В корне лежит неотслеживаемый файл `nul` — след перенаправления вывода в оболочке. В установщик ничего из этого не попадает (в `resources` их нет), но в запись изменений уедет шумом.

**9. Low (гипотеза) | `src-tauri/installer_hooks.nsh:134-144` | Ожившая ветка меняет поведение при РУЧНОМ запуске установщика поверх работающей программы: теперь она снимается без предупреждения.**
Прежде фильтр не совпадал никогда, и установщик просто не мог переписать занятые файлы. Теперь программа и движок снимаются принудительно (`/F`). Для пути самообновления это безразлично — программа и так выходит сама. Для человека, который открыл установщик руками при открытом проекте, это новое: окно закрывается мгновенно. Насколько это больно, зависит от того, есть ли несохранённое состояние — я этого не измеряла, поэтому помечаю гипотезой.

## Итог

Critical — 0. High — 0. Medium — 3 (№1 версия движка, №2 цена потолка ожидания, №3 глобальное исключение сторожа). Low — 6.

**Сборку блокирует одно:** находка №1 — движок обязан быть пересобран штатным `build_sidecar.py` после бампа версии, иначе в 2.5.5 уедет файл, представляющийся 2.5.4. Остальное сборку не блокирует.

## Журнал по ходу

- 12:00 старт, снят `git status --short` и `git diff --stat`: 17 изменённых отслеживаемых файлов, 580 вставок.
- 12:12 diff разобран по шести блокам; копии движка сверены — рассинхрона шаблона и кода нет.
- 12:20 зонд дерева процессов: внук пережил снятие дерева предка. Класс Docs Lab закрыт.
- 12:30 учебный установщик на дословной копии макроса: компилируется, снимает, ждёт факта; замеры 6,59 с / 5,41 с.
- 12:40 прогоны cargo test (4+20+3+3) и pytest (3+54) — всё зелёное.
- 12:50 реплика сторожа достижимости на настоящем дереве: 41 тело из 1424, 37 имён, находок 0; LIFO и FIFO совпадают; глобальное исключение `active_path` ловит одну законную строку.
- 13:00 проверка версий: движок собран после бампа, но с версией 2.5.4.0 в ресурсе — находка №1.
- 13:05 компиляция настоящего файла хуков в раскладке шаблона Tauri — чисто.
- 13:10 журнал и итог записаны.
