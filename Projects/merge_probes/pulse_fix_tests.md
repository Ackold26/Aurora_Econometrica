# Пульс: разбор 9 падающих тестов после слияния

## Задача (своими словами)
После слияния origin/feat/ai-insights-tier2 в наш августовский код приехало 2 файла тестов
(test_deliverable_thinness_disclosure.py — 8 шт, test_diagnostics_verdict.py — 1 шт), которые
падают против нашего кода. Нужно по каждому тесту определить: класс А (тест ловит реальный
пробел в коде — чинить код) или класс Б (тест закрепляет старую июньскую формулировку, а наше
свойство выполнено — расширить ожидание теста). Работаю только в sidecar/econometrica/.

## План
1. Прогнать тесты, посмотреть точные падения.
2. Разобрать по одному: найти проверяемое свойство, найти текущий код, классифицировать.
3. Чинить класс А в коде (с мутационной проверкой), класс Б — расширять ожидание теста с комментарием.
4. Финальный прогон всего набора, добиться 0 failed либо честно объяснить оставшиеся.

## Старт
2026-08-04, начало работы.

## Прогресс (отметка 1)
Исходный прогон подтверждён: 9 failed, 986 passed, 1 skipped.
Починено (класс А, реальный пробел в коде):
1. `test_no_overfitting_artefact_wording_in_caveat` + `test_high_r2_thin_data_verdict_not_misleading` —
   `utils/diagnostics.py::format_thinness_caveat` нёс старый алармистский тон («артефакт
   переобучения», «высокий риск переобучения, ненадёжны») вместо тона McElreath (модель
   опирается на априорные допущения, надёжность ограничена). Переписал обе ветки.
2. `test_sanitize_slug_no_service_word_leak` — `engines/narrative_adapter.py::_sanitize_project_slug`
   не отличал дата-код методологии «ммх-2404» (ДДММ) от настоящего года и не ограничивал
   клиентское имя 2 токенами (докстринг это обещал, код — нет). Добавил разбор
   marker+isolated-4-digit → дата-код (дроп), marker+пара 4-значных → год-диапазон (сохранить);
   `client_label = " ".join(labels[:2])`.
3. `test_channel_reasoning_no_anglicisms` — `engines/channel_action.py` reasoning-строки несли
   англицизмы «Optimizer»/«breakeven» в клиентский текст. Заменил на «Оптимизатор» и «точка
   безубыточности» (уже канон в aurora_pptx/builder.py) в 7 местах (5 reasoning-строк).

## Готово
Все 9 тестов разобраны, полный набор sidecar/econometrica/tests: 995 passed, 1 skipped, 0 failed.
Все правки класса А прошли мутацию (сломать → красный тест → откатить). Отчёт отдан текстом
финального ответа команде.
