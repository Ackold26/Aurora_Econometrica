# Маячок C2 — проверка CPD-91…CPD-111 на дереве Econometrica (thinwt)

## Задача своими словами
Взять записи реестра кросс-продуктовых дефектов CPD-91…CPD-111 (находки 16–17.08 от соседних продуктов, у Эконометрики не проверялись) и по каждой сказать: есть ли этот дефект СЕГОДНЯ в дереве EC, чем доказано, файл:строка, что увидит клиент, чем лечить (не применяя). Запись реестра — гипотеза, проверяю и механизм, и класс тяжести; отрицательный вердикт доказываю так же строго. Ничего не правлю. Ответ = гейт сегодняшнего выпуска 2.5.0.

## План
1. CPD-101 (ПДн в форме обратной связи) — блокирующий класс, первым.
2. CPD-106 (второй путь безусловного копирования в папку выдачи).
3. CPD-104 (обе записи) — xlsx с формулами без кэша значений, зонд openpyxl data_only.
4. CPD-107 (два пути записи со встречными правилами имён).
5. CPD-100 (bounds.py, что осталось незакрытым).
6. Остальные: 91–99, 102, 103 (обе), 105, 108, 109, 110, 111.
7. Отдельно — дефект самого реестра: номера 103 и 104 заняты дважды.

## Отметки

- [04:52:56] Старт. Маячок создан. Далее — вычитка реестра по частям (grep по `^## CPD-`), затем зонды. Первая тихая фаза — чтение записей 91–111 из реестра, оценка ~10 минут.
- [05:02] **CPD-101 → ЕСТЬ, канал живой, блокирующий по ПДн.** Доказано: `src/routes/settings/+page.svelte:770-790` – видимая секция «Обратная связь» с полем «Контакт для связи», `:298` `invoke('submit_feedback', {category, message, contact})`; команда зарегистрирована `src-tauri/src/lib.rs:3864`; тело – в общем крейте, `~/.cargo/git/checkouts/aurora-platform-core-646048ec9d057d6a/35d8345/aurora_core/src/feedback.rs:25` – `https://docs.google.com/forms/d/e/1FAIpQLSdK_JroYSnX09.../formResponse`, поля `entry.1292211421/1623086744/1356992057` (:97-101). Ревизия крейта закреплена в `Cargo.lock:385` (tag `aurora_core-v0.1.0`, rev 35d8345). **Две поправки к формулировке реестра для EC:** (1) правится НЕ в `src-tauri/src/commands/feedback.rs` (там с 15.08 только 16-строчная обёртка), а в общем крейте – это меняет план замены на всю линейку; (2) `obfstr!` в EC НЕТ – адрес открытым литералом (`grep -rn obfstr src-tauri` → пусто).
- [05:14] **CPD-106 → ЕСТЬ (подтверждено), второго такого пути в Rust НЕТ; но найдено ХУЖЕ – см. ниже.** Доказано: `src-tauri/src/session/manager.rs:664-682` `copy_dir_into` – голый `std::fs::copy(entry.path(), &dst_file)` (:674), вызовы `:332` и `:336` из `sync_exports` в `session.user_workspace/exports` (Рабочий стол). `grep -rn "fs::copy" src-tauri/src` – остальные копирования (`brand.rs:311`, `campaign.rs:568/675/789`) идут НЕ в папку выдачи клиента, а в служебные каталоги; `find_free_sibling_name` в дереве отсутствует (`grep -rn find_free_sibling_name` → пусто).
- [05:14] 🔴 **ВНЕПЛАНОВАЯ НАХОДКА, срочная для гейта выпуска.** `src-tauri/src/commands/mod.rs:90-92` – в теле `place_generated_export` первой строкой стоит `// МУТАЦИЯ: поведение ДО правки — безусловная перезапись приёмника.` + `std::fs::rename(produced, dest)?; return Ok(PlacedExport::Created);`, весь защитный код ниже под `#[allow(unreachable_code)]`. Время чтения 05:08, mtime файла 05:07:52, `git diff --stat` = +238 строк не закоммичено → это АКТИВНАЯ мутационная проверка соседнего исполнителя, не дефект master. Два следствия: (1) собирать выпуск в этом состоянии нельзя – защита CPD-70/106 уедет клиенту выключенной; (2) `place_generated_export` пока НЕ вызывается ниоткуда (`grep -rn place_generated_export --include=*.rs .` вне mod.rs → пусто) – то есть защита ещё не подключена к боевому пути.
