# Маячок: правки интерфейса 09.09 (пункты 0, 1, 4)

## Задача своими словами

Три хирургические правки фронта перед показом покупателю 10.09. (0) В строке конвейера
`pipeline/+layout.svelte` убрать ВТОРОЙ логотип-картинку, заменив её текстовой ссылкой
«← На главную», переход `href="/"` сохранить – это единственный клик обратно на стартовый экран.
(1) На шаге «Оптимизация» скрыть блок-записку разработчика с внутренним жаргоном
(`training_horizon`, `forecast budget`, «В roadmap») – скрыть целиком, не переписывая, без
пустой рамки. (4) В таблице декомпозиции переименовать заголовки колонок: «Gap» → «Разрыв, пп»,
«Decay» → «Затухание». Ничего не коммитить, не собирать, окно не открывать.

## План

1. Прочитать план и три файла, сверить строки с фактом кода.
2. Снять числа «до»: проверка типов (`npm run check`) + тесты фронта (`npm run test:unit`).
3. Правка 0 → правка 1 → правка 4.
4. Снять числа «после», сравнить.
5. Отчёт `Projects/FIX_UI_2026-09-09.md`.

## Журнал

- **старт** – план прочитан, маячок создан. Идём читать код и снимать числа «до».
- **шаг 1** – три файла прочитаны, адреса плана совпали с кодом дословно, расхождений нет.
  Поиском подтверждено: тесты не опираются ни на `planning-warn`, ни на заголовки `Gap`/`Decay`,
  ни на классы `.pipeline-logo*`.
- **шаг 2 (числа «до»)** – проверка типов: 4159 файлов, 0 ошибок, 177 предупреждений,
  31 файл с замечаниями. Тесты: 105/105 файлов, 1489/1489 тестов, 0 упало.
- **шаг 3 (правки)** – пункт 0: картинка логотипа в строке конвейера заменена текстовой ссылкой
  «← На главную», `href="/"`, `title`, `aria-label` сохранены; два CSS-правила убранной картинки
  заменены стилем текстовой ссылки. Пункт 1: условие показа блока-записки на шаге «Оптимизация»
  сменено на заведомо ложное, разметка блока не тронута. Пункт 4: «Gap» → «Разрыв, пп»,
  «Decay» → «Затухание».
- **шаг 4 (числа «после»)** – проверка типов: 0 ошибок, 177 предупреждений, 31 файл – в точности
  как «до», новых предупреждений по правленым файлам нет. Тесты: 105/105, 1489/1489, 0 упало.
- **шаг 5** – отчёт `Projects/FIX_UI_2026-09-09.md` записан. Готово. Ничего не коммитила,
  не собирала, окно не открывала.
