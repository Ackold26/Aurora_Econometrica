# PHASE3 — покрытие проверок гигиены клиентского текста (Econometrica)

Старт: 2026-07-25 23:08 (ветка `phase3-econ-text-coverage`, от `feat/econ-v2.3.0` @ `62ac25e`)

## Задача
Проверка `sidecar/econometrica/tests/test_report_text_hygiene.py` покрывает 2 из 17 секций HTML-отчёта.
Расширить на все секции + печатать охват числом. Завести лёгкие проверки для Rust-таблицы (report.rs) и Svelte-интерфейса.
Находки не глушить: очевидное чинить, спорное — в отдельный список Антону.

## План
1. [x] Установить факт: список секций в sections.py, какие сейчас проверяются
2. [x] Прочитать текущий test_report_text_hygiene.py — механику правил
3. [x] Расширить проверку на все секции + печать охвата числом
4. [x] INV-99 доказательство для расширенной HTML-проверки (внести нарушение → красный код → откат → зелёный)
5. [x] Найти таблицу в report.rs (Rust) — завести лёгкую проверку по строковым образцам
6. [x] INV-99 для Rust-проверки
7. [x] Найти тексты интерфейса (Svelte) — завести лёгкую проверку
8. [x] INV-99 для Svelte-проверки
9. [x] Прогнать все тесты sidecar/econometrica/tests, зафиксировать итог числом
10. [x] Разобрать находки: починить очевидное (тире, вендорные имена), спорное — в список
11. [x] Проверить CI-шаг — новые шаги в CI не заводились (все 3 проверки — pytest-тесты внутри
       уже существующего sidecar/econometrica/tests, гоняются существующим CI-шагом без изменений
       workflow; заглушать нечего)
12. [ ] Коммиты явными путями, без пуша
13. [ ] Отчёт

## Итог (2026-07-25)

### 1. HTML-отчёт (sidecar/econometrica/aurora_html/sections.py + tests/test_report_text_hygiene.py)
Было: 2/17 секций (render_key_message, render_at_a_glance). Стало: 17/17, параметризовано по
живому SECTION_RENDERERS (новая секция автоматически попадёт в охват). 16 зелёных + 1 xfail
(глоссарий, спорная находка). Печать охвата (дословно):
`ОХВАТ П8-1/П8-2 (HTML-отчёт, aurora_html.sections.SECTION_RENDERERS): секций проверено 16 из 17
без замечаний; вне охвата (спорная находка, xfail) 1 — ['glossary']`

Найдено и починено в sections.py: 6× em-dash (summary×2, method×2, trust×2), голое «Validate»×2
(method, retro — заменено на «Валидация», реальный RU-лейбл из project-state.js), голый baseline
(sources), голый adstock (retro), внутренняя аудит-заметка утекала в клиентский HTML как
`<!-- -->` комментарий (timeline) — вынесена в Python-комментарий.

Оставлено спорным: глоссарий определяет термин «Baseline» headword-строкой — тот же паттерн,
что уже разрешён для «Adstock» в коде, но исключения для baseline в проверке нет. Решение — Антону.

### 2. Rust-таблица (src-tauri/src/commands/report.rs + tests/test_report_rs_client_text_hygiene.py)
Было: 0 покрытия. Стало: строковые образцы по тем же 4 правилам, скан production-кода до
`#[cfg(test)]` (669 строк со строковыми литералами). Найдено и починено: голый adstock в
fallback model_spec (subtitle + normalization), 5× голый «Baseline»/«baseline» в DISPLAY-тексте
листа «Данные» и explainer (переименовано в «Базовый спрос» + обновлены 2 Rust unit-теста).
Оставлено спорным: «Лифт vs baseline» (Сценарии-лист — другой смысл baseline, не MMM-компонент,
нужен домен-контекст) + «Adstock (geometric)»/«Adstock (Weibull)» (спец-таблица transformations).

### 3. Интерфейс (src/**/*.svelte + tests/test_frontend_client_text_hygiene.py)
Было: 0 покрытия. Наивный скан всего файла — почти чистый шум (1057 «хитов» em-dash по всему
src/ до фильтрации CSS/JS-идентификаторов/mustache-выражений). После вырезания
`<script>/<style>/<!-- -->`/атрибутов-разметки/`{...}`-выражений — 91 реальное вхождение
em-dash в 27 файлах (все — клиентский текст), почищено автоматической точечной заменой
внутри «безопасных» зон (проверено построчным диффом на выборке, 0 повреждений кода/CSS).
Плюс 4 точечные находки baseline/media-/adstock — почищены вручную. Оставлено спорным:
10 находок adstock в 4 файлах (ConfigPanel, ChannelCategoriesPanel, DecomposeStep, ReportStep) —
плотные двуязычные технические тултипы продвинутых панелей, точечный фикс слова оставит текст
непоследовательным, нужно решение о степени русификации.

### Прогон полного набора
`PYTHONPATH=sidecar/econometrica python -m pytest sidecar/econometrica/tests/ -q -o addopts="" -n 4 --dist worksteal`
→ **730 passed, 1 skipped, 4 xfailed**, exit code 0 (было 706 passed + 1 skipped = 707; +28 новых
тестов, из них 4 — сознательные xfail на спорные находки).

### Не хватило на это задание (открыто)
- `cargo test`/`cargo build` для report.rs НЕ запускался (нет данных, что Rust-тулчейн собран
  в этом окружении, машина загружена — не стал слепо запускать долгую сборку). Рекомендация:
  прогнать `cargo test` перед публикацией, особенно 2 обновлённых unit-теста
  (timeline_columns_from_decomposition_series, timeline_columns_legacy_fallback).
- Svelte-чекер не видит текст внутри `title={` + JS template-literal (backtick без кавычек) —
  документированное ограничение лёгкого инструмента, не скрытое сужение.
- 4 спорные находки (по одной в HTML/Rust/Svelte, плюс Rust имеет 2) ждут решения Антона.
