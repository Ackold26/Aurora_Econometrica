# Аудит блока e8d50f1..324d2ba (INV-50 sweep + стандарт справки+PDF) — внешний аудитор, чистый контекст

Формат: severity | file:line | суть | воспроизводимый сценарий

---

Medium | tools/check_help_pdf_consistency.py:6223-6259 (весь набор проверок) | INV-50 — центральный инвариант блока — в HTML-справке НЕ покрыт никаким линтером. Новый линтер проверяет U+2014, копирайт/sipovich, версии, свежесть PDF, орфаны nav — но НЕ запрещённые «доверительный интервал»/«CI». lint_prompt_commands.py покрывает только промпты кабинета (New_AI_Agency/*), не src-tauri/help-econometrica/*.html. | Внести «доверительный интервал» в любой src-tauri/help-econometrica/*.html → `python tools/check_help_pdf_consistency.py` → exit 0 (не ловит). Усиливающий вектор: задокументированный долг #6 — перегенерация glossary.html через build_glossary.py из отставшего GLOSSARY_v2_1_0.md вернёт «доверительный интервал» в glossary.html, и это тоже пройдёт мимо гейта (поймает лишь косвенно sha-манифест как «PDF устарел», без указания на INV-50). CLAUDE.md правило 18: «каждый новый линтер — способен реально падать» соблюдено для U+2014/копирайта, но INV-50-измерение в HTML вообще не заведено.

Low | src/lib/program-help.js:5080-5081 | Путь логов в справке зашит только под облачный identifier `%LOCALAPPDATA%\com.aurora.econometrica\logs\`. Для локальной редакции (productName «Optimizer MMM Local», identifier `com.aurora.econometrica.local`, per-app изоляция путей — см. CLAUDE.md «Упаковка двух редакций») реальные логи лежат под `com.aurora.econometrica.local`, а текст указывает несуществующую для неё папку. | Пользователь локальной редакции читает справку → идёт в `%LOCALAPPDATA%\com.aurora.econometrica\logs\` → папки нет (его логи в `...econometrica.local\logs\`) → не может приложить логи к обращению в поддержку. Правка сама по себе — улучшение (старый текст «Документы\Aurora MMM Optimizer\Logs\» был неверен для ОБЕИХ редакций), но неполна для .local. Вне фокуса INV-50, справочный текст.

---

## Категории с проверкой боем — находок нет

- tools/build_help_pdf.py (968 строк): логика разобрана целиком. build_merged_html + split_install_body прогнаны боем на живом install.html (probe): appendix_body непустой (2173 б, содержит `<div class="container">` и «Удаление»), раздел «Удаление программы» вырезан из основного документа (count=0 в merged). Хрупкие точки по handoff (split регекс, strip_dark_theme_leaks, force_details_open, sanitize_error_codes_literals) не падают на реальном контенте. add_page_numbers индексы корректны (range(1, page_count-1) → обложки без номера). Path/subprocess без инъекций (аргументы фиксированы). НЕТ НАХОДОК.
- tools/check_help_pdf_consistency.py: прогнан боем → exit 0, 5 WARN (исторические версии v1.0.16/v1.1.0 — задокументированный компромисс). Реально падает на U+2014/копирайте (внутри своих измерений). package.json 2.4.0 = tauri.conf.json 2.4.0. 15 html = PAGE_ORDER = PAGES econ-nav.js (орфанов нет). PDF и logo-full.png на месте. sha-манифест актуален (манифест generated 01:19 UTC = PDF mtime; html mtime раньше). НЕТ НАХОДОК (кроме gap выше по INV-50).
- src-tauri/src/lib.rs save_help_pdf: path-traversal нет (имя фиксировано), download_dir/create_dir_all/коллизия(2)(3) обработаны, dev-fallback CARGO_MANIFEST_DIR не активируется в prod (resources glob «help-econometrica/*» в tauri.conf.json ВКЛЮЧАЕТ .pdf — проверено, prod-путь resource_dir найдёт PDF). НЕТ НАХОДОК.
- INV-50 замены рантайм (40 файлов) + справка: все замены — в отображаемом тексте/легендах/docstrings/тултипах/CSV-заголовках. Ключи (`roi_ci_low/high`, `ci_method`, `roi_ci_lower/upper`, `ate_ci_high`, `_ci_lo_eff`), CSS-классы (`.ci-bracket`, `.roi-ci`, `.col-ci`, `.att-ci`), переменные, идентификатор `ci_method='cate_mean_se_fallback'`, ключи словарей (`'type'`, `'severity'`, `'verdict_rule'`) — НЕ тронуты. Подстрока «90% HDI» сохранена (report.rs, builder.py). Тест forecast-chart-format.test.js синхронно обновлён (ДИ→Диапазон). Грепом подтверждено: ноль тестов (JS/Rust/Python) ассертят устаревший термин. НЕТ НАХОДОК.
- CommandPalette.svelte: replace/keywords-синоним — pg.keywords гарантированно определён (использовался и до правки), filtered защищён optional chaining. НЕТ НАХОДОК.
- Механические HTML-замены (U+2014, копирайт, шаг Планирования, версии): build_merged_html спарсил все 15 html без ошибок (merged_len=351091) → разметка валидна, битых ссылок в парсинге нет. econ-nav.js download-имя = имя save_help_pdf. НЕТ НАХОДОК.

Вердикт: готов к merge. Активных Critical/High/дефектов не найдено; новый код работает боем. Две находки — gap в защите инварианта (Medium) и неточность справки для .local (Low), обе не блокирующие.
