# Аудитор B — 4 LEGACY-команды ядра MMM-конвейера (2026-07-13)

Периметр: mmm-prepare / mmm-model / mmm-decomposition / mmm-optimize
(New_AI_Agency/econometrist/.claude/commands/). Статус: скрыты из UI-грида
(cabinet.rs:262-268), ручной ввод работает (backward compat) = клиентская
поверхность без витрины. inbox/exports живы в коде (session/manager.rs:152-208) —
механика команд НЕ мертва.

## mmm-prepare.md — зрелость 3 / эффективность 4

- [medium] mmm-prepare.md:5 — edge cases не заданы: пустой inbox, несколько csv/xlsx, нечитаемый файл — поведение не определено (рубрика прямо требует контракт на «пустой inbox, кривой файл») — «Прочитай csv/xlsx из inbox» без ветвлений.
- [medium] mmm-prepare.md:1,3,8,12,14,18 — длинное тире U+2014 ×6, байтово подтверждено (`rg '\x{2014}'` → 6 совпадений); запрещено рубрикой в промптах и выдаче; модель копирует стиль тире в клиентский текст.
- [medium] система↔команда: CLAUDE.md «Контракт данных» — «Файлы самостоятельно не ищи: ни %APPDATA%, ни рабочую папку — всё нужное уже в сообщении» — без оговорки для legacy; mmm-prepare.md:5 велит читать inbox. При ручном вводе модель получает оба указания одновременно.
- [medium] граница данных: нет ограждения от инъекций через содержимое файла/имена столбцов (текст данных = данные, не инструкции); CLAUDE.md кабинета тоже не закрывает.
- [low] mmm-prepare.md:12 — противоречивые пороги monthly: «52 недели / 18 месяцев (предупреждение), 104 недели / 24 месяца (рекомендация)» vs тут же «для monthly: минимум 24 месяца, рекомендация 36» — 18 vs 24 и 24 vs 36 конфликтуют.
- [low] mmm-prepare.md:24 — «напиши ровно одну фразу: "Все задачи выполнены."» — сентинел пайплайн-эпохи: Rust-код фразу не детектирует (grep по src-tauri пуст), фронт вычищает её как мусор (InsightsPanel.svelte:464, tier2-context.js:293 прямо запрещает писать); «ровно одну фразу» рискует подавить сам вердикт валидации.

Сильное: плотный чек-лист с числовыми порогами (ratio 4:1/10:1 согласован с SSOT-таблицей CLAUDE.md), формат выхода с листами xlsx, антиблокировка явная («НЕ спрашивай — подготовь автоматически»), язык русский.

## mmm-model.md — зрелость 4 / эффективность 4

- [high] mmm-model.md:22 — висячая ссылка: «параметры draws/chains/sampler — по правилам из секции "Windows: облегчённый MCMC" в CLAUDE.md», но секция ПЕРЕНЕСЕНА в LEGACY_COMMANDS.md:19 (в econometrist/CLAUDE.md её нет — grep подтверждает). При ручном запуске модель не найдёт правила и рискует запустить NUTS 4×2000 на Windows без компилятора — многочасовое зависание у клиента, ровно тот сценарий, против которого секция писалась («НЕ пытаться сначала NUTS»).
- [medium] em dash U+2014 ×10 байтово (строки 1,7-12,21,24,28…) — та же корпусная проблема тире.
- [medium] граница данных: авто-детекция KPI/медиа по ИМЕНАМ столбцов (строки 7-9) — имена столбцов из пользовательского файла напрямую управляют спецификацией модели, ограждения от инъекций нет.
- [low] LEGACY_COMMANDS.md:77 — дубль контракта с расхождением: жёсткие «2000 draws, 4 chains, target_accept=0.9» противоречат и собственной Windows-секции (LEGACY:19-32), и живой команде (mmm-model.md:22); дубль также не содержит backdoor-проверки (mmm-model.md:12), калибровки priors (:14), prior predictive check (:24) — два источника контракта без владельца.

Сильное: цепочка защищена (строка 5: нет валидации → провести самому = деградация лучше допустимой); методология образцовая — backdoor-отбор контролей, informative priors с тегом [CALIBRATED], prior predictive check с zero-stop, протокол divergences, horseshoe/R2D2 с честной оговоркой [ASSUMED], MQS + чек-лист. INV-50 соблюдён: допущения маркируются, диагностика обязательна.

## mmm-decomposition.md — зрелость 2 / эффективность 3

- [medium] mmm-decomposition.md:5 — «Загрузи результаты модели (из предыдущего /mmm-model)» — НЕТ поведения при отсутствии обученной модели: ни допустимой деградации «сначала выполните /mmm-model», ни fallback (рубрика прямо требует контракт на отсутствие предыдущего шага). Смягчено лишь общим правилом CLAUDE.md «нет нужного результата — скажи какого шага не хватает», но оно написано для блока данных консультационного потока, не для workspace-артефактов.
- [medium] mmm-decomposition.md:5 — источник артефактов не задан: mmm-model сохраняет «pickle + xlsx» (mmm-model.md:23) без фиксированного имени/пути pickle — decomposition не знает, ЧТО загружать; контракт стыка шагов разорван.
- [medium] mmm-decomposition.md:6-16 — нет шага «сгенерируй и выполни Python-скрипт», в отличие от соседей (mmm-model.md:15 → exports/scripts/mmm_model.py; mmm-optimize.md:12 → mmm_optimize.py). «Рассчитай» без инструмента исполнения → риск чисел «из головы» без расчёта — прямой INV-50-риск на клиентской поверхности.
- [medium] mmm-decomposition.md:9 — «ROI каждого канала с доверительными интервалами (credible intervals)» противоречит CLAUDE.md п.7: «называй интервал байесовской оценки правдоподобным диапазоном… не "доверительный интервал"» — система↔команда, клиентская формулировка.
- [medium] em dash U+2014 ×7 байтово (строки 1,7,10-12,17…).

Сильное: конкретный перечень расчётов с ожидаемым диапазоном baseline 60-80%, маркировка [MODELED] с CI (строка 18), формат xlsx с листами. Но контракт вокруг расчёта — самый слабый в четвёрке.

## mmm-optimize.md — зрелость 4 / эффективность 5

- [medium] mmm-optimize.md:3,5 — оптимизация «на основе обученной MMM-модели», но в workflow нет шага загрузки/проверки модели и нет деградации при её отсутствии (workflow стартует сразу с дефолтов и mROI) — тот же разрыв цепочки, что у decomposition.
- [medium] mmm-optimize.md:20,26 — клиентская таблица «Optimized Spend [CI 5–95%]» и вывод «[CI: Z1%-Z2%]» — англицизм CI вместо «правдоподобный диапазон» из CLAUDE.md п.7 (та же система↔команда, что decomposition:9); при этом строка 22 сама же даёт правильный образец «доля A: 38% [27–46%]».
- [medium] em dash U+2014 ×7 байтово (строки 1,11,17,22,27…).

Сильное: лучшая методологическая плотность периметра — оптимум по K≥200 draws из posterior с агрегацией ПО СЭМПЛАМ (не по средним параметров), CI на доли, пометка [ЭКСТРАПОЛЯЦИЯ] строго при выходе за диапазон, строка чувствительности к priors («оптимум определён допущением, не данными»), дефолты [ASSUMED] без вопросов, готовые образцы формулировок. INV-50 фактически образцово.

## Агрегат периметра

Средняя зрелость 3.25 / средняя эффективность 4.0. Главный риск: разрыв цепочки
model→decomposition→optimize (нет контракта на отсутствие предыдущего шага + нет
пути к артефактам + у decomposition нет шага исполнения кода → числа без расчёта)
и висячая ссылка mmm-model на MCMC-правила, уехавшие в LEGACY_COMMANDS.md.
