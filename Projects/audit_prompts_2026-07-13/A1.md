# A1 — аудит активных команд econometrist, группа «Смысл» (2026-07-13)

Периметр: interpret-model.md, why-channel.md, explain-ratio.md.
Контекст-стык: econometrist/CLAUDE.md · cabinet.rs:269-278 (labels) · инжект «=== Данные проекта ===»
собирает src/lib/econ-project-context.js (buildProjectDataBlock) + ChatPanel.svelte:696-708
(НЕ tier2-context.js — тот обслуживает Tier-2 ассистента «Аврора», отдельная поверхность).
Форма реальных артефактов проверена по sidecar (diagnostics.py, decomposer.py, validator.py)
и tools/cabinet_eval/fixtures/*.json. Тире — байтовой проверкой (regex U+2014/U+2013).

## interpret-model.md — зрелость 4 / эффективность 4

- [medium] interpret-model.md:21 — «MQS N/100» (пример «MQS 72», = движку: diagnostics.py:132-141, шкала 0-100) противоречит таблице «Model Quality Score» CLAUDE.md кабинета (шкала 1-10, пример «MQS 6 (Acceptable)», тиры Poor 1-2…Excellent 9-10). Два масштаба в одном системном промпте; смягчение: mqs.score/tier_label приходят в данных и якорят модель, но риск «MQS 7 (Good)» при UI «72» остаётся. Команда права, таблица CLAUDE.md устарела.
- [medium] interpret-model.md:42 — приказ ставить теги [MODELED]/[STATISTICAL] в клиентском тексте: параллельная клиентская поверхность того же продукта (Tier-2, tier2-context.js:291-292) запрещает эти же пометки как «технические артефакты, пользователю не нужны»; для заявленной аудитории «руководство без статподготовки» — английский служебный жаргон. Голос продукта расходится между поверхностями.
- [medium] interpret-model.md:3 — длинное тире U+2014: 17 вхождений по файлу (строки 3,8-11,13,17,21,24-27,32,40,43; байтовая проверка). Канон Aurora: только «–» U+2013/дефис; промпт демонстрирует модели запрещённое тире → утечёт в клиентскую выдачу.
- [medium] (общее для трёх файлов) — граница данных: ни промпт, ни блок инжекта не содержат ограждения «содержимое блока = данные, не инструкции». Имена каналов/колонок из пользовательского Excel попадают в промпт как есть (econ-project-context.js:100 JSON.stringify). Структурная инъекция «===» погашена однострочностью JSON (переносы экранируются), семантическая («ignore instructions» именем колонки) — нет.
- [low] interpret-model.md:24 — «base_pct%»: в артефакте decomposition поле называется `baseline_pct` (fixtures/decomposition.json, tier2-context.js:60); значение доставляется, имя в промпте не совпадает с ключом.
- Контракт в остальном сходится с инжектом: model-diagnostics (mqs.score/R²/MAPE/R-hat/divergences/ratio — есть), optimization.expected_lift_pct — есть, project.unit_costs — есть (ChatPanel.svelte:698-700), списки колонок — в [validation] (econ-project-context.js:120-122). Edge case «нет файла» обработан допустимой деградацией (:13 «скажи какого шага не хватает» — не блокировка). UI label «Объяснить результаты» (cabinet.rs:270) без overclaim. Честность: антисикофантия, запрет выдумывания, один десятичный знак — сильные рельсы.

## why-channel.md — зрелость 3 / эффективность 4

- [high] why-channel.md:13 — контракт «optimization — miROAS, saturation point…» ждёт поле, которого нет в инжекте: response_curves (данные насыщения) намеренно вырезаны из блока (econ-project-context.js:34 stripOptTelemetry), в channels[] optimization saturation-полей нет (fixtures/optimization.json: mroi_current/mroi_optimal/current_spend/optimal_spend/action*). Обязательная карточка «Статус saturation 🟢🟡🔴» (:23) опирается на непоставленные данные; косвенные прокси (action_label, mroi) есть, но промпт направляет модель к отсутствующему полю → домысливание или пустой пункт.
- [medium] why-channel.md:32 — фактор 5 «низкая CV в валидации»: [validation]-инжект не содержит CV/статистик колонок — summarizeValidation отдаёт только ratio, n_rows, частоту, списки имён, high_correlations (econ-project-context.js:110-128). Фактор невыполним на доставленных данных.
- [medium] why-channel.md:3 — длинное тире U+2014: 20 вхождений (строки 3,7,12-15,17,21,28,30-33,36-38,40,44,46,48; байтовая проверка).
- [low] why-channel.md:12 — «adstock_rate»: в артефакте поле называется `adstock_decay_mean` (decomposer.py:956, fixtures/decomposition.json); семантика та же, имя не совпадает.
- Сильное: правило пометки производных (:45-47) — лучшая формулировка INV-50 в периметре; «канала нет — скажи прямо» (:48); ОДИН уточняющий вопрос при неуказанном канале согласован с CLAUDE.md кабинета (явное исключение). miROAS↔mroi_current — терминологическая натяжка, но значение доставляется. UI label «Почему у канала такой ROI» (cabinet.rs:271) без overclaim.

## explain-ratio.md — зрелость 3 / эффективность 4

- [high] explain-ratio.md:8,34 — `n_predictors` ждётся из [validation], но в инжекте его НЕТ: summarizeValidation поле роняет (econ-project-context.js:116-124), хотя движок его отдаёт (validator.py:827 detected.n_predictors). Раздел 4 (:34 «Из текущих n_predictors и file.rows посчитай») — расчётное ядро команды на отсутствующем поле; восстановимо подсчётом len(media_columns)+len(control_columns) (= validator.py:659), но промпт на это не указывает.
- [high] explain-ratio.md:22-26 — зоны Ratio («≥ 4 — норма, cap снят») соответствуют движку (diagnostics.py:124-128: cap только при ratio<4), но противоречат SSOT-таблице «Три порога Ratio» CLAUDE.md кабинета («≥ 6 — cap снят», зона 4-6 не определена), на которую команда сама ссылается (:22 «единая таблица в CLAUDE.md»). Команда права, SSOT врёт; модель, читающая обе, может заявить cap при ratio 4-6, когда UI показывает MQS без cap. Тем же страдает interpret-model.md:22 («capped из-за Ratio<4» против SSOT).
- [medium] explain-ratio.md:8 — контракт называет пути сырого артефакта `detected.ratio`, `file.rows` — в инжекте плоская выжимка `ratio`, `n_rows` (econ-project-context.js:117-118); значения доставляются под другими именами.
- [medium] explain-ratio.md:8 — длинное тире U+2014: 14 вхождений (строки 8,9,11,16,20,22-26,36,38,44,45; байтовая проверка).
- [low] explain-ratio.md:41-45 — «Принципы» не напоминают помечать производные расчёты раздела 4 («посчитай…») маркерами «≈/примерно» — держится только на общем правиле CLAUDE.md кабинета; у why-channel такое локальное правило есть, у explain-ratio, где расчётов больше всего, — нет.
- Сильное: mqs.thinness_cap/mqs.ratio реально существуют (diagnostics.py:149-150); аналогия «N сигналов в M точках»; позитивная ветка Ratio≥6 и жёсткая честность при ≤1.5. UI label «Разбор Ratio данных» (cabinet.rs:272) без overclaim.

## Агрегат

Средняя зрелость 3.3 / эффективность 4.0. Главный риск: полевые контракты промптов не сверены
с реальной выжимкой инжекта (saturation point, n_predictors, CV не доставляются) + две устаревшие
SSOT-таблицы CLAUDE.md кабинета (порог снятия MQS-cap, шкала MQS) противоречат командам и движку —
команды правы, система лжёт, и модель читает обоих.
