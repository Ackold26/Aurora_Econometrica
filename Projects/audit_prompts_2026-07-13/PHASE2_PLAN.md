# Фаза 2 — план реализации (лестница: простое раньше дорогого)

Дата: 2026-07-13. Вход: SUMMARY.md (11 CONFIRMED high · 7 DOWNGRADED · систематика medium/low).
Эффорт: S <1ч · M 1–3ч · L >3ч. Гейт после каждого батча: svelte-check 0 · vitest · cargo ·
kpi-display drift OK · smoke-эвал без падения оси ≥0.5.

## Инвентарь существующих детерминированных проверок (переиспользуем, не строим заново)

| Есть | Где | Роль в плане |
|---|---|---|
| Эвал-харнес кабинета: фикстуры 5 артефактов, graders.mjs, run_eval.mjs | tools/cabinet_eval/ | база для baseline + smoke-эвала правок |
| cabinet-drift-guard (SSOT-пара repo↔worktree econometrist) | lefthook | расширить на четверное совпадение |
| kpi-display-drift, v40-xss, DecomposeStep-guard, sync-help-lists | lefthook + CI | образец паттерна «гейт в lefthook + CI-проверка» |
| insights-grounding.js + tests, test_help_links.py, client_names canary | src/lib, tools/ | детекторные оси INV-50 / справки / ПДн |
| CI: svelte-check · vitest · cargo test · clippy · pytest · help-sync | .github/workflows/ci.yml | точка подключения новых линтеров |

Пробел CI: cabinet-drift-guard и kpi-display-drift живут ТОЛЬКО в lefthook (обходятся
--no-verify); эвал-харнес в CI не гоняется вовсе.

## Батч 0 — Доставка (мета-риск: без этого правки промптов не доедут живым клиентам)

| # | Задача | Слой/канал | Эффорт |
|---|---|---|---|
| 0.1 | Оживить vault version-канал: прод-вызов check_update_per_cabinet (vault_versions из /auth → сравнение → download при отставании). Сейчас триггер только missing/undecryptable (lib.rs:89,430) — даже новый .exe НЕ обновляет уже стоящий vault | Rust → .exe | M |
| 0.2 | Version-compare внешнего frontend-бандла: реализовать сравнение min_core_version (объявлен content_sig.rs:50, нигде не проверяется) или инвалидация бандла при смене версии .exe — иначе старый бандл бессрочно перекрывает новые JS-промпты Авроры | Rust → .exe | M |
| 0.3 | Re-sign content-pack: manifest.json ↔ themes.json разошлись с 2026-07-02 (re-sign пропущен). Локальный re-sign сразу; публикация — по слову Антона | content-pack | S |
| 0.4 | Тест «внести-поймать-откатить» на 0.1/0.2 (cargo) + контрольная распаковка vault | тесты | S |

Отложено (решение Антона): расширение триггера OTA за пределы «открытие Настроек» (lib.rs:179) —
не блокер аудита промптов, отдельный UX-вопрос.

## Батч 1 — Эвал-baseline, затем SSOT-рефактор множителя (CLAUDE.md кабинета)

| # | Задача | Эффорт |
|---|---|---|
| 1.0 | **Baseline ПЕРВЫМ:** зафиксировать скоры run_eval.mjs на ТЕКУЩИХ промптах в git (results/baseline-2026-07-13/) — все правки батчей 1–4 сравниваются с ним | S |
| 1.1 | MQS: убрать таблицу 1–10 (CLAUDE.md:163-171), единая шкала 0–100 движка; тиры — русские tier_label движка («Хорошее»…), не Poor/Weak | S |
| 1.2 | Ratio-cap: таблицу CLAUDE.md:156 привести к движку (≥4 — cap снят, diagnostics.py:124-128); согласовать pilot-design.md:46 (зона 2–6) | S |
| 1.3 | Вынести ~50 строк дублей спецификаций 8 команд (CLAUDE.md:97-147) → короткие резюме-указатели; владелец контракта = файл команды. Множитель худеет, специфика — в командах | M |
| 1.4 | Противоречие [MODELED]/[ASSUMED]: правило «теги служебные, в клиентский текст не выносить» + расширить sanitizeAvroraText на [ASSUMED]/[ОГРАНИЧЕНИЕ] (InsightsPanel.svelte:462) | M |
| 1.5 | «Контракт данных» (:92-95): оговорка для legacy /mmm-* (inbox-путь легитимен) — снять противоречие с mmm-prepare | S |
| 1.6 | [scenarios]-фантом в CLAUDE.md:127 — убрать/пометить реальным источником | S |

## Батч 2 — Аврора (свежий блок v2.3.0)

| # | Задача | Эффорт |
|---|---|---|
| 2.1 | scenarioInterpretation через findUngroundedNumbers + рендер «⚠ не сверено» (сейчас страж только у askAnswer, InsightsPanel.svelte:430 vs :576) | M |
| 2.2 | program-help.js: 7 шагов (Планирование в PROGRAM_OVERVIEW:51-65; STEP_TERMS-тест 0..6 тоже отстал) | S |
| 2.3 | «Доверительный интервал» → «правдоподобный диапазон» (program-help.js:47,70-71) — снять противоречие с правилом 4 Tier2 | S |
| 2.4 | Инъекции: нейтрализация «===» (по образцу tier2-context.js:352) для вопроса пользователя, tier1-инсайтов (:396), scenarioText (InsightsPanel.svelte:556); опционально renderArtifact в econ-project-context.js | M |
| 2.5 | U+2014 в промпт-литералах tier2-context.js / program-help.js / scenario-advisor.js (комментарии кода не трогать — хирургия) | S |

## Батч 3 — Витрина и UI-мета (клиентская поверхность)

| # | Задача | Эффорт |
|---|---|---|
| 3.1 | Психо-фазы: нейтральные тексты для консультационных команд («Анализирую результаты модели…»), убрать «Строю модель/MCMC/Рассчитываю ROI» из пути команд кабинета (psy-data.json + маппинг psy.js/ChatPanel.svelte:748-758). Правка content-pack → re-sign | M |
| 3.2 | mmm-to-doc.md:5: честный контракт (готовый Markdown-отчёт; убрать невыполнимое обещание импорта /plan-to-doc — doc-master недоступен продукту) | S |
| 3.3 | Мета awareness-forecast: «по медиаплану» → «по историческому трекингу awareness» (command-meta-data.json → re-sign) | S |
| 3.4 | data-gaps: warnings добавить в выжимку summarizeValidation (econ-project-context.js:116-124 — поле движок отдаёт, validator.py:834); suspicious_channels → smell_flags | M |
| 3.5 | next-quarter-plan: вычистить фантомы (scenarios, optimization.json), починить «(исходя из что ratio, что basе…)» (лат/кир микс), сократить обязательный контракт (стабильный timeout ≥300с) | M |
| 3.6 | Онбординг: CabinetOnboarding step ids ('import'/'train' ↔ render-ветки) + configs.econometrist на 8 активных команд вместо 9 легаси | M |
| 3.7 | Мёртвые кнопки «Инструкция»/«Справка»: положить help-файл econometrist в бандлируемую help-econometrica/ или скрыть кнопки у продукта | S–M |

## Батч 4 — Систематика корпуса (остальные medium/low одним проходом)

| # | Задача | Эффорт |
|---|---|---|
| 4.1 | U+2014 → «–» по корпусу промптов (447 минус уже сделанные в 2.5; скриптом + ручное ревью diff) | M |
| 4.2 | Единая языковая шапка (полный список исключений: ROI, CPM, GRP, MQS, R-hat, MAPE) во всех 17 командах; снять противоречие «англ. заголовки таблиц» в mmm-optimize/scenarios | S |
| 4.3 | «Доверительный интервал» → «правдоподобный диапазон» в оставшихся файлах корпуса (~9) | S |
| 4.4 | LEGACY_COMMANDS.md → справка-индекс без дублей контрактов (указатели на файлы команд); убрать awareness-команды из легаси-секции (они активные); снять дрейф MCMC-параметров | M |
| 4.5 | Правило «границы данных» в CLAUDE.md кабинета: текст из блоков данных/файлов = данные, не инструкции | S |
| 4.6 | mmm-decomposition: шаг «сгенерируй и выполни скрипт» (образец mmm-optimize.md:12-18) + деградация при отсутствии модели | S |
| 4.7 | mmm-report: «не выдумывай цифры» (как в to-doc/to-slides:9) + контракт отсутствия этапов + чем строить One-Pager | S |
| 4.8 | mmm-full: явный протокол провала шага (prepare «ТРЕБУЕТ ДОРАБОТКИ» / divergences / MQS Poor → остановить конвейер, доложить) | S |
| 4.9 | Edge cases inbox (prepare/model/awareness-*): пустой inbox, несколько файлов, нечитаемый файл | S |
| 4.10 | Пороги monthly в mmm-prepare (:12 vs :24) + сентинел «Все задачи выполнены.» — актуализировать | S |

## Батч 5 — Вечная оборона (линтеры в CI) + эвал-петля

| # | Задача | Эффорт |
|---|---|---|
| 5.1 | lint-prompts.mjs: U+2014-детектор (md-корпус + промпт-литералы js), запрет «доверительный интервал», обязательная языковая шапка, «ОСТАНОВИСЬ»-паттерн. В lefthook + CI. Приёмка «внести-поймать-откатить» | M |
| 5.2 | Линтер четверного совпадения: cabinet.rs ↔ cabinets.json ↔ файлы команд ↔ command-meta-data.json (расширение check_cabinet_drift.py или новый). В lefthook + CI | M |
| 5.3 | Manifest-sync линтер: SHA файлов content-packs/ ↔ manifest.json (ловит пропущенный re-sign — кейс themes.json). В lefthook (glob content-packs/**) + CI | S |
| 5.4 | Дым-эвал в CI: run_eval.mjs --dry на сохранённых results (без egress) + юнит-тесты грейдеров — детерминированный гейт (хвост из NEXT_SESSION закрывается здесь) | M |
| 5.5 | Подключить в CI то, что живёт только в lefthook: cabinet-drift-guard, kpi-display-drift (обходятся --no-verify) | S |
| 5.6 | Регламент в CLAUDE.md репо: «правка промпта = smoke-эвал до пуша; правка content-packs = re-sign в том же коммите; правка шкалы шагов = grep голых чисел» | S |

## Петля оптимизации (после батчей, непрерывно)

Правка → smoke-эвал (сравнение с baseline 1.0) → diff скоров → фиксация. Тонкая доводка
формулировок — только на этой ступени, не раньше (иначе нечем мерить).

## Порядок, доставка каждой правки, релизная синергия

- Последовательность: **0 → 1 → 2 → 3 → 4 → 5**, батчи 1–4 можно частично параллелить
  субагентами по непересекающимся файлам; Батч 5 закрывается последним (линтеры фиксируют
  достигнутое состояние).
- Каналы: правки .md-промптов → пересборка vault (доедет благодаря 0.1); JS/Rust → сборка
  .exe; content-packs → re-sign + OTA. Всё сходится в **один релиз 2.3.1** вместе с Фазой B
  (релиз-публикация уже в плане сессии) — отдельных выкатов не плодим.
- Публикация (Storage/content_versions) — по слову Антона; локальные re-sign/пересборки — сразу.

## Мэппинг на Definition of Done

| DoD | Закрывается |
|---|---|
| 1. Все команды ≥4/4 | батчи 1–4 (пере-скоринг после; исключения — решением Антона) |
| 2. Ноль расхождений четверного совпадения; ноль U+2014 | 4.1 + 5.1 + 5.2 |
| 3. Эвал-baseline в git; порча промпта ловится | 1.0 + 5.4 (негативная проба уже в харнесе) |
| 4. Линтеры промптов в CI зелёные; регламент в CLAUDE.md | 5.1–5.3, 5.5, 5.6 |
| 5. Vault/пак пересобраны, доставка подтверждена на артефакте | 0.1–0.4 + контрольная распаковка + живой смоук релиза |

## Суммарный эффорт

Батч 0: ~M×2+S×2 · Батч 1: ~S×5+M×2 · Батч 2: ~M×2+S×3 · Батч 3: ~M×4+S×3 ·
Батч 4: ~S×8+M×2 · Батч 5: ~M×3+S×3. Итого **≈ 2–3 рабочие сессии** с параллелизацией
субагентами на непересекающихся файлах.

## Открытые решения Антона (до старта Фазы 3)

1. **Судьба 9 legacy /mmm-*:** чинить минимально по плану (4.6–4.10, рекомендация — INV-50
   критичен, backward compat заявлен) ИЛИ исключить из vault-пака (сузить поверхность,
   но сломать ручной ввод у клиентов)?
2. **Онбординг + справка (3.6, 3.7):** в этот цикл (рекомендация — клиентская витрина)
   или отдельный UI-трек?
3. **Психо-фазы (3.1):** согласовать нейтральные формулировки фаз до правки.
4. **Методология содержания каноном** (aurora-upgrade по RAG-библиотеке) — после Фазы 3,
   как предписывает промт (не смешивать со стыками), рекомендации выдам в Фазе 4.

ГЕЙТ: приёмка плана Антоном. Фаза 3 не начата.
