# SKEPTIC S5 — адверсариальная перепроверка находок аудита промптов (legacy /mmm-*)

Дата: 2026-07-13. Метод: личное чтение файлов, поиск опровержения. Периметр: legacy-команды скрыты из UI (cabinet.rs:262-268), работают ручным вводом; Claude CLI запускается с `--dangerously-skip-permissions` (claude.rs:185-190) — инструментальных ограничений у сессии кабинета НЕТ.

---

## Находка 1 — mmm-full без протокола провала шага → DOWNGRADED

**Факт подтверждён лично:** mmm-full.md:3 «Все шаги подряд, без остановок», :36 «Между шагами НЕ останавливайся»; mmm-prepare.md:18 вердикт «ГОТОВ К МОДЕЛИРОВАНИЮ / ТРЕБУЕТ ДОРАБОТКИ». Явной ветки останова/эскалации НЕТ нигде: ни в mmm-full.md, ни в CLAUDE.md кабинета (прочитан целиком), ни в LEGACY_COMMANDS.md (прочитан целиком; /mmm-model шаг 5 — диагностика без abort-ветки).

**Почему severity ниже high (три компенсатора, найдены лично):**
1. mmm-full.md:7 — «Если нужна подготовка (латиница, сокращение переменных) — сделай автоматически»: типовые причины «ТРЕБУЕТ ДОРАБОТКИ» имеют ветку авто-исправления прямо в оркестраторе.
2. CLAUDE.md кабинета — «Принципы мышления (применять ко всем запросам)», п. 3: «Всегда давай результат с оговорками, а не отказывай» — продолжение с маркировкой вместо останова является ОСОЗНАННЫМ дизайном системного промпта, а не пробелом; п. 2 (антисикофантия): «результаты ненадёжны для принятия решений» обязано прозвучать.
3. Неявный протокол эскалации существует в форме обязательной маркировки: mmm-full.md:14 «Проверь диагностику, рассчитай MQS» + LEGACY_COMMANDS.md:155-156 (финальный отчёт обязан включать «MQS + Diagnostics Checklist» и рекомендации с уровнями [ВЫСОКАЯ]/[СРЕДНЯЯ]/[НИЗКАЯ]) + MQS-шкала CLAUDE.md: тир Poor = «Ненадёжна. Не использовать для решений». Несходимость MCMC (R-hat>1.2) механически даёт Poor → вердикт «не использовать» доезжает до клиента в самом отчёте.

**Что остаётся верным:** жёсткой ветки «остановись и не строй шаги 4-5» нет; рекомендации на слабой модели будут ПОСТРОЕНЫ (пусть и с маркировкой). Ядро находки — правда, следствие («клиент получит рекомендации без сигнала невалидности») — преувеличено. Вердикт: **DOWNGRADED (high → medium)**.

---

## Находка 2 — mmm-scenarios без обязательного Python-скрипта → DOWNGRADED

**Факт подтверждён лично:** mmm-scenarios.md:10-14 (шаг 2 — прогнозы, ROI, CI) — слов «сгенерируй скрипт» / «выполни через Bash» нет; контраст с mmm-optimize.md:12-18 (шаг 3 «Сгенерируй Python-скрипт exports/scripts/mmm_optimize.py», шаг 4 «Выполни скрипт через Bash») реален.

**Почему severity ниже high (опровергающие рычаги):**
1. mmm-scenarios.md:22 (шаг 4.1) требует «прогони оптимизацию на 2 контрастных спецификациях (geometric vs Weibull adstock)» — прогон оптимизации без исполнения кода невозможен; сам промпт вынуждает исполнительный путь, «на глаз» весь блок не закрыть.
2. LEGACY_COMMANDS.md:34 — общий регламент эпохи: «Все Python-скрипты сохраняются в exports/scripts/ для воспроизводимости»; CLAUDE.md кабинета («Работа с файлами»): «legacy-расчёты сохраняют… Python-скрипты exports/scripts/» — скриптовый расчёт закреплён как норма контура, в котором выполняется команда.
3. Гипотеза аудитора о «расчёте из готовых чисел» частично подтверждена: ре-фит MCMC действительно НЕ нужен — /mmm-model шаг 7 сохраняет артефакты модели в exports/ (LEGACY_COMMANDS.md:85), сценарии = прямой прогон сохранённых кривых отклика на новые сплиты. Но и такой прямой прогон — численный расчёт (Hill/adstock/CI), который LLM обязана исполнить кодом, а промпт этого явно не требует для шагов 2-3.

**Что остаётся верным:** для центральных чисел (прогноз продаж, ROI, CI по сценариям) явной обязанности «скрипт+исполнение» нет → окно для оценки «на глаз» (INV-50) существует, особенно при ручном одиночном вызове без предыстории. Вердикт: **DOWNGRADED (high → medium)** — пробел реален, но исполнительный путь навязан п. 4.1 и регламентом эпохи, ре-фит не нужен.

---

## Находка 3 — mmm-to-doc: ложный контракт с /plan-to-doc → CONFIRMED

**Перепроверено лично, оба контракта целиком:**
- mmm-to-doc.md:5 обещает Markdown-отчёт, «готовый для импорта в Aurora AI Docu-Master через команду /plan-to-doc».
- plan-to-doc.md (New_AI_Agency/doc-master/.claude/commands/, прочитан полностью, 111 строк): Шаг 1 (строки 8-12) требует «Медиаплан (.xlsx)» + «Шаблон приложения (.doc/.docx)»; вся дальнейшая логика (шаги 2-6) — извлечение площадок/бюджетов/АВ/НДС из ЛИСТОВ xlsx. Гибкость есть только по шаблону («используй стандартную структуру») и по маппингу листов xlsx («спроси маппинг», строка 19) — ветки приёма Markdown/MMM-отчёта НЕТ. MMM-отчёт не является медиапланом (нет строк площадок, объёмов, построчных бюджетов) — импортировать нечего и некуда. Доку-мастер CLAUDE.md подтверждает: «/plan-to-doc — Извлекает данные из xlsx-медиаплана».
- Отягчающее: в продукте Econometrica кабинет doc-master НЕДОСТИЖИМ — cabinet.rs:121 `"econometrica" => Some(&["econometrist"])` (тест cabinet.rs:613-616 фиксирует «только econometrist»). Обещание ссылается на команду, которую клиент этого продукта не может выполнить в принципе.

**Влияние недостижимости doc-master на severity:** практический вред ограничен (клиент не сгенерирует неверный документ — он вообще не доберётся до /plan-to-doc), но само обещание из ложного становится дважды ложным: неверен и формат, и существование адресата. Ложный контракт в клиентском промпте — подтверждён. Вердикт: **CONFIRMED** (mmm-to-doc.md:5 vs plan-to-doc.md:8-12 + cabinet.rs:121).

---

## Находка 4 — mmm-to-slides: PPTX «без исполнительного пути» → REFUTED (ядро), остаточная оговорка по брендингу

**Проверка утверждений аудитора:** pptx-скилл в CLAUDE.md кабинета не упомянут (верно — раздел «Работа с файлами» перечисляет только xlsx/docx/pdf); python-pptx нет в pip-строке LEGACY_COMMANDS.md:13-14 (верно); шаблона/брендбука в vault нет (верно — состав кабинета: CLAUDE.md, LEGACY_COMMANDS.md, .claude/commands/*.md, БЕЗ .claude/skills и без шаблонов); PPTX делает движок (верно, cabinet.rs:262-263).

**Но вывод «нет реального пути создать PPTX» ОПРОВЕРГНУТ — путь есть и он тривиален:**
1. **python-pptx гарантированно установлен на машине клиента Econometrica** — это зависимость СОБСТВЕННОГО движка программы: `sidecar/econometrica/requirements.txt`, секция Export: `python-pptx>=0.6.21`; sidecar поставляется исходниками в resources (tauri.conf.json:33-37) и исполняется системным python (econ_sidecar.rs:313 `let python = "python"`).
2. **Приложение само доустанавливает python-pptx**, если его нет: pptx_processor.rs:21-23 (`REQUIRED_PACKAGES ["pptx","docx"]` / `PIP_PACKAGES ["python-pptx","python-docx"]`) + install_packages() pptx_processor.rs:78-103 (`python -m pip install --user python-pptx python-docx`).
3. **Сессия кабинета не ограничена в инструментах:** claude.rs:185-190 — CLI запускается с `--dangerously-skip-permissions`; Bash доступен (на нём построены все legacy-команды: mmm-optimize.md:18 «Выполни скрипт через Bash»). Тот же системный python с установленным python-pptx виден Bash'у сессии.
   → Паттерн «сгенерируй скрипт → выполни через Bash», являющийся нормой legacy-контура, полностью применим: mmm-to-slides исполним на любой рабочей инсталляции Econometrica.
4. Дополнительно (мягкий рычаг): скиллы xlsx/docx/pdf, на которые опирается CLAUDE.md кабинета, в vault не поставляются → они приходят из комплекта самого Claude CLI; тот же комплект Anthropic-скиллов включает pptx. Аудитор проверял vault, но не источник скиллов, которым кабинет УЖЕ пользуется.

**Что остаётся верным (не тянет на high):** «брендированная тёмная тема» (mmm-to-slides.md:20) не обеспечена активами — брендбука/шаблона в vault нет, тема будет импровизацией LLM (косметический риск качества, не риск ложных чисел: источник данных в mmm-to-slides.md:9 честный — блок проекта / exports / отказ без выдумывания цифр). Вердикт: **REFUTED** в формулировке «нет исполнительного пути»; остаточная низко-severity находка «брендинг не обеспечен активами».

---

## Сводка

| # | Находка | Вердикт |
|---|---------|---------|
| 1 | mmm-full без протокола провала | DOWNGRADED (high→medium): ветки останова нет, но авто-fix mmm-full.md:7 + обязательная MQS-маркировка (LEGACY_COMMANDS.md:155, шкала Poor «не использовать») = неявная эскалация в отчёте |
| 2 | mmm-scenarios без скрипта | DOWNGRADED (high→medium): пробел есть, но п.4.1 (mmm-scenarios.md:22) вынуждает исполнение кода, регламент LEGACY_COMMANDS.md:34 предписывает скрипты, ре-фит не нужен |
| 3 | mmm-to-doc ложный контракт | CONFIRMED: plan-to-doc.md:8-12 принимает только xlsx-медиаплан+шаблон; doc-master недостижим в продукте (cabinet.rs:121) — обещание дважды невыполнимо |
| 4 | mmm-to-slides без пути к PPTX | REFUTED: python-pptx — зависимость движка (sidecar requirements.txt) + авто-установка pptx_processor.rs:78-103 + Bash без ограничений (claude.rs:189); не обеспечен только брендинг |
