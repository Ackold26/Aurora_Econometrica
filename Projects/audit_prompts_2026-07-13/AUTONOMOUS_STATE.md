# 🔴 АВТОНОМНЫЙ РЕЖИМ — аудит и усиление промптов кабинета econometrist (Optimizer MMM v2.3.0)

> **ТОЧКА ВХОДА ПОСЛЕ КОМПРЕССИИ.** Читать ПЕРВЫМ, продолжать БЕЗ переспроса с раздела «NEXT».
> cwd = `D:\Docs\Aurora_Ai\Dev\Aurora_Econometrica_v230` (worktree ветки `feat/econ-v2.3.0`).
> Создан 2026-07-13. Мандат Антона: максимальная автономность, вести до конца.

## Мандат и правила автономии (Антон, 2026-07-13)
- **Автономна на исполнении И тактике.** Развилки продумывать глубоко, выбирать уверенный вариант.
- **Вопрос Антону ТОЛЬКО если:** принципиальный/концептуальный (не технический) + высокая важность
  + неочевиден даже после глубокого обдумывания. Технические/тактические — решать самой.
- **Делегирование:** многоходовую реализацию выносить в субагентов на модели попроще (Sonnet для
  кода Rust/JS, Haiku только для чисто механического под жёсткой спекой). Я = оркестратор:
  спека → приёмка (проверяю diff+гейты лично, ~40% находок субагентов FP) → принять/на доработку.
- **Auto-commit local** после приёмки каждого батча (не «commit-and-fix-later»: аудит до коммита).
  **Push к origin — ТОЛЬКО по слову Антона** (+ diff на показ). Публикация Storage/Supabase — тоже.
- **Гейт после каждого батча:** svelte-check 0 · vitest · `CARGO_TARGET_DIR="D:/cargo-targets/ai-agency"
  cargo test` · kpi-display drift OK · (для промптов) smoke-эвал без падения оси ≥0.5.
- Хирургические правки; клиентский текст — короткое тире «–», без англицизмов; INV-50 честность чисел.

## Что уже сделано (Фазы 0-2, НЕ переделывать)
- **Фаза 0** — разведка: периметр = кабинет econometrist (единственный видимый продукту).
  18 пунктов: 8 активных команд грида + 9 legacy /mmm-* (скрыты, но ручной ввод работает) + Аврора.
- **Фаза 1** — аудит 8 субагентами + 6 скептиков. Итог: `SUMMARY.md` (в этой папке).
  Critical 0 · **11 high CONFIRMED** · 7 DOWNGRADED · 1 REFUTED. Оценки: активные 3.4/3.9,
  legacy 2.9/3.7, Аврора+система 3.4/4.0, доставка 3.2, стиль 3.25, UI 3.75. Durable: A1,A2,B,C,D,E,F,G
  + SKEPTIC_S1..S6 (в этой папке).
- **Фаза 2** — план реализации: `PHASE2_PLAN.md` (в этой папке). ПРИНЯТ Антоном.
- **Решения Антона по развилкам:** (1) 9 legacy /mmm-* — ЧИНИТЬ; (2) онбординг+справка — В ЭТОТ
  ЦИКЛ; (3) психо-формулировки — предложить нейтральные самой, показать (не блокировать вопросом,
  раз включён max-autonomy); (4) канон RAG-библиотеки (aurora-upgrade) — Фаза 4, учитывать уже
  проиндексированное/использованное ранее, искать методологические ГЭПЫ.

## План Фазы 3 — 6 батчей (полная детализация → PHASE2_PLAN.md)
- **Батч 0 — доставка (Rust):** оживить vault version-канал (0.1) + frontend version-compare (0.2)
  + re-sign паков (0.3, ключ вне репо — уточнить инструмент) + юнит-тесты (0.4). Серверная часть
  vault_versions (Supabase Edge) = БЛОКЕР вне репо, пометить, не чинить.
- **Батч 1 — множитель CLAUDE.md кабинета:** 1.0 эвал-baseline в git → 1.1 MQS единая 0-100 →
  1.2 Ratio-cap ≥4 (по движку) → 1.3 вынести дубли спецификаций команд → 1.4 теги [MODELED] →
  1.5 контракт данных для legacy → 1.6 [scenarios]-фантом.
- **Батч 2 — Аврора (src/lib/*.js):** 2.1 страж INV-50 на scenarioInterpretation → 2.2 справка
  7 шагов → 2.3 «доверительный интервал»→«правдоподобный диапазон» → 2.4 «===»-нейтрализация
  инъекций → 2.5 U+2014 в промпт-литералах.
- **Батч 3 — витрина:** 3.1 психо-фазы нейтральные → 3.2 честный mmm-to-doc → 3.3 мета
  awareness-forecast → 3.4 warnings в выжимку → 3.5 next-quarter-plan разгрузить → 3.6 онбординг →
  3.7 мёртвые кнопки справки.
- **Батч 4 — систематика корпуса .md:** 4.1 U+2014→«–» (447) → 4.2 единая языковая шапка →
  4.3 «правдоподобный диапазон» остаток → 4.4 LEGACY_COMMANDS индекс → 4.5 границы данных →
  4.6 mmm-decomposition шаг кода → 4.7 mmm-report «не выдумывай» → 4.8 mmm-full протокол провала →
  4.9 edge cases inbox → 4.10 monthly пороги.
- **Батч 5 — вечная оборона (CI):** 5.1 lint-prompts.mjs (U+2014/термины/шапка/блокировка) →
  5.2 линтер четверного совпадения → 5.3 manifest-sync линтер → 5.4 дым-эвал в CI → 5.5 подключить
  lefthook-гейты в CI → 5.6 регламент в CLAUDE.md репо.
- Всё сходится в один релиз **2.3.1** вместе с Фазой B (релиз-публикация). Отдельных выкатов нет.

## Каналы доставки каждой правки (карта Фазы 0)
- `.md`-промпты (New_AI_Agency/econometrist/**) → пересборка vault → OTA (доедет после 0.1).
- `src/lib/*.js`, `src-tauri/**` → сборка .exe (OTA код не достаёт).
- `content-packs/*.json` → re-sign ОБЯЗАТЕЛЕН + OTA (иначе verify падёт на рассинхроне).

## СТАТУС (обновлять после каждого батча)
- [✅] **Батч 0** — ПРИНЯТ, коммит `61a74ae`. Version-докачка vault (0.1) + frontend
      min_core-защита (0.2 частично). cargo test 190 passed (проверено лично). durable `IMPL_B0.md`.
      ОСТАТКИ: (a) серверная часть vault_versions (Supabase /auth) — блокер вне репо;
      (b) 0.2 прямой сценарий (старый frontend-бандл переживает апдейт .exe) — нужна
      константа BUNDLED_FRONTEND_VERSION на сборке, latent (сейчас frontend встроен);
      (c) 🟠 предсуществующий clippy-долг 7 ошибок (online_auth.rs:26-28 doc-quote,
      report.rs:281, fingerprint.rs:27/31/34) — НЕ мой, блокирует CI clippy релиза,
      тривиальный → решение Антона в конце: чинить в 2.3.1 или отдельно.
- [🔄] **Батч 1** — baseline ЧАСТИЧНО: снимок промптов сделан (results/baseline-2026-07-13/,
      в .gitignore харнеса → baseline-состояние = git-коммит 61a74ae + оценки Фазы 1); разведка
      харнеса и egress-механика выяснены (durable IMPL_B1_baseline.md). Численные эвал-скоры НЕ
      досведены (субагент оборвался на живых прогонах) → доснять в петле Батча 4/5 одним прогоном
      старое-vs-новое. 🔑 EGRESS: `ANTHROPIC_API_KEY` баланс исчерпан → эвал гонять ТОЛЬКО через
      `env -u ANTHROPIC_API_KEY node run_eval.mjs` (claude.ai-подписка). `--dry` детерминирован,
      без egress. next-quarter-plan-full стабильно timeout ≥300с — исключать из общих прогонов.
      Далее: правки CLAUDE.md кабинета (SSOT).
- [✅] **Батч 2** — ПРИНЯТ. Аврора: страж INV-50 на scenarioInterpretation (зеркалит askAnswer),
      справка 7 шагов (Планирование поз.6 = project-state.js), «доверительный»→«правдоподобный»,
      sanitizePromptFragment на все пути инъекции, U+2014→U+2013 в промпт-литералах.
      insights-grounding.js НЕ тронут. svelte-check 0 · vitest 1263. durable IMPL_B2.md.
- [ ] Батч 3 · [ ] Батч 4 · [ ] Батч 5
- [ ] Пере-скоринг команд после правок (DoD: все ≥4/4).
- [ ] Финальный самоаудит сессии (тех+логич+методолог).
- [ ] Фаза 4 — рекомендация по канону RAG-библиотеки (отдельный инструмент aurora-upgrade).

## Психо-формулировки Батча 3.1 (моё предложение — применять, показать Антону, не блокировать)
Механика (аудит S6): cabinetPhases.econometrist в psy-data.json показываются таймером ChatPanel
как FALLBACK когда движок молчит (= консультационные команды/чат); реальный расчётный шаг Model
идёт через отдельные `pipeline_phase`-события движка (перекрывают таймер). Значит текущие
«Строю байесовскую модель…/MCMC-сэмплирование…/Рассчитываю ROI…» показываются ИМЕННО на
консультациях, которые ничего не считают → ложный процесс. Замена (честно, нейтрально, «–», без
англицизмов, не обещает вычисление, т.к. Аврора работает с ГОТОВЫМИ результатами):
- ~10с: «Анализирую данные модели…»
- ~30с: «Готовлю разбор…»
- ~300с: «Формулирую выводы и рекомендации…»
Реальный пайплайн (шаг Model) НЕ затронут — его прогресс идёт своими pipeline_phase.
Правка psy-data.json (content-pack) → re-sign ОБЯЗАТЕЛЕН.

## NEXT (продолжить отсюда без переспроса)
1. Дождаться финиша субагента Батч 0 → прочитать `IMPL_B0.md` + `git diff` → лично прогнать
   `CARGO_TARGET_DIR="D:/cargo-targets/ai-agency" cargo test --manifest-path src-tauri/Cargo.toml`.
   Принять или вернуть на доработку. При приёмке — auto-commit local (pathspec своих файлов).
2. Запустить Батч 1: сперва эвал-baseline субагентом (`env -u ANTHROPIC_API_KEY` если ключ исчерпан,
   fallback подписка; недетерминирован — один прогон, зафиксировать в git results/baseline-2026-07-13/),
   затем правки CLAUDE.md кабинета (SSOT MQS/Ratio — верифицировать сверкой с
   sidecar/econometrica/utils/diagnostics.py, НЕ эвалом).
3. Далее батчи 2→3→4→5 по плану. Психо-формулировки (3.1) — предложить нейтральные самой
   («Анализирую результаты модели…», «Готовлю разбор…»), показать Антону в отчёте батча, не блокировать.
4. После всех батчей — пере-скоринг + финальный самоаудит + Фаза 4 (канон).

## Открытые развилки — как решаю сама (без вопроса Антону, если не помечено 🟠)
- re-sign паков (0.3): найти инструмент подписи content-pack (не в tools/; проверить
  aurora-pack.py в родительских репо / память reference). Если ключ недоступен на машине —
  пометить блокером «локальный re-sign требует ключ», сделать всё остальное, доложить.
- Эвал-baseline упирается в egress/timeout: зафиксировать частичный baseline (что прошло),
  пометить next-quarter-plan timeout-кейс, не блокировать фактические правки.
- 🟠 ЕДИНСТВЕННЫЙ кандидат в вопрос Антону: если при правке множителя/Авроры вскроется
  КОНЦЕПТУАЛЬНАЯ развилка (например, менять ли поведение движка ради согласования с промптом —
  это затрагивает не текст, а логику продукта). Технические — решаю сама.

## Ключевые уроки (не наступить повторно)
- Shared-репо: зонд HEAD/автора ДО коммита/тега ([[concurrent_session_collision_shared_repo]]).
- Юнит доказывает функцию, живой прогон ловит проводку — Батч 0 без живого прогона верифицирован
  лишь юнит-тестом, пометить в отчёте ([[feedback_unit_test_proves_function_not_wiring_live_catches_trigger]]).
- Не гонять N субагентов на ОДИН файл ([[feedback_no_parallel_subagents_same_file_verify_rebuild]]);
  разводить батчи по непересекающимся файлам.
- JSON сверять парсером, не grep (ложный ноль Фазы 0).
- content-pack правка → re-sign в ТОМ ЖЕ коммите (иначе verify падёт; кейс themes.json).
