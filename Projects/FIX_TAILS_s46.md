# FIX_TAILS s46 — три хвоста внешнего аудита

Контекст: правки в уже собранный и проверенный установщик не входят, едут следующей сборкой.
Сборка не запускалась, push не делался.

## 1. Жадная граница вычистки пути (`src-tauri/src/commands/feedback.rs`)

Паттерн `path_pattern()` доработан по предложению аудита: пробел разрешён только в СРЕДНИХ
сегментах пути, не в последнем (имя файла) — поэтому граница останавливается перед хвостовой
причиной отказа, а фамилия внутри пути по-прежнему не утекает. В ходе собственной проверки
нашла реальный изъян в предложении аудита: без дополнительного исключения двоеточия из
стоп-класса сегментов два пути в одной фразе («…a.csv в D:\Backup\…») склеивались в одну
утечку — средний сегмент дожёвывал до буквы ВТОРОГО диска через пробел. Добавила двоеточие
в стоп-класс (оно живёт только в префиксе диска, не внутри сегментов).

Новый тест `sanitize_error_text_keeps_failure_cause_after_path_with_space_in_middle_segment`
— 12 сценариев (пробел в учётной записи + хвост причины отказа, пробел без хвоста, прямые
слеши, UNC с пробелом, кавычки, скобки, точка/запятая в конце, два пути в одной фразе,
пробел в среднем сегменте UNC + хвост, пробел в среднем сегменте прямых слешей + хвост,
причина отказа не пропадает). Все 12 зелёные.

Мутация (вернуть прежний паттерн) — дословный вывод:
```
left:  "Ошибка: [путь]"
right: "Ошибка: [путь] не найден на диске"
```
Покраснела на причине потери отказа (сценарий 1). Откат подтверждён:
`cargo test --lib commands::feedback::tests` — 14/14.

## 2. Кнопка обращения в шапке без поведенческой проверки

Новый файл `src/tests/feedback-report-button-header.test.js` — рендерит НАСТОЯЩИЙ
`src/routes/pipeline/+layout.svelte` (не изолированную копию разметки), кликает по кнопке
шапки и проверяет вызов `open_feedback_form` ровно один раз с `ekran` текущего шага и пустым
`oshibka`. `pipelineCurrentStep=1` подобран так, чтобы `ProjectSelector` (только шаг 0) и
`InsightsPanel` (objective overlay) не монтировались — тяжёлые моки не понадобились.

Две обязательные мутации, каждая — один файл, откат сразу после прогона:
- (а) удалён вызов `openFeedbackForm` — тест покраснел (timeout на ожидании вызова).
- (б) `return;` первой строкой обработчика при целом тексте вызова — тест покраснел тем же
  образом (ровно то, что текстовый сторож `guard_left_position_egress.rs` пропускал).

Дерево после откатов чисто: `grep -n "МУТАЦИЯ" src/routes/pipeline/+layout.svelte` — 0 совпадений.
Смежные файлы (`feedback-report-button.test.js`,
`error-feedback-buttons-optimize-model.test.js`) — 6/6, не сломаны.

## 3. Пункт оглавления «Прогноз» ведёт в никуда без сценариев

`sidecar/econometrica/aurora_html/builder.py::_toc_items` — добавлен пропуск для
`sid == "forecast"` по образцу уже стоящего у `trust`; условие зеркалит
`render_forecast_plan` (sections.py:2488): `status != "ok" or not scenarios` → пункта нет.

Новый тест `test_html_toc_skips_forecast_link_without_scenarios`
(`sidecar/econometrica/tests/test_forecast_report.py`) — 4 состояния из отчёта аудита:
forecast отсутствует, сценарии пустые, `status != "ok"`, сценарии реально сохранены (норма).
Все 4 зелёные, файл целиком — 26/26.

Мутация (`if False and sid == "forecast" and not (...)`) — покраснела дословно:
```
AssertionError: пункт «Прогноз» есть при отсутствующем forecast
```
Откат подтверждён: `grep -n "МУТАЦИЯ" sidecar/econometrica/aurora_html/builder.py` — 0.

## Прогоны (после всех трёх правок, дерево чистое)

| Прогон | Было (по заданию) | Стало |
|---|---|---|
| `cargo test` (src-tauri) | — | 567 passed, 0 failed (521+12+5+22+7 по крейтам/интеграционным бинарям) |
| `npm test` | 1591/120 файлов | **1592 passed / 121 файлов** |
| `pytest sidecar/econometrica/tests` | 1605/2 skipped | **1606 passed / 2 skipped** |

Числа выросли ровно на новые тесты (по одному на задачу 1–3, плюс новый тестовый файл
задачи 2). Ни одного падения.
