<script>
  /**
   * Мок TrainingProgress для теста поведенческого сторожа кнопки обращения (s46).
   * Настоящий компонент опрашивает бэкенд по задаче обучения — к делу не относится.
   * Мок сразу же (на монтировании) зовёт onError с фиксированным текстом — ровно так
   * же, как настоящий компонент звал бы его при отказе бэкенда.
   */
  let { onError } = $props();
  $effect(() => {
    onError?.('Мок: обучение отказало');
  });
</script>

<div data-testid="mock-training-progress"></div>
