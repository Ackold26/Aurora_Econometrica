# Живой ретест инсталлятора v2.2.0 — находки (2026-07-10, Антон у экрана, Маша ведёт)

Объект: установленный `Optimizer MMM_2.2.0_x64-setup.exe` (sha256 44ab3880…, собран из e1-backtest @c27d64a). Файл: `synth_fmcg_brand.xlsx` (Desktop, свежий). Проект: synth_fmcg_brand MMX 1007-26. Чек-лист: 8 пунктов из `NEXT_SESSION_econ_gui_fixes.md`.

## Формат
`[severity] пункт/экран — симптом — корень (гипотеза/подтверждён) — repro`

---

## Находки

### 🟠 П5-1 · Валидация/Инсайты — stale Ratio-инсайт не вытесняется пересчётом + дубль с разной тональностью
- **Симптом:** после «Применить все рекомендации (4)» панель инсайтов держит ТРИ Ratio-инсайта одновременно: «2.0:1 — ниже рекомендованного» (устаревший, со старта импорта), «6.9:1 — хорошее… Модель надёжна», «6.9:1 — допустимо. 2 предупреждений…». Карточка «Запас данных» при этом консистентна (6.9:1 шапка+шкала+expert breakdown 48/7).
- **Корень (гипотеза, класс DRIFT_MAP «UI мимо SSOT»):** инсайты не инвалидируются/не дедупятся по ключу (ratio) при пересчёте — старый инсайт живёт рядом с новым; плюс два генератора ratio-инсайта с разной тональностью («надёжна» vs «допустимо») срабатывают на одном значении.
- **Repro:** импорт synth_fmcg_brand.xlsx → Валидация → инсайты показывают Ratio 2.0:1 + 4.4:1 → «Применить все рекомендации (4)» → остаются 2.0:1 (stale) + 6.9:1 ×2.
- **Не блокер** обучения; чинить после ретеста. Кандидат-класс: жизненный цикл инсайтов (invalidate-by-key при событии пересчёта ролей).

### 🟡 П5-2 · Инсайты — обрезка имён каналов до 6 символов в заголовках
- **Симптом:** заголовки парных инсайтов: «TV:», «DIGITA:», «PERFOR:» — digital/performance обрезаны некрасиво (без многоточия, посреди слова).
- **Repro:** Валидация → инсайты пар. Косметика, премиальность.

### 🟡 П5-3 · Валидация/Корреляционная матрица — подписи обрезаны до неразличимости (находка Антона)
- **Симптом:** подписи строк/столбцов матрицы: «digital_spe…»/«digital_imp…», «performance…»/«performance…» (ДВЕ строки визуально идентичны — spend vs clicks неотличимы), «competitor_…», «category_sa…». Клиент не может понять, какая строка что.
- **Направление фикса:** полные имена (место слева есть) ИЛИ умная обрезка с хвостом («performance…clicks») + tooltip с полным именем на hover.
- **Repro:** Валидация → Расширенная диагностика → корреляционная матрица.

### 🟡 П6-1 · Модель/Эксперт — «Adstock по каналам»: пустые селекты
- **Симптом:** в Эксперт-режиме блок «Adstock по каналам» — 4 выпадающих списка без значений. При Adstock=«Авто» после обучения тип уже разрезолвлен BIC-селектором (NEW-1, пикл хранит конкретный тип) — UI мог бы показать «Geometric (авто)» и т.п. Пустой селект выглядит сломанным.
- **Repro:** Модель → обучить (adstock авто) → тумблер «Эксперт» → Расширенные настройки → Adstock по каналам.

### 🔴 П2-1 · Декомпозиция/Динамика по периодам — drill-чипы «Детализация» НЕ работают ВООБЩЕ (обычный вид + fullscreen; эскалировано Антоном)
- **Симптом:** чипы детализации (Развернуть всё / База / Медиа / Внешние / Конкуренты) не реагируют на клики ни в обычном виде панели Декомпозиции, ни в fullscreen — группы НЕ раскрываются на каналы. Легенда (вкл/выкл фактора) работает, анимация при переключениях легенды плавная, 4 группы+сезонность рисуются.
- **Контекст:** это симптом F-A1-14 приёмки 2026-07-05; фикс `c715f3a` (side-write seriesTopGroup из $derived.by рвал граф Svelte 5) в артефакте ПРИСУТСТВУЕТ (installer c27d64a собран после), принимался vitest'ом на данных option — но живой клик-рендер до сего дня никто не проверял (#10 не проводился). Либо фикс не закрыл клик-путь чипов (чинился граф данных, не обработчик), либо второй корень в обработчике клика чипа.
- **Repro:** Декомпозиция → Динамика по периодам → клик по чипу «База»/«Медиа» (левый верхний угол панели) → изменений нет. Fullscreen — то же.
- **План диагностики (после прохода ретеста):** dev+мост → DOM-зонд обработчика чипа (dispatchEvent/handler привязка), сверить option до/после клика — разделит «обработчик не привязан» vs «стор не мутирует» vs «option не перестраивается». Юнит доказал данные — живой клик-путь тестами не покрыт (класс [[feedback_test_asserts_what_agent_claims]] — тест утверждал option, не клик).
- **Блокирует П2 полностью** (drill + праздники-в-базе смотрятся через раскрытие Базы).

## Пройдено ✅
- **Шаг 2 (Модель) — ЗАКРЫТ:** оценка «10-30 секунд (4 канала)» и факт 11с ✓; R² одним числом (0.825 шапка = 83% инсайт) ✓; Эффективный Ratio 3.2:1 ОДИН на экране, честный, согласован с MQS-карточкой ✓; инсайты Модели без stale-дублей ✓; сезонность «годовой период ρ=0.64, спрос отделён от рекламы» ✓; авто-праздники 12 с бейджами «влияет» ✓; R-hat 1.0000/дивергенций 0/MAPE 4.1% консистентны ✓; витрины E1 «Проверка на истории» + E3 «История модели» на месте ✓; Эксперт: калибровка экспериментом (E2) + настройки выборки по-русски ✓. **П4 (prior-check): ложный «44% FAIL» на экран НЕ вылезает** (раньше сам появлялся) — первая половина закрыта; lift — на Шаге 4.
- **П7-A4 (бейдж пар на карте корреляций) — ЗАКРЫТ:** зелёный бейдж «✓ 4 пар каналов „бюджет + метрика" (ожидаемо) · прочей мультиколлинеарности нет» + под матрицей каждая пара: «r=0.995 — Пара канала — ожидаемо: в модель уйдёт одна колонка пары» ×4. Пары не пугают.
- **П3 (роли) — ЗАКРЫТ:** category_sales → «Оставить: контроль категории» (НЕ «исключите»); пары найдены все 4 (tv_trp/digital_impressions/ooh_contacts/performance_clicks → «Исключить», spend → «Оставить»); счётчик «Применить все рекомендации (4)»; competitor_trp/price_index → контроль. Применение рекомендаций отработало (Медиа 4, Не использовать 4, Ratio карточка 2.0→6.9 пересчиталась).
- Честный инсайт category_sales «120× больше — проверьте единицы» — корректен (synth-данные), не находка.

### 🔴 A6-1 · Валидация (спец-файл) — суммарная бюджетная колонка ПРИНЯТА медиа-каналом (авто-снятие не сработало)
- **Симптом:** `total_media_budget` (точная сумма 4 spend-колонок, 1 077.8M ≈ 678.4+233.8+126.1+39.6) получила роль «Медиа-канал» + зелёное «Оставить»; счётчик «Медиа-канал 9», инсайт «Оставим бюджеты (5 канала)» — сумма посчитана пятым бюджетом. Ожидание A6: авто-снятие роли с подсказкой «суммарный бюджет задваивает вклад».
- **Риск:** двойной учёт бюджета → OVB/мультиколлинеарность со всеми spend, неверные ROI. Клиентская ловушка.
- **Контекст:** механизм total-budget в validator есть (Т3-плюс П1 2026-07-04, `_normalize_channel_name`, validator.py~439) — на живом имени `total_media_budget` не сработал. Разобраться: критерий имени/суммы?
- **Repro:** новый проект → `Desktop\Тестовые_файлы_оптимизатора\synth_fmcg_A5_A6.xlsx` → Валидация → роли.

### 🟡 A5-1 · Валидация (спец-файл) — black_friday: распознана и не потеряна ✅, но текст рекомендации неудачен
- **Что ок (ядро A5):** клиентская колонка события БЕЗ префикса не потеряна: роль «Контрольная», честная пометка «92% нулей», рекомендация «Проверить» (не «исключите» — урок false_suppression учтён).
- **Что не ок:** текст инсайта «black_friday — 91.7% нулей. Рекомендуем объединить с другим КАНАЛОМ» — для событийной дамми бессмыслен (это не канал). Ожидаемый текст: «событие; возможно дублирует авто-праздник „Чёрная пятница" — проверьте» (детектор дублей user_covered_auto_holidays есть в движке, предупреждение о дубле на экране не видно).
- **Repro:** тот же файл → Валидация → инсайт black_friday.

### 🟠 П5-1b · подтверждение stale-Ratio на втором проекте
На A5_A6-проекте панель инсайтов держит одновременно «Ratio 1.8:1 — слишком мало (красный)» и «Ratio 3.7:1» ×2 — тот же класс, что П5-1 (stale не вытесняется + дубль тональностей). Класс подтверждён, не единичный.

## П1 · Оптимизация — ЗАКРЫТ ✅ (с зондовым доказательством)
- **Анализ «+0.0%» = ЧЕСТНЫЙ оптимум, не баг.** Три зонда на проекте Антона: (1) optimization.json converged_at_current=True, binding=False; (2) живой SLSQP из репо на том же пикле — тот же ответ; (3) решающий — ручные перекладки 30% бюджета во все 5 направлений ВСЕ ухудшают KPI (−0.09…−0.41%). Новая синтетика строит беты из ROI при фактических бюджетах → распределение «из коробки» у оптимума; «4 из 4 перенасыщены» — честное описание.
- **Планирование (бюджет 1.2 млрд ≠ текущие 906М): прирост +3.4%, эффективность медиа +36.2%, дельты НЕРАВНОМЕРНЫЕ** (экстраполяция-маркеры: tv 1.61× / digital 2.15× / ooh 2.06× / performance 2.63× исторического max — каждому своё). «ГЛАВНАЯ РЕКОМЕНДАЦИЯ: нарастите ooh_spend на 117.3 млн ₽» + «предельная отдача следующего рубля» по каналам 0.30–0.72× — actionable, математика связная. Маркеры экстраполяции (Chan&Perry) работают в проде.
- Разделяющий тест «Анализ-оптимум ↔ Планирование-прирост» подтвердил живой решатель.

## Статус чек-листа
- [x] П1 оптимизация в планировании ✅ (+ Анализ-оптимум доказан зондами)
- [ ] П2 drill + морф + fullscreen — 🔴 П2-1 (drill-чипы мертвы), чинить после прохода
- [x] П3 роли колонок ✅
- [x] П4 демо fmcg ✅ (prior-check: ложный FAIL не появляется; lift +3.4% виден)
- [~] П5 ratio один и честный — Модель ✅ (эфф. 3.2:1 один, честный); 🟠 П5-1 stale-инсайт на Валидации
- [~] П6 тексты — по пройденным экранам чисто; 🟡 П6-1 пустые селекты Adstock в Эксперте
- [x] П7 — A4 ✅ (бейдж пар на матрице); A5 🟡 частично (не потеряна, но текст «объединить с каналом» мимо); A6 🔴 FAIL (total_media_budget принят медиа-каналом)
- [x] П8 экспорт PPTX+HTML — проверен программно: wireframe 0, дисклеймер есть, период «янв 2022 – дек 2025» корректен; 🟡 П8-1 длинное тире/дефис в текстах, 🟡 П8-2 «baseline/media-вклад/adstock» англицизмы в ключевом выводе деки; П8-3 (9% vs 11%) снят зондом — рассинхрона нет (baseline_pct=90.9 в артефакте, «11%» был другой проект)

## ═══ ИТОГ РЕТЕСТА (2026-07-10, пройден целиком) ═══
**Ядро v2.2.0 работает:** роли/пары/категория ✅ · модель 11с, честные метрики, один Ratio ✅ · Анализ-оптимум доказан зондами ✅ · Планирование живое (+3.4%, неравномерные дельты, экстраполяция-маркеры, «Главная рекомендация», предельная отдача рубля) ✅ · отчёты без wireframe, с дисклеймерами ✅.
**К фиксам (по убыванию):** 🔴 П2-1 drill-чипы мертвы (обычный+fullscreen) · 🔴 A6-1 суммарный бюджет принят каналом · 🟠 П5-1(+b) stale-Ratio инсайты · 🟡 A5-1 текст black_friday · 🟡 П5-2 «DIGITA:»/«PERFOR:» обрезки · 🟡 П5-3 подписи корр-матрицы · 🟡 П6-1 пустые селекты Adstock (Эксперт) · 🟡 П8-1 тире/дефис в текстах отчётов · 🟡 П8-2 baseline/media/adstock в клиентских текстах деки.
**Вердикт:** релиз-блокеры — П2-1 и A6-1; остальное полировка. После фиксов — пересборка и повторная точечная проверка этих мест.
