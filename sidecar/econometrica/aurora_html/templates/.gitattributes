# Файлы, чья контрольная сумма проверяется по байтам (aurora_html/__init__.py,
# ASSET_SHA256), обязаны выгружаться байт-в-байт. Без этого git при autocrlf=true
# переписывает переводы строк, сумма на диске расходится с зашитой в коде, и на
# КАЖДОМ экспорте отчёта в журнал уходит «Asset integrity failure».
# Так уже было 07.06.2026: тогда обновили константу под факт — вылечили симптом,
# причина осталась и дрейф повторился 14.09.2026.
*.min.js -text
*.woff2 -text binary
