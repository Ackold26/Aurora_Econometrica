# E1 Backtest-витрина + UX-аудит — durable-реестр автономной работы

> **Старт:** 2026-07-03 · ветка `feat/econ-e1-backtest` (от `feat/econ-commercial-readiness` @83533ad)
> **Вход:** `docs/ROADMAP_v3_IDEAL_PRODUCT.md` §E1 (Фаза 1 ⭐) — утверждено Антоном, переход после
> волн commercial_readiness БЕЗ отдельной команды (цепочка утверждена).
> **Мандат:** максимально автономно; развилки решать самой (взвешивая трудозатраты↔эффект,
> стабильность); улучшения копить и принести в конце; heartbeat ScheduleWakeup(600).
> **Стоп** — только по слову Антона.
> **При старте/после компрессии:** прочитать этот файл, продолжать БЕЗ переспроса с первой
> незакрытой задачи реестра.
> **🆕 Живое задание Антона (2026-07-03, вторая волна этапа):** глубокий аудит UX,
> пайплайна и интерфейса — см. раздел «Волна UX» ниже. Выполняется ПОСЛЕ E1-5
> (батч умирает в пределах хода; E1 доводится до конца первым).

## 🔴 ПРАВИЛО СЕССИИ (унаследовано, действует)
MCP-мост tauri (`mcp__tauri__*`) НЕ вызывать — вести работу headless (standalone
sidecar на localhost-порту + файловые зонды). Память: [[feedback_tauri_mcp_bridge_half_dead_after_rebuild]].

## Метод (наследие мат-аудита, обязательно)
Число-доказательство до правки, число-подтверждение после · первый дефект класса — линза ·
батч умирает в пределах хода · леса красть (`_build_project`, живой Kagocel-зонд) ·
каждая фича честности СРАЗУ во всех слоях: движок → endpoint → Rust → UI → отчёт ·
методология только с RAG-атрибуцией · INV-50 · детерминизм · pickle-additive · тесты `-n 4`.
**Реестр отражает ФАКТ: статус ✅ ставится только после числа-подтверждения.**

## Цель E1 (из ROADMAP, дословно)
Витрина «модель vs факт»: rolling-origin валидация (обучение на [1..k] → предсказание
[k+1..k+h] против факта; k скользит). Метрики: **coverage 90%-интервала (главная!)**,
MAPE holdout, сравнение с наивным (модель обязана бить наивный — иначе честно сказать).
Доставка сразу все слои: endpoint + Rust + карточка UI («Проверка на истории: 7 из 8
кварталов в интервале») + слайд PPTX + narrative. Триггер: кнопка «Проверить модель на
истории» (авто при переобучении — см. решение D-1 ниже).
**Критерий готовности:** на Кагоцеле витрина показывает честный holdout-результат;
coverage сверен канарейкой с пересчётом; слайд в отчёте; никакого in-sample вранья.
**Осторожно:** короткие ряды → при N<3 окон «истории недостаточно для проверки».

## RAG-канон (поднят 2026-07-03, батч E1-0)
- **Gelman, Bayesian Workflow §9.4:** cross-validation predictive checking обнаруживает
  меньшие расхождения, чем posterior predictive (in-sample) — канон: доверие модели
  проверяется ТОЛЬКО out-of-sample.
- **McElreath, Statistical Rethinking §7.5:** «fit will always favor more complex models» —
  in-sample fit не годится как мера доверия.
- **Robyn (Meta 2024, arXiv 2403.14674):** out-of-sample validation — индустриальный
  стандарт MMM (ts_validation, NRMSE на test).
- **Hernan/Robins, Fine Point 18.2:** overfitting и cross-validation.
- Термин «rolling origin» — Hyndman (FPP, tscv); книги нет в корпусе — в докстринге
  упоминать как общепринятый термин без псевдоцитаты (verify_external_citation).

## Инвентаризация лесов (батч E1-0, доказано чтением кода)
| Актив | Состояние |
|---|---|
| `engines/backtest.py::run_backtest` | Одноразовый holdout (посл. K периодов), вердикт по R²-gap. **PI coverage = None всегда** (стр. 189–195, комментарий устарел). Наивного бенчмарка нет. Даты в per_period нет (докстринг обещает). `predicted[:len(actual)]` может быть короче actual → broadcast-риск. Тестов НЕТ. |
| Доставка run_backtest | **НУЛЕВАЯ**: endpoint `/compute/backtest` НЕ существует (докстринг врёт), Rust-команды нет, UI не вызывает. |
| `scenario.predict_scenario` | ✅ Возвращает per-period `predictions` + `predictions_ci_low/high` (90% HDI веер, bayesian, стр. 567–590) — готовый лес для coverage. |
| `utils/forecast_validation.py` | ✅ detect_granularity (D/W/M/Q/Y + confidence), conformal_planning_intervals (OLS, half_width, confidence параметризуем), detect_seasonality. |
| `inverse.py::posterior_sampler`-мост | per-draw forward (мат-аудит F-02); `econ_forecast_scaling` в Rust помечен «кандидат для E1». |
| UI «Backtest» виджеты | МЁРТВЫЕ: `pipeline/DiagnosticsPanel.svelte` никем не рендерится; `StepSummary.svelte` (шаг 6 визарда) получает `diagnostics=null` и замокан в тестах из-за краша. Строка «Backtest (4 месяца holdout)» с живыми данными не показывалась. |

## Дизайн-решения (приняты самой, батч E1-0)
- **D-1 Авто-триггер при переобучении — НЕ делать сейчас** (только кнопка + хранение
  результата с пометкой устаревания). Почему: bayesian rolling из N окон = N×34с и больше
  на слабом CPU — авто-запуск удорожает каждое обучение в ~5×, бьёт по E5 «≤30 мин до
  инсайта». Пометка «результат устарел (модель переобучена)» — честная замена;
  авто-связка = E3 (дрейф). Трудозатраты↔эффект: кнопка покрывает ценность слайда целиком.
- **D-2 Окна = кварталы без нахлёста** (h = квартал в терминах гранулярности: M→3, W→13,
  D→90, Q→1? — уточнить при реализации по числу точек). k скользит с шагом h назад от
  конца; min_train = max(12, 2×каналы+4) точек И порог OLS/bayesian тренера.
  N<3 окон → честный отказ «истории недостаточно» (status='insufficient').
- **D-3 Две coverage-метрики из одного прогона:** per-period coverage 90% PI (канон,
  главная) + per-window «квартальный тотал в 90% HDI суммы» (коммуникация «7 из 8
  кварталов»). В UI/слайде ведёт per-window (человеческий язык), per-period — рядом.
- **D-4 Наивные бенчмарки:** naive-last (random walk) + seasonal-naive (если n_train ≥
  сезон и сезонность детектирована). Сравнение с ЛУЧШИМ из наивных (строже к модели —
  честнее). MAPE_model vs MAPE_naive → в вердикт (worse_than_naive — честно).
- **D-5 PI per-period:** bayesian → predictions_ci_low/high из predict_scenario (90% HDI);
  OLS → conformal вокруг точки с confidence=0.9 (единый уровень с HDI; проверить
  фактический уровень auto_intervals тестом, не верить сигнатуре).
- **D-6 Гранулярность из detect_granularity** по датам данных; при confidence<0.6 —
  дефолт M + пометка в результате.
- **D-7 Хранение:** `models/backtest.json` в project_dir (atomic write); PPTX/narrative
  читают его; при отсутствии — слайд НЕ рисуется (никаких wireframe-дефолтов — урок B1
  «замаскированная дефолтом честность»).
- **D-8 run_backtest (старый) НЕ удалять** — хирургия; новый `run_rolling_backtest` рядом,
  общие куски — в приватные хелперы. Врущую строку докстринга про endpoint поправить.
- **D-9 Скорость:** режим окна наследует режим модели (bayesian→bayesian, OLS→OLS).
  Кнопка в UI показывает честную оценку времени. Преждевременных оптимизаций (сокращение
  draws) не делать до зонда реального времени на Kagocel.
- **D-10 Предиктивный интервал, не интервал средней (E1-5, живой зонд):** HDI
  сценария = неопределённость ПАРАМЕТРОВ; coverage против него проваливается даже
  у честной модели (Kagocel: 0/9 при ширине ±5%). Канон posterior predictive
  (Gelman BW): var_pred = var_mean + σ². Реализация: σ ≈ in-sample RMSE окна
  (native, есть в диагностике всегда) → полуширины расширены квадратурой
  (_widen_pi_with_noise); тотал окна ⊕ √h·noise. pi_method =
  'posterior_predictive_90' (при отсутствии RMSE — честный
  'posterior_hdi_90_mean_only'). Точный путь (sigma-samples в pickle,
  per-draw noise) — кандидат E3, additive-поле. OLS-путь не менялся —
  conformal уже включает остатки (потому синтетика дала 100%).

## Реестр задач (статусы — только по факту)
| # | Задача | Статус |
|---|---|---|
| E1-0 | Аудит backtest.py + инвентаризация + RAG + реестр + ветка | ✅ 2026-07-03 |
| E1-1 | Движок: `run_rolling_backtest` (rolling-origin, coverage×2, naive, granularity, insufficient при N<3) + тесты с канарейкой-пересчётом | ✅ 14 тестов, 7.4с |
| E1-2 | Доставка-1: endpoint `/compute/backtest` (+чтение сохранённого) + Rust `econ_backtest` + lib.rs | ✅ 6 тестов + cargo check |
| E1-3 | Доставка-2: UI-карточка «Проверка на истории» (кнопка, вердикт, устаревание) | ✅ 9 vitest, svelte-check 0 |
| E1-4 | Доставка-3: PPTX-слайд + narrative (is_live гейт, без wireframe) + канарейка-маркеры | ✅ 13 тестов + verify 43/43 |
| E1-5 | Живой зонд Kagocel (bayesian окна, реальное время) + канарейка coverage + сводный отчёт | ✅ 2 зонда «ВСЁ ЗЕЛЁНОЕ» + отчёт |

## Волна UX — глубокий аудит UX / пайплайна / интерфейса (задание Антона 2026-07-03)

**Мандат (дословно по смыслу):** найти через аудит ответы — что можно сделать лучше
и эффективнее; как упростить путь пользователя и сделать его понятнее; как упростить
использование программы в целом. Посыл продукта: **«эконометрика не для эконометристов»**.
Ограничение: БЕЗ потери профессионализма, точности и эффективности — UX, дизайн и путь
пользователя легче, проще, понятнее, быстрее.

**Метод волны:** headless-аудит кода UI (маршруты, шаги пайплайна, все клиентские
тексты), прошлые визуальные находки (TEST_FINDINGS_2026-06-02 — топ «перегруз
интерфейса»), E5-рамка ROADMAP (время-до-инсайта ≤30 мин), Ильяхов (стайлгайд B4
распространить на интерфейс), C3-методика живых прогонов где применимо без моста.
Каждая находка = (что мешает не-эконометристу) + (предложение) + (цена/эффект);
правки — хирургически (тексты, дефолты, раскрытия — делать; перестройка навигации —
только планом Антону на утверждение).

| # | Задача | Статус |
|---|---|---|
| UX-0 | Карта пути as-is: «файл → первый инсайт» по коду шагов пайплайна; число решений/полей/кликов на шаг; точки трения → docs/audits/UX_JOURNEY_MAP.md | ✅ карта + T1-T7 + приоритеты |
| UX-1 | Язык интерфейса: все user-facing тексты по Ильяхову (жаргон R-hat/ESS/HDI/adstock → «профессионально, но понятно»: смысловой ярлык + число в раскрытии); англицизмы; длинные конструкции | ⏳ TODO |
| UX-2 | Прогрессивное раскрытие: какие решения пользователя реально обязательны? Авто-детекты (гранулярность, KPI-тип, adstock, роли колонок) → умные дефолты + «поправить при желании»; свести обязательный путь к минимуму решений | ⏳ TODO |
| UX-3 | Смысловые статусы: вместо голых метрик — вердикты («модель надёжна для решений» + число в раскрытии); унифицировать светофоры по шагам | ⏳ TODO |
| UX-4 | Замер времени-до-инсайта (E5): headless-хронометраж пути на Kagocel-фикстуре — от импорта до (а) вердикта модели, (б) первой бюджетной рекомендации; цель ≤30 мин силами маркетолога | ⏳ TODO |
| UX-5 | Онбординг и пустые состояния: первый экран, пустые шаги (нет модели/сценариев) — каждое состояние называет следующее действие | ⏳ TODO |
| UX-6 | Ошибки как помощь: сквозной аудит user-facing ошибок — каждая говорит ЧТО случилось и ЧТО ДЕЛАТЬ (системно довести начатое в C1/C3) | ⏳ TODO |
| UX-7 | Сводный отчёт волны: docs/audits/UX_AUDIT_2026_07.md — внедрённые правки + план крупных изменений на утверждение Антону (приоритизация эффект×цена) | ⏳ TODO |

## Находки по ходу
- **F-E1-1:** Докстринг backtest.py обещает endpoint `/compute/backtest` — его нет.
  Класс «документация врёт о доставке». → Поправить при E1-1.
- **F-E1-2:** UI-виджеты «Backtest» мёртвые, подпись «4 месяца holdout» — wireframe.
  StepSummary крашится на diagnostics=null (замокан в тестах) → проверить, показывается
  ли шаг 6 живьём (риск живого краша визарда) при E1-3.
- **F-E1-3:** `predicted[:len(actual)]` в run_backtest без проверки длины → тихий
  broadcast-краш при коротком predictions. → В новом коде защита + код ошибки окна.
- **F-E1-4 (живой Kagocel-зонд, КЛАСС «интервал средней ≠ предиктивный»):**
  первый прогон дал coverage 0/9 при интервалах ±5% — вскрыл, что bayesian-путь
  брал HDI средней. Исправлено (D-10). Проверить этот класс в СОСЕДЯХ: CI-веер
  сценариев (MultiScenarioChart) и forecast-fan подписаны как интервал
  «прогноза» или «средней»? — кандидат на аудит следующего батча/UX-волны.
- **F-E1-5 (содержательная, про реальный проект):** модель реального
  Kagocel-проекта (обучена 2026-06-24, 31 мес, без сезонных контролов) на
  quarterly-holdout: MAPE 47.8% vs сезонный наивный 18.7% — сезонность продаж
  (противопростудный пик ноя-янв 1.6 млрд vs лето 0.58 млрд) моделью не
  выучена. Вердикт worse_than_naive ЧЕСТЕН — ровно та правда, которую продукт
  раньше не показывал. Для пилотов: включать сезонные контролы/праздники в
  конфигурацию — иначе витрина справедливо забракует модель.

## Предложения Антону (копить, нести в конце)
1. (E3) Авто-запуск витрины после переобучения как фоновая задача, когда появится
   механизм фоновых работ с прогрессом.

## Журнал батчей (только совершённое)
- **E1-0 (2026-07-03):** ветка `feat/econ-e1-backtest` создана; ROADMAP §E1 прочитан
  целиком; backtest.py + forecast_validation.py прочитаны; доставка проверена grep'ами
  (нулевая); scenario per-period CI подтверждён чтением (стр. 567–590); RAG-канон поднят
  (Gelman BW §9.4, McElreath §7.5, Robyn 2024, Hernan/Robins FP 18.2); дизайн-решения
  D-1..D-9 зафиксированы; реестр создан. Урок хода: первая версия реестра содержала
  выдуманные статусы будущих батчей — переписана; правило «статус только по факту»
  внесено в Метод.
- **E1-1 (2026-07-03):** `run_rolling_backtest` в engines/backtest.py (~330 строк:
  _plan_windows без нахлёста хронологически, _naive_forecasts last+seasonal,
  _rolling_verdict с приоритетом worse_than_naive→coverage_low→validated,
  merge_rules повтор на df, C3-резолвер данных, PI: posterior_hdi_90 → conformal_90
  (×y_std из окна) → residual_z90 фолбэк, тотал окна ±hw√h для OLS, WINDOW_* коды,
  atomic backtest.json + load_saved_backtest с выживанием на битом JSON).
  Врущий докстринг legacy про endpoint исправлен (F-E1-1). Грабля хода: импорт
  load_posterior_samples — он в utils.posterior_propagation, НЕ в persistence.
  Тесты tools/test_rolling_backtest.py: **14 зелёных за 7.4с** (юниты чистых функций,
  боевой OLS-путь на синтетике 40 мес × 6 окон, 2 канарейки-пересчёта coverage/MAPE,
  детерминизм двух прогонов, insufficient/NO_MODEL/NO_DATA русские, битый JSON).
  Живой зонд чисел: coverage per-period 1.0 (18 точек), per-window 5/6, MAPE 5.99%
  vs seasonal_naive 5.32% → вердикт worse_than_naive: витрина ЧЕСТНО показала, что
  OLS с фикс. Hill на линейной синтетике проигрывает сезонному наивному — ровно
  та честность, ради которой E1 строится (модели не льстим).
- **E1-2 (2026-07-03):** endpoint POST /compute/backtest (BacktestRequest:
  Field-валидация horizon 1..90 / min_train 8..200 / max_windows 3..12 /
  mode Literal → 422 на мусор; NO_MODEL/NO_DATA → 404; insufficient → 200
  честный результат; read_only=true → сохранённая витрина мгновенно /
  not_found по-русски). Rust: econ_backtest + отдельный BACKTEST_CLIENT
  (3600с — 8 bayesian-окон на слабом CPU не влезали в TRAIN 900с; read_only
  идёт через quick_client). Регистрация в lib.rs. Тесты
  tools/test_server_backtest.py: **6 зелёных за 9.6с** (422×6 кейсов, 404×2
  русские, insufficient-контракт, read_only not_found, боевой OLS-прогон через
  TestClient + read_only roundtrip generated_at). cargo check: чисто.
- **E1-3 (2026-07-03):** BacktestCard.svelte — карточка «Проверка на истории»
  в ModelTrainingStep после ConvergenceDashboard ({#key picklePath} — свежее
  обучение перечитывает витрину). Состояния: loading → empty (кнопка + честное
  «несколько минут, модель переобучается до 8 раз») / running / done /
  insufficient (плашка, не ошибка) / error (+Повторить). Done: вердикт-бейдж
  (нелестные worse_than_naive/coverage_low — так же заметно), герой «X из Y
  кварталов» (слово «кварталов» только при квартальном горизонте M3/W13/D90),
  MAPE vs наивный + выигрыш %, свёрнутая таблица окон (период/факт/прогноз/
  интервал/✓✕), метод по-русски. Устаревание: движок дописывает
  model_trained_at_current при чтении (mtime latest.pkl) → жёлтая пометка
  «модель переобучена — обновите». Гейты: **9 vitest зелёных**, svelte-check
  **0 ошибок** (была 1 моя — VERDICTS index type, починена JSDoc Record),
  python-регресс 20 зелёных.
- **E1-4 (2026-07-03):** слайд «Проверка на истории» (s10b_backtest) в
  aurora_pptx/builder.py: физический №10 после методологии, дека 12→13 ТОЛЬКО
  при живой витрине (self.backtest = data.backtest при is_live+status ok+окна;
  wireframe-дефолта НЕТ по построению). Заголовок = вердикт-фраза (нелестные
  worse_than_naive / coverage_low — прямо в заголовок), слева герой «X из Y» +
  MAPE vs наивный + покрытие, справа таблица окон (период/факт/прогноз/
  интервал/✓✕), сноска метода по-русски без жаргона. Сдвиг хвостовой нумерации
  и TOC (toc_page_refs [3,6,9,11,12], slide_to_section 13-строчный,
  total_slides 13) — условный через _page_shift. Шов: narrative_adapter
  (_map_pipeline_to_builder_data параметр backtest — пропускает только
  status=ok с окнами) → pptx_export.build_pptx(backtest=) → server export_pptx
  читает load_saved_backtest(project_path) с диска (как сценарии). Гейты:
  **tools/test_pptx_backtest_slide.py 5 зелёных** (13 слайдов с витриной /
  12 без и БЕЗ следов витрины / содержимое честное: «4 из 4 кварталов»,
  «модель точнее на 26%», даты окон, метод / нелестный заголовок живьём),
  регресс test_report_fidelity_live 8 зелёных, verify_aurora_pptx_narrative
  **43/43**. Урок хода: тощая channels-фикстура (2 голых канала) валит
  s06_action_chart («no categories») — для интеграционных PPTX-тестов только
  полный decompose/optimize путь. HTML-секция витрины — кандидат следующего
  шага (адаптер общий, данные уже доступны; PPTX = носитель критерия E1).
- **E1-5 (2026-07-03):** живой зонд на РЕАЛЬНОМ Kagocel-проекте (standalone
  sidecar 8911 headless): POST /compute/backtest mode auto→bayesian, 3 окна за
  24–27с (JAX-кэш после первого окна); первый прогон ВСКРЫЛ дефект «интервал
  средней ≠ предиктивный» (coverage 0/9 при ±5%) → D-10 фикс (predictive =
  mean ⊕ z90·RMSE квадратурой, _widen_pi_with_noise + юнит) → повторный зонд:
  интервалы предиктивные (pi_method=posterior_predictive_90), канарейки
  coverage сошлись бит-в-бит, read_only детерминирован, backtest.json атомарный.
  Вердикт Kagocel честно worse_than_naive (MAPE 47.8% vs сезонный наивный
  18.7% — сезонность counter-простудных продаж моделью проекта не выучена,
  F-E1-5: для пилотов включать сезонные контролы). PPTX живьём полным
  продукт-путём (decompose+optimize с CPP 150К для TRP → /export/pptx):
  **дека 13 слайдов, слайд «0 из 3» с датами окон, 0 wireframe-маркеров** —
  «ВСЁ ЗЕЛЁНОЕ». Регресс после D-10: 26 python-тестов зелёные. Сводный отчёт:
  docs/audits/E1_BACKTEST_2026_07.md. Сайдкар остановлен, tmp-зонды оставлены
  в tmp/ (probe_e1_*.py — переиспользуемы для UX-волны).
- **UX-0 (2026-07-03):** карта пути docs/audits/UX_JOURNEY_MAP.md: 6 шагов,
  минимальный путь ~7-9 обязательных действий (плотность решений — Валидация,
  5 под-шагов); технически ≤15 мин до инсайта (запас против E5 ≤30 мин) —
  реальные тайм-киллеры не механика, а понимание. Точки трения T1-T7
  (онбординг-перегруз [истор. ONBOARD-1] · навигация под-шагов [NAV-2] ·
  жаргон на экранах решений · противоречивые сигналы качества [SEV-1/MQS-1/
  MQS-2] · стена под-шагов Валидации · ошибки-констатации · мёртвый ряд
  Backtest). 6 сильных сторон «не сломать» (детект грязи 208×, «Применить все»,
  авто-детекты, «Зачем шаг X?», честность, один CTA). Метод: Explore-субагент
  собрал фактуру (маршруты/gating/тексты/туры/термины/ошибки) + TEST_FINDINGS
  2026-06-02 (histор. находкам пометка [проверить] — код менялся). Приоритеты
  волны сведены в таблицу (жаргон+вердикты → онбординг → валидация-план Антону
  → ошибки → замер). Терминологию субагента в UX-1 перепроверяю грепами (40% FP).
