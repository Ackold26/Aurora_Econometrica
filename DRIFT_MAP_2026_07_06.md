# Карта класса дрейфа «UI считает/лейблит мимо SSOT» — 2026-07-06

> Собрана ДО фиксов (мета-совет приёмки 2026-07-05: чинить корень, не 22 симптома).
> 5 линз Explore (sonnet) + личная верификация опорных точек (Fable/Opus main).
> Полные отчёты линз: `tmp/drift_map_lens_{detectors,metrics,verdicts,export,text}.md`.
> Реестр симптомов приёмки: `TEST_FINDINGS_GUI_2026_07_05.md`. Промт: `NEXT_SESSION_econ_gui_fixes.md`.
> ✅ = проверено мной лично по коду. ⚠ = утверждение линзы, проверить при фиксе.

## Корень 1 — детекторы имён колонок (→ Пакет 1)

UI и python держат независимые копии логики распознавания. SSOT существуют:
JS `src/lib/channel-pairs.js` (parseChannelMetric, groupChannelColumns, PHYSICAL_SUFFIX_RE)
+ `src/lib/services/classifier-patterns.js`; PY `sidecar/econometrica/utils/column_detection.py`
(classify_column, export_patterns_as_json → Tauri cmd econ_classifier_patterns);
роле-детектор `engines/validator.py` (detect_column_role_with_confidence).

| Место | Дрейф | Закрывает |
|---|---|---|
| ✅ `ColumnMapperConfirm.svelte:199` isPhysicalMetric | нет CONTACT/КОНТАКТ (+6 паттернов SSOT) | F-A1-1 |
| ✅ `ColumnMapperConfirm.svelte:211` isMonetaryMetric | нет INVESTMENT/EXPENSE/ЗАТРАТ/СТОИМОСТЬ/USD/EUR | близнец (не пройден приёмкой) |
| ✅ `ColumnMapperConfirm.svelte:223` extractChannelRoot | hardcoded 23 префикса вместо parseChannelMetric | близнец: findPairedMetric vs buildExpressPlan разные базы |
| ✅ `ColumnMapperConfirm.svelte:498-516` kpiLikeRe | реко по regex-имени, col.kind игнорирован; F-003 hardening (2026-05-18) был полумерой — при активной KPI ловушка вернулась | F-A1-2 (category_sales → «исключите», OVB) |
| ⚠ `validator.py:21-44` MEDIA_PATTERNS | плоский substring vs sep-aware column_detection; promo/apteka/cpm/cpc/cpv/OTS только в validator | py-py дрейф, хронический (Д-1 патч закрыл часть) |
| ⚠ `validator.py:152-156` _CATEGORY_THEME дубль | копия внутри функции vs module-level column_detection | структурный риск |
| SSOT-пробел (оба языка) | прилагательные `рыночн*`/`конкурентн*` RU+EN не ловятся («рыночные продажи» → ложно KPI 0.7 — опаснейший) | F-A1-3 |

**Фикс-направление:** ColumnMapperConfirm удаляет 4 локальных детектора → читает parseChannelMetric/
groupChannelColumns + col.kind (альт.цель только kind ∈ {target_monetary, target_count}; kind=category →
позитивный бейдж «контроль спроса»). Прилагательные в ОБА SSOT + канарейка-корпус + мутации
(корпус-оракул обязателен). Русский «контакт» в PHYSICAL_SUFFIX_RE. py-py: validator наследует
паттерны column_detection (или явный re-export), не свой список.

## Корень 2 — производные метрики без SSOT-значения (→ Пакет 4)

| Метрика | Места | Дрейф |
|---|---|---|
| Ratio | ⚠ `insights-rules.js:359/401/1012` локальный rows/(media+control); backend validate НЕ отдаёт n_params_full (праздники 12 + Фурье) | 4.4 → 6.9 → 3.0 (F-A1-5). Корень: фронт не ЗНАЕТ эффективное число параметров до обучения |
| Период-единица | ⚠ `ModeDerivedExplanation.svelte:219` (у приёмки :360) хардкод «нед»+порог 52 | 48 мес = «48 нед < 52» (F-A1-6); granularity-логика существует (importInsights rows≤60) |
| Время обучения | ⚠ 3 формулы: ConfigPanel:241-251 (акт.), insights-rules:1095 (комментарий «синхронизирована» ЛОЖЕН), confirm-инсайт хардкод | F-A1-7; решение Антона: «10-30 сек» единый SSOT |
| Прогноз KPI подпись | ✅ `OptimizeStep.svelte:1822` хардкод «за весь период анализа»; displayKPI в planner-режиме реально пересчитан на плановый период; forecastConfig.periodLabel есть в сторе, не пробрасывается | Вероятный корень «обвала» F-A1-18 (проверить числами при Пакете 2!) + F-A1-17 |
| R² | ✅ один источник, разные .toFixed(3/4) | N-5, тривиален |
| MQS | ✅ образцово через metric-views.js mqsView() | ЭТАЛОННЫЙ ПАТТЕРН для остальных |
| avgROI | легитимная агрегация | не трогать |

**Фикс-направление:** backend отдаёт в validate/status эффективные SSOT-значения (n_params_full
с авто-инжектом, granularity, оценка времени); фронт-хелперы (по образцу metric-views.js) читают.
Подпись KPI из periodLabel.

## Корень 3 — вердикты и наследование качества (→ Пакет 4, частично каскад Пакета 2)

- ✅ `channel_action.py:234` `ratio<=0.95 && mroas>=1.0 → Reduce` — правило ЛЕГИТИМНО (docstring:25).
  **F-A1-19 «4🔴 перенасыщены» = КАСКАД F-A1-21:** proportional split → у ВСЕХ ratio=0.25 → все Reduce.
  После фикса планирования переоценить. Остаточные дефекты: (а) tooltip `OptimizeStep.svelte:481`
  «0.8-1.5=🟡» молчит про optimizer-signal → дополнить причиной вердикта; (б) label «перенасыщен»
  неверен для Reduce-по-масштабу (не по насыщению).
- ✅ `OptimizeStep.svelte:380` modelReliability = optData?.model_reliability → ДО оптимизации null,
  баннер ненадёжности физически не рендерится, светофор/mROAS уже видны (R-3).
- ⚠ `DecomposeStep.svelte` — 0 упоминаний model_reliability/MQS: денежные советы без дисклеймера (F-A1-12).
- ⚠ `aurora_html/sections.py` — model_reliability_verdict НЕ вызывается (только thinness_caveat):
  клиентский HTML молчит при r_hat≥1.05. PPTX читает honesty_verdict (норм).
- ⚠ `ratio-classifier.js:121` «высокая надёжность» при ratio≥6 — семантический конфликт с prior-coverage
  «надёжной модели НЕ получится» (ConfigPanel:778). Убрать «надёжность» из оси объёма (F-A1-9).

**Фикс-направление:** model-diagnostics.json = SSOT вердикта качества; DecomposeStep/OptimizeStep-idle/
aurora_html читают и показывают плашку; вердикты каналов объясняют причину (mROAS vs optimizer-signal);
язык осей развести («объём данных» ⊥ «согласованность с индустрией»).

## Корень 4 — клиентский текст (→ Пакет 5)

Из линзы 5 (`tmp/drift_map_lens_text.md`): 17 англицизмов (see methodology, Hill summation,
autocorr, OLS fallback, JAX/NumPyro, priors/posterior в туториале); 13 мест носителей в adstock
(5 утверждённых Антоном + 8 новых: ConfigPanel:582-590 tooltip, IntroTutorial:35, glossary:88/98/391);
2 шаблона-обещания без сверки (insights-rules:208/213 «конвертируются через CPP/CPM» при isDeclaredPair);
24 внутренних имени («Блок B/C/D/E», Forward solver, Goal-Seek v1.3). Python-сайдкар по носителям чист.
Экспорт (линза 4): ✅ PPTX `builder.py:1375` «Декомпозиция недельной динамики» хардкод при любой
гранулярности (HTML уже чинён через _period_unit()); HTML live англицизмы sections.py:1277/1283/1309-1310
(awareness, data quality, adstock decay); em-dash в trust loop PPTX:3187/3201/3213 + HTML:1780/1797;
wireframe-режим PPTX:2624-2643 с выдуманными числами (только is_live=False — проверить, не течёт ли в прод).

## Вне класса — точечные (свои пакеты)

- Оптимизатор-планирование F-A1-21/22/18/17 (`server.py:1434` proportional split вместо SLSQP;
  текст сдвигов не сверен со знаками дельт) → Пакет 2. INV-50-критично.
- Drill Декомпозиции F-A1-14/13 (`ChannelTimeline.svelte:566` option→EChartBase, notMerge/
  universalTransition; fullscreen resize) → Пакет 3. Нужен HMR+console.
- Демо-качество F-A1-10/20 (prior 44% FAIL, lift +0.0%; GROUND_TRUTH калибровка; гейт всех категорий) → Пакет 6.

## Каскадные связи (порядок имеет значение)

1. Пакет 2 (планирование → SLSQP) МЕНЯЕТ картину F-A1-19 (светофор) и F-A1-18 (KPI): сначала фикс, потом переоценка.
2. Пакет 1 и Пакет 5 независимы от всех — можно параллельно (разные файлы).
3. После ЛЮБОГО py-фикса — пересборка sidecar (stale-дубли dist/_internal подтверждены).
4. Ретест — на СВЕЖЕМ проекте (sticky-настройки MMX 0507-26 исказили прошлую приёмку).

## Верификационные пометки (личная проверка main-моделью)

Подтверждены построчно: ColumnMapperConfirm 4 детектора; channel_action.py:234 правило + docstring;
OptimizeStep:380/1822; builder.py:1375 (+stale-дубли). Скорректировано против линз: имя поля
`ratio` (линза 3 писала «optimizer_ratio» — по сути права, буквально нет); F-A1-19 переквалифицирован
в каскад. Не проверено лично (проверить при фиксе): validator.py MEDIA_PATTERNS детали,
insights-rules ratio-строки, n_params_full отсутствие в validate-ответе, wireframe is_live гейт.
