# Живая приёмка A (GUI) — находки, 2026-07-05

> Сессия Opus 4.8, живая приёмка Блок A промта `NEXT_SESSION_econ_live_recs_and_C.md`.
> Антон кликает, Маша инспектирует через MCP-мост (screenshot + DOM snapshot) и зонды.
> Клик через мост НЕ работает (координаты (0,0)) — навигацию ведёт Антон вживую.
> Файл: `synth_fmcg_brand` (4 пары spend+физ, 48 мес).

## 🔴 F-A1-1 — реко-бейдж пар рассинхронён с SSOT: `contacts` не физика в UI-слое

**Симптом (GUI, шаг Валидация → Роли колонок):** из 4 пар канала
(tv/digital/ooh/performance) программа честно свернула ТРИ — физ-половине бейдж
«Исключить: дублирует парный бюджет». OOH-пару пропустила: `ooh_contacts`
(1 529 171 795 контактов, без единицы измерения в объёме) стоит бейджем
«Оставить: колонка подходит для выбранной роли». Счётчик «исключим 3
промежуточных метрик» + кнопка «Оставить бюджеты (4 канала)» (3≠4). Инсайты
называют 3 пары (TV/DIGITAL/PERFORMANCE), OOH нет.

**Корень (подтверждён зондом + чтением, не глазами):** три независимых
распознавателя физ-метрики разошлись на суффиксе `contacts`:
- `validator.py` MEDIA_PATTERNS — есть `contact`/`контакт` → роль media ✓
- `channel-pairs.js` `PHYSICAL_SUFFIX_RE` — есть `contacts?` → `resolvePairSelection`
  корректно кладёт `ooh_contacts` в **disable** (зонд `tmp/probe_pairs.mjs`:
  enable=[4 spend], disable=[tv_trp, digital_impressions, **ooh_contacts**,
  performance_clicks]) ✓
- `ColumnMapperConfirm.svelte:199 isPhysicalMetric` regex
  `TRP|GRP|ПОКАЗ|IMPRESS|КЛИК|CLICK|ВИЗИТ|VISIT|OTS|VIEW|ПРОСМОТР|РЕЙТИНГ|REACH|ОХВАТ`
  — **НЕТ `CONTACT`/`КОНТАКТ`** → `ooh_contacts` не physical → `findPairedMetric`
  не находит пару → реко падает в дефолт «Оставить». ✗

Класс: нарушение SSOT (три копии логики суффиксов). `contacts`/контакты — реальные
OOH и аптечные/indoor-экраны (фарма) = media-KPI пар по реестру.

**Побочно замечено:** `channel-pairs.js PHYSICAL_SUFFIX_RE` имеет `contacts?` (англ),
но НЕ русское `контакт[ы]` — русская пробельная форма «наружка контакты» промахнётся
даже у SSOT-движка. Проверить.

**Тяжесть — РАЗРЕШЕНА как 🟠 КОСМЕТИЧЕСКАЯ (шаг «Метрики каналов», скрин Антона):**
экран показал «4 активных канала / 4 исключено», tv/digital/ooh/performance →
спенд в ₽. Арифметика: 8 медиа − 4 spend активных = 4 физ исключено (включая
ooh_contacts). Значит фактический план фита строит `resolvePairSelection` (знает
contacts) → в модель 4 канала, OOH НЕ задвоён. Баг F-A1-1 = только косметика:
реко-бейдж на Ролях врёт «Оставить» для ooh_contacts + счётчик «исключим 3» вместо
4 + инсайты 3 пары. Пользователь-эксперт заметит непоследовательность, но модель
корректна. Фикс всё равно нужен (доверие к UI), но не 🔴.

**Фикс (потом, субагентом):** заменить локальные `isPhysicalMetric`/`isMonetaryMetric`
в ColumnMapperConfirm.svelte на SSOT `parseChannelMetric` из channel-pairs.js
(устранить рассинхрон в корне, не латать суффикс). + русский `контакт` в
PHYSICAL_SUFFIX_RE. Обязательно юнит с парой на `contacts` (её не было в 10 юнитах
channel-pairs — реальный пример вскрыл дыру).

## 🔴 F-A1-2 — реко «Альтернативная цель» решает по regex-имени, игнорирует SSOT-kind

**Симптом (Роли колонок):** `category_sales` (роль Контрольная — ВЕРНО) получает
реко-бейдж «Альтернативная цель» + справка «Похоже на KPI… переназначьте роль,
иначе исключите». Следуя ей, пользователь теряет экзогенный контроль спроса
категории (Фаза Б) → молчаливый OVB: сезонность рынка спишется на рекламу, ROI
задерётся. Реко активно уводит от лучшей модели.

**Корень (`ColumnMapperConfirm.svelte:502-515`):** условие «Альтернативная цель» =
`hasActiveKpi && isNumeric && role≠kpi/media && kpiLikeRe.test(ИМЯ)` где
`kpiLikeRe=/продаж|sales|выручк|revenue|…/`. Смотрит на regex ПО ИМЕНИ, НЕ на
`col.kind`. SSOT (зонд) даёт `category_sales → kind=category, control 0.85` —
но реко это игнорирует. Тот же класс, что F-A1-1: реко-слой не наследует SSOT.
Побочно накрывает `competitor_sales`/«Продажи конкурентов» (kind=signed_competitor,
control 0.9) — их тоже нельзя толкать на исключение (замечание Антона: продажи
категории и продажи конкурентов — один класс, рыночный/конкурентный спрос как
контроль, на 2 языках).

**Фикс (потом, субагентом):** условие «Альтернативная цель» перевести с regex-имени
на SSOT-kind — помечать только `kind ∈ {target_monetary, target_count}`. Тогда
настоящая вторая KPI бренда ловится, а именованные контролы (category/competitor/
price/macro) — нет. Бонус: для `kind=category` позитивный бейдж «Оставить: контроль
спроса категории — стабилизирует сезонность». `col.kind` уже приходит в columns
(строка 24).

## 🔴 F-A1-3 — SSOT category/competitor не ловят ПРИЛАГАТЕЛЬНЫЕ формы (корпус-зонд)

**Метод — корпус-оракул (замечание Антона «продажи конкурентов, 2 языка» → зонд
20+ форм RU/EN):** 20 форм ловятся верно (category/signed_competitor → control),
но ДВЕ проваливаются:
- 🔴 **`рыночные продажи` → classify=unknown, validator role=KPI 0.7.** «рыночн…»
  НЕ в `_CATEGORY_THEME` (категори/рынок/market/category — существительные).
  Итог опаснейший: SSOT сам считает это целевой метрикой → «Альтернативная цель»,
  а переназначение по совету = модель объясняет РЫНОК вместо бренда. Реко-фикс
  F-A1-2 НЕ спасёт (kind не category, role=kpi ещё до реко).
- 🟠 **`конкурентные продажи` → classify=unknown, validator role=control 0.9.**
  Рассинхрон Д-1 (validator знает, classify нет) → приоры/карта корреляций могут
  разойтись с ролью.

**Фикс (потом, субагентом):** расширить SSOT-паттерны прилагательными формами RU+EN:
`_CATEGORY_THEME` += `рыночн[а-яё]*`, `market[_\s-]?wide`?; competitor-паттерны +=
`конкурентн[а-яё]*`. ОБЯЗАТЕЛЬНО канарейка-корпус этих форм (test_client_names…)
+ мутация (убрать токен → тест краснеет). Анти-ложное: «рыночная цена» (price, не
category), «конкурентоспособность» (не sales). Прогнать ранее исключённые ловушки.

## 🟢 F-A1-4 — УЛУЧШЕНИЕ (запрос Антона): адаптивная подсказка на шаге «Метрики каналов»

**Идея Антона:** шаг «Метрики каналов» в happy-path (все медиа в ₽) — пустой:
делать нечего, но пользователь не понимает «что тут от меня». Дать состояние-
зависимую подсказку: всё в ₽ → «переводить ничего не нужно, можно пропустить»;
есть физ без ₽ → «здесь укажите цену единицы (CPP/CPM) для перевода в рубли».

**Факт:** механизм ЧАСТИЧНО есть — `validateMetricsInsights` (insights-rules.js,
покрыт R3-тестами) уже даёт инсайт «Все N каналов готовы: бюджеты в ₽ или
физические с конверсией. Можно идти к подтверждению». Но формулировка слабая/
двусмысленная («или физические с конверсией», когда физ нет — сбивает).

**Рекомендация (моя, Антон согласовывал направление):**
- Подсказку — НА ЭКРАНе, в карточке «Применённый режим» (место действия, не правая
  панель инсайтов). Инсайт-строку оставить кратким эхо, переписать под состояние.
- Тексты состояние-зависимо:
  - все ₽: «Все медиаканалы уже в рублях — переводить ничего не нужно. Нажмите
    «Далее».»
  - есть физ без ₽: «Каналы [список] в физических метриках (TRP, показы). Укажите
    цену за единицу (CPP/CPM) — модель переведёт в рубли и посчитает ROI. Без этого
    канал останется без оценки отдачи.» (последствие = honest-UX)
  - смешанный: «N готовы (₽), M требуют цену единицы: [список]».
- НЕ авто-скипать шаг (прозрачность + вход в режим эксперта тут). НЕ дублировать
  текст экран↔инсайты (экран полный, инсайт короткий).
**Реализация (потом, субагентом):** ветка состояния в ColumnMapperConfirm/шаг
«Метрики каналов» (компонент AppliedModeSummary?) + правка формулировки
validateMetricsInsights. Vitest на 3 состояния (все₽/физ-без-₽/mixed).

## 🔴 F-A1-6 — единица периода «нед» захардкожена, игнорирует месячную гранулярность

**Симптом (Подтверждение → Контроль качества):** «Период 48 нед · < 52 нед», бейдж
тревоги. Но данные МЕСЯЧНЫЕ: «48 наблюдений с 01.2022 по 12.2025» = 48 месяцев =
**4 года**. Программа обозвала 4 года «48 недель < года» → ложная тревога «мало
данных» на отличном датасете. Подрывает доверие к оценке качества (core-ценность).

**Корень (`ModeDerivedExplanation.svelte:360-361, 188, 216`):**
`{nObs + ' нед'}` + `periodWarn = nObs>0 && nObs<52` + бейдж «<52 нед» — хардкод
единицы и порога БЕЗ гранулярности. SSOT-хелперы существуют и НЕ используются:
`ruPeriodForm(nPeriods)` (ReportStep.svelte:346), гранулярность-логика
(insights-rules.js importInsights NUM-1: rows≤60 → месяцы). Тот же класс, что
F-A1-1..3: компонент держит локальный лейбл мимо SSOT.

**Фикс (потом, субагентом):** единицу и порог периода из гранулярности
(месячные → «N мес», порог ~24; недельные → «N нед», порог 52; или в наблюдениях).
Через существующий period-helper, не хардкод. Vitest на месячный/недельный кейс.

## 🟠 F-A1-5 — Ratio скачет между под-шагами Валидации (не-SSOT метрика)

**Симптом:** вход Валидации «Ratio 4.4:1 приемлемо», Подтверждение «Ratio 6.9:1
Идеально». Скачок без объяснения. Арифметика: 48/11=4.4 (вход считал по 8 медиа-
ролях + 3 контроля, ДО свёртки пар), 48/7=6.9 (подтверждение: 4 spend + 3 контроля,
ПОСЛЕ свёртки). На входе показано ЗАНИЖЕННОЕ пугающее 4.4, хотя реальная модель = 7
переменных → 6.9. Класс — Ratio считается по разной базе на разных экранах (не-SSOT).

**ОБНОВЛЕНО (обучение, avt_08): ТРИ разных ratio на пути!** вход 4.4 → pre-train 6.9
→ **post-train «эффективный» 3.0** (плашка результата + инсайт). Разгадка: pre-train
6.9 = 48/7 (4 медиа + 3 контроля), НЕ учитывал 12 авто-праздников + сезонность,
которые РЕАЛЬНО в модели. Post-train 3.0 их учитывает = ЧЕСТНОЕ значение.
🔴 Канон = ЭФФЕКТИВНЫЙ post-train 3.0, НЕ 6.9 (мой прошлый вывод «6.9» — НЕВЕРЕН,
отозван). Хуже: pre-train кричит «6.9 идеально», реальная модель «3.0 ниже
рекомендованного» → пользователя ложно обнадёживают ДО обучения. Фикс: pre-train
ratio должен учитывать авто-инжект (праздники + сезонность), т.е. предсказывать
эффективное число параметров модели, не только видимые роли. SSOT = effective ratio.

## 🧭 СИСТЕМНЫЙ ВЫВОД приёмки A1 (мета-класс всех находок)

F-A1-1..6 — ОДИН корневой класс: **компоненты pipeline держат СОБСТВЕННЫЕ
локальные вычисления/детекторы/лейблы, рассинхронённые с SSOT.**
- физ-метрика: `ColumnMapperConfirm.isPhysicalMetric` без `contacts` ≠ channel-pairs SSOT (F-A1-1)
- «альтернативная цель»: regex по имени ≠ col.kind SSOT (F-A1-2)
- category/competitor: SSOT не ловит прилагательные (F-A1-3)
- ratio: по непросвёрнутым парам ≠ по модельным переменным (F-A1-5)
- период: хардкод «нед» ≠ granularity-helper (F-A1-6)
Пакетный фикс = «UI-слой читает SSOT, а не пере-угадывает». Один субагентский заход
на все + канарейки. Это же — доказательство ценности живой приёмки: юниты каждого
слоя зелёные, рассинхрон видно только на реальном прогоне (наш урок «палец по цепочке»).

## 🟠 F-A1-7 — оценка времени обучения расходится между экранами (5–10×)

**Симптом:** Подтверждение (validateConfirmInsights) — «Обучение займёт 30–60 секунд
(Bayesian) или ~5 секунд (OLS)». Шаг Модель (ConfigPanel estimateMinutes) — «Оценка:
~5 мин (4 канала)». Один прогон, две несовместимые оценки. Класс не-SSOT: хардкод
«30–60 сек» в confirm-инсайте vs формула estimateMinutes в ConfigPanel. Плюс третья
формула в modelPreTrainingInsights (`0.3*mediaCount+1`≈2 мин, скрыта при ≤5 каналов).
**Фикс:** единый источник оценки времени (одна формула), синхронно на всех экранах.
**ФАКТ (avt_08): обучение заняло ~10 СЕК** (4 канала, fmcg). Обе оценки завышены:
«~5 мин» в 30×, «30-60 сек» в 5×. Решение Антона: диапазон «10-30 сек» (с запасом на
слабые машины/больше каналов). Применить как SSOT-оценку на всех экранах.

## 🟢 F-A1-11 — ФИЧА (идея Антона): управляемый выбор праздников + прозрачность ratio

**Идея Антона (2026-07-05, двухчастная):**
1. ДО обучения — дать пользователю выбрать актуальные для его категории праздники,
   с предупреждением «каждый праздник снижает ratio при обучении». В модель попадут
   не все 12, а ключевые → сюрприза ratio нет + чистим нерелевантные праздники.
2. ПОСЛЕ обучения — инсайт: «праздники снизили ratio с 6.9 до 3.0; для сохранения
   ratio выключите все или часть праздников».

**First-principles (почему верно):** каждый праздник-контрол = OVB vs df trade-off.
Влияющий оставить (иначе эффект → на рекламу, ROI завышен); пустой убрать (зря съел df,
ratio↓). Идеальный набор = только влияющие. Экспертный prior пользователя (какие
праздники важны для категории) система не знает — дать выбор. Тот же принцип, что F-A1-8.
Закрывает КОРЕНЬ F-A1-5 (сюрприз 6.9→3.0), смягчает F-A1-9/F-A1-10 (проще модель).

**🔴 ОБЯЗАТЕЛЬНЫЙ ПРЕДОХРАНИТЕЛЬ (red-team, иначе фича навредит):** выбор по ПАРЕ
сигналов (влияние + Δratio), НЕ только ratio. Иначе пользователь выключит важный
праздник ради цифры → OVB (эффект праздника уедет в ROI рекламы). Ratio = последствие,
не критерий. Усиление: post-train у нас ЕСТЬ вклад каждого праздника (holiday в
декомпозиции/decomposer breakout) → инсайт даёт точную рекомендацию per-праздник
(«X: вклад ~0% → выключить, +0.3 ratio; Y: 8% в декабре → оставить»). До обучения —
сигнал через всплеск KPI в окне праздника по истории. Дефолт = авто-отбор по данным,
НЕ «все 12».

**Инфра ГОТОВА (grep подтвердил):** `disabled_holidays` (per-праздник) + `use_holidays`
(мастер) в `modeler.py:305,322`; фронт `ConfigPanel.svelte:473` (disabled) + панель
«Авто-праздники РФ» (сейчас спрятана в Эксперт-режиме). Нужно: (а) вынести панель в
обычный режим первоклассно; (б) per-праздник data-signal (всплеск в истории) + прогноз
Δratio; (в) live-пересчёт ratio при вкл/выкл; (г) post-train инсайт с фактом Δratio +
per-праздник вкладом из декомпозиции + сбалансированной рекомендацией + actionable
«пересобрать без пустых праздников». Крупный трек → кандидат в отдельное тех-задание.

## 🟠 F-A1-12 — вердикты декомпозиции не наследуют дисклеймер ненадёжности модели

**Симптом (Декомпозиция, avt_09):** модель на шаге Модель честно помечена «ненадёжной»
(prior coverage 44%, R² «признак переобучения», 2 MCMC-дивергенции). На Декомпозиции
инсайты дают КАТЕГОРИЧНЫЕ советы без оговорки: «Убыточный канал: tv_spend ROI 0.58× –
расходы превышают вклад», «performance 2.4× выше ожидания – докрутить бюджет на
Оптимизации». Дисклеймера «модель ориентировочная / переобучение» на экране нет.
Опасно: пользователь по переобученной модели реально урежет TV / перельёт в
performance. Класс: вердикт качества (ненадёжность) не доезжает до слоя рекомендаций
(тот же мета-класс — слой не наследует состояние модели). СТАТУС: проверить «Что делать
дальше»/шапку — точно ли нет дисклеймера нигде (кандидат).
**Фикс:** при MQS «ориентировочно»/низком prior-coverage — плашка-дисклеймер в
декомпозиции/оптимизации/отчёте (вердикты подавать с пометкой «на ненадёжной модели»).
**ПОДТВЕРЖДЁН НА ОПТИМИЗАЦИИ (avt_11, деньги!):** инсайты «бюджет 906 027 622₽, средний
ROI 0.98×», «оптимизатору некуда переливать (~0%)» — БЕЗ дисклеймера ненадёжности.
Самое опасное место цепочки: пользователь по переобученной модели перекроит 906 млн ₽.
Из кандидата → ПОДТВЕРЖДЁН. Дисклеймер здесь обязателен в первую очередь.

## 🟠 F-A1-15 — англицизмы и технический жаргон в клиентском UI (Оптимизация)

**Симптом (avt_11):** client-surface hygiene протекла:
- «Прогноз… — **see methodology**» — англ. фраза в русском UI (→ «см. методологию»).
- «оптимизатор переключился на планирующий режим (**per-period Hill summation, 3-way
  alignment с scenario engine**)» — англо-технический жаргон, менеджеру нечитаемо.
- «**autocorr** 0.64», «**Блок D**» — сокращённый термин + внутренняя ссылка без пояснения.
Целевой пользователь — менеджер-маркетолог, не эконометрист. Класс: технический жаргон/
англицизмы в клиентских подписях (тот же client-surface принцип, что F-A1-8).
**Фикс:** человеческий язык. «see methodology»→«см. методологию»; жаргон режима →
«оптимизатор подбирает распределение помесячно с учётом насыщения каналов»; «autocorr»→
«годовая сезонность (сила 0.64)»; «Блок D»→понятная ссылка. Грепнуть весь pipeline на
англицизмы/жаргон (Hill/scenario/alignment/methodology/autocorr/Блок).

## 🟠 F-A1-16 — предложенный «Бюджет периода» планирования, возможно, кратно завышен

**Симптом (avt_11, Оптимизация):** предложенный бюджет года планирования = **800.7М ₽**.
Историческое медиа = 906М за 48 мес (4 года) → годовой ≈ 226М. Предложенный **в ~3.5×
выше** исторического годового. Приходит из `forecastConfig.budgetMoney` (backend forecast-
контекст, `OptimizeStep.svelte:494 planningBudgetMoney`), не локальный расчёт.
**Риск:** менеджер спланирует год на 800М вместо реальных ~226М — кратное завышение плана.
**КОРЕНЬ ФОРМУЛЫ (ForecastHorizonPicker.svelte:87-89):** `suggestBudget = currentBudgetMoney
× (periods/trainNPeriods)`. Правильно для года: 906М × 12/48 = **226М**. Показано 800.7М —
НЕ выводится ни из 48 (=226М), ни из fallback-52 (=209М). Две версии, различаются кнопкой
«Сбросить к предложенному»:
- (вероятнее) **sticky manual budget существующего проекта** MMX 0507-26: флаг
  `budgetManuallyEdited` (стр.83, F-018) липкий → старый ручной ввод НЕ пересчитывается
  пресетами, показывается как «предложенный». UX-баг: старое значение неотличимо от свежего
  suggest. Фикс: пометка «ваше значение» vs «предложено (226М)» + подсказка сбросить.
- (иначе) **баг trainNPeriods** (granularity 52/12: fallback=52 недели, данные месячные) —
  если trainNPeriods≈13.6, формула завышает. Проверить, чему равен trainNPeriods из
  forecast_context для месячных.
ТЕСТ: нажать «Сбросить к предложенному» → ~226М = sticky-legacy; завышено = баг формулы.
**РАЗРЕШЕНО (Антон нажал): → 226 506 906 ₽ = ТОЧНО прогноз 906027622×12/48. Формула
ВЕРНА.** 800.7М был sticky manual budget существующего проекта MMX (budgetManuallyEdited
из прошлой сессии), показанный как «предложенный». Переклассификация: НЕ баг расчёта, а
🟠 UX — sticky-manual неотличим от свежего suggest, может кратно отклоняться. Фикс:
пометка «ваше значение (изменено) · предложено 226.5М» + подсказка сбросить. Метод
«предскажи число → проверь нажатием» сработал (точное совпадение).

## ✅ Блок A (Текущий бюджет) — разобран, чисто
Общий бюджет 906М · Прогноз KPI 8057.6М (= ИТОГО декомпозиции, SSOT-консистентность ✓) ·
Средний ROI 0.98× (честно, <1 = на грани) · Светофор 1/2/1 (согласован с miROAS-таблицей).
Tooltips подробные (сильная сторона). Сквозное замечание — дисклеймер ненадёжности (F-A1-12).

## 🔴 F-A1-17 — рассинхрон бюджета в Блоке B (планирование 226.5М vs распределение 906М)

**Симптом (Блок B, скрин):** плашка «Оптимизирую бюджет 226 506 906 ₽ на 12 периодов
(Год)» — плановый год. НО «РАСПРЕДЕЛЕНИЕ БЮДЖЕТА»: «Общий бюджет 906.0M ₽ 🔒» + слайдеры
каналов = 906М (119.9+130.8+269.5+385.8, весь период 48 мес). Инсайт: «бюджет 906 027 622₽».
Один экран смешивает горизонты: оптимизирую 226.5М (год) vs распределяю 906М (весь период).
Пользователь не поймёт, что реально оптимизируется. В режиме планирования слайдеры логично
должны быть в плановом бюджете (226.5М), не историческом (906М). Класс: не-SSOT + смешение
«год ↔ весь период». Фикс: в планировании слайдеры/общий бюджет/инсайт — в плановых 226.5М
(или явно пометить «текущее историческое распределение → применится к плановому году»).

## 🔴 F-A1-18 — Прогноз KPI ОБВАЛИЛСЯ после оптимизации при +0.0% приросте

**Симптом (после «Оптимизировать бюджет»):** Блок A Прогноз KPI был 8057.6M ₽ →
стал **5076.4M ₽** (−2981М, −37%). При этом «Прирост итогового KPI +0.0%», бюджет тот же
906М. Абсурд: оптимизация без изменения бюджета и с нулевым приростом уронила прогноз на
3 млрд. Гипотезы: (а) баг пересчёта Прогноз KPI после оптимизации; (б) значение стало
плановым годом (226.5М-бюджет → меньший KPI), а подпись осталась «за весь период анализа»
(врёт). Смешение горизонтов (родня F-A1-17). Пользователь видит «оптимизация уронила
прогноз на 3 млрд» → катастрофа доверия. Проверить: что считает Прогноз KPI до/после опт.

## 🔴 F-A1-19 — светофор насыщения противоречит miROAS/инсайтам

**Симптом:** после оптимизации Светофор насыщения = 0🟢/0🟡/**4🔴** (все перенасыщены),
инсайт «4 из 4 перенасыщены». НО инсайт рядом «лучший performance_spend **3.86×**», а
3.86× по tooltip = mROAS>1.5 = недонасыщен/масштабировать. Канал не может быть и
«перенасыщен» (4/4 red), и «масштабировать». Светофор и miROAS считают по разным
определениям/данным. Внутреннее противоречие вердиктов на одном экране (мета-класс не-SSOT).

## 🟠 F-A1-20 — демо-пример не демонстрирует ценность оптимизации (+0.0% lift)

**Симптом (наблюдение Антона):** оптимизация образцового synth_fmcg даёт +0.0% прироста —
продукт выглядит бесполезным на СОБСТВЕННОМ демо. Корень (родня F-A1-10): генератор даёт
данные где все каналы в плато + модель ненадёжна (prior 44%) → оптимизировать нечего.
**Требование:** демо-примеры ОБЯЗАНЫ показывать ценность. GROUND_TRUTH в
`tools/synthetic_pilot_data.py` калибровать под ЗАВЕДОМО субоптимальный старт (перекос
бюджета от оптимума) → оптимизация находит видимый lift (+15-30%). Иначе первое впечатление
клиента: «оптимизатор ничего не даёт». Связать с F-A1-10 (prior-check демо) — единый трек
«демо-файлы как образец: проходят prior + дают оптимизационный lift». Все категории.

## ⚠️ F-A1-20 УТОЧНЕНИЕ + sticky-настройки существующего проекта (родня F-A1-16)

**Тест «расширить границы 10/300» НЕ подействовал:** у 4 каналов заданы per-channel
Мин/Макс overrides (из существующего проекта MMX 0507-26, прошлая сессия), перекрывающие
глобальные. Плашка ЧЕСТНО предупредила «глобальные не применяются» (👍 прозрачность —
сильная сторона). Следствие: **+0.0% может быть артефактом sticky-overrides, НЕ плато** →
F-A1-20 под вопросом до чистого теста (сброс overrides + реопт). F-A1-18 держится (KPI
5076.4 не вернулся к 8057.6).
**Наблюдение (класс sticky-состояния, родня F-A1-16):** существующий проект тащит липкие
настройки прошлой сессии — бюджет (800.7М) И per-channel ограничения (4 канала). Приёмка/
работа на «загрязнённом» состоянии → выводы искажаются. Урок для приёмки: для чистого
теста оптимизатора — новый проект ИЛИ явный сброс. Урок для продукта: пометить, что
настройки унаследованы из прошлой сессии (не свежие) + лёгкий «сброс к дефолту всего шага».

## 🔴🔴 F-A1-21 — оптимизатор игнорирует явный сигнал перераспределения (советы ⊥ результат)

**Симптом (чистый тест: overrides сброшены, границы 10/300, avt SCR):** +0.0% прироста,
и оптимизатор урезал ВСЕ каналы ровно на −75% (tv/digital/ooh/performance все −75%,
пропорции не тронуты). Это НЕ перераспределение — смена масштаба 906М (весь период) →
226.5М (год) = ×0.25 = −75% всем. НО все сигналы кричат перераспределить:
- response curves: performance крутая/недонасыщена (точка на подъёме), tv пологая/насыщена;
- miROAS: performance 3.86× (масштабировать) vs tv 0.80× (сократить), разброс 4.8×.
При границах 10/300 и таком разбросе mROAS оптимум ТОЧНО не в равных пропорциях —
оптимизатор обязан перелить tv→performance, но дал +0.0% и равное урезание.
**Противоречие:** продукт ОДНОВРЕМЕННО «performance недонасыщен, масштабируй» И «+0.0%,
менять нечего». Либо баг целевой функции оптимизатора (не видит выигрыш перекладки), либо
оптимизирует интеграл за весь период вместо маргинала — для пользователя удар по доверию.
Проверить: SLSQP objective (server.py optimize) — что максимизирует; почему не двигает
пропорции при разном mROAS. Раскрыть «Главные сдвиги бюджета» для факта per-канал.
🔴 Самая серьёзная находка Оптимизации (ядро ценности продукта — перераспределение —
под вопросом). Связать с F-A1-18 (KPI обвал) + F-A1-17 (горизонт).

**ПОДТВЕРЖДЕНО раскрытием «Главные сдвиги» + локализация в коде:** ВСЕ 4 канала −75%
(performance −89.9М, digital −202.1М, tv −289.3М, ooh −98.1М), включая недонасыщенный
performance. Никакой перекладки. Код: `server.py:1434` «Use PROPORTIONAL SPLIT of
forecast_budget_money matching training proportions» — планирование делит бюджет
пропорционально ТЕКУЩИМ пропорциям, не через SLSQP-перераспределение. Отсюда −75% всем.
Гипотеза: режим планирования → пропорциональный scaling вместо оптимизации. Разделяющий
тест (режим «Анализ» → перераспределяет ли) подтвердит. Читать optimize_budget:1201 +
optimizer.py: запускается ли реальный SLSQP в planning или возвращается proportional split.

**РАЗДЕЛЯЮЩИЙ ТЕСТ ЗАВЕРШЁН (avt_12, режим «Анализ»): ОПТИМИЗАТОР ИСПРАВЕН.** В Анализе —
реальное перераспределение: performance_spend +99% (+119.2М), ooh −20%, tv −15%, digital
−13%; рекомендация «переложи 58.1М tv→performance»; +1.7% итог / +13.3% медиа; текст
перекладки ПРАВДИВ (есть + и −); честные оговорки (экстраполяция performance за истор.
максимум; media=13% KPI → рычаг ограничен). ⇒ **F-A1-21/F-A1-22 = баг ТОЛЬКО режима
«ПЛАНИРОВАНИЕ»** (дефолтный + главный: клиент планирует будущий бюджет). Планирование
масштабирует (−75% всем, proportional split server.py:1434) вместо SLSQP. Ядро ценности не
работает в дефолтном режиме + ложный текст. 🔴 НАХОДКА №1 приёмки, приоритет фикса.
Фикс: режим планирования должен запускать SLSQP-перераспределение на плановом бюджете
(как Анализ), не proportional split. Плюс 🟠 в Анализе Прогноз KPI 9124.2М (=+13.2% от
8057.6, media-scaled) ≠ подпись «+1.7%» (итог) — рассинхрон подачи метрики (родня F-A1-18).

## 🔴🔴🔴 F-A1-22 — инсайт «Главные сдвиги» ЛЖЁТ про перекладку

**Симптом:** подпись под сдвигами «Перекладка идёт из перенасыщенных каналов в
недонасыщенные — где каждый рубль ещё работает на полную». ФАКТ: все 4 канала −75%,
ни один не получил «+», недонасыщенный performance тоже срезан. Текст-шаблон описывает
перераспределение, которого НЕ БЫЛО, и не сверяется с фактическими цифрами. Ложное
утверждение в клиентском отчёте (хуже путаницы). Класс: captation-текст не валидируется
против данных (родня N-4/F-A1-8 — текст мимо факта). Фикс: текст сдвигов генерировать
ИЗ фактических per-канал дельт (есть «+» и «−»? тогда «перекладка»; все «−» одинаково?
тогда «масштабирование под плановый период, пропорции сохранены»). + гейт: текст-вывод
сверять со знаком дельт.

## Блоки C/D/E Оптимизации + обновление F-A1-19

**F-A1-19 обновление (miROAS-вердикты нестабильны):** digital 1.27× / ooh 1.08× помечены
«🔴 Сократить умеренно» — но раньше (Блок B) были «🟡 Удерживать»/«Под наблюдением», и по
определению tooltip (0.8-1.5× = 🟡 стабильно) должны быть жёлтыми, не 🔴. Вердикты не
следуют своему определению + меняются между состояниями. Часть светофор/вердикт-рассинхрона.

**Блок C (Что-если):** 👍 честная Hill-оговорка (затухающий прирост / нелинейный спад),
ЭКСПЕРТ-инфляция грамотно. 🟠 масштабирует «оптимизированные пропорции» — в Планировании они
НЕ оптимизированы (F-A1-21) → масштабирует неоптимальный микс.
**Блок D (Сценарный анализ):** 🟠 заголовок «что будет, если изменить бюджет канала на N%»
≠ реальные функции (Сохранить текущее/оптимум, Сравнить сценарии). Подпись мимо содержимого.
**Блок E (Сбывшиеся рекомендации):** 👍 сильная концепция проверяемых прогнозов (доверие).
🟠 ирония: «Зафиксировать прогноз» — в режиме Планирование (сломан F-A1-21) → фиксирует
неоптимальный прогноз. После фикса F-A1-21 заработает как задумано.

## 👍 Хорошее на Оптимизации (не только дефекты)
Честные методологические оговорки: инфляционная плашка (multi-year, денежные оценки
средние по training, CPP/CPM 25-30%/год — используйте свой бюджет); «прирост ~0%, некуда
переливать» (ROI 0.98 честно, без золотых гор); сезонность период=12 обнаружена.

## ✅ A2 подтверждён зелёным (Декомпозиция): водопад тождество (7167.9+вклады=8057.6),
4 группы в легенде (База/Медиа/Внешние/Конкуренты) + сезонность %-кривая на 2-й оси,
Расходы-vs-Эффект наглядно. Морф universalTransition (drill) — не проверен (нужен клик).
A3 (праздники) — свёрнуты в БАЗА, нужен drill «База» для проверки непустоты на месячном
фите. A4 (бейдж пар heatmap) — карта корреляций на пути не открывалась (на Валидации).

## 🔴 F-A1-14 — drill «Динамика по периодам» не раскрывает группы (блокирует A2 морф + A3 праздники)

**Симптом (avt SCR, fullscreen):** клик по chip «База» (data-drill="БАЗА") даёт фокус,
но график не перестраивается — БАЗА не раскрывается на baseline/сезонность/праздники.
DOM: `aria-pressed="false"`, каретка `▸` ПОСЛЕ клика → `toggleGroup` не выполнился
(иначе expanded.has → pressed=true). Компонент `ChannelTimeline.svelte` (toggleGroup:98,
expanded $state:80 — код корректен). Блокирует A2 (морф universalTransition) И A3
(праздники внутри БАЗА, через GUI не увидеть).
**ЛОКАЛИЗАЦИЯ ФИНАЛЬНАЯ (диагностика приёмки, потолок без runtime):** сломан ВЕЗДЕ
(не fullscreen — тест Антона подтвердил). Цепочка исправна ВСЯ, кроме последнего звена:
- backend `build_decomposition_series` (decomposer.py:481-498) КЛАДЁТ членов: baseline +
  каждый медиа-канал отдельно + bands (holiday=«Праздники», seasonality=«Сезонность»,
  signed-факторы), все с `top_group`. Данные полные (не агрегаты).
- `collapse_series_to_top_groups` — только в HTML/PPTX отчётах, НЕ в GUI.
- `planViewSeries`/`topGroupOf` (decomposition-view.js) корректны (expanded → члены).
- `option` $derived (ChannelTimeline.svelte:566) читает expanded через buildCanonicalOption
  → planViewSeries — зависимость по коду верна.
- chips РЕАГИРУЮТ: DOM показал Медиа+Внешние `.active` (expanded меняется, aria-pressed
  true, caret ▾). Клик НЕ мёртв (мой прежний вывод «клик не срабатывает» — ОТОЗВАН).
- echarts ЖИВ: legend-пиктограммы toggle работают (Антон подтвердил).
🔴 РАЗРЫВ: смена expanded не отражается на графике (avt_10: 2 группы expanded, но те же
4 агрегатные полосы, легенда 4 группы, tooltip 4 агрегата). Звено `option`→**EChartBase**
(setOption `notMerge` — стр.433 / `universalTransition` морф) для DRILL. legend-путь
отдельный → жив. Нужен runtime (console: пересчитывается ли option? применяет ли
EChartBase? удаляет ли notMerge старые серии?) — `execute_js` мост timeout-ит, это
потолок приёмки. → СУБАГЕНТУ с HMR+console (точный адрес: EChartBase apply / universalTransition).

## 🟠 F-A1-13 — fullscreen «Динамика по периодам» занимает ½ высоты
Развёрнутый на весь экран график сидит в верхней половине, низ пустой — echarts не
resize на высоту контейнера в полноэкранном режиме. Вероятно один баг с F-A1-14.

## A3 (праздники на месячном фите) — проверяется ОБХОДОМ (GUI-drill сломан F-A1-14)
Раз GUI не даёт раскрыть БАЗА — методологию (праздники непустые на месячном фите после
календаря v2.1/v2.2, декабрь/ноябрь несут вклад) проверяю зондом на свежем pkl, не через
GUI. Разделение: методология (backend) ⊥ визуализация (GUI-drill). Статус: в работе.

## Наблюдение N-5 (мелочь): R² тремя числами
Результат обучения показывает R² тремя значениями: плашка «93%», инсайт «0.931»,
график «0.935». Рассинхрон округления/источника — свести к одному (SSOT метрики R²).

**Уточнение F-A1-5 (локализация):** Модель тоже показывает Ratio 6.9:1 (инсайт
modelPreTrainingInsights). Итого: ВХОД Валидации = 4.4 (по 11), Подтверждение +
Модель = 6.9 (по 7). Рассинхрон в ОДНОМ месте — под-шаг входа Валидации
(validateInsights/validationHeaderMetrics по непросвёрнутым ролям). Канон = 6.9.

## 🟢 F-A1-8 — убрать привязку adstock-описаний к НОСИТЕЛЯМ (решение Антона)

**Решение Антона (2026-07-05, повышено из N-4):** во всех описаниях adstock убрать
упоминание конкретных носителей (TV/OLV/digital/performance/OOH/радио). Причина —
медиаландшафт дрейфует (OLV уже практически неотличим от TV; connected TV,
programmatic размыли границу), а привязка к носителю зашивает устаревающий прокси.
Adstock — свойство ФОРМЫ отклика (быстрый/долгий), не бренда носителя; BIC и так
подбирает форму по ДАННЫМ. Описывать инвариант (природу эффекта) словами, без
носителей: «часть медиа даёт быстрый эффект, но быстро заканчивается; часть —
пролонгированный, но затухает долго».

**Смежная под-находка:** `ConfigPanel:593` и `insights:1036` пишут «авто-выбор ПО
ИМЕНИ/названию канала», но экран показал «авто по BIC» (по данным). Текст описывает
устаревшую эвристику — тоже переписать на «по данным».

**Карта правки (5 файлов, потом субагентом):**
- `ConfigPanel.svelte:187` — dropdown label «Авто (цифровые — мгновенный, ТВ —
  отложенный)» → «Авто — система подберёт тип отклика для каждого канала по вашим данным».
- `ConfigPanel.svelte:593` — «по названию (TRP/OOH→Weibull, digital→Geometric)» → «по данным».
- `insights-rules.js:1035-1036` (modelPreTrainingInsights) — убрать носители, дать
  природу: «Тип отклика подбирается по данным каждого канала. Часть каналов даёт
  быстрый эффект — почти сразу, но и затухает быстро. Другие работают вдолгую —
  нарастает постепенно и держится дольше.» tip: «Система оценивает форму отклика из
  истории и выбирает модель затухания сама.»
- `IntroTutorial.svelte:25` — «TV 8-12 недель, Performance 1-3» → природа без носителей.
- `glossary.js:87,96,135` — примеры «ТВ 4-8 недель» смягчить до «охватные каналы обычно
  работают дольше» (вторично — обучающий контекст prior, не авто-выбор).
Термины Geometric/Weibull оставить (математика, не носитель). ✅ ФОРМУЛИРОВКА
УТВЕРЖДЕНА Антоном 2026-07-05 (применить при фиксе как выше). Vitest на insights-текст.

## 🔴 F-A1-9 — конфликт вердиктов надёжности на одном экране (Модель)

**Симптом (скрин avt_07):** красная плашка «Проверка данных: надёжной модели на этих
данных НЕ получится · покрытие приоров 44% < 50% · результаты ненадёжные» ОДНОВРЕМЕННО
с инсайтом «Ratio 6.9:1 — идеально · высокая НАДЁЖНОСТЬ · оптимальный объём». Лобовое
противоречие на пороге «Запустить модель».

**Корень:** две РАЗНЫЕ оси качества, обе названы «надёжностью». Ratio = объём
(наблюдений/переменную) — ок. Prior coverage = согласованность индустриальных priors с
данными — плохо (отклик нетипичен). Ratio-инсайт присвоил «высокую надёжность» по ОДНОЙ
оси (объём) — ложное успокоение, опровергаемое соседней плашкой. Текст «высокая
надёжность» зашит в `ratio-classifier.js` description для высокого ratio (переобещание).
Мета-класс: не-SSOT вердикта качества + семантический перегруз слова «надёжность».

**Фикс (потом, субагентом):**
1. Убрать «надёжность»/«идеально» из ratio-инсайта — ratio = только объём: «6.9:1 —
   наблюдений на переменную с запасом». Править `ratio-classifier.js` descriptions.
2. Единый предобучающий вердикт, агрегирующий оси (объём + prior-coverage + период):
   coverage<50% → общий тон «требует внимания», ratio не перебивает «идеально».
3. Развести язык: «Объём данных: отлично (6.9:1)» ⊥ «Согласованность с индустрией:
   низкая (44%) — отклик каналов нетипичен». Vitest на непротиворечивость вердиктов.

## 🟠 F-A1-10 — встроенный ДЕМО-пример synth_fmcg проваливает prior-check (44%)

**Симптом:** наш образцовый файл `synth_fmcg_brand` даёт prior coverage 44% → «надёжной
модели не получится». Эталонный демо пугает клиента «ненадёжно» при первом знакомстве —
плохое первое впечатление, риск для коммерческой готовности.
**Проверить (отдельно от текста, калибровка):** priors слишком узкие (не покрывают
легитимные FMCG-отклики) ИЛИ GROUND_TRUTH генератора (`tools/synthetic_pilot_data.py`)
вне prior-диапазона (рассинхрон после пересборки генератора; связано с
test_priors_calibration).

**🔴 ТРЕБОВАНИЕ Антона (2026-07-05): гейт демо-качества по ВСЕМ примерам всех
категорий.** Ни один встроенный sample-файл (fmcg / otc_pharma / real_estate /
retail_ecom — и любой добавленный впредь) НЕ должен проваливать prior-check: демо =
образец, первое впечатление клиента. Это не разовая проверка, а ПОСТОЯННЫЙ РЕГРЕСС-ТЕСТ
(каждый релиз/пересборка примеров прогонять prior-check всех категорий; провал любого =
красный гейт). Статус фактов: fmcg = 44% FAIL (экран, подтверждён). Остальные 3 — НЕ
проверены (чистой функции нет — нужен полный preflight pipeline `server.py:884` per файл;
прогон вынесен субагенту, числа НЕ выдумывать). Если priors узкие — расширить под
реальные отраслевые отклики; если генератор врёт — привести GROUND_TRUTH в диапазон.
Добавить `tools/test_sample_files_prior_check.py` (все категории, гейт).

## Наблюдения (не дефекты, на карандаш)
- **N-1:** две кнопки «Далее» на Импорте — центральная синяя работает, нижняя правая
  серая/неактивна. Возможна путаница (A4-развилка? уточнить у Антона).
- **N-2 → повышено до F-A1-2/F-A1-3** (подтверждено зондом, см. выше).
- **N-3:** инсайт на шаге «Целевая метрика» «4 бюджетных + 4 физических, физические
  конвертируются через CPP/CPM, укажете на следующем шаге» — в парной схеме физ
  выключаются, конверсии не будет. Generic validateKpiInsights не знает про пары.

## Что подтверждено зелёным (не только дефекты)
- Пары как механизм (channel-pairs.js `resolvePairSelection`) — РАБОТАЮТ: 8 медиа →
  4 канала enable, 4 физ disable (зонд).
- Метрики валидации сходятся с реестром: Ratio 4.4:1, Период 48, MQS-прогноз 90.
- KPI `sales_rub` распознан; competitor_trp/price_index → Контрольная (верно).
