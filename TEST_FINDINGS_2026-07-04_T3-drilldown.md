# TEST_FINDINGS — Т3 drill-down 4 групп + сезонная %-кривая (2026-07-04)

Приёмка реализации Т3 (последняя задача автосезонности). Метод по AVT-стандарту:
чистую логику — юнит-тестами; форму данных — программной инспекцией на РЕАЛЬНЫХ
decompose-результатах (готовые pkl, без MCMC); визуальную форму echarts —
совместно с Антоном на живом окне (UX-развилки).

## Что реализовано
- `src/lib/decomposition-view.js` — SSOT view-логики: `planViewSeries(ds, expanded)`
  (свёртка по top_group → 4 полосы / drill-down раскрытие), `seasonalityPctOfBase`,
  `presentTopGroups`, `fallbackTopGroup`, константы. **26 юнит-тестов** (тождество
  свёрнуто/развёрнуто/частично, edge-cases).
- `ChannelTimeline.svelte` — свёрнутый режим 4 полос по умолчанию, chips-раскрытие
  групп (`$state expanded`) + клик по полосе, сезонная %-кривая на второй оси Y
  (тумблер-chip), tooltip: сезонность отдельной строкой «% к базе» (вне total/групп),
  highlight пропускает %-линию.

## 🔵 Достоверность / тождество — на РЕАЛЬНЫХ данных (зонды tmp/probe_t3_*)
- [OK] MMX_4groups (43 мес, сезонность): top_group на всех сериях; pct_of_base
  ±(−31.5%…+41.6%); тождество Σplan==Σисходных свёрнуто (2 полосы) И развёрнуто
  (8 полос), maxErr 3.7e-9.
- [OK] Venarus (31 мес, мульти-клиент): pct_of_base −38.8%…+61.4%; тождество
  свёрнуто (3 полосы) / развёрнуто (9), maxErr 1.2e-7.
- [OK] БАЗА-агрегат всегда положителен (MMX min 8.1M) → знакопеременная сезонность
  «спрятана» внутри baseline в свёрнутом виде — ровно замысел (UX-развилка 1).

## 🟡 Находка: знакопеременная полоса «Конкуренты» у Venarus (НАСЛЕДИЕ, не блокер)
- Симптом: свёрнутая КОНКУРЕНТЫ Venarus Σ≈0, скачет −67.6M…+47.1M, 14 периодов
  противоположного знака к назначенному стеку (side=negative по mean). В area-стеке
  читается как визуальный скачок вокруг нуля.
- Корень: `signed_competitor` реально колеблется вокруг нуля (конкурент то помогает,
  то мешает). Backend `build_decomposition_series` назначает side по mean per_period.
- **Почему НЕ регрессия Т3:** группа КОНКУРЕНТЫ = 1 член → свёрнутый агрегат == тот
  же член == как рисовал старый код до Т3 (свёрнуто=развёрнуто, 1 полоса). Моя
  свёртка новой знакопеременности не создаёт; тождество данных цело.
- Вывод: существующее поведение отображения знакопеременного signed-фактора, вне
  предмета Т3 (хирургичность — не трогаю). Показать Антону скриншотом; если решит
  улучшать подачу таких факторов — отдельная задача.

## Гейты (зелёные)
- vitest ПОЛНЫЙ **828 passed** (802 база + 26 новых), 53 файла.
- svelte-check **0 errors** (мои файлы без новых warnings).
- decomposer_invariants **114 passed** (backend не трогался).

## 🔍 САМОАУДИТ Т3 (2026-07-04, по запросу Антона) — 8 находок, ВСЕ исправлены
- **А-1 🔴 [логика tooltip]** Свёрнутая группа давала дубль: заголовок «База · 990 (…%)»
  + идентичная строка «База 990» под ним (в дефолте — 4 дубля на каждый период).
  FIX: группа из 1 агрегата (seriesName == groupLabel) печатает только заголовок.
- **А-2 🟠 [моя UX-регрессия]** Toggle chip/полосы пересоздаёт option → EChartBase
  setOption(notMerge) сбрасывал dataZoom юзера к 0..100 на КАЖДЫЙ клик (до Т3 option
  менялся только при смене данных). FIX: `currentZoomWindow()` читает окно из chartRef
  (guard isDisposed) и подставляет start/end при перестройке.
- **А-3 🟠 [легенда]** Агрегированный фактор SSOT «Сезонность» (ключ агрегации Фурье)
  получал display-имя «Сезонность: Сезонность» (префикс groupLabel + name==groupLabel) —
  дубль в легенде/highlight-блоке; то же грозило «Категория: Категория». FIX:
  memberDisplay без префикса при p.name===groupLabel + fallbackTopGroup точные имена
  (робастность legacy) + тесты.
- **А-4 🟡 [читаемость %-оси]** Ось сезонности была несимметрична (диапазон по данным
  −31..+41) — нулевая линия смещена, волна читается хуже. FIX: `symmetricPctBound()`
  (наименьшее кратное 5 ≥ max|pct|; MMX→±45, Venarus→±65) + min/max на оси + 5 тестов.
- **А-5 🟡 [мой мусор]** Осиротевший JSDoc-блок старого buildCanonicalOption повис без
  функции (между ним и функцией я вставила memberDisplay). FIX: удалён.
- **А-6 🟡 [a11y]** Chips-тумблеры без aria-pressed. FIX: добавлен на оба вида chips.
- **А-7 🟡 [контракт]** JSDoc typedef props не описывал top_group/pct_of_base (новые
  поля SSOT после Т2). FIX: typedef дополнен.
- **А-8 🟡 [онбординг врёт]** Тур decompose-timeline описывал СТАРЫЙ вид («вклад
  каналов»; ни слова о 4 группах/клике/сезонной кривой) — после Т3 дефолт другой.
  FIX: текст шага обновлён (4 полосы · клик-раскрытие · пунктир % к базе · fullscreen).

**Осознанные решения (НЕ дефекты, документирую):**
- Порядок укладки positive-стека при развороте изменился: Сезонность/Праздники теперь
  прижаты к базе (между baseline и медиа), раньше — над медиа. Семантически ВЕРНЕЕ
  (сезонность — часть базы по решению Антона, группы укладываются блоками).
- Легенда оставлена стандартной ECharts (клик = скрыть/показать серию), drill-down —
  chips + клик по полосе. Перехват legend-клика убил бы полезную функцию скрытия.
- Legacy signedFactors-путь не тронут (свой формат имён; вне объёма Т3).

**Предсуществующее (НЕ Т3, не трогала — отметила):** (a) скрытие серии через легенду
исключает её из «Итого» tooltip (поведение echarts, было всегда); (b) mousemove-DOM-
слушатели контейнера при dispose/reinit EChartBase перевешиваются с перезаписью
cleanup-ссылок (класс утечки существовал до Т3; onDestroy чистит последние).

**Гейты после аудит-фиксов:** vitest ПОЛНЫЙ **834** (828 + 6 новых) · svelte-check
**0 errors** · канарейка палитр 3/3 (перепрогнана — парсит изменённый Svelte-файл).

## 🚀 5 УЛУЧШЕНИЙ ПОСЛЕ АУДИТА (Антон: «реализуй все 5», 2026-07-04)
- **П1 анимация раскрытия** — universalTransition (id+groupId==top_group), morph
  агрегат↔составляющие. seriesIdentity() в модуле +3 vitest. Визуальный морф —
  на живой приёмке (headless не докажет); структура id/groupId покрыта юнитом.
- **П5 «Развернуть/Свернуть всё»** — chip-кнопка, allExpanded $derived.
- **П4 канарейка py↔js** (decomposition-view-parity.test.js, 9): множество+порядок
  групп + подписи + fallbackTopGroup↔_TOP_GROUP_MAP. Поймала свой ложный кейс
  («База: X» невалиден для baseline) И реальный gap 'Внешние'→МЕДИА (закрыт).
- **П3 регрессия** — юнит (seriesIdentity/тождество) + e2e-сценарий
  docs/e2e/T3_drilldown_driver_session.md (driver_session live, не CI).
- **П2 двухуровневость отчётов** — timeline PPTX+HTML свёрнут в 4 группы (паритет
  «программа==отчёт», принцип INV-50); детализация в waterfall+таблице.
  collapse_series_to_top_groups SSOT (7 py-тестов). **playwright-приёмка HTML**:
  2 группы (MMX без внешних/конкурентов), суммы 514 005 204 + 99 566 706 = тождество
  613 571 910 на реальном рендере, цвета grey/teal корректны. verify: PPTX 43/43,
  HTML 35/35+a11y 15/15, brand 0-регресс vs baseline (2 предсущ. CSP/em-dash — не мои).
  🟡 PPTX «отдельный слайд раскрытия» НЕ делался: ломает verify-контракт «12 slides»
  ×7 + рендер PPTX глазами автономно недоступен. Форма-развилка — Антону.

## 🔍 САМОАУДИТ №2 (2026-07-04, по запросу Антона) — неаудированная часть (5 улучшений): 8 находок, ВСЕ исправлены
- **Б-1 🔴 [П1 не работал]** Анимация раскрытия НЕ морфила: series-level `groupId`
  в echarts НЕ существует (по типам 5.6: groupId только у data-item для
  item-drilldown) → поле игнорировалось, id агрегата/членов разные → обычный
  fade. FIX: `universalTransition.seriesKey = top_group` (серии с одним seriesKey
  морфятся 1→N divide / N→1 combine); мёртвый groupId убран из series.
- **Б-2 🟠 [PPTX подпись врёт]** s08 «Базовый уровень + вклад каждого канала» при
  свёрнутом обзоре. FIX: подпись из ФАКТИЧЕСКОГО состава («База + Медиа + … ·
  период»); + `baseline_label` в make_timeline_area → легенда «База» (русская,
  паритет с программой), legacy default 'Baseline' цел.
- **Б-3 🟠 [HTML подпись врёт]** subtitle «Декомпозиция: baseline + вклад каналов».
  FIX: «Декомпозиция по группам (обзор) или по каналам и факторам (детально)».
- **Б-4 🟠 [инструмент устарел молча]** fidelity_diff проверял «каждый канал/фактор
  серией timeline» — ложные FAIL при первом живом прогоне после П2 (сейчас PASS
  вакуумно: все проекты SKIP). FIX: канон-проверки — HTML detail несёт ВСЕ
  каналы+факторы, HTML overview == SSOT-свёртка, PPTX серии ⊇ collapsed-группы.
  Доказано живым зондом на фикстуре (не вакуумно): HTML ok / PPTX ok.
- **Б-5 🟠 [полнота П2]** «Двухуровневость» HTML была одноуровневой (только обзор).
  FIX: payload {weeks, overview, detail} + кнопка «Детально ⇄ Обзор» (btn-inline
  в title-bar, слушатель setupTimelineViewToggle, CSP-hash пересчитывается
  сборкой автоматически). Тест tools/test_html_timeline_two_level.py (4):
  структура, overview==collapse, detail-полнота, Σoverview[t]==Σdetail[t].
- **Б-6 🟡 [тексты программы]** ReportStep «Динамика по периодам - вклад каналов
  во времени» (×2) описывал старый состав отчёта. FIX: «база, медиа, внешние
  факторы и конкуренты во времени».
- **Б-7 🟡 [a11y]** У «Развернуть/Свернуть всё» aria-pressed + меняющийся лейбл =
  противоречивый двойной сигнал скринридеру. FIX: aria-pressed убран (состояние
  несёт лейбл); групповые chips (стабильный лейбл) — aria-pressed остаётся.
  e2e-сценарий согласован.
- **Б-8 🔴 [живой прогон поймал]** HTML detail: «Сезонность: Сезонность» — класс
  А-3, но в JS-шаблоне interactive.py (label = group+': '+name при group==name).
  FIX: префикс только при group !== name. Найдено ТОЛЬКО playwright-прогоном
  (юнит-фикстура даёт согласованные имена — живой рендер реального MMX нет).

**Живая приёмка №2 (playwright, реальный MMX):** обзор 2 серии «База»+«Медиа»
(русская легенда) → клик «Детально» → 8 серий (…, «Сезонность» без дубля),
кнопка «Обзор» → возврат к 2. Тождество в ОБОИХ режимах: 613 571 910.

**Гейты после аудита №2:** vitest **846** · svelte-check **0** · python
**141** (invariants 114 + collapse 7 + palette 3 + two_level 4 + series 13) ·
verify PPTX 43/43 + brand==baseline · HTML 35/35 + a11y 15/15 + brand==baseline ·
fidelity_diff живой зонд ok.

**Отмечено (предсуществующее, НЕ трогала):** (a) «Бюджет ДО НДС» идёт медиа-серией
в decomposition_series, хотя из таблицы каналов дропнут как total-budget — вопрос
к составу серий бэкенда (существовал до Т3, виден и в detail-режиме, и раньше);
(b) chart-title HTML «Продажи по неделям · stacked area» — «по неделям» может
врать для месячных данных + англицизм (было всегда).

## 🚀 3 УЛУЧШЕНИЯ ИЗ АУДИТА №2 (Антон: «1,2,4 на реализацию», 2026-07-04, коммит 88a9012)
- **П1 total-budget как media** — суммарная колонка «Бюджет ДО НДС» авто-детектится
  media (conf 0.70), обучается отдельным каналом → **зонд: 6.45% вклада в MMX
  (39 560 906)**, дублирует сумму каналов, рассинхрон timeline↔таблица.
  🔴 Дроп из timeline НЕВОЗМОЖЕН — тождество рухнет 613 571 910 → 574 011 004 (−6.45%).
  FIX в ПРАВИЛЬНОМ месте (вход, не отображение): гейт в validate_data по ЕДИНОМУ
  с _merge_channels критерию (_normalize_channel_name → None) → роль до 'unused' +
  warning total_budget_as_media (паттерн У3; юзер вернёт вручную). Новые модели не
  задваивают + timeline/таблица сходятся; обученные честны (timeline не тронут).
  +2 теста. **Отмечено:** для УЖЕ обученного MMX-теста «Бюджет ДО НДС» остаётся в
  timeline (6.45%) — это верно (модель его знает); чинится переобучением после warning.
- **П2 гранулярность заголовка** — «по неделям»/«stacked area» врали на месячных
  данных. FIX: `_period_unit()` (detect_granularity дат) → «Продажи по месяцам»;
  kicker→«ПРОДАЖИ ПО ПЕРИОДАМ», s08_leader→«во времени», англицизм убран. Живой
  MMX (месячный) подтвердил «по месяцам»; «по неделям»/«stacked area» отсутствуют.
- **П4 тултип тождества** — переключатель «Обзор ⇄ Детально» + «Итоговая сумма
  продаж одинакова в обоих режимах» (sections статичный + JS-title динамический).
- **Отложено (Антон «остальное после»):** П3 PPTX «слайд раскрытия» (я не рекомендую —
  ломает 12-slide verify, детализация уже в HTML-toggle + waterfall + таблице).

Гейты: python 48 (validator+collapse+two_level+narrative+series) · fidelity ВСЕ PASS ·
verify HTML 35/35+15/15 (brand==baseline) · PPTX 43/43.

## 🔍 САМОАУДИТ №3 (2026-07-04, по запросу Антона) — фиксы Б-1..Б-8 + П1/П2/П4: 3 находки FIX, 12 чисто
- **В-1 🔴 [фича-декорация, коммит 543a731]** Фильтр `activeWarnings` (ValidateStep)
  скрывал warnings колонок с role='unused' — а АВТО-снижение роли (У3
  non_numeric_role И П1 total_budget_as_media) само ставит unused → причина
  снятия была НЕВИДИМА с первого рендера (юзер видел исключение без объяснения;
  затрагивало и У3 с прошлой сессии). FIX: типы авто-снижения показываются всегда
  (AUTO_DEMOTION_TYPES); фильтр остаётся для колонок, исключённых юзером.
  Бонус: statusLabel честнее — «Готово с предупреждениями» вместо «пройдена».
  ⏳ Live-проверка баннера в GUI Валидации — при живой сессии (логика тривиальна).
- **В-2 🟡** «на устойчивом baseline» (s08_leader, трогала вчера) → «на устойчивой
  базе» — консистентность с русской легендой. Живой MMX: «baseline» отсутствует.
- **В-3 🟡** Импорт `_normalize_channel_name` вынесен из цикла колонок валидатора.

**Проверено зондами — чисто (12):** единственный читатель CHART_DATA.timeline —
toggle (перестройка payload безопасна); `ds=` в s08 один (перестройка чистая);
theme-override красит оси merge'ем — TL_VIEW/серии переживают смену темы; циклов
импорта validator↔narrative_adapter нет; краевые имена («Вклад, млн» → unknown —
не задет; «TB Бюджет» → канал TB — верно); legacy-подпись s08 честна (там серии
реально по каналам); PNG-экспорт сохраняет текущий режим; П4-тултипы sections↔JS
согласованы; contains_any подстрочный осознанно (диагностика обрезанных легенд);
kicker статичен намеренно (метка секции); vitest/гейты покрытие подтверждено.

**Гейты №3:** vitest **846** · svelte-check **0** · python 19 (validator+two_level)
· verify HTML 35/35 + a11y 15/15 (brand==baseline) · PPTX не трогался.

## 🚀 4 ПРЕДЛОЖЕНИЯ АУДИТА №3 РЕАЛИЗОВАНЫ (Антон: «по балансу», коммит c414683)
- **П-1 стоп-токены агрегатов:** «итого/всего/сумма/total» в _CHANNEL_NAME_STOP_
  PHRASES — «ИТОГО Бюджет» раньше нормализовался в канал «ИТОГО» (задвоение).
  Единый критерий (таблица+гейт); зонд: агрегаты→None, «Total TV»→TV и
  «Диджитал всего»→Диджитал целы. +7 тестов.
- **П-2 fidelity строгие группы:** PPTX-проверка групп точным равенством
  (подстрочный матч коротких «база»/«медиа» давал бы ложный зелёный).
- **П-4 динамический kicker:** «ПРОДАЖИ ПО МЕСЯЦАМ» из period_unit (живой MMX
  подтвердил; TOC-label не задет, strings-ключ резерв).
- **П-3 чек-лист live дополнен:** баннер авто-снятой роли (В-1) + морф Б-1
  в e2e-сценарии.
Гейты: python 29 · fidelity зонд ok · HTML 35/35+15/15 · PPTX 43/43 (brand==baseline).

## 🔍 САМОАУДИТ №4 (2026-07-05) — В-1..В-3 + П-1..П-4: 1 находка FIX, 23 чисто (кривая сошлась: 8→8→3→1)
- **Г-1 🟡 [UX, порождена В-1, коммит 0f5ae4a]** Авто-снятые warnings несли
  action='exclude' → в «Рекомендациях» кнопка «Исключить» на колонке, УЖЕ
  исключённой валидатором (предлагала сделать сделанное; клик — идемпотентный
  no-op, но семантика ложная). FIX: action='acknowledge' у обоих авто-типов →
  фронт рендерит нейтральную «Принять» (единственный потребитель w.action —
  fix-list). Тест фиксирует контракт.
- **Проверено чисто (23):** blast radius стоп-токенов согласован — decomposer
  display_name и HTML detail используют ТУ ЖЕ функцию с fallback `or col`
  («Total TV» → «TV» везде одинаково; «Бюджет ДО НДС» из старой модели →
  сырое имя в detail); InsightsPanel не задет (свой механизм действий);
  \b-границы кириллицы работают, «Totals» не матчится; PPTX-группы <22 симв —
  строгий матч валиден (обрезание _short не мешает); kicker.upper() кириллица;
  идемпотентность setColumnRolesBulk для повторного unused; зонд коллизии
  «Диджитал»+«Диджитал всего» → штатный first-wins + warning в лог (до
  стоп-токенов под-агрегат шёл каналом-ДВОЙНИКОМ в таблице — стало честнее).
- **Отмечено (мелочь, не трогала):** badge «исключено по рекомендациям»
  включает авто-снятые (снял валидатор, не рекомендация) — семантика чуть
  неточна; excludedCount честен.

## ⏳ Осталось — СОВМЕСТНО с Антоном (live co-design, он у экрана = быстрейший верификатор)
Визуальная форма echarts, что код структурно не докажет + продуктовые UX-развилки.
Готовые фикстуры (без MCMC): `mmx_4groups_dw7vr06g`, `Venarus_wj1n_gsq` в %TEMP%.
Запуск: `npm run tauri:dev` (мост 9223), открыть проект-фикстуру.
- Визуальная сверка: 4 свёрнутые полосы + цвета/наложение; клик-drill (chips + area);
  сезонная %-кривая на второй оси (читаемость, масштаб); tooltip-строка сезонности.
- UX-развилка 1: показывать ли сезонность ₽-полосой при развороте БАЗЫ, или только
  %-кривой (сейчас — и полоса в стеке для тождества, и %-кривая поверх).
- UX-развилка 2: семантика «% к базе» — от финальной базы (сейчас) vs intercept+тренд.
- Наблюдение: dataZoom сбрасывается при toggle chip (setOption notMerge) — мелочь,
  чинить только если мешает.
