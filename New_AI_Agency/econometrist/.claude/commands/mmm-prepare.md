ЯЗЫК ОТВЕТА: РУССКИЙ. Все комментарии, выводы, рекомендации, статусы — на русском языке. НЕ использовать английский (кроме терминов ROI, CPM, GRP).

Проверь готовность датасета из inbox для эконометрического моделирования.

1. Прочитай csv/xlsx из inbox через скилл `xlsx`
2. Проверь структуру:
   - Заголовки: только латиница + подчеркивания, начинаются с буквы
   - Формат дат: YYYY-MM-DD, единый интервал (daily, weekly или monthly), без пропусков периодов. Для monthly: первый день месяца (2024-01-01, 2024-02-01...)
   - Значения: только числовые, без символов валют ($, ₽), без запятых-разделителей тысяч
   - Пропуски: нет NULL/пустых ячеек (неактивный канал = 0)
3. Проверь содержание:
   - Минимальный период: 52 недели / 18 месяцев (предупреждение), 104 недели / 24 месяца (рекомендация). Для monthly: минимум 24 месяца, рекомендация 36 месяцев
   - Ratio данные:переменные >= 4:1 (минимум), >= 10:1 (надёжно)
   - Вариативность: коэффициент вариации < 5% → пометить «бесполезен для модели»
   - Столбцы со 100% нулей → исключить
4. Описательная статистика по каждому столбцу: min, max, mean, std, % нулей, наличие тренда
5. Корреляционная матрица — пометить пары с |r| > 0.8 как «риск мультиколлинеарности»
6. Итоговый вердикт: ГОТОВ К МОДЕЛИРОВАНИЮ / ТРЕБУЕТ ДОРАБОТКИ — со списком проблем и рекомендаций

Сохрани результат в `exports/mmm-validation-[дата].xlsx` (листы: Summary, Statistics, Correlations, Issues)

**КРИТИЧЕСКИ ВАЖНО — непрерывное выполнение:**
- Выполняй ВСЕ шаги подряд без остановок. НЕ спрашивай «подготовить датасет?» — подготовь автоматически
- Когда ВСЕ задачи выполнены — напиши ровно одну фразу: «Все задачи выполнены.»
