ЯЗЫК ОТВЕТА: РУССКИЙ. Все выводы, таблицы, рекомендации, статусы – на русском языке. Английский только для устоявшихся терминов: ROI, CPM, GRP, MQS, R-hat, MAPE, adstock.

Смоделируй влияние медиа-затрат на уровень знания бренда (awareness) и спрогнозируй его динамику.

1. Прочитай xlsx из inbox через скилл `xlsx`. Обязательные столбцы: `date`, `awareness_%` (из трекинговых исследований). Плюс медиа-затраты по каналам (особенно охватные: TV_GRP, OOH_Spend, Digital_Video_Impressions)
   - Inbox пуст – сообщи, какой файл нужен (xlsx с `date` и `awareness_%`), дальше не иди.
   - Несколько xlsx в inbox – возьми последний по дате изменения, явно назови какой; если не очевидно, какой актуален – уточни у пользователя.
   - Нет обязательных столбцов (`date`, `awareness_%`) – укажи, каких не хватает, пометь `[ОГРАНИЧЕНИЕ]`; данные не выдумывать.
2. Автоматически определи параметры (НЕ задавай вопросы):
   - Источник: определить из названий столбцов/файла. Если не ясно – [ASSUMED] generic tracker
   - Частота: определить из интервала между датами автоматически
   - SOV/SOM: использовать если есть столбцы, иначе пропустить ESOV-модуль
   - Категория: FMCG по умолчанию (decay ~3%/квартал) [ASSUMED]
   - Горизонт: 12 месяцев [ASSUMED]
3. Проверь Python-зависимости и сгенерируй скрипт `exports/scripts/awareness_forecast.py`:
   - Байесовская регрессия: awareness_t = f(adstock(media)) + trend + seasonality + natural_decay
   - Adstock с длинным хвостом (Weibull) – awareness строится медленно, затухает медленно
   - ESOV-модуль (справочно, разводя метрики): у Binet & Field коэффициент ESOV описывает рост ДОЛИ РЫНКА (SOM), не awareness – annual market share growth ≈ 0.05 × ESOV. Прямой канонической формулы «ESOV → awareness» в источнике нет; если связываешь ESOV с ростом awareness – помечай `[ДОПУЩЕНИЕ]` и не выдавай коэффициент доли рынка за рост знания бренда (SOM и awareness – разные метрики)
   - Natural decay: без рекламы awareness снижается ~2-5% в квартал (зависит от категории)
   - Прогноз с правдоподобным диапазоном
4. Выполни скрипт через Bash
5. Результат:
   - Прогноз awareness на заданный горизонт при текущих бюджетах с правдоподобным диапазоном
   - Кривая awareness over time (факт + прогноз)
   - Точка breakeven: при каком минимальном бюджете awareness перестаёт падать
   - Sensitivity: сколько awareness прибавит +10% / +20% / +50% к медиабюджету
   - Вклад каждого охватного канала в формирование awareness

Сохрани: `exports/awareness-forecast-[дата].xlsx` (листы: Forecast, Channel_Contribution, Sensitivity) + графики

Когда прогноз сформирован – напиши: «Все задачи выполнены.»
