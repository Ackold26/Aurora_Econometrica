# Живой AVT-прогон режима Планирования — находки (2026-07-10)

Метод: программная инспекция (стандарт AVT) — backend-путь на реальной фикстуре напрямую + GUI-сверка. Фикстура `tmp/avt_planning_fixture.xlsx` (24 мес истории + 6 мес хвоста, каналы «ТВ»/«Онлайн-видео» кириллицей, ТВ с carryover 0.6). Ветка `feat/econ-planning-mode` @ fd6227e.

## 🔴 F-AVT-1 (КРИТИЧНЫЙ) — channels пустой для кириллических имён каналов → медиаплан не читается
- **Симптом:** `validate` детектит хвост (n=6, даты, granularity верны), но `media_plan.json.channels = {}` → `scenario` возвращает `MEDIA_PLAN_EMPTY` «Медиаплан пуст». Вся фича не работает.
- **Корень:** `validator.py:421-423` строит `_media_hints` только по `detect_column_role_with_confidence(col)=='media'`. На кириллице `'ТВ'→unknown`, `'Онлайн-видео'→unknown` (паттерны английские tv/digital). Пустые хинты → `detect_media_plan_tail` строит channels из [] → пусто. Основная логика media_cols (validator:477-553) богаче и каналы ловит (обучение сработало), но детектор хвоста использует упрощённую.
- **Класс:** типичный русский клиент → критично. Юнит-тест детектора не поймал (там media_cols передавались явно) — [[feedback_verify_in_use_artifact_not_repo_copy]].
- **Фикс:** fallback — если role-хинты пусты, media-кандидаты = числовые колонки кроме date/kpi. ПРИМЕНЁН, см. ниже.

## 🟡 F-AVT-2 — validate status=error на тонкой фикстуре (разбирается)
- **Симптом:** `validate` возвращает `status:error`, `verdict:ТРЕБУЕТ ДОРАБОТКИ`, `message:None`. rows:24 (notna-обрезка хвоста сработала верно). Колонки Дата/Продажи распознаны.
- **Гипотеза:** by-design для тонких данных (24 мес / 2 канала, n_years=2) ИЛИ реальная проблема потока. Требует проверки: блокирует ли это шаг в GUI, или это «жёлтый» вердикт с проходимым потоком. Разбирается.

## Что РАБОТАЕТ (подтверждено зондом)
- Детекция хвоста: n_future=6, period_labels, granularity=M, future_dates, source_hash — верны.
- notna-обрезка: статистика по 24 истории, не 30. media_plan.json записан.
- train OLS на истории — status ok, pickle создан.
- carry_in инфраструктура на месте (проверить после фикса channels).

## Прогресс прогона
- [x] Backend-зонд на фикстуре (вскрыл F-AVT-1, F-AVT-2)
- [x] **Фикс F-AVT-1 ЗАКРЫТ** (validator.py fallback числовые колонки) + регресс-тест `test_planning_cyrillic_channels.py` (2 passed). Переприрогон: channels заполнен, scenario **ok**, **carry_in доказан ЖИВЬЁМ: predictions[0] с carry 20 773 969 vs без 12 318 330** (+68%, ТВ carryover), disclaimers ×3, future_dates, праздники РФ.
- [x] **F-AVT-2 = by-design** (не регресс планирования): основная media-детекция не ловит кириллицу → `no_media` critical → «укажите вручную» (GUI ColumnMapper решает). Fallback хвоста даёт те же каналы, что ручное назначение — консистентно. UX-трение для русских клиентов (автодетект англ. паттернов) — существующая особенность, кандидат на отдельное улучшение детектора ролей (не в scope планирования).
- [x] Регресс-гейт: sidecar 486 passed (фикс не сломал).
- [ ] GUI-сверка PlanningStep (dom_snapshot + скриншот) — dev запущен, sidecar healthy.

## Итог backend-пути (доказано вживую на реальной фикстуре)
Полный путь планирования РАБОТАЕТ: Excel с хвостом → детекция (channels на кириллице после фикса) → обучение на истории (notna) → прогноз с carry-in (доказан числами) + праздники + disclaimers. Ядро фичи подтверждено не только в CI, но на живом sidecar-пути.
