# GUI-клик-лист — живой прогон марафона E1→E4 (2026-07-04)

> Реестр = ФАКТ: статусы только после наблюдения. Промт: NEXT_SESSION_gui_clickpath.md.
> Среда: dev-окно @49324d9, мост 9223 ✅ (hasTauri:true), sidecar :7529 (PID 24936).
> Плацдарм: проект «Кагоцел РФ+…2306-26» — latest.pkl (06-24), backtest.json (07-03),
> decomposition/optimization (07-03), promises НЕТ (пустые состояния — тоже пункт).

## NEXT
Холодный старт ЗАКРЫТ (см. итог ниже). Дальше — ГОРЯЧИЙ путь: загрузить исходник Кагоцела
(C:\Users\ackol\Desktop\Файлы для тестирования Авроры\...Кагоцел...29.08.xlsx) → обучить →
проверить вживую карточки E1 витрина / E3 история / E2 калибровка / E4 «Зафиксировать прогноз»
+ PromisesCard. Затем слайды PPTX 12/13/14, HTML-тема trust. Правки (G-2 фикс) — пакетом ПОСЛЕ
прогона (закрыть окно, чтобы HMR не мешал).

## Итог холодного старта (изолировано чисто)
Из 6 шагов не восстанавливается РОВНО ОДИН — «Модель» (G-2). Импорт открыт, Валидация (роли
реконструированы), Декомпозиция (график ✅), Оптимизация (сводка + honesty INV-50 ✅),
Отчёт (MQS 70, R² 0.976, MAPE 6.4%, бюджет 3575М, экспорт-кнопки ✅). Диагностика модели
доступна в Отчёте, но спрятана на своём шаге → подтверждает: данные есть, гейт stepState прячет.
Кнопка «Запустить модель» активна без предупреждения о данных (data_file:null) — отдельно
проверить в коде: econ_train при null-данных = ошибка или тупик (E4-сверка тоже под вопросом).

## ✅ G-2 ЗАКРЫТ ЖИВЬЁМ (2026-07-04)
Фикс внесён (ModelTrainingStep.svelte $effect) + тест на класс (model-cold-start-restore.test.js
2/2, exit 0, echarts-шум замокан ConvergenceDashboardStub) + svelte-check 0 ошибок.
ЖИВОЕ доказательство после перезапуска: холодный старт → шаг Модель →
`#model-results-anchor` present, «Проверка на истории» (E1) + «История модели» (E3)
смонтированы, MQS виден, форма «Запустить» не доминирует (panelLen 473→2286).
BacktestCard читает витрину с диска: «модель не точнее наивного 0 из 3, MAPE 47.8% vs
наивный 18.7%» (та самая сезонная грабля — мотивация автосезонности А→Б). ModelHistoryCard —
корректное пустое состояние. Сценарий E4 «вернулся свериться» РАЗБЛОКИРОВАН. Осталось: коммит.

## ✅ G-4 ЗАКРЫТ ЖИВЬЁМ (2026-07-04)
Фикс: promisesVersion (writable) в project-state + инкремент в fixForecastPromise (OptimizeStep)
после успеха + PromisesCard onMount→$effect на activeProjectId+promisesVersion. Тест-регресс
(promises-card 7/7, новый кейс «bump version перечитывает»). Гейты: svelte-check 0, vitest
794/794 (было 793, +1). ЖИВОЕ доказательство после перезапуска: (1) холодный старт → Оптимизация →
карточка СРАЗУ показывает ранее невидимое обещание (260 183 664 ₽, CI, экстраполяция); (2)
фиксация ВТОРОГО прогноза → карточка обновилась 1→2 обещания НЕМЕДЛЕННО, без навигации.
Механизм E4 (петля обещаний) визуально работает end-to-end.

## Findings (G-N)
- **G-4 (ПОДТВЕРЖДЁН, тот же класс что G-2 — карточка читает состояние однократно):**
  «Зафиксировать прогноз» создаёт обещание на диске (results/promises.json ✅, эндпоинт
  econ_promises_list возвращает total:1 ✅), успех-строка появляется — НО PromisesCard
  «Сбывшиеся рекомендации» показывает «Прогнозов пока не зафиксировано». Корень:
  PromisesCard.svelte:52 `onMount(() => void reload())` грузит обещания ОДНОКРАТНО;
  навигация между шагами НЕ размонтирует компонент (панели visibility, не {#if}) → onMount
  отработал при первом показе (обещаний не было) и больше не перезагружался; создание
  обещания в OptimizeStep не уведомляет карточку (fixForecastPromise:1217 ставит лишь
  локальную promiseSuccess). Последствие: обещания создаются невидимо → механизм E4
  визуально сломан (пилот-блокер петли доверия). Живьём: кнопка работает end-to-end,
  но карточка вечно пуста. Гейт analyst/planner при этом КОРРЕКТЕН (кнопка активна в
  планировании, формулировка Антона на месте, обещание с CI + check_after_index:31).
  ФИКС: стор-триггер promisesVersion (writable) в project-state + инкремент в
  fixForecastPromise после успеха + PromisesCard onMount→$effect на activeProjectId+version.

- **G-1 (наблюдение, by-design?):** холодный старт («Продолжить проект» с главной) открывает
  проект на шаге ИМПОРТ со статусом `complete`; остальные шаги — `ready` (доступны, но НЕ
  `complete`), хотя на диске есть модель/декомпозиция/оптимизация. Шапка пайплайна не
  отражает достигнутый прогресс. Проверить: ожидание пользователя vs факт.
- **G-2 (КЛЮЧЕВАЯ, ROOT CAUSE локализован — ПОДТВЕРЖДЁННЫЙ БАГ, не by-design):**
  при холодном старте готового проекта шаг «Модель» показывает форму «Запустить модель»,
  а НЕ восстановленную модель: карточки E1 «Проверка на истории» + E3 «История модели»
  НЕ монтируются (`#model-results-anchor` отсутствует, textContent body без заголовков).
  · Корень: `restoreProjectResults` (project-state.js:1123-1128) ВОССТАНАВЛИВАЕТ
    `modelData.diagnostics` из model-diagnostics.json ✅ — значит diagnostics в сторе ЕСТЬ.
    НО блок результатов ModelTrainingStep.svelte:292 гейтится на ЛОКАЛЬНЫЙ
    `stepState === 'trained' && diagnostics`, а `stepState` = `$state('idle')` (стр. 38)
    становится 'trained' ТОЛЬКО в handleComplete (после реального обучения) или из
    localStorage-задачи. restoreProjectResults его НЕ трогает → остаётся 'idle' → весь
    блок (диагностика, MQS, конвергенция, BacktestCard, ModelHistoryCard 309-313) скрыт.
  · Асимметрия с Оптимизацией: OptimizeStep гейтит показ на своём СТОРЕ ($optimizeData),
    а ModelTrainingStep — на локальном флаге компонента. Отсюда Оптимизация восстанавливается,
    Модель — нет. Диагностика/backtest на диске ЕСТЬ, вина 100% на stepState-гейте.
  · ПОСЛЕДСТВИЕ (острое): сценарий E4 «вернулся через квартал свериться с фактом»
    физически невозможен — открыв проект, пользователь не видит витрину E1, историю E3;
    петля доверия работает только в пределах ОДНОЙ живой сессии после обучения.
  · ФИКС (точечный, отложен в пакет после прогона — не править src под HMR):
    в onMount ModelTrainingStep — если `get(modelData)?.diagnostics` уже присутствует
    и нет активной задачи обучения → `stepState = 'trained'`. Раскроет блок результатов
    из восстановленных данных; BacktestCard/ModelHistoryCard дочитают артефакты по эндпоинтам.
    Тест на класс: интеграционный — restore гидрирует modelData → компонент показывает
    #model-results-anchor без обучения. (Юнит на restoreProjectResults прошёл бы — шов
    между стором и локальным флагом компонента, ловится только интеграцией/живым прогоном.)
- **G-3 (рассинхрон конфига):** project.json `use_holidays: false`, а шаг «Модель» показывает
  блок «Авто-праздники РФ 12 (5 можно отключить)» как активный. Проверить фактическое
  состояние тумблера и что реально уходит в обучение.

## Артефакты отчётов — проверены вживую (2026-07-04, после фикса)
- **PPTX** (mmm_report_20260704_024817, 13 слайдов): слайд витрины backtest №6 в секции
  «01 ГЛАВНОЕ» ✅; нумерация секций 01/05…05/05 корректна; **0 wireframe-маркеров** (канарейка B1);
  generation/promises слайды отсутствуют (нет данных — условная вставка верна). Слайд витрины
  несёт ЧЕСТНЫЕ данные: «модель пока не точнее наивного, 0 из 3, MAPE 47.8% vs 18.7%», таблица
  окон с реальными числами и ✓/✕ — не заглушка, INV-50 держит.
- **HTML** (mmm_report_20260704_025127, 1034 КБ): секция «Петля доверия» ✅ с витриной E1
  «0 из 3»; TOC-пункт trust ✅; условные под-блоки E3/E2/E4 корректно СКРЫТЫ (нет данных);
  0 wireframe. ⚠️ Урок: первый зонд дал ложный «WIREFRAME True» из-за `or PLACEHOLDER` в
  одном булевом выводе (PLACEHOLDER=9 — легитимные input/echarts, WIREFRAME=0). Разные
  маркеры-сигнатуры НЕ сливать через or — маскирует, который сработал (класс «exit 0
  маскирует подмену»). Проверять раздельно с count+контекстом.

## Осталось по клик-листу (горячий путь — требует данных/планирования)
- Оптимизация: кнопка «Зафиксировать прогноз» (D-E4-3) + PromisesCard в режиме Планирование
  (нужно задать будущий бюджет/цель). E2 калибровка вживую (нужно обучение — data_file:null).
  Все покрыты юнит-тестами + headless-зондами марафона; живьём — в сессии с загрузкой данных.
- Валидация: экспресс-баннер (на Кагоцеле честно отсутствует — TRP/2 целевых).
- G-1 (stepper ready/complete) и G-3 (use_holidays рассинхрон) — вторичны, проверить в коде.

## Пройдено
- Среда поднята чисто (порты 5173/9223/8901 free до старта), мост hasTauri:true, sidecar Готов.
- Холодный старт: главная → «Продолжить проект» → /pipeline на шаге Импорт (проект открылся).
- Оптимизация: результаты восстановлены с диска; «Зафиксирован» ×5 = статус каналов
  (title «годовая сделка / неизменяемый»), НЕ обещания E4 — ложная тревога снята.
- Переключатель «Планирование будущий период» есть, кликается.
