# GUI-клик-лист — живой прогон марафона E1→E4 (самодостаточный промт)

> Требует ЖИВОГО окна (dev-запуск + мост) на свежем окружении. Копировать в
> начало сессии, cwd = `D:\Docs\Aurora_Ai\Dev\Aurora_Econometrica`, ветка
> `feat/econ-e1-backtest` @9b72f22 (запушена).

## ⚠️ Среда (уроки, без них сессия утонет)

1. **Мост tauri**: запускать `npm run tauri:dev` (НЕ `tauri dev`) — иначе
   `window.__TAURI__` отсутствует. После kill/rebuild окна порт 9223
   ПОЛУМЁРТВ — вызовы виснут 20+ минут без таймаута
   ([[feedback_tauri_mcp_bridge_half_dead_after_rebuild]]): перед сессией
   убить ВСЕ хвосты (gui/vite 5173/sidecar/9223 по PID через
   Get-NetTCPConnection, НЕ taskkill в Git Bash), поднять заново, мост
   проверить дешёвым вызовом ДО плана.
2. Данные: реальный Kagocel `C:\Users\ackol\Desktop\Файлы для тестирования
   Авроры\Эконометрика - тестовые файлы\Кагоцел...29.08.xlsx`; Венарус/MMX —
   `D:\Docs\Aurora_Ai\TestData\Econometrica\`.
3. Метод AVT: программная инспекция (webview_dom_snapshot/find_element) >
   скриншоты; скриншот — для форм/вёрстки; durable TEST_FINDINGS-файл с
   NEXT-указателем; находки верифицировать и чинить с тестом на класс.

## Клик-лист (накоплен марафоном 2026-07-03/04; порядок = путь пользователя)

### Валидация
- [ ] **Экспресс-подтверждение (П1-ядро)**: на чистых monetary-данных зелёный
  баннер «Автоматика уже настроила этот шаг» виден; 1 клик → под-шаг
  «Подтверждение» → футерная «Далее» → Модель разлочена. На Kagocel (TRP,
  2 целевых) баннер ЧЕСТНО отсутствует. Умный KPI-дефолт (F-UX-2): на
  count-only данных дефолт не залипает на «Выручке».
- [ ] «Запас данных (Ratio)» — чип в шапке, карточка, сводка (П4): единый термин.
- [ ] NAV-2 остатки: клик-назад по пройденным под-шагам; тултип футерной «Далее».

### Модель
- [ ] **CalibrationPanel** (bayesian): добавить гео-тест → обучить → в
  диагностике calibration_check; в PPTX [CALIBRATED] у канала; кривой ввод —
  русские ошибки.
- [ ] **BacktestCard**: кнопка с честной оценкой времени → прогресс → вердикт
  (на Kagocel ожидаем worse_than_naive — сезонность); пометка устаревания
  после переобучения; таблица окон.
- [ ] **ModelHistoryCard**: после ретрейна поколение в списке; «Что изменилось
  с прошлого поколения?» → headline с CI; дрейф-плашка «Пора переобучить».
- [ ] Статусы обучения: «Обучаем модель — байесовский расчёт» / «Подготовка
  модели» / «Проверка качества» (UX-1).

### Оптимизация
- [ ] **«Зафиксировать прогноз»** (D-E4-3, формулировка Антона): в analyst —
  disabled с тултипом; в Планировании — фикс → успех-строка; **PromisesCard**
  внизу: pending со счётчиком; после дописывания данных и «Сверить с фактом» —
  kept/missed честно.
- [ ] Хвосты E1-эпохи: compare-бейдж и торнадо-подписи вживую (покрыты юнитами,
  живьём не смотрены); заголовок «Оптимальные доли и их разброс (интервал 90%)».

### Отчёты
- [ ] PPTX живьём из окна: дека 12/13/14 по артефактам; слайды №6 витрина /
  №7 «Что изменилось» в секции «Главное»; строки калибровки и «сбылось/не
  сбылось» на «Данные и качество»; 0 wireframe-маркеров (канарейка B1).
- [ ] **HTML**: секция «Петля доверия» — 4 под-блока, пункт в TOC только при
  данных; тёмная/светлая тема (стили trust-* в layout.css свежие — глазами!).

### Прочее
- [ ] Vault на кириллическом пути (давний хвост E1-эпохи).
- [ ] Онбординг first-run: ровно ОДИН welcome-тур (П2), «вы»-форма (UX-3).

## После клик-листа — крупная задача на отдельное решение

**Полная «Валидация одним экраном»** (П1 полный, план в
`docs/audits/UX_AUDIT_2026_07.md`): сводка авто-решений + точечное
редактирование. Проектировать ПОСЛЕ живых наблюдений этого прогона.

## Ссылки

Реестры: `AUTONOMOUS_WORK_STATE_{E1_BACKTEST,E3_LIFECYCLE,E2_E4}.md` ·
отчёты `docs/audits/{E1_BACKTEST,UX_AUDIT,E3_LIFECYCLE,E2_E4}_2026_07.md` ·
мат-истина `docs/MATH_REFERENCE.md` §Trust loop · зонды `tmp/probe_*.py`
(переиспользуемы headless).
